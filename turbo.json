{
  "$schema": "https://turbo.build/schema.json",
  "//": "globalPassThroughEnv: turbo 2.x STRICT env-mode mặc định → task chỉ nhận env được khai báo. ci.yml chạy `turbo run test`; thiếu các biến này thì task KHÔNG thấy chúng dù tiến trình cha đã export. S6-SEC-ROTATE-1 (KI-043): fallback `changeme_*` ĐÃ BỊ GỠ, nên nay thiếu SUPERUSER_DB_PASSWORD/APP_DB_PASSWORD/WORKER_DB_PASSWORD là vitest CHẾT NGAY ở bước load config ('LANE_DB được set nhưng THIẾU …') ⇒ 0 test chạy, không phải đỏ-giả mà là KHÔNG CHẠY. FULL gate 2026-07-28 bắt đúng ca này: check.sh export 3 biến rồi gọi `pnpm test`, turbo strict nuốt SUPERUSER_DB_PASSWORD. Máy này che mất vì chunk-runner (KI-014) gọi vitest thẳng, bỏ qua turbo; Linux/macOS thì đỏ 100%. passThrough (KHÔNG cache-hash) đúng cho biến runtime/CI/secret. CHÚ Ý: turbo chỉ chấp nhận DUY NHẤT key comment `//` (key `//xxx` khác → 'unknown key' fail parse). PGBOUNCER_URL CỐ Ý KHÔNG passthrough: gate GX-4 pgbouncer-tenant-isolation chưa từng chạy trong ci.yml (env bị strip → skip); khi bật, 3 test đỏ vì edoburu/pgbouncer nối backend bằng user 'postgres' KHÔNG tồn tại (POSTGRES_USER=mediaos) — lỗi cấu hình pgbouncer-CI (follow-up GX-4-PGB-CI), giữ skip tới khi sửa.",
  "globalPassThroughEnv": [
    "DATABASE_URL",
    "DATABASE_DIRECT_URL",
    "DATABASE_WORKER_URL",
    "PG_HOSTPORT",
    "LANE_DB",
    "VALKEY_URL",
    "JWT_SECRET",
    "KMS_PROVIDER",
    "KMS_LOCAL_KEK_PATH",
    "WORKERS_SCHEDULER_ENABLED",
    "ALLOW_SUPERUSER_ROTATION",
    "TWO_FACTOR_ENFORCEMENT_ENABLED",
    "APP_DB_PASSWORD",
    "WORKER_DB_PASSWORD",
    "PGBOUNCER_AUTH_PASSWORD",
    "SUPERUSER_DB_PASSWORD",
    "OWNER_DB_PASSWORD"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
