/**
 * S15-PAYROLL-DEBT-1 — bản duy nhất của `useLocalPref` nằm ở `@mediaos/ui` (thêm ở S15-UI-SHELL-1). File này
 * chỉ re-export để các import `@/hooks/use-local-pref` sẵn có không phải đổi.
 */
export { useLocalPref } from "@mediaos/ui";
