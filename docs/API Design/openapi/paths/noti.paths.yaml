  # ==========================================================================
  # NOTI - My Notifications (NOTI-API-001..005)
  # ==========================================================================
  /api/v1/notifications:
    get:
      tags: [Notifications - My]
      summary: Lấy danh sách thông báo của tôi
      operationId: notiListMyNotifications
      x-required-permission: NOTI.NOTIFICATION.VIEW_OWN
      x-allowed-roles: []
      x-data-scope: [Own]
      x-idempotency: "No"
      x-audit-log: none
      x-notification-event: null
      parameters:
        - $ref: '#/components/parameters/CursorParam'
        - $ref: '#/components/parameters/LimitParam'
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SearchParam'
        - $ref: '#/components/parameters/SortParam'
        - { name: status, in: query, required: false, schema: { type: string, enum: [Unread, Read, Hidden, Archived] } }
        - { name: notification_type, in: query, required: false, schema: { type: string, enum: [System, Account, HR, Attendance, Leave, Task, Project, Approval, Reminder, Warning, Error] } }
        - { name: source_module, in: query, required: false, schema: { type: string } }
        - { name: event_code, in: query, required: false, schema: { type: string } }
        - { name: priority, in: query, required: false, schema: { type: string, enum: [Low, Normal, High, Urgent, Critical] } }
        - { name: created_from, in: query, required: false, schema: { type: string, format: date-time } }
        - { name: created_to, in: query, required: false, schema: { type: string, format: date-time } }
        - { name: include_archived, in: query, required: false, schema: { type: boolean } }
        - { name: include_hidden, in: query, required: false, schema: { type: boolean } }
      responses:
        '200': { $ref: '#/components/responses/SuccessList' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/notifications/dropdown:
    get:
      tags: [Notifications - My]
      summary: Lấy dropdown notification mới nhất
      operationId: notiGetDropdown
      x-required-permission: NOTI.NOTIFICATION.VIEW_OWN
      x-allowed-roles: []
      x-data-scope: [Own]
      x-idempotency: "No"
      x-audit-log: none
      x-notification-event: null
      parameters:
        - { name: limit, in: query, required: false, schema: { type: integer, default: 10, maximum: 20 } }
        - { name: unread_only, in: query, required: false, schema: { type: boolean } }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/notifications/unread-count:
    get:
      tags: [Notifications - My]
      summary: Đếm thông báo chưa đọc
      operationId: notiGetUnreadCount
      x-required-permission: NOTI.NOTIFICATION.COUNT_UNREAD_OWN
      x-allowed-roles: []
      x-data-scope: [Own]
      x-idempotency: "No"
      x-audit-log: none
      x-notification-event: null
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/notifications/{notification_id}:
    get:
      tags: [Notifications - My]
      summary: Xem chi tiết thông báo của tôi
      operationId: notiGetMyNotification
      x-required-permission: NOTI.NOTIFICATION.VIEW_DETAIL_OWN
      x-allowed-roles: []
      x-data-scope: [Own]
      x-idempotency: "No"
      x-audit-log: none
      x-notification-event: null
      parameters:
        - { name: notification_id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: auto_mark_read, in: query, required: false, schema: { type: boolean } }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    delete:
      tags: [Notifications - Actions]
      summary: Xóa mềm notification của tôi
      operationId: notiDeleteMyNotification
      x-required-permission: NOTI.NOTIFICATION.DELETE_OWN
      x-allowed-roles: []
      x-data-scope: [Own]
      x-idempotency: "No"
      x-audit-log: conditional
      x-notification-event: null
      parameters:
        - { name: notification_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        '204': { $ref: '#/components/responses/NoContent' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/notifications/{notification_id}/open-target:
    post:
      tags: [Notifications - My]
      summary: Lấy target info và mở nghiệp vụ liên quan
      operationId: notiOpenTarget
      x-required-permission: NOTI.NOTIFICATION.VIEW_DETAIL_OWN
      x-allowed-roles: []
      x-data-scope: [Own]
      x-idempotency: "No"
      x-audit-log: none
      x-notification-event: null
      parameters:
        - { name: notification_id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                mark_read: { type: boolean }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  # ==========================================================================
  # NOTI - Actions (NOTI-API-101..106)
  # ==========================================================================
  /api/v1/notifications/{notification_id}/mark-read:
    post:
      tags: [Notifications - Actions]
      summary: Đánh dấu một thông báo là đã đọc
      operationId: notiMarkRead
      x-required-permission: NOTI.NOTIFICATION.MARK_READ_OWN
      x-allowed-roles: []
      x-data-scope: [Own]
      x-idempotency: "Optional"
      x-audit-log: none
      x-notification-event: null
      parameters:
        - { name: notification_id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                read_at: { type: string, format: date-time, nullable: true }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/notifications/{notification_id}/mark-unread:
    post:
      tags: [Notifications - Actions]
      summary: Đánh dấu một thông báo là chưa đọc
      operationId: notiMarkUnread
      x-required-permission: NOTI.NOTIFICATION.MARK_READ_OWN
      x-allowed-roles: []
      x-data-scope: [Own]
      x-idempotency: "No"
      x-audit-log: none
      x-notification-event: null
      parameters:
        - { name: notification_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/notifications/mark-all-read:
    post:
      tags: [Notifications - Actions]
      summary: Đánh dấu tất cả thông báo là đã đọc
      operationId: notiMarkAllRead
      x-required-permission: NOTI.NOTIFICATION.MARK_ALL_READ_OWN
      x-allowed-roles: []
      x-data-scope: [Own]
      x-idempotency: "Optional"
      x-audit-log: none
      x-notification-event: null
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                source_module: { type: string, nullable: true }
                notification_type: { type: string, nullable: true }
                created_before: { type: string, format: date-time, nullable: true }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/notifications/{notification_id}/hide:
    post:
      tags: [Notifications - Actions]
      summary: Ẩn thông báo
      operationId: notiHide
      x-required-permission: NOTI.NOTIFICATION.HIDE_OWN
      x-allowed-roles: []
      x-data-scope: [Own]
      x-idempotency: "No"
      x-audit-log: none
      x-notification-event: null
      parameters:
        - { name: notification_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/notifications/{notification_id}/archive:
    post:
      tags: [Notifications - Actions]
      summary: Lưu trữ thông báo
      operationId: notiArchive
      x-required-permission: NOTI.NOTIFICATION.HIDE_OWN
      x-allowed-roles: []
      x-data-scope: [Own]
      x-idempotency: "No"
      x-audit-log: none
      x-notification-event: null
      parameters:
        - { name: notification_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  # ==========================================================================
  # NOTI - Admin (NOTI-API-201..204)
  # ==========================================================================
  /api/v1/notifications/admin/notifications:
    get:
      tags: [Notifications - Admin]
      summary: Danh sách notification toàn công ty
      operationId: notiAdminListNotifications
      x-required-permission: NOTI.NOTIFICATION.VIEW_COMPANY
      x-allowed-roles: [Super Admin, Admin]
      x-data-scope: [Company, System]
      x-idempotency: "No"
      x-audit-log: conditional
      x-notification-event: null
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SearchParam'
        - $ref: '#/components/parameters/SortParam'
        - { name: recipient_user_id, in: query, required: false, schema: { type: string, format: uuid } }
        - { name: recipient_employee_id, in: query, required: false, schema: { type: string, format: uuid } }
        - { name: source_module, in: query, required: false, schema: { type: string } }
        - { name: event_code, in: query, required: false, schema: { type: string } }
        - { name: status, in: query, required: false, schema: { type: string, enum: [Unread, Read, Hidden, Archived, Deleted] } }
        - { name: priority, in: query, required: false, schema: { type: string, enum: [Low, Normal, High, Urgent, Critical] } }
        - { name: batch_key, in: query, required: false, schema: { type: string } }
        - { name: dedupe_key, in: query, required: false, schema: { type: string } }
        - { name: created_from, in: query, required: false, schema: { type: string, format: date-time } }
        - { name: created_to, in: query, required: false, schema: { type: string, format: date-time } }
      responses:
        '200': { $ref: '#/components/responses/SuccessList' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/notifications/admin/notifications/{notification_id}:
    get:
      tags: [Notifications - Admin]
      summary: Chi tiết notification admin
      operationId: notiAdminGetNotification
      x-required-permission: NOTI.NOTIFICATION.VIEW_COMPANY
      x-allowed-roles: [Super Admin, Admin]
      x-data-scope: [Company, System]
      x-idempotency: "No"
      x-audit-log: required
      x-notification-event: null
      parameters:
        - { name: notification_id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: include_delivery_logs, in: query, required: false, schema: { type: boolean } }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/notifications/admin/system-notifications:
    post:
      tags: [Notifications - Admin]
      summary: Tạo thông báo hệ thống thủ công
      operationId: notiAdminCreateSystemNotification
      x-required-permission: NOTI.NOTIFICATION.CREATE_SYSTEM
      x-allowed-roles: [Super Admin, Admin]
      x-data-scope: [Company, System]
      x-idempotency: "Required"
      x-audit-log: required
      x-notification-event: null
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, content]
              properties:
                title: { type: string }
                content: { type: string }
                short_content: { type: string }
                notification_type: { type: string, enum: [System, Account, HR, Attendance, Leave, Task, Project, Approval, Reminder, Warning, Error] }
                priority: { type: string, enum: [Low, Normal, High, Urgent, Critical] }
                channels: { type: array, items: { type: string, enum: [IN_APP, EMAIL, PUSH, REALTIME, INTEGRATION] } }
                recipient_mode: { type: string, enum: [User, Employee, Role, Department, Company] }
                recipient_role_codes: { type: array, items: { type: string } }
                recipient_user_ids: { type: array, items: { type: string, format: uuid } }
                recipient_employee_ids: { type: array, items: { type: string, format: uuid } }
                target_url: { type: string, nullable: true }
                send_now: { type: boolean }
                scheduled_at: { type: string, format: date-time, nullable: true }
                expires_at: { type: string, format: date-time, nullable: true }
      responses:
        '201': { $ref: '#/components/responses/Created' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/notifications/admin/system-notifications/{draft_id}/send:
    post:
      tags: [Notifications - Admin]
      summary: Gửi thông báo hệ thống thủ công
      operationId: notiAdminSendSystemNotification
      x-required-permission: NOTI.NOTIFICATION.SEND_SYSTEM
      x-allowed-roles: [Super Admin, Admin]
      x-data-scope: [Company, System]
      x-idempotency: "Required"
      x-audit-log: required
      x-notification-event: null
      parameters:
        - { name: draft_id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [confirm]
              properties:
                confirm: { type: boolean }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  # ==========================================================================
  # NOTI - Events (NOTI-API-301..302)
  # ==========================================================================
  /api/v1/notifications/events:
    get:
      tags: [Notifications - Events]
      summary: Danh sách notification events
      operationId: notiListEvents
      x-required-permission: NOTI.EVENT.VIEW
      x-allowed-roles: [Super Admin, Admin, HR]
      x-data-scope: [Company, System]
      x-idempotency: "No"
      x-audit-log: none
      x-notification-event: null
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SearchParam'
        - $ref: '#/components/parameters/SortParam'
        - { name: module_code, in: query, required: false, schema: { type: string } }
        - { name: event_code, in: query, required: false, schema: { type: string } }
        - { name: enabled, in: query, required: false, schema: { type: boolean } }
      responses:
        '200': { $ref: '#/components/responses/SuccessList' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/notifications/events/{event_id}:
    get:
      tags: [Notifications - Events]
      summary: Chi tiết event
      operationId: notiGetEvent
      x-required-permission: NOTI.EVENT.VIEW
      x-allowed-roles: [Super Admin, Admin, HR]
      x-data-scope: [Company, System]
      x-idempotency: "No"
      x-audit-log: none
      x-notification-event: null
      parameters:
        - { name: event_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    patch:
      tags: [Notifications - Events]
      summary: Cập nhật cấu hình event
      operationId: notiUpdateEvent
      x-required-permission: NOTI.EVENT.CONFIG
      x-allowed-roles: [Super Admin, Admin, HR]
      x-data-scope: [Company, System]
      x-idempotency: "Optional"
      x-audit-log: required
      x-notification-event: null
      parameters:
        - { name: event_id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled: { type: boolean }
                default_priority: { type: string, enum: [Low, Normal, High, Urgent, Critical] }
                dedupe_enabled: { type: boolean }
                dedupe_window_seconds: { type: integer, minimum: 0 }
                enabled_channels: { type: array, items: { type: string, enum: [IN_APP, EMAIL, PUSH, REALTIME, INTEGRATION] } }
                description: { type: string }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  # ==========================================================================
  # NOTI - Templates (NOTI-API-303..306)
  # ==========================================================================
  /api/v1/notifications/templates:
    get:
      tags: [Notifications - Templates]
      summary: Danh sách notification templates
      operationId: notiListTemplates
      x-required-permission: NOTI.TEMPLATE.VIEW
      x-allowed-roles: [Super Admin, Admin, HR]
      x-data-scope: [Company, System]
      x-idempotency: "No"
      x-audit-log: none
      x-notification-event: null
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SearchParam'
        - $ref: '#/components/parameters/SortParam'
        - { name: event_code, in: query, required: false, schema: { type: string } }
        - { name: module_code, in: query, required: false, schema: { type: string } }
        - { name: channel, in: query, required: false, schema: { type: string, enum: [IN_APP, EMAIL, PUSH, REALTIME] } }
        - { name: locale, in: query, required: false, schema: { type: string } }
        - { name: status, in: query, required: false, schema: { type: string, enum: [Active, Inactive, Draft, Deprecated] } }
        - { name: company_override, in: query, required: false, schema: { type: boolean } }
      responses:
        '200': { $ref: '#/components/responses/SuccessList' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [Notifications - Templates]
      summary: Tạo notification template
      operationId: notiCreateTemplate
      x-required-permission: [NOTI.TEMPLATE.CREATE, NOTI.TEMPLATE.UPDATE]
      x-permission-mode: anyOf
      x-allowed-roles: [Super Admin, Admin, HR]
      x-data-scope: [Company, System]
      x-idempotency: "Optional"
      x-audit-log: required
      x-notification-event: null
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [event_code, channel, title_template, content_template]
              properties:
                event_code: { type: string }
                channel: { type: string, enum: [IN_APP, EMAIL, PUSH, REALTIME] }
                locale: { type: string }
                template_code: { type: string }
                title_template: { type: string }
                short_template: { type: string }
                content_template: { type: string }
                variables_schema:
                  type: object
                  properties:
                    required: { type: array, items: { type: string } }
                    optional: { type: array, items: { type: string } }
                status: { type: string, enum: [Active, Inactive, Draft, Deprecated] }
                is_default: { type: boolean }
      responses:
        '201': { $ref: '#/components/responses/Created' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/notifications/templates/{template_id}:
    get:
      tags: [Notifications - Templates]
      summary: Chi tiết template
      operationId: notiGetTemplate
      x-required-permission: NOTI.TEMPLATE.VIEW
      x-allowed-roles: [Super Admin, Admin, HR]
      x-data-scope: [Company, System]
      x-idempotency: "No"
      x-audit-log: none
      x-notification-event: null
      parameters:
        - { name: template_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    patch:
      tags: [Notifications - Templates]
      summary: Cập nhật notification template
      operationId: notiUpdateTemplate
      x-required-permission: NOTI.TEMPLATE.UPDATE
      x-allowed-roles: [Super Admin, Admin, HR]
      x-data-scope: [Company, System]
      x-idempotency: "Optional"
      x-audit-log: required
      x-notification-event: null
      parameters:
        - { name: template_id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title_template: { type: string }
                short_template: { type: string }
                content_template: { type: string }
                variables_schema:
                  type: object
                  properties:
                    required: { type: array, items: { type: string } }
                    optional: { type: array, items: { type: string } }
                status: { type: string, enum: [Active, Inactive, Draft, Deprecated] }
                is_default: { type: boolean }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }
    delete:
      tags: [Notifications - Templates]
      summary: Xóa mềm notification template
      operationId: notiDeleteTemplate
      x-required-permission: NOTI.TEMPLATE.DELETE
      x-allowed-roles: [Super Admin, Admin, HR]
      x-data-scope: [Company, System]
      x-idempotency: "No"
      x-audit-log: required
      x-notification-event: null
      parameters:
        - { name: template_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        '204': { $ref: '#/components/responses/NoContent' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /api/v1/notifications/templates/{template_id}/preview:
    post:
      tags: [Notifications - Templates]
      summary: Preview render template
      operationId: notiPreviewTemplate
      x-required-permission: NOTI.TEMPLATE.VIEW
      x-allowed-roles: [Super Admin, Admin, HR]
      x-data-scope: [Company, System]
      x-idempotency: "No"
      x-audit-log: none
      x-notification-event: null
      parameters:
        - { name: template_id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                variables: { type: object, additionalProperties: true }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  # ==========================================================================
  # NOTI - Channels (NOTI-API-307..308)
  # ==========================================================================
  /api/v1/notifications/channels:
    get:
      tags: [Notifications - Channels]
      summary: Danh sách/cấu hình kênh gửi
      operationId: notiListChannels
      x-required-permission: NOTI.CHANNEL.VIEW
      x-allowed-roles: [Super Admin, Admin]
      x-data-scope: [Company, System]
      x-idempotency: "No"
      x-audit-log: none
      x-notification-event: null
      responses:
        '200': { $ref: '#/components/responses/SuccessList' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/notifications/channels/{channel_code}:
    patch:
      tags: [Notifications - Channels]
      summary: Cập nhật cấu hình kênh
      operationId: notiUpdateChannel
      x-required-permission: NOTI.CHANNEL.UPDATE
      x-allowed-roles: [Super Admin, Admin]
      x-data-scope: [Company, System]
      x-idempotency: "Optional"
      x-audit-log: required
      x-notification-event: null
      parameters:
        - { name: channel_code, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled: { type: boolean }
                provider: { type: string }
                settings: { type: object, additionalProperties: true }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  # ==========================================================================
  # NOTI - Delivery Logs (NOTI-API-401..403)
  # ==========================================================================
  /api/v1/notifications/delivery-logs:
    get:
      tags: [Notifications - Delivery Logs]
      summary: Danh sách delivery logs
      operationId: notiListDeliveryLogs
      x-required-permission: NOTI.LOG.VIEW
      x-allowed-roles: [Super Admin, Admin]
      x-data-scope: [Company, System]
      x-idempotency: "No"
      x-audit-log: none
      x-notification-event: null
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SortParam'
        - { name: notification_id, in: query, required: false, schema: { type: string, format: uuid } }
        - { name: recipient_user_id, in: query, required: false, schema: { type: string, format: uuid } }
        - { name: channel, in: query, required: false, schema: { type: string, enum: [IN_APP, EMAIL, PUSH, REALTIME, INTEGRATION] } }
        - { name: delivery_status, in: query, required: false, schema: { type: string, enum: [Pending, Sent, Delivered, Failed, Skipped, Cancelled] } }
        - { name: provider, in: query, required: false, schema: { type: string } }
        - { name: created_from, in: query, required: false, schema: { type: string, format: date-time } }
        - { name: created_to, in: query, required: false, schema: { type: string, format: date-time } }
        - { name: next_retry_before, in: query, required: false, schema: { type: string, format: date-time } }
      responses:
        '200': { $ref: '#/components/responses/SuccessList' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/notifications/delivery-logs/{log_id}:
    get:
      tags: [Notifications - Delivery Logs]
      summary: Chi tiết delivery log
      operationId: notiGetDeliveryLog
      x-required-permission: NOTI.LOG.VIEW
      x-allowed-roles: [Super Admin, Admin]
      x-data-scope: [Company, System]
      x-idempotency: "No"
      x-audit-log: conditional
      x-notification-event: null
      parameters:
        - { name: log_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/notifications/delivery-logs/{log_id}/retry:
    post:
      tags: [Notifications - Delivery Logs]
      summary: Retry delivery log thất bại
      operationId: notiRetryDeliveryLog
      x-required-permission: NOTI.LOG.RETRY
      x-allowed-roles: [Super Admin, Admin]
      x-data-scope: [Company, System]
      x-idempotency: "Required"
      x-audit-log: required
      x-notification-event: null
      parameters:
        - { name: log_id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                reason: { type: string }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  # ==========================================================================
  # NOTI - Internal (INTERNAL-NOTI-API-001..006)
  # ==========================================================================
  /internal/v1/notifications/events:
    post:
      tags: [Notifications - Internal]
      summary: Nhận event nghiệp vụ từ module khác
      operationId: notiInternalReceiveEvent
      security: [{ internalServiceAuth: [] }]
      x-required-permission: null
      x-allowed-roles: []
      x-data-scope: [System]
      x-idempotency: "Required"
      x-audit-log: conditional
      x-notification-event: null
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [event_code]
              properties:
                event_code: { type: string }
                dedupe_key: { type: string }
                payload: { type: object, additionalProperties: true }
      responses:
        '202': { $ref: '#/components/responses/Accepted' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /internal/v1/notifications/send:
    post:
      tags: [Notifications - Internal]
      summary: Tạo/gửi notification trực tiếp
      operationId: notiInternalSend
      security: [{ internalServiceAuth: [] }]
      x-required-permission: null
      x-allowed-roles: []
      x-data-scope: [System]
      x-idempotency: "Required"
      x-audit-log: conditional
      x-notification-event: null
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [recipient_user_id, title, content]
              properties:
                company_id: { type: string, format: uuid }
                recipient_user_id: { type: string, format: uuid }
                title: { type: string }
                content: { type: string }
                short_content: { type: string }
                notification_type: { type: string }
                priority: { type: string, enum: [Low, Normal, High, Urgent, Critical] }
                source_module: { type: string }
                event_code: { type: string }
                target_module: { type: string, nullable: true }
                target_type: { type: string, nullable: true }
                target_id: { type: string, nullable: true }
                target_url: { type: string, nullable: true }
                payload: { type: object, additionalProperties: true }
                channels: { type: array, items: { type: string } }
                dedupe_key: { type: string }
      responses:
        '200': { $ref: '#/components/responses/SuccessObject' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /internal/v1/notifications/bulk-send:
    post:
      tags: [Notifications - Internal]
      summary: Gửi bulk notification
      operationId: notiInternalBulkSend
      security: [{ internalServiceAuth: [] }]
      x-required-permission: null
      x-allowed-roles: []
      x-data-scope: [System]
      x-idempotency: "Required"
      x-audit-log: conditional
      x-notification-event: null
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [event_code, recipients]
              properties:
                company_id: { type: string, format: uuid }
                event_code: { type: string }
                recipients: { type: array, items: { type: object, additionalProperties: true } }
                batch_key: { type: string }
                dedupe_key: { type: string }
                channels: { type: array, items: { type: string } }
                payload: { type: object, additionalProperties: true }
      responses:
        '202': { $ref: '#/components/responses/Accepted' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /internal/v1/notifications/reminder-jobs/run:
    post:
      tags: [Notifications - Internal]
      summary: Chạy reminder job
      operationId: notiInternalRunReminderJob
      security: [{ internalServiceAuth: [] }]
      x-required-permission: null
      x-allowed-roles: []
      x-data-scope: [System]
      x-idempotency: "Required"
      x-audit-log: conditional
      x-notification-event: null
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [job_type]
              properties:
                job_type: { type: string }
                dry_run: { type: boolean }
                limit: { type: integer }
      responses:
        '202': { $ref: '#/components/responses/Accepted' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /internal/v1/notifications/delivery-jobs/retry:
    post:
      tags: [Notifications - Internal]
      summary: Retry delivery job
      operationId: notiInternalRetryDeliveryJob
      security: [{ internalServiceAuth: [] }]
      x-required-permission: null
      x-allowed-roles: []
      x-data-scope: [System]
      x-idempotency: "Required"
      x-audit-log: conditional
      x-notification-event: null
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                batch_size: { type: integer }
                max_attempts: { type: integer }
      responses:
        '202': { $ref: '#/components/responses/Accepted' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }

  /internal/v1/notifications/cleanup-jobs/run:
    post:
      tags: [Notifications - Internal]
      summary: Cleanup/expire notification theo retention
      operationId: notiInternalRunCleanupJob
      security: [{ internalServiceAuth: [] }]
      x-required-permission: null
      x-allowed-roles: []
      x-data-scope: [System]
      x-idempotency: "Required"
      x-audit-log: conditional
      x-notification-event: null
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                dry_run: { type: boolean }
                retention_days: { type: integer }
      responses:
        '202': { $ref: '#/components/responses/Accepted' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }
