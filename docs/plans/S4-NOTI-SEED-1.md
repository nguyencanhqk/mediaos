```yaml
wo: S4-NOTI-SEED-1
zone: red
generated_by: auto-loop
reconciled_at: "dbd43b3"
lanes: [{"id":"notiSeedMig","task":"Migration 0481 (db-migration, NỐI TIẾP sau 0480 idx160) — THUẦN ADDITIVE DATA (KHÔNG DDL, KHÔNG db:generate, mirror 0480). Chạy qua migrator (owner rolbypassrls=true → ghi được global company_id NULL, mẫu 0435). (1) INSERT notification_events GLOBAL (company_id NULL) = UNION SPEC-08 §15.1-15.6 + DB-07 §14.1: MVP set (DB-07 §14.1) is_enabled=true, event ngoài MVP is_enabled=false (GIỮ trong catalog). event_code verbatim (TASK_MENTIONED, TASK_COMMENT_CREATED). ON CONFLICT(event_code) WHERE company_id IS NULL AND deleted_at IS NULL DO NOTHING. (2) INSERT notification_templates IN_APP vi-VN cho MỌI event is_enabled=true: title_template + body_template(NOT NULL, set tường minh) + short_body_template + variables_schema; channel='IN_APP', locale='vi-VN', status='Active', is_default=true; ON CONFLICT(template_code) WHERE company_id IS NULL DO NOTHING. (3) INSERT 6 cặp permissions NOTI config is_sensitive=true: (view,notification-config)(update,notification-config)(view,notification-template)(update,notification-template)(view,notification-delivery-log)(view,notification-audit-log) — ON CONFLICT(action,resource_type) DO NOTHING; KHÔNG cặp 'channel'. (4) DO-block grant: company-admin @Company cho 6 cặp config; DO-block grant read:notification @Own cho employee/manager/hr/company-admin. Cả hai enumerate role slug TƯỜNG MINH + RAISE EXCEPTION fail-LOUD nếu role thiếu (mirror 0480:91-93); super-admin KHÔNG enumerate (runtime bootstrap company-scoped). Append entry _journal.json idx161 when≥1717500800000.","builder":"db-migration","paths":["apps/api/migrations/0481_s4_notiseed1_event_template_perms.sql","apps/api/migrations/meta/_journal.json"]},{"id":"notiSeedRegistryVerify","task":"Registry const + int-spec (backend-builder, depends notiSeedMig). (A) Tạo const nguồn-sự-thật apps/api/src/foundation/seed/notification-event-catalog.const.ts: liệt kê nguyên văn từng event_code + is_enabled + module + type + priority (UNION SPEC-08 §15/DB-07 §14.1) + 7 cặp NOTI (read:notification + 6 config) với is_sensitive + ma trận role→scope. (B) int-spec apps/api/test/integration/noti-seed-catalog-permissions.int-spec.ts gate hasDb && LANE_DB (mẫu att-permissions-seed.int.spec.ts): catalog==const (không mã lạ/thiếu); template coverage (mỗi enabled event có 1 template IN_APP vi-VN Active default, body_template non-null, locale='vi-VN'); 6 cặp config is_sensitive=true tồn tại + KHÔNG 'channel'; POSITIVE grant qua role_permissions SQL — company-admin có 6 cặp config @Company + mỗi role employee/manager/hr/company-admin có read:notification @Own; DENY-path — HR 0 cặp config, employee 0 cặp config; idempotent triple re-apply không drift scope/count.","builder":"backend-builder","paths":["apps/api/src/foundation/seed/notification-event-catalog.const.ts","apps/api/test/integration/noti-seed-catalog-permissions.int-spec.ts"]}]
acceptanceChecks: ["Migration 0481: journal idx161, when≥1717500800000, nối tiếp đơn điệu sau 0480 idx160 (đã xác minh head THẬT). THUẦN ADDITIVE DATA — KHÔNG DDL, KHÔNG đụng RLS/FORCE/policy/grant của 0479, KHÔNG db:generate (mirror 0480).","notification_events: mọi event_code UNION SPEC-08 §15.1-15.6 + DB-07 §14.1 hiện diện dạng GLOBAL (company_id NULL). is_enabled=true cho tập MVP DB-07 §14.1 (~34 mã), is_enabled=false cho phần dư SPEC-08 §15 (AUTH_PASSWORD_CHANGED, AUTH_USER_UNLOCKED, HR_PROBATION_ENDING, HR_EMPLOYEE_STATUS_CHANGED, ATT_CHECKIN_REMINDER, ATT_CHECKOUT_REMINDER, LEAVE_START_REMINDER, TASK_UPDATED, TASK_ASSIGNEE_CHANGED, TASK_DEADLINE_CHANGED, PROJECT_CLOSED, DASH_WIDGET_ERROR, SYSTEM_* §15.6 …). event_code verbatim: TASK_MENTIONED + TASK_COMMENT_CREATED (KHÔNG TASK_COMMENT_MENTIONED). ON CONFLICT DO NOTHING.","notification_templates: mỗi event is_enabled=true có ĐÚNG 1 template channel='IN_APP', locale='vi-VN' (KHÔNG 'vi'), status='Active', is_default=true, body_template NOT NULL set tường minh, có title_template + short_body_template + variables_schema. ON CONFLICT(template_code) WHERE company_id IS NULL DO NOTHING.","permissions: 6 cặp NOTI config seeded is_sensitive=true = (view,notification-config)(update,notification-config)(view,notification-template)(update,notification-template)(view,notification-delivery-log)(view,notification-audit-log). read:notification giữ nguyên non-sensitive. KHÔNG cặp 'channel' (phantom — DB-02 §9.7 = 0 kết quả).","role_permissions: company-admin có ĐỦ 6 cặp config @Company; HR có 0/6 cặp config; employee/manager có 0/6 cặp config. employee+manager+hr+company-admin đều có read:notification @Own. super-admin KHÔNG enumerate trong DO-block (nhận qua SuperAdminBootstrap runtime — company-scoped listAllPermissions).","DO-block grant enumerate role slug TƯỜNG MINH (config=['company-admin']; own=['employee','manager','hr','company-admin']) + RAISE EXCEPTION fail-LOUD khi role/permission thiếu (mirror 0480:91-93) — KHÔNG CONTINUE im lặng ở nhánh GRANT.","Idempotent: chạy migrate lại 3× = no-op (ON CONFLICT DO NOTHING + DELETE-wrong-scope per-pair) — count events/templates/permissions/grants + data_scope KHÔNG đổi.","int-spec gate cứng hasDb && LANE_DB (chỉ .env → hasDb=true = đỏ-giả), colocated test-glob (test/**/*.int-spec.ts chạy — đã xác minh vitest include). RED trên DB migrate tới 0480, GREEN sau 0481.","FULL gate PASS: security-reviewer + database-reviewer. DoD §8: backlog cập nhật, không phá luồng chính, seed qua migration (data) — không cần audit-log riêng."]
testTasks: ["Catalog match (không mã lạ): SELECT event_code FROM notification_events WHERE company_id IS NULL == const registry (UNION SPEC-08 §15 + DB-07 §14.1); assert set bằng nhau (thiếu-mã ĐỎ, thừa-mã ĐỎ); assert is_enabled đúng cho từng mã (MVP=true, còn lại=false).","Template coverage: mỗi event is_enabled=true có ĐÚNG 1 row notification_templates channel='IN_APP' AND locale='vi-VN' AND status='Active' AND is_default=true; body_template IS NOT NULL AND length>0; event is_enabled=false KHÔNG có template (hoặc bỏ qua).","Permission catalog: 6 cặp config tồn tại với is_sensitive=true (query permissions); assert KHÔNG tồn tại cặp resource_type='channel' hay 'notification-channel'.","POSITIVE grant (BẮT BUỘC — deny-path một mình không bắt được grant-0-row): qua role_permissions JOIN roles/permissions — company-admin có data_scope='Company' cho MỖI cặp config; MỖI role employee/manager/hr/company-admin có read:notification data_scope='Own'. (super-admin: bootstrap runtime rồi assert catalog-coverage, HOẶC ghi chú kiểm ở tầng bootstrap — company-scoped role KHÔNG nằm ở migration seed).","DENY-path RED (permission/least-privilege): HR có 0 grant trong (notification-config, notification-template, notification-delivery-log, notification-audit-log); employee + manager cũng 0 grant các cặp config. Assert COUNT(*)=0.","Idempotency: snapshot before → re-apply INSERT ON CONFLICT + grant DO-block 3× (mô phỏng migrator re-run) → snapshot after == before (count + data_scope không drift), mẫu att-permissions-seed §D.","(GHI CHÚ verify Đội 3) /auth/me surface cặp config = KHÔNG kiểm ở WO này: getCapabilities lọc BỎ mọi cặp sensitive ⇒ config (sensitive) không xuất hiện trừ khi append SENSITIVE_CAPABILITY_ALLOWLIST (permission.service.ts — NGOÀI paths). WO này assert grant ở tầng role_permissions; surface /auth/me là WO FE/allowlist kế tiếp."]
steps: ["ĐỌC _journal.json (đã xác minh): head THẬT = idx160 0480_s4_taskrecon1 when1717500795000 → tạo 0481 idx161 when≥1717500800000, tag 0481_s4_notiseed1_event_template_perms. KHÔNG hard-code, append đơn điệu.","(Lane notiSeedRegistryVerify — RED trước) Viết notification-event-catalog.const.ts liệt kê nguyên văn UNION event_code + is_enabled + 7 cặp NOTI + ma trận role→scope; viết int-spec assert (catalog/template/pair/grant/deny/idempotent). Chạy trên DB migrate tới 0480 → ĐỎ (thiếu event/template/pair/grant).","(Lane notiSeedMig) Viết 0481 phần (1) events GLOBAL: INSERT company_id NULL cho UNION SPEC-08 §15 + DB-07 §14.1; is_enabled=true cho MVP (DB-07 §14.1), false cho phần dư SPEC-08 §15; ON CONFLICT(event_code) WHERE company_id IS NULL DO NOTHING. Reconcile drift SYSTEM_* (DB-07 §14.1 dùng SYSTEM_CONFIG_WARNING/SYSTEM_ERROR_DETECTED; SPEC-08 §15.6 dùng SYSTEM_CONFIG_CHANGED/…/SYSTEM_JOB_FAILED) — union cả hai, chốt tập enabled theo DB-07 §14.1.","(Lane notiSeedMig) Phần (2) templates: resolve event_id qua subquery (SELECT id FROM notification_events WHERE event_code=… AND company_id IS NULL); INSERT IN_APP vi-VN cho MỌI event enabled=true; body_template NOT NULL set tường minh; locale='vi-VN'; status='Active'; is_default=true; nội dung lấy DB-07 §14.2 + cột 'Nội dung gợi ý' SPEC-08 §15; variables_schema jsonb {task_code,employee_name,…}. ON CONFLICT(template_code) WHERE company_id IS NULL DO NOTHING.","(Lane notiSeedMig) Phần (3) catalog quyền: INSERT 6 cặp config is_sensitive=true; ON CONFLICT(action,resource_type) DO NOTHING. Xác nhận read:notification đã có (0005) — không thêm lại.","(Lane notiSeedMig) Phần (4) grant DO-block kiểu 0480: array config = ['company-admin'] @Company (RAISE EXCEPTION nếu role/perm thiếu); array own = ['employee','manager','hr','company-admin'] read:notification @Own (DELETE-wrong-scope per-pair + INSERT ON CONFLICT DO NOTHING, RAISE EXCEPTION fail-loud). super-admin KHÔNG có trong array (runtime bootstrap phủ catalog). RAISE NOTICE tổng kết.","Chạy DB cô lập: bash scripts/lane-db-setup.sh notiseed → export LANE_DB=mediaos_notiseed → pnpm --filter @mediaos/api test → int-spec GREEN. Chạy migrate lần 2 xác nhận idempotent (no-op, 0 drift).","FULL gate: security-reviewer + database-reviewer. Cập nhật harness/backlog.mjs. Ghi reconcileNotes vào docs/plans/S4-NOTI-SEED-1.md để tái dùng."]
```

GAP-ANALYSIS (đã đối chiếu code hiện tại):
1) SEED QUA MIGRATION, KHÔNG QUA app role — notification_events/templates (0479) có company_id NULLABLE, RLS+FORCE, GRANT app SELECT-only; global rows company_id NULL cần table-owner. Đã xác minh migrator (DATABASE_DIRECT_URL) chạy role owner có rolbypassrls=true (apps/api/src/db/worker-role.spec.ts:14 + mẫu 0435 "admin System qua table-owner (bypass FORCE)") ⇒ INSERT company_id NULL chạy TRỰC TIẾP, KHÔNG cần SET LOCAL/GUC. WITH CHECK(company_id=GUC) chỉ chặn app role, không chặn owner-bypass. foundation/seed/** trong paths = nơi đặt const registry TS (nguồn-expected cho test), KHÔNG phải runtime app-role seeder (app-role không INSERT được global).

2) super-admin KHÔNG có trong DO-block GRANT (INVARIANT quan trọng, tránh reviewer báo thiếu): role 'super-admin' là COMPANY-SCOPED, seed RUNTIME qua SuperAdminBootstrapService (apps/api/src/permission/super-admin-bootstrap.repository.ts) — mỗi boot phủ TOÀN BỘ catalog (listAllPermissions), KHÔNG có row roles company_id IS NULL. Nếu enumerate 'super-admin' trong DO-block resolve (company_id IS NULL) → NULL → RAISE EXCEPTION vỡ migration. ⇒ config grant chỉ ['company-admin']; super-admin nhận 6 cặp config tự động ở boot kế (catalog-coverage). Owner chốt "company-admin + super-admin có config" thoả qua: migration cấp company-admin + runtime cấp super-admin.

3) SENSITIVE-CAP /auth/me trap: getCapabilities() (permission.service.ts:280) CỐ Ý filter `!g.isSensitive` ⇒ 6 cặp config (is_sensitive=true theo DB-02 §9.7) KHÔNG xuất hiện trong /auth/me capabilities. Muốn surface làm UI-hint phải APPEND vào SENSITIVE_CAPABILITY_ALLOWLIST (permission.service.ts:43) — file NGOÀI paths WO ⇒ OUT-OF-SCOPE. done_when "admin thấy cặp config qua /auth/me" chỉ đạt đủ sau WO FE-registry/allowlist kế tiếp. WO NÀY assert grant ở role_permissions (đúng nguồn sự thật, không đỏ-giả). Đội 3 verify theo đó.

4) PIN CẶP (chống pair-drift) — 3 nơi phải khớp: seed THẬT (mig 0481) ↔ FE registry.ts PERMISSION_CODE_TO_PAIR ↔ NOTI-BE @RequirePermission. Convention lowercase module-prefixed hyphenated (mẫu 'attendance-rule','foundation-setting','employee-code-config'): NOTI.CONFIG.VIEW→view:notification-config · NOTI.CONFIG.UPDATE→update:notification-config · NOTI.TEMPLATE.VIEW→view:notification-template · NOTI.TEMPLATE.UPDATE→update:notification-template · NOTI.DELIVERY_LOG.VIEW→view:notification-delivery-log · NOTI.AUDIT_LOG.VIEW→view:notification-audit-log. own-notification = read:notification (đã dùng bởi FE registry.ts:139 + 0005:432 + notifications.controller own-scoped không @RequirePermission). FE registry.ts + NOTI-BE controller NGOÀI paths ⇒ ghi 7 cặp này vào plan/const làm source-of-truth để WO FE-REGISTRY/NOTI-BE mirror. LƯU Ý: notifications.controller.ts hiện KHÔNG có @RequirePermission (own-scope theo user context) — cặp config sẽ do NOTI-BE config controller tương lai enforce.

5) DRIFT DB-07 vs SPEC-08 SYSTEM_*: DB-07 §14.1 seed dùng SYSTEM_CONFIG_WARNING + SYSTEM_ERROR_DETECTED (enabled); SPEC-08 §15.6 dùng bộ khác (DASH_WIDGET_ERROR, SYSTEM_CONFIG_CHANGED, SYSTEM_MAINTENANCE_NOTICE, SYSTEM_IMPORT_FAILED, SYSTEM_JOB_FAILED). KHÔNG trùng. Seed UNION cả hai; tập enabled=DB-07 §14.1 (nguồn seed chuẩn + CLAUDE.md docs/DB thắng); phần SPEC-08 §15.6 = catalog is_enabled=false. Ghi rõ để reviewer đối chiếu — KHÔNG bỏ mã nào.

6) NAMING/HEAD: mig tag 0481_s4_notiseed1_event_template_perms; idx161; when≥1717500800000; ĐỌC _journal.json trước (đã xác minh head 0480 idx160/1717500795000).

INVARIANTS (CLAUDE.md §2/§3): #1 RLS+FORCE + policy đã tạo ở 0479 TRƯỚC seed; 0481 chỉ INSERT data (không đụng cô lập). #2 append-only: notification_delivery_logs không liên quan seed này; notification_events/templates là danh mục (không append-only) nhưng seed qua owner. #3 không secret plaintext — template body_template KHÔNG chứa dữ liệu nhạy cảm (DB-07 §4.6: payload/nội dung nhạy cảm ép ở tầng service, không ở seed).

VERIFY/GATE: DB cô lập LANE_DB (scripts/lane-db-setup.sh) — RED trên 0480, GREEN sau 0481; idempotent 3×. Gate FULL (permission+seed+migration): security-reviewer + database-reviewer + silent-failure-hunter (nếu có). Model: crown → planner Sonnet5 xhigh + plan-reviewer, IMPLEMENT/REVIEW Opus.

OUT-OF-SCOPE (chống scope-creep): (a) SENSITIVE_CAPABILITY_ALLOWLIST append (permission.service.ts) → WO FE/allowlist. (b) FE registry.ts PERMISSION_CODE_TO_PAIR NOTI config entries → S4-FE-REGISTRY. (c) NOTI-BE config/template controller + @RequirePermission → S4-NOTI-BE. (d) notification producer (TASK phát TASK_MENTIONED/TASK_COMMENT_CREATED) → S4-TASK/NOTI-BE. (e) company-override write cho events/templates → NOTI-BE-3. (f) EMAIL/PUSH template + non-vi locale.
</content>
