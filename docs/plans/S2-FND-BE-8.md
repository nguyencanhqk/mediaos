```yaml
wo: S2-FND-BE-8
zone: red
generated_by: auto-loop
reconciled_at: "4d3befb"
lanes: [{"id":"mig-audit-module","task":"MIGRATION NỐI TIẾP (chạy TRƯỚC lane be-module-toggle). Tạo migration 0474 UNION-add object_type 'module' vào CHECK audit_logs_object_type_chk (DO-block idempotent clone 0468 — append-only #2, KHÔNG rewrite/DROP, KHÔNG đụng RLS/grant/FORCE) + sync 'module' vào AUDIT_OBJECT_TYPES trong schema/audit.ts CÙNG commit. VERIFY (không thêm lại): 'system_setting' đã có trong CHECK 0439 (v_new=['company_setting','system_setting']) và 'retention_policy' trong 0456. Journal idx nối tiếp head 0473; KHÔNG db:generate (DO-block thủ công). Chain 0000→0474 xanh trên LANE_DB; INSERT audit object_type='module' KHÔNG vỡ CHECK.","builder":"db-migration","paths":["apps/api/migrations/**","apps/api/src/db/schema/audit.ts"]},{"id":"be-system-settings","task":"GET /foundation/system-settings(/:key) + PATCH /foundation/system-settings/:key. Gate system-manage:foundation-setting (0435:343, is_sensitive=true, System-scope). PATCH: validate value_type/validation_schema ĐỌC TỪ hàng system_settings (KHÔNG từ company override) — sai type 400, sai schema 422; ghi qua updateSystemTx/insertSystemTx MỚI trong setting.repository (RIÊNG system_settings, KHÔNG ghi company_settings); audit action='SYSTEM_SETTING_UPDATED' object_type='system_setting' — BẮT BUỘC db.withTenant(actor.companyId) bọc CẢ mutation lẫn audit trong CÙNG tx (KHÔNG withTransaction) để audit_logs.company_id = actor.companyId. Mask sensitive khi đọc y hệt company path (setting-mask, drop secret_ref). DTO cục bộ trong settings.dto.ts (mẫu PatchCompanySettingDto — KHÔNG sửa packages/contracts/settings.ts). KHÔNG viết test 2-tenant isolation cho system-setting (global no-RLS — mọi tenant chia sẻ 1 hàng).","builder":"backend-builder","paths":["apps/api/src/foundation/settings/**","apps/api/test/foundation/system-settings-permission-deny.int-spec.ts","apps/api/test/foundation/system-settings.int-spec.ts"]},{"id":"be-module-toggle","task":"PATCH /foundation/modules/:code (enable/disable). Gate update:foundation-module (0435:339, is_sensitive=true). Ghi qua company_settings key 'module.<code>.enabled' (bảng TENANT-SCOPED, RLS+FORCE) bằng repo-method RIÊNG upsert trong module-catalog.repository (import companySettings từ db/schema/settings; KHÔNG export SettingRepository, KHÔNG sửa settings.module.ts) + audit CONFIG TỰ VIẾT trong module-catalog service (action ModuleEnabled/ModuleDisabled, actionGroup='CONFIG_UPDATE', object_type='module' [cần migration 0474], permissionCode='FOUNDATION.MODULE.UPDATE') CÙNG tx withTenant — KHÔNG mượn SettingService.updateCompanySetting. 7 module lõi MVP KHÓA CỨNG (AUTH/HR/ATT/LEAVE/TASK/DASH/NOTI) → toggle 400, 0 write, 0 audit. getMyApps + getAllModules phản ánh enabled ngay (đã resolve qua resolveMany key module.<code>.enabled). Contract patchModuleToggleSchema (enabled:boolean) APPEND vào file đã-export packages/contracts/src/foundation/module-catalog.ts (KHÔNG đụng barrel index.ts).","builder":"backend-builder","paths":["apps/api/src/foundation/module-catalog/**","packages/contracts/src/foundation/module-catalog.ts","apps/api/test/foundation/module-toggle-permission-deny.int-spec.ts","apps/api/test/foundation/module-toggle-tenant-isolation.int-spec.ts"]},{"id":"be-retention-create-simulate","task":"POST /foundation/retention-policies (create — gate manage:foundation-retention 0435:363, is_sensitive=true) + POST /foundation/retention-policies/:id/simulate (expose RetentionService.simulate). createPolicy (retention.service.ts:90-129) HIỆN THIẾU audit → thêm audit action='RetentionPolicyCreated' (actionGroup='CONFIG_UPDATE' hoặc 'CREATE') object_type='retention_policy' (đã có CHECK 0456) in-tx withTenant + snapshot cấu hình (KHÔNG secret/PII). simulate/runCleanup: guard policyRows[0] undefined → NotFoundException 404 (hết 500 do cast 'as RetentionPolicyRow'). GIỮ PROTECTED_TABLES + isSensitive như S2-FND-BE-3. Contracts createRetentionPolicySchema + simulateResultSchema APPEND vào file đã-export packages/contracts/src/foundation/retention.ts (KHÔNG đụng barrel).","builder":"backend-builder","paths":["apps/api/src/foundation/retention/**","packages/contracts/src/foundation/retention.ts","apps/api/test/foundation/retention-create-simulate-deny.int-spec.ts"]}]
acceptanceChecks: ["system-settings: GET /foundation/system-settings(/:key) trả 200 với sensitive value ĐÃ mask (secret_ref không bao giờ trả); principal KHÔNG có grant EXACT system-manage:foundation-setting (kể cả super-admin *:*) → 403.","PATCH /foundation/system-settings/:key: validate value_type/validation_schema đọc TỪ hàng system_settings — sai type→400, sai schema→422; thành công ghi system_settings qua updateSystemTx/insertSystemTx (KHÔNG chạm company_settings) + đúng 1 audit row action='SYSTEM_SETTING_UPDATED' object_type='system_setting' có company_id = actor.companyId (ghi trong CÙNG withTenant tx).","module-toggle: PATCH /foundation/modules/:code enabled=false cho module NON-core → 200, ghi company_settings 'module.<code>.enabled'=false + đúng 1 audit row object_type='module' actionGroup='CONFIG_UPDATE' permissionCode='FOUNDATION.MODULE.UPDATE'; getMyApps + getAllModules phản ánh enabled=false NGAY.","module-toggle core-lock: toggle bất kỳ trong 7 module MVP (AUTH/HR/ATT/LEAVE/TASK/DASH/NOTI) → 400, 0 company_settings write, 0 audit — đo được từng module.","module-toggle gate: principal KHÔNG có grant EXACT update:foundation-module → 403 + 0 audit.","migration: query pg_constraint xác nhận 'module' ∈ audit_logs_object_type_chk (0474) VÀ ∈ AUDIT_OBJECT_TYPES; chain 0000→0474 xanh LANE_DB; INSERT audit object_type='module' không vỡ CHECK; 'system_setting' (0439)/'retention_policy' (0456) verify-only, KHÔNG thêm lại.","retention create: POST /foundation/retention-policies → 200 tạo policy company_id=actor.companyId + đúng 1 audit row action='RetentionPolicyCreated' object_type='retention_policy' in-tx (rollback ⇒ 0 audit); gate manage:foundation-retention (pair 363, sensitive) → thiếu grant 403.","retention simulate/not-found: POST /:id/simulate → 200 đếm eligible READ-ONLY; id lạ → 404 (KHÔNG 500); runCleanup id lạ → 404. PROTECTED_TABLES + isSensitive giữ nguyên (bảng append-only không bao giờ purge).","export DEFER: Grep xác nhận 0 tham chiếu export:foundation-audit-log ở app-surface (module-app-metadata requiredAny, web-core registry, sidebar-registry đều view:audit-log; audit.controller chỉ route view:audit-log); export route KHÔNG build; defer pin S2-FND-DOC-1.","contracts dual-build (pnpm build) xanh; barrel index.ts KHÔNG đổi; DoD §8: validation input, permission guard, audit-in-tx cho hành động quan trọng, test có, FULL gate PASS, company_id ở mọi query nghiệp vụ (BẤT BIẾN #1/#2/#3), không secret vào DTO/audit."]
testTasks: ["RED deny-path system-settings (viết TRƯỚC): PATCH /foundation/system-settings/:key với principal thiếu system-manage:foundation-setting → 403 + assert count(audit_logs)=trước (0 audit). Positive-path dùng principal có grant EXACT cặp sensitive (super-admin/wildcard KHÔNG đủ). TUYỆT ĐỐI KHÔNG test '2-tenant isolation' cho system-setting (global no-RLS).","RED deny-path module-toggle: PATCH /foundation/modules/:code với principal thiếu update:foundation-module → 403 + 0 audit.","Core-module hard-lock (đo được từng module): vòng lặp 7 module MVP, PATCH toggle enabled=false → 400 + 0 company_settings write + 0 audit.","Module-toggle 2-tenant deny ĐÚNG TRỤC (LANE_DB, thay test system-setting sai trục): actor tenant A toggle module X off → company_settings 'module.X.enabled' của tenant B KHÔNG đổi VÀ getMyApps của B vẫn thấy X; 0 cross-tenant leak.","Module-toggle happy-path (LANE_DB): non-core toggle off → getMyApps/getAllModules enabled=false ngay + đúng 1 audit row object_type='module'.","RED deny-path retention: POST /foundation/retention-policies thiếu manage:foundation-retention → 403 + 0 audit; POST /:id/simulate thiếu quyền → 403.","Retention create audit-in-tx: POST create thành công → đúng 1 audit row RetentionPolicyCreated object_type='retention_policy' (giả lập rollback tx ⇒ 0 audit — chứng minh cùng tx).","Retention not-found guard: POST /:id/simulate + runCleanup với UUID không tồn tại → 404 (assert KHÔNG 500).","Migration chain (LANE_DB cô lập): drizzle migrate 0000→0474 xanh; assert 'module' trong CHECK qua pg_constraint; INSERT 1 audit row object_type='module' thành công.","Integration DB cô lập: chạy dưới scripts/lane-db-setup.sh <lane> + LANE_DB=mediaos_<lane> (gate hasDb && LANE_DB — chống xanh-giả trên DB dev chung); unit spec colocate src/**/*.spec.ts, integration deny-path test/**/*.int-spec.ts (verify thực sự xuất hiện trong run summary)."]
steps: ["1) SERIAL FIRST — mig-audit-module: viết migration 0474 UNION-add 'module' + sync AUDIT_OBJECT_TYPES; verify 'system_setting' (0439) + 'retention_policy' (0456) đã tồn tại; chain 0000→0474 xanh LANE_DB. Lý do đi trước: be-module-toggle ghi audit object_type='module' → AuditObjectType union + DB CHECK phải có 'module' trước, nếu không TS-error + CHECK-violation.","2) PARALLEL (sau khi migration merge, chạy trên DB đã migrate): be-system-settings + be-retention-create-simulate — KHÔNG phụ thuộc 0474 (system_setting/retention_policy object_type đã có sẵn trong CHECK). Mỗi lane file rời rạc → không giẫm chân.","3) be-module-toggle CHỈ sau khi mig-audit-module xong (phụ thuộc 'module' trong CHECK + AUDIT_OBJECT_TYPES).","4) Mỗi lane: viết deny-path RED TRƯỚC (403 thiếu quyền + assert 0 audit rows), chạy đỏ → GREEN implementation → FULL gate. Positive-path dùng principal có grant EXACT cặp is_sensitive (super-admin/wildcard *:* KHÔNG thoả sensitive gate — permission.service L157-174).","5) Contracts: schema APPEND vào file đã-export (module-catalog.ts, retention.ts); system-settings dùng DTO cục bộ. Barrel packages/contracts/src/foundation/index.ts KHÔNG chỉnh (không file contract MỚI) → 0 xung đột 3-chiều. pnpm build contracts (dual ESM/CJS) xanh.","6) Verify (round2-confirmed, KHÔNG tốn công gỡ): Grep xác nhận export:foundation-audit-log KHÔNG xuất hiện ở app-surface (module-app-metadata requiredAny dùng view:audit-log; web-core registry + sidebar-registry + audit.controller đều view:audit-log). Pin quyết định DEFER export vào S2-FND-DOC-1 (ghi chú, không build export route).","7) Verify integration trên DB cô lập: bash scripts/lane-db-setup.sh <lane> → export LANE_DB=mediaos_<lane> → pnpm --filter @mediaos/api test (gate hasDb && LANE_DB, tránh xanh-giả).","8) FULL gate (security-reviewer + database-reviewer + silent-failure-hunter) + người chốt red-zone TRƯỚC merge (3 gate mới đều crown-jewel: permission sensitive + audit append-only)."]
```

GAP-ANALYSIS (đối chiếu code 2026-07-04, head mig 0473):
1) system-settings — settings.controller.ts CHỈ có /settings/public + /settings/resolve + PATCH /company-settings/:key; THIẾU GET/PATCH /system-settings. setting.service.ts có updateCompanySetting (company path, audit COMPANY_SETTING_UPDATED) nhưng THIẾU updateSystemSetting. setting.repository.ts có insertCompanyTx/updateCompanyTx + read-only findOneSystemTx/findSystemByKeysTx nhưng THIẾU insertSystemTx/updateSystemTx. object_type 'system_setting' ĐÃ ở AUDIT_OBJECT_TYPES (audit.ts:231) + CHECK 0439 (v_new=['company_setting','system_setting']) ⇒ KHÔNG cần migration cho nhánh này.
2) module-toggle — module-admin.controller.ts CHỈ GET modules + GET modules/:code; THIẾU PATCH modules/:code. object_type 'module' KHÔNG có trong AUDIT_OBJECT_TYPES (grep rỗng) ⇒ BẮT BUỘC migration 0474 UNION-add + sync array. getMyApps/getAllModules đã resolve enabled qua SettingService.resolveMany key 'module.<code>.enabled' (default=true) ⇒ toggle chỉ cần upsert company_settings hàng đó là phản ánh ngay.
3) retention — retention.service.ts createPolicy (L90-129) THIẾU audit (updatePolicy L194 đã có RetentionPolicyUpdated). simulate (L296)/runCleanup (L331) cast policyRows[0] 'as RetentionPolicyRow' KHÔNG guard undefined → 500 khi id lạ. controller THIẾU POST create + POST /:id/simulate. object_type 'retention_policy' đã ở CHECK 0456 ⇒ KHÔNG cần migration.
4) export:foundation-audit-log (0435:346) — ĐÃ deprecated ở S2-FND-BE-5. Round2-fix #3 XÁC MINH bằng Grep: KHÔNG xuất hiện trong requiredAny của module-app-metadata.ts (AUTH dùng view:audit-log 0340; foundation-audit-log chỉ trong COMMENT). web-core/registry.ts + app/sidebar-registry.ts + audit.controller.ts đều map/route view:audit-log. ⇒ KHÔNG có việc gỡ ở app-surface; chỉ VERIFY + pin DEFER vào S2-FND-DOC-1 (owner chốt DEFER 2026-07-04). KHÔNG build export route.

MÔ HÌNH DỮ LIỆU (round2-fix #1/#2 — KHÓA CỨNG): system_settings (settings.ts:26-61) là GLOBAL, KHÔNG company_id/RLS (mirror system_modules/permissions). ⇒ (a) KHÔNG test 2-tenant isolation cho system-setting; (b) repo phải có insertSystemTx/updateSystemTx RIÊNG ghi system_settings (KHÔNG company_settings); (c) value_type/validation_schema đọc TỪ hàng system_settings; (d) VẪN bọc db.withTenant(actor.companyId) khi PATCH để audit_logs (tenant-scoped, company_id NOT NULL) nhận đúng company_id — write system_settings trong withTenant tx vô hại (không policy keyed company_id). Trục 2-tenant deny ĐÚNG = MODULE-TOGGLE (ghi company_settings key 'module.<code>.enabled' — TENANT-SCOPED, company_id NOT NULL + RLS+FORCE): actor A toggle KHÔNG đổi company_settings/getMyApps của B.

SENSITIVE-GATE (permission.service.ts L157-174, guard.ts): 3 cặp mới đều is_sensitive=true (system-manage:foundation-setting 0435:343, update:foundation-module :339, manage:foundation-retention :363). 0435 chỉ bulk-grant company-admin các cặp foundation-* is_sensitive=FALSE ⇒ company-admin KHÔNG có 3 cặp này. Sensitive gate: company-level ALLOW dù wildcard *:* hay super-admin ĐỀU KHÔNG thoả — cần grant EXACT non-wildcard. ⇒ positive-path test PHẢI seed principal có grant tường-minh cặp sensitive (KHÔNG dựa super-admin wildcard).

CẮT LANE (ranh giới FILE, 0 chồng): mig-audit-module=migrations/** + schema/audit.ts (db-migration); be-system-settings=settings/** + 2 test file riêng; be-module-toggle=module-catalog/** + contracts/module-catalog.ts + 2 test file riêng; be-retention-create-simulate=retention/** + contracts/retention.ts + 1 test file riêng. schema/audit.ts CHỈ mig-lane sửa (append 'module'); module-toggle chỉ IMPORT AUDIT_OBJECT_TYPES/type. Test đặt theo vitest include: unit colocate src/**/*.spec.ts (đuôi .int.spec.ts vẫn khớp src/**/*.spec.ts, skipIf!hasDb), integration deny-path test/**/*.int-spec.ts (đuôi hyphen). Mỗi lane 1 file test riêng → KHÔNG đụng glob nhau.

BARREL (hot-file): packages/contracts/src/foundation/index.ts GIỮ NGUYÊN — mọi schema APPEND vào file ĐÃ export (module-catalog.ts, retention.ts); system-settings dùng DTO cục bộ settings.dto.ts (mẫu company path, tránh drift packages/contracts/settings.ts CŨ). ⇒ 0 file contract MỚI ⇒ 0 append barrel ⇒ tránh xung đột 3-chiều mà WO cảnh báo (nếu builder chọn tách file contract riêng thì đúng 1 lane cuối append barrel).

MIGRATION LANE (nối tiếp): head 0473 → kế 0474_s2_fndbe8_module_audit_object_type. Chỉ UNION-add 'module' vào CHECK object_type — KHÔNG backfill company_id, KHÔNG DDL bảng mới ⇒ ràng buộc 'RLS+FORCE trước backfill' KHÔNG áp dụng (không có backfill). DO-block idempotent clone 0468, append-only #2 nguyên vẹn (chỉ mở rộng tập giá trị hợp lệ, KHÔNG cấp UPDATE/DELETE). PHỤ THUỘC: be-module-toggle chạy SAU (audit object_type='module' cần 'module' trong AuditObjectType union + DB CHECK).

INVARIANTS (CLAUDE §2): #1 mọi write/read company_settings + audit đi qua withTenant(companyId) — system_settings write bọc trong withTenant để audit đúng company_id; #2 audit append-only — chỉ UNION-add CHECK, ghi audit-in-tx, KHÔNG UPDATE/DELETE; #3 không secret plaintext — mask sensitive khi đọc, drop secret_ref, snapshot audit KHÔNG secret/PII. DEPENDENCY §3: PermissionService + audit đã sẵn (module nhạy cảm hợp lệ).

GATE: zone red, crown-jewel (permission sensitive + audit append-only + config toggle) → FULL gate (security-reviewer + database-reviewer + silent-failure-hunter) + người chốt red-zone TRƯỚC merge. Deny-path RED viết-trước từng route. Coverage ≥80% (ngưỡng module nhạy cảm).

VERIFY (Đội 3): chạy trên LANE_DB cô lập (hasDb && LANE_DB) — chain 0000→0474; deny-path 403+0 audit từng route; core-lock 400 từng module; module 2-tenant no-leak; retention create audit-in-tx (rollback⇒0) + not-found 404; pg_constraint có 'module'; contracts dual-build xanh; Grep 0 tham chiếu export:foundation-audit-log app-surface.

OUT-OF-SCOPE (KHÔNG làm ở WO này): build audit-export route (DEFER owner 2026-07-04, pin S2-FND-DOC-1); gỡ seed row 0435:346 (append-only #2 — GIỮ, chỉ deprecated); đổi gate GET /settings/public (WO FND-SETTINGS-PUBLIC-GATE); run:foundation-seed / view+run:foundation-job orphan (WO khác); pin doc lệch (S2-FND-DOC-1). KHÔNG tạo file contract MỚI / KHÔNG sửa barrel / KHÔNG sửa app.module.ts (module đã wire qua FoundationModule).
</content>
