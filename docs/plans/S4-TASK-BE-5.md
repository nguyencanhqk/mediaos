```yaml
wo: S4-TASK-BE-5
zone: yellow
generated_by: auto-loop
reconciled_at: "db8f081"
lanes: [{"id":"L1-mig-verify","task":"SERIAL đầu tiên, CONDITIONAL/verify-only (KHÔNG mint mặc định). Head THẬT master ĐÃ verify = 0493 (git ls-tree master apps/api/migrations = ...0493_s4_dashcatalog2; KHÔNG có 0494+ đang bay ⇒ chống double-mint OK). Verify mig 0485 đã seed đủ: ('file-upload','task') + ('file-delete','task') non-sensitive + grants (employee@Own file-upload; manager@Team file-upload/file-delete; hr@Company; admin@Company; employee KHÔNG có file-delete) + ('view-report','project') sensitive + grants (manager@Team; hr@Company; admin@Company; employee KHÔNG). task_activity_logs target_type 'File' ∈ chk_task_activity_target_type (0478); cột action FREE-TEXT (không CHECK) ⇒ TASK_FILE_UPLOADED/DELETED không cần migration. KẾT LUẬN kỳ vọng: KHÔNG mint migration (head giữ 0493). CHỈ mint 0494 nối tiếp head THẬT (thuần DATA, ON CONFLICT DO NOTHING, KHÔNG đụng RLS/FORCE/policy) NẾU thiết kế resolver quyết định thêm cặp MỚI (vd file-view:task riêng cho gate read). TUYỆT ĐỐI KHÔNG mint migration rỗng.","builder":"db-migration","paths":["apps/api/migrations/**"]},{"id":"L2-task-file-access","task":"CROWN (permission/file-access/audit — FULL gate + santa-method). Bề mặt CANONICAL /tasks/:taskId/files qua Foundation FileService + file_links (TÁI DÙNG NGUYÊN pattern S2-HR-EMPFILE-1: employee-file.resolver/service/controller/repository — KHÔNG bảng task_files riêng). (1) task-file.resolver.ts: class TaskFileResolver implements FileOwnerPermissionResolver, moduleCode='TASK', entityTypes=['task']; canViewFile/canDownloadFile→resolveAndAssert('read','task')+task-in-scope; canLinkFile→('file-upload','task'); canDeleteFile/canUnlinkFile→('file-delete','task'); mỗi hàm resolveAndAssert data_scope (throw Forbidden khi 0 grant ⇒ policy map deny-error) + kiểm task-in-scope/membership (assignee OR project-member) qua TaskCoreRepository.buildReadScopeExists/isTaskInScopeTx TRONG withTenant(companyId); not-found/cross-tenant RLS 0-row/out-of-scope ⇒ false ⇒ fail-closed (KHÔNG escalate FOUNDATION.FILE.* fallback). (2) task-file.repository.ts: findLinkedFileTx(file_links module_code='TASK', entity_type='task', entity_id=taskId JOIN files, soft-delete filter cả 2) + listLinkedFilesByTaskTx + isTaskInScopeTx (mirror employee-file.repository, chỉ đọc join an toàn — KHÔNG storage_path/checksum). (3) task-file.service.ts: assertScope (resolveAndAssert pair + task-in-scope 404) + loadLinkedFileOr404 (cross-task 404) + link()→FileService.link({moduleCode:'TASK',entityType:'task',entityId:taskId,linkType:'Attachment',...}); getDownloadUrl()→STRICT scan-guard DOWNLOADABLE_SCAN={Clean,NotRequired} else 409 NOT_DOWNLOADABLE TRƯỚC khi gọi FileService.getDownloadUrl (vì FileService.downloadStateDenyReason CHỈ chặn Infected+not-Uploaded, KHÔNG chặn Pending/Failed); delete()→FileService.deleteFile (soft). (4) task-files.controller.ts prefix 'tasks/:taskId/files', class-level @UseGuards(PermissionGuard)+@UsePipes(ZodValidationPipe): POST @RequirePermission('file-upload','task') · GET list @RequirePermission('read','task') · GET :fileId @RequirePermission('read','task') · GET :fileId/download @RequirePermission('read','task') @Res 302 redirect signed-url · DELETE :fileId @HttpCode(204) @RequirePermission('file-delete','task') — guard opt-in TƯỜNG MINH từng route (thiếu = route MỞ). (5) task-activity.service.ts: mở union TaskActivityAction += TASK_FILE_UPLOADED|TASK_FILE_DELETED (additive TS, action free-text — KHÔNG migration); service ghi task_activity_logs CÙNG tx (target_type 'File'). (6) tasks.module.ts: import FilesModule (exports FileService+FilePolicyService), provide TaskFileResolver+TaskFileService+TaskFileRepository + controller TaskFilesController, implements OnModuleInit → this.filePolicy.registerResolver(this.taskFileResolver) (mirror EmployeesModule.onModuleInit — APPEND, KHÔNG rewrite; thiếu resolver ⇒ FilePolicy deny-no-resolver 403 câm). (7) packages/contracts/src/task-file.ts: linkTaskFileSchema{fileId:uuid,category?:string} + TaskFileDto + listTaskFilesQuerySchema; export ở index.ts (append barrel).","builder":"backend-builder","paths":["apps/api/src/tasks/task-files.controller.ts","apps/api/src/tasks/task-file.service.ts","apps/api/src/tasks/task-file.repository.ts","apps/api/src/tasks/task-file.resolver.ts","apps/api/src/tasks/task-activity.service.ts","apps/api/src/tasks/tasks.module.ts","packages/contracts/src/task-file.ts","packages/contracts/src/index.ts"]},{"id":"L3-project-report","task":"GET /projects/:id/report (TASK-API, SPEC-06 §16.1) — thêm route vào ProjectsController hiện có (@UseGuards(PermissionGuard)+@RequirePermission('view-report','project',{isSensitive:true}) — cặp sensitive 0485; ProjectsController hiện đã ghi nhận view-report:project OUT-OF-SCOPE ở comment, nay HIỆN THỰC). KHÔNG động tasks.module.ts (ProjectsService/Repository đã provided). ProjectsService.getReport: resolveReadScopeExists('read','project') tồn tại; report resolveAndAssert('view-report','project') (manager@Team; hr/admin@Company; employee KHÔNG grant ⇒ PermissionGuard 403 tại route) + kiểm project-in-scope qua findDetailByIdTx(scopeExists) → ngoài scope/cross-tenant 404 (KHÔNG lộ). ProjectsRepository.aggregateReportTx (withTenant + company_id, RLS): countsByStatus (đếm task theo task_status Todo/InProgress/InReview/Done/Cancelled), overdueCount (due_date < now() AND status ∉ Done/Cancelled), assigneeWorkload[] (đếm task active theo main_assignee_employee_id) + LIMIT top-N workload. Trả DTO thô (envelope API-01 do interceptor toàn cục). Contract: projectReportSchema/ProjectReportDto trong packages/contracts/src/task.ts (đã export sẵn ở index — KHÔNG đụng barrel index.ts).","builder":"backend-builder","paths":["apps/api/src/tasks/projects.controller.ts","apps/api/src/tasks/projects.service.ts","apps/api/src/tasks/projects.repository.ts","packages/contracts/src/task.ts"]},{"id":"L4-legacy-attachments-lock","task":"CROWN/security (SUPERSEDE OWNER 2026-07-12). KHÓA route legacy /tasks/:taskId/attachments (TaskAttachmentsController) — hiện là LỖ ĐỌC-RỘNG in-tenant né membership: TaskAttachmentsService.listByTask/getDownloadUrl CHỈ assertTaskInTenant (KHÔNG data-scope/membership) trong khi gate read:task cấp cho employee@Own/manager@Team/hr@Company/admin@Company ⇒ bất kỳ user có read:task LIỆT KÊ/TẢI attachment của MỌI task tenant = IDOR. QUYẾT ĐỊNH (Đội 1, trong latitude owner 'hoặc chặn hẳn 410'): DEPRECATE-IN-PLACE 410 GONE toàn bộ controller — MỌI handler (POST/GET list/GET :id/download/DELETE) throw GoneException (code TASK_ATTACHMENTS_SUPERSEDED, trỏ /tasks/:id/files). Lý do chọn 410 thay vì thêm membership-check: (a) bảng task_attachments PARK — KHÔNG ghi mới (POST phải chết); (b) canonical = /tasks/:id/files (L2) đã thay thế + FE S4-FE-TASK-4 nối bề mặt mới; (c) legacy KHÔNG có cột scan_status ⇒ scan-guard không áp được; (d) 410 đóng dứt điểm lỗ IDOR với ít code + không rủi ro logic scope legacy. GIỮ task-attachments.service.ts + bảng task_attachments NGUYÊN (PARK, KHÔNG xóa) — controller không còn gọi service. Nếu crown-review/owner muốn giữ legacy đọc-được: fallback = bơm data-scope+membership (TaskCoreRepository.buildReadScopeExists) vào listByTask/getDownloadUrl/softDelete + 404 out-of-scope (phương án B, MỞ RỘNG paths sang service). Mặc định = phương án A (410).","builder":"backend-builder","paths":["apps/api/src/tasks/task-attachments.controller.ts","apps/api/src/tasks/task-attachments.service.ts"]},{"id":"L5-int-spec","task":"Int-spec RED-TRƯỚC (deny-path/IDOR/scan-guard/resolver-fallback/report-scope/legacy-regression), colocated apps/api/test/integration/*.int-spec.ts, chạy trên LANE_DB cô lập (đề xuất mediaos_s4_task_be5). Fixture: 1 project + tasks nhiều status/overdue + assignee (assignee-employee + project-member), foundation file (Uploaded+Clean / Pending / Failed / Infected / NotRequired), file link tới task KHÁC (cross-task) + tenant KHÁC (cross-tenant), 1 attachment legacy (task_attachments) cho regression. PHỦ: (1) /tasks/:id/files: deny thiếu file-upload/file-delete:task (403 tại guard); cross-task/cross-tenant/ngoài-membership 404; download scan Pending/Failed/Infected 409 NOT_DOWNLOADABLE (KHÔNG signed-url); resolver fail-closed (chỉ FOUNDATION.FILE.* rộng KHÔNG mở được file task-linked; deny-no-resolver khi CHƯA đăng ký); TASK_FILE_UPLOADED/DELETED + FileLinked/FileDeleted + file_access_logs append-only; soft-delete file_links/files. (2) /projects/:id/report: deny employee (403); manager@Team xin project ngoài team 404; counts/overdue/workload khớp fixture + limit. (3) LEGACY REGRESSION (chứng minh lỗ đọc-rộng đóng): user có read:task GET/POST/DELETE /tasks/:otherTaskId/attachments → 410 (KHÔNG 200/data) — trước đây trả attachment của task né membership, nay 410 (hoặc 404-out-of-scope nếu chọn phương án B).","builder":"backend-builder","paths":["apps/api/test/integration/**"]}]
acceptanceChecks: ["POST /tasks/:id/files: link foundation file (Uploaded+confirm) qua FileService.link (module 'TASK', entity_type 'task', entityId=taskId) — gate file-upload:task; employee@Own có upload grant + task-in-scope (assignee/project-member) thành công; không grant → 403; file_links row có company_id; TASK_FILE_UPLOADED ghi task_activity_logs (target_type 'File') + FileLinked audit (append-only) CÙNG tx.","GET /tasks/:id/files + GET /tasks/:id/files/:fileId: chỉ file link tới ĐÚNG task trong tenant (RLS), lọc scope/membership; cross-task/cross-tenant/ngoài-scope → 404 (không lộ); gate read:task; envelope API-01; KHÔNG lộ storage_path/checksum.","GET /tasks/:id/files/:fileId/download: chỉ signed-url TTL-ngắn (302 redirect) khi scan_status ∈ {Clean,NotRequired}; Pending/Failed/Infected → 409 NOT_DOWNLOADABLE TRƯỚC khi presign (STRICT hơn FileService.downloadStateDenyReason vốn chỉ chặn Infected/not-Uploaded); file_access_logs Download append; gate read:task.","DELETE /tasks/:id/files/:fileId: soft-delete file_links/files (deleted_at, KHÔNG hard-delete) — gate file-delete:task; employee (KHÔNG có file-delete grant) → 403; TASK_FILE_DELETED activity + FileDeleted audit append; 204.","TaskFileResolver đăng ký trong TasksModule.onModuleInit cho (module='TASK', entity_type='task'): file task-linked KHÔNG truy cập được bằng grant FOUNDATION.FILE.* rộng (không escalate fallback); chưa đăng ký resolver = deny-no-resolver fail-closed 403.","GET /projects/:id/report: gate view-report:project (sensitive 0485) — manager@Team thấy project team; hr/admin@Company; employee (không grant) → 403; project ngoài scope/cross-tenant → 404; trả {countsByStatus, overdueCount, assigneeWorkload[] có limit}; envelope API-01.","LEGACY LOCK: MỌI route /tasks/:taskId/attachments (POST/GET list/GET download/DELETE) → 410 Gone; user có read:task KHÔNG còn liệt kê/tải attachment của task né membership (regression 410, KHÔNG 200/data); bảng task_attachments + service PARK (không xóa, không ghi mới).","Mọi truy cập qua withTenant(companyId); mọi query AND company_id; KHÔNG đổi RLS/FORCE (file_links 0433/0472, files, task_activity_logs 0478 đã RLS+FORCE). KHÔNG migration mới (0485 đủ file/report pairs+grants; head giữ 0493) TRỪ khi cố ý thêm cặp (0494 nối head, ON CONFLICT DO NOTHING). KHÔNG bảng task_files riêng.","check.sh --lane-db xanh; FULL gate (security-reviewer + silent-failure-hunter + santa-method resolver) trên nhánh file-access + legacy-lock; coverage ≥80% nhánh nhạy cảm; DoD §8 (validation input, permission guard opt-in per-route, audit, append-only, không phá luồng chính, cập nhật backlog); FE S4-FE-TASK-4 nối bề mặt MỚI (WO khác)."]
testTasks: ["RED deny-path (permission): POST /tasks/:id/files không có file-upload:task → 403; DELETE file bởi employee (không file-delete:task) → 403; GET /projects/:id/report bởi employee (không view-report:project) → 403.","RED IDOR/cross-tenant: link/list/get/download/delete file của task KHÁC (cross-task) → 404; task/file thuộc tenant KHÁC → 404 (RLS 0-row); task ngoài membership/scope actor → 404.","RED scan-guard: download file scan_status Pending/Failed/Infected → 409 NOT_DOWNLOADABLE, KHÔNG signed-url; Clean/NotRequired → 302 redirect + file_access_logs Download append.","RED resolver fail-closed: file task-linked truy cập bởi user chỉ có FOUNDATION.FILE.* rộng (không quyền task) → deny (không fallback escalation); regression-guard deny-no-resolver khi CHƯA register.","RED report scope + đúng số: manager@Team xin project ngoài team → 404; countsByStatus + overdueCount + assigneeWorkload khớp fixture; limit workload tôn trọng.","RED LEGACY REGRESSION (chứng minh lỗ đọc-rộng đóng): user có read:task gọi POST/GET list/GET download/DELETE /tasks/:otherTaskId/attachments → 410 (KHÔNG 200/data né membership). [Nếu chọn phương án B membership-check thay 410: → 404 out-of-scope, đồng thời assert user in-scope vẫn 200.]","Append-only/soft-delete: task_activity_logs + file_access_logs CHỈ INSERT (không UPDATE/DELETE bởi app role); file_links/files soft-delete (deleted_at) không hard-delete; task_attachments PARK (không ghi mới).","QA hạ tầng: int-spec colocated apps/api/test/integration/task-files-*.int-spec.ts + project-report-*.int-spec.ts + legacy-attachments-lock-*.int-spec.ts, chạy DB cô lập LANE_DB (bash harness/check.sh --lane-db); không set → banner XANH KHÔNG ĐỦ BẰNG CHỨNG (int-spec skip)."]
steps: ["L1 (SERIAL đầu): xác nhận head master=0493 (đã verify) + 0485 đã seed file-upload/file-delete:task + view-report:project + grants + 0478 CHECK target 'File' → KẾT LUẬN KHÔNG cần migration mới (mặc định). Chỉ mint 0494 nối head nếu thiết kế thêm cặp mới (vd file-view:task).","L5 (RED trước) + L2 song song: viết int-spec deny/IDOR/scan/resolver → dựng TaskFileResolver + wire FilesModule/onModuleInit vào tasks.module.ts → task-file.repository (file_links entity_type='task') → task-file.service (FileService.link/getDownloadUrl/deleteFile + STRICT scan-guard {Clean,NotRequired} + TASK_FILE_* activity) → task-files.controller (/tasks/:taskId/files, gate file-upload/read/file-delete, download @Res 302) → contracts task-file.ts + export index. GREEN.","L3 song song: GET /projects/:id/report vào ProjectsController (gate view-report:project sensitive) → ProjectsService.getReport (scope + project-in-scope 404) → ProjectsRepository.aggregateReportTx (counts/overdue/workload + limit) → contracts projectReportSchema (task.ts). GREEN.","L4 song song (CROWN/security): 410 GONE toàn bộ TaskAttachmentsController (deprecate-in-place; task-attachments.service + bảng PARK); L5 thêm regression chứng minh route legacy KHÔNG còn đọc-rộng in-tenant né membership.","Hợp nhất: bash harness/check.sh --lane-db (LANE_DB=mediaos_s4_task_be5) — deny-path/IDOR/cross-tenant/scan/legacy-regression THỰC THI thật. FULL gate (security-reviewer + silent-failure-hunter + database-reviewer L1/repo + santa-method cho TaskFileResolver crown) trên nhánh file-access + legacy-lock. Coverage ≥80% nhánh nhạy cảm. Checkpoint → PR."]
```

RECONCILE 2026-07-12 (head master=0493 verified, code re-checked): reused=FALSE. Lý do bắt buộc phân rã lại: WO RE-RUN sau plan-block wf_b860acb0 + OWNER SUPERSEDE — plan cũ (355dd9e) đặt việc khóa route legacy /tasks/:taskId/attachments vào OUT-OF-SCOPE ('COEXIST, KHÔNG xóa/sửa'); done_when MỚI đưa việc này VÀO CÙNG WO. Thêm lane L4-legacy-attachments-lock (CROWN/security) + testTask legacy-regression. Phần canonical surface (L1/L2/L3) + int-spec giữ nguyên thiết kế đã đối chiếu code còn đúng.

GAP-ANALYSIS: 1. LỖ ĐỌC-RỘNG legacy XÁC NHẬN: task-attachments.service.ts listByTask()/getDownloadUrl() CHỈ assertTaskInTenant — KHÔNG data-scope/membership; controller gate read:task (employee@Own/manager@Team/hr@Company/admin@Company theo 0485) ⇒ user có read:task liệt kê/tải attachment MỌI task tenant né membership = IDOR. QUYẾT ĐỊNH (latitude owner 'hoặc chặn hẳn 410'): 410 GONE toàn controller (deprecate-in-place), bảng+service PARK. Chọn 410 vì task_attachments PARK không ghi mới (POST chết); canonical /tasks/:id/files thay thế + FE-TASK-4 nối mới; legacy không có scan_status; 410 đóng dứt điểm ít code/rủi ro. Fallback B (buildReadScopeExists membership + 404) nếu owner muốn giữ legacy đọc-được. 2. MIGRATION NO-OP mặc định: 0485 đã seed file-upload/file-delete:task + view-report:project + grants; head=0493 (không 0494+ bay) ⇒ KHÔNG mint; chỉ 0494 nối head nếu cố ý thêm cặp. 3. GATE READ file = read:task (DB-06 §12.1 không có file-view:task); FILE_* áp POST(file-upload)/DELETE(file-delete). 4. CROWN resolver BẮT BUỘC: FilePolicy deny-no-resolver cho file link chưa có resolver ⇒ TaskFileResolver (mirror EmployeeFileResolver, entityTypes=['task']) đăng ký TasksModule.onModuleInit. 5. WIRING: tasks.module.ts CHƯA import FilesModule ⇒ thêm + OnModuleInit register (APPEND). 6. SCAN-GUARD không ỷ lại: files.service downloadStateDenyReason (L951) chỉ chặn Infected+not-Uploaded, Pending/Failed lọt ⇒ STRICT {Clean,NotRequired} else 409 TRƯỚC getDownloadUrl. 7. task_activity_logs target 'File' ∈ chk 0478, action free-text ⇒ TASK_FILE_* additive TS không migration. 8. DownloadUrlDto={url,expiresAt}; download @Res 302. 9. FLOW link (không upload trực tiếp): upload Foundation lấy fileId → POST /tasks/:id/files{fileId}. 10. projects.controller ghi view-report OUT-OF-SCOPE ở comment — nay hiện thực; getReport +resolveAndAssert('view-report','project') sensitive + project-in-scope 404.

BẤT BIẾN §2: #1 withTenant+company_id (RLS+FORCE tường cuối). #2 append-only task_activity_logs/file_access_logs + soft-delete file_links/files; log cùng tx; task_attachments PARK. #3 không lộ storage_path/checksum/signed-url-dài; download signed-url TTL-ngắn 302.

VERIFY: check.sh --lane-db (LANE_DB=mediaos_s4_task_be5) — deny/IDOR/cross-tenant/scan/legacy-regression thực thi thật; thiếu LANE_DB ⇒ XANH KHÔNG ĐỦ BẰNG CHỨNG. RED-trước. Coverage ≥80%. Trước PR đỏ: check.sh --all (REQUIRE_LANE_DB=1) exit 1 nếu skip vượt ngưỡng.

GATE §6 FULL (file access+permission+audit+legacy security): security-reviewer + silent-failure-hunter + database-reviewer (L1/repo) + santa-method (TaskFileResolver crown) + review L4 lock chứng minh đóng IDOR. Crown lane → planner Sonnet5 xhigh + plan-reviewer, IMPLEMENT/REVIEW Opus. typescript-reviewer+quality-gate baseline.

OUT-OF-SCOPE: /projects/:id/files (SPEC-06 TK-1 có nhưng DB-06 §12.1+0485 HOÃN — resolver entityTypes=['task'] YAGNI); export report CSV/PDF (phase sau); FE S4-FE-TASK-4 (WO riêng); Foundation upload grant (giả định đã cấp).

RỦI RO/CHỐT: (a) L4 410 vs membership-check — Đội 1 chọn 410; nếu đã có attachment thật FE đang hiển thị thì 410 tạm khoá tới khi FE-TASK-4 ship (N=1, feature mới, rủi ro thấp, owner sanction). (b) linkType 'Attachment' phải ∈ linkFileInputSchema enum — builder verify, nếu không dùng 'Document'.
</content>
