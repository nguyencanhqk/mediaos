# S15-PAYROLL-BE-5B — Track D phần 2: PDF phiếu lương 083 · 084 · 085 + hạ tầng tệp server

> 🟡 Chở tiền + PII **ra tệp**. **Opus** code; gate = **FULL** (§0b B4): `security-reviewer` + `database-reviewer` +
> `silent-failure-hunter` + `typescript-reviewer` (chạm job crown `TEMP_FILE_CLEANUP` + resolver file-policy). Plan-review đối kháng **một vòng**.
> Nhánh `feat/s15-payroll-be-5b` từ master `4fbc5beb`. Lane `mediaos_be5b`. **KHÔNG migration** (đo: `outbox.event_type`
> text tự do; `file_links.module_code/entity_type` không CHECK; `link_type` đã có `Export`; `file_access_logs.action`
> đã có `GenerateSignedUrl`).

## 0. Quyết định

### 0.1 Owner chốt 17/09/2026 (AskUserQuestion)

| #   | Câu hỏi                                    | Chốt                                                                                                                                                                                                                                  |
| --- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| O-3 | (từ BE-5) PDF giao thế nào                 | signed-URL + job dọn tệp tạm xoá CẢ object; 085 chạy nền                                                                                                                                                                              |
| O-4 | Client hỏi trạng thái 085 bằng gì          | **085 = lấy-hoặc-tạo.** Route file chung `/foundation/files/*` gác `download:foundation-file` (seed chỉ company-admin; migration 0569 chốt KHÔNG cấp cho vai khác) và danh sách PAYROLL ĐÓNG ở 85 ⇒ không thêm route, không cấp quyền |
| O-5 | Cơ chế chạy nền                            | **Outbox consumer** (bền, restart không mất). Đổi lại: worker outbox tuần tự ⇒ sự kiện khác trễ ~40 s khi có lô 2.000 phiếu — chấp nhận                                                                                               |
| O-6 | `TEMP_FILE_CLEANUP` xoá object phạm vi nào | **Mọi hàng job dọn** (tệp tạm hết hạn + Pending > 24 h của mọi module) — đúng TODO `files.service.ts:60`                                                                                                                              |
| O-7 | Hạn tệp tạm                                | **PDF lẻ 15 phút · ZIP 24 giờ** (signed-URL vẫn TTL env `S3_PRESIGN_TTL_SEC`, 300 s)                                                                                                                                                  |

### 0.2 Đo trước (17/09, không đo lại)

- `ObjectStorageService.putObject` CÓ (`object-storage.service.ts:159`) nhưng **0 caller production**, **không** `assertKeyInTenant`.
- `S3StorageAdapter.resolveTtl` chỉ đổi `expiresAt`; `createDownloadUrl`/`createUploadUrl` ký bằng `config.presignTtlSec` ⇒ bug thật (0 caller truyền TTL hôm nay).
- `TEMP_FILE_CLEANUP` chọn `(is_temporary ∧ expires_at < now) ∨ (Pending ∧ created_at < now − 24h)` **VÀ không có link sống**; chỉ xoá mềm, không inject storage. **Không code nào set `is_temporary=true`**.
- `getDownloadUrl` không xét `is_temporary/expires_at`. Tệp chưa link ⇒ rơi về `FOUNDATION.FILE.*` ⇒ holder `download:foundation-file` tải được ⇒ tệp PAYROLL **phải có link + resolver**.
- `GET /foundation/files` liệt kê metadata MỌI tệp tenant (có `original_name`) ⇒ tên tệp **không** được chứa tên/mã NV.
- Không có hàng đợi job một-lần; outbox: consumer không tx, tự `withTenant`; không có consumer ⇒ event `done`; reaper 5′ (2.000 phiếu × ~17 ms ≈ 34 s — dưới trần).
- ZIP: `jszip@3.10.1` + `archiver@5.3.2` đã có trong lock (transitive của exceljs) nhưng **không import được** từ `apps/api` ⇒ thêm `jszip@3.10.1` direct (0 gói mới vào cây; license `MIT OR GPL-3.0` — chọn MIT). pdfmake `0.3.11` (MIT, 12/06/2026 — đủ tuổi; deps `pdfkit ^0.19.1` · `linebreak` · `xmldoc`).
- Đọc lại PDF (DECISIONS-14 §6.2): cây KHÔNG có bộ trích văn bản ⇒ devDependency `pdfjs-dist@4.10.38` (Apache-2.0; `engines >=20` khớp root; đã vá CVE-2024-4367 từ 4.2.67). 5.x đòi Node ≥ 22.13 — không chọn.
- CI API có MinIO thật (`api.yml` step Start MinIO) ⇒ ca int đo object biến mất bằng `headObject`.
- `@Idempotent()` cache JSON envelope 900 s theo (company, user, method, path, key) — không dùng được để hỏi trạng thái (FE gửi key mới mỗi lượt hỏi).

### 0.3 Quyết định thiết kế (lật rẻ — ghi chỗ đổi)

| #    | Câu hỏi            | Chốt                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Đổi ở đâu                                     |
| ---- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| E-1  | Cặp + decorator    | Thêm 3 khoá route-pair: `payslipPdf` (083) = `view-payslip:payslip` · `mePayslipPdf` (084) = `view-own-payslip:payslip` (cờ như 032) · `payslipPdfBatch` (085) = `export:payroll`. Service assert THÊM: 083 `periodExport` · 085 `payslipList`. KHÔNG cặp mới (PAY-DEC-019)                                                                                                                                                                                                           | `payroll-route-pairs.const.ts`                |
| E-2  | Dữ liệu PDF        | 083 = CHÍNH `findTx(…, null)` + `itemsByPayslipIdTx` của 030; 084 = `findTx(…, user.id)` của 032 (bộ lọc `PUBLISHED_PERIOD_STATUSES` nằm trong `selectPayslips`) ⇒ phiếu người khác / kỳ chưa phát hành ⇒ **404 010**, 0 audit. KHÔNG tính lại. Tên qua `PayrollPeopleRepository.namesByUserIdsTx` (actor của route). Nhãn khoản = `payslip_items.label` (phiếu của chính người đó — cùng thứ 032 trả)                                                                                | `payroll-payslip-pdf.service.ts`              |
| E-3  | Dựng PDF           | `payslip-pdf.document.ts` (hàm THUẦN → `docDefinition`) + `payslip-pdf.renderer.ts` (pdfmake, font `pdfmake/fonts/Roboto` nạp MỘT lần, `setUrlAccessPolicy(() => false)`, local access chỉ thư mục font). Tiền: chuỗi numeric → định dạng bằng thao tác chuỗi (KHÔNG `Number`). Tên công ty đọc `companies` với `c.id = companyId` tường minh                                                                                                                                         | payroll                                       |
| E-4  | Tệp server         | Service foundation MỚI `ServerFileService` (không đụng `FileService.upload`): `reserveTx(tx, {…})` chèn `files` `Pending` (`is_temporary=true`, `expires_at`, `storage_path = buildFileKey`, size 0) + `file_links` (`module_code='PAYROLL'`, `link_type='Export'`, `access_scope='Owner'`) → `put` object (ContentType cố định) → `markStoredTx` (Uploaded, size, sha256). Hỏng giữa chừng ⇒ hàng Pending/Failed, job dọn xoá object ⇒ **không bao giờ có object mồ côi không hàng** | `foundation/files/server-file.service.ts`     |
| E-5  | Giao URL           | `ServerFileService.issueUrlTx` = presign (TTL env) + `file_access_logs` `GenerateSignedUrl` granted. Chỉ route PAYROLL gọi; route file chung bị `PayrollExportFileResolver` **từ chối mọi thao tác** (view/download/link/unlink/delete ⇒ false)                                                                                                                                                                                                                                       | resolver + `payroll.module.ts` `onModuleInit` |
| E-6  | Tên tệp            | `phieu-luong-<yyyy-mm>.pdf` · `phieu-luong-<yyyy-mm>.zip`; entry ZIP = `<mã NV hoặc 8 ký tự userId>-<yyyy-mm>.pdf` (trong ZIP, không lên danh sách metadata)                                                                                                                                                                                                                                                                                                                          | —                                             |
| E-7  | 083/084 phản hồi   | `200 { fileId, fileName, url, expiresAt }`. 083 audit `(payslip, payslipId, {format:'pdf'})` trong tx1 cùng lượt đọc; 084 0 audit (SPEC-11 §18.1 B). `Cache-Control: no-store`                                                                                                                                                                                                                                                                                                        | contracts `payroll-pdf.ts`                    |
| E-8  | 085 lấy-hoặc-tạo   | Khoá `pg_advisory_xact_lock(hashtext('payroll-pdf-batch:'‖periodId‖':'‖userId))`. Tìm lô SỐNG mới nhất của (người gọi, kỳ): link `PAYROLL/payslip-pdf-batch/periodId`, `uploaded_by = user`, `deleted_at IS NULL`, `expires_at > now`, `metadata.fingerprint` = vân tay hiện tại. `Pending` ⇒ **202** · `Uploaded` ⇒ **200** + url · `Failed` ⇒ **200** `{status:'Failed'}` trừ khi body `retry:true` ⇒ tạo mới. Không có ⇒ tạo mới ⇒ **202**                                         | `payroll-payslip-pdf-batch.service.ts`        |
| E-9  | Vân tay lô         | `payslips` chỉ có `created_at` (không `updated_at`/`deleted_at` — đo 17/09) ⇒ vân tay = `count(*)` ‖ `max(created_at)` ‖ `md5(string_agg(id order by id))` của phiếu trong kỳ ⇒ phiếu sinh lại/xoá sau khi có ZIP ⇒ lô cũ không khớp ⇒ tạo lô mới                                                                                                                                                                                                                                     | cùng service                                  |
| E-10 | Trần + rỗng        | Đếm TRƯỚC mọi thứ: `> 2.000` ⇒ **422 `PAYROLL-ERR-031` kind `pdf-batch-too-large`**, 0 audit, 0 file. Kỳ không tồn tại/khác tenant ⇒ 404 010. 0 phiếu ⇒ mã «kỳ chưa có phiếu» ĐANG CÓ trong catalog (tra lúc code; KHÔNG thêm mã)                                                                                                                                                                                                                                                     | —                                             |
| E-11 | Audit 085          | Ghi `(payroll_period, periodId, {payslipCount, format:'zip'})` **khi tạo lô** (cùng tx với hàng file + outbox). Mỗi lượt cấp URL ⇒ `file_access_logs` `GenerateSignedUrl` (append-only) — dấu giao tệp                                                                                                                                                                                                                                                                                | —                                             |
| E-12 | Consumer           | Event `payroll.payslip_pdf_batch.requested` payload **chỉ id** `{fileId, periodId, requestedBy}`; consumer `payroll-payslip-pdf-batch`. Hàng file không `Pending` ⇒ bỏ qua (idempotent). Đọc phiếu+item+tên trong MỘT tx ngắn, sinh PDF NGOÀI tx, `await setImmediate` sau mỗi phiếu, ZIP `STORE`, `put`, `markStoredTx`. Lỗi ⇒ `Failed` + `metadata.errorKind`, **không ném lại** (không retry mù 5 lượt)                                                                            | `payroll-payslip-pdf-batch.consumer.ts`       |
| E-13 | Tên trong consumer | Actor dựng lại cho `requestedBy` bằng CHÍNH đường `PayrollAccessService` (không actor hệ thống). Người yêu cầu mất cặp giữa chừng ⇒ `Failed` `errorKind:'forbidden'`                                                                                                                                                                                                                                                                                                                  | —                                             |
| E-14 | Vá TTL             | `createDownloadUrl/createUploadUrl(key, companyId, expiresInSec?)`; adapter truyền TTL đã kẹp ⇒ `expiresAt` = chữ ký thật. `putObject` thêm `assertKeyInTenant(key, companyId)` (đổi chữ ký có `companyId`)                                                                                                                                                                                                                                                                           | `storage/*`                                   |
| E-15 | Job dọn            | Nhánh tạm-hết-hạn **bỏ điều kiện «không link sống»** (tệp PAYROLL luôn có link); nhánh Pending giữ nguyên. Mỗi hàng: `storage.delete(key)` **TRƯỚC** (S3 xoá key vắng = thành công) → lỗi ⇒ `failed++`, giữ hàng, lượt sau thử lại → tx: xoá mềm hàng + xoá mềm link sống của nó + access log `Delete` + audit như cũ                                                                                                                                                                 | `temp-file-cleanup.*`                         |
| E-16 | Hạn                | Hằng `PAYSLIP_PDF_FILE_TTL_SEC = 900` · `PAYSLIP_PDF_BATCH_FILE_TTL_SEC = 86_400` · `PAYSLIP_PDF_BATCH_MAX = 2_000` (contracts)                                                                                                                                                                                                                                                                                                                                                       | contracts                                     |

## 0b. Vá plan-review vòng 1 (17/09) — ĐÈ lên §0.3–§6 ở mọi chỗ mâu thuẫn

Verdict vòng 1 = **REVISE** (4 BLOCKING + cảnh báo). Vá đủ 4/4 + nhận cảnh báo rẻ ⇒ **PASS, KHÔNG mở vòng 2**.
Ghi chú sai của reviewer: nhánh làm việc là `feat/s15-payroll-be-5b` (merge-base `4fbc5beb`), không phải nhánh BE-5.

| ID     | Lỗ                                                                                    | Vá                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------ | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **B1** | E-9 dùng `updated_at` không tồn tại; `payslips` append-only (app role SELECT+INSERT)  | Đã vá ở E-9 (`count` ‖ `max(created_at)` ‖ `md5(id)`). Bỏ ca int «sửa phiếu»; thay bằng unit bảng quyết định (vân tay lệch ⇒ lô mới)                                                                                                                                                                                                                                                                                         |
| **B2** | `@Idempotent()` chốt 201 trước handler + cache body có signed-URL 900 s               | **Owner O-8 (17/09): BỎ `@Idempotent()` ở 085.** Lấy-hoặc-tạo dưới advisory lock đã idempotent (bấm hai lần = một lô). Header vẫn nhận, không cache. SPEC-11 §15.1 + API-18 §5.1b ghi lệch như tiền lệ 076 (danh sách route idempotent v2 bớt 085). Ca int: POST hai lần cùng `Idempotency-Key` sau khi lô đổi trạng thái ⇒ lượt hai phản ánh trạng thái MỚI                                                                 |
| **B3** | Quên đăng ký consumer ⇒ event `done` ngay, lô `Pending` 24 h im lặng                  | (a) ca int batch chạy qua `OutboxWorker` thật (`test/helpers/outbox-drain.ts`), không gọi thẳng consumer; (b) ca boot: `bus.consumersFor('payroll.payslip_pdf_batch.requested').length === 1`; (c) `Pending` quá `PAYSLIP_PDF_BATCH_STALE_SEC = 900` ⇒ 085 trả `Failed{failure:'stale'}` (`retry:true` gỡ được) — có ca                                                                                                      |
| **B4** | Mảnh crown (job dọn · resolver · xoá object) mà gate hẹp; plan không nói đảo ca crown | **Gate = FULL**: `security-reviewer` + `database-reviewer` (advisory lock · vân tay · câu tìm lô · SQL job dọn · xoá mềm link hệ thống) + `silent-failure-hunter` + `typescript-reviewer`. **Không đảo ca crown**: nhánh tạm-hết-hạn chỉ bỏ qua link **`link_type='Export'`** (link khác vẫn chặn) ⇒ ca (1) `Attachment` giữ nguyên; thêm ca (1b) chỉ-có-link-Export ⇒ xoá + link xoá mềm; ca (4) Uploaded thường giữ nguyên |

Cảnh báo nhận (rẻ):

- **Consumer**: trần chạy `PAYSLIP_PDF_BATCH_RUNTIME_CAP_MS = 240_000` (< reaper 5′) ⇒ quá trần ⇒ `Failed{errorKind:'timeout'}`; `markStoredTx`/`markFailedTx` có guard `upload_status='Pending'` + kiểm số hàng; ghi `Failed` mà ném ⇒ **ném lại**; không thấy hàng ⇒ `warn` (không im lặng); `periodId`/`requestedBy` lấy từ HÀNG file + link (payload chỉ để tra `fileId`); E-13 assert CẢ `payslipPdfBatch` lẫn `payslipList` + user còn sống. Đo RSS đỉnh lô 2.000 trước PR (ghi §8).
- **Job dọn**: liệt kê trong tx → mỗi tệp: xoá object NGOÀI tx → tx ngắn xoá mềm + log + audit. `StorageDeleteInput` thêm `companyId`, `deleteObject` dùng `assertKeyInTenant`. Chỉ xoá object khi `storage_provider ∈ {MinIO, S3}`; provider khác giữ hành vi cũ (chỉ xoá mềm). PR ghi lệnh đếm `is_temporary=true` trên PROD/dev-online trước deploy.
- **URL**: chỉ dùng lại tệp khi `expires_at > now + presignTtl`; `issueUrlTx` lặp guard `fileDownloadStateDenyReason`. `Cache-Control: no-store` cho **cả 085**. Không log URL.
- **083/084 dùng lại tệp sống** của (người gọi, phiếu) ⇒ không sinh PDF mới mỗi lượt GET (chống spam); 083 vẫn audit mỗi lượt.
- **IDOR 085**: census ghim `payslipPdfBatch.companyFloor === true` và `payslipList.companyFloor === true`; câu chọn phiếu của lô AND `actor.peopleVisibleCond`.
- **Metadata**: `owner_user_id` = người gọi (không phải chủ phiếu).
- **Lời lỗi theo kind**: 031 `pdf-batch-too-large` lời riêng; 0 phiếu = **`PAYROLL-ERR-007`** kind `no-payslip-for-pdf` lời riêng (chốt, không «tra lúc code»).
- **Tên người đã xoá mềm** ⇒ PDF in `—` (unit có ca); tên entry ZIP dùng mã NV, thiếu ⇒ 8 ký tự `userId`.
- **Trần 2.000 tiêm qua provider** (`PAYSLIP_PDF_BATCH_LIMITS` token): int dùng trần 3; unit giữ biên trên hằng thật (2.000 qua / 2.001 chặn).
- **Deny thêm**: 083 cặp cấp Department ⇒ 403 · ALLOW đúng 1 audit; 084 có `view-payslip` thiếu `view-own-payslip` ⇒ 403 · phiếu tenant B ⇒ 404 · kỳ `Paid` ⇒ 200; 085 thiếu `export:payroll` ⇒ 403 · cặp Department ⇒ 403 · 0 phiếu ⇒ 007 + 0 file/audit/outbox · người yêu cầu mất cặp trước consumer ⇒ `Failed forbidden`.
- **Route file chung**: metadata/download-url/delete trên CẢ PDF lẫn ZIP ⇒ 403 cho company-admin; resolver cài cả `canUnlinkFile`.
- **Paths WO**: thêm `docs/_review/**` (route-census artifact). pdfjs-dist kéo optional `@napi-rs/canvas` (MIT) — `pnpm audit --audit-level=high` đã sạch sau khi cài.
- **Không làm**: kill-switch env (YAGNI — rollback = revert; consumer vắng ⇒ B3(c) trả `stale`).

## 1. Ranh giới

- TRONG: 3 route · `ServerFileService` · resolver PAYROLL · vá TTL + `putObject` · job dọn xoá object · deps `pdfmake`/`jszip` (+ dev `pdfjs-dist`) · census 82→85 · FE wiring/error-kind · SPEC-11/API-18 đánh dấu đã hiện thực + ghi O-4.
- NGOÀI: UI nút PDF (FE-4) · ký số PDF · gửi PDF qua email · đổi `FileService.upload/getDownloadUrl` · xoá object của tệp ĐÃ xoá mềm trước WO này (job chỉ chọn hàng `deleted_at IS NULL`).

## 2. Route

| Mã  | Route                                                                        | Decorator                  | Service assert thêm | Audit                                                           |
| --- | ---------------------------------------------------------------------------- | -------------------------- | ------------------- | --------------------------------------------------------------- |
| 083 | `GET /payslips/:id/pdf`                                                      | `view-payslip:payslip`     | `periodExport`      | `(payslip, id, {format:'pdf'})`                                 |
| 084 | `GET /me/payslips/:id/pdf`                                                   | `view-own-payslip:payslip` | —                   | KHÔNG                                                           |
| 085 | `POST /payroll-periods/:id/payslips/pdf-batch` (KHÔNG `@Idempotent()` — O-8) | `export:payroll`           | `payslipList`       | `(payroll_period, id, {payslipCount, format:'zip'})` khi tạo lô |

## 3. File

- `packages/contracts/src/payroll-pdf.ts` (+ `index.ts`): DTO 083/084/085, body 085 `.strict()`, hằng E-16.
- `apps/api/src/storage/object-storage.service.ts` · `s3-storage.adapter.ts` · `storage-adapter.port.ts` (E-14) + spec.
- `apps/api/src/foundation/files/server-file.service.ts` (+ spec) · `files.module.ts` (export) · `temp-file-cleanup.job-handler.ts` · `temp-file-cleanup.repository.ts` (+ spec).
- `apps/api/src/payroll/`: `payslip-pdf.document.ts` · `payslip-pdf.renderer.ts` · `payroll-payslip-pdf.service.ts` · `payroll-payslip-pdf-batch.service.ts` · `payroll-payslip-pdf-batch.consumer.ts` · `payroll-export-file.resolver.ts` · `payroll-pdf.controllers.ts` · `payroll-route-pairs.const.ts` · `payroll.errors.ts` · `payroll.module.ts` (import `FilesModule` + `StorageModule`; `onModuleInit` đăng ký resolver + consumer) · spec unit từng file.
- `apps/api/test/integration/s15-payroll-be5b-{pdf-gates,pdf,pdf-batch,temp-file-cleanup}.int-spec.ts` · helper đọc PDF `apps/api/test/helpers/pdf-text.ts`.
- Census: `payroll-two-layer-guard-census` 82→85 · `route-http-coverage` MIN +3 · route-census artifact (`ROUTE_CENSUS_WRITE=1`) · `payroll-error-code-census` (kind mới) · FE `constants.ts`/`payroll-wiring.spec.ts` 82→85 · `payroll-errors.ts` + census kind + i18n.
- `apps/api/package.json` + `pnpm-lock.yaml`; `docs/SPEC/SPEC-11 PAYROLL.md` · `docs/API Design/API-18_PAYROLL_API_Design.md` (083–085 ✅ + O-4 lấy-hoặc-tạo).

## 4. RED-first (chạy ĐỎ đúng lý do trước khi code route)

1. **Gates** — 083: thiếu `export:payroll` ⇒ 403 · thiếu `view-payslip` ⇒ 403 · phiếu tenant B ⇒ 404 · 0 audit mọi ca deny. 084: phiếu người khác ⇒ 404 010 · kỳ `Approved` ⇒ 404 · kỳ **đúng `Published`** ⇒ 200 (SPEC-11 §13.2 (c)) · 0 audit kể cả ALLOW. 085: thiếu `view-payslip` ⇒ 403 · kỳ tenant B ⇒ 404 · 2.001 phiếu (gieo `generate_series`) ⇒ 422 031 `pdf-batch-too-large` + 0 file + 0 audit + 0 outbox.
2. **PDF** — 083 ALLOW: tải url thật từ MinIO ⇒ `pdfjs-dist` trích văn bản chứa `PHIẾU LƯƠNG`, họ tên có dấu của fixture (`Nguyễn Thị Ánh Tuyết`), nhãn khoản có dấu, số net đúng snapshot (fixture tiền lẻ `19.999.999,99`); PDF có `/FontFile2` và **không** có `/Helvetica`. Hàng `files`: `is_temporary`, `expires_at ≈ now+900 s`, link `PAYROLL/Export`; access log `GenerateSignedUrl`; `expiresAt` phản hồi = `X-Amz-Expires` trong url. **Company-admin gọi `/foundation/files/:id/download-url` ⇒ 403** (resolver). Sửa `payslip_items` sau khi sinh? — không cần: snapshot bất biến; ca đối chiếu là số của `payslip_items`, không phải của dòng kỳ.
3. **Batch** — POST ⇒ 202 Pending + 1 audit + 1 outbox; POST lại (key khác) ⇒ 202 cùng `fileId`, 0 audit thêm; chạy consumer ⇒ POST ⇒ 200 + url ⇒ ZIP có đúng N entry, mỗi entry trích được tên đúng người; sửa phiếu (`updated_at`) ⇒ POST ⇒ lô mới; consumer gặp lỗi `put` (adapter giả) ⇒ `Failed` ⇒ POST ⇒ 200 `Failed` ⇒ POST `retry:true` ⇒ 202 lô mới; người khác POST cùng kỳ ⇒ lô riêng (không thấy lô của A).
4. **Job dọn** — tệp tạm hết hạn CÓ link + object thật ⇒ sau `run` `headObject` 404, hàng `Deleted`, link xoá mềm; adapter `delete` ném ⇒ hàng giữ nguyên, `failed=1`; Pending > 24 h không link ⇒ object bị xoá (O-6); tệp tạm CHƯA hết hạn ⇒ không đụng.
5. **Unit** — document builder (thứ tự khoản, định dạng tiền chuỗi, không `Number`), resolver từ chối cả 5 thao tác, `resolveTtl` → chữ ký, `putObject` từ chối key tenant khác, quyết định lấy-hoặc-tạo (bảng trạng thái), consumer bỏ qua hàng không Pending.

## 5. Cổng & bằng chứng

`bash harness/check.sh --lane-db=be5b` · `vitest run test/foundation` (bài học DEBT-1) · `test:cov:payroll` ≥ 85 % · `pnpm audit --audit-level=high` sạch · đo thời gian 1 PDF (< 1,5 s, log) · typescript-reviewer + security-reviewer HẸP + silent-failure-hunter (job dọn + consumer) · PR base master, không auto-merge; STATUS regen SAU merge.

## 6. Rủi ro

| #   | Rủi ro                                                | Chặn                                                                                                  |
| --- | ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| R1  | Mất dấu tiếng Việt im lặng                            | Font nhúng tường minh + ca trích văn bản đọc lại + assert `/FontFile2`, không `/Helvetica`            |
| R2  | Tệp PAYROLL tải qua route file chung                  | Link bắt buộc (E-4 trong cùng tx với hàng) + resolver từ chối + ca int company-admin 403              |
| R3  | Job dọn xoá object của tệp còn dùng (mọi module, O-6) | Chỉ đổi nhánh `is_temporary` (không module nào khác set cờ này) + nhánh Pending giữ «không link sống» |
| R4  | Object mồ côi                                         | Hàng trước, object sau (E-4); xoá object trước, hàng sau (E-15)                                       |
| R5  | ZIP cũ sau khi phiếu sinh lại                         | Vân tay E-9                                                                                           |
| R6  | Bộ nhớ lô 2.000 phiếu                                 | Đo kích thước 1 PDF lúc code; > 100 KB/phiếu ⇒ ghi lại + cân nhắc ghi luồng (không đổi hợp đồng)      |
| R7  | Sự kiện outbox khác trễ khi có lô                     | Owner chấp nhận (O-5); `setImmediate` giữ event loop thông cho HTTP                                   |
| R8  | Supply chain (pdfmake → pdfkit mới)                   | `pnpm audit --audit-level=high` tại chỗ; đỏ ⇒ `overrides` range-scoped, không suppress                |

## 8. Bằng chứng + việc còn lại (17/09/2026 — CHECKPOINT, chưa PR; owner chốt vá ở phiên mới vì hook chi phí ~$860)

### 8.1 Đã chạy (lane `mediaos_be5b`, MinIO thật)

| Cổng                                 | Kết quả                                                                                                                                        |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| Int 083/084 `s15-payroll-be5b-pdf`   | **13/13** — PDF tải thật, đọc lại chữ có dấu (`pdfjs-dist`), Roboto nhúng, `expiresAt` = `X-Amz-Expires`, route file chung 403                 |
| Int 085 `s15-payroll-be5b-pdf-batch` | **15/15** — qua `OutboxWorker` thật, cùng `Idempotency-Key` vẫn thấy trạng thái mới, vân tay · Failed/retry · stale · mất cặp ⇒ `forbidden`    |
| Int `temp-file-cleanup` (crown)      | **14/14** — 9 ca cũ giữ nguyên (ca (1) `Attachment` KHÔNG đảo) + 5 ca object MinIO thật (1b · 1c · 3b · 6 · 4b)                                |
| Unit mới                             | document/renderer 21 · decision 20 · document-input 5 · resolver 7 · server-file 13 · consumer + trần 17 · cleanup handler 10 · storage 55     |
| `test/foundation` (lane)             | **42 file · 454/454** (census 2 tầng 85 · route-census regen · `MIN_COVERED_COUNT` 629 → 632 đo 632/632)                                       |
| FE                                   | `payroll-wiring` + `payroll-error-kind-census` **37/37**                                                                                       |
| Typecheck · audit                    | `tsc --noEmit` API sạch · `pnpm audit --audit-level=high` sạch (2 low · 8 moderate có sẵn)                                                     |
| Đột biến                             | 5/5 ĐỎ đúng ca: bỏ assert `periodExport` (083) · bỏ link Export · bỏ miễn trừ Export ở job dọn · bỏ assert `payslipList` (085) · bỏ so vân tay |
| Đo                                   | 1 PDF ≈ **18 ms · 24 KB** ⇒ lô 2.000 ≈ 36 s · ~49 MB (dưới ngưỡng R6)                                                                          |
| `harness/check.sh --lane-db=be5b`    | xem §8.3                                                                                                                                       |

Reviewer FULL gate (mỗi người một lượt, đọc tĩnh): **security PASS** · **database PASS** · **silent-failure PASS** · **typescript PASS** (dừng sớm vì hook chi phí — chưa đọc sâu `server-file.*`/`storage/*`/`temp-file-cleanup.*`; security + database đã phủ các file đó).

### 8.2 PHẢI vá trước PR (phiên sau — đọc mục này, KHÔNG chạy lại reviewer từ đầu)

1. **HIGH (database + security M3) — TOCTOU job dọn.** `temp-file-cleanup.job-handler.ts`: ngay TRƯỚC `storage.delete` mở tx ngắn kiểm lại ĐÚNG vị từ chọn cho riêng tệp đó (tách `eligibleWhere(companyId, pendingCutoff, now)` trong repository, thêm `isStillEligibleTx(companyId, fileId, …)`); lặp lại kiểm đó trong tx xoá mềm. Không còn đủ điều kiện ⇒ `skipped`, không xoá object. Unit: harness thêm mock `isStillEligibleTx` + ca «hết đủ điều kiện ⇒ không gọi storage».
2. **HIGH (silent-failure #1) — consumer `load()` không được bọc.** `payroll-payslip-pdf-batch.consumer.ts#handle`: bọc `load()`; lỗi ⇒ `logger.error(fileId, err.name)` + `markFailed(generation-failed)` rồi NÉM lại (outbox thử lại; hàng đã Failed nên lượt sau `not-pending` bỏ qua). Unit: `batchPayslipsTx` ném ⇒ markFailed + rejects.
3. **MEDIUM (security M1) — `FileService.link()` gắn link thật lên tệp tạm** ⇒ job dọn bỏ qua mãi (phá O-7). Trong tx của `link()`: `file.isTemporary` ⇒ `BadRequestException(LINK, "tệp tạm do hệ thống sinh không gắn link được")`. Int (spec 085): company-admin có `link:foundation-file` `POST /foundation/files/:id/links` (`moduleCode:"FOUNDATION"`) lên ZIP ⇒ 400 + số link sống không đổi.
4. **MEDIUM (security M2) — `confirmUpload` không qua resolver** ⇒ admin làm hỏng lô Pending (ghi đè metadata, mất vân tay). Sau `if (!row)`: `row.isTemporary` ⇒ `NotFoundException` (404 sentinel). Int: lô Pending (chưa drain) + admin có `upload:foundation-file` `POST /foundation/files/:id/confirm` ⇒ 404, hàng vẫn Pending + còn `metadata.fingerprint`.
5. **MEDIUM (database) —** `temp-file-cleanup.repository.ts` NOT EXISTS: viết chữ `files.is_temporary` thay `${files.isTemporary}` (cột nhúng render không kèm tên bảng).
6. **MEDIUM (silent-failure #2) —** `markFailed()` bỏ qua số hàng: `0` ⇒ `logger.warn`.
7. **MEDIUM (typescript) —** `payroll-payslip-pdf.repository.ts` import `rowsOf` từ `payroll-report.sql.ts` (bỏ bản chép); `payroll-payslip-pdf.service.ts` ba `await` viết tuần tự thường (cùng `tx`); tách `PAYROLL_FILE_MODULE` · `PAYSLIP_PDF_ENTITY` · `PAYSLIP_PDF_BATCH_ENTITY` · `PAYSLIP_PDF_BATCH_EVENT` sang `payroll-pdf.const.ts`.
8. **LOW/INFO —** `payroll-payslip-pdf.service.ts` thêm `Logger` + lớp lỗi có tên cho nhánh «không còn Pending»; consumer chưa loại tài khoản bị khoá (chỉ loại xoá mềm — không rò vì họ không đăng nhập tải được); key lệch tenant ⇒ `failed` mãi (an toàn, chỉ nhiễu log) — ghi nợ, không chặn PR.

Sau vá: chạy lại `unit` các file đụng + 3 int-spec BE-5B + `temp-file-cleanup.int-spec` + `vitest run test/foundation` (lane) + `test:cov:payroll` ≥ 85 % ⇒ commit ⇒ PR base master, KHÔNG auto-merge. PR ghi: đếm `files.is_temporary = true` trên PROD/dev-online TRƯỚC deploy (job dọn giờ xoá object thật).

### 8.3 `harness/check.sh --lane-db=be5b` (17/09) — ĐỎ 4 file thật, đều do WO này (vá cùng §8.2)

secret-literals · lint · typecheck · migration-no-drop · tooling ✅ · FE/contracts/ui/web-core ✅ · API **722 file, 4 đỏ thật**:

9. `test/helpers/outbox-worker-lock.unit-spec.ts` — luật S7-QA-OUTBOXPROBE-1: spec lái `OutboxWorker` PHẢI giữ mutex. Vá: `s15-payroll-be5b-pdf-batch.int-spec.ts` gọi `acquireOutboxWorkerLock` (xem `test/helpers/outbox-worker-lock.ts`) quanh `drain()`.
10. `test/integration/s13-payroll-db1-invariants.int-spec.ts` D1 (48 ≠ 32) — ROOT-CAUSE: `s15-payroll-be5b-pdf.int-spec.ts` `UPDATE companies SET name = 'Công ty Cổ phần Ánh Dương'` ⇒ lọc `NOT_FIXTURE_TENANT` (`c.name = 'Company ' || c.slug`) không nhận ra tenant fixture ⇒ role của spec chạy song song bị đếm. Vá: KHÔNG đổi tên công ty; assert `Company ${A.slug}` (chữ có dấu của tên công ty đã có ở unit `payslip-pdf.document.spec.ts`).
    11–12. `test/integration/s13-payroll-qa1-scope-floor.int-spec.ts` mục E (82 ≠ 85; ROUTES ∪ EXEMPT lệch) — thêm `payslipPdf` + `payslipPdfBatch` vào ROUTES (mục A — kèm ca HTTP sàn scope Department ⇒ 403 như các route khác của file) và `mePayslipPdf` vào EXEMPT_KEYS (mục C — Own); nâng neo 82 → 85 (78 → 80 · 4 → 5); cập nhật tiêu đề (32 + 3).
