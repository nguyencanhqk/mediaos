```yaml
wo: S4-NOTI-BE-2
zone: red
generated_by: auto-loop
reconciled_at: "10447b0"
lanes: [{"id":"L0-contracts","task":"Additive contracts: enum TitleCase (notificationTypeEnumSchema, notificationPrioritySchema khớp CHECK 0479:257/260) + internalEventIntakeSchema (eventCode, actorUserId?, sourceModule, sourceEntityType?, sourceEntityId?, dedupeKey?, recipient{mode,userIds[],employeeIds[]}, payload, priorityOverride?, occurredAt? — KHÔNG có company_id) + intakeSummarySchema (createdCount/skippedCount/dedupedCount). KHÔNG internalDirectSendSchema (đẩy BE-3). KHÔNG sửa notificationSchema legacy lowercase. Dual-build.","builder":"backend-builder","paths":["packages/contracts/src/notification.ts","packages/contracts/src/index.ts"]},{"id":"L1-repos","task":"3 repo đọc catalog + createFromEngine dual-write. findEnabledEvent(company-override>global, is_enabled, deleted_at NULL) · findActiveTemplate(status='Active', company-override>global, is_default fallback) · insertLog(CHỈ INSERT terminal, GRANT SELECT,INSERT — cấm UPDATE). createFromEngine PHẢI dual-write cột MỚI (recipient_user_id, event_id, event_code, notification_type, priority, status='Unread', title, dedupe_key, source_entity_*, target_*, payload, module_code) VÀ legacy NOT NULL (user_id=recipient, body=rendered, type='general', is_read=false) — bỏ cột mới ⇒ partial-unique coi NULL distinct ⇒ dedupe hỏng im lặng.","builder":"backend-builder","paths":["apps/api/src/notifications/notification-event.repository.ts","apps/api/src/notifications/notification-template.repository.ts","apps/api/src/notifications/notification-delivery-log.repository.ts","apps/api/src/notifications/notifications.repository.ts"]},{"id":"L2-engine","task":"Pipeline (mỗi file ≤400 dòng): resolver (actor-exclusion trừ is_system_event, lọc active/cùng-company, RLS ẩn cross-tenant) + renderer (template→title/body/target_url; missing/inactive→fallback non-silent metadata.reason='template_fallback') + dedupe (computeKey theo strategy: TimeWindow bucket floor(epoch/window), EntityRecipient/DedupeKey ổn định, None→không set dedupe_key; DEFAULT_DEDUPE const cho TASK_COMMENT_CREATED/TASK_STATUS_CHANGED window 300s khi catalog='None', catalog override được; app isDuplicate) + engine.intake (withTenant, SAVEPOINT sp_recipient MỖI recipient: 23505→ROLLBACK TO→deduped_count++ tx ngoài sống). Validate target_url nội bộ + ban key nhạy cảm. Error-code const NOTI-ERR-TARGET-UNAVAILABLE/NOTI-ERR-TEMPLATE-VARIABLE-INVALID/NOTI-ERR-EVENT-NOT-FOUND. Event-level skip→audit 'notification_skipped'+skipped_count, KHÔNG delivery_log. WS emit SAU commit ngoài withTenant, best-effort qua DTO mask.","builder":"backend-builder","paths":["apps/api/src/notifications/notification-recipient-resolver.service.ts","apps/api/src/notifications/notification-renderer.service.ts","apps/api/src/notifications/notification-dedupe.service.ts","apps/api/src/notifications/notification-dedupe.const.ts","apps/api/src/notifications/notification-engine.service.ts","apps/api/src/notifications/notification-engine.errors.ts"]},{"id":"L3-http","task":"1 controller @Controller('internal/v1/notifications') @UseGuards(InternalGuard) @UsePipes(ZodValidationPipe), route DUY NHẤT POST /events, KHÔNG @Public (JwtAuthGuard toàn cục ⇒ 401 thiếu Bearer), companyId=req.user.companyId, body.company_id≠token→400. Wire providers engine/resolver/renderer/dedupe/3-repo + controller khối ADDITIVE vào notifications.module.ts (đã có MyNotifications*+PermissionModule sau #133). KHÔNG đăng ký consumer eventType TASK. Mẫu duyệt: attendance/attendance-internal.controller.ts.","builder":"backend-builder","paths":["apps/api/src/notifications/internal-notifications.controller.ts","apps/api/src/notifications/notifications.module.ts"]},{"id":"L4-test","task":"Int-spec RED-trước, deny-path đi đầu. Gate cứng const runDb=hasDb && Boolean(process.env.LANE_DB); describe.skipIf(!runDb). Bootstrap Nest thật (JwtAuthGuard→CompanyGuard→InternalGuard→controller), seed company A+B+users (actor/recipient/recipient-B) qua directPool, set INTERNAL_API_KEY. 8 nhóm a–h §7.","builder":"backend-builder","paths":["apps/api/test/integration/noti-event-intake.int-spec.ts"]}]
acceptanceChecks: ["Chỉ POST /internal/v1/notifications/events tồn tại trong diff; grep KHÔNG thấy route /send hay internalDirectSendSchema (đẩy S4-NOTI-BE-3).","Trust boundary fail-closed: không Bearer→401; JWT hợp lệ + thiếu x-internal-key→403; sai key→403; INTERNAL_API_KEY unset→403; controller KHÔNG có @Public.","company_id lấy TỪ TOKEN (withTenant(req.user.companyId)); body.company_id≠token→400; recipient company khác vô hình qua RLS FORCE (resolve 0 row).","Dedupe 2 tầng có SAVEPOINT sp_recipient mỗi recipient: unique-violation 23505→ROLLBACK TO→deduped_count++, tx ngoài sống, recipient khác VẪN tạo, KHÔNG 500; ép app isDuplicate + backstop uq_notifications_dedupe_active(company_id,recipient_user_id,event_code,dedupe_key).","createFromEngine dual-write: notification tạo ra có recipient_user_id/event_code/dedupe_key NOT NULL (khi có dedupe) + notification_type/priority/status='Unread'/title/event_id + legacy user_id/body/type='general'/is_read=false; thiếu cột mới ⇒ backstop chết im lặng, thiếu legacy ⇒ INSERT fail.","notification_delivery_logs chỉ INSERT-terminal ('Sent'/'Skipped'/'Failed'), KHÔNG UPDATE (GRANT SELECT,INSERT). Event-level skip (disabled/0 recipient/resolver lọc inactive)→0 delivery_log (FK notification_id NOT NULL), chỉ audit 'notification_skipped' + skipped_count.","Actor-exclusion: bỏ actorUserId khỏi recipients TRỪ is_system_event=true (SYSTEM_* seed 0481:85-86,100-103).","target_url phải là route nội bộ (bắt đầu '/', chặn http:/https:/javascript:/data: và '//' và '\\') → ngoài whitelist reject 422 NOTI-ERR-TARGET-UNAVAILABLE (loud, KHÔNG strip im lặng); payload chứa key nhạy cảm (password/token/salary/bank_account/identity_number)→400 NOTI-ERR-TEMPLATE-VARIABLE-INVALID.","WS emit nằm SAU COMMIT, ngoài withTenant, best-effort/no-throw, qua DTO đã mask, CẤM io.emit raw row.","Template missing/inactive→fallback non-silent (metadata.reason='template_fallback'), vẫn tạo notification + delivery_log 'Sent'.","DEFAULT_DEDUPE const áp cho TASK_COMMENT_CREATED/TASK_STATUS_CHANGED (window 300s) khi catalog dedupe_strategy='None'; catalog override được; None ngoài default ⇒ không set dedupe_key.","/events fire-and-forget: disabled/dedupe trả 200 + summary (skippedCount/dedupedCount); 422 EVENT-DISABLED & 409 DEDUPE-CONFLICT KHÔNG khai ở BE-2 (thuộc BE-3, mã treo không có deny-path).","KHÔNG thêm migration (head giữ 0483); KHÔNG wiring TASK→NOTI; KHÔNG đăng ký consumer eventType TASK; mỗi file ≤400 dòng (tách engine/resolver/renderer/dedupe).","Contracts additive dual-build (enum legacy lowercase KHÔNG đụng); typecheck + check.sh (TURBO_FORCE=1) xanh; DoD §8: validation input, audit hành động quan trọng, secret không log.","FULL gate security-reviewer + silent-failure-hunter + database-reviewer PASS; int-spec a–h xanh dưới LANE_DB."]
testTasks: ["Gate cứng: const runDb = hasDb && Boolean(process.env.LANE_DB); describe.skipIf(!runDb)(...). Bootstrap Nest app thật (JwtAuthGuard→CompanyGuard→InternalGuard→controller); seed company A + company B + users (actor, recipient, recipient-B) qua directPool; set process.env.INTERNAL_API_KEY.","(a) DENY-PATH RED — untrusted context: không Bearer→401; JWT hợp lệ + thiếu x-internal-key→403; sai key→403; INTERNAL_API_KEY unset→403.","(b) dedupe app: TASK_COMMENT_CREATED cùng sourceEntityId + recipient, 2 lần trong window → created=1, deduped=1, đúng 1 notification; bucket kế → tạo mới.","(b2) dedupe backstop DB + savepoint: chèn TRƯỚC row xung đột qua directPool (cùng company_id/recipient_user_id/event_code/dedupe_key), intake 2 recipient → recipient trùng deduped, recipient còn lại VẪN tạo, response KHÔNG 500; assert notification có recipient_user_id/event_code/dedupe_key NOT NULL.","(c) actor-exclusion: actor∈recipients event non-system → actor 0 notification; is_system_event=true → actor có.","(d) cross-tenant: recipient thuộc company B → không tạo; body.company_id=B, token=A → 400.","(e) event disabled: 0 notification, 0 delivery_log, đúng 1 audit_logs 'notification_skipped', response 200 + skippedCount≥1, KHÔNG 500; verify AuditService.record chấp nhận objectId undefined TRƯỚC khi khoá assert.","(f) template missing: notification fallback + dấu vết non-silent (assert metadata.reason='template_fallback' khác path thường).","(g) target ngoài: target_url = https://evil.com / //evil / javascript: → 422; payload chứa salary/token → 400.","(h) happy-path: TASK_ASSIGNED + mode=UserIds → 1 notification (đủ cột legacy + mới) + 1 delivery_log 'Sent' attempt_no=1.","FULL gate: security-reviewer + silent-failure-hunter + database-reviewer PASS."]
steps: ["L4 RED trước — viết int-spec 8 nhóm (a–h), chạy phải ĐỎ (chưa có route/engine).","L0 contracts — additive enum TitleCase + internalEventIntakeSchema + intakeSummarySchema vào packages/contracts/src/notification.ts, export ở index.ts; KHÔNG sửa notificationSchema legacy; KHÔNG internalDirectSendSchema. pnpm --filter @mediaos/contracts build.","L1 repos — 3 repo đọc catalog (findEnabledEvent/findActiveTemplate/insertLog INSERT-terminal) + createFromEngine dual-write (cột mới + legacy NOT NULL) vào notifications.repository.ts.","L2 engine — dedupe.const (DEFAULT_DEDUPE) → dedupe.service → recipient-resolver → renderer → engine.service (SAVEPOINT/withTenant/audit-skip/WS-sau-commit) + engine.errors const. Mỗi file ≤400 dòng.","L3 http — internal-notifications.controller (1 route POST /events, InternalGuard, companyId từ token, body-company≠token→400) + wire providers khối additive vào notifications.module.ts.","L4 GREEN — bash scripts/lane-db-setup.sh notibe2 → export LANE_DB=mediaos_notibe2 → pnpm --filter @mediaos/api test; rồi typecheck + check.sh TURBO_FORCE=1.","Verify AuditService.record chấp nhận objectId undefined (đã xác minh: objectId?: string optional) TRƯỚC khi khoá assert test (e).","FULL gate: security-reviewer + silent-failure-hunter + database-reviewer PASS; ghi deviation SPEC-08 §19 (422 EVENT-DISABLED/409 DEDUPE-CONFLICT thuộc BE-3) vào code comment."]
```

GAP-ANALYSIS (reconcile 2026-07-10 vs code hiện tại):
- ĐÃ ĐÓNG: PR #133 (S4-NOTI-BE-1) MERGE (commit 64d4787) → notifications.module.ts nay đã có MyNotificationsController/Service/Repository + import PermissionModule. Rủi ro "rebase sau #133" trong plan §2.2/§8 KHÔNG còn; L3 chỉ append providers engine + InternalNotificationsController khối additive, không conflict (đã ở master history). Head migration = 0483 (WO thêm 0 migration).
- CÒN MỞ (chưa build): toàn bộ pipeline chưa tồn tại — apps/api/src/notifications KHÔNG có internal-notifications.controller / engine / resolver / renderer / dedupe. contracts/notification.ts vẫn enum legacy lowercase (notificationTypeSchema:3), CHƯA có internalEventIntakeSchema/intakeSummarySchema. L0–L4 đều TODO.

SỰ THẬT ĐÃ XÁC MINH (code, không đoán):
- Migrations ở apps/api/migrations/ (KHÔNG phải src/db/migrations). uq_notifications_dedupe_active ON (company_id,recipient_user_id,event_code,dedupe_key) WHERE dedupe_key IS NOT NULL AND deleted_at IS NULL (0479:279-281) — TOÀN cột MỚI ⇒ dual-write bắt buộc.
- notification_delivery_logs GRANT SELECT,INSERT app (0479:205) — APPEND-ONLY, INSERT-terminal, cấm UPDATE. notification_id FK NOT NULL ⇒ event-level skip không ghi được delivery_log.
- notifications legacy NOT NULL: user_id, type(DEFAULT 'general'), body, is_read (0479:210); cột mới đều nullable; notifications GRANT SELECT/INSERT/UPDATE đã land 0010 ⇒ createFromEngine INSERT hợp lệ. CHECK notification_type TitleCase (0479:257), priority (0479:260), status IN ('Unread','Read','Hidden','Archived','Deleted','Failed') (0479:251), module_code IN (AUTH,HR,ATT,LEAVE,TASK,DASH,NOTI,SYSTEM) (0479:254).
- Seed 0481: is_system_event=true cho SYSTEM_* (85-86,100-103); dedupe_strategy KHÔNG set (0 lần) ⇒ mọi event nhận DEFAULT 'None' ⇒ cần DEFAULT_DEDUPE const cho TASK_COMMENT_CREATED/TASK_STATUS_CHANGED. GRANT SELECT-only cho notification_events/notification_templates.
- InternalGuard (permission/guards/internal.guard.ts:22-33): so x-internal-key với process.env INTERNAL_API_KEY; env unset→throw ForbiddenException (fail-closed 403), không log key. JwtAuthGuard là APP_GUARD toàn cục.
- AuditService.record(tx, entry) — objectId?: string OPTIONAL (audit.service.ts:18,124) ⇒ audit event-skip với objectId undefined AN TOÀN; vẫn verify runtime trước khi assert test (e).
- INTERNAL_API_KEY KHÔNG có trong env.schema.ts (guard đọc thẳng process.env, fail-closed) ⇒ KHÔNG sửa env.schema.ts (ngoài paths).

BẤT BIẾN (CLAUDE.md §2/§3): #1 company_id từ TOKEN + withTenant + RLS FORCE (không lấy từ body); #2 append-only delivery_log (INSERT-terminal), không hard-delete; #3 INTERNAL_API_KEY từ env, không log/không vào DTO. Trust boundary CROWN: cả JWT + internal-key (defense-in-depth), không @Public. Dedupe PHẢI SAVEPOINT mỗi recipient (Postgres abort cả tx khi 23505) — bắt 23505 trong cùng tx không có savepoint ⇒ 'current transaction is aborted' mất notification recipient khác hoặc 500.

VERIFY: DB cô lập bash scripts/lane-db-setup.sh notibe2 → export LANE_DB=mediaos_notibe2 → pnpm --filter @mediaos/api test (gate hasDb && LANE_DB, tránh false-green/false-red band 0479–0483). typecheck + check.sh TURBO_FORCE=1 (tránh turbo cache xanh giả).

GATE: FULL (red/crown trust boundary) — security-reviewer + silent-failure-hunter + database-reviewer. Model routing crown: planner Sonnet+xhigh lập micro-plan (đã có, plan v2), IMPLEMENT/REVIEW Opus. RED deny-path (a) TRƯỚC.

OUT-OF-SCOPE (chống scope creep): POST /internal/v1/notifications/send + internalDirectSendSchema → S4-NOTI-BE-3 (direct-send bỏ qua catalog, admin tự soạn title/body/target_url/payload — bề mặt rủi ro cao hơn, WO riêng có test riêng). Wiring TASK→NOTI (đăng ký consumer eventType) → S4-INT-1 (test bằng event giả qua HTTP intake). My-notification APIs → S4-NOTI-BE-1 (#133 done). bulk-send/reminder job/retry job → BE-3. Kênh EMAIL/PUSH/REALTIME thật (chỉ IN_APP). KHÔNG migration; KHÔNG sửa env.schema.ts. Mã 422 EVENT-DISABLED/409 DEDUPE-CONFLICT KHÔNG khai ở BE-2 (thuộc /send BE-3) — ghi deviation SPEC-08 §19 vào code comment cho BE-3.

RỦI RO CẦN NGƯỜI CHỐT (red-zone trước merge): (1) savepoint bị bỏ quên → race dedupe hỏng batch (test b2 là lưới); (2) quên dual-write cột mới → dedupe hỏng IM LẶNG (chỉ b2 phát hiện, b vẫn xanh vì app chặn); (3) quên legacy NOT NULL → INSERT fail toàn bộ; (4) company_id từ body thay vì token → cross-tenant spoof; (5) @Public nhầm → mất lớp JWT; (6) enum lẫn legacy lowercase vs mới TitleCase.
