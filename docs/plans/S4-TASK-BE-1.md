```yaml
wo: S4-TASK-BE-1
zone: yellow
generated_by: auto-loop
reconciled_at: "608b008"
lanes: [{"id":"L1-schema-sync","task":"ĐỒNG BỘ Drizzle ORM schema với migration 0478 đã chạy (KHÔNG tạo .sql mới — head giữ 0485). Thêm cột NULLABLE additive vào bảng `projects` (project_status, project_priority, owner_employee_id, department_id, visibility, project_code, completed_at, closed_at/by, archived_at/by, cancelled_at/by, cancel_reason, progress_percent, metadata, created_by, updated_by, deleted_by) và `project_members` (employee_id, project_role, member_status, joined_at, left_at, invited_by, removed_by, remove_reason, metadata, created_by, updated_by, deleted_by) trong media.ts — GIỮ NGUYÊN cột+CHECK+index cũ, ĐẶC BIỆT user_id NOT NULL + project_members_active_uq(company_id,project_id,user_id) WHERE deleted_at IS NULL vẫn LIVE (media.ts:420-439) — KHÔNG nới lỏng. Tạo bảng typed `taskActivityLogs` (append-only, KHÔNG deleted_at) khớp DDL 0478 §5 ở file MỚI schema/task-activity.ts, export append vào schema/index.ts. KHÔNG db:generate (RLS/grant/CHECK do 0478 đã áp DB thật).","builder":"db-migration","paths":["apps/api/src/db/schema/media.ts","apps/api/src/db/schema/task-activity.ts","apps/api/src/db/schema/index.ts"]},{"id":"L2-contracts","task":"Zod DTO (dual-build) cho Project domain — append vào packages/contracts/src/task.ts (tái dùng barrel index đã re-export task.ts): projectStatusSchema (Planning|Active|On Hold|Completed|Cancelled|Archived) · projectPrioritySchema (Low|Medium|High|Urgent) · projectRoleSchema (Owner|Manager|Member|Viewer) · createProjectSchema (name required, code?, description?, ownerEmployeeId?, departmentId?, priority?, startDate?, endDate? với refine endDate>=startDate) · updateProjectSchema · closeProjectSchema · projectResponse/projectListItem · listProjectsQuerySchema (status/ownerEmployeeId/search + limit/offset clamp, z.preprocess idempotent cho query param) · addMemberSchema (employeeId + projectRole — user_id KHÔNG nhận từ client, server tự resolve) · updateMemberRoleSchema · memberResponse. Validate tại boundary, infer type export.","builder":"backend-builder","paths":["packages/contracts/src/task.ts"]},{"id":"L3-projects-impl","task":"ProjectsController + ProjectsService + ProjectsRepository + TaskActivityService (helper ghi task_activity_logs append-only trong cùng tx) dưới apps/api/src/tasks/ (projects.controller.ts, projects.service.ts, projects.repository.ts, task-activity.service.ts). Route dưới prefix /projects, mỗi route @UseGuards(PermissionGuard)+@RequirePermission đúng cặp seed 0485. Mọi query qua db.withTenant(companyId)+lọc company_id tường minh. (a) DATA-SCOPE ĐỌC BẮT BUỘC: GET /projects + GET /:id + GET /:id/members resolve strongest read scope (DataScopeService.resolveStrongestScope) rồi buildEmployeeScopeCondition(scope, ctx) — mirror hr-read.service.ts:62 / contract.service.ts:84; Company→không lọc thêm; Own/Team→chỉ trả project có ≥1 ACTIVE member (member_status='Active', deleted_at NULL) mà employee row thoả predicate (EXISTS join project_members→employee_profiles) — employee @Own = project mình là member, manager @Team = project có member thuộc team-tree. (b) MEMBER INSERT — SCHEMA LEGACY: resolve employeeId→employee_profiles; nếu employee_profiles.user_id IS NULL → 400 fail-loud mã lỗi rõ (KHÔNG chèn được vì project_members.user_id NOT NULL); INSERT set CẢ user_id (legacy NOT NULL) LẪN employee_id (cột mới) + project_role + member_status='Active' + joined_at + invited_by (cột legacy status giữ default 'active'). Chống-trùng đo trên CẢ HAI unique: project_members_active_uq(user_id) VÀ uq_project_members_active_employee(employee_id) → 409; soft-remove set deleted_at + member_status='Removed' + removed_by/at để re-add hợp lệ với partial unique. (c) OWNER-CHECK manager @Team cho close/delete/manage-member: actor.employeeId === projects.owner_employee_id; owner_employee_id NULL → 403 FAIL-CLOSED; scope Company (company-admin) KHÔNG cần owner-check; hr KHÔNG có các pair này (0485) → 403 do thiếu quyền, KHÔNG liên quan owner-check. (d) Creator=Owner: actor có employee mapping active → owner-member với user_id=actor.userId + employee_id=actor.employeeId, set projects.owner_employee_id + created_by; không mapping → tạo project, set created_by, KHÔNG chèn owner-member (owner_employee_id NULL ⇒ mọi owner-check manager fail-closed 403). (e) BLOCK-NEW-TASK: guard trong createHubTask đọc CỘT MỚI project_status TitleCase (Completed/Cancelled/Archived) + deleted_at — KHÔNG đọc cột legacy status lowercase; MVP CHẶN CỨNG, KHÔNG expose override qua API (cờ override chỉ là tham số nội bộ service cho WO sau); mối nối createHubTask↔project-states legacy (project mới không có states nên tạo task dưới project mới có thể fail độc lập) = phạm vi S4-TASK-BE-2, ghi nhận tường minh, KHÔNG sửa ở đây. Ghi task_activity_logs + AuditService.record(objectType='project') trong cùng tx. Register providers/controllers vào tasks.module.ts (khối additive).","builder":"backend-builder","paths":["apps/api/src/tasks/**"]},{"id":"L4-int-tests","task":"Integration test RED-trước (colocated apps/api/test/integration/*.int-spec.ts). GATE ĐÚNG LOGIC: describe.skipIf(!(hasDb && process.env.LANE_DB)) — CHỈ chạy khi CÓ LANE_DB cô lập (bash scripts/lane-db-setup.sh <lane> + export LANE_DB); TUYỆT ĐỐI KHÔNG viết skipIf(!hasDb&&LANE_DB) (ngược logic, false-green — memory integration-test-LANE_DB-gate). Phủ: deny-path 403 mọi cặp thiếu quyền (gồm hr gọi close/delete/manage-member → 403 vì 0485 KHÔNG seed pair đó cho hr) · cross-tenant 404 không lộ · DATA-SCOPE TRONG-TENANT (employee @Own chỉ thấy project mình là member; manager @Team thấy project team, KHÔNG thấy ngoài scope; admin @Company thấy tất) · member trùng 409 trên CẢ HAI unique (user_id legacy + employee_id mới) · re-add sau soft-remove OK · employee KHÔNG có user account → 400 · employee nghỉ/chấm dứt bị chặn · creator=Owner mapping (user_id+employee_id) · owner-check manager (non-owner 403 · owner OK · owner_employee_id NULL → 403) · close/soft-delete lifecycle · block-new-task theo project_status mới · append-only proof (UPDATE/DELETE task_activity_logs qua KẾT NỐI APP-ROLE mediaos_app, KHÔNG directPool → fail) · audit_logs objectType='project'. Chứng minh RED trước khi GREEN.","builder":"backend-builder","paths":["apps/api/test/integration/**"]}]
acceptanceChecks: ["8+ route tồn tại và pair-gate ĐÚNG seed 0485: GET /api/v1/projects (read:project) · POST /projects (create:project) · GET /projects/:id (read:project) · PATCH /projects/:id (update:project) · POST /projects/:id/close (close:project) · DELETE /projects/:id (delete:project) · GET /projects/:id/members (read:project) · POST /projects/:id/members (manage-member:project) · PATCH /projects/:id/members/:memberId (manage-member:project) · DELETE /projects/:id/members/:memberId (manage-member:project).","Mọi truy vấn DB đi qua db.withTenant(companyId) + điều kiện company_id tường minh; RLS+FORCE đã active sẵn (projects/project_members media 0007/0023; task_activity_logs 0478) — cross-tenant test chứng minh 404 không lộ.","DATA-SCOPE ĐỌC (bắt buộc, KHÔNG hoãn): GET /projects + GET /:id + GET /:id/members lọc theo scope resolve từ grant 0485 qua DataScopeService.buildEmployeeScopeCondition (mirror hr-read.service): employee @Own chỉ thấy/mở project mình là ACTIVE member; manager @Team chỉ project có member thuộc team-tree; hr/admin @Company thấy toàn tenant. Test trong-tenant chứng minh KHÔNG lộ project ngoài scope (scope không phải trang trí).","POST /projects: chỉ create:project mới tạo; project_status mặc định hợp lệ; actor có employee_profiles active → INSERT owner-member (project_role='Owner', member_status='Active', user_id=actor.userId, employee_id=actor.employeeId) + set projects.owner_employee_id + created_by; KHÔNG có employee mapping → vẫn tạo + created_by, KHÔNG chèn owner-member (owner_employee_id NULL); endDate<startDate → 400; code/name trùng active → 409/400.","POST /:id/members: resolve employeeId→employee_profiles; employee status IN (inactive,resigned,terminated) hoặc deleted → chặn; employee_profiles.user_id NULL → 400 fail-loud mã lỗi rõ (project_members.user_id NOT NULL legacy — KHÔNG chèn mù); trùng ACTIVE member đo trên CẢ project_members_active_uq(company,project,user_id) LẪN uq_project_members_active_employee → 409; INSERT set cả user_id lẫn employee_id + member_status='Active' + joined_at + invited_by.","PATCH /:id/members/:memberId đổi project_role trong CHECK (Owner/Manager/Member/Viewer); DELETE /:id/members/:memberId soft-remove (member_status='Removed', removed_by/removed_at, deleted_at — deleted_at BẮT BUỘC để partial unique cho re-add); chặn xóa Owner cuối cùng → 400/409; re-add sau soft-remove thành công.","POST /:id/close set project_status='Completed' + closed_at + closed_by; DELETE /:id soft-delete (deleted_at + deleted_by), loại khỏi list mặc định; không hard-delete (bất biến #2).","Project project_status IN (Completed,Cancelled,Archived) — CỘT MỚI TitleCase, KHÔNG phải legacy status lowercase — hoặc deleted → createHubTask từ chối với thông báo rõ; MVP chặn cứng, KHÔNG expose override qua API (cờ nội bộ service); mối nối project-states legacy của createHubTask ghi nhận tường minh → S4-TASK-BE-2.","task_activity_logs được APPEND (PROJECT_CREATED/PROJECT_UPDATED/MEMBER_ADDED/MEMBER_REMOVED; target_type Project/Member) trong CÙNG tx withTenant; app role không UPDATE/DELETE được (append-only #2 — test qua kết nối app-role).","AuditService.record cho create/update/delete/close project + manage-member, objectType='project' (đã trong CHECK, không migration) trong cùng tx.","List có pagination (limit/offset clamp) + filter (status/owner/search); DTO validate tại boundary qua ZodValidationPipe + @mediaos/contracts (dual-build).","OWNER-CHECK manager @Team (close/delete/manage-member): actor.employeeId === projects.owner_employee_id; owner_employee_id NULL → 403 FAIL-CLOSED (test nhánh NULL); company-admin @Company không qua owner-check; hr gọi các route này → 403 vì 0485 KHÔNG seed pair cho hr (deny-path test riêng, KHÔNG nhầm là owner-check).","Routes archive:project + view-report:project (đã seed 0485) OUT-OF-SCOPE WO này — ghi nhận tường minh, không route nào chết im lặng.","check.sh xanh + FULL GATE BẮT BUỘC (không điều kiện — diff chạm permission/audit/RLS theo CLAUDE.md §6): security-reviewer PASS + review theo lens database (RLS/unique/append-only) + lens silent-failure (mọi nhánh lỗi fail-loud, không nuốt); implement/review model Opus; typecheck sạch; không file >800 dòng; backlog cập nhật (DoD §8)."]
testTasks: ["Deny-path RED (int-spec): user thiếu create:project → POST /projects 403 · thiếu update:project → PATCH 403 · thiếu close:project → POST /:id/close 403 · thiếu delete:project → DELETE 403 · thiếu manage-member:project → add/update/remove member 403 · hr gọi close/delete/manage-member → 403 (0485 không seed pair cho hr). Chứng minh RED trước khi impl.","Cross-tenant RED: GET/PATCH/close/delete project của company khác → 404 (không lộ tồn tại) · add/update/remove member trỏ project hoặc memberId của tenant khác → 404.","DATA-SCOPE TRONG-TENANT RED: employee @Own list/detail CHỈ thấy project mình là ACTIVE member (project cùng company mà mình không là member → KHÔNG xuất hiện trong list, GET /:id → 404/403 nhất quán) · manager @Team thấy đúng project có member thuộc team-tree, KHÔNG thấy ngoài team · admin @Company thấy tất; GET /:id/members cùng scope với detail.","Member-rule RED: thêm employee resigned/terminated/inactive → chặn · employee KHÔNG có user account (employee_profiles.user_id NULL) → 400 mã lỗi rõ · trùng member Active → 409 đo trên CẢ HAI unique (legacy user_id + mới employee_id) · soft-remove rồi re-add → thành công (deleted_at đã set) · xóa Owner cuối cùng → chặn.","Creator=Owner: POST /projects actor có employee mapping → project_members có hàng Owner với user_id=actor.userId VÀ employee_id=actor.employeeId + projects.owner_employee_id set; actor không employee → không owner-member, created_by set, owner_employee_id NULL.","Lifecycle: close → project_status='Completed' + closed_at/by; DELETE → deleted_at set + biến khỏi list mặc định; project Completed/Cancelled/Archived (cột project_status MỚI) → createHubTask bị từ chối; project Active bình thường không bị chặn.","OWNER-CHECK: manager @Team không phải owner → close/delete/manage-member 403; manager là owner (employeeId=owner_employee_id) → cho phép; owner_employee_id NULL → 403 fail-closed (test riêng nhánh NULL).","Append-only + audit: sau create/update/member ops có hàng task_activity_logs đúng action; UPDATE/DELETE task_activity_logs qua KẾT NỐI APP-ROLE mediaos_app (KHÔNG directPool) → fail (chứng minh ledger); audit_logs có hàng objectType='project'.","GATE test: mọi int-spec dùng describe.skipIf(!(hasDb && process.env.LANE_DB)) — chỉ chạy khi có LANE_DB cô lập (scripts/lane-db-setup.sh); KHÔNG dùng biểu thức ngược !hasDb&&LANE_DB (false-green); chạy đủ và GREEN dưới LANE_DB trước khi gate."]
steps: ["L1 (db-migration, NỐI TIẾP ĐẦU TIÊN): đồng bộ Drizzle schema với 0478 — thêm cột NULLABLE additive vào projects+project_members (media.ts, GIỮ user_id NOT NULL + unique legacy), tạo typed taskActivityLogs (task-activity.ts mới), export index.ts. KHÔNG .sql mới, head=0485. typecheck xanh chứng minh cột/table đã typed.","L2 (backend, SONG SONG với L1): viết Zod DTO project/member vào contracts/task.ts, pnpm --filter @mediaos/contracts build (dual ESM/CJS).","L3 (backend, sau L1+L2): dựng ProjectsRepository (withTenant, filter company_id, DATA-SCOPE EXISTS-join theo buildEmployeeScopeCondition, join owner/employee, pagination), ProjectsService (P0 rules + resolve employee→user_id fail-loud + dedupe 2 unique + owner-check fail-closed NULL + activity + audit trong 1 tx), ProjectsController (@RequirePermission theo cặp seed), TaskActivityService helper append-only; guard block-new-task vào createHubTask đọc project_status MỚI; register tasks.module.ts additive.","L4 (backend, sau L3): int-spec RED-trước cho deny-path (gồm hr)/cross-tenant/data-scope trong-tenant/member-rule (2 unique + user NULL)/lifecycle/owner-check (gồm NULL)/append-only app-role; gate describe.skipIf(!(hasDb && process.env.LANE_DB)); chạy trên LANE_DB cô lập → GREEN.","Gate: check.sh xanh + FULL GATE (không điều kiện): security-reviewer PASS + lens database + lens silent-failure; Opus implement/review. Không file >800 dòng."]
```

GAP-ANALYSIS (fresh decompose + vá theo plan-review round 1 — 4 BLOCKING đã xử):
DB đã sẵn — KHÔNG migration mới. 0478 đã ALTER-ADD mọi cột TitleCase project/member + tạo task_activity_logs (append-only, RLS+FORCE, GRANT SELECT/INSERT). 0485 seed đủ cặp permission project/task (employee read:project=@Own, manager=@Team, hr/admin=@Company — xác minh 0485 L117-150). Head=0485. projects/project_members có GRANT + RLS từ media 0007/0023. 'project'/'task' đã trong audit object_type CHECK (0020) → audit reuse, không migration.

PLAN-REVIEW ROUND 1 → FIX (2026-07-10):
1. GATE: WO chạm permission/audit/RLS → FULL gate KHÔNG điều kiện (CLAUDE.md §6), bỏ mọi chữ 'LIGHT gate'; Opus implement/review. Lưu ý runner: agent database-reviewer/silent-failure-hunter không đăng ký trong workflow — lens DB + silent-failure được ghi TƯỜNG MINH vào acceptance để security-reviewer + qa-test-engineer + completion-evaluator soi thay; đây là degrade khai báo, không im lặng.
2. DATA-SCOPE: KHÔNG hoãn nữa — GET list/detail/members lọc theo buildEmployeeScopeCondition (mirror hr-read.service.ts:62); Own=membership, Team=team-tree, Company=không lọc. Test trong-tenant bắt buộc.
3. SCHEMA LEGACY: project_members.user_id NOT NULL + project_members_active_uq(company,project,user_id) WHERE deleted_at IS NULL còn LIVE (media.ts:420-439, xác minh 2026-07-10). Mọi INSERT member phải set user_id (resolve từ employee_profiles.user_id; NULL → 400 fail-loud) + employee_id; dedupe trên CẢ 2 unique; soft-remove PHẢI set deleted_at.
4. LANE_DB GATE: đúng biểu thức describe.skipIf(!(hasDb && process.env.LANE_DB)).

INVARIANT/HOT-FILE: Drizzle schema CHƯA sync 0478 (projects/project_members media.ts chỉ có cột lowercase cũ; task_activity_logs không typed) → L1 sync trước, CHỈ thêm cột NULLABLE additive, giữ nguyên cột/CHECK/unique cũ; task_activity_logs file mới schema/task-activity.ts, export append index.ts; KHÔNG db:generate.

PATHS: WO thiếu apps/api/src/db/schema/** → mở rộng workOrder.paths + lane L1 (chủ đích, khai báo tường minh cho guard-scope).

MAPPING builder: 'close'=project_status='Completed' (SPEC §6.11 không có literal Closed); tập chặn-task-mới={Completed,Cancelled,Archived}+deleted trên CỘT MỚI project_status TitleCase (KHÔNG đọc legacy status lowercase); employee active qua employee_profiles.status∈(active|…)+deleted_at; project_role∈Owner|Manager|Member|Viewer; member_status∈Active|Inactive|Removed; bảng thật employee_profiles/org_units.

OWNER-CHECK (seed 0485 hoãn về WO này — ĐÃ CHỐT cột): manager @Team destructive (close/delete/manage-member) hợp lệ ⟺ actor.employeeId === projects.owner_employee_id; owner_employee_id NULL → 403 FAIL-CLOSED (kể cả manager là creator — created_by KHÔNG thay thế owner_employee_id); company-admin @Company bỏ owner-check; hr thiếu pair → 403 thiếu quyền (khác owner-check).

MỐI NỐI LEGACY (ghi nhận, KHÔNG sửa ở WO này): createHubTask (tasks.service.ts) phụ thuộc project-states legacy (findDefaultStateTx/allocateSequenceTx) → project MỚI tạo bởi ProjectsController chưa có states, tạo task dưới project mới có thể fail độc lập với guard block-new-task. Xử lý thuộc S4-TASK-BE-2 (task CRUD). Guard block-new-task ở đây chỉ THÊM điều kiện chặn theo project_status mới, không đổi hành vi states.

OUT-OF-SCOPE (tường minh, có WO tiếp nhận): (a) route POST /:id/archive + GET /:id/report (pair archive/view-report:project đã seed 0485 nhưng không trong done_when) → WO sau; (b) task write employee/manager 5 grant DEFERRED (TASK_DEFERRED_GRANTS) = S4-TASK-BE-2; (c) task_comments soft-delete grant = WO khác; (d) sửa luồng createHubTask↔project-states = S4-TASK-BE-2.

VERIFY: int-spec trên LANE_DB cô lập (lane-db-setup.sh) tránh xanh/đỏ-giả; baseline ~31 int-spec fail env-red (Vault/KMS/2FA/mail) — đối chiếu trước khi quy lỗi diff. FULL gate theo CLAUDE.md §6; deny-path/cross-tenant/data-scope RED là lưới an toàn; auto build-fix nếu đỏ (không @ts-ignore).
