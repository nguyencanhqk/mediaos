```yaml
wo: S4-INT-5
zone: red
generated_by: auto-loop
reconciled_at: "f4a4a62"
lanes: [{"id":"hrCreateProducer","task":"Producer HR create → activation/welcome (STORY-098). Inject OutboxService (đã export @Global EventsModule — KHÔNG đổi employees.module.ts imports) vào HrWriteService. TRONG tx createEmployee (hr-write.service.ts ~L182, NGAY SAU audit user.created), CHỈ KHI provisioned!=null (mint tài khoản MỚI — link user cũ KHÔNG phát, không có 'account created'): outbox.enqueue(tx,{eventType:'auth.user_created',payload:{eventCode:'AUTH_USER_CREATED',userId,employeeId:created.id}}). Recipient=user MỚI (payload.userId, SPEC-08 §15 NOTI-EVENT-001 'User mới'). KHÔNG set actorUserId (user mới luôn ≠ actor HR ⇒ actor-exclusion vô hại; omit để bảo đảm welcome kể cả edge). CHỈ thêm 1 khối enqueue additive trong tx hiện có — KHÔNG đổi chữ ký createEmployee/resolveUserId, KHÔNG đụng audit/employee_status_histories. Verify createUserTx đặt status='active' (nếu 'invited'/'pending' ⇒ filterActiveUsers loại ⇒ 0 welcome — flag).","builder":"backend-builder","paths":["apps/api/src/employees/hr-write.service.ts"]},{"id":"authLockProducer","task":"Producer AUTH account-locked (crown-AUTH). Password-reset ĐÃ có producer (auth.service.ts:1198 eventType 'auth.password_reset_requested' payload{userId,resetTokenEnc}) — KHÔNG đổi producer (bridge lo sanitize+wire). THÊM producer account-lock: trong login() nhánh WrongPassword (SAU recordLoginFailure ~L327), khi rateLimiter.isLocked(accountKey) CHUYỂN true (edge sạch: login() đã 429 ở L199 nếu pre-locked) VÀ result.userId THẬT (WrongPassword, không phải UserNotFound ⇒ anti-enumeration) → mở withTenant(companyId) tx RIÊNG (mirror bumpFailedLoginCount) → outbox.enqueue(tx,{eventType:'auth.user_locked',payload:{eventCode:'AUTH_USER_LOCKED',userId}}) + audit(action:'auth.user_locked') + securityEvents.record(eventType:'ACCOUNT_LOCKED'). Payload CHỈ userId — KHÔNG IP/attempts/chi tiết bảo mật (done_when 'KHÔNG lộ chi tiết nhạy cảm'). KHÔNG set actorUserId (recipient=chủ tài khoản; nếu set actor=owner thì actor-exclusion LOẠI owner ⇒ 0 notification — BUG). Lỗi enqueue → logger.error (KHÔNG nuốt câm) NHƯNG KHÔNG đổi 401 đồng nhất (notify là courtesy, không phải security-control). Chỉ áp cho lock account-bucket đường login chính (line 327). 2FA re-auth lock (L406/L409 đã có securityAlert) NGOÀI phạm vi.","builder":"backend-builder","paths":["apps/api/src/auth/auth.service.ts"]},{"id":"authHrNotiBridgeReg","task":"Wiring 3 mapping AUTH/HR lên OutboxNotificationBridge (INT-1) — TÁI DÙNG bridge generic, KHÔNG consumer/bridge MỚI. (1) EXTEND NotiEventMapping + handle() additive: thêm optional payloadOf?:(ctx)=>Record<string,unknown> (default=ctx.payload — backward-compat TASK/LEAVE/ATT). LÝ DO CROWN: password-reset payload mang resetTokenEnc (key 'resetTokenEnc' KHÔNG khớp SENSITIVE_PAYLOAD_KEYS set-exact ⇒ assertPayloadSafe KHÔNG chặn ⇒ nếu forward verbatim thì token-envelope lọt vào notifications.payload/body recipient — vi phạm BẤT BIẾN #3). payloadOf STRIP: password-reset→{userId}, lock→{userId}, created→{userId,employeeId}. (2) New auth-hr-noti-bridge.registrar.ts (OnModuleInit, mirror TaskNotiBridgeRegistrar; import CHỈ notifications/**+events/** — KHÔNG import AuthModule/EmployeesModule, giữ acyclic). Đăng ký 3 mapping eventCode VERBATIM: auth.user_created→AUTH_USER_CREATED, auth.password_reset_requested→AUTH_PASSWORD_RESET_REQUESTED, auth.user_locked→AUTH_USER_LOCKED (catalog:61-63,64). resolveRecipients=(ctx)=>[strField(ctx.payload.userId)] (KHÔNG reader — userId trong payload). KHÔNG set actorUserId (3 event owner-directed — actor-exclusion inapplicable). (3) APPEND 3 khoá strategy:'DedupeKey' (dedupeKey=ctx.eventId) vào notification-dedupe.const.ts. (4) APPEND AuthHrNotiBridgeRegistrar vào notifications.module.ts providers[] (khối S4-INT-5 additive — KHÔNG rewrite INT-1/exports[]/controllers[]). Optional defense-in-depth: thêm 'resettokenenc' vào SENSITIVE_PAYLOAD_KEYS (backstop fail-loud nếu ai quên payloadOf). Zero-migration.","builder":"backend-builder","paths":["apps/api/src/notifications/outbox-notification-bridge.service.ts","apps/api/src/notifications/auth-hr-noti-bridge.registrar.ts","apps/api/src/notifications/notification-dedupe.const.ts","apps/api/src/notifications/notifications.module.ts","apps/api/src/notifications/notification-engine.errors.ts"]},{"id":"authHrNotiIntSpec","task":"Int-spec RED-trước (Postgres cô lập LANE_DB): auth-hr-noti-e2e.int-spec.ts — đường THẬT producer→OutboxWorker.processBatch→noti-bridge:auth.*→OutboxNotificationBridge→engine.intake→notifications+notification_delivery_logs. Phủ: (a) createEmployee provision (POST /hr/employees email) → đúng 1 AUTH_USER_CREATED cho user MỚI, actor(HR) KHÔNG nhận; link user cũ (dto.userId) → 0 AUTH_USER_CREATED. (b) forgotPassword THẬT → 1 AUTH_PASSWORD_RESET_REQUESTED cho chủ TK; ASSERT notification.payload + body KHÔNG chứa resetTokenEnc/token (security-leak). (c) account-lock: N login sai vượt accountMaxAttempts → 1 AUTH_USER_LOCKED cho chủ TK; body KHÔNG chứa IP/attempts; email GHOST bị khoá → 0 notification (anti-enumeration); actor(owner) KHÔNG bị actor-exclusion loại. (d) idempotent: re-consume cùng outbox event → vẫn 1 notification (2 tầng: processed_events + partial-unique uq_notifications_dedupe_active dedupeKey=eventId). (e) cross-tenant deny: event company A → 0 notification cho recipient company B. (f) boot-guard standalone: 3 eventCode AUTH enabled → registerSource KHÔNG throw. Tắt tạm registrar → RED, bật → GREEN. Regression: noti-event-intake.int-spec + task-noti-e2e.int-spec (INT-1 vẫn xanh) + auth login/forgot int-spec (countOutbox password-reset KHÔNG đổi).","builder":"backend-builder","paths":["apps/api/test/integration/auth-hr-noti-e2e.int-spec.ts"]}]
acceptanceChecks: ["3 mapping AUTH đăng ký lên OutboxNotificationBridge qua registerSource — KHÔNG có class consumer/bridge MỚI ngoài 1 registrar (grep: không thêm EventBus.register thủ công, không gọi NotificationEngine trực tiếp ngoài bridge).","eventType→eventCode VERBATIM: auth.user_created→AUTH_USER_CREATED, auth.password_reset_requested→AUTH_PASSWORD_RESET_REQUESTED, auth.user_locked→AUTH_USER_LOCKED (khớp notification-event-catalog.const.ts:61-63; 3 mã isEnabled=true ⇒ boot-guard PASS, zero-migration).","Recipient=chủ tài khoản/nhân sự vừa tạo = payload.userId (đo được bằng test): activation→user MỚI (chỉ khi provision, link user cũ→0); reset/lock→chủ TK. KHÔNG set actorUserId cho 3 event ⇒ owner KHÔNG bị actor-exclusion loại (test: chủ TK tự reset/tự gây lock vẫn NHẬN).","SECURITY (crown): notification của AUTH_PASSWORD_RESET_REQUESTED KHÔNG chứa resetTokenEnc/token trong payload/body (payloadOf strip); AUTH_USER_LOCKED payload+body KHÔNG chứa IP/attempts/chi tiết bảo mật — chứng minh bằng test đọc notifications.payload + delivery_log.","Anti-enumeration: login sai với email GHOST (user không tồn tại) vượt ngưỡng → 0 AUTH_USER_LOCKED (result.userId=null ⇒ không emit); chỉ chủ TK THẬT nhận.","Producer HR: createEmployee provision phát đúng 1 outbox 'auth.user_created' trong tx (payload->>'userId' NOT NULL); link user cũ → 0; countOutbox employees hiện có KHÔNG đổi. Producer password-reset GIỮ NGUYÊN (countOutbox auth forgot int-spec KHÔNG đổi).","consumerName DUY NHẤT: noti-bridge:auth.user_created / :auth.password_reset_requested / :auth.user_locked — EventBus.register ném nếu trùng (event-bus.ts:34); không đụng noti-bridge TASK (INT-1) / LEAVE-ATT (INT-3/4).","Idempotent retry: re-consume CÙNG outbox event → vẫn đúng 1 notification/recipient; chặn 2 tầng (OutboxWorker.processed_events consumer_name+event_id; partial-unique uq_notifications_dedupe_active company+recipient+event_code+dedupe_key=eventId).","Cross-tenant: event company A KHÔNG tạo notification cho recipient company B (RLS + resolver filterActiveUsers eq company_id); reader N/A (userId in payload) nhưng RLS trong intake tx vẫn ẩn cross-tenant.","Silent-failure: bridge handler RE-THROW (OutboxWorker retry/dead-letter); producer account-lock tx lỗi → logger.error (KHÔNG nuốt câm) NHƯNG KHÔNG đổi 401 đồng nhất login.","Zero-migration: head mig KHÔNG đổi; chỉ thêm const DedupeKey + 1 provider + 1 registrar + producer field + bridge payloadOf — KHÔNG file migrations/** mới, KHÔNG đụng seed catalog.","DoD §8: input đọc payload an toàn (strField); audit đã có ở producer (không đụng), FE N/A; có int-spec; không phá luồng login/forgot/HR-create; FULL gate security-reviewer + silent-failure-hunter + plan-reviewer PASS; coverage int ≥80% module nhạy cảm; check.sh --all không LOUD 'XANH KHÔNG ĐỦ BẰNG CHỨNG'."]
testTasks: ["Int-spec auth-hr-noti-e2e.int-spec.ts (test/integration, Postgres cô lập LANE_DB): createEmployee provision → 1 AUTH_USER_CREATED cho user MỚI, HR(actor) KHÔNG nhận; link user cũ → 0 AUTH_USER_CREATED.","Deny-path/security RED: forgotPassword THẬT → 1 AUTH_PASSWORD_RESET_REQUESTED cho chủ TK; ASSERT notifications.payload + rendered body KHÔNG chứa key 'resetTokenEnc'/'token'/giá trị envelope (chống rò secret — BẤT BIẾN #3).","Deny-path RED anti-enumeration + actor: N login sai (>= accountMaxAttempts) email THẬT → 1 AUTH_USER_LOCKED cho chủ TK (owner KHÔNG bị actor-exclusion loại vì mapping không set actorUserId); email GHOST bị khoá → 0 notification; body KHÔNG lộ IP/attempts.","Idempotent RED: mô phỏng OutboxWorker re-consume cùng event_id (retry/reaper) → assert vẫn đúng 1 notification (2 tầng độc lập: xoá dấu processed_events vẫn bị partial-unique uq_notifications_dedupe_active chặn).","Cross-tenant deny RED: outbox event company A với recipient thuộc company B → 0 notification cho B (RLS + company_id trong intake tx); đếm skipped/dropped đúng.","Boot-guard standalone: registerSource 3 eventCode AUTH (isEnabled=true) KHÔNG throw; wire nhầm mã disabled (vd AUTH_PASSWORD_CHANGED) → throw tại boot (fail-loud, mirror INT-1).","Regression: task-noti-e2e.int-spec (INT-1 bridge vẫn xanh — extend payloadOf backward-compat) + noti-event-intake.int-spec + auth login/forgot int-spec (countOutbox password-reset KHÔNG đổi) + hr-employee-write int-spec (countOutbox create thêm đúng 1 khi provision).","QA đối chiếu: SPEC-08 §15.1 (NOTI-EVENT-001 AUTH_USER_CREATED→User mới; AUTH_PASSWORD_RESET_REQUESTED→User; AUTH_USER_LOCKED→User) + SPEC-08 §16 (không resolve được recipient → log Skipped, KHÔNG delivery_log ma) + STORY-098 (activation notification khi HR tạo employee↔AUTH tạo user)."]
steps: ["plan-reviewer TRƯỚC khi code (crown-AUTH): chốt (i) eventCode activation = AUTH_USER_CREATED (recipient 'User mới', SPEC-08 §15) — KHÔNG HR_EMPLOYEE_CREATED (recipient 'HR/Admin', cần role-audience resolver ⇒ hoãn); (ii) điểm+cơ chế phát account-lock (edge Valkey account-bucket line 327, tx riêng); (iii) sanitize payload resetTokenEnc qua payloadOf; (iv) KHÔNG set actorUserId cho 3 event owner-directed.","Lane hrCreateProducer: inject OutboxService vào HrWriteService; enqueue 'auth.user_created' trong tx createEmployee (chỉ provisioned!=null); verify status user mới='active'; countOutbox regression employees không phá.","Lane authLockProducer (song song): thêm producer 'auth.user_locked' ở nhánh WrongPassword sau recordLoginFailure — edge isLocked(accountKey) + result.userId thật; tx riêng enqueue+audit+securityEvents; log-not-swallow; KHÔNG đổi 401. Password-reset producer GIỮ NGUYÊN.","Lane authHrNotiBridgeReg (song song, merge cùng PR): EXTEND bridge payloadOf (additive optional, default ctx.payload); viết auth-hr-noti-bridge.registrar.ts đăng ký 3 mapping VERBATIM + resolveRecipients=[payload.userId] + payloadOf strip; APPEND 3 DedupeKey; APPEND provider vào notifications.module.ts; (optional) thêm 'resettokenenc' vào SENSITIVE_PAYLOAD_KEYS.","Lane authHrNotiIntSpec (cuối): viết auth-hr-noti-e2e.int-spec.ts RED-trước (tắt registrar→đỏ), bật→xanh; assert KHÔNG-lộ-token + anti-enumeration + idempotent 2 tầng + cross-tenant; regression task-noti-e2e/noti-event-intake/auth login+forgot.","Verify DB cô lập: bash scripts/lane-db-setup.sh int5 → export LANE_DB=mediaos_int5 → pnpm --filter @mediaos/api test; trước PR vùng đỏ: bash harness/check.sh --all (int-spec skip ≤ ngưỡng, exit 1 nếu vượt — deny-path chạy THẬT).","FULL gate crown: security-reviewer + silent-failure-hunter + plan-reviewer PASS (+ santa-method); ghi khối yaml máy-đọc vào docs/plans/S4-INT-5.md; cập nhật harness/backlog.mjs; serialize MERGE 1 PR SAU INT-1 (đã ở master); nếu INT-3/INT-4 land trước → rebase khối additive notifications.module.ts providers[] + notification-dedupe.const.ts (append KHÔNG rewrite)."]
```

GAP-ANALYSIS (code hiện tại, master; bridge INT-1 đã merged #168):
(1) eventCode: cả 4 mã AUTH_USER_CREATED / AUTH_PASSWORD_RESET_REQUESTED / AUTH_USER_LOCKED / HR_EMPLOYEE_CREATED đã isEnabled=true trong notification-event-catalog.const.ts:61-64 ⇒ boot-guard PASS, ZERO-MIGRATION (template đã seed 0481 vì coverage test bắt buộc). QUYẾT ĐỊNH eventCode 'activation/welcome' = AUTH_USER_CREATED (SPEC-08 §15.1 dòng 1247 recipient 'User mới' = nhân sự vừa tạo). HR_EMPLOYEE_CREATED (dòng 1259) recipient='HR/Admin liên quan' ≠ nhân sự mới ⇒ KHÔNG phải activation; cần role-audience resolver (list HR/company-admin) ⇒ HOÃN out-of-scope (mảnh thiếu STORY-098 chỉ là activation-to-new-user).
(2) PRODUCER STATE: password-reset ĐÃ có producer (auth.service.ts:1198 eventType 'auth.password_reset_requested', payload{userId,resetTokenEnc}) — chỉ THIẾU wire bridge. HR create employee KHÔNG có outbox (grep employees/** = 0) ⇒ THÊM producer 'auth.user_created' trong tx createEmployee (hr-write.service.ts:182, chỉ provisioned!=null). account-lock KHÔNG có producer VÀ KHÔNG có DB lock-state: bumpFailedLoginCount comment (auth.service.ts:1564) nói 'khoá theo ngưỡng locked_at là WO RIÊNG' ⇒ lock hiện CHỈ Valkey rate-limiter (LoginRateLimiter). Producer 'auth.user_locked' phát ở edge account-bucket khoá (login() nhánh WrongPassword sau recordLoginFailure ~L327; login() đã 429 ở L199 nếu pre-locked ⇒ edge sạch) trong tx riêng (mirror bumpFailedLoginCount).
(3) SECURITY CROWN #1 — resetTokenEnc rò: assertPayloadSafe (notification-engine.errors.ts:83-95) so khớp KEY chính-xác; 'resetTokenEnc'.toLowerCase()='resettokenenc' KHÔNG có trong SENSITIVE_PAYLOAD_KEYS ('token'/'secret'/'secret_ref' là exact-key) ⇒ nếu bridge forward ctx.payload verbatim thì token-envelope lọt vào notifications.payload (engine:200) + có thể render body — vi phạm BẤT BIẾN #3. FIX: EXTEND bridge với optional payloadOf (additive, default=ctx.payload) để mapping password-reset trả CHỈ {userId} (drop resetTokenEnc). KHÔNG thêm resetTokenEnc vào blocklist làm cách chặn chính (sẽ throw 400 ⇒ re-throw ⇒ dead-letter, hỏng notification) — chỉ dùng làm defense-in-depth SAU khi payloadOf đã strip.
(4) SECURITY CROWN #2 — actor-exclusion đảo chiều: 3 event owner-directed (recipient=chủ TK). notification-recipient-resolver.service.ts:50 LOẠI dto.actorUserId khỏi recipient (trừ system-event). Với reset/lock, actor=chủ TK (self) ⇒ NẾU set actorUserId=owner thì owner bị LOẠI ⇒ 0 notification (BUG). Password-reset producer hiện KHÔNG mang actorUserId (đúng); lock producer + mapping PHẢI KHÔNG set actorUserId. AUTH_USER_CREATED: actor=HR ≠ user mới ⇒ set/omit đều được, KHUYẾN NGHỊ omit (bảo đảm welcome kể cả degenerate self-provision). Đây chính là 'actor loại nơi áp dụng' trong done_when.
(5) SECURITY CROWN #3 — filterActiveUsers (resolver:88-107) yêu cầu users.status='active' AND lockedAt IS NULL. Với AUTH_USER_LOCKED: khoá HIỆN là Valkey (users.lockedAt vẫn NULL, status vẫn 'active') ⇒ recipient resolve OK, notify được. NHƯNG nếu WO tương lai thêm users.lockedAt on threshold-lock thì lock-notify sẽ bị DROP — mâu thuẫn 'notify chủ TK đang bị khoá'. FLAG cho tương lai (không chặn INT-5). Với AUTH_USER_CREATED: verify createUserTx đặt status='active' (nếu 'invited'/'pending' ⇒ 0 welcome).
INVARIANTS: #1 company_id — mọi intake qua withTenant, RLS ẩn cross-tenant; recipient=payload.userId cùng company. #2 append-only — producer chỉ THÊM khối enqueue trong tx nghiệp vụ hiện có, KHÔNG đụng audit/employee_status_histories/login_logs/user_security_events (append-only). #3 no-secret — payloadOf strip resetTokenEnc; lock payload chỉ userId. handler bridge RE-THROW (không nuốt); lock-producer tx lỗi → logger.error (observable) KHÔNG đổi 401.
CONSUMER UNIQUENESS: bridge tự đặt noti-bridge:auth.* (3 mã) — EventBus cho nhiều consumer/eventType (idempotency riêng consumer_name+event_id); 'auth.password_reset_requested' hiện 0 consumer bền vững (mail gửi SYNC ngoài tx) ⇒ noti-bridge là consumer đầu tiên, không đá. RECIPIENT: KHÔNG cần reader raw-SQL (khác TASK/LEAVE/ATT) vì userId nằm trong payload cả 3 event.
VERIFY: int-spec dưới LANE_DB cô lập (scripts/lane-db-setup.sh int5; check.sh --all trước PR vùng đỏ, exit 1 nếu int-spec skip vượt ngưỡng — deny-path/idempotent/cross-tenant/no-leak chạy THẬT). GATE: crown/FULL — plan-reviewer TRƯỚC code, security-reviewer + silent-failure-hunter (+ santa-method); PLAN stage Sonnet5 xhigh, IMPLEMENT/REVIEW Opus.
HOT-FILE & MERGE (append KHÔNG rewrite): notifications.module.ts providers[] APPEND khối S4-INT-5 (KHÔNG sửa INT-1/INT-3/INT-4); notification-dedupe.const.ts APPEND 3 khoá cuối object; outbox-notification-bridge.service.ts EXTEND interface+handle additive (payloadOf optional) — SHARED với INT-1/3/4, nếu chạy song song rebase phần additive. events/** = REUSE-ONLY (EventBus/OutboxService/OutboxWorker — KHÔNG chỉnh sửa; import không cần khai path). Serialize MERGE 1 PR SAU INT-1 (đã master).
OUT-OF-SCOPE: (a) HR_EMPLOYEE_CREATED → HR/Admin audience (cần role-audience resolver) — hoãn WO sau. (b) 2FA re-auth lock (auth.service.ts:406/409 đã securityAlert) — KHÔNG wire AUTH_USER_LOCKED. (c) DB-based lock users.lockedAt threshold — WO riêng chưa làm. (d) Template render AUTH_* — đã seed 0481 (coverage test), nếu thiếu thì renderer fallback tiêu đề; thêm/sửa template = migration → out-of-scope. (e) mail-consumer bền vững cho auth.password_reset_requested — ngoài phạm vi (bridge chỉ tạo IN_APP notification, KHÔNG gửi mail).
SPEC-vs-CODE: WO title gọi 'activation/welcome' — chốt = AUTH_USER_CREATED theo SPEC-08 §15.1 (spec thắng). WO nêu 'account-locked' như đã có producer — thực tế producer THIẾU (chỉ Valkey lock), phải thêm; ghi lệch để builder biết.
</content>
