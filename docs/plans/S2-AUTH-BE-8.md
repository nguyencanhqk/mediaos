```yaml
wo: S2-AUTH-BE-8
zone: red
generated_by: auto-loop
reconciled_at: "48382cd"
lanes: [{"id":"a_contracts","task":"Contracts: thêm SECURITY_EVENT_TYPES (union event_type §22.2: PASSWORD_CHANGED · PASSWORD_RESET_REQUESTED · PASSWORD_RESET_COMPLETED · REFRESH_TOKEN_REUSE_DETECTED · SESSION_REVOKED · ALL_SESSIONS_REVOKED · USER_LOCKED · USER_UNLOCKED · ROLE_ASSIGNED · ROLE_REMOVED · TOTP_ENABLED · TOTP_DISABLED) + map SECURITY_EVENT_SEVERITY (event_type→severity, REUSE_DETECTED='critical', USER_LOCKED='high', còn lại low/medium/info) khớp SECURITY_EVENT_SEVERITIES đã có. KHÔNG đổi shape DTO viewer/list (append-only constants).","builder":"backend-builder","paths":["packages/contracts/src/auth.ts"]},{"id":"b_authwriter","task":"SecurityEventWriter (service dùng chung, crown/auth) + wire emit-point trong AuthModule: tạo apps/api/src/auth/security-event-writer.service.ts record(tx, {eventType,userId,actorUserId?,ip?,userAgent?,payload?}) → INSERT user_security_events dùng CHÍNH tx truyền vào (KHÔNG mở withTenant mới), severity từ contracts map, payload MASK qua AuditMaskerService (@Global). Gọi writer TRONG CÙNG tx (bên cạnh audit.record đang có — GIỮ dual-write) tại: auth.service.changePassword(PASSWORD_CHANGED) · resetPassword(PASSWORD_RESET_COMPLETED+ALL_SESSIONS_REVOKED) · forgotPasswordImpl(PASSWORD_RESET_REQUESTED) · refresh reuse-branch(REFRESH_TOKEN_REUSE_DETECTED) · logout(SESSION_REVOKED) · revokeSession/revokeOtherSessions(SESSION_REVOKED) · two-factor.service.confirmEnable(TOTP_ENABLED) · disable(TOTP_DISABLED). Đăng ký provider+export SecurityEventWriter ở auth.module.ts.","builder":"backend-builder","paths":["apps/api/src/auth/security-event-writer.service.ts","apps/api/src/auth/auth.service.ts","apps/api/src/auth/two-factor.service.ts","apps/api/src/auth/auth.module.ts"]},{"id":"c_userslock","task":"Emit USER_LOCKED/USER_UNLOCKED: gọi SecurityEventWriter.record TRONG cùng tx tại auth-users.service.lockUser (sau setLockTx + audit 'user.locked') và unlockUser (sau setUnlockTx + audit 'user.unlocked'); actorUserId=actor.id, userId=target id, payload chỉ reason-code (KHÔNG PII). Đăng ký SecurityEventWriter làm provider trong users.module.ts (writer stateless, chỉ phụ thuộc AuditMaskerService @Global → tránh import-cycle).","builder":"backend-builder","paths":["apps/api/src/users/auth-users.service.ts","apps/api/src/users/users.module.ts"]},{"id":"d_permrole","task":"Emit ROLE_ASSIGNED/ROLE_REMOVED: gọi SecurityEventWriter.record TRONG cùng tx tại permission-admin.service.assignRole (sau audit 'RoleAssigned/RoleReassigned') và revokeRole (sau audit 'RoleRevoked'); userId=targetUserId, actorUserId=actor.id, payload={roleId} non-sensitive. Đăng ký SecurityEventWriter làm provider trong permission.module.ts (stateless, tránh cycle Auth↔Permission).","builder":"backend-builder","paths":["apps/api/src/permission/permission-admin.service.ts","apps/api/src/permission/permission.module.ts"]},{"id":"e_tests","task":"Integration deny-path/RED + e2e trên LANE_DB cô lập (apps/api/test/integration/): (1) atomicity — ép mutation gốc throw sau khi writer chạy ⇒ 0 orphan row trong user_security_events; (2) no-secret — payload chứa password/token/refresh_token/hash bị mask '***' (không lộ); (3) e2e viewer hết-rỗng — changePassword rồi GET /auth/security-events trả event vừa ghi; (4) 2-tenant — event tenant A KHÔNG hiện dưới tenant B (RLS); (5) per-event-type — mỗi emit-site ghi đúng 1 row event_type+severity. Gate hasDb && LANE_DB.","builder":"backend-builder","paths":["apps/api/test/integration/security-event-writer.int-spec.ts","apps/api/test/integration/security-event-atomicity.int-spec.ts"]}]
acceptanceChecks: ["SecurityEventWriter.record(tx, ...) INSERT user_security_events dùng CHÍNH tx của withTenant tại mọi emit-site (grep: KHÔNG có db.withTenant lồng bên trong writer/emit) — event + mutation gốc cùng commit/rollback.","Đủ tối thiểu §22.2 có điểm phát: PASSWORD_CHANGED · PASSWORD_RESET_REQUESTED · PASSWORD_RESET_COMPLETED · REFRESH_TOKEN_REUSE_DETECTED · SESSION_REVOKED (logout + revokeSession/revokeOtherSessions) · USER_LOCKED · USER_UNLOCKED · ROLE_ASSIGNED · ROLE_REMOVED · TOTP_ENABLED · TOTP_DISABLED — mỗi site vẫn còn audit.record cũ (dual-write, KHÔNG bỏ nguồn audit_logs).","severity gán theo contracts map, ∈ {info,low,medium,high,critical}; REFRESH_TOKEN_REUSE_DETECTED='critical'; KHÔNG vỡ CHECK user_security_events_severity_check.","payload jsonb đi qua AuditMaskerService (mask-at-write) — test chèn password/token/refresh_token/*_hash/secret ⇒ lưu '***', KHÔNG lộ (BẤT BIẾN #3).","company_id đúng tenant (DB DEFAULT current_setting app.current_company_id qua withTenant), user_id=subject NOT NULL, actor_user_id=actor hoặc null (hệ thống).","Append-only giữ: writer CHỈ INSERT (0 UPDATE/DELETE userSecurityEvents trong code); GRANT vẫn SELECT,INSERT; KHÔNG thêm migration (event_type là text, severity CHECK + grant đã có ở mig 0443).","Rollback atomicity (RED): ép mutation gốc throw ⇒ 0 orphan row user_security_events cho user đó.","e2e: sau changePassword, GET /auth/security-events (AUTH-API-402, gate view:audit-log) trả event vừa ghi — viewer KHÔNG còn rỗng.","2-tenant deny: event tenant A KHÔNG xuất hiện khi query dưới tenant B (RLS+FORCE).","DoD §8: validation input (event_type ∈ contracts union), audit-on-action giữ, test có mặt & chạy thực (xuất hiện trong run summary trên LANE_DB), FULL gate auth PASS + người chốt, regression auth suite xanh; cập nhật harness/backlog.mjs."]
testTasks: ["RED deny-path atomicity (integration, LANE_DB): stub/ép mutation gốc (vd changePassword hoặc lockUser) throw SAU writer.record ⇒ assert count(user_security_events WHERE user_id=target)=0 (không event mồ côi cùng tx rollback).","RED no-secret: gọi writer với payload={password,accessToken,refresh_token,password_hash,secret_ref} ⇒ đọc row (bypass qua raw/admin trong test) assert mọi value nhạy cảm='***' (mask allowlist như audit masker).","e2e viewer hết-rỗng: chạy changePassword → GET /auth/security-events trả ≥1 item event_type=PASSWORD_CHANGED, severity đúng map, KHÔNG có field payload/secret trong DTO.","2-tenant isolation: ghi event tenant A, withTenant(B) list ⇒ KHÔNG trả (RLS).","per-event-type coverage: 1 spec lặp qua từng emit-site (password change/reset/forgot, refresh-reuse, logout, revokeSession, lock/unlock, assign/revoke role, TOTP enable/disable) assert ghi đúng 1 row đúng event_type+severity trong tx.","Regression: auth.service.spec + auth-appendonly.int-spec + auth-logs-viewer.int.spec + two-factor + admin/auth-users + permission-admin specs GIỮ XANH (dual-write không phá audit cũ).","Chạy cô lập: bash scripts/lane-db-setup.sh <lane> → export LANE_DB=mediaos_<lane> → pnpm --filter @mediaos/api test; gate skip đúng (hasDb && LANE_DB) để tránh xanh-giả/đỏ-giả trên DB dev chung."]
steps: ["Lane a_contracts (nền, KHÔNG phụ thuộc): thêm SECURITY_EVENT_TYPES + SECURITY_EVENT_SEVERITY map vào packages/contracts/src/auth.ts; build contracts (dual ESM/CJS) để B/C/D/E import được.","Lane e_tests (RED viết TRƯỚC): viết deny-path atomicity + no-secret + 2-tenant + e2e-viewer; chạy → ĐỎ (chưa có writer) chứng minh gap.","Lane b_authwriter: tạo SecurityEventWriter (mirror AuditService.record — nhận tx, mask payload, INSERT userSecurityEvents, severity từ map); wire 8 emit-point trong auth.service.ts + two-factor.service.ts TRONG tx sẵn có, GIỮ NGUYÊN audit.record (dual-write); provider+export ở auth.module.ts.","Lane c_userslock (sau B — cần writer): emit USER_LOCKED/UNLOCKED tại auth-users.service, đăng ký provider ở users.module.ts.","Lane d_permrole (sau B — cần writer): emit ROLE_ASSIGNED/REMOVED tại permission-admin.service, đăng ký provider ở permission.module.ts.","Verify: bash scripts/lane-db-setup.sh <lane> → export LANE_DB=mediaos_<lane> → pnpm --filter @mediaos/api test (deny-path GREEN + regression auth suite xanh); typecheck+lint.","FULL gate (auth crown): security-reviewer + silent-failure-hunter + santa-method; người chốt red-zone trước merge (no auto-merge)."]
```

GAP: user_security_events có schema (apps/api/src/db/schema/auth-logs.ts) + reader (security-event.repository.ts) + viewer (auth-logs-viewer.service.ts AUTH-API-402) nhưng grep xác nhận 0 writer INSERT trong src ⇒ viewer rỗng-vĩnh-viễn. Cần: (1) SecurityEventWriter dùng chung, (2) gọi TRONG tx tại mutation-site, (3) contracts map event_type→severity (AUTH_AUDIT_LOG hiện chỉ có {RESOURCE,VIEW} — chưa có severity map, phải thêm mới).

EMIT-POINTS (đã xác minh dòng, tất cả đã có withTenant tx + audit.record để dual-write cạnh nó):
- auth.service.ts: changePassword ~L500-539 (audit 'auth.password_changed') → PASSWORD_CHANGED; resetPassword ~L1122-1151 (revokeAllSessionsForUserTx 'password_reset') → PASSWORD_RESET_COMPLETED (+ALL_SESSIONS_REVOKED); forgotPasswordImpl ~L1043-1078 (audit 'auth.password_reset_requested', CHỈ khi user tồn tại — giữ anti-enumeration) → PASSWORD_RESET_REQUESTED; refresh reuse-branch ~L606-622 (audit 'auth.token_reuse_detected') → REFRESH_TOKEN_REUSE_DETECTED (severity CRITICAL); logout ~L707-736 → SESSION_REVOKED; revokeSession ~L802-832 + revokeOtherSessions ~L846-874 (audit 'auth.session_revoked') → SESSION_REVOKED.
- two-factor.service.ts: confirmEnable ~L147-161 → TOTP_ENABLED; disable ~L177 → TOTP_DISABLED (enroll ~L103 KHÔNG cần — chưa bật).
- users/auth-users.service.ts: lockUser ~L173-181 (audit 'user.locked') → USER_LOCKED; unlockUser ~L197-205 (audit 'user.unlocked') → USER_UNLOCKED.
- permission/permission-admin.service.ts: assignRole ~L71-117 (audit 'RoleAssigned/RoleReassigned') → ROLE_ASSIGNED; revokeRole ~L136-147 (audit 'RoleRevoked') → ROLE_REMOVED.

INVARIANTS: #1 company_id qua DB DEFAULT currentCompanyDefault (current_setting) trong withTenant — writer KHÔNG nhận companyId, KHÔNG mở tx mới (dùng tx truyền vào, mirror AuditService.record). #2 append-only: chỉ INSERT, grant giữ SELECT,INSERT (mig 0443), KHÔNG xin thêm; audit_logs GIỮ NGUYÊN (dual-write). #3 payload mask qua AuditMaskerService (@Global, EventsModule) — reader đã KHÔNG select payload; writer mask-at-write theo SENSITIVE_STEMS (password/token/secret/…).

WRITER HOME + DI: đặt writer ở apps/api/src/auth/security-event-writer.service.ts (cạnh reader). Writer STATELESS (chỉ phụ thuộc AuditMaskerService @Global) → để tránh import-cycle Auth↔Permission (đã forwardRef) và Auth↔Users: khuyến nghị đăng ký SecurityEventWriter làm provider RIÊNG trong TỪNG module (auth/users/permission) — mỗi instance đồng nhất, KHÔNG cần cross-module import ngoài import file class. (Thay thế: export từ AuthModule + import — nhưng dễ đụng cycle, ưu tiên per-module provider.)

MIGRATION: KHÔNG cần. event_type là text (không enum → không thiếu enum), severity CHECK IN(info,low,medium,high,critical) khớp contracts SECURITY_EVENT_SEVERITIES, RLS ENABLE+FORCE + tenant_isolation + GRANT SELECT,INSERT đã có ở mig 0443. Head hiện tại idx ~0465 — KHÔNG thêm file migration.

VERIFY/GATE: DB cô lập LANE_DB (scripts/lane-db-setup.sh) bắt buộc — memory 'integration LANE_DB gate' + 'vitest unit specs colocated' (integration để apps/api/test/integration/*.int-spec.ts, xác nhận xuất hiện trong run summary). FULL gate auth crown: security-reviewer + silent-failure-hunter + santa-method; red-zone → người chốt, KHÔNG auto-merge.

OUT-OF-SCOPE (không làm ở WO này, ghi để không scope-creep): PERMISSION_CHANGED (role_permissions đổi qua role-admin.service.assignPermissionToRole) — có trong §22.2 nhưng KHÔNG trong done_when tối thiểu → follow-up WO. admin-users.service suspend/reactivate (status 'suspended' ≠ 'locked') — KHÔNG map USER_LOCKED (spec §22.2 USER_LOCKED = status locked); cân nhắc follow-up nếu cần audit suspend. FE apps/app/.../SecurityEventsPage.tsx đã tồn tại, ngoài paths — KHÔNG chạm. NOTI event (§23) triển khai sau.

PITFALL: forgotPasswordImpl chạy trong try/catch uniform-void (anti-enumeration) — writer.record phải nằm TRONG tx (cạnh outbox.enqueue + audit.record ~L1066-1077), CHỈ khi user tồn tại; lỗi ⇒ rollback cả event (không lộ email tồn tại). refresh reuse-branch commit (không throw) → writer.record TRƯỚC return {kind:'reuse'} vẫn cùng tx. Đừng để writer throw làm hỏng luồng nghiệp vụ chính — nhưng vì cùng tx & append-only đơn giản, để lỗi propagate (fail-closed) là đúng, KHÔNG nuốt im (silent-failure).
</content>
