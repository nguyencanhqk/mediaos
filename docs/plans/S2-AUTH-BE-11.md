```yaml
wo: S2-AUTH-BE-11
zone: red
generated_by: auto-loop
reconciled_at: "68616f9"
lanes: [{"id":"l1-contracts","task":"Additive contracts (permission.ts): thêm requiresTwoFactor: z.boolean().optional() vào createRoleSchema + updateRoleSchema; thêm requiresTwoFactor: z.boolean() vào roleWriteResultSchema. two-factor.ts twoFactorStatusSchema.required ĐÃ CÓ trên base → chỉ verify no-op, KHÔNG breaking client cũ. Foundation lane — L3 depends.","builder":"backend-builder","paths":["packages/contracts/src/permission.ts","packages/contracts/src/two-factor.ts"]},{"id":"l2-2fa-enforce-disable","task":"CROWN: (1) requiresTwoFactorTx = role-flag (roles.requires_two_factor) OR users.require_two_factor (đọc trong CÙNG tx). (2) POST /auth/2fa/disable khi requiresTwoFactorTx=true → 409 ConflictException code=TWO_FACTOR_ENFORCED (export const mirror TWO_FACTOR_SETUP_REQUIRED của enforcement guard), fail-closed TRƯỚC delete/audit — KHÔNG xoá totp/recovery, KHÔNG revoke, KHÔNG audit, KHÔNG security-event. Giữ nguyên wiring BE-8: audit auth.2fa_disabled + TOTP_DISABLED CHỈ khi disable thành công. Tùy chọn fail-fast: check sớm trong auth.service.disableTwoFactor trước password re-auth. status.required + mustSetupTwoFactor(me) + TwoFactorEnforcementGuard TỰ ăn theo qua requiresTwoFactorTx — KHÔNG sửa guard (đúng WO 'KHÔNG đổi chỗ khác'). Deny-path RED viết TRƯỚC.","builder":"backend-builder","paths":["apps/api/src/auth/two-factor.service.ts","apps/api/src/auth/auth.service.ts","apps/api/src/auth/two-factor.service.spec.ts","apps/api/test/integration/two-factor.int-spec.ts"]},{"id":"l3-role-write-flag","task":"CROWN (depends l1-contracts): POST/PATCH /auth/roles nhận requiresTwoFactor optional — CHỈ role thường. insertRoleTx/updateRoleTx nhận+set cột requiresTwoFactor; createRole/updateRole ghi audit RoleCreated/RoleUpdated before/after CÓ requiresTwoFactor (diff cờ); roleWriteResult trả cờ. System role: rule cấm sửa HIỆN CÓ (updateRole isSystem=true → BadRequest 400) đã bao trọn field mới → gửi requiresTwoFactor lên system role tự động 4xx, 0 update/0 audit. Deny-path RED viết TRƯỚC ở role-admin.service.spec.ts.","builder":"backend-builder","paths":["apps/api/src/permission/role-admin.service.ts","apps/api/src/permission/role-admin.repository.ts","apps/api/src/permission/role-admin.dto.ts","apps/api/src/permission/role-admin.service.spec.ts"]}]
acceptanceChecks: ["Bước 0: base có migration 0466 + cột users.require_two_factor + pair reset-2fa:user + grant company-admin; KHÔNG sinh migration mới. Base thiếu → needs_human (không tự vá).","requiresTwoFactorTx(tx,userId) = true khi (roles.requires_two_factor) HOẶC (users.require_two_factor); đọc trong CÙNG tx của login/status; chứng minh bằng test nguồn per-user-only (role không cờ).","GET /auth/2fa/status trả {enabled, required}; required=true cho cả nguồn role và nguồn per-user, false khi không nguồn nào; twoFactorStatusSchema.required tồn tại (đã có, non-breaking).","POST /auth/2fa/disable khi bị ép (requiresTwoFactorTx=true) → HTTP 409 + machine code TWO_FACTOR_ENFORCED; userTotp + userRecoveryCodes KHÔNG bị xoá (isEnabled vẫn true); KHÔNG ghi audit auth.2fa_disabled; KHÔNG ghi security-event TOTP_DISABLED; không revoke gì.","POST /auth/2fa/disable khi KHÔNG bị ép → 200; xoá secret+recovery; audit auth.2fa_disabled + security-event TOTP_DISABLED (đúng wiring BE-8) — hành vi cũ không đổi (regression).","POST/PATCH /auth/roles nhận requiresTwoFactor optional; role thường set được đúng giá trị; roleWriteResult trả requiresTwoFactor; audit RoleCreated/RoleUpdated before/after CHỨA requiresTwoFactor (diff cờ).","PATCH /auth/roles/:id trên system role (isSystem=true) kèm requiresTwoFactor → 4xx (400 rule hiện có), 0 update, 0 audit.","Contracts additive: createRoleSchema/updateRoleSchema/roleWriteResultSchema thêm field; pnpm typecheck (contracts+api+web) xanh; client cũ (không gửi field) không vỡ.","Bất biến §2: mọi truy vấn qua withTenant(companyId) (RLS+FORCE); 2-tenant deny xanh; audit role + 2FA append-only (§2.2); KHÔNG log/return secret/plaintext (§2.3).","DoD §8: FULL gate security-reviewer (auth crown) PASS + người chốt; coverage ≥80% nhánh mới; regression auth suite xanh; harness/backlog.mjs cập nhật."]
testTasks: ["int-spec (LANE_DB cô lập, two-factor.int-spec.ts) (a): user bị ép PER-USER (users.require_two_factor=true, role KHÔNG cờ) → 2fa/disable 409 code=TWO_FACTOR_ENFORCED + isEnabled vẫn true + KHÔNG audit/security-event TOTP_DISABLED.","int-spec (b): user bị ép QUA ROLE (roles.requires_two_factor=true) → disable 409, vẫn enabled.","int-spec (c) regression: user KHÔNG bị ép → disable OK (xoá secret+recovery, audit auth.2fa_disabled + TOTP_DISABLED ghi đúng).","int-spec (d): status.required đúng cho CẢ 3 nguồn — role=true, per-user=true, không-nguồn=false.","unit role-admin.service.spec.ts (e): createRole/updateRole requiresTwoFactor trên role thường → set + audit before/after diff cờ; updateRole system role (isSystem=true) gửi requiresTwoFactor → BadRequest 400, 0 update, 0 audit.","int-spec (f): 2-tenant deny giữ nguyên (cross-tenant không đọc/ghi role/2FA của tenant khác); toàn bộ regression auth suite xanh.","QA sign-off: FULL gate security-reviewer (auth-crown) + người chốt; coverage ≥80% cho nhánh mới; verify RED-trước-GREEN cho mọi deny-path."]
steps: ["Bước 0 (read-only, KHÔNG tạo migration): verify base có migration 0466 (users.require_two_factor + pair reset-2fa:user + grant company-admin). Base đúng = feat/security-wave1 / auto/S2-AUTH-BE-11 (chứa 0198f38). Nếu chạy trên nhánh THIẾU 0466 (vd wip/s2-fe-hr-5-hr5-wc, origin/master @9961849) → DỪNG needs_human (đừng tự tạo 0467).","L1 contracts additive: permission.ts (createRole/updateRole/roleWriteResult + requiresTwoFactor) + xác nhận two-factor.ts.required đã có → pnpm --filter @mediaos/contracts build (dual ESM/CJS).","RED trước: viết deny-path (a)-(f) — int-spec 2FA (LANE_DB) + unit role-admin.service.spec — cho ĐỎ trước khi impl.","L2 (auth/**): requiresTwoFactorTx OR users.require_two_factor cùng tx; disable() 409 fail-closed với code const; giữ TOTP_DISABLED/audit chỉ-khi-thành-công; (tùy chọn) early-check trong auth.service.disableTwoFactor.","L3 (permission/**, sau L1): role-admin insert/update set cờ + audit before/after diff + DTO trả cờ; dựa rule isSystem hiện có cho system-role 4xx.","GREEN trên DB cô lập: bash scripts/lane-db-setup.sh <lane> → export LANE_DB=mediaos_<lane> → pnpm --filter @mediaos/api test; pnpm typecheck + build xanh.","FULL gate security-reviewer (auth crown) + người chốt; cập nhật harness/backlog.mjs (DoD §8)."]
```

## Prose reconcile

**TIỀN-ĐỀ (bước 0) — TRẠNG THÁI THỰC:** migration 0466 (0198f38: `users.require_two_factor` + pair/grant `reset-2fa:user`) KHÔNG nằm trên nhánh làm việc hiện tại (`wip/s2-fe-hr-5-hr5-wc` @68616f9, head mig 0465) và KHÔNG trên `origin/master` (@9961849/#103). Nó CHỈ có trên `feat/security-wave1` (+origin) và `auto/S2-AUTH-BE-11`. ⇒ Đội 2 PHẢI thực thi trên base `feat/security-wave1` (hoặc worktree `auto/S2-AUTH-BE-11`); nếu base thiếu 0466 → DỪNG `needs_human`, KHÔNG tự tạo migration 0467. Không cần lane db-migration.

**GAP-ANALYSIS (đối chiếu CODE trên base `feat/security-wave1`):**
- done_when#1 (0466): ĐÃ CÓ trên base (`users.ts` line 41 `requireTwoFactor` col; migration-smoke section 7). CHỈ verify read-only.
- done_when#2 (`requiresTwoFactorTx` OR per-user): CHƯA — `two-factor.service.ts` `requiresTwoFactorTx` hiện CHỈ join `roles.requires_two_factor`. Cần OR thêm `users.require_two_factor` đọc cùng tx. `mustSetupTwoFactor` (`auth.service.ts` ~line 1021) + `TwoFactorEnforcementGuard` đều gọi `requiresTwoFactorTx`/`requiresTwoFactor` → TỰ ăn theo, KHÔNG sửa (đúng WO). LƯU Ý by-design: guard chỉ enforce role/per-user khi global `TWO_FACTOR_ENFORCEMENT_ENABLED=true` hoặc company-policy bật (nhánh `globalEnabled?...:false` giữ nguyên) — per-user với global OFF sẽ KHÔNG bị guard chặn; đây là "KHÔNG đổi chỗ khác" → out-of-scope, đừng mở rộng.
- done_when#3 (`status.required`): ĐÃ CÓ trên base — `status()` trả `{enabled, required}` (`two-factor.service.ts` line 100-109) và `twoFactorStatusSchema.required = z.boolean()` (`contracts/two-factor.ts` line 46-49), controller `GET /auth/2fa/status` trả `TwoFactorStatus`. ⇒ Chỉ còn phần semantic: sau khi sửa `requiresTwoFactorTx` thì `required` phản ánh cả per-user. KHÔNG cần đổi contract status.
- done_when#4 (disable 409): CHƯA — `disable()` (`two-factor.service.ts` line 197) + `auth.service.disableTwoFactor` (line 506) KHÔNG check enforcement. Thêm 409 fail-closed. VỊ TRÍ khuyến nghị: đặt TRONG `TwoFactorService.disable` tx (atomic + defense-in-depth, đọc `require_two_factor` cùng tx trước delete) — throw `ConflictException({code:TWO_FACTOR_ENFORCED,message})`. Tùy chọn early-check trong `auth.service.disableTwoFactor` để fail-fast trước password re-auth. Giữ audit `auth.2fa_disabled` + `SecurityEventWriter TOTP_DISABLED` chỉ khi thực xoá (đã có `if deleted.length>0`).
- done_when#5 (role-write `requiresTwoFactor`): CHƯA — `createRoleSchema`/`updateRoleSchema`/`roleWriteResultSchema` (`contracts/permission.ts`) thiếu field; `insertRoleTx`/`updateRoleTx` (`role-admin.repository.ts`) không set cờ; `createRole`/`updateRole` audit before/after chỉ `{name,description}`. System-role guard đã có (`updateRole isSystem→400`) → tự bao field mới. Role type (`schema/permissions.ts` line 21 `requiresTwoFactor`) đã tồn tại nên repo set được ngay. Audit masker giữ boolean `requiresTwoFactor` (không PII/secret) → diff ghi được.

**PATH GAP (guard-scope sẽ cảnh báo — cần mở rộng WO.paths):**
- Role-write (done_when#5) nằm ở `apps/api/src/permission/**` — NGOÀI paths khai báo (`auth/**`, `users/**`, `contracts/**`). Đã bổ sung vào `workOrder.paths`.
- Integration test 2FA ở `apps/api/test/integration/**` — cũng ngoài paths → bổ sung.
- `apps/api/src/users/**` thực tế KHÔNG bị chạm (per-user flag do WO khác admin ghi; ở đây chỉ ĐỌC bảng users trong auth service) — giữ trong khai báo cho an toàn nhưng lane không dùng.

**MÃ LỖI (spec-gap):** SPEC-02 đánh dấu 2FA "Phase sau" (line 151) → KHÔNG có `AUTH-ERR` chính tắc cho disable-khi-bị-ép. Theo convention `AUTH-ERR` hiện có = pattern machine-code đã dùng trong 2FA code: enforcement guard ném `ForbiddenException({code:TWO_FACTOR_SETUP_REQUIRED})`. ⇒ Dùng `ConflictException({code:'TWO_FACTOR_ENFORCED', message})`, export const `TWO_FACTOR_ENFORCED` cạnh `TWO_FACTOR_SETUP_REQUIRED`. Pin đúng chuỗi code trong acceptance; đây là impl-defined (spec 2FA hoãn) — ghi vào spec khi 2FA được spec hoá.

**INVARIANTS:** (§2.1) mọi access qua `withTenant(companyId)` — `requiresTwoFactorTx`/disable/role-write đều trong tx tenant; RLS roles `USING company_id own OR NULL`, `WITH CHECK own-tenant` chặn ghi system role. (§2.2) `audit_logs` + `user_security_events` append-only — 409 path KHÔNG ghi gì; success path ghi trong CÙNG tx với mutation. (§2.3) không log/return secret — 2FA giữ envelope-encrypt; `requiresTwoFactor` là cờ boolean không nhạy cảm.

**VERIFY (RED-trước-GREEN, DB cô lập):** `apps/api` chỉ chạy `src/**/*.spec.ts` cho unit — spec role-admin để colocated. int-spec chạy qua `test/integration` gate `hasDb && LANE_DB` (nếu chỉ `hasDb` qua `.env` → đỏ-giả trên DB dev chung). Dùng `bash scripts/lane-db-setup.sh` + `export LANE_DB`. `two-factor.int-spec.ts` đã có helper `seedUser`/`seedUserRole`/`seedCompany` + 2-tenant A/B → mở rộng thêm case per-user (UPDATE `users.require_two_factor` trực tiếp trên seed) + disable-409.

**GATE:** FULL (auth crown) — `security-reviewer` + `silent-failure-hunter`(nếu có) + `typescript-reviewer` + `quality-gate`; crown → `santa-method`. Model routing: IMPLEMENT/REVIEW = Opus (crown); plan-stage đã xong (đây). Người chốt red-zone TRƯỚC merge.

**OUT-OF-SCOPE (chống scope-creep):** (1) admin reset-2fa endpoint dùng pair `reset-2fa:user` — là WO khác (S2-AUTH-DB-4/admin-reset), KHÔNG làm ở đây; ở WO này `users.require_two_factor` chỉ ĐỌC. (2) Không đổi `TwoFactorEnforcementGuard` logic global/company-policy. (3) Không đổi contract status (`required` đã có). (4) Không tạo migration mới.
