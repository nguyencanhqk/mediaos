# S15-PAYROLL-BE-5 — Track D phần 1: Tổng quan · Lời nhắc · 7 báo cáo · tổng cột toàn kỳ

> 🟡 Chở tiền nhưng CHỈ ĐỌC. **Opus** code; gate = `typescript-reviewer` + `security-reviewer` HẸP (IDOR · sàn scope ·
> cặp nguồn · `company_id` trên TỪNG bảng của mọi câu SQL tay). Plan-review đối kháng **một vòng**.
> Nhánh `feat/s15-payroll-be-5` từ master `3e08ac7f`. Lane `mediaos_be5` (head `0575` — **KHÔNG migration**).
> PDF 083–085 **KHÔNG thuộc WO này** (owner O-1) ⇒ `S15-PAYROLL-BE-5B`.

## 0. Quyết định

### 0.1 Owner chốt 17/09/2026 (AskUserQuestion)

| #   | Câu hỏi                                                   | Chốt                                                                                                                                                                                                                                                                                                                                                       |
| --- | --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| O-1 | Tách WO?                                                  | **Tách.** BE-5 = 078–082 + tổng cột toàn kỳ; BE-5B = PDF 083–085 + hạ tầng tệp server. BE-5 khoá **82** route (không 85).                                                                                                                                                                                                                                  |
| O-2 | Báo cáo lộ tiền THEO NGƯỜI có đòi thêm cặp đọc của nguồn? | **Có.** `employee-income` + `view-payslip:payslip` · `salary-history` + `view:salary-profile` · `payment-summary` + `view:payment-batch` · `budget-status` + `view:payroll-budget`. 080 **chỉ liệt kê** báo cáo caller mở được (SPEC-11 §9.1 PAY-SCREEN-016 «lọc theo quyền»). Role seed (officer/admin) giữ đủ cả 4 cặp ⇒ không đổi hành vi với vai seed. |
| O-3 | PDF giao thế nào? (cho BE-5B)                             | Signed-URL qua file-service + job dọn tệp tạm xoá CẢ object; 085 chạy nền.                                                                                                                                                                                                                                                                                 |

### 0.2 Quyết định thiết kế (lật rẻ — ghi chỗ đổi)

| #    | Câu hỏi                            | Chốt                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Đổi ở đâu                                      |
| ---- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| D-1  | Nguồn số của báo cáo/tổng quan     | **Phiếu lương** (`payslips` + `payslip_items`) của kỳ `status ∈ PUBLISHED_PERIOD_STATUSES` (import — KHÔNG danh sách thứ hai), `period_month ∈ [fromMonth, toMonth]`. Cùng nguồn với «thực hiện» ngân sách (BE-4 D-4) ⇒ báo cáo, tổng quan, 073 ra cùng một con số. Kỳ `Approved` (đã sinh phiếu, chưa phát hành) KHÔNG tính — nó là Lời nhắc #1                                                                                                                                 | `payroll-report.sql.ts` `publishedPayslipsCte` |
| D-2  | Đơn vị của một phiếu               | `employee_profiles.org_unit_id` **HIỆN TẠI** của chủ phiếu (hồ sơ sống) — đúng BE-4 D-4/C12; ghi DTO doc «theo đơn vị hiện tại». `payslips` không lưu đơn vị                                                                                                                                                                                                                                                                                                                     | cùng CTE                                       |
| D-3  | Chi phí DN (BH phần công ty)       | Σ `component_values_json.components[kind='statutory_employer'].value` của `payroll_period_lines` sống cùng (kỳ, người) — kỳ đã phát hành thì dòng đã đóng băng (`FROZEN_STATUSES`). Dòng v1 (không khoá `components`) ⇒ 0. `totalCost = gross + employerInsurance`. **Ngân sách vẫn đo bằng gross** (không đổi 073)                                                                                                                                                              | `employerInsuranceSql`                         |
| D-4  | Bảo hiểm NV / thuế TNCN theo người | Σ `abs(payslip_items.amount)` theo `meta->>'kind'` = `statutory_employee` / `tax`. Phiếu v1 ⇒ 0                                                                                                                                                                                                                                                                                                                                                                                  | R1 builder                                     |
| D-5  | Tham số báo cáo                    | MỘT query schema phẳng `.strict()` (`fromMonth · toMonth · orgUnitId · userId · fiscalYear · batchStatus · page · per_page`); mỗi báo cáo khai `required[]`; service kiểm ⇒ **400 `VALIDATION-ERR-001`** có `field`. Khoảng tháng `fromMonth ≤ toMonth`, tối đa **24 tháng** (NFR §19.1 «12 kỳ < 2s» — 24 là trần an toàn). Tham số không áp cho báo cáo đó ⇒ bỏ qua (không 400)                                                                                                 | `payroll-reports.registry.ts`                  |
| D-6  | Trần dòng                          | `count(*)` theo bộ lọc **> 50.000** ⇒ **422 `PAYROLL-ERR-031` `report-too-large`** ở CẢ 081 lẫn 082 (SPEC-11 §12.1 — **không** tái dùng 016 như backlog cũ ghi). Đếm TRƯỚC khi đọc dòng                                                                                                                                                                                                                                                                                          | service                                        |
| D-7  | Hình DTO 081                       | `paginated({ reportCode, columns[{key,type}], rows[], totals }, pagination)`; `rows` = object theo `key`; tiền = `number` (khuôn DTO PAYROLL hiện có); `totals` = SUM các cột tiền trên **cả bộ lọc** (một câu aggregate, không cộng JS). Nhãn cột ở FE (i18n); nhãn XLSX ở BE (khuôn 017)                                                                                                                                                                                       | registry + mapper                              |
| D-8  | 080                                | Trả `[{ code, requiredParams[], optionalParams[], columns[], exportable }]` — chỉ báo cáo mà caller qua được CẢ `view:payroll-report` + cặp nguồn (kiểm MỀM `resolveOrNull` + sàn Company, khuôn `canRevealTaxCode`); `exportable` = thêm `export:payroll`@Company. **0 audit** (SPEC-11 §18.1 B)                                                                                                                                                                                | `PayrollAccessService.canResolve`              |
| D-9  | 082                                | Decorator `view:payroll-report`; service assert **`reportData` + `periodExport` + cặp nguồn** (khuôn 071 BA cặp). XLSX exceljs, chuỗi qua `xlsxSafe`, hàng tổng cuối; trần 50.000 (không dùng 10.000 của 017 — SPEC §12.1 cấp 031 riêng cho báo cáo). audit `action:'export'`                                                                                                                                                                                                    | `payroll-report-export.service.ts`             |
| D-10 | Tổng quan 078 — kỳ neo             | `latestPeriod` = kỳ phát hành có `period_month` lớn nhất ≤ `toMonth` (mặc định: lớn nhất). Xu hướng = `months` kỳ (mặc định 12, trần 24) kết thúc ở `toMonth`. `fiscalYear` mặc định = năm của `latestPeriod` (không có kỳ ⇒ năm hiện tại theo TZ công ty). Không có kỳ phát hành ⇒ khối rỗng + `latestPeriod: null` (200, không 404)                                                                                                                                            | `payroll-overview.service.ts`                  |
| D-11 | 6 khối                             | (1) phân bố gross kỳ neo theo 7 dải cố định `[0,5tr) [5,10) [10,15) [15,20) [20,30) [30,50) [50,∞)` (hằng contracts) · (2) cơ cấu thu nhập kỳ neo = R3 builder, top 8 khoản thu + «khác» · (3) ngân sách năm: kế hoạch = hàng toàn công ty nếu có, không thì Σ hàng đơn vị; thực hiện = CTE D-4 của BE-4 (tách thành hàm export, không chép) · (4)+(5) chi phí & thu nhập BQ theo kỳ = R2 builder · (6) theo đơn vị kỳ neo = R4 builder (avg/min/max)                            | builders dùng chung                            |
| D-12 | Lời nhắc 079                       | #1 kỳ `Approved` có ≥ 1 phiếu (đếm kỳ + phiếu, ≤ 5 kỳ cũ nhất `{id, periodMonth, payslipCount}`) · #2 `insuranceIssue='not-joined'` · #3 `insuranceIssue='salary-out-of-range'` — #2/#3 **dùng CHÍNH vị từ** mà filter mới của 036 dùng ⇒ số đếm lời nhắc = `pagination.total` của 036 cùng filter (ca int ghim đẳng thức)                                                                                                                                                       | `PayrollEmployeesRepository.insuranceIssueSql` |
| D-13 | «NV chính thức»                    | `employee_profiles.status='active'` · `employment_type ∈ {full_time, part_time}` · `official_date IS NULL OR official_date ≤ hôm nay` (TZ công ty) · user sống. «Chưa tham gia BH» = không có hàng `payroll_employee_settings` sống HOẶC `joins_social_insurance = false`                                                                                                                                                                                                        | cùng hàm                                       |
| D-14 | «Lương BH ngoài quy định»          | NV chính thức, `joins_social_insurance = true`, hồ sơ lương hiệu lực hôm nay (`effective_date ≤ today` mới nhất — CÙNG vị từ `effectiveProfilesTx`), căn cứ = `COALESCE(probation_salary, insurance_salary, base_salary)` như engine §3.11 — **bỏ qua** hồ sơ `NET` mà cả hai cột kia NULL (engine dùng `b` sau gross-up, không biết trước); ngoài `[min_region_wage, si_cap]` của bản tỉ lệ hiệu lực hôm nay. Không có bản tỉ lệ ⇒ `count: 0` + `rateMissing: true` (không lỗi) | cùng hàm                                       |
| D-15 | Tổng cột toàn kỳ (nợ FE-2)         | **Mở rộng 018**: `GET /payroll-periods/summary?payrollPeriodId=<uuid>` ⇒ tóm tắt KỲ ĐÓ + `lineTotals` (8 cột tiền) + `componentTotals[{code,label,kind,sortOrder,total}]` — SUM trên `payroll_period_lines` sống (jsonb_array_elements), không cộng JS. Vắng tham số ⇒ hành vi cũ (kỳ mới nhất, KHÔNG tính totals — widget DASH không đổi). Kỳ không tồn tại ⇒ 404 010. Cùng cặp `view-line` + sàn Company. **Không thêm route** (50 mã đã đóng). Audit 018 thêm `scope`         | `PayrollCalcRepository.periodTotalsTx`         |
| D-16 | "Hôm nay"                          | SQL `(now() at time zone coalesce(c.timezone, <mặc định>))::date` đọc `companies` trong cùng câu; mặc định = `getSettingDefault('company.timezone')` (không chép chuỗi)                                                                                                                                                                                                                                                                                                          | `payroll-report.sql.ts`                        |
| D-17 | Không cache                        | 078/079/081/082 không cache, không memo; audit trong CÙNG tx với lượt đọc (rollback ⇒ 0 audit)                                                                                                                                                                                                                                                                                                                                                                                   | —                                              |

## 0b. Vá plan-review vòng 1 (17/09) — ĐÈ lên §0.2–§5 ở mọi chỗ mâu thuẫn

Verdict vòng 1 = **REVISE** (7 BLOCKING + cảnh báo). Vá đủ 7/7 + nhận các cảnh báo rẻ ⇒ **PASS, KHÔNG mở vòng 2**
(memory `plan-review-rounds-inject-new-holes`).

| ID     | Lỗ                                                                                                                                                                               | Vá                                                                                                                                                                                                                                                                                                              |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **B1** | Filter `insuranceIssue=salary-out-of-range` của 036 là vị từ TRÊN LƯƠNG nhưng 036 chỉ gác `view:payroll-employee` (money-free) ⇒ ghép `q=<mã NV>` dò được lương BH của một người | Giá trị `salary-out-of-range` đòi THÊM `view:salary-profile`@Company (kiểm mềm, khuôn `canRevealTaxCode`) — thiếu ⇒ **403**. `not-joined` giữ nguyên. Ca int deny + ALLOW                                                                                                                                       |
| **B2** | Khối 3 (ngân sách) của 078 lách O-2 (`budget-status` đòi `view:payroll-budget`)                                                                                                  | Khối `budget` chỉ có mặt khi `canResolve(user,'budgetList')`; thiếu ⇒ **vắng khoá** (DTO `.optional()`). Ca int hai phía                                                                                                                                                                                        |
| **B3** | R3/khối 2 gom theo `label` ⇒ nhãn `Điều chỉnh: <lý do>` · `Nghỉ không lương (N ngày)` thành hàng riêng theo người                                                                | Khoá gom = `coalesce(meta->>'componentCode', item_type)` × chiều (dấu). Nhãn: v2 = `min(label)` theo mã (nhãn cột mẫu, không riêng người); v1/`meta` NULL = **bảng hằng** theo `item_type` — KHÔNG bao giờ chiếu nhãn item. Ca int: công ty D có 2 điều chỉnh khác lý do ⇒ 1 hàng, không chuỗi lý do nào lọt    |
| **B4** | «SUM mọi cột tiền» sai: `budget-status` cộng trùng hàng công ty + đơn vị; cộng cả avg/min/max/baseSalary                                                                         | Registry khai `total: 'sum' \| 'none'` từng cột. `budget-status` tổng = SQL riêng `budgetYearTotalsTx`: lọc đơn vị ⇒ hàng đó; không lọc ⇒ hàng công ty nếu có, không thì Σ hàng đơn vị (thực hiện Σ trên CÙNG các đơn vị đó) — dùng CHUNG cho khối 3. XLSX dùng cùng quy tắc; không cột `sum` ⇒ không hàng tổng |
| **B5** | D-16 đọc `companies` mà không ràng `c.id` — RLS `companies` đọc chéo tenant (`qual=true`)                                                                                        | `where c.id = ${companyId} and c.deleted_at is null` tường minh; §3 thêm `companies` vào danh sách bảng; fixture công ty thứ hai đổi `timezone`                                                                                                                                                                 |
| **B6** | Thiếu deny phân biệt: (a) `view:payroll-report`@Company + cặp nguồn/export @Department; (b) 082 thiếu cặp nguồn cho cả 4 mã; (c) `orgUnitId`/`userId` của công ty khác           | (a) user `mixedScope` ⇒ 081/082 403, 080 ẩn 4 mã + `exportable:false` (đột biến (a) giờ đo được); (b) vòng 4 mã; (c) `orgUnitId`/`userId` của B ⇒ 0 hàng; 018 kỳ của B ⇒ 404 (đã có)                                                                                                                            |
| **B7** | Assert cặp nguồn dùng biến ⇒ census 2 tầng mù O-2; §5 thiếu 2 pin                                                                                                                | Census mới: ĐẲNG THỨC map `{code → sourceRouteKey}` + AST: site 081/082 gọi `resolveActor(…, <x>.sourceRouteKey)`. Pin: ca (8) sensitive 29→30 / distinct 32→33 · `payroll-error-code-census` 32→33                                                                                                             |
| C1     | `employerInsurance` gồm cả KPCĐ                                                                                                                                                  | đổi tên **`employerStatutory`** (Σ kind `statutory_employer`)                                                                                                                                                                                                                                                   |
| C2     | Thuế do công ty chịu (`pitPayer=COMPANY`) không có item                                                                                                                          | DTO doc ghi rõ `pitWithheld` = thuế KHẤU TRỪ của NV; D-4 dùng `-sum(amount)` (không `abs`)                                                                                                                                                                                                                      |
| C3     | net ≠ gross − khấu trừ khi có điều chỉnh/kẹp 0                                                                                                                                   | R1 thêm `totalAdjustment` (Σ có dấu)                                                                                                                                                                                                                                                                            |
| C4     | `changePct`                                                                                                                                                                      | `LAG` `PARTITION BY user_id` trên MỌI phiên bản sống TRƯỚC khi lọc; đổi GROSS↔NET ⇒ NULL                                                                                                                                                                                                                        |
| C5     | `payment-summary`                                                                                                                                                                | Σ net qua `payslips` của dòng sống — cùng biểu thức 066                                                                                                                                                                                                                                                         |
| C6     | Tên NV đã xoá mềm vắng (`namesByUserIdsTx`)                                                                                                                                      | chấp nhận, ghi DTO doc; tra tên CHIA LÔ 5.000 id (export 50.000 dòng)                                                                                                                                                                                                                                           |
| C7     | Nhóm nhỏ (đơn vị 1 người) lộ lương qua avg/min/max ở R4/khối 6                                                                                                                   | **Rủi ro chấp nhận có ghi** (R2 §7): cặp `view:payroll-report` sensitive + sàn Company, chỉ officer/admin giữ; done_when khối 6 đòi avg/min/max. Báo owner ở PR                                                                                                                                                 |
| C8     | 079 phải gọi CHÍNH `countTx` của 036; fixture ≥1 khớp/≥1 không                                                                                                                   | 079 gọi `PayrollEmployeesRepository.countTx({insuranceIssue})`; fixture 12 nhân sự phủ nhánh, ngày cách «hôm nay» ≥ 2 ngày                                                                                                                                                                                      |
| C9     | > `si_cap` không phải vi phạm (engine kẹp)                                                                                                                                       | GIỮ cả hai phía (chữ «ngoài quy định» của PAY-DEC-018) — ghi DTO doc «lương BH khai vượt trần sẽ bị kẹp khi tính»; chỉ `si_cap` (không `hi_cap`/`ui_cap`)                                                                                                                                                       |
| C10    | `canResolve` thành helper «cặp phụ cho tiền»                                                                                                                                     | tham số là union ĐÓNG `PayrollSoftGateKey` (`payslipList`·`salaryProfileList`·`batchList`·`budgetList`·`periodExport`)                                                                                                                                                                                          |
| C11    | Trần 031                                                                                                                                                                         | unit phủ 081 + 082: đếm TRƯỚC khi đọc dòng, 0 audit; đếm trên số hàng SAU gom nhóm                                                                                                                                                                                                                              |
| C12    | 018                                                                                                                                                                              | `componentTotals[].isVisible` (= bool_or); nhãn tất định `min(label)`; `payrollPeriodId` rác ⇒ 400                                                                                                                                                                                                              |
| C13    | Cache trình duyệt                                                                                                                                                                | `Cache-Control: no-store` cho 078/079/081/082                                                                                                                                                                                                                                                                   |
| C14    | Hai nghĩa «kỳ mới nhất» (018 mọi trạng thái · 078 chỉ đã phát hành) · R3 trộn hạt v1/v2                                                                                          | ghi DTO doc; không bảng quy đổi v1↔v2 (ngoài phạm vi)                                                                                                                                                                                                                                                           |

## 1. Ranh giới

**TRONG:** 5 route `PAYROLL-API-078..082` · 018 thêm `payrollPeriodId` + totals · 036 thêm filter `insuranceIssue` · mã `PAYROLL-ERR-031` (kind `report-too-large`; `pdf-batch-too-large` để BE-5B) · census/FE pin · SPEC-11 §15.1 hàng 018/036/078–082 + §12.1 + API-18 §5b · route-census artifact.

**NGOÀI:** PDF 083–085 (BE-5B) · màn FE (FE-4) · widget DASH 002/003 (DASH-1) · lịch gửi báo cáo (PARK-002) · đổi cách tính «thực hiện» ngân sách.

## 2. Route

| Mã  | Route                                     | Key                 | Decorator             | Service assert                                             | Sàn | Audit (§18.1 B)                                                                         |
| --- | ----------------------------------------- | ------------------- | --------------------- | ---------------------------------------------------------- | --- | --------------------------------------------------------------------------------------- |
| 078 | `GET /payroll/overview`                   | `overview`          | `view:payroll-report` | `overview`                                                 | ✅  | `read` · `payroll_report` · NULL · `{reportCode:'overview', latestPeriodMonth, months}` |
| 079 | `GET /payroll/overview/reminders`         | `overviewReminders` | idem                  | `overviewReminders`                                        | ✅  | `{reportCode:'overview.reminders'}`                                                     |
| 080 | `GET /payroll/reports`                    | `reportList`        | idem                  | `reportList` (+ `canResolve` mềm)                          | ✅  | **không**                                                                               |
| 081 | `GET /payroll/reports/:reportCode`        | `reportData`        | idem                  | `reportData` + cặp nguồn                                   | ✅  | `{reportCode, filters, rowCount}`                                                       |
| 082 | `GET /payroll/reports/:reportCode/export` | `reportExport`      | idem                  | `reportExport` + `reportData` + `periodExport` + cặp nguồn | ✅  | `export` · `{reportCode, filters, rowCount, format:'xlsx'}`                             |

Controller mới `PayrollReportsController` (`@Controller("payroll")`, khai `reports` TRƯỚC `reports/:reportCode`). `:reportCode` validate bằng `z.enum` (400), không phải UUID. `MONEY_FREE_ROUTES` += `overviewReminders` · `reportList`.

## 3. Bảy báo cáo (`PAYROLL_REPORT_CODES`, contracts)

| code               | Tên                           | Cặp nguồn (O-2)     | Bắt buộc   | Tuỳ chọn                                             | Hạt               | Cột                                                                                                                                                           |
| ------------------ | ----------------------------- | ------------------- | ---------- | ---------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `employee-income`  | Tổng hợp thu nhập NV          | `payslipList`       | from/to    | orgUnitId                                            | 1 người           | employeeCode · displayName · orgUnitName · payslipCount · totalGross · insuranceEmployee · pitWithheld · totalDeduction · totalNet                            |
| `salary-by-period` | Thống kê lương theo thời gian | —                   | from/to    | orgUnitId                                            | 1 kỳ              | periodMonth · periodStatus · headcount · totalGross · totalDeduction · totalNet · avgGross · avgNet · employerInsurance · totalCost                           |
| `income-structure` | Cơ cấu thu nhập               | —                   | from/to    | orgUnitId                                            | 1 khoản × chiều   | componentCode · itemType · label · direction · totalAmount · sharePct                                                                                         |
| `cost-by-org-unit` | Chi phí lương theo đơn vị     | —                   | from/to    | orgUnitId                                            | 1 đơn vị          | orgUnitName · headcount · totalGross · employerInsurance · totalCost · totalNet · avgGross · minGross · maxGross                                              |
| `salary-history`   | Lịch sử lương NV              | `salaryProfileList` | —          | from/to (trên `effective_date`) · orgUnitId · userId | 1 phiên bản hồ sơ | employeeCode · displayName · orgUnitName · effectiveDate · salaryType · baseSalary · insuranceSalary · probationSalary · payRatioPct · itemsTotal · changePct |
| `payment-summary`  | Tổng hợp chi trả              | `batchList`         | from/to    | orgUnitId (lọc dòng) · batchStatus                   | 1 đợt             | batchCode · periodMonth · method · status · payDate · lineCount · paidLineCount · totalNet · completedAt                                                      |
| `budget-status`    | Tình hình ngân sách           | `budgetList`        | fiscalYear | orgUnitId                                            | 1 hàng ngân sách  | orgUnitName · plannedAmount · actualAmount · variance · usagePct                                                                                              |

- Tên người qua `PayrollPeopleRepository.namesByUserIdsTx(actor)` SAU khi phân trang ở SQL (ratchet identity: SQL thô không chạm `full_name`/`email`; ORDER BY `employee_code` là vị từ, không phải chiếu).
- `changePct` = `LAG(base_salary)` trong SQL, `round(…, 2)`, NULL ở phiên bản đầu/prev = 0.
- `sharePct` / `usagePct` / `avg*` tính ở SQL `round(…, 2)`.
- `budget-status` gọi `PayrollBudgetsRepository.listTx` (CÙNG số với 073), phân trang bằng cắt mảng — số hàng bị chặn bởi unique (năm, đơn vị) nên không có vòng tổng hợp JS; ghi rõ ở code.
- Mỗi câu: `company_id` bind ở **từng** bảng join (payslips · payroll_periods · payslip_items · payroll_period_lines · employee_profiles · org_units · salary_profiles · salary_profile_items · batches · lines).

## 4. File

| File                                                                                                                  | Việc                                                        |
| --------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| `src/payroll/payroll-route-pairs.const.ts`                                                                            | +5 key; JSDoc 77→82                                         |
| `src/payroll/payroll-access.service.ts`                                                                               | `canResolve(user, key)` (mềm + sàn); `MONEY_FREE_ROUTES` +2 |
| `src/payroll/payroll-report.sql.ts` (mới)                                                                             | CTE phiếu phát hành · đơn vị hiện tại · BH DN · hôm nay     |
| `src/payroll/payroll-reports.registry.ts` (mới)                                                                       | 7 định nghĩa: cặp nguồn · tham số · cột · nhãn XLSX         |
| `src/payroll/payroll-reports.repository.ts` (mới)                                                                     | 6 builder (rows/count/totals)                               |
| `src/payroll/payroll-reports.service.ts` (mới)                                                                        | 080/081                                                     |
| `src/payroll/payroll-report-export.service.ts` (mới)                                                                  | 082                                                         |
| `src/payroll/payroll-overview.repository.ts` + `.service.ts` (mới)                                                    | 078/079                                                     |
| `src/payroll/payroll-reports.controllers.ts` (mới)                                                                    | 5 route                                                     |
| `src/payroll/payroll-budgets.repository.ts`                                                                           | tách CTE «thực hiện» thành hàm export (cùng SQL)            |
| `src/payroll/payroll-employees.repository.ts` · `payroll-employees.service`/dto                                       | filter `insuranceIssue` + `insuranceIssueSql`               |
| `src/payroll/payroll-calc.repository.ts` · `payroll-calc.service.ts` · `payroll.controllers.ts` · `payroll.mapper.ts` | 018 D-15                                                    |
| `src/payroll/payroll.errors.ts`                                                                                       | `REPORT_TOO_LARGE` 031                                      |
| `src/payroll/payroll.module.ts`                                                                                       | provider/controller additive                                |
| `packages/contracts/src/payroll-reports.ts` (mới) + `index.ts`                                                        | enum mã · query · DTO 078–081 · dải lương                   |
| `packages/contracts/src/payroll.ts` · `payroll-employees.ts`                                                          | 018 query/DTO additive · 036 filter                         |
| `apps/app/src/routes/payroll/{constants,payroll-wiring.spec,payroll-errors}.ts` + i18n                                | FE pin 82 · kind 031                                        |
| test (§5) · `apps/api/package.json` `test:cov:payroll`                                                                | + file mới                                                  |
| `docs/spec/SPEC-11 PAYROLL.md` · `docs/API Design/API-18…` · `docs/_review/S6-SEC-ROUTEMAP-1-route-census.json`       | đồng bộ                                                     |

## 5. RED-first

**Int (`LANE_DB=mediaos_be5`) — viết & chạy ĐỎ trước code:**

1. `s15-payroll-be5-report-gates.int-spec.ts` — deny: thiếu `view:payroll-report` ⇒ 403 ở 078–082 · grant **Department** ⇒ 403 ở 078/079/080/081 · thiếu cặp nguồn ⇒ 081 `employee-income`/`salary-history`/`payment-summary`/`budget-status` 403 và 080 **không liệt kê** đúng mã đó (ALLOW đối chứng: officer thấy đủ 7) · thiếu `export:payroll` ⇒ 082 403 + 080 `exportable:false` · 403 để lại **0 audit** · mã lạ ⇒ 400 · thiếu tham số bắt buộc ⇒ 400 · khoảng > 24 tháng ⇒ 400 · khoá query lạ ⇒ 400.
2. `s15-payroll-be5-reports.int-spec.ts` — fixture 2 công ty; A: 3 NV × 2 đơn vị, 2 kỳ `Published`/`Paid` (dòng v2 có `statutory_employer`), 1 kỳ `Approved` có phiếu, 1 hồ sơ lương 2 phiên bản, 1 đợt chi trả, 1 ngân sách; B: kỳ phát hành số khác. Mỗi báo cáo: số khớp bảng tay (tính trong spec từ chính hàng đã gieo) · kỳ `Approved` KHÔNG vào số · B KHÔNG vào số A · `totals` = Σ trang khi 1 trang · audit +1 hàng đúng payload, **không khoá tiền** · 082 đọc lại XLSX (exceljs) khớp 081 + chuỗi `=cmd` được thoát.
3. `s15-payroll-be5-overview.int-spec.ts` — 6 khối khớp tay · công ty không có kỳ ⇒ 200 khối rỗng · 079 đếm #1/#2/#3 và **đẳng thức** với `pagination.total` của 036 cùng `insuranceIssue` · rate thiếu ⇒ `rateMissing` · 018 `payrollPeriodId` ⇒ `lineTotals`/`componentTotals` khớp Σ SQL, không tham số ⇒ không có khoá totals · kỳ lạ ⇒ 404.

**Unit:** registry (cặp nguồn pin ĐẲNG THỨC · required/optional · cột tiền) · kiểm tham số (400 + field) · trần 031 (repo giả `count = 50_001` ⇒ 422, `50_000` ⇒ qua) · `canResolve` (null/Department/Company/System) · XLSX builder (header · xlsxSafe · hàng tổng).

**Census/pin sửa CÙNG commit:** 2 tầng 77→82 (+ `PayrollReportsController`, `SERVICE_SITE_TO_KEYS`, ngưỡng literal) · `s13-payroll-qa1-scope-floor` 77→82 · mã lỗi +031 · `MONEY_FREE_ROUTES` đẳng thức · `route-http-coverage` 624→629 · FE wiring 77→82 · distinct 32→33 · FE kind `report-too-large` · route-census artifact (`ROUTE_CENSUS_WRITE=1`).

**Đột biến (mỗi cái đỏ đúng ca, revert):** (a) bỏ sàn ở `canResolve` · (b) bỏ assert cặp nguồn 081 · (c) bỏ `periodExport` ở 082 · (d) thêm `Approved` vào tập kỳ · (e) bỏ `company_id` ở join `payslip_items` (RLS đỡ ⇒ ghi nhận nếu KHÔNG đỏ) · (f) 079 #2 dùng vị từ khác 036 · (g) audit trước khi đọc xong (rollback) · (h) 018 totals cộng JS trên trang.

## 6. Cổng & bằng chứng

`bash harness/check.sh --lane-db=be5` · `vitest run test/foundation` (bài học DEBT-1) · `test:cov:payroll` ≥ 85 % · typescript-reviewer + security-reviewer HẸP · PR base master, không auto-merge; STATUS regen SAU merge.

## 7. Rủi ro

| #   | Rủi ro                                                                                                                                                                     | Chặn                                                                                                                                                                                                                                                                                                             |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | Số báo cáo lệch 073/018                                                                                                                                                    | D-1/D-11 dùng chung CTE/hàm; ca int so 073 ↔ `budget-status`                                                                                                                                                                                                                                                     |
| R2  | Role tuỳ biến có `view:payroll-report` đọc tiền theo người                                                                                                                 | O-2 cặp nguồn; Tổng quan chỉ trả số tổng/dải/đơn vị (không theo người)                                                                                                                                                                                                                                           |
| R2b | Suy ra lương qua NHÓM NHỎ: 3 báo cáo tổng hợp + khối 6 lọc/gom theo đơn vị 1 người ⇒ lộ gross/net/cơ cấu của người đó mà không cần `view-payslip` (security-review MEDIUM) | **Owner CHẤP NHẬN 17/09/2026** (AskUserQuestion): `view:payroll-report` sensitive + sàn Company, chỉ officer/admin giữ — hai vai đó vốn đọc được phiếu. Kích hoạt lại khi cấp `view:payroll-report` cho vai KHÔNG giữ `view-payslip` ⇒ lúc đó đòi thêm `payslipList` khi có `orgUnitId` hoặc ẩn đơn vị < k người |
| R3  | `jsonb_array_elements` trên 12 kỳ × 500 dòng chậm                                                                                                                          | đo thời gian ở int (log, không assert cứng); trần 24 tháng                                                                                                                                                                                                                                                       |
| R4  | Identity ratchet đỏ vì SQL thô                                                                                                                                             | tên qua `namesByUserIdsTx`; `employee_code` chỉ ở ORDER BY                                                                                                                                                                                                                                                       |
| R5  | 018 đổi hình phá widget DASH                                                                                                                                               | totals chỉ khi có `payrollPeriodId`; ca widget hiện có giữ xanh                                                                                                                                                                                                                                                  |

## 8. Bằng chứng (17/09/2026, lane `mediaos_be5`, head `0575`)

| Cổng                  | Kết quả                                                                                                                                                                                                          |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RED trước code        | 3 int-spec mới chạy ĐỎ đúng lý do (404 route chưa có · 018 bỏ qua tham số); D-18 đo ĐỎ trên bản `payroll-employees.ts` HEAD (2 ca contracts) rồi khôi phục                                                       |
| Int-spec BE-5         | `s15-payroll-be5-{report-gates,reports,overview}` **40/40**                                                                                                                                                      |
| Unit + foundation     | `src/payroll` + `test/foundation` **62 file · 808 test** (gồm census 2 tầng 82 · `payroll-report-source-pairs` mới · mã lỗi 33 · route-http-coverage 629 · route-census artifact regen `ROUTE_CENSUS_WRITE=1`)   |
| `test:cov:payroll`    | **44 file · 997 test** xanh (lượt 1 sập `ERR_IPC_CHANNEL_CLOSED` — bug tinypool đã biết — sau khi bắt được pin 32→33 ở `s13-payroll-qa1-scope-floor`); coverage `src/payroll` **94,78 % stmts · 87,11 % branch** |
| FE                    | `payroll-wiring.spec` + `payroll-error-kind-census.spec` **37/37**; contracts `payroll-reports.spec` **8/8**                                                                                                     |
| Typecheck             | contracts build + `tsc --noEmit` API sạch                                                                                                                                                                        |
| security-reviewer HẸP | **PASS** (0 C/H). MEDIUM nhóm nhỏ ⇒ R2b owner chấp nhận. LOW ×2 đã vá: join `payroll_periods` của `payment-summary` thêm `deleted_at` · `xlsxSafe` thoát thêm TAB/CR đầu chuỗi                                   |
| typescript-reviewer   | **PASS** (0 C/H; 3 LOW không chặn: `Promise.all` trên một tx là tuần tự hoá · hai `await` tuần tự ở 018 · `sortOrder::int` không fallback — snapshot do engine ghi)                                              |

**Lệch plan đã ghi:** 082 assert `reportExport` + `periodExport` + cặp nguồn (bỏ `reportData` trùng cặp với `reportExport`) · `budget-status` cắt trang bằng mảng (số hàng chặn bởi unique năm×đơn vị) · `hasSalaryProfile` vá ở contracts (D-18, không có trong plan gốc — bug FE-1 đang chạy).

**Nợ để lại:** FE-4 dùng `lineTotals`/`componentTotals` của 018 + `insuranceIssue` của 036 (đã ghi done_when FE-4) · `namesByUserIdsTx` bỏ người đã xoá mềm ⇒ báo cáo lịch sử hiện mã/tên rỗng cho họ (C6) · PDF 083–085 = `S15-PAYROLL-BE-5B`.
