```yaml
wo: S10-FND-VALKEYSCOPE-1
zone: red
generated_by: auto-loop
reconciled_at: "42062607"
lanes: [{"id":"L1-KEYCORE","task":"CROWN — Dựng MỘT chỗ dựng khoá Valkey duy nhất: file mới apps/api/src/common/valkey/valkey-key.ts IMPORT `resolveEnvScope` từ realtime/ws-adapter-config.ts:69-75 (KHÔNG copy, KHÔNG viết hàm thứ hai). API: (a) builder THUẦN nhận `envScope` tường minh để test tiêm được, (b) `currentEnvScope()` memo LAZY = resolveEnvScope(loadEnv(), process.env.LANE_DB) — lazy vì gọi loadEnv() lúc import sẽ giết fixture int-spec, (c) họ hàm theo namespace: rlKey(bucket,...parts) · permCapKey · permObjKey · idemKey · replayKey · chatKey · meKey. Định dạng chuẩn = `{namespace}:{envScope}:{phần còn lại}` (giống chat:presence đã đúng, để `--scan --pattern` của vận hành đọc được envScope BẰNG MẮT — CẤM nhét envScope vào bên trong hash sha256 của idem). BỔ SUNG cổng runtime ở ValkeyService: mọi key vào get/set/setNx/incr/del/sAddWithTtl/sRemCount/sCard phải chứa currentEnvScope(); kiểm TRƯỚC nhánh `if (!this.client) return` (để test không có Valkey vẫn là một lượt census thật); NODE_ENV!=='production' → NÉM (fail-loud), production → warn ĐÚNG MỘT LẦN/namespace rồi vẫn chạy (không tự cắt tính năng ở PROD).","builder":"backend-builder","paths":["apps/api/src/common/valkey/valkey-key.ts","apps/api/src/common/valkey/valkey-key.spec.ts","apps/api/src/common/valkey/valkey-key-census.spec.ts","apps/api/src/permission/valkey.service.ts","apps/api/src/permission/valkey.service.spec.ts","apps/api/src/realtime/ws-adapter-config.ts","apps/api/src/realtime/ws-adapter-config.spec.ts"]},{"id":"L2-AUTHRL","task":"CROWN — Chuyển TOÀN BỘ khoá họ rate-limit + replay sang builder của L1. Đo được 8 điểm: login-rate-limiter.ts:41/46/56/60 (rl:ip · rl:acct · rl:forgot:ip · rl:forgot:acct — hậu tố :cnt/:lock GIỮ NGUYÊN vì suy từ key đã scoped); auth.service.ts:469 `2fa|{co}|{uid}`, :590 `2fa-disable|{co}|{uid}`, :625 `change-pw|{co}|{uid}`; two-factor.service.ts:183 `2fa-enable|{co}|{uid}`; replay-guard.service.ts:64 `replay:{key}` (một chỗ phủ cả hai caller `2fa-jti:` auth.service.ts:465 và `totp-step:` two-factor.service.ts:273-274). ⚠️ Bốn khoá dạng ống (`2fa|`,`2fa-disable|`,`2fa-enable|`,`change-pw|`) hiện KHÔNG có tiền tố `rl:` — chúng nằm ở namespace GỐC của Valkey; đưa vào họ `rl:` qua rlKey(bucket,...) và ghi rõ việc đổi hình dạng trong docblock. GIỮ NGUYÊN ngữ nghĩa fail-soft/fail-closed hiện có (KHÔNG đụng ngưỡng, KHÔNG đụng luồng 429/401 đồng nhất).","builder":"backend-builder","paths":["apps/api/src/auth/login-rate-limiter.ts","apps/api/src/auth/login-rate-limiter.spec.ts","apps/api/src/auth/auth.service.ts","apps/api/src/auth/two-factor.service.ts","apps/api/src/auth/replay-guard.service.ts","apps/api/src/auth/replay-guard.service.spec.ts","apps/api/src/auth/forgot-password-rate-limit.spec.ts","apps/api/test/integration/auth-toprisk-http.int-spec.ts"]},{"id":"L3-PERMCACHE","task":"CROWN (nhạy nhất — 253/288 khoá đang sống là perm:cap) — permission.cache.ts:36 capKey + :45 objKey đi qua builder L1. BẮT BUỘC: đường INVALIDATE (permission.cache.ts:164-171 `invalidateUser` → `del(this.capKey(...))`) dùng ĐÚNG hàm dựng khoá của đường GHI (getCompanyRoleGrants:49) — cấm dựng lại chuỗi tại chỗ ở nhánh xoá. Viết MỚI spec colocated permission.cache.spec.ts (hiện KHÔNG tồn tại) với fake ValkeyService dạng Map: ca ghi-rồi-invalidate chứng minh DEL trúng ĐÚNG khoá vừa SET (lệch một chữ = grant cũ sống tới hết TTL 300s = leo thang quyền im lặng), + ca hai envScope khác nhau không đọc/không xoá trúng khoá của nhau.","builder":"backend-builder","paths":["apps/api/src/permission/permission.cache.ts","apps/api/src/permission/permission.cache.spec.ts"]},{"id":"L4-IDEM","task":"idempotency.interceptor.ts:129-133 `scopeKey` → `idem:{envScope}:{sha256(companyId^@userId^@method^@path^@key)}` qua builder L1. envScope nằm NGOÀI hash (bên trong hash thì vận hành không SCAN ra được môi trường nào đang giữ khoá, và một lần đổi công thức hash sẽ câm lặng). Giữ nguyên vòng đời first/in-flight/replay/key-reused + BẤT BIẾN #1 (companyId+userId vẫn trong material hash). idempotency-store.service.ts nhận key đã dựng sẵn ⇒ KHÔNG đổi chữ ký, chỉ bổ sung docblock nói rõ 'key đến đây PHẢI đã scoped'.","builder":"backend-builder","paths":["apps/api/src/common/idempotency/idempotency.interceptor.ts","apps/api/src/common/idempotency/idempotency.interceptor.spec.ts","apps/api/src/common/idempotency/idempotency-store.service.ts","apps/api/src/common/idempotency/idempotency-store.service.spec.ts"]},{"id":"L5-RESIDUAL","task":"Bốn không gian khoá CÒN SÓT mà WO không liệt kê nhưng cổng grep của done_when #4 sẽ bắt: chat-typing.service.ts:82 `chat:typing:co:…` · chat-call-cooldown.service.ts:64 static key() `chat:cooldown:{scope}:co:…` · chat-call-ice.service.ts:217 rejectionKey() `chat:ice-turn-reject:co:{companyId}` · me/me.constants.ts:151 meTrainingCacheKey() `me:training:{co}:{uid}`. Mỗi chỗ một dòng, đi qua builder L1; cập nhật literal đang bị đóng đinh ở chat-typing.service.spec.ts:103 và chat-call-ice.service.spec.ts:407. KHÔNG đụng logic tiết lưu/cooldown/fallback in-memory.","builder":"backend-builder","paths":["apps/api/src/chat/chat-typing.service.ts","apps/api/src/chat/chat-typing.service.spec.ts","apps/api/src/chat/chat-call-cooldown.service.ts","apps/api/src/chat/chat-call-ice.service.ts","apps/api/src/chat/chat-call-ice.service.spec.ts","apps/api/src/me/me.constants.ts"]},{"id":"L6-DOCS-OPS","task":"KI-067 vào RELEASE-02 (MỞ + ĐÓNG cùng PR, theo đúng khuôn KI-066 gạch ngang ở dòng 17) — nêu: db0 duy nhất dùng chung 4 môi trường, 16 không gian khoá không scoped, mức độ 253/288 khoá là cache QUYẾT ĐỊNH QUYỀN, bằng chứng khoá đăng nhập chéo môi trường. Kèm PHẦN VẬN HÀNH viết sẵn lệnh dọn tay (không để người deploy tự nghĩ): DEL theo pattern NEO-SLUG chỉ khớp khoá CŨ (`rl:ip:funtime|*`, `rl:acct:funtime|*`, `rl:forgot:ip:funtime|*`, `rl:forgot:acct:funtime|*`, `2fa|*`, `2fa-enable|*`, `2fa-disable|*`, `change-pw|*`, `replay:2fa-jti:*`) + CẤM `FLUSHDB` (giết luôn presence + kênh socket.io adapter) + ghi rõ `perm:*`(300s)/`idem:*`(900s)/`me:training:*`(60s) TỰ LÀNH nên KHÔNG dọn (pattern glob không phân biệt được cũ/mới) + câu lệnh NGHIỆM THU sau dọn. Cập nhật harness/backlog.mjs: mở rộng `paths` của WO (thêm common/valkey/**, permission.cache.spec.ts, chat/**, me/**, các spec colocated) và đóng status theo DoD §8.","builder":"backend-builder","paths":["docs/RELEASE/RELEASE-02_Known_Issues_MVP.md","harness/backlog.mjs"]}]
acceptanceChecks: ["ĐÚNG MỘT định nghĩa envScope trong apps/api/src: `grep -rn 'function resolveEnvScope' apps/api/src` trả về CHÍNH XÁC 1 hit (realtime/ws-adapter-config.ts:69). File mới common/valkey/valkey-key.ts IMPORT nó — không có bản sao thứ hai của phép suy {NODE_ENV}:{db}.","16/16 không gian khoá đã đo đi qua builder chung: rl:ip · rl:acct · rl:forgot:ip · rl:forgot:acct · 2fa| · 2fa-enable| · 2fa-disable| · change-pw| · replay:* · perm:cap · perm:obj · idem:* · chat:typing · chat:cooldown · chat:ice-turn-reject · me:training. (chat:presence + kênh socket.io đã đúng sẵn, giữ nguyên.)","Census spec XANH: không còn literal khoá Valkey dựng tại chỗ ngoài allowlist; allowlist loại đúng 4 false-positive đã đo (dedupeKey NOTI, storage key, file-policy Map key, module setting key). Cố ý thêm lại một literal ⇒ spec ĐỎ (đo cổng bằng VI PHẠM thật, không tin PASS rỗng).","Cổng runtime ValkeyService: một key thiếu envScope truyền vào get/set/setNx/incr/del(nhiều khoá)/sAddWithTtl/sRemCount/sCard ⇒ NÉM khi NODE_ENV!=='production', WARN-một-lần khi production; ca test chứng minh gate chạy CẢ KHI VALKEY_URL vắng (kiểm trước nhánh client null).","Pairwise distinctness: với 4 bộ env thật (production:mediaos · development:mediaos_dev · development:mediaos · test:mediaos_<lane>), MỌI builder cho ra chuỗi khác nhau từng đôi một — `new Set(...).size === n`, khuôn mở rộng từ ws-adapter-config.spec.ts:65-87.","🔴 permission.cache.spec.ts (MỚI) có ca ghi-rồi-invalidate: chuỗi khoá DEL của invalidateUser === chuỗi khoá SET của getCompanyRoleGrants, so sánh chuỗi THẬT (không mock capKey). Đổi capKey mà quên invalidateUser ⇒ spec ĐỎ.","Idempotency: envScope nằm NGOÀI sha256 (`idem:{envScope}:{hash}`) — SCAN theo tiền tố đọc được môi trường bằng mắt; material hash vẫn chứa companyId+userId (BẤT BIẾN #1 không suy giảm).","Hành vi cũ KHÔNG đổi: ngưỡng LOGIN_MAX_ATTEMPTS=5 / LOGIN_ACCOUNT_MAX_ATTEMPTS=20, 4 ca cuối login-rate-limiter.spec.ts (KI-066) vẫn xanh; 401/429 đồng nhất chống enumeration vẫn nguyên; fail-closed của ReplayGuard/fail-soft của cache không đảo chiều.","docblock DEPLOY ở valkey-key.ts nói rõ: đổi tiền tố ⇒ mọi khoá cũ MỒ CÔI; perm:*(300s)/idem:*(900s)/replay:*(600s)/me:training:*(60s) tự lành; MỌI lockout rl:* đang có bị VÔ HIỆU ngay lúc deploy (người đang bị khoá được cấp lại budget — nêu tường minh, đây là nới lỏng an ninh trong một cửa sổ ngắn).","RELEASE-02 có KI-067 mở+đóng cùng PR (khuôn KI-066) kèm lệnh DEL neo-slug chỉ khớp khoá CŨ, cảnh báo CẤM FLUSHDB, và lệnh nghiệm thu sau dọn (`--scan --pattern 'rl:*'` không còn dòng nào THIẾU chuỗi `production:mediaos`).","KHÔNG có migration mới (head migration giữ nguyên) và KHÔNG có thay đổi FE — WO thuần BE.","`pnpm --filter @mediaos/api typecheck` xanh; `bash harness/check.sh --all` với LANE_DB=mediaos_valkeyscope XANH THẬT (không phải 'XANH KHÔNG ĐỦ BẰNG CHỨNG'); TURBO_FORCE=1 khi chạy test.","harness/backlog.mjs: `paths` của WO đã mở rộng khớp lane map, status đóng theo DoD §8."]
testTasks: ["RED trước tiên (crown) — apps/api/src/common/valkey/valkey-key.spec.ts: mở rộng ĐÚNG khuôn ws-adapter-config.spec.ts:65-87; 4 env thật × MỌI builder ⇒ đôi một khác nhau. Phải viết và thấy ĐỎ trước khi có valkey-key.ts.","apps/api/src/permission/permission.cache.spec.ts (MỚI, colocated theo luật vitest của repo): fake ValkeyService dạng Map ghi lại chuỗi khoá thật. Ca 1 ghi-rồi-invalidate xoá TRÚNG. Ca 2 hai envScope không thấy nhau (đọc miss, DEL không chạm khoá env kia). Ca 3 objKey cũng scoped.","apps/api/src/permission/valkey.service.spec.ts: cổng census runtime — key thiếu envScope ném ở test env; production chỉ warn một lần/namespace; phủ del(k1,k2), setNx, sAddWithTtl, sRemCount, sCard, incr; và ca VALKEY_URL vắng vẫn ném (gate đứng trước nhánh client null).","apps/api/src/auth/login-rate-limiter.spec.ts — deny-path RED: khoá đạt ngưỡng ở scope A KHÔNG khoá cùng (slug,email,ip) ở scope B; giữ nguyên 4 ca KI-066 (per-IP 5 / per-account 20) đang xanh.","apps/api/src/auth/replay-guard.service.spec.ts: cập nhật assert dòng 55 sang dạng có envScope; thêm ca cùng jti ở hai scope ⇒ CẢ HAI claim=true (không chặn chéo môi trường) và replay trong CÙNG scope vẫn false (fail-closed không suy giảm).","apps/api/src/common/idempotency/idempotency.interceptor.spec.ts: cùng Idempotency-Key + cùng actor + cùng route ở HAI scope ⇒ không phát lại chéo; giữ nguyên 409 IN_PROGRESS / KEY_REUSED / nhả khoá khi handler ném.","apps/api/src/common/valkey/valkey-key-census.spec.ts: quét nguồn tìm literal namespace ngoài allowlist ⇒ ĐỎ. Nghiệm thu cổng bằng cách CỐ Ý chèn một literal rồi xác nhận đỏ (chống PASS rỗng).","Cập nhật literal đang bị đóng đinh: chat-typing.service.spec.ts:103, chat-call-ice.service.spec.ts:407 — assert khoá CHỨA envScope thay vì chuỗi cũ.","Integration DB CÔ LẬP (vùng đỏ, bắt buộc trước PR): `bash scripts/lane-db-setup.sh valkeyscope` → `export LANE_DB=mediaos_valkeyscope` → `bash harness/check.sh --all`. Đặc biệt phải chạy THẬT: auth.int-spec, two-factor-login.int-spec, two-factor.int-spec, auth-reset-deny-path.int-spec, auth-toprisk-http.int-spec, account-self-service.int-spec, security-policy-2fa.int-spec (nhóm này đi qua rate-limiter/replay-guard/idempotency).","Coverage ≥80% cho file mới common/valkey/** (module nhạy cảm) — đếm NHÁNH: mỗi ca DENY (thiếu scope) phải có ca ALLOW đối ứng, tránh deny xanh rỗng."]
steps: ["0. TRƯỚC KHI CODE: mở rộng `paths` của WO trong harness/backlog.mjs cho khớp bản đồ lane (paths lái guard-scope, review gate VÀ scheduler — thiếu là lọt gate, memory wo-paths-drive-gate-and-scheduler). Xác nhận KHÔNG có migration nào trong WO này ⇒ head migration giữ nguyên, không có lane db-migration.","1. L1 — viết RED trước: valkey-key.spec.ts mở rộng ĐÚNG khuôn ws-adapter-config.spec.ts:65-87 (4 env thật: production:mediaos · development:mediaos_dev · development:mediaos(local) · test:mediaos_<lane>), assert MỌI builder cho chuỗi đôi một khác nhau (`new Set(keys).size === keys.length`).","2. L1 — dựng apps/api/src/common/valkey/valkey-key.ts: import resolveEnvScope (KHÔNG copy), builder thuần nhận envScope + currentEnvScope() memo LAZY. Chạy spec → GREEN.","3. L1 — cổng runtime ở ValkeyService: kiểm key chứa currentEnvScope() TRƯỚC nhánh `if (!this.client)`; test env → ném, production → warn 1 lần/namespace. Spec phải phủ CẢ `del(k1,k2)` nhiều khoá, `setNx`, và hai pipeline `sAddWithTtl`/`sRemCount` (đúng cảnh báo trong notes của WO về keyPrefix).","4. L1 — census spec: quét apps/api/src/**/*.ts tìm literal template chứa `rl:`/`perm:cap`/`perm:obj`/`idem:`/`replay:`/`chat:`/`me:training:` ngoài allowlist file đã khai → FAIL. Allowlist phải loại đúng các FALSE POSITIVE đã đo: dedupeKey NOTI (chat-noti-bridge.registrar.ts:190, khoá DB), storage key (buildFileKey/buildAttachmentKey), file-policy.service.ts buildKey (Map in-process), moduleEnabledKey (setting DB).","5. L2/L3/L4/L5 chạy SONG SONG sau khi L1 xanh (khác file hoàn toàn). Mỗi lane: sửa literal → gọi builder → cập nhật/viết spec đóng đinh.","6. L3 — spec permission.cache.spec.ts MỚI: ghi (getCompanyRoleGrants miss → SET) rồi invalidateUser → DEL phải trúng ĐÚNG chuỗi vừa SET; thêm ca khác envScope không xoá nhầm.","7. L2 — cập nhật replay-guard.service.spec.ts:55 (`replay:2fa-jti:xyz` → dạng có envScope) + thêm ca: cùng jti ở HAI scope đều claim được true (chứng minh không chặn chéo môi trường).","8. Verify cục bộ: `pnpm --filter @mediaos/api typecheck` + `pnpm --filter @mediaos/api test` với TURBO_FORCE=1 (chống xanh-giả từ cache turbo).","9. Verify như CI (BẮT BUỘC vì vùng đỏ): `bash scripts/lane-db-setup.sh valkeyscope` → `export LANE_DB=mediaos_valkeyscope` → `bash harness/check.sh --all`. Không được để int-spec skip vượt ngưỡng.","10. L6 — RELEASE-02 KI-067 (mở+đóng cùng PR) + phần vận hành DEL/nghiệm thu; docblock deploy ở valkey-key.ts.","11. Đóng backlog.mjs theo DoD §8 (status + ghi chú số đo), regen STATUS sau khi land.","12. Gate FULL trước merge: security-reviewer + silent-failure-hunter + santa-method (crown). Người chốt vùng đỏ duyệt trước merge — đây là cache QUYẾT ĐỊNH QUYỀN + khoá đăng nhập."]
```

GAP-ANALYSIS (đo lại 18/08/2026 trên cây master sạch — WO ĐẾM THIẾU)

WO liệt kê 9 không gian khoá thiếu envScope. Đo thật bằng census `ValkeyService` (19 file import) ra 16, cộng 2 chỗ đã đúng:

| # | Khoá | Dựng tại | Trạng thái |
|---|------|----------|-----------|
| 1-4 | rl:ip · rl:acct · rl:forgot:ip · rl:forgot:acct | login-rate-limiter.ts:41,46,56,60 (static) | THIẾU |
| 5 | `2fa\|{co}\|{uid}` | auth.service.ts:469 | THIẾU — **WO không liệt kê** |
| 6 | `2fa-disable\|{co}\|{uid}` | auth.service.ts:590 | THIẾU |
| 7 | `change-pw\|{co}\|{uid}` | auth.service.ts:625 | THIẾU — **WO không liệt kê** |
| 8 | `2fa-enable\|{co}\|{uid}` | two-factor.service.ts:183 | THIẾU — **WO không liệt kê** |
| 9 | `replay:{key}` (phủ `2fa-jti:` auth.service.ts:465 + `totp-step:` two-factor.service.ts:273) | replay-guard.service.ts:64 | THIẾU |
| 10-11 | perm:cap · perm:obj | permission.cache.ts:36,45 | THIẾU (253+18/288 khoá đang sống) |
| 12 | `idem:{sha256}` | idempotency.interceptor.ts:129-133 (`scopeKey`) | THIẾU |
| 13 | `chat:typing:co:…` | chat-typing.service.ts:82 | THIẾU — **WO không liệt kê** |
| 14 | `chat:cooldown:{scope}:co:…` | chat-call-cooldown.service.ts:64 (static) | THIẾU — **WO không liệt kê** |
| 15 | `chat:ice-turn-reject:co:{co}` | chat-call-ice.service.ts:217 | THIẾU — **WO không liệt kê** |
| 16 | `me:training:{co}:{uid}` | me/me.constants.ts:151 | THIẾU — **WO không liệt kê** |
| ✓ | `chat:presence:{envScope}:co:…:user:…` | chat-presence-reader.service.ts:43 | ĐÃ ĐÚNG |
| ✓ | `socket.io:{envScope}` | ws-adapter-config.ts:83-88 | ĐÃ ĐÚNG |

Hệ quả với PATHS: WO gốc thiếu `apps/api/src/chat/**`, `apps/api/src/me/**`, `common/valkey/**`, các spec colocated (`permission.cache.spec.ts` HIỆN KHÔNG TỒN TẠI, `ws-adapter-config.spec.ts`, `replay-guard.service.spec.ts`, `valkey.service.spec.ts`). Vì `harness/backlog.mjs` nằm trong paths nên bước 0 hợp lệ là mở rộng paths. Nếu owner muốn ĐÓNG HẸP theo WO gốc thì phải ghi tường minh miễn trừ cho 5 khoá chat/me và thu hẹp cổng grep — nhưng như thế bất biến "một cơ chế cô lập duy nhất" chỉ đóng một nửa và sẽ trôi (lớp module-closed-by-second-assert-not-scope).

QUYẾT ĐỊNH THIẾT KẾ (chốt trước khi code)
1. MỘT builder, KHÔNG hai phép suy: `common/valkey/valkey-key.ts` IMPORT `resolveEnvScope` (ws-adapter-config.ts:69-75). File đó là TS thuần, không Nest DI ⇒ import từ common/ sang realtime/ không tạo vòng module (khác bẫy Chat↔Realtime). CẤM move/copy sang chỗ khác trong PR này (move = churn + rủi ro hai bản trong lịch sử).
2. `currentEnvScope()` phải LAZY + memo. Gọi `loadEnv()` ở top-level lúc import sẽ nổ ở fixture int-spec (lớp env-schema-floor-breaks-test-fixtures) và lỗi sẽ hiện ở file KHÁC hoàn toàn. Builder luôn nhận `envScope` như tham số có mặc định ⇒ test tiêm 4 env mà không cần reset cache.
3. Hình dạng khoá `{namespace}:{envScope}:{rest}` — theo đúng `chat:presence` đã đúng. Với `idem:`, envScope nằm NGOÀI hash: nhét vào material sha256 vẫn cô lập được nhưng vận hành mất khả năng `--scan --pattern` để biết môi trường nào đang giữ khoá.
4. Bốn khoá dạng ống (`2fa|`, `2fa-enable|`, `2fa-disable|`, `change-pw|`) hiện nằm ở NAMESPACE GỐC của Valkey (không cả tiền tố `rl:`) dù chúng là bucket của `LoginRateLimiter` — gom vào họ `rl:` luôn. Đổi hình dạng ở đây MIỄN PHÍ vì toàn bộ khoá cũ dù sao cũng thành mồ côi lúc deploy.
5. TỪ CHỐI `keyPrefix` của ioredis (đúng cảnh báo trong notes WO): áp toàn cục ⇒ presence + kênh socket.io bị scope HAI LẦN, và pipeline/`del(...keys)` phải chứng minh riêng. Hai cơ chế cô lập song song = không ai biết cái nào đang thật sự cưỡng chế.
6. TỪ CHỐI tách theo số db Valkey (`redis://…/1`): cùng lý do — hai cơ chế song song.
7. THÊM (ngoài chữ của WO nhưng là thứ ép done_when #4 không trôi): cổng RUNTIME ở `ValkeyService` — key không chứa envScope thì NÉM ở dev/test, WARN-một-lần ở production. Chỉ grep là cổng CHẾT (lớp identity-projection-census-misses-alias: literal ẩn trong template lồng nhau, alias vô hình với grep); cổng runtime bắt được cả đường mà grep không thấy. Guard phải đứng TRƯỚC `if (!this.client) return` để lượt test không có Valkey vẫn là một lượt census.

BẤT BIẾN PHẢI GIỮ (CLAUDE.md §2)
- #1 company_id: `perm:*`, `idem:*`, `me:training:*` vẫn phải chứa companyId+userId sau khi thêm envScope — envScope là THÊM, không THAY.
- #3 không secret/PII trong khoá: cấm nhét email vào khoá mới (me.constants.ts:146-148 đã cảnh báo; khoá `rl:*` vốn có email — GIỮ NGUYÊN hiện trạng, đừng mở rộng, và đừng log key ở mức info).
- Fail-closed/fail-soft không được đảo chiều: ReplayGuard + LoginRateLimiter fail-soft xuống memory (KHÔNG fail-open); permission cache fail-soft xuống DB; idempotency fail-open có chủ đích. Cổng census mới KHÔNG được biến các đường này thành 500 ở production (vì thế production chỉ warn).

RỦI RO & ĐIỂM CẦN NGƯỜI CHỐT
- R1 (cao): `perm:cap` là cache QUYẾT ĐỊNH QUYỀN. Nếu `capKey` đổi mà `invalidateUser` không đổi ĐỒNG THỜI thì DEL trượt ⇒ grant cũ sống tới hết 300s ⇒ leo thang quyền IM LẶNG (không log, không exception). Đây là lý do L3 bắt buộc có spec ghi-rồi-invalidate so chuỗi THẬT.
- R2 (trung bình, chấp nhận + ghi rõ): lúc deploy, MỌI lockout `rl:*:lock` đang có bị vô hiệu — một cuộc brute-force đang bị chặn sẽ được cấp lại budget đầy. Cửa sổ ngắn, đổi lại là đóng hẳn đường khoá chéo môi trường. Phải nêu tường minh trong KI-067, KHÔNG giấu.
- R3: khoá cũ mồ côi. TẤT CẢ đều có TTL (perm 300s · idem 900s · replay 600s · rl lock = LOGIN_LOCKOUT_SEC · me:training 60s · presence có TTL) ⇒ tự lành. Lệnh DEL là vệ sinh, KHÔNG phải điều kiện đúng-sai. Pattern dọn phải NEO SLUG (`rl:ip:funtime|*`) vì `rl:ip:*` khớp CẢ khoá mới. CẤM `FLUSHDB` (giết presence + trạng thái adapter socket.io đang chạy).
- R4: `LoginRateLimiter.key/accountKey/forgotKey` là STATIC và được gọi từ auth.service (6 chỗ) — đổi chữ ký sẽ lan; giữ chữ ký static, chỉ đổi phần thân gọi builder.
- R5: `apps/api/test/**` chỉ có MỘT chỗ nhắc literal và là COMMENT (auth-toprisk-http.int-spec.ts:24) — sửa comment cho khỏi lạc hướng người đọc sau này.

VERIFY (như CI, vùng đỏ)
`bash scripts/lane-db-setup.sh valkeyscope` → `export LANE_DB=mediaos_valkeyscope` → `bash harness/check.sh --all` (phải là XANH THẬT, không phải "XANH KHÔNG ĐỦ BẰNG CHỨNG"); chạy test với `TURBO_FORCE=1`. VALKEY_URL thường VẮNG trong test ⇒ bằng chứng hành vi nằm ở unit spec với fake store dạng Map, không phải ở Valkey thật; cổng runtime trong ValkeyService là thứ khiến mọi int-spec đi qua Valkey trở thành một lượt census.

GATE
FULL (auth + permission + secret-adjacent): security-reviewer + silent-failure-hunter + typescript-reviewer + quality-gate, cộng santa-method cho L1/L2/L3 (crown). Không có migration ⇒ không cần database-reviewer bắt buộc, nhưng L3 nên nhờ đọc chéo vì nó là tầng cache trước RLS. Người chốt vùng đỏ duyệt trước merge.

OUT-OF-SCOPE (KHÔNG làm trong WO này)
- Không thêm/đổi migration; không đụng `docs/DB`, ERD.
- Không đổi ngưỡng rate-limit, không đụng TRUST_PROXY (đã đóng ở S10-AUTH-IPTRUST-1).
- Không tách Valkey theo db index / instance riêng cho từng môi trường (hạ tầng — nếu owner muốn, seed WO DEVOPS riêng; envScope vẫn cần vì test-only .env cũng dùng chung).
- Không đụng FE.
- Đã kiểm: apps/api KHÔNG dùng BullMQ (0 hit `bullmq`/`new Queue`) ⇒ không có `bull:*` cần scope ở PR này; nếu sau này bật BullMQ thì tiền tố queue phải đi qua ĐÚNG builder này (ghi vào docblock để lần sau không đẻ cơ chế thứ hai).
- RELEASE-02 là file xung đột đa-PR: đảm bảo không có PR khác đang mở ghi KI cùng lúc (notes WO đã dặn).
