```yaml
wo: S10-ATT-NOTIPROD-1
zone: yellow
generated_by: auto-loop
reconciled_at: "71ba8803"
lanes: [{"id":"L1-att-noti-logic","task":"Logic THUẦN của detector (không DB, không DI, không số ma). (a) closedDayBoundary(now, shift, tz) — biên 'ngày ĐÃ ĐÓNG' tính THEO CA CỦA CHÍNH NHÂN VIÊN (chốt #3): shift.end_time + grace, tz qua shiftTimezone(shift) (attendance.mappers.ts:48), ca đêm cross_day (end_time <= start_time) ⇒ biên rơi sang hôm sau; trả workDate 'YYYY-MM-DD'. (b) buildDedupeKey — missing-checkout/late = `${recordId}:${record.workDate}` (workDate là CỘT DB = nguồn ĐÓNG BĂNG), absent = `${employeeId}:${closedWorkDate}` (suy từ biên đóng, TUYỆT ĐỐI KHÔNG từ new Date()/Date.now() tại điểm phát). (c) buildPayload khớp ĐÚNG placeholder template mig 0481:153-163 — work_date (cả 3 mã) + employee_name (MISSING_CHECKOUT, ABSENT). (d) CHỐT #11: BỎ HẲN `late_notification_enabled` — khoá này KHÔNG tồn tại trong repo (att-master-data.seeder.ts:116-117 chỉ có missing_checkout_policy + block_when_leave_approved); v1 LUÔN BẬT theo SPEC-04, KHÔNG đẻ cấu hình ma. Chỉ đọc rule_config.missing_checkout_policy nếu thật sự có. (e) CHỐT #8: hằng DEFAULT_ATT_ALERT_DETECT_INTERVAL_MS + resolveAttAlertDetectIntervalMs(raw = process.env.ATT_ALERT_DETECT_INTERVAL_MS) — parse-an-toàn + clamp biên, rác/NaN/ngoài biên ⇒ mặc định, KHÔNG ném; MIRROR NGUYÊN KHUÔN dashboard/dashboard-mv-refresh.job-handler.ts:14-42. CẤM đụng config/env.schema.ts (ngoài paths + bài học env-schema-floor-breaks-test-fixtures) — ghi nợ khai env ở WO sau. (f) const 3 eventCode + jobCode ATT_ALERT_DETECT + trần batch/lookback ĐẶT TÊN.","builder":"backend-builder","paths":["apps/api/src/attendance/attendance-alert-noti.logic.ts","apps/api/src/attendance/attendance-alert-noti.logic.spec.ts"]},{"id":"L2-att-noti-repo","task":"Repository CHỈ-ĐỌC chạy trong TenantTx do handler mở, mọi truy vấn có eq(companyId) tường minh + LIMIT trần. (1) missingCheckout: attendance_records check_in_at NOT NULL, check_out_at IS NULL, deleted_at IS NULL, work_date = ngày đã đóng. (2) late: is_late = true HOẶC late_minutes > 0 (ngưỡng grace ĐÃ áp lúc check-in — attendance.logic.ts:111-115, KHÔNG tính lại); CHỐT #12 LOẠI bản ghi đã điều chỉnh/khoá sổ: is_adjusted IS TRUE · locked_at IS NOT NULL · status='approved_adjustment' · attendance_status='Adjusted' (db/schema/hr.ts:135,139,159,183). (3) absent: anti-join CHỐT #4 khoá theo `user_id` (khớp unique attendance_records_company_user_date_uq trên company_id+user_id+work_date WHERE deleted_at IS NULL — db/schema/hr.ts:93-96,114), KHÔNG theo employee_id (cột thêm sau ở mig 0452, NULLABLE ⇒ người đã chấm công bị coi là vắng); employee_id chỉ dùng lấy tên. CHỐT #13: record đã soft-delete tính là 'ĐÃ CÓ DỮ LIỆU' ⇒ fail-closed BỎ QUA (không bắn vắng mặt). Loại trừ: leave_requests + remote_work_requests theo CHỐT #5 — inArray(status, ['approved','Approved']) mirror attendance.repository.ts:485 và luật nửa ngày mirror :488-491 (chỉ FullDay/MultipleDays/durationType NULL); public_holidays (company ∪ global); shifts.work_days không chứa thứ đó; employee_profiles active + user_id NOT NULL + chưa xoá; CHỐT #6 cửa sổ hợp đồng (start_date IS NULL OR start_date <= closedWorkDate) AND (end_date IS NULL OR end_date >= closedWorkDate) (db/schema/employees.ts:56-57). CHỐT #3: ca/quy tắc phân giải THEO TỪNG NHÂN VIÊN bằng hàm CÓ SẴN attendance.repository.ts:310-356 resolveEffectiveShiftTx (+ findDefaultShiftTx fallback) và resolveRule như attendance.service.ts:384-403 — KHÔNG một-ca-cho-cả-lượt, KHÔNG viết nguồn sự thật thứ hai (attendance-shift.service.ts:48 gọi đó là 'ONE implementation'). GHI ĐÈ ràng buộc 'set-based, cấm N+1' của plan cũ: tập ứng viên lấy set-based, phân giải ca/rule per-employee được PHÉP N truy vấn nhưng BẮT BUỘC chặn bằng hằng trần batch/lần chạy. CHỐT #7 (chống hồi sinh): thêm 1 truy vấn set-based lấy các dedupe_key ĐÃ TỒN TẠI cho tập khoá ứng viên, KHÔNG lọc deleted_at (uq_notifications_dedupe_active … deleted_at IS NULL — mig 0479:279-281 + notification-dedupe.service.ts:102-117 chỉ chặn khi notification CÒN SỐNG, mà người dùng có nút xoá my-notifications.controller.ts:131) — lấy TRONG CÙNG tx materialize để handler lọc trước khi intake. Trả kèm userId + employeeName + workDate + recordId + dedupeKey.","builder":"backend-builder","paths":["apps/api/src/attendance/attendance-alert-noti.repository.ts","apps/api/src/attendance/attendance-alert-noti.int.spec.ts"]},{"id":"L3-att-noti-job","task":"Job handler + wiring + dedupe. (a) attendance-alert-noti.job-handler.ts: @Injectable() + @SystemJobHandler(), jobCode ATT_ALERT_DETECT. CHỐT #2 — CẤM gọi intake() bên trong withTenant: intake() TỰ mở withTenant (notification-engine.service.ts:70) và withTenant = db.transaction() thuần (db.service.ts:74) ⇒ tx lồng + giữ 2 kết nối/bản ghi qua PgBouncer. Khuôn ĐÚNG: db.withTenant(ctx.companyId, tx => materialize 3 tập + tập dedupe_key đã tồn tại) → ĐÓNG tx → MỚI vòng lặp intake() (mirror notifications/task-reminder.job-handler.ts:73-93; chính scheduler/job-runner.ts:69 cảnh báo điều này). BẤT BIẾN #1: JobRunContext chỉ có companyId, handler TỰ mở withTenant, mọi query eq(companyId) tường minh. Phát qua NotificationEngineService.intake() in-process — KHÔNG outbox.enqueue, KHÔNG insert notifications thẳng, KHÔNG io.emit. TUYỆT ĐỐI KHÔNG truyền actorUserId (3 mã isSystemEvent=false ⇒ notification-recipient-resolver.service.ts:49-51 lọc BỎ actor khỏi recipient ⇒ chính người bị nhắc KHÔNG nhận, hỏng im lặng). Recipient mode UserIds = user của chính nhân viên (fan-out manager/HR HOÃN — không có bề mặt cấu hình, mirror ghi chú hoãn task-reminder.job-handler.ts:51-53). Lọc bỏ mọi ứng viên có dedupeKey nằm trong tập 'đã từng phát' (CHỐT #7). Fire-and-forget từng bản ghi có try/catch GHI LOG ERROR (không nuốt câm) + đếm success/failed/skipped theo từng mã vào JobRunResult.metadata. CHỐT #8: throttle in-memory (lastRunAtMs) dùng resolveAttAlertDetectIntervalMs của L1 — scheduler tick MỌI handler mỗi 60s (worker-scheduler.service.ts:76-85, worker-scheduler.config.ts:23) nên tick thứ 2 trong cùng cửa sổ phải return sớm: 0 truy vấn repo, 0 intake. Job CHỈ ĐỌC attendance_records (0 UPDATE/INSERT). (b) attendance.module.ts: APPEND imports NotificationsModule (đã verify KHÔNG chu trình — notifications.module.ts:83 chỉ import Database/Events/RealtimeEmitter/Permission) vào dòng imports hiện tại (attendance.module.ts:91) + APPEND providers [AttendanceAlertNotiJobHandler, AttendanceAlertNotiRepository]; repo mới tái dùng AttendanceRepository (đã là provider, attendance.module.ts:103). BẪY DI: tham số ctor nào KHÔNG phải provider Nest thì BẮT BUỘC @Optional() + default an toàn (system-job-runs-retention.job-handler.ts:86-91 — thiếu là sập AppModule kéo 100+ spec đỏ); ưu tiên chỉ dùng provider THẬT (DatabaseService, NotificationEngineService, repo mới) như task-reminder.job-handler.ts:61-64. KHÔNG đụng scheduler/scheduler.module.ts. (c) notification-dedupe.const.ts: APPEND ĐÚNG 3 khoá ATT_MISSING_CHECKOUT/ATT_LATE_DETECTED/ATT_ABSENT_DETECTED = { strategy:'DedupeKey', windowSeconds:null } — catalog 0481 để dedupe_strategy='None' ⇒ computeKey trả null ⇒ dedupe_key NULL ⇒ partial-unique coi NULL là distinct ⇒ mỗi nhịp đẻ 1 thông báo trùng. Hot-file: CHỈ THÊM, 0 dòng cũ bị sửa/xoá.","builder":"backend-builder","paths":["apps/api/src/attendance/attendance-alert-noti.job-handler.ts","apps/api/src/attendance/attendance-alert-noti.job-handler.spec.ts","apps/api/src/attendance/attendance-alert-noti-producer.int.spec.ts","apps/api/src/attendance/attendance.module.ts","apps/api/src/notifications/notification-dedupe.const.ts"]},{"id":"L4-att-noti-close","task":"Đóng sổ + ghi nợ. CHỐT #10 — sửa RELEASE-02 ĐÚNG 3 CHỖ: (a) dòng bảng KI-021 (dòng 40, cột chủ đang 'Sau MVP') → đánh dấu ĐÓNG kèm ngày + mã WO + bằng chứng file:dòng (producer mới, tên spec ALLOW/DENY, số notification đo trong int-spec, ghi rõ 'KHÔNG migration mới'); (b) mục chi tiết §KI-021 (dòng 503) → đánh dấu ĐÓNG; (c) THÊM MỚI một dòng 'Cập nhật 2026-08-14 (S10-ATT-NOTIPROD-1): S2 4 → 3 (còn KI-025 · KI-050 · KI-056)'. TUYỆT ĐỐI KHÔNG sửa các ghi chú lịch sử (dòng 88 · 126 · 806 · 819 và mọi dòng 'Cập nhật <ngày>' cũ khác) — sửa chúng làm tài liệu tự mâu thuẫn. CHỐT #1: harness/backlog.mjs CHỈ đổi status S10-ATT-NOTIPROD-1 → 'done'; paths (backlog.mjs:12192-12197) ĐÃ đúng, builder KHÔNG sửa paths nữa. CHỐT #15: ghi NỢ tường minh (PR body + RELEASE-02) rằng template ATT_ABSENT_DETECTED (mig 0481:161-164) viết theo giọng gửi HR/quản lý ('cần được kiểm tra') trong khi v1 gửi cho CHÍNH nhân viên — sửa câu chữ cần migration ⇒ WO này CẤM; không để nó im lặng thành 'đã đóng KI-021 nhưng nội dung lệch đối tượng'. CHỐT #14: ghi vào PR rằng ATT_LATE_DETECTED phát SAU biên ngày-đã-đóng là CỐ Ý (job theo lô, không realtime lúc check-in) để QA không coi là lỗi. Ghi thêm 4 nợ có địa chỉ, KHÔNG đẻ KI mới nếu owner chưa chốt: fan-out manager/HR; wiring widget ATTENDANCE_ALERTS ở dashboard-cache-invalidation.const.ts:43-48; job auto-checkout/auto-attendance (ATT_AUTO_ATTENDANCE_CREATED vẫn 0 producer); khai ATT_ALERT_DETECT_INTERVAL_MS vào config/env.schema.ts.","builder":"backend-builder","paths":["docs/RELEASE/RELEASE-02_Known_Issues_MVP.md","harness/backlog.mjs"]}]
acceptanceChecks: ["Có ĐÚNG 1 file job-handler mới trong apps/api/src/attendance/ mang @SystemJobHandler() + jobCode 'ATT_ALERT_DETECT' (không trùng jobCode đang có: RETENTION_CLEANUP · TEMP_FILE_CLEANUP · TASK_REMINDER · LEAVE_ACCRUAL/CARRYOVER · SYSTEM_JOB_RUNS_RETENTION · CHAT_* · DASHBOARD_MV_REFRESH · GOAL_RECONCILIATION · LMS_USER_SYNC) và được khai trong providers của attendance.module.ts.","[CHỐT #2] 0 lời gọi intake() nằm trong callback của withTenant: đọc code thấy khuôn materialize → đóng tx → vòng lặp intake (mirror task-reminder.job-handler.ts:73-93); unit-spec giả DatabaseService ghi nhận thứ tự và assert mọi intake xảy ra SAU khi callback withTenant đã resolve (0 tx lồng).","[CHỐT #3] Phân giải ca/rule THEO TỪNG NHÂN VIÊN qua hàm có sẵn: grep thấy resolveEffectiveShiftTx (attendance.repository.ts:310-356) + findDefaultShiftTx + resolveRule được TÁI DÙNG; 0 bản sao logic specificity Employee≻Department≻Company trong file mới. Spec đối chiếu: cùng nhân viên/ngày ⇒ ca phân giải KHỚP kết quả resolveShiftAndRule hiện hành.","[CHỐT #4] Anti-join vắng mặt khoá theo user_id (khớp attendance_records_company_user_date_uq, db/schema/hr.ts:93-96,114): grep điều kiện join KHÔNG dùng employee_id làm khoá đối chiếu; deny-path: record có user_id khớp nhưng employee_id IS NULL ⇒ 0 ATT_ABSENT_DETECTED.","[CHỐT #5] Trạng thái đơn LẪN HOA/THƯỜNG: grep thấy inArray(status, ['approved','Approved']) cho CẢ leave_requests và remote_work_requests; luật nửa ngày đúng attendance.repository.ts:488-491 (chỉ FullDay/MultipleDays/durationType NULL). Deny-path status='approved' chữ thường phủ ngày ⇒ 0 notification.","[CHỐT #6] Cửa sổ hợp đồng có mặt: (start_date IS NULL OR start_date <= closedWorkDate) AND (end_date IS NULL OR end_date >= closedWorkDate) (db/schema/employees.ts:56-57) + 2 deny-path test xanh.","[CHỐT #7] Chống HỒI SINH đo được: int-spec phát → soft-delete notification → chạy lại job ⇒ KHÔNG tạo lại (count không tăng). Truy vấn kiểm tồn tại theo dedupe_key KHÔNG lọc deleted_at (khác notification-dedupe.service.ts:102-117) và chạy TRONG tx materialize, không đẻ tx phụ mỗi bản ghi.","[CHỐT #8] Throttle: có hằng đặt tên + resolveAttAlertDetectIntervalMs parse-an-toàn/clamp mirror dashboard-mv-refresh.job-handler.ts:14-42; spec chứng minh tick thứ 2 trong cùng cửa sổ ⇒ 0 truy vấn repo + 0 intake. `git diff --name-only origin/master...HEAD | grep -c 'config/env.schema.ts' == 0`.","[CHỐT #9] Test discover KHÔNG assert đếm tuyệt đối: assert 'danh sách jobCode CHỨA ATT_ALERT_DETECT' + 'không có jobCode trùng trong tập discover' (baseline vừa đổi vì DASHBOARD_MV_REFRESH).","[CHỐT #10] RELEASE-02 sửa ĐÚNG 3 chỗ: dòng bảng KI-021 (dòng 40) ĐÓNG + mục chi tiết §KI-021 (dòng 503) ĐÓNG + THÊM MỚI dòng 'Cập nhật 2026-08-14 (S10-ATT-NOTIPROD-1): S2 4 → 3 (còn KI-025 · KI-050 · KI-056)'. `git diff` KHÔNG chạm dòng 88 · 126 · 806 · 819 (ghi chú lịch sử).","[CHỐT #11] grep 'late_notification_enabled' trong toàn diff = 0 kết quả (không đẻ cấu hình ma; v1 LUÔN BẬT theo SPEC-04).","[CHỐT #12] Tập 'đi muộn' loại bản ghi đã điều chỉnh/khoá sổ: có điều kiện loại is_adjusted · locked_at IS NOT NULL · status='approved_adjustment' · attendance_status='Adjusted' (db/schema/hr.ts:135,139,159,183) + deny-path test cho từng cái.","[CHỐT #13] Record đã soft-delete ⇒ fail-closed BỎ QUA (không bắn vắng mặt) — 1 ca test ghim, xanh.","[CHỐT #14] PR body ghi rõ ATT_LATE_DETECTED phát SAU biên ngày-đã-đóng là CỐ Ý (job theo lô).","[CHỐT #15] PR body + RELEASE-02 ghi NỢ template ATT_ABSENT_DETECTED (mig 0481:161-164) lệch đối tượng (giọng gửi HR/quản lý, v1 gửi nhân viên) — sửa cần migration ⇒ ngoài phạm vi WO.","[CHỐT #1] harness/backlog.mjs diff CHỈ đổi status S10-ATT-NOTIPROD-1 → 'done'; 0 dòng paths bị sửa (paths đã đúng ở backlog.mjs:12192-12197).","DI an toàn: int-spec dựng AppModule (Test.createTestingModule) chạy XANH — 0 lỗi 'Nest can't resolve dependencies'. Tham số ctor không phải provider Nest phải mang @Optional() (system-job-runs-retention.job-handler.ts:86-91).","Đường phát ĐÚNG: grep trong file mới cho 0 kết quả với `outbox.enqueue`, `io.emit`, `insert(notifications)`, `NotificationsService.create`; điểm phát DUY NHẤT là `NotificationEngineService.intake(`. grep `actorUserId` trong job-handler = 0; unit-spec assert DTO có actorUserId === undefined và recipient.userIds = [userId nhân viên] (chống bẫy lọc actor notification-recipient-resolver.service.ts:49-51 do isSystemEvent=false).","0 file mới/sửa dưới apps/api/migrations/** trong diff PR (`git diff --name-only origin/master...HEAD | grep -c '^apps/api/migrations/' == 0`); is_system_event và target_url_template GIỮ NGUYÊN như 0481/0497.","notification-dedupe.const.ts thuần additive: đúng 3 khoá mới = { strategy:'DedupeKey', windowSeconds:null }; 0 dòng khoá cũ bị sửa/xoá.","Idempotent theo KỲ: int-spec chạy handler 2 lần trên cùng fixture ⇒ count(notifications) không đổi, lần 2 không ném lỗi. dedupeKey suy từ nguồn ĐÓNG BĂNG: grep thấy khoá dựng từ record.workDate (cột DB) hoặc biên ngày-đã-đóng, KHÔNG từ new Date()/Date.now() tại điểm phát.","Không hard-code ngưỡng nghiệp vụ: 0 literal phút/giờ làm ngưỡng trong logic/repository (chỉ hằng ĐẶT TÊN cho trần batch/lookback/interval). Đi muộn đọc is_late/late_minutes đã tính bằng shifts.grace_late_minutes (attendance.logic.ts:111-115); thiếu check-out dùng shifts.end_time + tz; vắng mặt dùng shifts.work_days + public_holidays + leave/remote approved.","Deep-link không câm: int-spec assert notification có target_url = '/attendance/today' (mig 0497:59-61) cho cả 3 mã; nội dung render sạch — expect(content).not.toMatch(/\\{[a-z_]+\\}/); payload đủ work_date (3 mã) + employee_name (MISSING_CHECKOUT, ABSENT) theo 0481:153-163.","ALLOW TRƯỚC DENY: mỗi mã có ≥1 ca ALLOW tạo ĐÚNG 1 notification cho ĐÚNG recipient, và ca ALLOW commit TRƯỚC ca DENY (thấy trong lịch sử commit nhánh); coverage % Funcs của file mới ≥ 80%.","Bán kính có trần: quét ĐÚNG 1 ngày đã đóng (không backfill) — int-spec seed 1 ngày vắng mặt 30 ngày trước ⇒ 0 notification; có hằng trần bản ghi/lần chạy; JobRunResult.metadata trả đếm theo từng mã.","Cô lập tenant: mọi truy vấn qua db.withTenant(companyId, …) + eq(companyId) tường minh; int-spec cross-tenant: job cho công ty A ⇒ nhân viên vắng mặt công ty B nhận 0 notification.","Job CHỈ ĐỌC: 0 UPDATE/INSERT vào attendance_records/attendance_logs trong diff ⇒ không đụng bản ghi locked/adjusted, không phát sinh nghĩa vụ audit mới.","int-spec THỰC SỰ chạy (không SKIP): dưới LANE_DB=mediaos_attnotiprod và `bash harness/check.sh --lane-db` kết luận XANH (KHÔNG phải 'XANH KHÔNG ĐỦ BẰNG CHỨNG').","DoD §8: có test · không migration · validation input job không cần nhưng nhánh lỗi log ERROR · không phá luồng chính · cập nhật harness/backlog.mjs = done."]
testTasks: ["RED trước — unit L1: biên grace (đúng bằng grace ⇒ KHÔNG muộn, mirror lateMinutesFor `minsLate <= grace ⇒ 0`); biên ngày theo tz của ca (shiftTimezone fallback DEFAULT_TZ); ca cross_day (end_time <= start_time ⇒ biên đóng sang hôm sau); dedupeKey ổn định khi đổi 'giờ chạy' (2 mốc now khác nhau trong cùng ngày ⇒ CÙNG khoá); resolveAttAlertDetectIntervalMs: rỗng/NaN/rác/dưới biên/trên biên ⇒ mặc định, giá trị hợp lệ ⇒ giữ nguyên, KHÔNG ném (chốt #8).","int-spec L2 (DB cô lập) — ALLOW TRƯỚC: (a) 1 record check-in không check-out của ngày đã đóng ⇒ lọt missingCheckout; (b) 1 record is_late=true ⇒ lọt late; (c) 1 nhân viên active có ca, ngày làm việc, không record ⇒ lọt absent. RỒI mới DENY.","DENY L2 nhóm ngày/ca (chốt #3): ngày lễ (public_holidays company và global); thứ không nằm trong shifts.work_days; ngày CHƯA đóng theo ca CỦA CHÍNH NHÂN VIÊN; **nhân viên gắn ca đêm (cross_day) đang CÒN TRONG CA ⇒ 0 thông báo**; hai nhân viên khác ca trong cùng lượt ⇒ mỗi người dùng biên ngày của ca mình (chống một-ca-cho-cả-lượt).","DENY L2 nhóm khoá đối tượng (chốt #4 + #13): record có user_id khớp nhưng employee_id IS NULL ⇒ 0 ATT_ABSENT_DETECTED; record đã soft-delete (deleted_at NOT NULL) ⇒ fail-closed BỎ QUA, KHÔNG bắn vắng mặt; employee_profiles status != active ⇒ loại; employee không có user_id ⇒ loại.","DENY L2 nhóm đơn từ (chốt #5): leave_requests status='approved' CHỮ THƯỜNG phủ ngày ⇒ 0 notification; status='Approved' ⇒ 0; durationType nửa ngày (HalfDay…) ⇒ VẪN tính vắng/đi muộn (không loại); remote_work_requests 'Approved' và 'approved' phủ ngày ⇒ 0 notification.","DENY L2 nhóm hợp đồng (chốt #6): start_date > closedWorkDate ⇒ 0 notification; end_date < closedWorkDate ⇒ 0 notification; cả hai NULL ⇒ VẪN tính (ALLOW).","DENY L2 nhóm điều chỉnh/khoá sổ (chốt #12): 4 ca riêng — is_adjusted=true · locked_at IS NOT NULL · status='approved_adjustment' · attendance_status='Adjusted' ⇒ mỗi ca 0 ATT_LATE_DETECTED; record đã check-out ⇒ 0 missingCheckout.","int-spec L3 producer end-to-end: chạy handler ⇒ đúng 3 notification (1/mã) cho đúng recipient, event_code đúng, target_url='/attendance/today', content không còn placeholder; audit/notification_delivery_logs không vỡ.","int-spec idempotency theo KỲ + CHỐNG HỒI SINH (chốt #7): (a) chạy 2 lần liên tiếp ⇒ count(notifications) không đổi; (b) chạy lần 3 sau khi đổi đồng hồ trong cùng ngày ⇒ vẫn không đổi; (c) **phát → soft-delete notification (my-notifications.controller.ts:131) → chạy lại ⇒ KHÔNG tạo lại** (ca (a) KHÔNG bắt được lỗi này).","unit-spec CHỐT #2 (tx lồng): giả DatabaseService ghi lại thứ tự mở/đóng withTenant và mọi lời gọi intake ⇒ assert 0 intake nằm trong callback withTenant; nếu ai đó kéo intake vào trong tx, spec phải ĐỎ.","unit-spec CHỐT #8 (throttle): gọi run() 2 lần trong cùng cửa sổ ⇒ lần 2 trả sớm với 0 lời gọi repo + 0 intake; đẩy đồng hồ quá interval ⇒ chạy lại bình thường.","int-spec bán kính/backfill: seed dữ liệu vắng mặt 30 ngày trước ⇒ 0 notification; seed số nhân viên vắng vượt trần ⇒ số phát ≤ trần batch và metadata phản ánh đúng theo từng mã.","int-spec cross-tenant (deny-path): job chạy cho company A KHÔNG tạo notification nào cho company B; đếm theo company_id.","unit-spec bẫy actor-exclusion: fake NotificationEngineService bắt DTO, assert KHÔNG có actorUserId và recipient.userIds = [userId nhân viên] — thêm actorUserId ⇒ spec ĐỎ (chống hỏng-im-lặng do isSystemEvent=false, notification-recipient-resolver.service.ts:49-51).","spec DI + discover (chốt #9): dựng AppModule (hoặc ATT + Notifications) chứng minh handler resolve được; discoverJobHandlers() ⇒ assert danh sách jobCode CHỨA 'ATT_ALERT_DETECT' và KHÔNG có jobCode trùng — CẤM assert đếm tuyệt đối.","spec nhánh lỗi (silent-failure): intake ném cho 1 bản ghi ⇒ handler GHI LOG ERROR, tiếp tục bản ghi còn lại, JobRunResult.failed tăng đúng 1 (không nuốt câm, không dừng cả lượt).","QA-04/QA-08 đối chiếu: gắn ca test vào bảng test case SPEC-04 (ATT-TC đi muộn/thiếu check-out/vắng mặt) trong mô tả PR để Đội 3 truy vết; chạy `bash scripts/lane-db-setup.sh attnotiprod` + `export LANE_DB=mediaos_attnotiprod` + `bash harness/check.sh --lane-db` và đính kèm kết luận XANH."]
steps: ["0. KHÔNG có lane db-migration — done_when CẤM tường minh. Đã verify lại 2026-08-14: catalog 3 mã (mig 0481:59-61) + template vi-VN (0481:153-163) + target_url_template '/attendance/today' (0497:59-61) đã seed; system_job_runs/system_job_locks KHÔNG có CHECK enum trên job_code; WorkerSchedulerService.discoverJobHandlers() (worker-scheduler.service.ts:76-85) gom handler qua metadata ⇒ jobCode mới KHÔNG cần DDL/seed. Nếu builder thấy 'cần migration' ⇒ DỪNG, leo lên người chốt.","1. Đọc §CHỐT (15 mục) trong docs/plans/S10-ATT-NOTIPROD-1.md TRƯỚC khi gõ dòng code đầu tiên. Mâu thuẫn giữa §CHỐT và phần plan cũ ⇒ §CHỐT THẮNG (đáng chú ý: 'set-based cấm N+1' của plan cũ BỊ GHI ĐÈ bởi chốt #3 phân giải ca per-employee + trần batch; 'late_notification_enabled' BỊ BỎ bởi chốt #11).","2. L1 + L2 chạy SONG SONG (paths rời nhau; L2 tự khai interface hàng của mình, KHÔNG import ngược L1).","3. RED TRƯỚC ở cả L1/L2: viết ca ALLOW (đủ điều kiện ⇒ lọt vào tập ứng viên) TRƯỚC, chạy phải ĐỎ, RỒI mới viết ca DENY — nhánh không có ca ALLOW làm mọi ca DENY xanh RỖNG (bài học deny-cases-vacuous-without-allow-case). Đếm NHÁNH (% Funcs), không đếm số ca.","4. L3 sau khi L1+L2 xanh: handler (materialize → ĐÓNG tx → intake, chốt #2) + throttle (chốt #8) + lọc chống-hồi-sinh (chốt #7) + wiring module + APPEND 3 khoá DEFAULT_DEDUPE. Chạy int-spec dựng AppModule để chứng minh DI KHÔNG sập (bẫy @SystemJobHandler + @Optional).","5. Chạy int-spec trên DB CÔ LẬP: bash scripts/lane-db-setup.sh attnotiprod → export LANE_DB=mediaos_attnotiprod → pnpm --filter @mediaos/api test. Không set LANE_DB thì int-spec SKIP ⇒ xanh-giả (KI-045).","6. Đo idempotency THẬT theo KỲ: run() 2 lần liên tiếp ⇒ count(notifications) không đổi; và ca chống-hồi-sinh: phát → soft-delete → chạy lại ⇒ KHÔNG tạo lại (ca 'chạy 2 lần count không đổi' KHÔNG bắt được lỗi này).","7. Đo bán kính: seed 1 ngày vắng mặt cách đây 30 ngày ⇒ 0 thông báo (chỉ quét ĐÚNG 1 ngày đã đóng, KHÔNG backfill lịch sử khi lần đầu bật). Đo throttle: tick thứ 2 trong cùng cửa sổ ⇒ 0 truy vấn repo / 0 intake.","8. Gate LIGHT + bổ sung: typescript-reviewer + quality-gate + database-reviewer (anti-join tenant, trần batch, per-employee resolve) + silent-failure-hunter (try/catch fire-and-forget + bẫy recipient rỗng). Không crown-jewel (không đụng permission/RLS/secret/audit/auth/migration).","9. L4 cuối cùng: đóng KI-021 đúng 3 chỗ (chốt #10), backlog chỉ đổi status (chốt #1), ghi nợ #14/#15 vào PR.","10. bash harness/check.sh --lane-db trước khi mở PR; đính kết luận + bằng chứng vào docs/plans/S10-ATT-NOTIPROD-1.md."]
```

RECONCILE 2026-08-14 (đo lại toàn bộ 15 anchor của §CHỐT — TẤT CẢ CÒN ĐÚNG, gap VẪN MỞ).

GAP: grep toàn repo cho 3 mã chỉ ra 2 nơi, KHÔNG có producer nào — `apps/api/src/foundation/seed/notification-event-catalog.const.ts:84-86` (isEnabled:true, isSystemEvent:false) và phần LOẠI TRỪ có chú thích ở `apps/api/src/dashboard/dashboard-cache-invalidation.const.ts:43,77`. Danh mục hứa, hệ thống không giao — đúng mô tả WO.

HẠ TẦNG ĐÃ SẴN, KHÔNG MIGRATION: catalog `migrations/0481_s4_notiseed1_event_template_perms.sql:59-61`; template vi-VN `0481:153-163` (placeholder {work_date} + {employee_name}); deep-link `0497:59-61` = '/attendance/today'; `system_job_runs`/`system_job_locks` không có CHECK enum trên job_code; `WorkerSchedulerService.discoverJobHandlers()` gom qua metadata (worker-scheduler.service.ts:76-85).

THAY ĐỔI SO VỚI PLAN CŨ (lý do reused=false): 15 quyết định §CHỐT trước đây nằm ở VĂN XUÔI, nay đã nhồi vào lanes/acceptanceChecks/testTasks. Hai chỗ §CHỐT GHI ĐÈ plan cũ và builder phải theo bản mới: (1) ràng buộc "3 truy vấn set-based, CẤM N+1" bị thay bằng chốt #3 — phân giải ca/rule PER-EMPLOYEE bằng resolveEffectiveShiftTx/resolveRule có sẵn, N+1 được phép nhưng phải chặn bằng trần batch; (2) `late_notification_enabled` (mục 6 plan cũ, từng là "điểm cần người chốt") bị BỎ HẲN theo chốt #11 — khoá không tồn tại trong repo, v1 luôn bật.

XÁC MINH TỪNG ANCHOR: backlog.mjs:12192-12197 ĐÃ liệt kê notification-dedupe.const.ts (chốt #1 đã thực hiện — builder chỉ đổi status). notification-engine.service.ts:70 `intake` TỰ mở `this.db.withTenant` + db.service.ts:74 `withTenant` = `db.transaction()` ⇒ nguy cơ tx lồng CÓ THẬT (chốt #2); job-runner.ts:69 đã ghi đúng khuôn "materialize RỒI đóng tx". attendance.repository.ts:310-356 resolveEffectiveShiftTx + :359 findDefaultShiftTx còn nguyên; :470-495 findApprovedFullDayLeaveTx với inArray(['approved','Approved']) ở :485 và luật nửa ngày :488-491. db/schema/hr.ts: user_id notNull, unique `attendance_records_company_user_date_uq` trên (company_id,user_id,work_date) WHERE deleted_at IS NULL; employee_id NULLABLE (mig 0452) ⇒ chốt #4 đúng; is_adjusted/locked_at/status enum có 'approved_adjustment'/attendance_status có 'Adjusted' ⇒ chốt #12 đúng. db/schema/employees.ts:56-57 start_date/end_date ⇒ chốt #6 đúng. notification-dedupe.service.ts:102-117 `isDuplicate` LỌC `isNull(notifications.deletedAt)` ⇒ rủi ro hồi sinh CÓ THẬT (chốt #7). dashboard-mv-refresh.job-handler.ts:14-42 = khuôn throttle chuẩn (hằng + parse an toàn + clamp + ghi rõ env NGOÀI env.schema.ts) ⇒ chốt #8 sao chép được nguyên. system-job-runs-retention.job-handler.ts:86-91 giữ nguyên comment bẫy @Optional. notification-recipient-resolver.service.ts:49-51 lọc actor khi `!isSystemEvent && dto.actorUserId` ⇒ cấm truyền actorUserId. RELEASE-02: KI-021 ở dòng 40 (bảng, cột chủ 'Sau MVP') + dòng 503 (§chi tiết); ghi chú lịch sử ở 88/118/119/121/123/126/806/819 — CHỈ được THÊM dòng cập nhật mới, không sửa dòng cũ.

BẤT BIẾN: #1 company_id — JobRunContext chỉ có companyId, handler tự mở withTenant, mọi query eq(companyId) tường minh; int-spec cross-tenant bắt buộc. #2 không hard-delete — job CHỈ ĐỌC attendance_records (không mark is_missing_check_out, không đổi attendance_status: BACKEND-06 §22.1 bước 2-3 HOÃN), nhánh lỗi log ERROR chứ không nuốt câm. #3 không secret — không liên quan.

VERIFY (lệnh đo): bash scripts/lane-db-setup.sh attnotiprod ; export LANE_DB=mediaos_attnotiprod ; pnpm --filter @mediaos/api test ; bash harness/check.sh --lane-db (phải XANH, không phải "XANH KHÔNG ĐỦ BẰNG CHỨNG") ; git diff --name-only origin/master...HEAD | grep -c '^apps/api/migrations/' (= 0) ; git diff --name-only origin/master...HEAD | grep -c 'config/env.schema.ts' (= 0).

GATE: LIGHT theo chữ (không đụng permission/RLS/secret/audit/auth/migration) + bổ sung database-reviewer (anti-join tenant, per-employee resolve, trần batch, quét toàn bảng) và silent-failure-hunter (try/catch fire-and-forget, recipient rỗng, throttle nuốt lượt). Không crown-jewel ⇒ không cần Opus-plan riêng. KHÔNG có lane db-migration: done_when CẤM migration; builder thấy "cần migration" ⇒ DỪNG và leo lên người chốt.

NGOÀI PHẠM VI (ghi nợ, không làm ở WO này): wiring 3 mã vào dashboard-cache-invalidation.const.ts:43-48 → widget ATTENDANCE_ALERTS; fan-out Manager/HR cho LATE/ABSENT (không có bề mặt cấu hình — SPEC-04 §2067-2069 / API-04 §1739-1741 chỉ nói "nếu cấu hình"); AutoCheckoutJob/AutoAttendanceJob (ATT_AUTO_ATTENDANCE_CREATED vẫn 0 producer); ghi is_missing_check_out/đổi attendance_status; đổi is_system_event hoặc target_url_template (cần migration); sửa câu chữ template ATT_ABSENT_DETECTED lệch đối tượng (mig 0481:161-164 — cần migration, chốt #15 buộc ghi nợ tường minh); khai ATT_ALERT_DETECT_INTERVAL_MS vào config/env.schema.ts; màn hình FE (đã có /attendance/today).

RỦI RO CẦN NGƯỜI CHỐT KHI VERIFY: (a) LATE phát theo lô sau biên ngày (chốt #14) — QA dễ báo là lỗi; (b) template ABSENT gửi cho chính nhân viên trong khi câu chữ viết cho HR (chốt #15) — nếu owner coi đây là chặn thì phải mở WO migration riêng trước khi tuyên KI-021 đóng hoàn toàn.


---

## §CHỐT VÒNG 2 (owner chốt 2026-08-14, sau plan-review lần 2) — GHI ĐÈ §CHỐT VÒNG 1 khi mâu thuẫn

> 7 điểm BLOCKING mới, tất cả đo được. Đây là bộ chốt CUỐI: không còn vòng plan-review nữa, builder thi công thẳng
> theo file này, Đội 3 kiểm. Mọi mục dưới đây là BẮT BUỘC, kể cả các ca test đi kèm.

16. **Throttle phải khoá THEO `companyId`, KHÔNG dùng biến cấp-instance.** JobRunner gọi `handler.run({companyId})`
    lần lượt cho từng tenant (`job-runner.ts:72-75`, ORDER BY id cố định) ⇒ một `lastRunAtMs` dùng chung làm
    tenant thứ 2..N **đói vĩnh viễn**, và ở N=1 test vẫn xanh nên không ai thấy. Dùng `Map<companyId, lastRunAtMs>`
    + dọn khoá cũ. Test: 2 company trong CÙNG tick ⇒ CẢ HAI được quét; tick thứ 2 của CÙNG company trong cửa sổ ⇒ 0 truy vấn/0 intake.
17. **Biên ca đêm đọc CỘT `shifts.cross_day`** (`db/schema/attendance.ts:63`, notNull default false) — KHÔNG suy
    bằng `end_time <= start_time`. `isOvernight()` (`attendance.logic.ts:81-83`) là nhánh LEGACY ScheduleCalc,
    không phải nguồn sự thật của ca; đường check-in/out thật dùng `ShiftCalc.crossDay` (`attendance.logic.ts:125`).
    Tái dùng `toShiftCalc`/`wallTimeToInstant` thay vì viết lại toán biên. Test ghim: ca 22:00–06:00 `crossDay=true`
    vs cùng giờ `crossDay=false` ⇒ biên KHÁC nhau đúng theo cột.
18. **FAIL-CLOSED khi cấu hình ca KHUYẾT** (schema cho phép khuyết: `work_days` jsonb NULLABLE `attendance.ts:64`,
    `start_time`/`end_time` NULLABLE `:51-52`, `resolveEffectiveShiftTx` rồi `findDefaultShiftTx` đều có thể trả null):
    không có ca hiệu lực & không có ca mặc định ⇒ **BỎ QUA** + đếm `metadata.skippedNoShift`;
    `end_time` NULL ⇒ KHÔNG xét missing-checkout; `work_days` NULL ⇒ KHÔNG xét absent.
    3 deny-path test, mỗi ca assert 0 notification. (Mặc định "không có work_days ⇒ mọi ngày là ngày làm" sẽ bắn
    ATT_ABSENT cho TOÀN CÔNG TY vào thứ Bảy/Chủ nhật.)
19. **Khoá dedupe LƯU trong DB là `${eventCode}:${rawKey}`**, không phải khoá thô — `computeKey` với
    strategy `DedupeKey` trả `${eventCode}:${dto.dedupeKey}` (`notification-dedupe.service.ts:78`). Truy vấn
    "đã từng phát" phải khớp ĐỦ cột của partial-unique (`company_id + recipient_user_id + event_code + dedupe_key`,
    mig `0479:279-281`) và KHÔNG lọc `deleted_at`. Unit assert ghim
    `repoLookupKey === dedupe.computeKey(...)` để hai bên không trôi khỏi nhau — thiếu assert này thì chốt #7
    chết IM LẶNG (tập "đã phát" luôn rỗng) và builder sẽ đi chữa test thay vì chữa khoá.
20. **NGOẠI LỆ `public_holidays`**: bảng này cố ý có `company_id` NULLABLE cho ngày lễ GLOBAL và đường đọc
    KHÔNG filter `eq(companyId)` (`holidays.repository.ts:6-10 findInRange`). Áp máy móc "mọi truy vấn có
    eq(companyId)" ⇒ mất ngày lễ quốc gia ⇒ bắn ATT_ABSENT cho cả công ty vào Tết/2-9. Sửa câu nghiệm thu thành
    "mọi bảng nghiệp vụ TRỪ public_holidays (hàng global)"; tái dùng ngữ nghĩa `HolidaysRepository.findInRange`.
21. **Hợp đồng giữa repo và logic (khép vòng, tránh nguồn sự thật thứ hai):** repository lấy ứng viên trong
    **cửa sổ ngày có trần** (vd `[today-1, today]` để phủ ca đêm) và TRẢ VỀ kèm ca đã phân giải; quyết định
    "ngày này đã đóng chưa" chạy DUY NHẤT ở hàm thuần, do handler gọi **sau khi đã đóng tx**. Với late/missing-checkout:
    map `user_id → employee_profiles` (active, chưa xoá) để lấy `employeeId`+`orgUnitId` cho `resolveEffectiveShiftTx`
    (vì `attendance_records.employee_id` NULLABLE — `hr.ts:114`); map không được ⇒ FAIL-CLOSED bỏ qua + đếm metadata + 1 ca test.
22. **Trần batch KHÔNG được nuốt người im lặng.** Bắt buộc: `ORDER BY` xác định + **loại tập "đã từng phát" TRƯỚC
    khi áp LIMIT** + log WARN + `metadata.truncated=true` khi chạm trần. Test: seed (trần+2) người vắng ⇒ lượt 1
    phát đúng trần, lượt kế (sau cửa sổ throttle) phát NỐT 2 người còn lại. (LIMIT đặt trước bộ lọc dedupe ⇒ mỗi lượt
    trả lại đúng N người cũ, người thứ N+1 KHÔNG BAO GIỜ được thông báo — và nghiệm thu cũ "phát ≤ trần" hợp thức hoá lỗ đó.)
23. **Clamp nhịp chạy tới mức NGÀY**, không bê nguyên `[30s, 1h]` của dashboard: MAX 24h, DEFAULT theo nhịp nghiệp vụ,
    để cấu hình "ngày 1 lần" mà không phải sửa code (detector NGÀY chạy 24 lượt/ngày là vô nghĩa và mỗi lượt N+1 truy vấn ca).
24. **Sửa RELEASE-02 neo theo VĂN BẢN, không theo SỐ DÒNG** (số dòng lệch khi PR khác land trước): tìm hàng bảng
    chứa `KI-021`, tiêu đề mục `§KI-021`; luật chung: **KHÔNG sửa bất kỳ dòng nào bắt đầu bằng "Cập nhật &lt;ngày&gt;"** —
    chỉ THÊM dòng cập nhật mới.
25. **`lookback` = TRẦN AN TOÀN của cửa sổ truy vấn (vd 2 ngày để phủ ca đêm), KHÔNG phải backfill.** Ghi rõ trong
    docblock; ca test "vắng mặt 30 ngày trước ⇒ 0 thông báo" vẫn giữ nguyên.
26. **Nợ ghi vào PR + RELEASE-02 (cùng nhóm với #14/#15):** (a) worker chết qua biên đóng ⇒ ngày đó MẤT thông báo
    vĩnh viễn (throttle in-memory reset khi restart không cứu) — gợi ý WO sau: quét N ngày gần nhất có trần;
    (b) người nghỉ NỬA NGÀY đã duyệt mà không check-in nửa còn lại VẪN nhận ATT_ABSENT (đúng code hiện hành
    `attendance.repository.ts:488-491`, nhưng dễ bị coi là báo sai).
27. **🔴 BÁN KÍNH PROD — bắt buộc trước khi bật:** PROD funtime có 45 nhân viên đã import; nếu phần lớn không
    check-in thực tế thì lượt đầu ATT_ABSENT bắn cho gần như toàn bộ nhân sự MỖI NGÀY. PR PHẢI ghi: (i) câu truy vấn
    ĐO KHÔ đếm số recipient của 1 ngày đã đóng (chạy trên staging/PROD-clone, dán số vào PR), và (ii) kill-switch =
    `notification_events.is_enabled=false` cho 3 mã (`intake` tự skip qua `findEnabledEvent`,
    `notification-engine.service.ts:72-79`) — clamp env KHÔNG cho phép tắt bằng biến môi trường.
28. **Đặt producer trong `AttendanceModule`** (đã verify không chu trình) dù tiền lệ `TaskReminderJobHandler` nằm
    trong `NotificationsModule` (`notifications.module.ts:112`) — ghi LÝ DO vào docblock để lần sau không có 2 khuôn.
29. **KI-021 đóng KÈM 2 NỢ CÓ ĐỊA CHỈ** (không hạ xuống "đóng một phần"): defect thật của KI-021 là "0 producer",
    và nó được đóng. Câu chữ template ABSENT lệch đối tượng + fan-out Manager/HR ghi thành nợ tường minh có địa chỉ file.
