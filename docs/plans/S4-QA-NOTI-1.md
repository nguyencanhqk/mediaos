```yaml
wo: S4-QA-NOTI-1
zone: yellow
generated_by: auto-loop
reconciled_at: "c7d9039"
lanes: [{"id":"qanoti1-permission-denypath","task":"QA sign-off NOTI permission/own-scope + deny-path (RED-first) trên DB cô lập mediaos_qanoti1. VERIFY 4 int-spec BE hiện có vẫn xanh (my-notifications · noti-event-intake · notification-admin-config · notification-admin-write) + THÊM 1 spec NOTI-QA hợp nhất, truy vết được, phủ đúng ma trận done_when + LẤP gap: mark-all-read idempotent (gọi 2 lần → updated_count=0, unread_count không đổi) và deny per-pair mảnh (user CÓ view:notification-config nhưng THIẾU view:notification-template → GET /notifications/templates/:id 403; THIẾU update:* → PATCH 403). Đo + báo coverage scoped src/notifications/** ≥80%. CHỈ chạm src/notifications khi QA phát hiện bug thật (fix additive, không đổi hành vi ngoài bug).","builder":"backend-builder","paths":["apps/api/test/integration/**","apps/api/src/notifications/**"]}]
acceptanceChecks: ["Own-scope tuyệt đối: user chỉ đọc/mark-read/delete notification recipient_user_id=CHÍNH MÌNH; cross-user (cùng company) GET/mark-read/DELETE → 404 NOTI-ERR-NOTIFICATION-NOT-FOUND (không lộ tồn tại), KHÔNG đổi trạng thái row nạn nhân.","mark-read idempotent: POST /notifications/:id/mark-read lần 2 (đã Read) → 200 status=Read; unread-count đúng sau mỗi lần (giảm 1 lần đầu, không âm/không đổi lần sau).","mark-all-read idempotent (GAP): POST /notifications/mark-all-read lần 2 → 200 updated_count=0, unread_count giữ 0 (không lỗi, không âm).","Intake dedupe: retry cùng eventCode+sourceEntityId+recipient trong cửa sổ TimeWindow → createdCount=0, dedupedCount≥1, chỉ 1 notification tồn tại (outbox retry KHÔNG nhân đôi).","Actor-exclusion: non-system event, actor∈recipients → actor createdCount=0 (không tự nhận); system-event → actor VẪN nhận (chứng minh loại trừ có điều kiện, không nuốt).","Admin-config deny GET: thiếu view:notification-config → 403 GET /notifications/events; thiếu view:notification-template → 403 GET /notifications/templates/:id; thiếu view:notification-delivery-log → 403 GET /notifications/delivery-logs (sensitive gate fail-closed, KHÔNG chạm DB).","Admin-config deny PATCH: thiếu update:notification-config → 403 PATCH /notifications/events/:id; thiếu update:notification-template → 403 PATCH /notifications/templates/:id.","Deny per-pair mảnh (GAP): user CÓ view:notification-config nhưng THIẾU view:notification-template → GET /notifications/templates/:id vẫn 403 (quyền theo TỪNG cặp engine, không blanket theo module).","Cross-tenant deny (BẤT BIẾN §1): own-scope cross-tenant GET → 404; admin-config company B KHÔNG thấy events-override/delivery-log của company A và ngược lại (RLS + withTenant).","Gate CỨNG hasDb && LANE_DB: mọi describe skipIf khi thiếu LANE_DB (tránh xanh-giả/đỏ-giả); test chạy THẬT trên mediaos_qanoti1 (chain migration 0000→head sạch).","Coverage src/notifications/** ≥80% (báo cáo text-summary; nhạy cảm → ngưỡng riêng CLAUDE.md §6); test colocated/int-spec khớp glob thực chạy (không false-green vitest-unit-specs-must-be-colocated).","check.sh xanh; LIGHT gate pass; security-reviewer xác nhận deny-path fail-closed; DoD §8: có test, không phá luồng chính, backlog cập nhật."]
testTasks: ["RED-first deny-path (viết + chứng minh đỏ trước GREEN): noPerm → 403 mọi route My-Notification; cross-user 404; cross-tenant 404 own-scope.","RED-first admin-config deny: employee (không có cặp config/template/delivery-log) → 403 cả GET events/templates/delivery-logs + PATCH events/templates.","RED-first deny per-pair mảnh: user grant CHỈ view:notification-config → GET /templates/:id 403 (thiếu view:notification-template); user grant view nhưng thiếu update:* → PATCH 403.","Idempotency: mark-read 2 lần + mark-all-read 2 lần → không lỗi, unread-count/updated_count đúng.","Intake dedupe retry: 2 lần cùng key → 1 notification (createdCount=1 rồi 0, dedupedCount=1).","Actor-exclusion: non-system actor loại; system-event actor nhận.","Cross-tenant regression 2-tenant: own-scope + admin events/delivery-log không rò A↔B.","Chạy toàn bộ trên mediaos_qanoti1 + báo cáo coverage scoped src/notifications/** ≥80%."]
steps: ["Dựng DB cô lập: `bash scripts/lane-db-setup.sh qanoti1 --reset` (chain 0000→head trên mediaos_qanoti1 — mig 0479/0481/0483/0487 catalog+grant NOTI phải áp sạch), rồi `export LANE_DB=mediaos_qanoti1`.","Đọc chéo done_when ↔ coverage hiện có: my-notifications.int-spec.ts (own-scope/mark-read idempotent/soft-delete/noPerm 403) · noti-event-intake.int-spec.ts nhóm (b)(b2) dedupe app+DB-race, (c) actor-exclusion, (d) cross-tenant, notification-admin-config.int-spec.ts (a) employee 403 cả 3 GET + (d)(d2) cross-tenant, notification-admin-write.int-spec.ts (a) employee 403 PATCH events/templates + (e) cross-tenant → chốt danh sách gap (mark-all-read idempotent · deny per-pair mảnh).","Viết spec hợp nhất truy vết NOTI-QA: apps/api/test/integration/noti-qa-permission.int-spec.ts — RED-first (deny-path đi đầu), gate `hasDb && LANE_DB`, dùng helpers seedCompany/seedUser/seedUserRole + directPool; KHÔNG mock guard/service (bootstrap AppModule thật).","Chạy RED: xác nhận spec đỏ khi thiếu quyền/own-scope (deny-path thực sự chặn TRƯỚC khi chạm DB), rồi GREEN trên mediaos_qanoti1.","Đo coverage scoped: `pnpm --filter @mediaos/api exec vitest run test/integration/noti-qa-permission.int-spec.ts test/integration/my-notifications.int-spec.ts test/integration/noti-event-intake.int-spec.ts test/integration/notification-admin-config.int-spec.ts test/integration/notification-admin-write.int-spec.ts apps/api/src/notifications --coverage --coverage.include='src/notifications/**' --coverage.reporter=text-summary` → báo cáo số ≥80% cho src/notifications/**.","Chạy cổng: `LANE_DB=mediaos_qanoti1 bash harness/check.sh`; LIGHT gate (typescript-reviewer + quality-gate); nhánh permission/deny-path → security-reviewer soi fail-closed; cập nhật harness/backlog.mjs done."]
```

GAP-ANALYSIS (reconcile 2026-07-11, không có plan cũ → phân rã mới): done_when của S4-QA-NOTI-1 ĐÃ được phủ ~90% bởi 4 int-spec viết cho lane BE — my-notifications.int-spec.ts (own-scope/cross-user 404/mark-read idempotent/soft-delete/noPerm 403/unread-count), noti-event-intake.int-spec.ts nhóm (b)(b2) dedupe app+DB-race, (c) actor-exclusion, (d) cross-tenant, notification-admin-config.int-spec.ts (a) employee 403 cả 3 GET + (d)(d2) cross-tenant, notification-admin-write.int-spec.ts (a) employee 403 PATCH events/templates + (e) cross-tenant. ĐÂY LÀ WO QA SIGN-OFF, KHÔNG build feature — chống scope-creep: KHÔNG sửa BE controller/service trừ khi QA lộ bug thật. GAP THẬT cần lấp: (1) mark-all-read GỌI 2 LẦN idempotent (spec BE chỉ test 1 lần); (2) deny per-pair MẢNH — user có view:notification-config nhưng THIẾU view:notification-template → GET /templates/:id 403 (spec BE chỉ test employee thiếu TẤT CẢ, chưa chứng minh quyền theo từng cặp engine); (3) 1 spec NOTI-QA hợp nhất truy vết được để sign-off + báo cáo coverage. INVARIANTS áp dụng: §1 company_id ở mọi query qua withTenant + RLS (cross-tenant 404/không-thấy, KHÔNG dựa kỷ luật dev); §2 notifications soft-delete (deleted_at + status=Deleted, KHÔNG hard-delete) và notification_delivery_logs append-only (app role không UPDATE/DELETE — đã có notification-delivery-append-only.int-spec.ts + migration-smoke (0) trong admin-write); own-scope TUYỆT ĐỐI = filter recipient_user_id=current user (SPEC-08 §16.5.1, KHÔNG đọc data-scope Team/Company). Cặp quyền THẬT: own-scope = (read|mark_read|mark_all_read|hide|delete):notification (is_sensitive=false, mig 0481/0483) từ notification-permissions.const.ts; admin = view/update:notification-config, view/update:notification-template, view:notification-delivery-log (is_sensitive=true, mig 0487) hard-code trong notification-admin.controller.ts — dùng ĐÚNG tuple thật, không bịa. VERIFY: DB cô lập mediaos_qanoti1 (scripts/lane-db-setup.sh qanoti1 --reset) vì migrator drizzle áp đơn điệu theo `when` → DB chung skip band → xanh/đỏ giả (memory integration-test-lane-db-gate + mediaos-shared-db-drift). GATE: mọi describe skipIf(!(hasDb && LANE_DB)) — .env trỏ DATABASE_URL vào DB dev nên hasDb=true nhưng phải có LANE_DB mới chạy thật; check.sh mặc định KHÔNG set LANE_DB ⇒ skip im lặng (memory ci-skips-most-integration-specs) → PHẢI chạy `LANE_DB=mediaos_qanoti1 bash harness/check.sh` để chứng minh. COVERAGE: apps/api/vitest.config.ts CHƯA có threshold cho notifications và file đó NẰM NGOÀI paths WO (paths chỉ có test/integration + src/notifications) → coverage ≥80% là số ĐO/BÁO CÁO scoped (--coverage.include='src/notifications/**'), KHÔNG persist per-file threshold trong lần này; nếu owner muốn siết cứng threshold thì cần WO nới paths thêm apps/api/vitest.config.ts + apps/api/package.json (flag out-of-scope). Nguồn coverage sẵn có: notifications.spec.ts, notification-dedupe.spec.ts, notification-preferences.spec.ts, notification-renderer.spec.ts, device-token.spec.ts, notification-engine.errors.spec.ts + các int-spec ⇒ khả năng đã ≥80%, lane chỉ cần đo + lấp mép mỏng. OUT-OF-SCOPE: phần TASK (S4-QA-TASK-1) và phần DASH-widget-visibility (giữ ở S4-QA-1 gộp, chờ DASH-BE-2); thay đổi hành vi BE NOTI; FE (apps/app); persist ngưỡng coverage vào vitest.config. depends_on S4-NOTI-BE-4 đã ship (mig 0487 grant INSERT/UPDATE, PATCH events/templates live) → WO READY actionable.
</content>
