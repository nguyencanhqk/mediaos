```yaml
wo: S5-ME-DOC-1
zone: green
generated_by: auto-loop
reconciled_at: "027e1be7"
lanes: [{"id":"me-doc-spec","task":"SPEC/PRD/README sync cho ME. (1) SPEC-01: thêm ME vào §7 module list (dòng MVP-bổ-sung) + §31 Ma trận liên kết module (ME phụ thuộc AUTH+HR; ME đọc-lại ATT/LEAVE/TASK/NOTI/DASH/FOUNDATION, scope Own). (2) RECONCILE xung đột số: SPEC-01 §7.2/§8/§12.8 hiện map SPEC-09→PAYROLL 'Tiền lương' nhưng README+file đã map SPEC-09→ME → dời đánh số spec Phase-2 +1 (PAYROLL→SPEC-10 … AI→SPEC-17) HOẶC đánh dấu placeholder; cần owner chốt. (3) Header nav 8 file SPEC-01..08: thêm link '· [SPEC-09 ME](<SPEC-09 ME.md>)' (SPEC-01 dùng đường dẫn cùng thư mục). (4) SPEC-09 self: điền §21 cột 'Đề xuất' làm quyết định chốt ME-DEC-001..010 + ghi ngày chốt 13/07/2026, sửa §22 mục 10 'API-09/10' → 'API-11', §1 flip Trạng thái Draft→Approved (chỉ khi owner duyệt PR). (5) PRD-00 §8: thêm khối 'MVP bổ sung' ME = Personal Hub/Employee Self-service. (6) README §8 dòng ME: thay placeholder 'dự kiến' bằng ref thật (SPEC-09, DB-08 user_preferences, API-11, DB-10 seed); UI/FRONTEND/BACKEND để 'dự kiến (WO sau)'.","builder":"backend-builder","paths":["docs/SPEC/SPEC-01 Tổng quan.md","docs/SPEC/SPEC-02 AUTH.md","docs/SPEC/SPEC-03 HR.md","docs/SPEC/SPEC-04 ATT.md","docs/SPEC/SPEC-05 LEAVE.md","docs/SPEC/SPEC-06 TASK.md","docs/SPEC/SPEC-07 DASH.md","docs/SPEC/SPEC-08 NOTI.md","docs/SPEC/SPEC-09 ME.md","docs/PRD/PRD-00 Enterprise Management System .md","docs/README.md"]},{"id":"me-doc-db","task":"DB docs sync cho ME (CHỈ docs — KHÔNG viết migration/schema, code thuộc S5-ME-DB-1). (1) DB-01 §8.2 modules: thêm ME vào enum module_code + §7 danh mục bảng ghi nhận user_preferences (user-scope). (2) DB-08: thêm mục mới '§8.5 Bảng user_preferences' theo SPEC-09 §15.2 — cột id/company_id/user_id/locale/timezone/theme/date_format/time_format/default_landing/density/favorite_modules(JSONB)/me_layout_config(JSONB)/created_at/updated_at; BẮT BUỘC ghi company_id + 'RLS ENABLE+FORCE, policy tenant_isolation' (bất biến #1, DECISIONS-02 §2) + UNIQUE(company_id,user_id); cập nhật §5.9 precedence thêm tầng User; note phương án B (mở rộng setting scope User) nếu owner chọn tái dùng thay tạo bảng. (3) DB-09: thêm index 'idx_user_preferences_company_user UNIQUE (company_id, user_id)' vào mục index per-table + partial soft-delete nếu áp dụng. (4) DB-10 §10 Seed modules: thêm dòng module ME + §seed permission: ME.ACCESS + ME.PREFERENCE.*/NOTIFICATION_PREFERENCE.UPDATE_OWN/AVATAR.UPDATE_OWN… theo SPEC-09 §11.1, ghi rõ pattern idempotent ON CONFLICT DO NOTHING.","builder":"backend-builder","paths":["docs/DB/DB-01 DATABASE DESIGN TỔNG QUAN.md","docs/DB/DB-08 Audit Files Settings Seeds Database Design.md","docs/DB/DB-09 Database Index Query Pattern Performance Design.md","docs/DB/DB-10_Migration_Plan_Initial_Seed_Data_Database_Design.md"]},{"id":"me-doc-api","task":"Tạo file MỚI 'docs/API Design/API-11_ME_API_Design.md' = ME API Design stub theo SPEC-09 §14 (API-09 ĐÃ là FOUNDATION, API-10 ĐÃ là Permission Matrix/Audit → dùng số 11). Tuân API-01 (envelope, mã lỗi, pagination, idempotency). Liệt kê endpoint §14.2: prefix /api/v1/me — GET /me, /me/overview, /me/profile, /me/account, /me/security/sessions, DELETE session, POST revoke-others, /me/security/activity, các *-summary, GET/PATCH /me/preferences(+appearance/notifications), POST/DELETE /me/avatar; §14.3 endpoint nguồn gọi-lại; §14.4 nguyên tắc BẮT BUỘC: KHÔNG nhận user_id/employee_id từ client (resolve từ token, chống IDOR), summary chịu lỗi cục bộ, mask theo quyền, mutation quan trọng ghi audit. Header breadcrumb + khối 'Liên quan' trỏ SPEC-09/DB-08/permission-matrix. Đánh dấu Trạng thái: Stub/Draft.","builder":"backend-builder","paths":["docs/API Design/API-11_ME_API_Design.md"]}]
acceptanceChecks: ["SPEC-01: ME xuất hiện trong danh sách module (§7, khối MVP-bổ-sung) VÀ §31 Ma trận liên kết có dòng ME↔AUTH/HR (phụ thuộc) + ME đọc ATT/LEAVE/TASK/NOTI/DASH; KHÔNG còn 2 tài liệu cùng số SPEC-09 (grep 'SPEC-09' trong SPEC-01 chỉ trỏ ME; PAYROLL đã dời số hoặc đánh dấu placeholder).","Cả 8 file SPEC-01..SPEC-08 có link header nav tới SPEC-09 ME resolve đúng (mở được file docs/SPEC/SPEC-09 ME.md).","PRD-00 §8 liệt kê ME là MVP bổ sung (Personal Hub / Employee Self-service).","DB-08 có mục user_preferences với ĐỦ cột theo SPEC-09 §15.2, có company_id, ghi RLS ENABLE+FORCE (bất biến #1) và UNIQUE(company_id,user_id); DB-01 §8.2 module_code enum gồm ME và danh mục bảng ghi user_preferences.","DB-09 có index UNIQUE user_preferences(company_id, user_id).","DB-10 §10 seed có dòng module ME + seed permission ME (ME.ACCESS + preference/avatar/notification-preference theo SPEC-09 §11.1) với pattern idempotent ON CONFLICT DO NOTHING.","File docs/API Design/API-11_ME_API_Design.md tồn tại, theo API-01 (envelope/lỗi/pagination), liệt kê endpoint SPEC-09 §14.2 với prefix /api/v1/me, và nêu rõ 'không nhận user_id/employee_id từ client, resolve từ token' (§14.4).","SPEC-09: §21 ME-DEC-001..010 = cột 'Đề xuất' + ghi ngày chốt; §22 mục 10 sửa thành 'API-11'; §1 Trạng thái Draft→Approved (đúng chỉ khi owner duyệt PR).","README §8 dòng ME trỏ tài liệu thật (SPEC-09 + DB-08 user_preferences + API-11 + DB-10 seed), không còn placeholder 'API-10 dự kiến'.","git diff --name-only chỉ nằm trong docs/**; KHÔNG đụng apps/** (bảo toàn loop song song trên apps/**) và không đụng UI/FRONTEND/BACKEND doc bodies (ngoài phạm vi WO)."]
testTasks: ["Doc-lint liên kết: mọi ref SPEC-09/API-11/DB-08 user_preferences mới thêm phải resolve tới file tồn tại (kiểm markdown link trên docs/SPEC, docs/README.md, docs/DB, docs/API Design).","Numbering-consistency: grep 'SPEC-09' toàn docs/SPEC — xác nhận SPEC-09 chỉ còn nghĩa ME; PAYROLL/'Tiền lương' không còn gán SPEC-09 (đã dời số hoặc placeholder nhất quán §7.2/§8/§12.8).","Invariant-doc check: grep DB-08 user_preferences xác nhận có 'company_id' + 'FORCE ROW LEVEL SECURITY' + 'UNIQUE (company_id, user_id)' (bất biến #1).","API stub check: xác nhận API-11 nêu 'không nhận user_id/employee_id' + prefix /api/v1/me + audit cho mutation (SPEC-09 §14.4/§17).","Scope guard: git diff --name-only — 100% path trong docs/**; deny-path/integration RED N/A (docs-only, không đổi code/permission/workflow — deny-path thật thuộc S5-ME-DB-1/ME-BE)."]
steps: ["Chốt số ME=SPEC-09 (khớp file + README) và quyết định renumber Phase-2 (PAYROLL→SPEC-10…) — trình owner ở PR (green nhưng chạm đánh số spec).","me-doc-api: viết API-11_ME_API_Design.md stub trước để khoá tên file, cho README/SPEC-09 §22.10 trỏ nhất quán.","me-doc-db: thêm user_preferences (DB-08) + module/permission seed (DB-10) + index (DB-09) + enum module ME (DB-01), luôn kèm company_id + RLS+FORCE + UNIQUE(company_id,user_id).","me-doc-spec: cập nhật SPEC-01 (§7/§8/§12.8/§31 + reconcile số), 8 header nav, SPEC-09 (§21 chốt/§22.10 sửa/§1 status), PRD-00 §8, README §8 dòng ME.","Doc-lint: chạy kiểm tra liên kết + numbering; xác nhận git diff chỉ nằm trong docs/**.","Owner duyệt PR → flip SPEC-09 Draft→Approved + ME-DEC Approved; auto-commit sau khi doc-lint xanh."]
```

GAP-ANALYSIS: (1) Xung đột đánh số SPEC-09 — HARD CONFLICT: SPEC-01 §7.2 (dòng 153), §8 (dòng 178) và §12.8 (dòng 785-787) map SPEC-09 → PAYROLL 'Tiền lương' (Phase 2 placeholder), nhưng README §8 (dòng 40, 179) và file SPEC-09 ME.md đã gán SPEC-09 → ME. Không thể để 2 module cùng số. Đề xuất reconcile (spec thắng, khớp file+README+reframe de-media-fy): ME giữ SPEC-09; dời placeholder Phase-2 +1 → PAYROLL=SPEC-10, RECRUIT=SPEC-11, ASSET=SPEC-12, ROOM=13, CHAT=14, SOCIAL=15, MOBILE=16, AI=17 tại SPEC-01 §7.2/§8/§12.8. Đây là thay đổi cấp spec → CẦN OWNER CHỐT ở PR (dù green). Nếu owner không muốn renumber, thay bằng ghi chú 'số Phase-2 là placeholder, sẽ cấp khi mở'. (2) §22.10 SPEC-09 hiện ghi 'Tạo API-09/10 ME API Design' — SAI vì API-09=FOUNDATION, API-10=Permission Matrix/Audit; sửa thành API-11 (đồng bộ file mới + README). (3) DB-08 hôm nay CHỈ có system_settings (§8.3) + company_settings (§8.4), CHƯA có tầng user-scope → user_preferences là bảng MỚI trong docs; §5.9 precedence phải thêm tầng User. SPEC-09 §15.3 cho phương án thay thế = mở rộng setting scope (System/Company/Role/User) thay vì bảng riêng — ghi cả hai, owner chọn ở S5-ME-DB-1. (4) README §8 dòng ME còn placeholder 'DB-08 §settings dự kiến / API-10 dự kiến / UI-09 dự kiến / FRONTEND dự kiến / BACKEND dự kiến' → cập nhật DB/API/SPEC sang ref thật, giữ UI/FRONTEND/BACKEND='dự kiến (WO sau)'.
INVARIANTS: DOCS-ONLY, không đổi runtime. Nhưng nội dung user_preferences PHẢI mô tả company_id + RLS ENABLE+FORCE + policy tenant_isolation (bất biến #1, DECISIONS-02 §2) + UNIQUE(company_id,user_id) để S5-ME-DB-1 code đúng ngay. Bất biến #2 (append-only) và #3 (secret) không áp cho preference. Seed ME (DB-10) theo hot-file convention: module catalog UNION, permission seed ON CONFLICT DO NOTHING (CLAUDE.md §9.3).
VERIFY: doc-lint link integrity; grep numbering (SPEC-09 chỉ ME); grep DB-08 (company_id/FORCE/UNIQUE); API-11 nêu no-user_id-from-client; git diff chỉ docs/**.
GATE: LIGHT (docs-only, zone green) — quality-gate + rà nhất quán tài liệu; KHÔNG cần FULL security/database gate vì không đổi code/schema/permission enforcement. Không có test RED runtime. Điểm CẦN NGƯỜI CHỐT: (a) quyết định renumber Phase-2 spec; (b) flip SPEC-09 Draft→Approved + ME-DEC Approved — đây là hành động owner-approval theo done_when ('flip khi owner duyệt PR docs này').
OUT-OF-SCOPE (chống scope-creep): apps/** (code/schema/migration ME = S5-ME-DB-1); SPEC-09 §22 mục 11-14 = UI-02/04/06/07/09/10, FRONTEND route/app registry docs, BACKEND aggregation docs, QA matrix, IMPLEMENTATION backlog → WO sau. SPEC-09 §22 mục 4-7 (DB-02/03/07 'xác nhận') KHÔNG nằm trong done_when (chỉ DB-01/08/09/10) → không sửa DB-02/03/07 ở WO này trừ khi chỉ là câu xác nhận 1 dòng. Loop khác đang chạy apps/** → tuyệt đối không chạm apps/. Builder = backend-builder cho MỌI lane (đều là .md docs; KHÔNG dùng db-migration vì WO không tạo migration — nếu builder định viết SQL migration thật là sai phạm vi).
</content>
