```yaml
wo: S5-ME-FE-3
zone: green
generated_by: auto-loop
reconciled_at: "33bcdf68"
lanes: [{"id":"fe3webcore","task":"web-core plumbing (KHÔNG chồng lane khác): (1) me-api.ts APPEND getAttendanceSummary/getLeaveSummary/getTaskSummary/getNotificationSummary (section-envelope meAttendanceSectionSchema… đã có contracts) + getPreferences(mePreferencesSchema) + patchAppearance(mePreferencesAppearancePatchSchema) — mọi method KHÔNG nhận/gửi user_id/employee_id (IDOR §14.4). (2) File MỚI notification-preferences-api.ts: list() GET /notifications/preferences (z.array(notificationPreferenceSchema)) + upsert() PUT /notifications/preferences body {notificationType,enabled} (upsertNotificationPreferenceSchema) — surface lỗi BE 400 'mandatory notification cannot be disabled' cho caller. (3) File MỚI lib/theme.ts: primitive theme SYSTEM/LIGHT/DARK (THEME_STORAGE_KEY='mediaos-theme', getStoredTheme→'system'|'light'|'dark', resolveSystemTheme() qua matchMedia, applyTheme(pref) áp class dark trên <html> + ghi localStorage fail-soft) — đặt Ở web-core vì ui→web-core (web-core KHÔNG import ui). (4) session.ts bootstrapSession: SAU authApi.me() thành công → meApi.getPreferences() → nếu theme!=null applyTheme; lỗi/không có → giữ theme local (FAIL-SOFT, KHÔNG đổi giá trị trả về true). (5) query-keys.ts APPEND meKeys.preferences() + notificationPreferenceKeys. (6) registry.ts ROUTE_REGISTRY APPEND ME-SCREEN-009..014 (paths /me/attendance /me/leave /me/tasks /me/notifications /me/preferences/notifications /me/preferences/appearance, gate literal ['access:me']). (7) i18n nav locale APPEND routeTitle.me* cho 6 route mới. + spec cho mỗi file mới/đổi.","builder":"frontend-builder","paths":["packages/web-core/src/lib/me-api.ts","packages/web-core/src/lib/notification-preferences-api.ts","packages/web-core/src/lib/theme.ts","packages/web-core/src/lib/session.ts","packages/web-core/src/lib/query-keys.ts","packages/web-core/src/lib/registry.ts","packages/web-core/src/i18n/locales/vi/nav.ts","packages/web-core/src/index.ts"]},{"id":"fe3uitheme","task":"packages/ui use-theme lên 3 giá trị (depends_on fe3webcore): use-theme.ts refactor ThemePreference='system'|'light'|'dark', DELEGATE primitive sang @mediaos/web-core lib/theme (getStoredTheme/applyTheme/resolveSystemTheme) — KHÔNG tự khai localStorage lần 2 (single source). useTheme trả {theme,setTheme(pref)} + giữ toggleTheme back-compat (light↔dark). theme-toggle.tsx: giữ nút toggle sáng/tối hiện có hoạt động (không vỡ topbar), 'system' resolve qua matchMedia. + use-theme.spec cập nhật (system→matchMedia; light/dark round-trip localStorage).","builder":"frontend-builder","paths":["packages/ui/src/hooks/use-theme.ts","packages/ui/src/components/ui/theme-toggle.tsx","packages/ui/src/index.ts"]},{"id":"fe3apppages","task":"apps/app pages + routing + sidebar + i18n (depends_on fe3webcore + fe3uitheme): (A) Công việc của tôi — MeAttendancePage(/me/attendance ME-SCREEN-009) card trạng thái chấm công HÔM NAY từ meApi.getAttendanceSummary + deep-link /attendance/today & /attendance/my-records; MeLeavePage(/me/leave ME-SCREEN-010) bảng rút gọn số dư phép + pendingRequestCount từ getLeaveSummary + deep-link /leave/me/requests; MeTasksPage(/me/tasks ME-SCREEN-011) counts assigned/dueToday/overdue từ getTaskSummary + deep-link /tasks/my-tasks — MỌI trang render theo section {status,data}: ok/error(degraded)/module_disabled/forbidden/unlinked_employee (§13); CHỈ gọi /me/*-summary, KHÔNG query bảng nguồn, KHÔNG thay trang nguồn (§7.5). (B) MeNotificationsPage(/me/notifications ME-SCREEN-012) TÁI DÙNG myNotificationApi.list (own-scope) + unread từ getNotificationSummary + deep-link /notifications; MeNotificationPreferencesPage(/me/preferences/notifications ME-SCREEN-013) đọc notificationPreferencesApi.list, nhóm theo §10.7, toggle In-app→upsert; loại mandatory: BE 400 khi tắt → REVERT + hiển thị disabled+giải thích (KHÔNG giả bật); kênh Email/Push = unavailable disabled (KHÔNG giả đã bật). (C) MeAppearancePage(/me/preferences/appearance ME-SCREEN-014) chọn theme System/Light/Dark → useTheme.setTheme(local) + meApi.patchAppearance({theme}) ghi server; lỗi server vẫn áp local (fail-soft); ngôn ngữ & múi giờ READ-ONLY (ME-DEC-008 P2). (D) router.tsx lazy-route 6 trang qua makeModuleRoute (mirror meOverviewRoute). (E) sidebar-registry.ts ME_SIDEBAR APPEND nhóm 'Công việc của tôi'/'Thông báo'/'Cài đặt cá nhân' theo §8.1. (F) i18n me.ts + nav routeTitle APPEND. + spec mỗi page (loading/error/empty/degraded).","builder":"frontend-builder","paths":["apps/app/src/routes/me/**","apps/app/src/i18n/**","apps/app/src/router.tsx","apps/app/src/layouts/workspace/sidebar-registry.ts"]}]
acceptanceChecks: ["/me/attendance (ME-SCREEN-009) hiển thị trạng thái chấm công hôm nay từ GET /me/attendance-summary + deep-link tới /attendance/today & /attendance/my-records; KHÔNG gọi endpoint bảng ATT nguồn trực tiếp (chỉ meApi.getAttendanceSummary).","/me/leave (ME-SCREEN-010) hiển thị bảng rút gọn số dư phép + pendingRequestCount từ GET /me/leave-summary + deep-link /leave/me/requests; KHÔNG tự tính lại số dư (§7.4).","/me/tasks (ME-SCREEN-011) hiển thị assignedCount/dueTodayCount/overdueCount từ GET /me/task-summary + deep-link /tasks/my-tasks; KHÔNG thay trang My Tasks (§7.5).","Cả 3 trang Công việc render đúng theo section status: ok / error(degraded, có retry) / module_disabled / forbidden / unlinked_employee (§13) — 1 nguồn lỗi KHÔNG làm trắng trang (§18.2).","/me/notifications (ME-SCREEN-012) tái dùng myNotificationApi.list (own-scope) + unread từ getNotificationSummary + deep-link /notifications; client KHÔNG truyền user_id/recipient khác.","/me/preferences/notifications (ME-SCREEN-013) đọc GET /notifications/preferences, nhóm theo §10.7; toggle In-app gọi PUT /notifications/preferences {notificationType,enabled}; loại mandatory: PUT tắt bị BE 400 → UI REVERT + render disabled + giải thích (KHÔNG hiển thị đã-tắt giả); kênh Email/Push hiển thị 'chưa hỗ trợ' disabled, KHÔNG giả lập đã bật (§10.7).","/me/preferences/appearance (ME-SCREEN-014): chọn theme System/Light/Dark áp NGAY qua useTheme (class dark trên <html> + localStorage 'mediaos-theme') VÀ ghi server qua PATCH /me/preferences/appearance {theme}.","Sync 2 chiều: bootstrapSession sau /me nạp GET /me/preferences → nếu theme!=null áp theme server; server lỗi/không có theme → giữ theme localStorage (fail-soft, bootstrap vẫn trả true — không chặn render).","Ngôn ngữ & múi giờ ở appearance hiển thị READ-ONLY (giá trị kế thừa) khi policy chưa mở — KHÔNG cho edit (ME-DEC-008, P2); KHÔNG gửi timezone override.","use-theme hỗ trợ 3 giá trị system/light/dark; 'system' resolve qua matchMedia; toggleTheme cũ (light↔dark) còn hoạt động (topbar không vỡ).","ROUTE_REGISTRY có 6 route mới gate ['access:me']; sidebar ME có nhóm Công việc của tôi/Thông báo/Cài đặt cá nhân (§8.1) trỏ ĐÚNG route đã tồn tại (không link chết); i18n vi đủ cho me + routeTitle 6 route.","REBUILD packages/web-core dist thành công (consumer import từ dist); pnpm typecheck + pnpm build (web-core/ui/app) xanh; harness/check.sh xanh; LIGHT gate react-reviewer + quality-gate PASS; mỗi page có test loading/error/empty (DoD §8)."]
testTasks: ["Unit spec 6 page (routes/me/*.spec.tsx): render loading → data → empty; 3 trang Công việc render mỗi section status ok/error(degraded+retry)/module_disabled/forbidden/unlinked_employee đúng (QA-02 ME, SPEC-09 §13).","me-api.spec.ts APPEND: getAttendanceSummary/getLeaveSummary/getTaskSummary/getNotificationSummary/getPreferences gọi đúng path /me/*, parse section/preferences schema; KHÔNG có tham số user_id/employee_id trong URL/body (chống IDOR, QA-05).","notification-preferences-api.spec.ts (MỚI): list() GET /notifications/preferences; upsert() PUT body {notificationType,enabled}; DENY-PATH: upsert enabled=false cho type mandatory → BE trả 400 → client ném lỗi có message, KHÔNG nuốt (silent-failure).","use-theme.spec.ts: setTheme('light'|'dark'|'system') → applyTheme ghi localStorage + toggle class dark; 'system' resolve qua matchMedia (mock matchMedia light/dark); getStoredTheme đọc lại đúng; toggleTheme back-compat light↔dark.","session.spec.ts APPEND theme-sync: sau /me thành công + getPreferences trả theme='light' → applyTheme('light'); getPreferences THROW → theme local giữ nguyên, bootstrapSession vẫn resolve true (FAIL-SOFT — deny toàn-trang-vỡ).","MeNotificationPreferencesPage.spec: type mandatory render toggle disabled + text giải thích; user cố tắt → PUT 400 → UI revert về enabled (KHÔNG hiển thị tắt giả); kênh Email/Push render 'chưa hỗ trợ' disabled, KHÔNG state đã-bật.","MeAppearancePage.spec: đổi theme gọi useTheme.setTheme + meApi.patchAppearance({theme}); patchAppearance lỗi mạng → theme local vẫn áp (fail-soft); language/timezone control ở trạng thái read-only (không editable).","Deep-link/no-source-table spec: 3 trang Công việc chỉ gọi meApi.*Summary (mock), assert KHÔNG gọi attendance/leave/task list-api nguồn; link deep-link trỏ đúng ME_QUICK_ACTION_PATHS (/attendance/today, /leave/me/requests, /tasks/my-tasks).","Integration wiring (registry-guard/route-authz spec): xác nhận 6 route mới gate access:me và render qua ProtectedRoute (mirror route-authz-wiring.spec)."]
steps: ["Lane fe3webcore TRƯỚC (nền plumbing): me-api summaries+preferences, notification-preferences-api MỚI, theme.ts primitive (web-core, vì ui→web-core), session.ts theme-on-login fail-soft, query-keys meKeys.preferences+notificationPreferenceKeys, registry.ts ROUTE_REGISTRY ME-SCREEN-009..014, nav routeTitle. RED trước cho api-client specs + session theme-sync fail-soft.","Lane fe3uitheme (sau khi theme.ts của fe3webcore merge): use-theme lên system/light/dark delegate primitive web-core; giữ toggleTheme back-compat; theme-toggle không vỡ.","Lane fe3apppages CUỐI (depends_on cả hai): 6 page + router lazy routes + sidebar ME items + i18n me/nav; consume meApi/notificationPreferencesApi + useTheme; render đủ section-status/loading/error/empty; deep-link về module gốc, KHÔNG query bảng nguồn.","Verify: pnpm --filter @mediaos/web-core build (REBUILD dist — consumer FE import từ dist) → pnpm --filter @mediaos/ui build → pnpm --filter @mediaos/app build → pnpm typecheck → bash harness/check.sh (LIGHT gate). Không có migration (green, FE-only).","Cập nhật harness/backlog.mjs (done_when) + LƯU docs/plans/S5-ME-FE-3.md (khối yaml máy-đọc) cho tái dùng."]
```

GAP-ANALYSIS (đối chiếu CODE 2026-07-16): docs/plans/S5-ME-FE-3.md CHƯA tồn tại → phân rã MỚI (reused=false). BE 100% sẵn: MeController (GET /me, /me/overview, 4 *-summary), MePreferencesController (GET /me/preferences, PATCH /me/preferences[/appearance]), NotificationsController (GET+PUT /notifications/preferences), MyNotificationsController (GET /notifications list). Contracts ĐÃ đủ (packages/contracts/src/me.ts: mePreferencesSchema/mePreferencesAppearancePatchSchema/meAttendanceSection…/meNotificationSummary; notification.ts: notificationPreferenceSchema/upsertNotificationPreferenceSchema/notificationTypeSchema) ⇒ WO.paths ĐÚNG khi bỏ packages/contracts (KHÔNG cần đổi contracts). FE thiếu: me-api.ts chỉ có getOverview; KHÔNG có web-core client cho notification preferences; use-theme (packages/ui) mới light|dark.

INVARIANTS (CLAUDE.md §2 luôn áp): FE-only, KHÔNG chạm DB/RLS/audit/secret. IDOR (§14.4/§17.1): mọi client resolve owner từ token — KHÔNG khai param user_id/employee_id; BE đã khoá. Masking là việc SERVER: FE chỉ render field nhận được. Green/LIGHT gate (react-reviewer + quality-gate) — KHÔNG FULL gate vì không đụng permission/RLS/secret/audit/migration.

QUYẾT ĐỊNH KIẾN TRÚC (builder phải theo): (1) ui→web-core (dependency 1 chiều) ⇒ theme primitive PHẢI ở web-core (lib/theme.ts): getStoredTheme/applyTheme/resolveSystemTheme + THEME_STORAGE_KEY='mediaos-theme' hỗ trợ system/light/dark; use-theme (ui) DELEGATE — KHÔNG tự khai localStorage lần 2 (single source, tránh drift). (2) Theme-on-login sync đặt trong bootstrapSession (session.ts, web-core) SAU authApi.me() — nơi duy nhất chạy mọi app khi authed VÀ trong scope; PHẢI fail-soft (getPreferences lỗi/không theme → giữ local, KHÔNG chặn bootstrap trả true). (3) Mandatory/security notification: BE GET /notifications/preferences KHÔNG expose cờ mandatory (nằm ở notification_rules.mandatory per-company, chỉ enforce ở PUT = 400 'mandatory notification cannot be disabled'). ⇒ FE HONEST: nhóm §10.7 bằng UI-constant + coi PUT-400 là AUTHORITATIVE (revert+lock+giải thích). KHÔNG giả bật/giả tắt. (4) Kênh: model BE chỉ có enabled per notificationType = kênh In-app; Email/Push CHƯA có cột kênh ⇒ render 'chưa hỗ trợ' disabled (§10.7). (5) Ngôn ngữ/múi giờ ở appearance = READ-ONLY (ME-DEC-008 timezone opt-in default-DENY, BE trả 422 ME-ERR-TIMEZONE-OVERRIDE-DENIED; locale ME-SCREEN-015 là P2) — tránh cần policy-read endpoint không có.

SCOPE-GAP (CẦN owner mở rộng WO.paths): wiring 6 route mới BẮT BUỘC sửa apps/app/src/router.tsx (lazy makeModuleRoute) + apps/app/src/layouts/workspace/sidebar-registry.ts (ME_SIDEBAR append §8.1) — CẢ HAI NGOÀI WO.paths hiện khai. Lane fe3apppages đã liệt 2 file này vào paths (guard-scope sẽ CẢNH BÁO). Đề xuất bổ sung 'apps/app/src/router.tsx' + 'apps/app/src/layouts/workspace/**' vào WO.paths. registry.ts (ROUTE_REGISTRY) NẰM TRONG scope (packages/web-core/src/**) — OK. Anti-FOUC: apps/*/index.html (out-of-scope) inline-script chỉ light/dark ⇒ theme 'system' lần sơn đầu = dark rồi bootstrapSession/useTheme resolve về đúng (FOUC nhẹ cho 'system'); chấp nhận MVP, không sửa index.html đợt này.

HOT-FILE quy ước APPEND (không rewrite): me-api.ts append method; query-keys.ts append meKeys.preferences + notificationPreferenceKeys; registry.ts ROUTE_REGISTRY append 6 RouteMeta (giữ pattern literal ['access:me'] mirror me.overview — KHÔNG qua PERMISSION_CODE_TO_PAIR, tránh pair-drift); nav locale + me.ts i18n deep-merge append; web-core/index.ts export thêm notificationPreferencesApi + theme primitives; ui/index.ts giữ export use-theme.

VERIFY: pnpm --filter @mediaos/web-core build (REBUILD dist — nếu quên, app import bản cũ = trang trắng, memory web-core-stale-dist) → pnpm --filter @mediaos/ui build → pnpm --filter @mediaos/app build → pnpm typecheck → pnpm test (vitest colocated *.spec.tsx) → bash harness/check.sh (TURBO_FORCE=1 chống false-green). KHÔNG migration (green, FE-only) ⇒ không cần LANE_DB.

OUT-OF-SCOPE (chống scope-creep): KHÔNG build ME-SCREEN-002..008 (S5-ME-FE-2), KHÔNG avatar upload UI, KHÔNG data-export (ME-DEC-009 P1), KHÔNG mở edit locale/timezone (P2), KHÔNG đổi BE/contracts/migration, KHÔNG sửa apps/*/index.html anti-FOUC. Nếu spec lệch code cũ → spec thắng (điểm lệch mandatory/channel đã ghi ở trên).
