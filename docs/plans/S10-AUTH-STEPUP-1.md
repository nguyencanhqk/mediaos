```yaml
wo: S10-AUTH-STEPUP-1
zone: red
generated_by: auto-loop
reconciled_at: "657984ee"
lanes: [{"id":"L0-ADR-SCOPE","task":"NỐI TIẾP, CHẶN mọi lane khác — chốt thiết kế bằng ADR TRƯỚC KHI CODE, APPEND vào DECISIONS-09 (§6 mới; KHÔNG tạo DECISIONS-10 vì paths của WO chỉ khai DECISIONS-09) + cập nhật §5 'Khi nào xem lại' trỏ sang §6. ADR phải chốt 8 điểm, mỗi điểm 1 câu quyết định: (1) cơ chế = CỬA SỔ LƯU PHÍA SERVER (Valkey-first, fallback memory per-process theo khuôn ReplayGuardService), khoá theo bộ-5 (companyId,userId,action,resourceType,resourceId), TTL ngắn — KHÔNG token bearer client-side, KHÔNG cờ session toàn cục, KHÔNG claim trên JWT; (2) CHỈ TOTP: cấm gọi `TwoFactorService.verifyChallenge` (two-factor.service.ts:264 — nó ĐỐT recovery code qua `userRecoveryCodes.usedAt` và claim `totp-step` của luồng LOGIN); đường verify của step-up là hàm RIÊNG, replay-marker RIÊNG (≠ 'totp-step') để hai luồng không ăn claim của nhau; (3) user CHƯA bật TOTP ⇒ KHÔNG mint được window: chốt mã lỗi + HTTP status (tái dùng mã 2FA sẵn có, KHÔNG đẻ mã mới nếu đã có), và ghi rõ đây là hệ quả có chủ đích của TOTP-only; (4) registry `REVEAL_CLASS_PAIRS` khởi tạo RỖNG, sống trong src, là DEFAULT của một DI token — endpoint đọc token, cổng test đọc CÙNG const; hệ quả tường minh: ở PROD hôm nay KHÔNG cặp nào mintable, endpoint tồn tại nhưng luôn 400 'pair không nằm trong registry' — đó là trạng thái ĐÚNG cho tới WO gắn cờ; (5) điều kiện để THÊM một cặp về sau: route phải có đúng segment `:id` VÀ có ReauthGuard trong chuỗi guard VÀ có đường cấp object grant thật; ghi rõ PATCH /settings/security-policy VẪN chưa đủ điều kiện (singleton, §2/§5); (6) window dùng-lại-được trong TTL hay single-use — chọn MỘT, ghi hệ quả; (7) mất Valkey ở đường GHI ⇒ TUYỆT ĐỐI không trả 200 kèm reauthValidUntil mà store không giữ (fallback memory single-instance hoặc lỗi hạ tầng tường minh — chọn MỘT, ghi hệ quả nhiều-instance); đường ĐỌC mất Valkey ⇒ không có window ⇒ deny (fail-closed, đúng); (8) CẤM tường minh: sửa `needsObjectGrant` trong permission.decide.ts, sửa permission.guard.ts, gán reauthContext ở interceptor/vô điều kiện, dùng chung bucket rate-limit của login (rl:ip / rl:acct). Đồng thời APPEND vào `paths` của WO trong harness/backlog.mjs 8 đường còn thiếu (packages/contracts/src/auth.ts · common/valkey/** · config/env.schema.ts · .env.example · test/integration/** · test/foundation/route-verdicts.ts · docs/_review census) — thiếu bước này thì guard-scope cảnh báo và gate/scheduler đọc sai tầng review (bài học wo-paths-drive-gate-and-scheduler).","builder":"backend-builder","paths":["docs/DECISIONS/DECISIONS-09_Security_Policy_Reauth_And_Object_Grant.md","docs/plans/S10-AUTH-STEPUP-1.md","harness/backlog.mjs"]},{"id":"L1-CONTRACT-KEY-ENV","task":"Hạ tầng KHOÁ + HỢP ĐỒNG (chạy sau L0, trước lõi): (1) `apps/api/src/common/valkey/valkey-key.ts` APPEND builder `stepUpKey(companyId,userId,action,resourceType,resourceId, envScope=currentEnvScope())` trả `stepup:{envScope}:{companyId}:{userId}:{action}:{resourceType}:{resourceId}` — envScope BẮT BUỘC (KI-067) ⇒ `isKeyScoped()` true; APPEND một ReplayMarker mới cho step-up (≠ 'totp-step'); KHÔNG đụng danh sách miễn trừ legacy; (2) APPEND `stepup:` vào KEY_PREFIXES của valkey-key-census.spec.ts + ca trong valkey-key.spec.ts chứng minh đổi BẤT KỲ thành phần nào của bộ-5 ⇒ khoá khác; (3) rate-limit dùng LẠI `rlKey('stepup', companyId+userId)` (bucket TÁCH HẲN rl:ip/rl:acct của login — bài học per-user-rate-limit-throttles-own-int-spec + login-lockout-keyed-by-email); (4) `packages/contracts/src/auth.ts` APPEND: `stepUpRequestSchema` (action/resourceType `.min(1).max(N)`, `resourceId: z.string().uuid()` — audit_logs.object_id là cột uuid nên thiếu ràng buộc này là 500 chứ không phải 400) + `stepUpResponseSchema` ({ reauthValidUntil }) + 2 mã `SecurityEventType` mới cho nhánh thành công/thất bại của step-up (event_type là cột text KHÔNG CHECK ở mig 0443 ⇒ KHÔNG cần migration) + gán severity trong `SECURITY_EVENT_SEVERITY` (Record exhaustive ⇒ quên gán là typecheck ĐỎ); rồi `pnpm --filter @mediaos/contracts build` (dist cũ ⇒ typecheck đỏ giả); (5) `env.schema.ts` + `.env.example`: `STEP_UP_TTL_SEC` và `STEP_UP_MAX_ATTEMPTS` theo mẫu dòng 115-119 nhưng PHẢI có CẢ `.default()` LẪN `.max()` (trần) — biến MỚI không có default từng giết fixture int-spec (env-schema-floor-breaks-test-fixtures).","builder":"backend-builder","paths":["apps/api/src/common/valkey/valkey-key.ts","apps/api/src/common/valkey/valkey-key.spec.ts","apps/api/src/common/valkey/valkey-key-census.spec.ts","packages/contracts/src/auth.ts","apps/api/src/config/env.schema.ts",".env.example"]},{"id":"L2-BE-STEPUP-CORE","task":"LÕI step-up (crown-jewel, RED trước): (1) `apps/api/src/auth/step-up/reveal-class-pairs.ts` — `export const REVEAL_CLASS_PAIRS: readonly RevealClassPair[] = []` (RỖNG, D3) + DI token; AuthModule provide token với DEFAULT === chính const đó (spec ghim đồng nhất bằng identity, để không ai fork ngầm); (2) `step-up-window.service.ts` — grant/read window qua `stepUpKey()`, Valkey-first + fallback memory theo khuôn ReplayGuardService; CODEC là chỗ dễ chết: `ValkeyService.get()` trả STRING nên đường đọc PHẢI parse lại thành `Date` (spec bắt buộc theo done_when); read chỉ trả expiry ĐỌC ĐƯỢC từ store, KHÔNG bao giờ tự tính `Date.now()+ttl`; (3) `step-up.service.ts` — TOTP-ONLY: đường verify THUẦN tự viết (nạp user_totp, giải mã secret, `TotpService.verify`, claim replay bằng marker RIÊNG), TUYỆT ĐỐI KHÔNG gọi `TwoFactorService.verifyChallenge` và KHÔNG chạm bảng `user_recovery_codes`; mọi truy cập DB trong `withTenant(companyId)` với companyId LẤY TỪ JWT (BẤT BIẾN #1); thứ tự bắt buộc: validate Zod → kiểm pair ∈ registry → `rateLimiter.isLocked()` TRƯỚC khi verify (khoá ⇒ từ chối, KHÔNG gọi verify) → verify → sai: recordFailure + audit Failure + security-event, KHÔNG cấp window; đúng: reset bucket + grant window + audit Success + security-event; (4) AUDIT CẢ HAI NHÁNH qua AuditService.record với `objectType:'auth'` (đã có sẵn trong AUDIT_OBJECT_TYPES ⇒ KHÔNG migration), `objectId = resourceId`, `resultStatus` ∈ Success/Failure/Denied (cột có từ mig 0432, CHECK ở SQL) + dual-write `SecurityEventWriter.record` trong CÙNG tx như mẫu 2FA; before/after CHỈ metadata (action/resourceType/resourceId/reauthValidUntil) — cấm mật khẩu/mã TOTP/recovery/hash (BẤT BIẾN #3); (5) `POST /auth/step-up` trong auth.controller.ts — KHÔNG @Public (đi JwtAuthGuard+CompanyGuard toàn cục), @HttpCode(200), DTO suy từ Zod của L1; user chưa bật TOTP ⇒ mã lỗi đã chốt ở ADR. Ghi chú: hàm verify TOTP thuần có thể đặt trong two-factor.service.ts để tái dùng giải-mã-secret, nhưng PHẢI là method MỚI, không sửa hành vi verifyChallenge hiện có (spec 2FA cũ giữ nguyên xanh).","builder":"backend-builder","paths":["apps/api/src/auth/step-up/**","apps/api/src/auth/auth.controller.ts","apps/api/src/auth/auth.dto.ts","apps/api/src/auth/auth.module.ts","apps/api/src/auth/two-factor.service.ts","apps/api/src/auth/two-factor.service.spec.ts"]},{"id":"L3-BE-REAUTH-GUARD","task":"ReauthGuard — NƠI DUY NHẤT GHI `req.reauthContext` (crown-jewel, RED trước): đọc metadata `@RequirePermission`; KHÔNG phải reveal-class (`isSensitive && requiresReauth`) ⇒ return true và KHÔNG ghi gì (0 thay đổi hành vi cho MỌI route đang có); reveal-class ⇒ resourceId = `req.params?.id` (null ⇒ KHÔNG ghi), tra StepUpWindowService theo bộ-5 lấy từ JWT (companyId/userId) + metadata (action/resourceType), CHỈ khi store trả expiry còn hạn mới gán `req.reauthContext = { reauthValidUntil }` và giá trị PHẢI là `instanceof Date` (assert ngay trong guard theo done_when — chống chuỗi ISO lọt xuống `isReauthValid` rồi luôn false, một lớp lỗi im lặng). CẤM tự tính TTL trong guard. Wire provider qua permission.module.ts theo mẫu forwardRef(AuthModule) đã dùng cho SecurityEventWriter/TwoFactorEnforcementGuard, để controller chỉ cần `@UseGuards(ReauthGuard, PermissionGuard)`. TUYỆT ĐỐI KHÔNG sửa permission.guard.ts / permission.decide.ts (DECISIONS-09 §3.2) — `git diff --stat` hai file đó phải RỖNG.","builder":"backend-builder","paths":["apps/api/src/permission/guards/reauth.guard.ts","apps/api/src/permission/guards/reauth.guard.spec.ts","apps/api/src/permission/permission.module.ts"]},{"id":"L4-QA-GATE-INTSPEC","task":"CỔNG + KIỂM THỬ THẬT (cuối, cần cả writer lẫn route mới tồn tại): (1) `reauth-reachability.e2e-spec.ts` — LẬT ca 'chưa có step-up nào trong src/**' sang `.toBe(true)` + cập nhật docblock; NÂNG CẤP `findUnreachableReauthRoutes` từ 2 lên 4 lý do: giữ `no-id-param` + `no-step-up-writer`, THÊM `no-reauth-guard` (route khai requiresReauth mà `classGuards`+`routeGuards` không chứa ReauthGuard — ghim bằng `ReauthGuard.name`, KHÔNG literal chuỗi) và THÊM `pair-not-mintable` (cặp action:resourceType của route không nằm trong `REVEAL_CLASS_PAIRS` import TỪ src — CÙNG registry endpoint đọc); mỗi lý do mới có ca thử-ngược chứng minh cổng không rỗng; GIỮ NGUYÊN các ca thử-ngược cũ của `hasReauthWriter` (gán null/undefined/comment không tính là writer) và KHÔNG nới điều kiện `:id` thành `startsWith(':')`; (2) int-spec DB cô lập `test/integration/step-up-reauth.int-spec.ts` — module test RIÊNG (KHÔNG đăng ký vào AppModule, để census/cổng không thấy route giả), controller fixture reveal-class `/test-reveal/:id` `@UseGuards(ReauthGuard, PermissionGuard)`, override DI token registry bằng cặp test, object_permissions THẬT, chạy HTTP thật ở LANE_DB; (3) `route-verdicts.ts` APPEND `AuthController#stepUp` verdict SELF (route không có @RequirePermission, chủ thể ép từ token) + regen `docs/_review/S6-SEC-ROUTEMAP-1-route-census.json` bằng `ROUTE_CENSUS_WRITE=1`; (4) chạy lại `route-http-coverage.e2e-spec.ts` SAU khi int-spec tồn tại: POST /auth/step-up là route ghi + miền auth ⇒ risk>=5, ratchet MAX_UNCOVERED_HIGH_RISK=0 phải vẫn XANH và `coveredCount` không tụt dưới MIN_COVERED_COUNT.","builder":"backend-builder","paths":["apps/api/test/foundation/reauth-reachability.e2e-spec.ts","apps/api/test/foundation/route-verdicts.ts","apps/api/test/integration/step-up-reauth.int-spec.ts","docs/_review/S6-SEC-ROUTEMAP-1-route-census.json"]}]
acceptanceChecks: ["`grep -rn \"reauthContext\" apps/api/src` cho ĐÚNG MỘT phép GÁN và nó nằm ở `apps/api/src/permission/guards/reauth.guard.ts`; ca cuối của `reauth-reachability.e2e-spec.ts` đã LẬT sang `.toBe(true)` và cổng chính `findUnreachableReauthRoutes(...)` vẫn trả `[]` (done_when: reauth-reachability tự nhả).","D1 — TOTP-ONLY qua đường verify THUẦN: tồn tại `POST /api/v1/auth/step-up` (không @Public); `grep -rn \"verifyChallenge\" apps/api/src/auth/step-up` = 0 kết quả; replay-marker của step-up KHÁC `totp-step` (grep chứng minh) ⇒ step-up không ăn claim của luồng login và ngược lại. Cửa sổ khoá theo bộ-5 qua `stepUpKey()`; KHÔNG tồn tại cờ toàn cục nào.","D2 — KHÔNG đốt recovery code: `grep -rn \"userRecoveryCodes\" apps/api/src/auth/step-up` = 0; int-spec đếm hàng `user_recovery_codes` used_at TRƯỚC/SAU một lượt step-up sai mã ⇒ delta = 0.","D3 — registry RỖNG: spec assert `REVEAL_CLASS_PAIRS.length === 0` trên chính const trong src/**, assert DEFAULT của DI token === const đó (identity); `grep -rn \"requiresReauth\" apps/api/src --include=*.ts | grep -v spec` không route sản phẩm nào khai cờ.","CODEC Valkey: stub `ValkeyService.get()` trả STRING, deserialize assert instanceof Date và isReauthValid đúng; ReauthGuard assert reauthContext.reauthValidUntil instanceof Date.","Ca deny CHUỖI LÝ DO: giữ object grant đúng rồi assert 403 chứa `deny-reauth-required` (không chỉ status); kèm ca ALLOW 2xx thật.","Rate-limit bucket RIÊNG theo companyId|userId từ JWT, tách khỏi rl:ip/rl:acct login; STEP_UP_MAX_ATTEMPTS có default và max; isLocked() chạy trước verify, verify 0 lần khi khoá; reset() khi thành công.","Audit CẢ HAI nhánh không cần migration: Success/Failure/Denied, object_type='auth', dual-write user_security_events cùng tx, metadata sạch không chứa credential; migrations head KHÔNG đổi (0546).","Validate input: resourceId uuid, action/resourceType có max và ∈ registry; UUID sai ⇒ 400, audit không tăng, không tạo window.","Cổng 4 lý do: no-reauth-guard ghim ReauthGuard.name, pair-not-mintable đọc cùng registry; mỗi lý do có ca thử-ngược; :id giữ nguyên không nới.","Census/coverage không vỡ: AuthController#stepUp verdict SELF, census regen, route-guard-coverage & route-http-coverage xanh không tụt ngưỡng.","BẤT BIẾN #1: withTenant(companyId từ JWT), khoá Valkey mang envScope, git diff permission.decide.ts/permission.guard.ts rỗng.","DoD §8: contracts build trước, TURBO_FORCE=1 check.sh --lane-db xanh không skip, check.sh --all không rơi 'XANH KHÔNG ĐỦ BẰNG CHỨNG'; backlog.mjs + STATUS.md cập nhật."]
testTasks: ["RED trước — reauth.guard.spec.ts: 4 nhánh (non-reveal không ghi; resourceId null không ghi; store rỗng không ghi; window hợp lệ ghi đúng giá trị + instanceof Date).","RED trước — step-up-window.service.spec.ts: bảng ca đổi từng thành phần bộ-5 ⇒ khoá khác; hết hạn ⇒ null; fallback memory tôn trọng TTL; codec string->Date.","RED trước — step-up.service.spec.ts: TOTP sai không cấp window + audit Failure; bucket khoá ⇒ verify 0 lần; thành công ⇒ reset bucket; cặp ngoài registry ⇒ từ chối; audit không chứa credential.","Hồi quy 2FA — two-factor.service.spec.ts: verifyChallenge giữ nguyên hành vi; step-up không claim cùng marker.","Integration DB cô lập — step-up-reauth.int-spec.ts: ca ALLOW trước, rồi các ca deny (hết hạn, khác user, khác object, cross-tenant, thiếu object grant).","Integration — resourceId không UUID ⇒ 400 không đổi audit/window; đếm recovery_codes delta 0; 2 hàng audit + 2 hàng security_events; audit append-only giữ nguyên.","Foundation — reauth-reachability: lật ca sang true; ca thử-ngược cho no-reauth-guard và pair-not-mintable; giữ ca cũ hasReauthWriter.","Census/contract — route-verdicts có AuthController#stepUp SELF, regen census, route-guard-coverage/route-http-coverage xanh; valkey-key spec phủ stepup:."]
steps: ["1. L0 NỐI TIẾP, CHẶN: viết §6 DECISIONS-09, nối §5→§6, APPEND paths backlog.mjs. Người chốt vùng đỏ DUYỆT ADR trước khi code.","2. L1: stepUpKey() + marker mới, KEY_PREFIXES append stepup:, Zod schema + SecurityEventType + severity trong contracts rồi build ngay; env STEP_UP_TTL_SEC/STEP_UP_MAX_ATTEMPTS có default + max.","3. L2 RED trước: spec đỏ StepUpWindowService + StepUpService rồi hiện thực service + registry rỗng + endpoint.","4. L3 RED trước: spec đỏ ReauthGuard 4 nhánh rồi hiện thực guard + wire permission.module.ts. Không chạm permission.guard.ts/permission.decide.ts.","5. L4 int-spec DB cô lập: lane-db-setup stepup, module test riêng, controller fixture, ca ALLOW trước rồi các ca DENY.","6. L4 cổng: lật ca cũ, thêm 2 lý do mới + ca thử-ngược; append verdict SELF; regen census; chạy lại route-http-coverage.","7. Nghiệm thu: contracts build → TURBO_FORCE=1 check.sh --lane-db → check.sh --all trước PR. FULL gate đầy đủ reviewer. Cập nhật backlog + STATUS."]
```

GAP-ANALYSIS (đo lại 19/08/2026 trên cây hiện tại, không suy đoán — plan cũ đã LỆCH done_when nên viết lại)

- Vì sao reused=false: bản plan trước cho phép method=password|totp và GỌI TwoFactorService.verifyChallenge, viết ADR mới DECISIONS-10, và cổng chỉ có 3 lý do. done_when hiện tại CẤM verifyChallenge, ép TOTP-only, ép registry REVEAL_CLASS_PAIRS RỖNG, ép lý do cổng THỨ TƯ pair-not-mintable, ép spec codec Valkey, ép audit CẢ nhánh sai bằng result_status, ép resourceId uuid — và paths của WO chỉ khai DECISIONS-09 (không có DECISIONS-10). Phần GAP nền vẫn đúng nguyên: 0 writer reauthContext, 0 route sản phẩm khai cờ.

- Hiện trạng đo được: permission.decide.ts:93 needsObjectGrant = objectGrantRequired ?? (isSensitive && requiresReauth); isReauthValid() ở dòng 145; permission.guard.ts:117-125 CHỈ ĐỌC req.reauthContext?.reauthValidUntil và chỉ ở nhánh reveal-class (resourceId = req.params?.id ?? null). grep -rn "reauthContext" apps/api/src ⇒ chỉ 1 khai kiểu + 1 đường đọc + văn xuôi docblock ⇒ vẫn 0 writer. WO này KHÔNG sửa lỗi đang chảy máu (S10-QA-SECPOLICY-GATE-1/#394 đã gỡ cờ khỏi route sản phẩm) — nó BỔ NĂNG LỰC còn thiếu để cờ có nghĩa.

- Vì sao CẤM verifyChallenge (đọc two-factor.service.ts:264+): nhánh TOTP đúng sẽ replayGuard.claim("totp-step", …) (ĐỘT chiếm claim của luồng LOGIN trong cùng time-step 30s) và khi TOTP sai nó rơi xuống UPDATE user_recovery_codes SET used_at = now() ⇒ một lượt step-up có thể ĐỐT recovery code của người dùng. Đường của step-up phải: nạp user_totp, giải mã secret, TotpService.verify, claim replay bằng MARKER RIÊNG (marker mới thêm ở valkey-key.ts). Đặt method mới trong two-factor.service.ts là hợp lệ (tái dùng giải-mã-secret), miễn KHÔNG đổi hành vi verifyChallenge — spec 2FA cũ phải giữ xanh.

- KHÔNG CẦN MIGRATION ⇒ KHÔNG có lane db-migration. Bằng chứng: (a) 'auth' đã nằm trong AUDIT_OBJECT_TYPES (apps/api/src/db/schema/audit.ts) và trong CHECK audit_logs_object_type_chk; (b) result_status varchar(50) đã có ở audit.ts:50 từ mig 0432, CHECK ở SQL, AuditService.record đã nhận entry.resultStatus và tự assertEnum; (c) user_security_events.event_type là text NOT NULL KHÔNG CHECK (mig 0443) — chỉ severity có CHECK, mà SecurityEventWriter lấy severity từ SECURITY_EVENT_SEVERITY (Record exhaustive) ⇒ thêm event_type mới chỉ cần sửa packages/contracts. Head migration hiện tại = 0546_s7calldb1_chat_calls.sql. NẾU lane thực thi đổi hướng lưu cửa sổ ở DB (KHÔNG khuyến nghị, lệch ADR) thì PHẢI mở lane db-migration riêng, nối tiếp, số 0547, RLS + FORCE TRƯỚC mọi backfill, và quay lại xin duyệt vùng đỏ.

- CĂNG THẲNG TRUNG TÂM của D3: registry RỖNG + endpoint bắt buộc (action,resourceType) ∈ registry ⇒ ở PROD không cặp nào mintable, endpoint luôn 400 — ĐÚNG chủ ý, nhưng int-spec không mint được window để đo ca ALLOW. Cách giải: REVEAL_CLASS_PAIRS là const RỖNG trong src/** và là giá trị DEFAULT của một DI token; endpoint inject token, cổng test import const. Int-spec overrideProvider(token).useValue([cặp test]). Bắt buộc kèm một spec ghim default provider === REVEAL_CLASS_PAIRS (identity).

- KHỚP NỐI ẨN: PermissionGuard KHÔNG phải APP_GUARD toàn cục — nó opt-in per controller. ReauthGuard cũng opt-in ⇒ người sau có thể khai requiresReauth mà QUÊN gắn guard và route lại chết im lặng ĐÚNG kiểu KI-065. Vì thế hai lý do mới của cổng (no-reauth-guard, pair-not-mintable) là phần chống-tái-diễn, không phải trang trí.

- paths gốc của WO THIẾU 8 đường (APPEND ở L0 trước khi code): packages/contracts/src/auth.ts · apps/api/src/common/valkey/valkey-key.ts + 2 spec · apps/api/src/config/env.schema.ts · .env.example · apps/api/test/integration/** · apps/api/test/foundation/route-verdicts.ts · docs/_review census. Thiếu ⇒ guard-scope cảnh báo + gate tụt tầng review.

BẤT BIẾN & RÀNG BUỘC ĐỎ
- BẤT BIẾN #1: mọi đọc/ghi DB của step-up qua withTenant(companyId); companyId từ JWT, KHÔNG từ body/param. Khoá Valkey PHẢI mang companyId + envScope.
- BẤT BIẾN #2: audit append-only; ghi CẢ nhánh đúng lẫn nhánh sai. KHÔNG update/xoá.
- BẤT BIẾN #3: mật khẩu/mã TOTP/recovery code/hash không vào audit/log/DTO/thông báo lỗi. Fixture giống-secret phải ghép chuỗi hoặc đọc env.
- CẤM: sửa permission.decide.ts/permission.guard.ts; nới needsObjectGrant; gán reauthContext ở interceptor/vô điều kiện; dùng chung bucket rate-limit login; gắn requiresReauth cho route sản phẩm ở WO này.
- Fail-soft của ValkeyService là dao hai lưỡi: đường ĐỌC mất Valkey ⇒ deny (fail-closed, ĐÚNG). Đường GHI mất Valkey ⇒ TUYỆT ĐỐI không trả 200 kèm reauthValidUntil mà store không giữ. ADR chọn MỘT (fallback memory single-instance hoặc lỗi hạ tầng tường minh) và ghi hệ quả nhiều-instance.

VERIFY (chạy được, không đoán)
- grep -rn "reauthContext" apps/api/src — kỳ vọng ĐÚNG 1 writer ở permission/guards/reauth.guard.ts.
- grep -rn "verifyChallenge|userRecoveryCodes" apps/api/src/auth/step-up — kỳ vọng 0 kết quả.
- pnpm --filter @mediaos/contracts build TRƯỚC pnpm typecheck.
- bash scripts/lane-db-setup.sh stepup && export LANE_DB=mediaos_stepup && pnpm --filter @mediaos/api test — int-spec step-up KHÔNG được SKIP.
- ROUTE_CENSUS_WRITE=1 chạy route-guard-coverage.e2e-spec.ts để regen census; rồi route-http-coverage.e2e-spec.ts.
- TURBO_FORCE=1 khi đo lại. Trước PR vùng đỏ: bash harness/check.sh --all.
- ls apps/api/migrations | tail -1 phải vẫn là 0546_s7calldb1_chat_calls.sql.

GATE & ROUTING
- Vùng ĐỎ / crown-jewel: FULL gate — security-reviewer + database-reviewer + silent-failure-hunter + santa-method + quality-gate; model crown (Opus implement/review), plan qua plan-reviewer trước khi code. Người chốt vùng đỏ DUYỆT ADR (L0) TRƯỚC khi L1..L4 chạy.
- Thứ tự thi công: L0 → L1 → L2 → L3 (có thể song song L2 sau khi chữ ký getValidWindow đóng băng trong ADR) → L4. Paths 5 lane KHÔNG chồng nhau.
- Hot-file & quy ước APPEND: valkey-key.ts · valkey-key-census.spec.ts KEY_PREFIXES · packages/contracts/src/auth.ts · auth.module.ts · permission.module.ts · route-verdicts.ts · env.schema.ts · DECISIONS-09.

OUT-OF-SCOPE
- KHÔNG gắn requiresReauth cho bất kỳ route sản phẩm nào và KHÔNG thêm cặp nào vào REVEAL_CLASS_PAIRS ở WO này.
- KHÔNG làm FE.
- KHÔNG hỗ trợ step-up bằng MẬT KHẨU và KHÔNG thêm SENSITIVE_CAPABILITY_ALLOWLIST mới.
- KHÔNG đổi ngữ nghĩa lớp reveal-secret, KHÔNG đụng S10-FND-VALKEYSCOPE-2, KHÔNG đổi MAX_UNCOVERED_HIGH_RISK/MIN_COVERED_COUNT theo chiều nới.

RỦI RO / BẪY ĐÃ BIẾT
- Ca 'chưa có step-up' của cổng là phép QUÉT VĂN BẢN: writer viết hình dạng lạ sẽ không được thấy ⇒ WO phải CHỦ ĐỘNG lật ca đó.
- Codec: ValkeyService.get() trả string | null. Quên parse ⇒ isReauthValid() nhận chuỗi ISO ⇒ luôn false ⇒ 403 vĩnh viễn, im lặng.
- Ca deny chỉ assert status 403 là bằng chứng RỖNG: deny-object-required trả TRƯỚC mọi kiểm tra reauth, nên phải giữ object grant đúng VÀ assert chuỗi deny-reauth-required.
- Rate-limit dùng chung bucket login ⇒ tự bóp nghẹt int-spec và khoá nhầm đăng nhập. Ngưỡng phải đọc từ env đặt TRƯỚC khi AppModule khởi tạo, và isLocked() PHẢI chạy trước verify.
- Controller fixture của int-spec phải nằm trong module test RIÊNG, KHÔNG đăng ký vào AppModule.
- Window dùng lại nhiều lần trong TTL vs single-use: ADR phải nói rõ; nếu single-use thì phải có ca 'gọi lần 2 trong TTL'.
- Đừng để req.reauthContext được ghi cho route non-reveal.
