```yaml
wo: S10-AUTH-STEPUP-1
zone: red
generated_by: auto-loop
reconciled_at: "b9a6d160"
lanes: [{"id":"L0-ADR-STEPUP","task":"ADR MỚI (DECISIONS-10) chốt TRƯỚC KHI CODE: (a) cơ chế = CỬA SỔ LƯU PHÍA SERVER khoá theo bộ-5 (companyId,userId,action,resourceType,resourceId), TTL ngắn — KHÔNG token bearer client-side, KHÔNG cờ session toàn cục; (b) hợp đồng API `POST /api/v1/auth/step-up` (method password|totp, credential, action, resourceType, resourceId → { reauthValidUntil }); (c) chính sách: user đã bật 2FA thì BẮT BUỘC method=totp; (d) TIÊU CHÍ route được gắn lại `requiresReauth` = phải có đúng segment `:id` VÀ có ReauthGuard trong chuỗi guard — ghi rõ PATCH /settings/security-policy VẪN chưa đủ điều kiện (singleton, DECISIONS-09 §2/§5); (e) cấm tường minh: sửa `needsObjectGrant` trong permission.decide.ts, gán reauthContext ở interceptor/vô điều kiện, dùng chung bucket rate-limit của login. Nối trỏ DECISIONS-09 §5 → ADR mới. APPEND paths còn thiếu vào WO trong backlog.mjs (contracts/valkey/env.schema/permission.module/test/foundation/_review census/.env.example) trước khi lane khác chạy.","builder":"backend-builder","paths":["docs/DECISIONS/DECISIONS-10_Step_Up_Reauthentication.md","docs/DECISIONS/DECISIONS-09_Security_Policy_Reauth_And_Object_Grant.md","harness/backlog.mjs"]},{"id":"L1-BE-STEPUP-CORE","task":"Cơ chế step-up THẬT (crown-jewel): (1) `stepUpKey()` trong common/valkey/valkey-key.ts dựng `stepup:{envScope}:{companyId}:{userId}:{action}:{resourceType}:{resourceId}` + APPEND prefix `stepup:` vào KEY_PREFIXES của valkey-key-census.spec.ts; (2) StepUpWindowService (Valkey-first, fallback memory per-process như ReplayGuardService — KHÔNG fail-open, KHÔNG trả expiry mà store không giữ được); (3) StepUpService: verify mật khẩu qua PasswordService.verify hoặc TOTP qua TwoFactorService.verifyChallenge trong withTenant(companyId), rate-limit qua LoginRateLimiter với bucket RIÊNG (thêm vào RlBucket), sai ⇒ KHÔNG cấp window; (4) audit khi CẤP window: AuditService objectType='auth' (đã có trong AUDIT_OBJECT_TYPES ⇒ không migration), action='step_up_granted', objectId=resourceId, after CHỈ metadata — cấm mật khẩu/mã TOTP/secret; (5) endpoint `POST /auth/step-up` (KHÔNG @Public) + DTO suy từ Zod schema mới ở packages/contracts/src/auth.ts; (6) env `STEP_UP_TTL_SEC` có MẶC ĐỊNH (không required — tránh giết fixture int-spec) + .env.example.","builder":"backend-builder","paths":["apps/api/src/auth/step-up/**","apps/api/src/auth/auth.controller.ts","apps/api/src/auth/auth.dto.ts","apps/api/src/auth/auth.module.ts","apps/api/src/common/valkey/valkey-key.ts","apps/api/src/common/valkey/valkey-key.spec.ts","apps/api/src/common/valkey/valkey-key-census.spec.ts","apps/api/src/config/env.schema.ts","packages/contracts/src/auth.ts",".env.example"]},{"id":"L2-BE-REAUTH-GUARD","task":"ReauthGuard — NƠI DUY NHẤT GHI `req.reauthContext` (crown-jewel): đọc metadata @RequirePermission; KHÔNG phải reveal-class (isSensitive && requiresReauth) ⇒ return true và KHÔNG ghi gì (0 thay đổi hành vi mọi route hiện có); reveal-class ⇒ lấy resourceId = req.params?.id, tra StepUpWindowService theo bộ-5, CHỈ khi store trả về expiry còn hạn mới `req.reauthContext = { reauthValidUntil }`. CẤM tự tính `new Date(Date.now()+ttl)`, cấm ghi khi resourceId null. Export/wire provider qua permission.module.ts (forwardRef AuthModule như mẫu TwoFactorEnforcementGuard) để controller chỉ cần `@UseGuards(ReauthGuard, PermissionGuard)`. TUYỆT ĐỐI KHÔNG sửa permission.guard.ts / permission.decide.ts (DECISIONS-09 §3.2).","builder":"backend-builder","paths":["apps/api/src/permission/guards/reauth.guard.ts","apps/api/src/permission/guards/reauth.guard.spec.ts","apps/api/src/permission/permission.module.ts"]},{"id":"L3-QA-GATE-STEPUP","task":"Cổng + kiểm thử: (1) LẬT ca 'chưa có step-up nào trong src/**' của reauth-reachability.e2e-spec.ts sang `.toBe(true)` + cập nhật docblock; (2) NÂNG CẤP cổng: thêm lý do thứ ba `no-reauth-guard` vào findUnreachableReauthRoutes (route khai requiresReauth mà classGuards+routeGuards không chứa 'ReauthGuard' ⇒ ĐỎ) + ca thử-ngược chứng minh cổng không rỗng; (3) int-spec DB cô lập `test/integration/step-up-reauth.int-spec.ts` dựng controller fixture reveal-class `/test-reveal/:id` + object grant THẬT, chạy qua HTTP thật ở LANE_DB với đủ ca deny-path; (4) thêm verdict SELF cho `AuthController#stepUp` vào route-verdicts.ts + regen census artifact bằng `ROUTE_CENSUS_WRITE=1`, giữ MAX_UNCOVERED_HIGH_RISK = 0 và coveredCount không giảm.","builder":"backend-builder","paths":["apps/api/test/foundation/reauth-reachability.e2e-spec.ts","apps/api/test/foundation/route-verdicts.ts","apps/api/test/integration/step-up-reauth.int-spec.ts","docs/_review/S6-SEC-ROUTEMAP-1-route-census.json"]}]
acceptanceChecks: ["`grep -rn \"reauthContext\" apps/api/src` cho ĐÚNG MỘT nơi GHI (phép gán) và nó nằm ở `apps/api/src/permission/guards/reauth.guard.ts`; `hasReauthWriter(readSrcSources(src))` === true ⇒ ca cuối của reauth-reachability.e2e-spec.ts đã LẬT sang `.toBe(true)` và cổng chính `findUnreachableReauthRoutes(...)` vẫn trả `[]` (done_when #4).","Cửa sổ step-up khoá theo bộ-5 (companyId,userId,action,resourceType,resourceId): chứng minh bằng builder `stepUpKey()` trong `apps/api/src/common/valkey/valkey-key.ts` — khoá mang envScope (`isKeyScoped()` === true, `assertKeysScoped` không ném ở NODE_ENV=test) và `stepup:` có trong KEY_PREFIXES của valkey-key-census.spec.ts. KHÔNG tồn tại cờ toàn cục nào (grep: không có trường kiểu `stepUpUntil`/`reauthUntil` trên JWT claim, session cookie, hay cột users) (done_when #1).","ReauthGuard chỉ ghi giá trị ĐỌC được từ store: unit spec chứng minh 4 nhánh — non-reveal ⇒ `req.reauthContext` undefined; reveal + resourceId null ⇒ không ghi; reveal + không có window ⇒ không ghi; reveal + window còn hạn ⇒ ghi đúng expiry của store. Không dòng nào trong guard tự tính `Date.now() + ttl`.","Deny-path chạy THẬT qua HTTP ở LANE_DB (int-spec, không skip): (a) window hết hạn ⇒ 403 `deny-reauth-required`; (b) window cấp cho user A ⇒ user B gọi vẫn 403 dù B có cùng object grant; (c) window cấp cho đối tượng X ⇒ gọi đối tượng Y 403; (d) window của công ty A ⇒ công ty B 403 (cross-tenant); (e) chỉ có company-level ALLOW (không object grant) + window hợp lệ ⇒ vẫn 403 `deny-object-required` (lớp reveal-secret KHÔNG bị nới) (done_when #2).","Sai credential ⇒ endpoint trả lỗi xác thực và KHÔNG tạo window: lượt gọi route reveal ngay sau đó vẫn 403 `deny-reauth-required`; bộ đếm rate-limit tăng ở BUCKET RIÊNG (không dùng chung `acct`/`ip` của login ⇒ không khoá nhầm đường đăng nhập).","Audit append-only khi CẤP window: đúng 1 hàng `audit_logs` mỗi lần cấp, `object_type='auth'` (đã có sẵn trong AUDIT_OBJECT_TYPES + CHECK ⇒ KHÔNG migration), `object_id` = resourceId, before/after CHỈ metadata (action/resourceType/resourceId/method/reauthValidUntil); assert KHÔNG chứa mật khẩu, mã TOTP, recovery code, hash (BẤT BIẾN #3). Bất biến append-only của audit_logs không đổi (done_when #3).","BẤT BIẾN #1: mọi truy cập DB của step-up đi qua `withTenant(companyId, …)`; companyId lấy từ JWT (req.user), KHÔNG từ body/param — có ca test truyền companyId giả trong body không đổi được kết quả.","Cổng chống tái diễn được NÂNG CẤP: `findUnreachableReauthRoutes` có lý do thứ ba `no-reauth-guard` (route khai `requiresReauth` mà chuỗi guard không chứa `ReauthGuard` ⇒ ĐỎ) kèm ca thử-ngược chứng minh cổng không rỗng; điều kiện `:id` giữ nguyên, KHÔNG nới thành `startsWith(':')`.","Route mới không phá cổng census: `AuthController#stepUp` có verdict SELF trong `test/foundation/route-verdicts.ts`, artifact `docs/_review/S6-SEC-ROUTEMAP-1-route-census.json` được regen bằng `ROUTE_CENSUS_WRITE=1`, `route-guard-coverage` + `route-http-coverage` XANH với `MAX_UNCOVERED_HIGH_RISK` vẫn = 0 và coveredCount không giảm.","ADR mới `docs/DECISIONS/DECISIONS-10_Step_Up_Reauthentication.md` chốt tường minh: cơ chế + TTL mặc định (env có default) + tiêu chí route ĐƯỢC gắn lại `requiresReauth` (bắt buộc segment `:id` VÀ có ReauthGuard) + ghi rõ `PATCH /settings/security-policy` VẪN chưa đủ điều kiện tới khi engine có khái niệm 'singleton resource'; DECISIONS-09 §5 có dòng trỏ sang ADR mới (done_when #5).","0 dòng thay đổi trong `apps/api/src/permission/permission.decide.ts` và `apps/api/src/permission/guards/permission.guard.ts` (`git diff --stat` chứng minh) — cam kết DECISIONS-09 §3.2 giữ nguyên; và 0 route sản phẩm nào được gắn `requiresReauth` trong WO này (chỉ fixture test).","DoD §8: `bash harness/check.sh --all` (hoặc `--lane-db`) XANH và KHÔNG rơi vào trạng thái 'XANH KHÔNG ĐỦ BẰNG CHỨNG'; coverage vùng auth/permission ≥80% (đếm NHÁNH, `% Funcs` không tụt); `harness/backlog.mjs` cập nhật status + paths; `docs/STATUS.md` regen bằng gen-status (không sửa tay)."]
testTasks: ["RED trước — `apps/api/src/permission/guards/reauth.guard.spec.ts`: non-reveal ⇒ KHÔNG ghi reauthContext (chứng minh 0 thay đổi hành vi route hiện có); reveal + resourceId null ⇒ KHÔNG ghi; reveal + store rỗng ⇒ KHÔNG ghi; reveal + window còn hạn ⇒ ghi ĐÚNG giá trị store trả về (assert bằng cùng tham chiếu Date, không phải 'một Date nào đó').","`apps/api/src/auth/step-up/step-up-window.service.spec.ts`: đổi BẤT KỲ thành phần nào của bộ-5 ⇒ khoá khác ⇒ không tra ra window (bảng ca: userId khác, companyId khác, action khác, resourceType khác, resourceId khác); window hết hạn ⇒ null; fallback memory vẫn tôn trọng TTL; khoá luôn qua `stepUpKey()` (không literal tại chỗ).","`apps/api/src/auth/step-up/step-up.service.spec.ts`: mật khẩu sai / TOTP sai ⇒ KHÔNG gọi window.grant + KHÔNG ghi audit; user đã bật 2FA + method='password' ⇒ từ chối; quá ngưỡng ⇒ khoá theo bucket riêng; payload audit không chứa credential/mã/hash (assert trên đối tượng ghi audit). Fixture giống-secret phải GHÉP CHUỖI hoặc lấy từ env (gitleaks).","Integration DB cô lập — `apps/api/test/integration/step-up-reauth.int-spec.ts` (LANE_DB, HTTP thật, controller fixture reveal-class `/test-reveal/:id` + object_permissions thật): ca ALLOW (grant + step-up ⇒ 2xx) PHẢI có, kèm 5 ca deny-path: hết hạn ⇒ deny-reauth-required · user A→user B ⇒ 403 · object X→object Y ⇒ 403 · cross-tenant ⇒ 403 · chỉ company-ALLOW ⇒ deny-object-required. Thiếu ca ALLOW thì mọi ca DENY là xanh rỗng (bài học deny-cases-vacuous-without-allow-case).","Integration — cùng file: hàng audit của lần cấp window (object_type='auth', object_id=resourceId, metadata sạch), và ca chứng minh audit_logs vẫn append-only (UPDATE/DELETE bị chặn) không bị nới.","Foundation — `apps/api/test/foundation/reauth-reachability.e2e-spec.ts`: lật ca 'chưa có step-up' sang `true`; thêm ca thử-ngược cho lý do `no-reauth-guard` (route giả có `:id` + có step-up nhưng thiếu ReauthGuard ⇒ vẫn bị BẮT; đủ cả ba điều kiện ⇒ nhả); giữ nguyên các ca thử-ngược cũ của `hasReauthWriter` (gán null/undefined/comment KHÔNG tính là writer).","Census/contract — `route-verdicts.ts` có `AuthController#stepUp` (SELF) + regen `docs/_review/S6-SEC-ROUTEMAP-1-route-census.json`; chạy `route-guard-coverage` và `route-http-coverage` xác nhận MAX_UNCOVERED_HIGH_RISK vẫn 0; `valkey-key.spec.ts` + `valkey-key-census.spec.ts` phủ `stepup:` (khoá mang envScope, không literal dựng tại chỗ)."]
steps: ["1. L0 TRƯỚC (nối tiếp, chặn mọi lane khác): viết DECISIONS-10 chốt cơ chế + hợp đồng API + tiêu chí eligibility route; nối DECISIONS-09 §5 sang ADR mới; APPEND vào `paths` của WO trong harness/backlog.mjs các đường còn thiếu (packages/contracts, common/valkey, config/env.schema.ts, permission/permission.module.ts, test/integration, test/foundation/route-verdicts.ts, docs/_review census, .env.example) — thiếu bước này thì guard-scope cảnh báo và gate/scheduler đọc sai tầng review (bài học wo-paths-drive-gate-and-scheduler). Người chốt vùng đỏ duyệt ADR trước khi code.","2. L1 hạ tầng khoá + hợp đồng: thêm `stepUpKey()` + bucket rate-limit mới vào valkey-key.ts, APPEND `stepup:` vào KEY_PREFIXES của census, thêm zod schema `stepUpRequestSchema`/`stepUpResponseSchema` vào packages/contracts/src/auth.ts rồi `pnpm --filter @mediaos/contracts build` (dist cũ ⇒ typecheck đỏ giả), thêm env `STEP_UP_TTL_SEC` có mặc định.","3. L1 lõi (RED trước): viết spec đơn vị cho StepUpWindowService (bộ-5 quyết định khoá, hết hạn ⇒ null, không có window ⇒ null, fallback memory vẫn tôn trọng TTL) và StepUpService (mật khẩu sai ⇒ không cấp; 2FA bật ⇒ method=password bị từ chối; rate-limit đếm; audit payload không chứa credential) — chạy ĐỎ, rồi mới hiện thực service + endpoint `POST /auth/step-up`.","4. L2 (RED trước): spec ReauthGuard — route non-reveal ⇒ `req.reauthContext` vẫn undefined; reveal + không có window ⇒ KHÔNG ghi; reveal + resourceId null ⇒ KHÔNG ghi; reveal + window còn hạn ⇒ ghi ĐÚNG giá trị đọc từ store (không phải giá trị tự tính). Rồi hiện thực guard + wire vào permission.module.ts. KHÔNG chạm permission.guard.ts/permission.decide.ts.","5. L3 int-spec DB cô lập: `bash scripts/lane-db-setup.sh stepup` → `export LANE_DB=mediaos_stepup`; dựng controller fixture reveal-class + object_permissions THẬT; viết ca deny-path ĐỎ trước (hết hạn / user B / object Y / cross-tenant / chỉ company-ALLOW) rồi mới cho xanh.","6. L3 cổng: lật ca 'chưa có step-up' sang true, thêm lý do `no-reauth-guard` + ca thử-ngược, thêm verdict SELF cho AuthController#stepUp, regen `docs/_review/S6-SEC-ROUTEMAP-1-route-census.json` bằng `ROUTE_CENSUS_WRITE=1`.","7. Nghiệm thu: `bash harness/check.sh --lane-db` (lint/typecheck/test, int-spec KHÔNG bị skip) rồi `bash harness/check.sh --all` trước khi mở PR (vùng đỏ). FULL gate: security-reviewer + database-reviewer + silent-failure-hunter + santa-method. Cập nhật backlog.mjs status + regen docs/STATUS.md."]
```

GAP-ANALYSIS (đo 19/08/2026, không suy đoán)
- `apps/api/src/permission/permission.decide.ts:93` `needsObjectGrant = objectGrantRequired ?? (isSensitive && requiresReauth)`; `isReauthValid()` (dòng 145) đọc `ctx.reauthValidUntil`. `permission.guard.ts:117-125` CHỈ ĐỌC `req.reauthContext?.reauthValidUntil`. Không dòng nào trong `apps/api/src` GÁN ⇒ mọi route khai cờ chết 403 vĩnh viễn. Hiện có 0 route sản phẩm khai `requiresReauth` (S10-QA-SECPOLICY-GATE-1 / PR #394 đã gỡ) ⇒ WO này KHÔNG sửa lỗi đang chảy máu, nó BỔ NĂNG LỰC còn thiếu để cờ có nghĩa.
- Hạ tầng TÁI DÙNG ĐƯỢC, không viết mới: `PasswordService.verify()` (auth/password.service.ts:41) · `TwoFactorService.verifyChallenge(userId, companyId, code)` (two-factor.service.ts:264, đã có rate-limit + replay-guard) · `LoginRateLimiter` (isLocked/recordFailure/reset) · `ValkeyService` + `common/valkey/valkey-key.ts` (mọi khoá PHẢI mang envScope) · `ReplayGuardService` làm KHUÔN cho mẫu Valkey-first/memory-fallback.
- KHÔNG CẦN MIGRATION ⇒ KHÔNG có lane db-migration: `'auth'` đã nằm trong `AUDIT_OBJECT_TYPES` (`apps/api/src/db/schema/audit.ts:102`) và trong CHECK `audit_logs_object_type_chk` ⇒ audit cấp window dùng lại type này. Head migration hiện tại = `0546_s7calldb1_chat_calls.sql`. NẾU lane thực thi đổi hướng sang lưu cửa sổ ở DB (không khuyến nghị) thì PHẢI mở lane `db-migration` riêng, nối tiếp, số 0547, RLS + FORCE TRƯỚC mọi backfill — và quay lại xin duyệt vùng đỏ, vì đó là lệch ADR.
- KHỚP NỐI ẨN quan trọng nhất: `PermissionGuard` KHÔNG phải APP_GUARD toàn cục (app.module.ts:114-116 chỉ có JwtAuthGuard/CompanyGuard/TwoFactorEnforcementGuard) — nó là opt-in per controller. Vậy `ReauthGuard` cũng opt-in ⇒ người sau có thể khai `requiresReauth` mà QUÊN gắn ReauthGuard và route lại chết im lặng ĐÚNG kiểu KI-065. Vì thế cổng `reauth-reachability` PHẢI có lý do thứ ba `no-reauth-guard` (RouteInfo đã sẵn `classGuards`/`routeGuards`) — đây là phần chống-tái-diễn, không phải trang trí.
- WO `paths` gốc THIẾU (phải APPEND ở L0 trước khi code): `packages/contracts/src/auth.ts` (DTO = Zod nguồn sự thật, auth.dto.ts import từ @mediaos/contracts) · `apps/api/src/common/valkey/**` (thêm họ khoá + bucket rate-limit) · `apps/api/src/config/env.schema.ts` + `.env.example` (TTL) · `apps/api/src/permission/permission.module.ts` (wire guard) · `apps/api/test/integration/**` · `apps/api/test/foundation/route-verdicts.ts` · `docs/_review/S6-SEC-ROUTEMAP-1-route-census.json`. Bài học wo-paths-drive-gate-and-scheduler: paths thiếu ⇒ guard-scope cảnh báo + gate tụt tầng.

BẤT BIẾN & RÀNG BUỘC ĐỎ
- BẤT BIẾN #1: mọi đọc/ghi DB của step-up qua `withTenant(companyId)`; companyId từ JWT, không từ body. Cửa sổ Valkey PHẢI có companyId trong khoá (nếu không, clone dev-online/PROD dùng chung Valkey sẽ mở cửa sổ chéo môi trường/công ty — KI-067).
- BẤT BIẾN #2: audit append-only; ghi khi CẤP window (cấp quyền tạm thời là hành động nhạy cảm). KHÔNG update/xoá.
- BẤT BIẾN #3: credential (mật khẩu, mã TOTP, recovery code) không vào audit/log/DTO/thông báo lỗi. Fixture giống-secret trong spec phải ghép chuỗi/đọc env (gitleaks `generic-api-key`).
- CẤM (WO notes + DECISIONS-09 §2/§3.2): gán `reauthContext` ở interceptor cho đủ điều kiện (cửa sau giả); nới `needsObjectGrant`; sửa `permission.decide.ts`/`permission.guard.ts`; gắn lại `requiresReauth` cho route singleton không có `:id`.
- Fail-soft của ValkeyService là con dao hai lưỡi: đường ĐỌC window mất Valkey ⇒ không có window ⇒ deny (fail-closed, ĐÚNG). Đường GHI mất Valkey ⇒ TUYỆT ĐỐI không trả 200 kèm `reauthValidUntil` mà store không giữ (người dùng thấy 'đã xác thực lại' rồi vẫn 403 — đúng lớp lỗi ui-promises-backend-never-reads). Hoặc fallback memory (single-instance, như ReplayGuard) hoặc trả lỗi hạ tầng tường minh; ADR phải chọn MỘT và ghi hệ quả nhiều-instance.

VERIFY (chạy được, không đoán)
- `grep -rn "reauthContext" apps/api/src` — kỳ vọng đúng 1 writer trong reauth.guard.ts.
- `bash scripts/lane-db-setup.sh stepup && export LANE_DB=mediaos_stepup && pnpm --filter @mediaos/api test` — int-spec step-up KHÔNG được SKIP (thiếu LANE_DB ⇒ deny-path là xanh rỗng).
- `pnpm --filter @mediaos/contracts build` TRƯỚC `pnpm typecheck` (dist cũ ⇒ đỏ giả).
- `ROUTE_CENSUS_WRITE=1 pnpm --filter @mediaos/api exec vitest run test/foundation/route-guard-coverage.e2e-spec.ts` để regen census sau khi thêm route.
- `TURBO_FORCE=1` khi đo lại (turbo cache trả log cũ = xanh giả). Trước PR: `bash harness/check.sh --all`.

GATE & ROUTING
- Vùng ĐỎ / crown-jewel (auth + permission + audit + secret): FULL gate — security-reviewer + database-reviewer + silent-failure-hunter + santa-method + quality-gate; model crown (Opus implement/review), plan qua plan-reviewer trước khi code. Người chốt vùng đỏ duyệt ADR (L0) TRƯỚC khi L1/L2 chạy.
- Thứ tự thi công: L0 (nối tiếp, chặn) → L1 và L2 có thể song song SAU KHI hợp đồng service đóng băng trong ADR (L2 chỉ phụ thuộc chữ ký `getValidWindow(...): Promise<Date | null>`), nhưng an toàn hơn là L1 → L2 → L3. L3 luôn cuối (cần cả writer lẫn route mới tồn tại).
- Hot-file & quy ước APPEND (không rewrite): `valkey-key.ts` (thêm builder + bucket, KHÔNG đụng miễn trừ legacy) · `valkey-key-census.spec.ts` KEY_PREFIXES (append `stepup:`) · `auth.module.ts` providers/exports (khối additive) · `packages/contracts/src/auth.ts` (append schema) · `route-verdicts.ts` (append 1 mục SELF) · `env.schema.ts` (biến MỚI phải có DEFAULT — siết sàn env từng giết fixture int-spec).

OUT-OF-SCOPE (chống scope creep — ghi để lần sau không tưởng là thiếu sót)
- KHÔNG gắn `requiresReauth` cho bất kỳ route sản phẩm nào trong WO này (ADR chỉ định TIÊU CHÍ; việc gắn là WO sau, kèm đường cấp object grant thật).
- KHÔNG làm FE (modal nhập lại mật khẩu/TOTP khi gặp `deny-reauth-required`) — chưa có route sản phẩm bật cờ nên chưa có màn nào cần; seed WO FE riêng khi gắn cờ thật.
- KHÔNG thêm `SENSITIVE_CAPABILITY_ALLOWLIST` / `user_security_events` event type mới (đụng packages/contracts + có thể migration) — chỉ cần khi có route sản phẩm bật cờ.
- KHÔNG đổi ngữ nghĩa lớp reveal-secret, không đụng `S10-FND-VALKEYSCOPE-2` (gỡ khoá legacy).

RỦI RO / BẪY ĐÃ BIẾT
- Ca 'chưa có step-up' của cổng là phép QUÉT VĂN BẢN: writer viết theo hình dạng lạ (`req["reauth"+"Context"] =`) sẽ không được thấy ⇒ WO phải CHỦ ĐỘNG lật ca đó, đừng chờ nó tự đỏ (docblock spec đã cảnh báo).
- Window dùng lại nhiều lần trong TTL vs single-use: ADR phải nói rõ; nếu chọn single-use thì phải có ca test 'gọi lần 2 trong TTL' để hành vi không tuỳ hứng.
- Rate-limit dùng chung bucket login ⇒ tự bóp nghẹt int-spec và khoá nhầm đăng nhập (bài học per-user-rate-limit-throttles-own-int-spec + login-lockout-keyed-by-email): dùng bucket riêng, ngưỡng đọc từ env đặt TRƯỚC khi AppModule khởi tạo.
- Đừng để `req.reauthContext` được ghi cho route non-reveal: PermissionGuard chỉ đọc nó ở nhánh reveal, nhưng một writer bừa bãi tạo tiền lệ nguy hiểm cho người sau.
