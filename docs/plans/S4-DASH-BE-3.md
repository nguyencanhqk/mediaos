# Plan S4-DASH-BE-3 — BE Dashboard widget CONFIG CRUD (GET /dashboard/configs · PATCH /configs/:id) theo company/role/user/dashboard-type + audit append-only

> Zone **yellow → escalate FULL gate** (CLAUDE.md §6): diff chạm permission (@RequirePermission mới) + audit append-only (audit.record in-tx) + migration (GRANT UPDATE + CHECK UNION-ADD) + RLS-adjacent (withTenant trên bảng đã FORCE RLS) ⇒ security-reviewer + database-reviewer + silent-failure-hunter (KHÔNG LIGHT).
> Hand-authored bởi lane **FIX-DASH-BE-3-DOC-CLOSE** (vòng sửa 2026-07-11) để int-spec header + RECONCILE-REFRESH resolve đúng — plan trước đó THIẾU (Đội 3 flag NON-BLOCK). Đồng bộ số migration **0491** (renumber từ 0490 sau va chạm NOTI-SEED-2).

```yaml
wo: S4-DASH-BE-3
zone: yellow
gate: FULL
generated_by: doc-close
reconciled_at: "6e32f16"
head_migration: "0491_s4_dashbe3_config_update_grant_audit_type"
lanes: [{"id":"L0-migration-grant-audit","task":"Migration 0491 (RENUMBER tu 0490 sau va cham NOTI-SEED-2 #157 e8c5f83 mint 0490_s4_notiseed2 idx 170): (1) GRANT UPDATE ON dashboard_widget_configs TO mediaos_app (KHONG DELETE, KHONG worker) — app chi co SELECT 0482 + INSERT 0484, thieu UPDATE => PATCH vo tang DB 'permission denied for table'; (2) DO-block UNION-ADD-only dua 'dashboard_widget_config' vao audit_logs_object_type_chk (clone superset moi nhat 0474, doc def tu pg_constraint roi union them, idempotent, KHONG rewrite/drop, KHONG dung GRANT/RLS/FORCE audit_logs); sync AUDIT_OBJECT_TYPES trong apps/api/src/db/schema/audit.ts CUNG commit. Journal idx 171 when 1717500850000 noi tiep head THAT 0490_s4_notiseed2 (idx 170 when 1717500845000). KHONG seed permission (cap view/update:dashboard-config da seed 0484).","builder":"db-migration","paths":["apps/api/migrations/**","apps/api/src/db/schema/audit.ts"]},{"id":"L1-contracts","task":"Zod DTO dual-build (ESM/CJS) — packages/contracts/src/dashboard-config.ts: dashboardConfigItemSchema, dashboardConfigListQuerySchema (dashboard_type/config_scope/role_id/user_id, boolean+enum query param idempotent z.preprocess), dashboardConfigPatchSchema (.refine chan body rong => 400, data_scope_override enum), dashboardConfigListResponseSchema. Append export './dashboard-config' vao index.ts; build @mediaos/contracts de api & test resolve type (tranh typecheck do-gia do dist cu).","builder":"backend-builder","paths":["packages/contracts/src/**"]},{"id":"L2-config-crud-impl","task":"DashboardConfigService (list withTenant(companyId) precedence-ordered company->role->user + patch withTenant tx: 404-on-missing/soft-deleted, audit.record in-tx object_type='dashboard_widget_config' action_group='CONFIG_UPDATE' permission_code='DASH.CONFIG.UPDATE' before!=after) -> DashboardConfigController (@Controller('dashboard') @UseGuards(PermissionGuard) MUC CLASS, GET /configs @RequirePermission VIEW + PATCH /configs/:id @RequirePermission UPDATE, cap lay tu DASH_PERMISSION_PAIRS KHONG go string roi) -> dashboard-config.dto.ts + dashboard-config.errors.ts (DASH-ERR-NOT_FOUND) -> dashboard.module.ts APPEND controller+service. Route /configs KHONG trung 3 controller dashboard hien co (Dashboard/DashboardResolver).","builder":"backend-builder","paths":["apps/api/src/dashboard/**"]},{"id":"L3-int-tests","task":"Int-spec RED-first apps/api/test/integration/dashboard-config-crud.int-spec.ts (gate hasDb && LANE_DB — chi chay DB co lap mediaos_dashbe3 da ap mig 0491): deny-path 403 (thieu view/update:dashboard-config), cross-tenant 404 (RLS an row KHONG lo ton tai), config-khong-escalate (bat is_enabled KHONG mo widget trai quyen — registry tier-2 authoritative), audit-on-change in-tx (rollback nghiep vu => audit rollback), append-only (PATCH=UPDATE giu row cu, app role KHONG DELETE), validation (body rong/enum sai/uuid), precedence effect. 18/18 GREEN tren chain 0490_notiseed2->0491.","builder":"backend-builder","paths":["apps/api/test/integration/**"]}]
acceptanceChecks: ["GET /api/v1/dashboard/configs tra danh sach config widget (id, widget_id, widget_code/name, dashboard_type, config_scope, role_id/user_id, is_enabled, sort_order, layout, data_scope_override) SAU gate @RequirePermission view:dashboard-config (DASH.CONFIG.VIEW, isSensitive) qua PermissionGuard class-level; filter theo company/role/user/dashboard-type; company-admin 200, non-admin 403 (SPEC-07 §17.3 DASH-API-201, BACKEND-10 §9.3, API-10:310).","PATCH /api/v1/dashboard/configs/{id} cap nhat sort_order/is_enabled/layout(size)/data_scope_override/refresh_seconds_override/config SAU gate update:dashboard-config (DASH.CONFIG.UPDATE, isSensitive); withTenant(companyId) RLS+FORCE; id ngoai tenant/soft-deleted => 404 DASH-ERR-NOT_FOUND; body rong hoac override sai enum => 400 (DASH-API-203, BAT BIEN #1).","Moi query list/patch di qua db.withTenant(companyId) (RLS+FORCE, company_id ep tang DB) + filter deleted_at IS NULL; admin company A KHONG doc/sua duoc config company B (404, khong ro ton tai); precedence company->role->user giu nguyen o registry (User>Role>Company, DB-07 §8.2).","Config PATCH KHONG noi quyen xem widget: bat is_enabled/dat data_scope_override cho mot widget KHONG cho user thieu quyen module-nguon thay widget do qua /dashboard/{type} — read-time gating (DashboardWidgetRegistryService tier-2 DASH_WIDGET_GATE_PAIR) la authoritative (permission-matrix-spec §7).","Audit ghi khi doi config: PATCH insert audit_logs object_type='dashboard_widget_config' action_group='CONFIG_UPDATE' permission_code='DASH.CONFIG.UPDATE', before/after (da mask), actor_user_id, TRONG cung withTenant tx (append-only, BAT BIEN #2, BACKEND-10 §11/§44); object_type co trong audit_logs_object_type_chk + AUDIT_OBJECT_TYPES (mig 0491).","Migration 0491 noi tiep head THAT 0490_s4_notiseed2 (idx 171): GRANT UPDATE dashboard_widget_configs cho mediaos_app (KHONG DELETE, KHONG cap worker), DO-block UNION-ADD 'dashboard_widget_config' vao CHECK (idempotent, khong rewrite/drop, khong dung GRANT/RLS audit_logs); AUDIT_OBJECT_TYPES sync cung commit; KHONG seed permission moi (cap da seed 0484).","DoD §8: DTO contracts dual-build, permission guard, FE-agnostic, co int-spec deny-path RED-truoc; FULL gate PASS (security-reviewer + silent-failure-hunter + database-reviewer) — diff cham permission+audit+migration+RLS => escalate FULL (CLAUDE.md §6, KHONG LIGHT); check.sh xanh; harness/backlog.mjs cap nhat (status done)."]
testTasks: ["RED permission deny (deny-path TRUOC): user THIEU cap view:dashboard-config goi GET /dashboard/configs => 403; user THIEU update:dashboard-config goi PATCH /configs/:id => 403; company-admin co cap => 200 — chung gate PermissionGuard class-level chan TRUOC xu ly (khong guard = moi user lot).","RED cross-tenant: admin company A PATCH id config thuoc company B => 404 DASH-ERR-NOT_FOUND (RLS an row, KHONG 403 lo ton tai); GET /configs cua A KHONG tra row company B (seed config 2 company).","RED config-khong-escalate: admin PATCH is_enabled=true cho config PENDING_LEAVE (gate tier-2 = view:leave) dashboard Employee => employee THIEU view:leave goi GET /dashboard/employee VAN KHONG thay widget (registry tier-2 gate), nhung hr CO view:leave THAY — chung config CRUD khong mo widget trai quyen.","RED audit-on-change: PATCH thanh cong ghi 1 row audit_logs object_type='dashboard_widget_config' action_group='CONFIG_UPDATE' permission_code='DASH.CONFIG.UPDATE', before!=after, actor_user_id dung, cung tx (rollback nghiep vu => audit cung rollback).","RED append-only: PATCH la UPDATE (row cu giu, updated_at doi); app role KHONG DELETE dashboard_widget_configs (thu DELETE truc tiep bang app role => bi chan quyen) — BAT BIEN #2.","RED validation: PATCH body rong => 400 (.refine it nhat 1 field); data_scope_override='Xxx' ngoai enum => 400; id khong phai uuid => 400/404 dung semantics.","RED precedence effect: PATCH is_enabled=false tren config Company cua mot widget => /dashboard/{type} qua resolver bo widget do khoi danh sach (User>Role>Company giu nguyen).","Gate hasDb && LANE_DB: int-spec dat tai apps/api/test/integration/dashboard-config-crud.int-spec.ts (khop include test/**/*.int-spec.ts — chay that), skip khi thieu LANE_DB; chay DB co lap mediaos_dashbe3 da ap mig 0491 — neu chay DB chung thieu band 0490_notiseed2/0491 = do-gia."]
steps: ["L0 TRUOC (noi tiep migration): viet mig 0491 (RENUMBER tu 0490 sau va cham NOTI-SEED-2 #157) — GRANT UPDATE dashboard_widget_configs cho mediaos_app + DO-block UNION-ADD 'dashboard_widget_config' vao audit_logs_object_type_chk clone superset 0474 + sync AUDIT_OBJECT_TYPES trong apps/api/src/db/schema/audit.ts CUNG commit. Journal idx 171 when > 0490_s4_notiseed2. Ap len DB co lap mediaos_dashbe3 (drizzle migrator don dieu theo when).","L1 (nen DTO): tao packages/contracts/src/dashboard-config.ts (item/listQuery/patch/listResponse schema, boolean param idempotent z.preprocess) + append export './dashboard-config' vao index.ts; build @mediaos/contracts dual ESM/CJS.","L3 RED-first: viet int-spec deny-path/cross-tenant/escalation/audit/append-only/validation ASSERT truoc; chay DB co lap (bash scripts/lane-db-setup.sh dashbe3 --reset -> export LANE_DB=mediaos_dashbe3 -> pnpm --filter @mediaos/api exec vitest run dashboard-config-crud) -> xac nhan.","L2 GREEN: DashboardConfigService (list withTenant precedence-ordered + patch withTenant tx audit-in-tx 404-on-missing) -> DashboardConfigController (GET /configs + PATCH /configs/:id, PermissionGuard class-level, cap view/update:dashboard-config tu const) -> dashboard-config.dto.ts + dashboard-config.errors.ts -> dashboard.module.ts APPEND. Kiem KHONG route trung 3 controller dashboard. Chay lai L3 -> GREEN 18/18.","Vong gate FULL (cham permission + audit append-only + migration GRANT + RLS): security-reviewer + silent-failure-hunter + database-reviewer doi chieu gate-truoc-xu-ly, audit-in-tx, 404-cross-tenant, config-khong-escalate, UPDATE-khong-DELETE. check.sh xanh (TURBO_FORCE=1); coverage >=80% lop config nhay cam.","Cap nhat harness/backlog.mjs (S4-DASH-BE-3 status done, done_when LIGHT->FULL, paths + migrations/audit.ts) + hand-author khoi yaml may-doc vao docs/plans/S4-DASH-BE-3.md de RECONCILE-REFRESH lan sau."]
```

## GAP-ANALYSIS (vòng sửa 2026-07-11 — fix theo Đội 3)

Migration collision đã xử: `feat/s4-wave-collab` fork tại `23e3edb` (#156, head 0489). Sau đó master merge NOTI-SEED-2 (#157, `e8c5f83`) mint `0490_s4_notiseed2_task_be3_event_catalog` (journal idx 170, when 1717500845000). Lane này ban đầu mint 0490 Y HỆT idx/when ⇒ **collision**: trên DB đã áp NOTI-0490, drizzle migrator coi idx-170 ĐÃ áp ⇒ BỎ QUA im lặng DASH-0490 ⇒ (a) GRANT UPDATE không chạy → PATCH 500; (b) 'dashboard_widget_config' không vào CHECK → audit INSERT vỡ 23514 → toàn PATCH tx rollback (phá BẤT BIẾN #2).

**Fix (L0-RENUM `f364087`):** rebase wave lên master, RENUMBER 0490→**0491** (nối tiếp head THẬT `0490_s4_notiseed2`), journal idx 170→171 + when 1717500850000, đổi tên file + comment band; AUDIT_OBJECT_TYPES giữ nguyên (append-only). L3-REVERIFY (`6e32f16`) chain-migrate lại + int-spec **18/18** trên DB đã có `0490_s4_notiseed2` (chứng không skip).

## Bất biến / hot-file đã xử lý

- **#1 company_id/RLS+FORCE:** `dashboard_widget_configs` đã RLS+FORCE + policy literal-GUC từ 0482 — KHÔNG đụng; mọi list/patch qua `db.withTenant(companyId)`.
- **#2 audit append-only:** GRANT UPDATE **không** kèm DELETE; **không** cấp worker. CHECK `audit_logs_object_type_chk` UNION-ADD-only (DO-block clone superset, idempotent) — không rewrite/drop; GRANT/RLS/FORCE của `audit_logs` không đụng.
- **Hot-file:** `dashboard.module.ts` khối additive (append controller+service); `schema/audit.ts` `AUDIT_OBJECT_TYPES` UNION-ADD; cặp permission KHÔNG seed lại (đã 0484).

## Gate

FULL (CLAUDE.md §6) — security-reviewer + database-reviewer + silent-failure-hunter. Lens: gate-trước-xử-lý (PermissionGuard class-level), audit-in-tx, 404-cross-tenant (không lộ tồn tại), config-không-escalate (registry tier-2 authoritative), UPDATE-không-DELETE. Verify: `check.sh` + int-spec 18/18 trên `mediaos_dashbe3` (mig 0491).
