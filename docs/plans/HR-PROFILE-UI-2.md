```yaml
wo: HR-PROFILE-UI-2
zone: yellow
generated_by: auto-loop
reconciled_at: "9839e0a"
lanes: [{"id":"MIG-EXPORT-SENSITIVE","task":"Migration NỐI TIẾP (band kế 0490): flip permissions.is_sensitive false→true cho cặp ('export','employee') — additive idempotent UPDATE (WHERE action='export' AND resource_type='employee'), KHÔNG DDL/RLS/FORCE, KHÔNG đụng grant 0444. Mục tiêu: export danh bạ PII = cặp NHẠY CẢM fail-closed (parity export:attendance/leave); wildcard *:* KHÔNG thoả. KHÔNG có consumer live (endpoint mới) ⇒ không cửa sổ 403; hr/company-admin giữ grant EXACT Company nên vẫn export được. CROWN: đổi độ nhạy cặp permission → red-zone, người duyệt trước merge.","builder":"db-migration","paths":["apps/api/migrations/**"]},{"id":"CONTRACT-HR-EXPORT-SORT","task":"packages/contracts/src/hr: (1) MỞ RỘNG HR_EMPLOYEE_SORT_FIELDS thêm cột mới directory-class ('startDate','officialDate') + cập nhật hrEmployeeListQuerySchema.sort enum; (2) THÊM contract export dùng chung server↔FE: hrEmployeeExportQuerySchema (filter-only parity list: search/orgUnitId/positionId/status + sort/order tuỳ chọn, KHÔNG page/pageSize), HR_EMPLOYEE_EXPORT_MAX_ROWS (10_000 mirror attendance), HR_EMPLOYEE_EXPORT_COLUMNS (mảng có thứ tự {key:keyof HrEmployeeListItem, header:VN, pii?:boolean} — CHỈ directory+PII, KHÔNG baseSalary/salaryType). Export qua hr/index.ts barrel. KHÔNG thêm field ngoài spec.","builder":"backend-builder","paths":["packages/contracts/src/hr/**"]},{"id":"BE-HR-EXPORT","task":"apps/api/src/employees: (1) hr-read.repository.ts — mở rộng SORT_COLUMNS allowlist KHỚP CHÍNH XÁC contract (startDate→employeeProfiles.startDate, officialDate→employeeProfiles.officialDate) [chống ORDER BY injection]; thêm listScopedForExportTx(tx,companyId,scopeCond,filters,limit) (cap+1, KHÔNG phân trang, sort xác định). (2) hr-export.csv.ts (thuần, mirror attendance-export.csv.ts): UTF-8 BOM+CRLF, neutralize formula-injection + RFC-4180 quoting, cột từ HR_EMPLOYEE_EXPORT_COLUMNS. (3) hr-export.service.ts: resolveAndAssert('export','employee',{isSensitive:true}) làm GATE 403 → resolveContext → buildEmployeeScopeCondition (SCOPE=filter, Own→chỉ hàng mình) → withTenant → listScopedForExportTx(MAX+1) → >MAX ⇒ 422 (KHÔNG cắt im lặng) → per-row canViewSensitive(row.id) để BLANK cột PII (mask NHƯ list) → serialize → audit append-only trong tx (action 'EmployeesExported', objectType 'employee' [đã có trong CHECK], actor+count+scope). (4) hr-read.controller.ts (hoặc hr-export.controller.ts mới): @Get('employees/export') KHAI TRƯỚC ':id', @Res library-mode + Header text/csv + Content-Disposition, @RequirePermission('export','employee',{isSensitive:true}). (5) employees.module.ts: đăng ký provider/controller. (6) permission.service.ts: APPEND 'export:employee' vào SENSITIVE_CAPABILITY_ALLOWLIST (mở CỜ HIỂN THỊ FE, KHÔNG nới enforcement). Gate REVIEW=FULL.","builder":"backend-builder","paths":["apps/api/src/employees/**","apps/api/src/permission/permission.service.ts"]},{"id":"FE-HR-GROUP-EXPORT-SORT","task":"FE Hồ sơ nhân sự: (A) GROUPING — packages/ui/src/components/ui/data-table.tsx: thêm TanStack getGroupedRowModel+getExpandedRowModel + render group-header row (collapse/expand, aggregation count) + props grouping/onGroupingChange (KHÔNG lib bảng mới); (B) SERVER-SORT — thêm affordance header click (props sorting/onSortingChange manual-mode) phát ra sort-key+direction; (C) route apps/app/src/routes/hr/employees: column-settings-popover.tsx thêm điều khiển 'Gom nhóm' 1–2 cấp (theo đơn vị/trạng thái), use-employee-list-filters.ts thêm state sort/order→queryParams, employee-table-columns.tsx đánh dấu cột groupable + enableSorting cho cột allowlist, EmployeeListPage.tsx nối grouping+sort headers vào DataTable, wire nút Export (đã có, giữ PermissionGate→đổi useCanExact('export','employee') sau khi cặp thành sensitive) → gọi client blob → triggerBlobDownload + loading/error inline (KHÔNG tải file lỗi); (D) client blob — packages/web-core/src/lib/hr-api.ts thêm exportEmployees(query) qua apiFetchBlob + export apiFetchBlob/ApiBlobResult qua barrel index.ts.","builder":"frontend-builder","paths":["packages/ui/src/**","apps/app/src/routes/hr/employees/**","packages/web-core/src/lib/hr-api.ts","packages/web-core/src/index.ts"]}]
acceptanceChecks: ["GET /hr/employees/export tồn tại, KHAI TRƯỚC route ':id' (Express không nuốt 'export' thành :id); @RequirePermission('export','employee',{isSensitive:true}) — caller KHÔNG có grant → 403 (deny-path đi RED trước rồi GREEN).","Response @Res library-mode: Content-Type text/csv; charset=utf-8 + Content-Disposition attachment (bỏ qua ResponseEnvelopeInterceptor); body = CSV bytes.","Data-scope áp NHƯ list: grant Company → mọi hàng trong tenant; grant Own (planted) → CHỈ hàng của caller (0 hàng người khác) = 'export ngoài scope không có row'. withTenant(companyId) + scopeCond mang company_id (BẤT BIẾN #1).","Masking per-row: cột PII (gender/dateOfBirth/phone/contractType…) CHỈ vào file khi caller có view-sensitive:employee (per-row canViewSensitive, resourceId=row.id, isSensitive→wildcard không mở); thiếu quyền → ô rỗng. baseSalary/salaryType KHÔNG có trong HR_EMPLOYEE_EXPORT_COLUMNS (quyết định: tránh audit-storm reveal per-row — ghi rõ).","Cap chống lộ im lặng: repo lấy MAX+1; >HR_EMPLOYEE_EXPORT_MAX_ROWS → 422 TRƯỚC serialize, KHÔNG trả file cắt.","Audit append-only trong tenant tx (BẤT BIẾN #2): action 'EmployeesExported', objectType 'employee', actorUserId + count chính xác + dataScope (nhãn Own/Team/Company).","HR_EMPLOYEE_SORT_FIELDS mở rộng (startDate,officialDate); SORT_COLUMNS repo KHỚP 1-1 (allowlist chống ORDER BY injection); FE header click set sort/order → server sắp; sort ngoài enum → Zod 400.","Panel Tùy chỉnh cột: gom nhóm 1–2 cấp theo đơn vị/trạng thái bằng TanStack getGroupedRowModel (KHÔNG lib bảng mới); group-header thu/gọn được; grouping áp trên hàng đã tải (server-pagination giữ nguyên).","CSV cứng hoá: UTF-8 BOM + CRLF + neutralize formula-injection (=,+,-,@,TAB,CR) + RFC-4180 quoting (mirror attendance-export.csv.ts).","'export:employee' được thêm SENSITIVE_CAPABILITY_ALLOWLIST + FE dùng useCanExact ⇒ nút Export hiện đúng cho hr/company-admin, ẩn với wildcard-only (khớp BE fail-closed, hết FE-permit/BE-403).","Contracts = nguồn sự thật DUY NHẤT (export columns/query/max-rows ở packages/contracts, server+FE lockstep); pnpm typecheck+lint+build+test xanh; int-spec chạy dưới LANE_DB; coverage ≥80%."]
testTasks: ["RED deny-path (int, LANE_DB, hr-export.int.spec.ts): caller role employee/manager (KHÔNG export:employee) GET /hr/employees/export → 403; đảm bảo test ĐỎ trước khi có handler.","RED scope (int): caller có grant export:employee scope OWN (planted) → file CHỈ chứa hàng của chính caller, 0 hàng người khác ('export ngoài scope không có row'); cross-tenant → 0 hàng.","Int happy: hr (Company) → 200 text/csv, header cột = HR_EMPLOYEE_EXPORT_COLUMNS, số dòng = tổng scope; assert Content-Disposition.","Int masking: caller CÓ view-sensitive → ô PII có giá trị; caller KHÔNG → ô PII rỗng (per-row) — chứng minh mask ở server, không phải FE.","Int cap: seed > MAX rows (hoặc hạ MAX test) → 422, KHÔNG trả CSV cắt.","Int audit: sau export, 1 hàng audit_logs action 'EmployeesExported' với count đúng + scope; app role KHÔNG UPDATE/DELETE được (append-only).","Int sort: sort=startDate&order=desc → thứ tự đúng; sort ngoài HR_EMPLOYEE_SORT_FIELDS → 400 (Zod enum) — chống injection.","Unit hr-export.csv.spec.ts: BOM+CRLF, neutralize '=SUM()'/'+' /'-'/'@', quoting dấu phẩy/xuống dòng; null→rỗng.","Unit hr-export.service.spec.ts: gate isSensitive:true fail-closed vs wildcard; blank PII khi thiếu view-sensitive; 422 khi over-cap.","FE data-table.spec.tsx: grouping getGroupedRowModel render group-header 1–2 cấp + expand/collapse; manual sort header phát onSortingChange.","FE EmployeeListPage.spec.tsx: nút Export gate useCanExact (ẩn khi thiếu cap); click → gọi exportEmployees → triggerBlobDownload; lỗi 403/422 → alert inline, KHÔNG tải file.","QA đối chiếu SPEC-03/API-10 màn Hồ sơ nhân sự: cập nhật TESTABLE-FEATURES nếu pass."]
steps: ["THỨ TỰ: MIG-EXPORT-SENSITIVE (nối tiếp, band 0490) → CONTRACT-HR-EXPORT-SORT → BE-HR-EXPORT (phụ thuộc contract + MIG) → FE-HR-GROUP-EXPORT-SORT (phụ thuộc contract types + endpoint BE; nhánh grouping/sort-header của packages/ui làm song song được).","1) MIG: UPDATE permissions SET is_sensitive=true cho ('export','employee') — idempotent, additive, band 0490 nối sau head 0489. Người duyệt red-zone (đổi độ nhạy cặp) trước merge. Áp trên DB cô lập LANE_DB khi verify.","2) CONTRACT: mở rộng HR_EMPLOYEE_SORT_FIELDS (+startDate,+officialDate) + hrEmployeeExportQuerySchema + HR_EMPLOYEE_EXPORT_MAX_ROWS + HR_EMPLOYEE_EXPORT_COLUMNS; build contracts (dual ESM/CJS) trước để BE/FE nhận type.","3) BE: RED deny-path trước (export thiếu export:employee → 403; export ngoài scope → 0 hàng lạ) → GREEN service/repo/controller/csv → allowlist append → audit append-only in-tx → gate FULL (security+database+silent-failure).","4) FE: mở rộng DataTable (grouping + server-sort) ở packages/ui → điều khiển gom nhóm + sort header + wire export blob ở route → useCanExact cho nút Export → xử lý loading/error/empty.","5) Verify: typecheck+lint+build xanh; int-spec chạy DƯỚI LANE_DB (skipIf(!hasDb&&LANE_DB)); coverage ≥80% (module nhạy cảm); cập nhật harness/backlog.mjs done_when."]
```

GAP-ANALYSIS (code hiện tại 2026-07-11):
- Cặp ('export','employee') ĐÃ tồn tại + grant hr/company-admin @Company (mig 0444), NHƯNG is_sensitive=FALSE — LỆCH với export:attendance/leave (is_sensitive=TRUE, fail-closed). done_when 'export = lộ dữ liệu, FULL gate' ⇒ ý định coi export danh bạ PII là NHẠY CẢM ⇒ CHỌN full-parity với precedent S3-ATT-EXPORT-1: flip is_sensitive→true (mig 0490) + allowlist + useCanExact + gate isSensitive:true.
- Endpoint GET /hr/employees/export CHƯA có (chỉ nút FE placeholder trong EmployeeListPage.tsx:161-165, PermissionGate export:employee, KHÔNG onClick).
- HR_EMPLOYEE_SORT_FIELDS hiện = [fullName,employeeCode,status,createdAt] (employee-read.ts:43) + SORT_COLUMNS repo (hr-read.repository.ts:185) — CHƯA có startDate/officialDate dù cột đã tồn tại ở list item (mig 0489). Mở rộng phải KHỚP contract↔repo 1-1 (allowlist chống injection).
- DataTable (packages/ui) mới có core/filtered/pagination + columnVisibility — CHƯA có getGroupedRowModel/getSortedRowModel ⇒ grouping+server-sort header phải thêm ở packages/ui (in-scope).
- Masking đã có sẵn: HrReadService.canViewSensitive(per-row, isSensitive:true) + revealSalary(audit atomic). Export TÁI DÙNG canViewSensitive per-row để blank PII. Salary CỐ Ý loại khỏi cột export (tránh audit-storm reveal per-row trong bulk; done_when chỉ yêu cầu PII).

PATH-EXPANSION (cần owner chốt — vượt paths khai báo của WO):
- apps/api/migrations/** (mig flip is_sensitive) — WO paths KHÔNG liệt kê. Bắt buộc nếu theo full-parity.
- apps/api/src/permission/permission.service.ts (append SENSITIVE_CAPABILITY_ALLOWLIST) — ngoài apps/api/src/employees/**.
- packages/web-core/src/lib/hr-api.ts + index.ts (client blob exportEmployees; apiFetchBlob CHƯA export khỏi barrel) — WO paths KHÔNG có web-core (bài học ready-wave3: 'WO paths must list web-core barrel').
FALLBACK nếu owner muốn ở TRONG paths: GIỮ is_sensitive=false, bỏ mig+allowlist, BE gate vẫn {isSensitive:true} (caller-forced fail-closed, permission.service.ts:225 effectivelySensitive=input.isSensitive OR grant.isSensitive — verify Read), FE giữ PermissionGate; hệ quả: super-admin (*:* wildcard) thấy nút nhưng 403 khi bấm (cosmetic, đã error-inline). Đánh đổi: yếu hơn parity + lệch is_sensitive với attendance/leave.

INVARIANTS:
- #1 company_id: mọi read qua withTenant(companyId); scopeCond mang eq(company_id) (belt-and-suspenders RLS). Export KHÔNG nhận company_id từ client.
- #2 append-only: audit 'EmployeesExported' INSERT trong tenant tx; app role không UPDATE/DELETE audit_logs. Migration THUẦN additive (UPDATE 1 cột is_sensitive), KHÔNG blanket, idempotent.
- #3 no secret/PII leak: masking per-row ở SERVER (canViewSensitive), FE chỉ render gì nhận được; CSV chỉ chứa cột HR_EMPLOYEE_EXPORT_COLUMNS (KHÔNG identity_*/salary).
- §3 phụ thuộc: Permission engine + audit đã sẵn (không vi phạm thứ tự). Migration RLS/FORCE KHÔNG đụng (chỉ đổi metadata is_sensitive) ⇒ không cần backfill.

VERIFY: chạy int trên DB cô lập (bash scripts/lane-db-setup.sh + export LANE_DB=...) vì migrator áp đơn điệu theo head; deny-path phải ĐỎ trước. Đối chiếu head mig hiện tại = 0489 (HR-PROFILE-UI-1b) → mig kế 0490.

GATE: BE-HR-EXPORT + MIG = FULL gate (security-reviewer + database-reviewer + silent-failure-hunter; +santa-method vì đổi độ nhạy permission). FE lane = LIGHT (react-reviewer + typescript-reviewer + quality-gate). Người duyệt red-zone chốt mig flip is_sensitive TRƯỚC merge.

OUT-OF-SCOPE (chống scope-creep): KHÔNG thêm export:employee cho manager/employee (least-privilege, giữ Company hr/company-admin theo 0444); KHÔNG đưa baseSalary/salaryType/identity_* vào file; KHÔNG server-side grouping (grouping FE trên hàng đã tải); KHÔNG dùng lib bảng mới; KHÔNG đổi luồng list/detail/summary hiện có; HR-IDENTITY-READ-1 (lộ CCCD) và HR-PERF-1 (batch permission) là WO riêng.
</content>
