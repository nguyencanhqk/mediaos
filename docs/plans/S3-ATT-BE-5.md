```yaml
wo: S3-ATT-BE-5
zone: red
generated_by: auto-loop
reconciled_at: "8427004"
lanes: [{"id":"S3-ATT-BE-5-MIG","task":"db-migration (SERIAL, nối head 0456→0457, crown/RLS+audit): audit object_type union-add 'remote_work_request' vào audit_logs_object_type_chk theo DO-block UNION-ADD-only idempotent (clone 0440/0451) — KHÔNG rewrite CHECK cứng, KHÔNG cấp UPDATE/DELETE (BẤT BIẾN #2). Xác nhận shape remote_work_requests + remote_work_request_approvals đã đủ ở 0452 (RLS ENABLE+FORCE + policy tenant_isolation + GRANT SELECT,INSERT,UPDATE app / SELECT worker); chỉ ALTER-ADD nếu skeleton thiếu cột workflow. KHÔNG db:generate (DDL thủ công RLS/CHECK).","builder":"db-migration","paths":["apps/api/migrations/0457_s3_attbe5_remote_request_audit_object_type.sql","apps/api/src/db/schema/attendance.ts"]},{"id":"S3-ATT-BE-5-CONTRACTS","task":"backend-builder (contracts, Zod = nguồn sự thật DTO): thêm schema CreateRemoteWorkRequest (requestType Remote|BusinessTrip|Offsite, startDate/endDate, startTime/endTime, attendanceMode SELF_CHECK_IN|AUTO_ATTENDANCE|NO_ATTENDANCE, locationText, reason bắt buộc, taskId/projectId optional), RemoteWorkRequestDto (masking-aware), ListRemoteWorkRequestsQuery (status/scope/paging), ApproveRemoteRequest/RejectRemoteRequest (rejectReason bắt buộc khi reject) — bám mã SPEC MODULE-API-XXX (CO-S4-004 / API-10). Không chồng path với apps/api.","builder":"backend-builder","paths":["packages/contracts/src/attendance.ts"]},{"id":"S3-ATT-BE-5-SVC","task":"backend-builder (CROWN — FSM phê duyệt): FILE MỚI (attendance.service.ts đã 790 dòng/max 800 → KHÔNG nhồi thêm) remote-work-request.service.ts + .repository.ts + -read.service.ts + .mappers.ts. Routes append vào attendance.controller.ts: POST /attendance/remote-work-requests (create Own) · GET /attendance/remote-work-requests/my · GET /attendance/remote-work-requests (scoped list) · GET :id · POST :id/approve · POST :id/reject (+ cancel-own nếu seed sẵn). State-machine Draft/Pending→Approved|Rejected|Cancelled (chỉ Pending mới approve/reject; row-lock FOR UPDATE chống double-approve như approveAdjustment). Mọi mutation trong withTenant(tx) + audit.record(objectType:'remote_work_request') + outbox.enqueue + ghi remote_work_request_approvals (APPEND-ONLY, INSERT-only) CÙNG tx. Approved → ảnh hưởng tính công: mark/generate attendance_records.remote_work_request_id + work_mode(Remote/BusinessTrip) + attendance_status(Remote Work/Auto Attendance) theo attendanceMode (AUTO_ATTENDANCE sinh record, SELF_CHECK_IN cho phép remote check-in, NO_ATTENDANCE bỏ). Gate qua PermissionService + DataScopeService.resolveAndAssert (view-own/view-team/view-company · approve/reject) + assertWriteScope chống IDOR. Wire providers vào attendance.module.ts (khối additive).","builder":"backend-builder","paths":["apps/api/src/attendance/remote-work-request.service.ts","apps/api/src/attendance/remote-work-request.repository.ts","apps/api/src/attendance/remote-work-request-read.service.ts","apps/api/src/attendance/remote-work-request.mappers.ts","apps/api/src/attendance/attendance.controller.ts","apps/api/src/attendance/attendance.module.ts","apps/api/src/attendance/remote-work-request.int.spec.ts","apps/api/src/attendance/remote-work-request.service.spec.ts"]}]
acceptanceChecks: ["POST /attendance/remote-work-requests tạo được đơn với scope Own; tạo hộ người khác (employeeId≠caller) → 403 (assertWriteScope).","GET /attendance/remote-work-requests/my trả đơn của chính caller; GET (list) lọc theo scope view-own/view-team/view-company qua DataScopeService.resolveAndAssert (view-team max=Team); GET :id ngoài scope → 404 không rò dữ liệu.","POST :id/approve chỉ đổi Pending→Approved (approve:remote-request, manager=Team/hr=Company); reject bắt rejectReason, Pending→Rejected; approve/reject khi status≠Pending → lỗi (double-approve chặn bằng row-lock FOR UPDATE).","Mỗi create/approve/reject/cancel ghi audit_logs objectType='remote_work_request' + 1 dòng remote_work_request_approvals (INSERT-only) + outbox event, TẤT CẢ trong CÙNG withTenant(tx); rollback nguyên khối khi lỗi.","Migration 0457 union-add 'remote_work_request' vào audit_logs_object_type_chk (idempotent, không rewrite CHECK, không cấp UPDATE/DELETE); audit INSERT không vỡ CHECK trên DB thật.","Approved ảnh hưởng tính công: attendance_records ngày trong range mang remote_work_request_id + work_mode(Remote/BusinessTrip) + attendance_status đúng theo attendanceMode (AUTO_ATTENDANCE sinh record / SELF_CHECK_IN cho remote check-in / NO_ATTENDANCE không sinh record).","Cross-tenant: đơn/approval của company khác không truy cập/duyệt được (RLS+FORCE + company_id ở mọi query); test chứng minh deny.","File service mới ≤800 dòng; contracts build xanh; typecheck/lint xanh; coverage ≥80% (module nhạy cảm); DoD §8: audit trên hành động quan trọng + FE-N/A (BE-only WO)."]
testTasks: ["RED deny-path (crown, viết TRƯỚC): tạo đơn hộ người khác (employeeId khác caller) → 403.","RED deny-path: user không có approve:remote-request hoặc ngoài data-scope (Team) duyệt đơn → 403.","RED deny-path: cross-tenant — company B approve/GET đơn company A → 403/404 (RLS chặn).","FSM negative: approve/reject một đơn đã Approved/Rejected/Cancelled → lỗi trạng thái; double-approve đồng thời → chỉ 1 thành công (row-lock).","Integration (LANE_DB cô lập): full flow create→list(scope)→detail→approve → assert status Approved + 1 approval ledger row + audit_logs objectType='remote_work_request' + outbox event + attendance_records mang remote_work_request_id/work_mode đúng theo attendanceMode.","Integration: reject không rejectReason → 400; reject có reason → status Rejected + audit/approval ghi.","Migration smoke (hasDb && LANE_DB): áp 0457, INSERT audit objectType='remote_work_request' KHÔNG vỡ CHECK; idempotent chạy lại không lỗi.","QA-* regression: các route attendance cũ (check-in/out, adjustments, records) không hồi quy sau khi append routes/module wiring."]
steps: ["MIG trước (nối tiếp): tạo 0457 union-add audit object_type 'remote_work_request' (DO-block idempotent clone 0440/0451); xác nhận 0452 đã đủ shape+RLS+FORCE+GRANT, chỉ ALTER-ADD nếu thiếu cột; áp migration trên DB lane cô lập.","CONTRACTS song song: thêm Zod DTO create/list/detail/approve/reject vào packages/contracts/src/attendance.ts; build dual ESM/CJS xanh.","SVC (crown, sau MIG+CONTRACTS): planner Sonnet5 xhigh lập micro-plan → plan-reviewer đối kháng; RED deny-path test trước (tạo hộ / duyệt ngoài scope / cross-tenant); IMPLEMENT Opus: repository (insert + findByIdForUpdate + list scoped + approval ledger insert) → service FSM + audit/outbox/approval trong tx + calc-affect → read.service scope → mappers → controller routes → module wiring.","GREEN: chạy unit + integration trên LANE_DB cô lập (bash scripts/lane-db-setup.sh + export LANE_DB); FULL gate (security-reviewer + database-reviewer + silent-failure-hunter + santa-method) + người chốt red-zone trước merge.","Cập nhật harness/backlog.mjs (done) + để STATUS tự sinh."]
```

NEW decomposition (không có docs/plans/S3-ATT-BE-5.md trước đó). TRẠNG THÁI SKELETON: migration 0452 (S3-ATT-DB-1) đã tạo ĐỦ shape remote_work_requests (id, company_id default GUC, request_code, employee_id→employee_profiles, request_type Remote|BusinessTrip|Offsite, start/end date+time, attendance_mode SELF_CHECK_IN|AUTO_ATTENDANCE|NO_ATTENDANCE, location_text, reason, task_id/project_id UUID trần, status Draft|Pending|Approved|Rejected|Cancelled, current_approver_user/employee_id, approved/rejected/cancelled_by+at, reject_reason, attachment_file_id, metadata, audit cols) + remote_work_request_approvals (APPEND-ONLY ledger, GRANT SELECT,INSERT) + RLS ENABLE+FORCE + policy tenant_isolation + attendance_records.remote_work_request_id FK + work_mode/attendance_status enum ('Remote Work','Auto Attendance','Remote','BusinessTrip','Auto'). Schema Drizzle (apps/api/src/db/schema/attendance.ts) đã export remoteWorkRequests + remoteWorkRequestApprovals + types → KHÔNG cần đổi shape. GAP THẬT: (1) audit object_type 'remote_work_request' CHƯA có trong audit_logs_object_type_chk (0452 chỉ tạo bảng, không union-add audit CHECK; 0451 là lần cuối union-add cho profile_change_request) → CẦN migration 0457 union-add (clone DO-block 0440/0451). (2) TOÀN BỘ tầng service/repo/controller cho remote-work workflow CHƯA tồn tại (attendance.service.ts/controller.ts chỉ có check-in/out, schedules, adjustments, periods). PERMISSIONS đã seed sẵn ở 0454: pair (action,resourceType) resource_type='remote-request' với create-own/view-own/view-team(max Team)/view-company/approve/reject/cancel-own → BE phải gate đúng CẶP engine (action,resource_type)=('approve','remote-request') v.v., KHÔNG bịa pair mới (bẫy pair-drift đã gặp ở S1-FND & S3-FE). INVARIANTS: #1 mọi query qua withTenant(companyId) + company_id (RLS+FORCE đã bật); #2 remote_work_request_approvals + audit_logs APPEND-ONLY (INSERT-only, app role không UPDATE/DELETE); #2 audit CHECK chỉ MỞ RỘNG union, không rewrite. PATTERN TÁI DÙNG: FSM approve/reject bám approveAdjustment/rejectAdjustment trong attendance.service.ts (findByIdForUpdateTx row-lock chống double-approve, audit+outbox+task-close CÙNG tx); scope list bám AttendanceReadService.listScoped (DataScopeService.resolveAndAssert GATE + buildEmployeeScopeCondition FILTER, resolveContext không cache); write-scope chống IDOR bám assertWriteScope (S2-HR). RÀNG BUỘC FILE: attendance.service.ts đã 790/800 dòng → BẮT BUỘC tách file mới remote-work-request.*.ts (nhiều file nhỏ, tránh BLOCK >800). CALC-AFFECT (done_when 'Approved ảnh hưởng cách tính công'): trên approve, cho mỗi ngày trong [start_date,end_date] set attendance_records.remote_work_request_id + work_mode + attendance_status theo attendance_mode (AUTO_ATTENDANCE→sinh/mark Auto Attendance; SELF_CHECK_IN→cho phép remote check-in đánh Remote Work; NO_ATTENDANCE→không sinh record) — xác nhận rule cụ thể theo DB-04 §7.8 / SPEC-04 khi implement. MIGRATION head hiện tại=0456 → migration kế 0457; KHÔNG cần backfill company_id (bảng đã RLS+FORCE ở 0452, không có row cũ). HOT-FILE & MERGE: attendance.controller.ts (append routes cuối class, không rewrite), attendance.module.ts (append providers khối additive), audit CHECK (DO-block UNION ADD-only idempotent), packages/contracts/src/attendance.ts (append schemas). VERIFY: LANE_DB cô lập bắt buộc (drizzle áp migration đơn điệu theo when; DB chung skip band thấp → xanh/đỏ giả); spec test PHẢI colocate src/**/*.spec.ts (test/ không chạy = green giả). GATE: FULL (crown workflow phê duyệt + permission + audit + migration) → security-reviewer + database-reviewer + silent-failure-hunter + santa-method + người chốt red-zone trước merge; PLAN stage Sonnet5 xhigh, IMPLEMENT/REVIEW Opus. OUT-OF-SCOPE: FE screen (WO khác); PAYROLL/KPI ảnh hưởng; đa-approver multi-step (schema có step_order nhưng MVP 1-step Team/Company theo seed); không đụng attendance_adjustment_requests cũ (0061) — remote là bảng riêng.
</content>
