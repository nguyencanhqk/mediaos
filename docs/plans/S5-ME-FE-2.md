```yaml
wo: S5-ME-FE-2
zone: yellow
generated_by: auto-loop
reconciled_at: "d5fc9e14"
lanes: [{"id":"fe2core","task":"web-core plumbing cho ME-SCREEN-002..008 (KHÔNG chồng lane app): (1) me-api.ts APPEND getSecurityActivity(query?: {page,per_page,from_date,to_date}) → GET /me/security/activity, parse z.array(meSecurityActivityItemSchema) — apiFetch/unwrapEnvelope CHỈ trả field data (block pagination sibling bị bỏ, mirror myNotificationApi/LoginLogsPage) ⇒ dùng full-page heuristic cho has-next. Query CHỈ page/per_page/from_date/to_date — TUYỆT ĐỐI KHÔNG user_id/employee_id (chống IDOR §14.4; owner 100% từ token, BE đã khoá). (2) query-keys.ts APPEND meKeys.securityActivity(query) (namespace rootKeys.me). (3) registry.ts ROUTE_REGISTRY APPEND 6 RouteMeta gate literal ['access:me'] (KHÔNG qua PERMISSION_CODE_TO_PAIR, mirror me.overview): me.profile (/me/profile, ME-SCREEN-002), me.profile.change-requests (/me/profile/change-requests, ME-SCREEN-003/004), me.account (/me/account, ME-SCREEN-005), me.security.password (/me/security/password, ME-SCREEN-006), me.security.sessions (/me/security/sessions, ME-SCREEN-007), me.security.activity (/me/security/activity, ME-SCREEN-008) — order tiếp 72+. (4) i18n/locales/vi/nav.ts APPEND routeTitle.meProfile/meProfileChangeRequests/meAccount/meSecurityPassword/meSecuritySessions/meSecurityActivity. (5) index.ts giữ export meApi (đã export). + spec me-api getSecurityActivity + registry-guard 6 route.","builder":"frontend-builder","paths":["packages/web-core/src/**"]},{"id":"fe2pages","task":"apps/app pages + routing + sidebar + i18n (depends_on fe2core): (A) TÁI DÙNG import-only (KHÔNG copy-paste, KHÔNG sửa component nguồn) mount dưới ME workspace qua makeModuleRoute(...,'ME',...): /me/profile→MyProfilePage (routes/hr/me), /me/profile/change-requests→MyChangeRequestPage (routes/hr/profile-change-requests), /me/account→AccountProfilePage, /me/security/password→ChangePasswordPage, /me/security/sessions→AccountSessionsPage (routes/account). makeModuleRoute bọc ModuleWorkspaceLayout('ME') ⇒ 5 màn tái dùng nay có sidebar ME (account pages trước dùng buildShellRouteContent không có workspace). Route cũ /hr/me + /hr/me/change-request + /account/* GIỮ NGUYÊN hoạt động (KHÔNG gãy bookmark/deep-link — done_when). (B) Màn MỚI MeSecurityActivityPage.tsx (ME-SCREEN-008 Hoạt động bảo mật): bảng READ-ONLY thời gian/loại sự kiện/thiết bị/IP đã mask + phân trang (page/per_page) từ meApi.getSecurityActivity; trạng thái §13 loading/error(retry)/empty/forbidden. BẤT BIẾN #3 + §17.1: CHỈ render field server trả (ipMasked/device đã mask server-side) — FE KHÔNG unmask/tái dựng raw IP/UA; KHÔNG hiển thị metadata/payload/email. (C) router.tsx: 6 route mới (mirror meOverviewRoute/makeModuleRoute) + addChildren; lazy MyProfilePage/AccountProfilePage/ChangePasswordPage/AccountSessionsPage đã có sẵn — chỉ THÊM route trỏ tới. (D) sidebar-registry.ts ME_SIDEBAR APPEND 2 nhóm §8.1: 'Hồ sơ của tôi' (Hồ sơ của tôi→/me/profile, Yêu cầu cập nhật hồ sơ→/me/profile/change-requests) + 'Tài khoản & bảo mật' (Tài khoản→/me/account, Đổi mật khẩu→/me/security/password, Phiên đăng nhập→/me/security/sessions, Hoạt động bảo mật→/me/security/activity), gate ['access:me'], group=nhãn tiếng Việt (mirror ME_SIDEBAR FE-3). (E) unlinked-employee (§12.2): MyProfilePage đã xử lý 404→notLinked message — verify giữ nguyên khi mount /me/profile. (F) i18n me.ts APPEND securityActivity.* (title/columns/states/pagination). (G) tùy chọn cohesion: routes/me/constants.ts ME_QUICK_ACTION_PATHS.EDIT_PROFILE→/me/profile, CHANGE_PASSWORD→/me/security/password (nay route đã tồn tại). + spec mỗi màn (loading/error/empty; security-activity read-only không lộ nhạy cảm + IDOR-safe).","builder":"frontend-builder","paths":["apps/app/src/routes/me/**","apps/app/src/router.tsx","apps/app/src/i18n/**","apps/app/src/layouts/workspace/sidebar-registry.ts","apps/app/src/routes/hr/**","apps/app/src/routes/account/**"]}]
acceptanceChecks: ["/me/profile (ME-SCREEN-002) TÁI DÙNG MyProfilePage (import-only, KHÔNG copy-paste) mount trong ME workspace (sidebar ME hiện); /me/profile/change-requests (ME-SCREEN-003/004) TÁI DÙNG MyChangeRequestPage — cả 2 render qua makeModuleRoute('ME') gate ROUTE_REGISTRY access:me.","/me/account (ME-SCREEN-005) TÁI DÙNG AccountProfilePage (gồm card 2FA); /me/security/password (ME-SCREEN-006) TÁI DÙNG ChangePasswordPage; /me/security/sessions (ME-SCREEN-007) TÁI DÙNG AccountSessionsPage — không dựng lại logic, import component sẵn có.","/me/security/activity (ME-SCREEN-008) là màn MỚI đọc GET /me/security/activity (BE-3, PR #220): bảng read-only thời gian · loại sự kiện · thiết bị · IP đã mask + phân trang (page/per_page); CHỈ render field server trả (ipMasked/device đã mask), FE KHÔNG unmask/tái dựng raw IP/UA, KHÔNG hiển thị metadata/payload/email (BẤT BIẾN #3 · §17.1).","meApi.getSecurityActivity gọi GET /me/security/activity với query CHỈ page/per_page/from_date/to_date — TUYỆT ĐỐI KHÔNG user_id/employee_id trong URL/body (chống IDOR §14.4); parse z.array(meSecurityActivityItemSchema); has-next dùng full-page heuristic (apiFetch bỏ block pagination — giới hạn đã biết, ghi rõ).","Mọi màn /me/* xử lý đủ trạng thái §13: loading · error(retry) · empty · forbidden; unlinked-employee ở /me/profile hiển thị thông điệp §12.2 + ẩn/disable phần hồ sơ công việc (MyProfilePage notLinked 404 giữ nguyên hành vi); hợp đồng CHỈ hiện nếu BE trả (ME-DEC-006, masking do server).","ROUTE_REGISTRY có 6 route mới (me.profile/me.profile.change-requests/me.account/me.security.password/me.security.sessions/me.security.activity) gate literal ['access:me']; ME_SIDEBAR có 2 nhóm 'Hồ sơ của tôi' + 'Tài khoản & bảo mật' (§8.1) trỏ ĐÚNG route vừa tạo (KHÔNG link chết); i18n vi đủ me.securityActivity + routeTitle 6 route.","Route cũ /hr/me + /hr/me/change-request + /account/{profile,change-password,sessions,setup-2fa} GIỮ hoạt động (KHÔNG gãy deep-link/bookmark — done_when line 1); KHÔNG đổi BE/contracts/migration/permission.","REBUILD packages/web-core dist thành công; pnpm typecheck + build (web-core/app) xanh; bash harness/check.sh xanh (TURBO_FORCE=1); LIGHT gate react-reviewer + quality-gate PASS; nhánh security-activity qua security-reviewer (read-only không lộ nhạy cảm); mỗi màn có test loading/error/empty (DoD §8)."]
testTasks: ["me-api.spec.ts APPEND: getSecurityActivity gọi ĐÚNG path /me/security/activity, truyền query page/per_page/from_date/to_date qua buildQueryString; DENY-IDOR: chèn user_id/employee_id vào query → URL KHÔNG chứa (owner-param bị loại; assert regex URL không match user_id=|employee_id=); parse z.array(meSecurityActivityItemSchema), shape sai → ném ngay (không âm thầm render sai).","MeSecurityActivityPage.spec.tsx (MỚI): render loading → bảng data (cột thời gian/sự kiện/thiết bị/IP-mask) → empty → error(retry) → forbidden (§13); assert CHỈ hiển thị ipMasked/device server trả, KHÔNG render/tái dựng raw IP (mock item ipMasked='203.0.*.*' → không có chuỗi full IP nào khác); phân trang next/prev (full-page heuristic) đổi query page.","route-authz-wiring spec (mirror me-routes-authz-wiring.spec.tsx): 6 route mới (/me/profile, /me/profile/change-requests, /me/account, /me/security/password, /me/security/sessions, /me/security/activity) render qua ProtectedRoute gate access:me; thiếu access:me → 403, KHÔNG render nội dung.","Reuse-mount spec: /me/profile mount MyProfilePage (assert notLinked 404 vẫn hiển thị thông điệp §12.2); /me/account mount AccountProfilePage; /me/security/sessions mount AccountSessionsPage — component tái dùng render trong ME workspace (KHÔNG copy-paste, cùng instance lazy component).","registry-guard spec: ROUTE_REGISTRY 6 route me.* mới gate ['access:me'] + showInSidebar; ME_SIDEBAR 2 nhóm 'Hồ sơ của tôi'/'Tài khoản & bảo mật' path khớp route đã đăng ký (KHÔNG dead-link); i18n me.securityActivity + routeTitle.me* 6 khoá tồn tại.","Regression/no-break spec: route cũ /hr/me, /hr/me/change-request, /account/profile, /account/change-password, /account/sessions vẫn resolve (bookmark/deep-link không gãy — done_when line 1).","(KHÔNG cần integration DB / deny-path RED tầng BE ở WO này: FE-only, own-scope + masking + IDOR đã do BE-3 int-spec me-security-activity.int-spec.ts phủ; FE chỉ assert client IDOR-safe + không unmask.)"]
steps: ["Lane fe2core TRƯỚC (nền plumbing web-core): me-api.getSecurityActivity (query chỉ page/per_page/from_date/to_date, z.array parse, KHÔNG owner-param), query-keys.meKeys.securityActivity, registry ROUTE_REGISTRY APPEND 6 RouteMeta gate ['access:me'], nav routeTitle.me* 6 khoá. RED trước cho me-api.spec getSecurityActivity (assert URL không chứa user_id/employee_id) + registry-guard spec.","Lane fe2pages (sau khi fe2core merge — cần meta key + me-api method + query-keys): (1) MeSecurityActivityPage.tsx MỚI (read-only table + pagination + §13 states, chỉ render field mask server trả). (2) router.tsx 6 route qua makeModuleRoute('ME',...) trỏ component TÁI DÙNG (import-only) + addChildren. (3) sidebar-registry ME_SIDEBAR APPEND 2 nhóm. (4) i18n me.ts securityActivity.*. (5) tùy chọn cập nhật ME_QUICK_ACTION_PATHS. RED trước cho MeSecurityActivityPage.spec (loading/error/empty/forbidden + không lộ raw IP) + route-authz-wiring spec (6 route gate access:me).","Verify: pnpm --filter @mediaos/web-core build (REBUILD dist — consumer FE import từ dist; quên = trang trắng, memory web-core-stale-dist) → pnpm --filter @mediaos/app typecheck test build → pnpm --filter @mediaos/web-core typecheck test → bash harness/check.sh (TURBO_FORCE=1 chống false-green). KHÔNG migration (FE-only) ⇒ KHÔNG cần LANE_DB.","Gate: LIGHT (react-reviewer + quality-gate) toàn WO; lane fe2pages chạm màn Hoạt động bảo mật → THÊM security-reviewer soi read-only KHÔNG lộ nhạy cảm (done_when line 4). Cập nhật harness/backlog.mjs done_when + LƯU docs/plans/S5-ME-FE-2.md (khối yaml máy-đọc) cho tái dùng."]
```

GAP-ANALYSIS (đối chiếu CODE 2026-07-17): docs/plans/S5-ME-FE-2.md CHƯA tồn tại → phân rã MỚI (reused=false). Depends_on S5-ME-FE-1 (registry/shell/overview) + S5-ME-BE-3 ĐÃ ship: BE-3 endpoint GET /me/security/activity wired (me.module.ts có MeSecurityActivityController/Service/Repository; commit d86806dd/PR #220), contracts đã đủ (packages/contracts/src/me.ts: meSecurityActivityItemSchema {id,source,eventType,severity|null,device|null,ipMasked|null,createdAt} + meSecurityActivityQuerySchema {page,per_page,from_date,to_date} KHÔNG owner-param). FE THIẾU: (a) web-core me-api.ts CHƯA có getSecurityActivity (chỉ có getOverview/*Summary/getPreferences/patchAppearance); (b) query-keys meKeys CHƯA có securityActivity; (c) ROUTE_REGISTRY mới có me.overview + 6 route FE-3 (order 65-71) — CHƯA có profile/account/security; (d) ME_SIDEBAR mới có overview + Công việc/Thông báo/Cài đặt (FE-3) — CHƯA có 'Hồ sơ của tôi'/'Tài khoản & bảo mật'; (e) màn Hoạt động bảo mật (ME-SCREEN-008) CHƯA có. Component TÁI DÙNG SẴN: MyProfilePage (apps/app/src/routes/hr/me), MyChangeRequestPage (routes/hr/profile-change-requests), AccountProfilePage+ChangePasswordPage+AccountSessionsPage (routes/account) — tất cả self-service own-scope, server là cổng quyền.

QUYẾT ĐỊNH KIẾN TRÚC (builder theo): (1) Reuse = IMPORT-ONLY qua makeModuleRoute('ME',...) — KHÔNG copy-paste, KHÔNG sửa component nguồn. makeModuleRoute bọc ModuleWorkspaceLayout('ME') ⇒ 5 màn tái dùng nay có sidebar ME (account pages trước dùng buildShellRouteContent KHÔNG có workspace — nay vào ME hub). (2) MOUNT-DƯỚI-/me + GIỮ route cũ (KHÔNG redirect): thêm /me/* trỏ CÙNG lazy component; /hr/me + /hr/me/change-request + /account/* giữ nguyên hoạt động (bookmark an toàn — done_when 'chốt trong plan mount vs redirect' → chọn mount, thấp rủi ro nhất; /account/setup-2fa KHÔNG được redirect vì ProtectedShell ép enroll dùng path này). (3) Sub-route §8.2 /me/profile/{personal,work,contracts}: KHÔNG build page riêng — SPEC §9 screen-list coi 'Hồ sơ của tôi' là 1 màn DUY NHẤT ME-SCREEN-002 (MyProfilePage đã render đủ nhóm cá nhân+công việc+hợp đồng trong 1 trang, KHÔNG có endpoint BE riêng từng nhóm). Tách 3 page slice = net-new + copy-paste = vi phạm mandate tái dùng + YAGNI. Sidebar 'Hồ sơ của tôi' = 2 mục (Hồ sơ→/me/profile, Yêu cầu cập nhật→/me/profile/change-requests). Spec §8.2 là 'đề xuất' sub-nav; §9 thắng → ghi rõ điểm chốt. (4) Gate tất cả route mới = literal ['access:me'] (KHÔNG qua PERMISSION_CODE_TO_PAIR — cùng kỹ thuật me.overview/att.shifts, tránh pair-drift); cả 4 role canonical có access:me + read:employee + create:profile-change-request (Own) nên không tạo hố 403; BE mỗi endpoint tự enforce own-scope. (5) Security-activity: apiFetch/unwrapEnvelope CHỈ trả field data (bỏ block pagination sibling — mirror myNotificationApi/LoginLogsPage) ⇒ getSecurityActivity trả MeSecurityActivityItem[], has-next = heuristic items.length===per_page (giới hạn đã biết, KHÔNG hiển thị tổng trang chính xác).

INVARIANTS (CLAUDE.md §2 luôn áp): FE-only — KHÔNG chạm DB/RLS/audit/secret/migration/BE/contracts. IDOR (§14.4/§17.1): client resolve owner 100% từ token — getSecurityActivity query CHỈ page/per_page/from_date/to_date, KHÔNG user_id/employee_id (BE zod strip key lạ; FE cũng không gửi). MASKING là việc SERVER: security-activity render ipMasked/device server trả (đã mask a.b.*.* / 'Chrome trên Windows'), FE KHÔNG unmask/tái dựng raw IP/UA, KHÔNG render metadata/payload/email/failure_reason (BE-3 repo không SELECT). MyProfilePage/AccountProfilePage: masking do server, client chỉ render field nhận được (đã tuân thủ). Hợp đồng CHỈ hiện nếu BE trả (ME-DEC-006).

GATE: LIGHT (react-reviewer + quality-gate) — KHÔNG FULL vì không đụng permission/RLS/secret/audit/migration (route-gate access:me là cặp seed SẴN CÓ mig 0495, KHÔNG thêm). NHƯNG màn Hoạt động bảo mật đọc dữ liệu login_logs/user_security_events (nhạy cảm) → THÊM security-reviewer soi read-only KHÔNG lộ nhạy cảm (done_when line 4) + silent-failure-hunter (không nuốt lỗi — màn bảo mật, degraded-giả nguy hiểm hơn lỗi tường minh). Test deny-path tầng BE do BE-3 int-spec phủ; FE test chỉ assert client IDOR-safe + không unmask.

SCOPE-GAP (CẦN owner mở rộng WO.paths): sidebar-registry.ts nằm ở apps/app/src/layouts/workspace/sidebar-registry.ts — NGOÀI WO.paths hiện khai (giống hệt tình huống S5-ME-FE-3 đã ship). Lane fe2pages BẮT BUỘC append ME_SIDEBAR 2 nhóm ở file này ⇒ guard-scope sẽ CẢNH BÁO. Đề xuất bổ sung 'apps/app/src/layouts/workspace/sidebar-registry.ts' (hoặc apps/app/src/layouts/workspace/**) vào WO.paths. Còn registry.ts (ROUTE_REGISTRY) NẰM TRONG scope (packages/web-core/src/**) — OK. routes/hr/** + routes/account/** có trong WO.paths (dành cho reuse-refactor nếu cần) nhưng kế hoạch mặc định IMPORT-ONLY, KHÔNG sửa.

HOT-FILE quy ước APPEND (KHÔNG rewrite): me-api.ts append 1 method getSecurityActivity; query-keys.ts append meKeys.securityActivity; registry.ts ROUTE_REGISTRY append 6 RouteMeta (order tiếp 72+, giữ pattern literal ['access:me']); nav.ts append routeTitle.me* 6 khoá; sidebar-registry.ts ME_SIDEBAR append 2 nhóm (group = nhãn tiếng Việt, mirror FE-3 — ModuleSidebar fallback in nguyên chuỗi); router.tsx append 6 route + 6 dòng addChildren (mirror meAttendanceRoute/meOverviewRoute — 5 component đã lazy-import sẵn, chỉ THÊM route trỏ tới; MeSecurityActivityPage lazy-import mới); me.ts (app i18n) append securityActivity.*.

VERIFY: pnpm --filter @mediaos/web-core build (REBUILD dist — consumer app import từ dist; quên = trang trắng, memory web-core-stale-dist) → pnpm --filter @mediaos/app typecheck test build → pnpm --filter @mediaos/web-core typecheck test → bash harness/check.sh (TURBO_FORCE=1 chống turbo cache false-green). KHÔNG migration (FE-only) ⇒ KHÔNG cần LANE_DB.

OUT-OF-SCOPE (chống scope-creep): KHÔNG build ME-SCREEN-002 slice riêng /personal /work /contracts (§9 = 1 màn); KHÔNG avatar upload UI (ME-FUNC-015); KHÔNG data-export (ME-DEC-009 P1); KHÔNG dựng lại sessions revoke (đã có /auth/sessions); KHÔNG đổi BE/contracts/migration/permission/seed; KHÔNG sửa viewer admin auth-logs; KHÔNG unmask/thêm setting mở raw IP. Nếu spec §8.2 lệch §9/code → §9 + reuse-mandate thắng, điểm chốt đã ghi ở QUYẾT ĐỊNH (3).
</content>
