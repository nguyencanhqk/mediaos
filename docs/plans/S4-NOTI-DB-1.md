```yaml
wo: S4-NOTI-DB-1
zone: red
generated_by: auto-loop
reconciled_at: "b19c953"
lanes: [{"id":"notidb1migration","task":"[db-migration · CROWN red · NỐI TIẾP TRƯỚC] Migration 0479_s4_notidb1_notification_core (nối tiếp head idx 158/0478 → idx 159, when > 1717500785000). (A) CREATE 3 bảng MỚI DB-07: notification_events (company_id NULLABLE — nullable-tenant RLS mẫu sequences/holidays: USING company_id=GUC OR company_id IS NULL, WITH CHECK company_id=GUC), notification_templates (company_id NULLABLE nullable-tenant, FK event_id), notification_delivery_logs (company_id NOT NULL, RLS ENABLE+FORCE + policy tenant, APPEND-ONLY: GRANT SELECT,INSERT app — KHÔNG UPDATE/DELETE; retry = attempt_no mới). Mỗi bảng ENABLE+FORCE + policy literal-GUC (mirror 0478 dòng 61-66) + indexes + CHECK + grants. (B) ALTER-ADD additive lên notifications (mig 0010 media-era) MỌI cột NULLABLE (recipient_user_id, recipient_employee_id, event_id, template_id, module_code, event_code, notification_type, priority, status, title, short_body, source_entity_type/id/code, target_module/type/id/url, payload, dedupe_key, batch_key, correlation_id, scheduled_at, sent_at, read_at, hidden_at, archived_at, expires_at, created_by, updated_at, updated_by, deleted_at, deleted_by) + CHECK status IN(Unread/Read/Hidden/Archived/Deleted/Failed) cho phép NULL — GIỮ cột legacy user_id/is_read/body/type/ref_*, KHÔNG drop, KHÔNG backfill. Partial index unread (company_id, recipient_user_id) WHERE status='Unread'; list index (company_id, recipient_user_id, created_at DESC) WHERE deleted_at IS NULL; uq_notifications_dedupe_active; uq global/company code active cho events/templates (partial theo company_id IS [NOT] NULL). Schema TS: ALTER-ADD notifications trong communication.ts + FILE MỚI apps/api/src/db/schema/noti.ts (events/templates/delivery_logs) + APPEND export ở index.ts. DDL thủ công (RLS/grant/CHECK) — KHÔNG db:generate. APPEND journal idx 159. Grant events/templates SELECT-only (write company-override để NOTI-BE-3).","builder":"db-migration","paths":["apps/api/migrations/0479_s4_notidb1_notification_core.sql","apps/api/migrations/meta/_journal.json","apps/api/src/db/schema/communication.ts","apps/api/src/db/schema/noti.ts","apps/api/src/db/schema/index.ts","docs/plans/S4-NOTI-DB-1.md"]},{"id":"notidb1rlstest","task":"[db-migration · test RED-trước · depends_on notidb1migration] Đăng ký rls-registry + int-spec deny-path DB cô lập. (1) APPEND rls-registry.ts 3 case MỚI: notification_events + notification_templates (skipNoContext:true — global rows company_id NULL hiển thị mọi tenant, seed hàng TENANT-scoped để kiểm cô lập chéo), notification_delivery_logs (company_id NOT NULL, seed direct; FK notifications). notifications/notification_preferences/notification_rules ĐÃ có — không đụng. (2) Int-spec RED (mẫu notifications-tenant-isolation.int-spec.ts, gate skipIf(!hasDb)): cross-tenant deny cho 3 bảng mới (withTenant(A) SELECT 0 hàng B; INSERT company_id=B bị RLS/WITH CHECK chặn) + append-only notification_delivery_logs (app role UPDATE/DELETE rejects). Chạy RED trước khi migration land (fail vì bảng chưa có) → GREEN sau. Xác minh rls-guards + rls-coverage-assert xanh (không thủng lưới im lặng). LƯU Ý: gate hasDb && LANE_DB (chỉ .env → hasDb=true = đỏ giả); int-spec dùng test/integration (không cần colocate).","builder":"db-migration","paths":["apps/api/test/integration/rls-registry.ts","apps/api/test/integration/notifications-noti-core-tenant-isolation.int-spec.ts","apps/api/test/integration/notification-delivery-append-only.int-spec.ts"]}]
acceptanceChecks: ["Migration 0479_s4_notidb1_notification_core.sql nối tiếp head idx 158 (0478) → journal APPEND idx 159 (when > 1717500785000), KHÔNG sửa entry cũ; migrate 0000→head trên DB trống (LANE_DB) chạy sạch.","notifications ALTER-ADD (recipient_user_id, module_code, event_code, notification_type, priority, status, title, short_body, target_module/type/id/url, payload, dedupe_key, read_at/hidden_at/archived_at/deleted_at, created_by/updated_at/updated_by/deleted_by, ...) MỌI cột NULLABLE; CHECK status IN ('Unread','Read','Hidden','Archived','Deleted','Failed') cho phép NULL; GIỮ NGUYÊN cột legacy user_id/is_read/body/type/ref_* + policy notifications_tenant_isolation.","notification_events tạo mới: company_id NULLABLE + RLS ENABLE+FORCE + policy nullable-tenant (USING company_id=GUC OR IS NULL / WITH CHECK company_id=GUC); cột event_code/module_code/event_name/notification_type/default_priority/default_channels(jsonb)/is_enabled/dedupe_strategy/dedupe_window_seconds/recipient_rule_config; CHECK module_code+type+priority+dedupe_strategy; uq global (event_code WHERE company_id IS NULL) + uq company (company_id,event_code WHERE company_id IS NOT NULL) đều WHERE deleted_at IS NULL.","notification_templates tạo mới: company_id NULLABLE + RLS+FORCE nullable-tenant; FK event_id→notification_events; cột channel/locale/title_template/short_body_template/body_template/variables_schema(jsonb)/status/is_default; CHECK channel IN(IN_APP,EMAIL,PUSH,REALTIME,INTEGRATION) + status IN(Draft,Active,Inactive,Archived); uq template_code active.","notification_delivery_logs tạo mới: company_id NOT NULL + RLS ENABLE+FORCE + policy tenant literal-GUC; APPEND-ONLY — GRANT SELECT,INSERT app (KHÔNG UPDATE/DELETE), GRANT SELECT worker; cột notification_id FK/recipient_user_id/channel/provider/delivery_status/attempt_no/max_attempts/error_code/error_message/next_retry_at; CHECK delivery_status + attempt (attempt_no>=1 AND max_attempts>=attempt_no).","Partial index unread: index (company_id, recipient_user_id) WHERE status='Unread' (đếm unread) + index list (company_id, recipient_user_id, created_at DESC) WHERE deleted_at IS NULL + uq_notifications_dedupe_active (company_id,recipient_user_id,event_code,dedupe_key) WHERE dedupe_key IS NOT NULL AND deleted_at IS NULL.","3 bảng mới đăng ký apps/api/test/integration/rls-registry.ts (events/templates skipNoContext:true); rls-guards.int-spec + rls-coverage-assert.int-spec xanh (mọi bảng company_id có ENABLE+FORCE + policy đọc/ghi theo GUC; không bảng company_id thiếu case).","Schema TS đồng bộ DDL: notifications (communication.ts) + noti.ts export additive ở index.ts; pnpm --filter @mediaos/api typecheck + build xanh; KHÔNG db:generate (DDL thủ công).","RECONCILE: KHÔNG drop bảng/cột legacy; KHÔNG backfill; push/device (push-sender/device-token/device_tokens) không đụng (out IN_APP MVP); legacy NotificationsService/Repository (is_read/body) vẫn chạy sau ALTER-ADD nullable.","DoD §8 + red-zone: int-spec RED-trước xanh sau land + migration-smoke xanh; FULL gate (database-reviewer + rls-tenant-isolation-tester) PASS; người duyệt red-zone; harness/backlog.mjs + STATUS regen."]
testTasks: ["Int-spec RED cross-tenant deny notification_events (nullable-tenant): withTenant(A) SELECT KHÔNG thấy hàng company-scoped của B; hàng global (company_id NULL) hiển thị; INSERT company_id=B qua app role bị WITH CHECK chặn (rejects).","Int-spec RED cross-tenant deny notification_templates: cùng mẫu nullable-tenant (A không thấy company-row B; INSERT company_id=B rejects); FK event_id hợp lệ cùng company.","Int-spec RED cross-tenant deny notification_delivery_logs (company_id NOT NULL): withTenant(A) SELECT 0 hàng B; INSERT company_id=B rejects (WITH CHECK).","Int-spec RED append-only notification_delivery_logs: app role UPDATE → rejects; DELETE → rejects (REVOKE — BẤT BIẾN #2); direct/superuser seed OK.","rls-guards + rls-coverage-assert: 3 bảng mới có relrowsecurity+relforcerowsecurity=true và policy USING/WITH CHECK tham chiếu app.current_company_id; không bảng company_id nào thiếu case registry (lưới không thủng im lặng).","migration-smoke: DB trống 0000→0479 áp đơn điệu (LANE_DB cô lập, gate hasDb && LANE_DB — tránh đỏ giả do .env); journal idx 159 nối tiếp 0478.","Regression legacy: notifications-tenant-isolation.int-spec + notifications-mandatory.int-spec + notifications.spec (colocate src) vẫn xanh sau ALTER-ADD (unread partial index không phá countUnread cũ)."]
steps: ["PLAN: ghi docs/plans/S4-NOTI-DB-1.md (khối yaml máy-đọc: lanes/acceptanceChecks/testTasks + reconcileNotes) để tái dùng lần sau.","RED-trước (notidb1rlstest): viết int-spec deny-path (cross-tenant 3 bảng mới + append-only delivery_logs) + APPEND rls-registry 3 case → chạy RED (fail: bảng chưa tồn tại).","Schema TS (notidb1migration): ALTER-ADD cột DB-07 (NULLABLE) vào pgTable notifications trong communication.ts; tạo FILE MỚI noti.ts (notificationEvents/notificationTemplates/notificationDeliveryLogs); APPEND export * from ./noti ở index.ts.","Migration 0479 SQL: CREATE 3 bảng (RLS ENABLE+FORCE + policy + CHECK + indexes + grants; delivery_logs append-only SELECT,INSERT; events/templates nullable-tenant policy) → ALTER notifications additive + partial index unread + uq dedupe → APPEND journal idx 159 (when > 1717500785000).","Verify migration-smoke: bash scripts/lane-db-setup.sh + LANE_DB trên DB trống (0000→head) chạy sạch, journal nối tiếp đơn điệu; pnpm --filter @mediaos/api typecheck xanh.","GREEN (notidb1rlstest): int-spec + rls-guards + rls-coverage-assert xanh; re-run notifications-tenant-isolation + notifications-mandatory + notifications.spec cũ (legacy is_read/body KHÔNG vỡ).","Gate FULL: database-reviewer + rls-tenant-isolation-tester + silent-failure-hunter PASS; sửa root-cause nếu đỏ; checkpoint; người duyệt red-zone trước merge; cập nhật harness/backlog.mjs + regen STATUS."]
```

GAP-ANALYSIS (code hiện tại vs DB-07): Tồn tại notifications (mig 0010, media-era: user_id/type/ref_id/ref_type/body/is_read — RLS+FORCE + policy notifications_tenant_isolation, app SELECT/INSERT/UPDATE, đã đăng ký rls-registry:1210) + notification_preferences + notification_rules (mig 0051). THIẾU: notification_events, notification_templates, notification_delivery_logs (0/3 bảng core mới). notifications thiếu toàn bộ trục DB-07 (recipient_user_id, event_code, module_code, status enum, target_*, payload, read/hidden/archived_at, dedupe_key).

QUYẾT ĐỊNH RECONCILE — Option-A evolve-additive (mirror y hệt 0478 S4-TASK-DB-1): ALTER-ADD cột DB-07 vào notifications MỌI cột NULLABLE, CHECK enum tên MỚI (status) cho phép NULL, GIỮ cột legacy (user_id/is_read/body/type/ref_*), KHÔNG drop, KHÔNG backfill (chưa có nguồn map + legacy service còn dùng is_read). Cut-over repo/service sang cột mới = S4-NOTI-BE-1 (OUT-OF-SCOPE). notifications KHÔNG append-only (mark-read/hidden/archived = UPDATE — giữ grant UPDATE).

INVARIANTS: (#1) 3 bảng mới ENABLE+FORCE + policy literal-GUC (mẫu 0478:61-66). events/templates company_id NULLABLE (DB-07 §4.3 catalog global+override) → policy nullable-tenant USING(company_id=GUC OR IS NULL)/WITH CHECK(company_id=GUC) mẫu sequences/holidays/system_job_runs; delivery_logs company_id NOT NULL policy tenant chuẩn. (#2) notification_delivery_logs APPEND-ONLY (đúng danh sách thiết kế CLAUDE §2/DB-07 §7.4): GRANT SELECT,INSERT — KHÔNG UPDATE/DELETE; retry = attempt_no mới (KHÔNG update in-place — refine done_when trên §7.4 updated_at/status-transition). (#3) không secret trong payload (DB-07 §4.6). timestamptz UTC-at-rest, UUID PK, soft-delete deleted_at/by.

STATUS enum: dùng SIÊU-TẬP DB-07 §7.3 (Unread/Read/Hidden/Archived/Deleted/Failed) — done_when liệt kê 5, thêm Failed cho delivery-fail (spec thắng, superset an toàn).

VERIFY/GATE: RED-trước int-spec (cross-tenant deny 3 bảng + append-only delivery) → GREEN sau land; rls-guards + rls-coverage-assert (registry-independent) khẳng định FORCE+policy tầng DB; migration-smoke DB trống LANE_DB (gate hasDb && LANE_DB — chỉ .env=đỏ giả). FULL gate: database-reviewer + rls-tenant-isolation-tester (opus) + silent-failure-hunter; người duyệt red-zone trước merge. Migration lane NỐI TIẾP trước (idx 159), lane test depends_on migration.

HOT-FILE & MERGE (append, KHÔNG rewrite): migrations/meta/_journal.json APPEND idx 159; schema/index.ts APPEND `export * from ./noti`; schema/communication.ts ALTER-ADD cột vào pgTable notifications (giữ index cũ); test/integration/rls-registry.ts APPEND 3 case (events/templates skipNoContext:true). File MỚI: 0479_*.sql, schema/noti.ts, 2 int-spec.

OUT-OF-SCOPE (park/defer): push/email delivery thật + device-token + push-sender (device_tokens giữ nguyên — ngoài IN_APP MVP); recipient resolver/template renderer/dedupe engine → NOTI-BE-2; my-notification APIs → NOTI-BE-1; seed event catalog + template IN_APP tiếng Việt (§9.5) → NOTI-SEED-1 (INSERT global catalog company_id NULL qua direct/superuser vì app grant events/templates SELECT-only); admin config write grants → NOTI-BE-3; audit object_type 'notification' cho config-action → NOTI-BE (WO này chỉ schema, không audit writer); notification_preferences UI + dashboard tables (DASH) → out. Migration additive-only, KHÔNG chạm bảng DASH (S4-DASH-DB-1 riêng).
</content>
