```yaml
wo: S5-ME-BE-1
zone: red
generated_by: auto-loop
reconciled_at: "df39364d"
lanes: [{"id":"mecontracts","task":"FOUNDATION Zod DTO ở packages/contracts (dual-build). (1) Section-status envelope: { status: 'ok'|'error'|'forbidden'|'module_disabled'|'unlinked_employee', data } — union PHẢI CÓ 'forbidden' (SPEC-09 §13). (2) MeIdentity (account từ token + link employee tối thiểu) + MeOverview (gom section) + Me{Attendance,Leave,Task,Notification}Summary. (3) ĐĂNG KÝ MÃ LỖI ME_ERROR_CODES={ DATA_INCONSISTENT:'ME-ERR-DATA-INCONSISTENT' } theo pattern append-only packages/contracts/src/foundation/error-codes.ts — đăng ký TRƯỚC khi service dùng (done_when). (4) Append barrel export packages/contracts/src/index.ts (KHÔNG rewrite). KHÔNG đặt field nhạy cảm (salary/PII/token/hash) vào DTO — masking là việc module nguồn. Chạy `pnpm --filter @mediaos/contracts build`.","builder":"backend-builder","paths":["packages/contracts/src/me.ts","packages/contracts/src/index.ts"]},{"id":"memodule","task":"CROWN. MeModule MỚI apps/api/src/me (MeController + MeAggregationService + current-person resolver), mount app.module KHỐI ADDITIVE. 6 route GET: /api/v1/me · /me/overview · /me/attendance-summary · /me/leave-summary · /me/task-summary · /me/notification-summary (SPEC-09 §14.2). Resolve user_id+company_id TỪ req.user (JwtAuthGuard+CompanyGuard+TwoFactorEnforcementGuard = APP_GUARD GLOBAL ⇒ auto-401) — KHÔNG @Param/@Query/@Body user_id|employee_id (chống IDOR §14.4/§17.1). Class-level @UseGuards(PermissionGuard)+@RequirePermission('access','me') — tuple THẬT khớp NGUYÊN VĂN mig 0495 (action='access', resourceType='me', is_sensitive=false), engine KHÔNG dùng dotted 'ME.ACCESS'. current-person resolver PHẢI ĐẾM TẤT CẢ employee active theo user_id trong withTenant (deleted_at IS NULL + status active) — KHÔNG dùng reader LIMIT-1: 0 active→unlinked_employee; 1→normal; >1→ném ME-ERR-DATA-INCONSISTENT (409) + audit object_type='user' action 'MeDataInconsistent' (hợp CHECK 0011), KHÔNG tự chọn (§12.4). MeAggregationService re-check cặp quyền NGUỒN TƯỜNG MINH qua PermissionService.can({userId,companyId,action,resourceType}) TRƯỚC khi đọc MỖI section (PermissionGuard chỉ ở controller ⇒ reader in-process bypass guard): HR(read,employee)·ATT(view-own,attendance)·LEAVE(view-own,leave-balance)·TASK(read,task)·NOTI(read,notification); thiếu → section.status='forbidden', KHÔNG đọc dữ liệu, KHÔNG throw (SPEC-09 §11.2). COMPOSE CHỈ reader own-scope canonical đã export: HrReadService.getMyProfile(user)·AttendanceService.getToday(actor)·LeaveReadService.listMyBalances(actor)·TaskCoreService.getMyTasks(user) [CANONICAL — KHÔNG TasksService.getMyTasks(companyId,userId)]·MyNotificationsService.unreadCount(companyId,userId) — bài học reused-method-must-be-actor-scoped: TUYỆT ĐỐI không listTeam/listCompany/listAll/listBoard; KHÔNG bypass masking nguồn (salary/PII không lộ thêm qua ME); KHÔNG tự tính lại số dư phép (§7.4). Fail-soft qua Promise.allSettled (mirror DashboardWidgetDataService) với phân loại exception CHÍNH XÁC: CHỈ ForbiddenException(403)→status='forbidden'; NotFoundException(404) từ reader nguồn→ok/empty (KHÔNG dán nhãn forbidden — sai ngữ nghĩa quyền); non-HttpException/lỗi hạ tầng→status='error'; CẤM nuốt 403 thành ok/error; section khác vẫn ok; HTTP 200, KHÔNG 500. module_disabled: mirror default-enabled module-catalog.service (r?.found ? value===false → 'module_disabled' : ok) — 5 core module thiếu row company_settings ⇒ ENABLED, CHỈ value===false → module_disabled, không stale (§12.3). company_id ở mọi query qua withTenant reader nguồn (BẤT BIẾN #1).","builder":"backend-builder","paths":["apps/api/src/me/**","apps/api/src/app.module.ts"]},{"id":"meinttests","task":"Int-spec RED-trước, colocate apps/api/test/integration (đuôi .int-spec.ts, gate hasDb && LANE_DB — chạy như CI). Seed target trực tiếp; seed cặp ME.ACCESS khớp NGUYÊN VĂN tuple mig 0495 (action='access', resourceType='me', is_sensitive=false) — cấm tự chế tuple lệch. Phủ: (a) permission-deny — user KHÔNG có ME.ACCESS → 403 AUTH-ERR-FORBIDDEN mọi route (RED trước gate đúng); (b) IDOR — ?user_id=<B>/body{employee_id:<B>} bị BỎ QUA (owner từ token) hoặc 400, hành vi KHÔNG đổi; (c) own-scope — A không thấy dữ liệu B cùng tenant qua bất kỳ route ME; (d) cross-tenant deny — token company X không lộ dữ liệu company Y; (e) degraded — mock 1 nguồn (TASK) throw non-HttpException → task.status='error', section khác ok, HTTP 200 (KHÔNG 500, KHÔNG nuốt 403→ok); (e2) 404-not-forbidden — reader nguồn throw NotFoundException(404) → section status ok/empty (KHÔNG 'forbidden'); (f) unlinked-employee — user không link employee → section HR/ATT/LEAVE='unlinked_employee', identity/account ok, HTTP 200; (g) module_disabled — INSERT THẲNG company_settings module.<code>.enabled=false → section='module_disabled', không stale + test all-modules-ok (không seed row → expect ok) làm chốt; (h) multi-active-employee → ME-ERR-DATA-INCONSISTENT 409 + audit object_type='user'; (i) MA TRẬN forbidden per-section ĐỦ 5 NGUỒN HR/ATT/LEAVE/TASK/NOTI — (CÓ ME.ACCESS, THIẾU source-pair X) → section X.status='forbidden' + section khác='ok' + HTTP 200; assert CẢ /me/overview LẪN route section chuyên biệt (/me/attendance-summary·leave-summary·task-summary·notification-summary) trả 200 + section-envelope (KHÔNG 403/500 lọt HttpException).","builder":"backend-builder","paths":["apps/api/test/integration/**"]}]
acceptanceChecks: ["MeModule tồn tại apps/api/src/me, mount app.module KHỐI ADDITIVE (không rewrite khối cũ); 6 route: GET /api/v1/me, /me/overview, /me/attendance-summary, /me/leave-summary, /me/task-summary, /me/notification-summary (SPEC-09 §14.2).","Mọi route class-guarded @UseGuards(PermissionGuard)+@RequirePermission('access','me') (tuple khớp NGUYÊN VĂN mig 0495: action='access', resourceType='me', is_sensitive=false); thiếu quyền → 403 AUTH-ERR-FORBIDDEN; JwtAuthGuard GLOBAL ép 401 khi chưa auth.","Controller KHÔNG nhận user_id/employee_id từ client: grep MeController KHÔNG có @Param/@Query/@Body ('user_id'|'employee_id'|userId|employeeId); owner 100% từ req.user (§14.4/§17.1).","current-person resolver ĐẾM TẤT CẢ employee active theo user_id trong withTenant (deleted_at IS NULL + status active), KHÔNG LIMIT-1: 0→unlinked_employee, 1→normal, >1→ME-ERR-DATA-INCONSISTENT 409 + audit object_type='user' (CHECK 0011), KHÔNG auto-pick (§12.4).","MeAggregationService re-check cặp quyền NGUỒN TƯỜNG MINH qua PermissionService.can TRƯỚC khi đọc MỖI section: HR(read,employee)·ATT(view-own,attendance)·LEAVE(view-own,leave-balance)·TASK(read,task)·NOTI(read,notification); thiếu source-pair → section.status='forbidden' (KHÔNG đọc dữ liệu).","Compose CHỈ reader own-scope canonical: HrReadService.getMyProfile · AttendanceService.getToday · LeaveReadService.listMyBalances · TaskCoreService.getMyTasks(user) [KHÔNG TasksService.getMyTasks(companyId,userId)] · MyNotificationsService.unreadCount — grep KHÔNG gọi listTeam/listCompany/listAll/listBoard; không tự tính lại số dư phép; không thêm field lộ salary/PII ngoài masking nguồn.","Phân loại exception fail-soft CHÍNH XÁC: CHỈ ForbiddenException(403)→'forbidden'; NotFoundException(404) từ reader→ok/empty (KHÔNG 'forbidden'); non-HttpException/infra→'error'; section khác 'ok', HTTP 200 (Promise.allSettled), KHÔNG bao giờ 500, KHÔNG nuốt 403 thành ok/null.","Mỗi section status ∈ {ok,error,forbidden,module_disabled,unlinked_employee} (union CÓ 'forbidden' — SPEC-09 §13); module_disabled mirror default-enabled module-catalog (5 core thiếu row company_settings ⇒ ok, CHỈ value===false → module_disabled, không stale §12.3); unlinked→identity/account ok, HR/ATT/LEAVE='unlinked_employee' (§12.2).","DTO ME ở packages/contracts (dual-build) + barrel exported; ME_ERROR_CODES.DATA_INCONSISTENT đăng ký catalog TRƯỚC khi service dùng; mỗi section 1 reader — không N+1 theo widget (§18.1).","Int-spec RED-first phủ permission-deny/IDOR/own-scope/cross-tenant/degraded/404-not-forbidden/unlinked/module_disabled/multi-employee + MA TRẬN forbidden per-section ĐỦ 5 nguồn HR/ATT/LEAVE/TASK/NOTI; assert cả /me/overview LẪN route section chuyên biệt trả 200 + section-envelope; gate hasDb && LANE_DB (không skip-giả); FULL gate security-reviewer + silent-failure-hunter PASS.","DoD §8: typecheck+lint+test xanh; WO read-only (không mutation) ⇒ audit chỉ ở nhánh anomaly §12.4; harness/backlog.mjs cập nhật."]
testTasks: ["deny-path RED (permission): user KHÔNG có cặp ME.ACCESS → 403 AUTH-ERR-FORBIDDEN trên cả 6 route; phải RED trước khi seed/gate đúng.","MA TRẬN forbidden per-section RED (ĐỦ 5 NGUỒN — blocker plan-reviewer): với TỪNG section HR/ATT/LEAVE/TASK/NOTI — user CÓ ME.ACCESS nhưng THIẾU source-pair X (HR read:employee · ATT view-own:attendance · LEAVE view-own:leave-balance · TASK read:task · NOTI read:notification) → section X.status='forbidden' + section khác='ok' + HTTP 200; assert trên /me/overview LẪN route section chuyên biệt.","IDOR RED: gửi ?user_id=<B>/body{employee_id:<B>} — response chỉ dữ liệu caller (từ token), user_id lạ bị bỏ qua hoặc 400; hành vi KHÔNG đổi.","own-scope: user A không thấy dữ liệu user B cùng tenant qua bất kỳ route ME (dựng A,B + employee/att/leave/task/noti của B, assert A không lộ).","cross-tenant deny: token company X không surface dữ liệu company Y (planted rows tenant Y) — mọi section rỗng/không lộ.","degraded (silent-failure guard): mock 1 nguồn (TASK) throw non-HttpException → task.status='error', section khác ok, HTTP 200 (KHÔNG 500, KHÔNG nuốt 403→ok).","404-not-forbidden: reader nguồn throw NotFoundException(404) → section status ok/empty, KHÔNG dán nhãn 'forbidden' (phân biệt thiếu-quyền vs không-có-dữ-liệu).","unlinked-employee: user không link employee → section employee-dependent='unlinked_employee', identity/account ok, HTTP 200.","module_disabled: INSERT THẲNG company_settings module.<code>.enabled=false (core-lock chỉ chặn toggle API, không chặn value) → section='module_disabled', không stale; + all-modules-ok (không seed row → expect ok) làm chốt chống impl sai.","multi-active-employee anomaly: user link >1 employee active → ME-ERR-DATA-INCONSISTENT 409 + ghi audit object_type='user', KHÔNG tự chọn.","Chạy DB cô lập: scripts/lane-db-setup.sh me → LANE_DB=mediaos_me; trước PR đỏ `bash harness/check.sh --lane-db` (ép deny-path/IDOR/forbidden-matrix chạy thật, chống xanh-giả)."]
steps: ["mecontracts: viết packages/contracts/src/me.ts (section-status envelope union CÓ 'forbidden' + MeIdentity/MeOverview/*Summary Zod + ME_ERROR_CODES.DATA_INCONSISTENT theo pattern error-codes.ts), append index.ts barrel, `pnpm --filter @mediaos/contracts build` (dual ESM/CJS) để api typecheck thấy.","memodule: MeModule import EmployeesModule(HrReadService)·AttendanceModule·LeaveModule(LeaveReadService)·TasksModule(TaskCoreService)·NotificationsModule(MyNotificationsService)·FoundationModule(module-enabled resolve)·PermissionModule — CHỈ inject service ĐÃ export, không re-provide instance thứ 2. Viết current-person resolver ĐẾM TẤT CẢ employee active theo user_id trong withTenant (0→unlinked, 1→normal, >1→409 ME-ERR-DATA-INCONSISTENT + audit object_type='user'). MeAggregationService: per-section PermissionService.can(source-pair) → thiếu→forbidden (không đọc); đủ→gọi reader own-scope canonical; compose Promise.allSettled fail-soft (403→forbidden, 404→ok/empty, infra→error). MeController 6 GET, @UseGuards(PermissionGuard)+@RequirePermission('access','me'), KHÔNG param/query/body user_id|employee_id. Mount app.module khối additive.","meinttests: viết int-spec RED-first (permission-deny/IDOR/own-scope/cross-tenant/degraded/404-not-forbidden/unlinked/module_disabled/multi-employee + MA TRẬN forbidden per-section ĐỦ 5 nguồn HR/ATT/LEAVE/TASK/NOTI), seed target trực tiếp khớp tuple mig 0495, `bash scripts/lane-db-setup.sh me` → export LANE_DB=mediaos_me → chạy RED trước khi GREEN.","GREEN → FULL gate security-reviewer + silent-failure-hunter (soi Promise.allSettled KHÔNG map 403-nguồn thành 'ok/null', KHÔNG nuốt 404 thành forbidden). Trước PR vùng đỏ: `bash harness/check.sh --lane-db` (deny-path/IDOR/forbidden-matrix chạy THẬT). Crown → người chốt red-zone trước merge; cập nhật harness/backlog.mjs."]
```

RECONCILE-REFRESH 2026-07-13 (reused=false — done_when tiếp thu tinh chỉnh vượt plan cũ). TIỀN ĐỀ ĐÃ CHỐT: mig 0495 (apps/api/migrations/0495_s5_medb1_user_preferences_me_module_perms.sql) ĐÃ LAND master (#199, df39364d, journal idx 175) — HARD DEPENDENCY S5-ME-DB-1 RESOLVED (plan cũ ghi status='todo' nay hết hạn). Tuple ME.ACCESS xác nhận NGUYÊN VĂN: ('access','me',is_sensitive=false) + 4 pref pair + 20 grant Own × 4 role canonical (employee/manager/hr/company-admin). apps/api/src/me VẪN greenfield.

NGUỒN READER own-scope đã export (verified): HrReadService.getMyProfile(user) apps/api/src/employees/hr-read.service.ts:217 (self findByUserId, giữ masking, gate read:employee; throw NotFoundException khi thiếu profile — line 881 spec); LeaveReadService.listMyBalances(actor) leave-read.service.ts:63; AttendanceService.getToday(actor) attendance.service.ts:161 (resolveEmployeeByUserId, unlinked→emptyToday); TaskCoreService.getMyTasks(user) task-core.service.ts:124 [CANONICAL — dashboard-widget-handlers.service.ts:233; KHÔNG TasksService.getMyTasks(companyId,userId) tasks.service.ts:65]; MyNotificationsService.unreadCount(companyId,userId) my-notifications.service.ts:76 (gate READ_NOTIFICATION=read:notification). MẪU fail-soft = DashboardWidgetDataService (Promise.allSettled); MẪU per-source gate = DashboardWidgetHandlersService.gateOrThrow:109 (permission.can TRƯỚC fetch). module default-enabled resolve = module-catalog.service.ts:71-72/142-143 (r?.found ? value===false→disabled : ENABLED). audit object_type CHECK mig 0011 gồm 'user' (hợp anomaly §12.4). error-catalog append-only pattern = packages/contracts/src/foundation/error-codes.ts (ME_ERROR_CODES ở me.ts).

6 CHANGES vs plan cũ (done_when refine): (1) HARD DEPENDENCY DB-1 RESOLVED — int-spec seed-target khớp tuple mig 0495, không còn blocker thứ tự merge. (2) EXCEPTION CLASSIFICATION SỬA: plan cũ map 'HttpException-4xx→forbidden' SAI — done_when nay CHỈ ForbiddenException(403)→forbidden; NotFoundException(404) từ reader→ok/empty (KHÔNG forbidden, tránh sai ngữ nghĩa quyền); non-HttpException→error. (3) current-person resolver ĐẾM TẤT CẢ employee active (deleted_at IS NULL + status active) trong withTenant, KHÔNG LIMIT-1 — để nhánh >1-active (409 ME-ERR-DATA-INCONSISTENT §12.4) kích hoạt THẬT. (4) audit anomaly object_type='user' + action 'MeDataInconsistent'. (5) module_disabled mirror coerceEnabled (missing row→ENABLED, chỉ value===false→disabled) + giữ test all-modules-ok làm chốt. (6) MA TRẬN forbidden per-section MỞ RỘNG từ 4→5 nguồn (thêm NOTI read:notification) + assert cả /me/overview LẪN route section chuyên biệt trả 200 section-envelope.

INVARIANTS: #1 company_id mọi query — ME KHÔNG mở withTenant riêng cho dữ liệu nghiệp vụ, đi qua reader nguồn (đã withTenant); resolver current-person chỉ đọc users/employees theo companyId+userId token. #2 append-only — WO read-only, chỉ audit nhánh anomaly. #3 secret — DTO không chứa hash/token; masking do nguồn quyết (KHÔNG mở rộng scope, KHÔNG bypass). §7.4 KHÔNG tự tính lại số dư phép. §18.1 không N+1: mỗi section 1 reader; /me/overview Promise.allSettled song song.

VERIFY: grep MeController no user_id/employee_id param; grep service no *Company*/listTeam/listAll/listBoard; grep TaskCoreService.getMyTasks (not TasksService); int-spec forbidden-matrix chứng minh HTTP 200 + section.status='forbidden' khi thiếu source-pair (đủ 5 nguồn); silent-failure-hunter soi Promise.allSettled không map 403→ok/null và không map 404→forbidden; hasDb && LANE_DB.

GATE: FULL (zone red — permission/auth/aggregation own-scope + IDOR + masking passthrough) = security-reviewer + silent-failure-hunter (+ typescript-reviewer baseline + quality-gate). Model: crown → planner Sonnet xhigh micro-plan qua plan-reviewer, IMPLEMENT/REVIEW Opus. RED-first bắt buộc permission/degraded/forbidden-matrix.

LANE STRUCTURE: 3 lane không chồng paths (mecontracts=packages/contracts/src/me.ts+index.ts; memodule=apps/api/src/me/**+app.module.ts; meinttests=apps/api/test/integration/**), builder=backend-builder MỌI lane. KHÔNG có lane db-migration (mig 0495 đã land — WO này không tạo migration). Hot-file: app.module.ts khối additive; packages/contracts/src/index.ts append barrel; harness/backlog.mjs cập nhật.

OUT-OF-SCOPE (chống scope-creep): (1) migration/schema/seed = S5-ME-DB-1 (đã land — KHÔNG viết lại); (2) /me/profile, /me/account, /me/security/sessions|activity, /me/preferences*, /me/avatar = S5-ME-BE-2/BE-3 (WO này CHỈ 6 route aggregation §14.2); (3) FE /me screens = S5-ME-FE-*; (4) mutation nghiệp vụ (change-password/check-in/leave request) điều hướng module gốc, ME KHÔNG định nghĩa lại (§14.3/§12.5). ĐIỂM CẦN NGƯỜI CHỐT: (a) crown red-zone → người duyệt trước merge; (b) chốt map 403-nguồn=section.status='forbidden' + 404=ok/empty (KHÔNG forbidden) bám SPEC-09 §11.2/§13 — reviewer verify không lộ-âm-thầm.
</content>
