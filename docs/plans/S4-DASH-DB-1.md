```yaml
wo: S4-DASH-DB-1
zone: red
generated_by: auto-loop
reconciled_at: "59faed8"
lanes: [{"id":"dashDbCore","task":"Migration 0482_s4_dashdb1_dashboard_core (NỐI TIẾP head 0481, idx162/when1717500805000): tạo 3 bảng DB-07 §8.1/8.2/8.3 — dashboard_widgets (company_id NULLABLE, RLS+FORCE NULLABLE-TENANT policy mirror notification_events 0479, GRANT app SELECT) · dashboard_widget_configs (company_id NOT NULL, RLS+FORCE literal-GUC, GRANT app SELECT) · dashboard_widget_cache (company_id NOT NULL, RLS+FORCE literal-GUC, GRANT app SELECT,INSERT,UPDATE cho runtime upsert/soft-delete + worker SELECT,INSERT,UPDATE). CHECK+partial-index+uq theo §8/§11.2. Drizzle parity dashboard.ts + export additive schema/index.ts. Đăng ký 3 bảng vào rls-registry.ts. Int-spec RED-trước cross-tenant deny configs/cache + migration-smoke. Additive KHÔNG drop dashboard/report/mv cũ.","builder":"db-migration","paths":["apps/api/migrations/**","apps/api/src/db/schema/**","apps/api/test/integration/**","docs/plans/S4-DASH-DB-1.md"]}]
acceptanceChecks: ["File apps/api/migrations/0482_s4_dashdb1_dashboard_core.sql tồn tại; _journal.json entry mới idx=162, when=1717500805000 > 1717500800000 (0481), ĐƠN ĐIỆU (đọc _journal.json xác nhận trước khi tạo).","3 bảng được tạo: dashboard_widgets (company_id NULLABLE + RLS ENABLE+FORCE + policy nullable-tenant + GRANT app SELECT) · dashboard_widget_configs (company_id NOT NULL + RLS ENABLE+FORCE + policy literal-GUC) · dashboard_widget_cache (company_id NOT NULL + RLS ENABLE+FORCE + policy literal-GUC) — BẤT BIẾN #1 (docs DB-07 §4.3).","Policy tenant_isolation đúng loại: nullable-tenant (USING company_id=GUC OR IS NULL / WITH CHECK company_id=GUC) cho dashboard_widgets; literal-GUC (USING=WITH CHECK company_id=GUC) cho configs+cache — copy mẫu 0479, tương thích PgBouncer txn-mode set_config.","Index đủ theo DB-07 §8/§11.2: configs (company_id,dashboard_type,is_enabled,sort_order)+role+user+widget; cache uq(company_id,cache_key)+expires(company_id,status,expires_at)+scope_ref+lookup; widgets uq global/company widget_code + (module_code,status,sort_order) + required_permission_code.","CHECK constraint đủ: widgets(module_code,widget_type,default_data_scope,status); configs(dashboard_type,config_scope,data_scope_override,role_user_scope tương hỗ); cache(dashboard_type,cache_scope,status,expires_at>=generated_at).","GRANT least-privilege: dashboard_widgets app SELECT-only; dashboard_widget_configs app SELECT-only (write=DASH-BE); dashboard_widget_cache app SELECT,INSERT,UPDATE (runtime upsert+soft-delete) — KHÔNG cấp thừa DELETE/UPDATE ngoài nhu cầu.","Cache KHÔNG lưu dữ liệu nhạy cảm ngoài scope/chưa mask: DDL COMMENT ghi rõ invariant (ép ở DASH-BE §9.7 step6) + cache_key composition (company+dashboard_type+widget_code+user/scope) tài liệu hoá trong migration.","apps/api/src/db/schema/dashboard.ts thêm mới + export trong schema/index.ts (APPEND additive, KHÔNG rewrite khối cũ); typecheck XANH.","rls-registry.ts đăng ký đủ 3 bảng; rls-coverage-assert.int-spec (\"không bảng company_id thiếu case\") + rls-guards.int-spec XANH trên LANE_DB.","migration-smoke 0000→head(0482) XANH trên DB tươi cô lập (hasDb && LANE_DB); KHÔNG xanh-giả (.env-only ⇒ đỏ-giả bị chặn bởi gate CỨNG).","FULL gate PASS: database-reviewer + rls-tenant-isolation-tester + silent-failure-hunter; coverage int-spec ≥ ngưỡng nhạy cảm.","Additive-only (DoD §8): git diff KHÔNG có DROP TABLE/POLICY; module dashboard cũ (dashboard.service/report.service/mv-dashboard.service/alerts.service, mv 0102/0103) KHÔNG bị đụng — reconcile park report/mv ngoài widget MVP."]
testTasks: ["RED-trước (deny-path RLS): dashboard-config-cache-rls.int-spec.ts — withTenant(A) SELECT dashboard_widget_configs/dashboard_widget_cache của tenant B ⇒ 0 row; no tenant context ⇒ 0 row (configs+cache tenant-scoped, KHÔNG skipNoContext). Gate CỨNG describe.skipIf(!(hasDb && !!process.env.LANE_DB)).","rls-registry.ts: thêm case dashboard_widgets (skipNoContext nullable-tenant) + dashboard_widget_configs + dashboard_widget_cache — rls-coverage-assert.int-spec XÁC NHẬN không bảng company_id nào thiếu case.","migration-smoke.int-spec 0000→0482 áp đơn điệu XANH trên mediaos_dashdbcore (drizzle migrator theo when) — chống drift head (memory dev-online-db-migration-drift).","Constraint test: uq (company_id,cache_key) chặn 2 cache cùng key/company; CHECK expires_at<generated_at bị REJECT; CHECK cache_scope/status/dashboard_type ngoài enum bị REJECT; configs CHECK role_user_scope (config_scope=Role mà role_id NULL) bị REJECT.","Cô lập DB verify: bash scripts/lane-db-setup.sh dashdbcore → export LANE_DB=mediaos_dashdbcore → pnpm --filter @mediaos/api test (KHÔNG set LANE_DB = xanh-giả/đỏ-giả)."]
steps: ["ĐỌC apps/api/migrations/meta/_journal.json NGAY TRƯỚC khi tạo file để neo head: xác nhận entry cuối = idx 161 / tag 0481_s4_notiseed1_event_template_perms / when 1717500800000. File mới = 0482_s4_dashdb1_dashboard_core.sql → drizzle-kit sinh journal idx 162, when 1717500805000 (bước +5000, ĐƠN ĐIỆU > 0481). KHÔNG hard-code, KHÔNG chèn giữa hai migration đã có.","Viết DDL bảng 1 dashboard_widgets (DB-07 §8.1): company_id NULLABLE (NULL=global catalog, NOT NULL=company custom phase sau) REFERENCES companies ON DELETE CASCADE; cột canonical theo §8.1 — widget_code, module_code, name, description, widget_type, required_permission_code, default_data_scope, data_source_key, component_key, default_refresh_seconds, is_cacheable, default_width/height, default_config/empty_state_config/action_config jsonb, status, is_system_widget, sort_order, metadata + audit (created_at/by, updated_at/by) + soft-delete (deleted_at/by). CHECK module_code∈(AUTH,HR,ATT,LEAVE,TASK,DASH,NOTI,SYSTEM) · widget_type∈(Summary,List,Chart,Calendar,Action,Alert) · default_data_scope∈(Own,Team,Department,Project,Company,System) · status∈(Active,Inactive,Deprecated). ENABLE+FORCE RLS + policy tenant_isolation NULLABLE-TENANT (USING company_id=NULLIF(current_setting('app.current_company_id',true),'')::uuid OR company_id IS NULL / WITH CHECK company_id=GUC) — copy nguyên mẫu 0479 notification_events. uq global (widget_code) WHERE company_id IS NULL AND deleted_at IS NULL · uq company (company_id,widget_code) WHERE NOT NULL · idx (module_code,status,sort_order) · idx (required_permission_code). GRANT SELECT app+worker (write company-custom = DASH-BE sau).","Viết DDL bảng 2 dashboard_widget_configs (DB-07 §8.2): company_id NOT NULL DEFAULT NULLIF(current_setting('app.current_company_id',true),'')::uuid REFERENCES companies CASCADE; widget_id NOT NULL REFERENCES dashboard_widgets, dashboard_type, role_id→roles, user_id→users, config_scope, is_enabled, sort_order, layout_x/y/width/height, data_scope_override, refresh_seconds_override, config jsonb + audit + soft-delete. CHECK dashboard_type∈(Employee,Manager,HR,Admin,System,Project) · config_scope∈(Company,Role,User) · data_scope_override NULL OR ∈(6 scope) · role_user_scope (Company⇒role/user NULL; Role⇒role NOT NULL,user NULL; User⇒user NOT NULL). ENABLE+FORCE RLS + policy literal-GUC (mirror 0479 notification_delivery_logs). idx (company_id,dashboard_type,is_enabled,sort_order) · idx role · idx user · idx widget (§11.2). GRANT SELECT app (config-update endpoint = DASH-BE sau — note trong DDL).","Viết DDL bảng 3 dashboard_widget_cache (DB-07 §8.3): company_id NOT NULL DEFAULT GUC REFERENCES companies CASCADE; widget_id NOT NULL REFERENCES dashboard_widgets, dashboard_type, user_id→users, role_id→roles, cache_scope, scope_reference_id uuid (polymorphic — KHÔNG FK), cache_key NOT NULL, data jsonb NOT NULL, data_hash, status, generated_at NOT NULL, expires_at NOT NULL, last_accessed_at, error_message, source_version, metadata + created_at/updated_at + deleted_at/by. CHECK dashboard_type · cache_scope∈(6) · status∈(Fresh,Stale,Expired,Error) · time (expires_at>=generated_at). ENABLE+FORCE RLS + policy literal-GUC. uq (company_id,cache_key) WHERE deleted_at IS NULL · idx lookup (company_id,widget_id,dashboard_type,cache_scope,scope_reference_id) · idx (company_id,user_id,widget_id) WHERE user_id NOT NULL · idx (company_id,status,expires_at) · idx (company_id,cache_scope,scope_reference_id). GRANT SELECT,INSERT,UPDATE app (runtime upsert cache + soft-delete invalidation) + SELECT,INSERT,UPDATE worker. DDL COMMENT invariant: cache CHỈ chứa dữ liệu đã mask/trong-scope (ép ở DASH-BE §9.7 step6) + cache_key gồm company+dashboard_type+widget_code+user/scope.","Tạo drizzle schema apps/api/src/db/schema/dashboard.ts PARITY với migration (inference thuần, ghi rõ RLS/grant/partial-index sống ở migration là chuẩn — KHÔNG db:generate). Export additive trong schema/index.ts (khối `export * from \"./dashboard\";` — APPEND, KHÔNG rewrite).","Đăng ký 3 bảng vào apps/api/test/integration/rls-registry.ts (RLS_TABLES, APPEND cuối, mirror block NOTI 0479): dashboard_widgets → skipNoContext:true (nullable-tenant, hàng global hiển thị mọi ngữ cảnh) seed 1 hàng company-scoped; dashboard_widget_configs → tenant-scoped (KHÔNG skipNoContext) seed widget+config; dashboard_widget_cache → tenant-scoped seed widget+cache. Helper seedDashboardWidget(direct,companyId) trả widgetId để cache/config FK.","Viết int-spec RED-trước apps/api/test/integration/dashboard-config-cache-rls.int-spec.ts: gate CỨNG describe.skipIf(!(hasDb && !!process.env.LANE_DB)); cross-tenant deny — withTenant(A) KHÔNG thấy configs/cache của tenant B; no-context ⇒ 0 row configs/cache; uq (company_id,cache_key) chặn trùng; CHECK expires<generated bị từ chối. Chạy trên DB cô lập: bash scripts/lane-db-setup.sh dashdbcore → export LANE_DB=mediaos_dashdbcore → pnpm --filter @mediaos/api test.","Hand-author docs/plans/S4-DASH-DB-1.md (khối ```yaml máy-đọc: lanes/acceptanceChecks/testTasks/steps) để bake plan-context + tái dùng lần sau; ghi rõ seed catalog + audit object_type PARK sang DASH-SEED-1/DASH-BE.","Verify FULL gate: database-reviewer + rls-tenant-isolation-tester (rls-guards.int-spec + rls-coverage-assert.int-spec + migration-smoke.int-spec XANH trên LANE_DB). Đối chiếu additive: git diff không DROP bảng/policy cũ; dashboard.service/report.service/mv-dashboard.service KHÔNG bị đụng."]
```

GAP-ANALYSIS: Chưa có schema code cho DASH widget-registry — 3 bảng dashboard_widgets/dashboard_widget_configs/dashboard_widget_cache là NET-NEW (không có trong apps/api/src/db/schema). Module apps/api/src/dashboard hiện có = ĐƯỜNG BÁO CÁO CŨ (G14): dashboard.service.ts (aggregate summary read-only, gated read:dashboard mig 0100), report.service.ts + mv-dashboard.service.ts + dashboard-refresh.service.ts (materialized views mig 0102/0103), alerts.service.ts. Theo done_when RECONCILE: GIỮ dashboard.service (khớp tinh thần DB-07 §4.9 không lưu dữ liệu gốc), PARK report/mv/alerts (đường MV ngoài phạm vi widget MVP), migration ADDITIVE KHÔNG drop bất cứ bảng/MV/policy nào.

DRIFT WO↔SPEC (spec thắng, CLAUDE.md §1): WO done_when đặt tên rút gọn (widget_name/source_modules/required_permission) — DB-07 §8.1 canonical là name/module_code/required_permission_code. Builder DÙNG tên DB-07 §8.1/8.2/8.3. WO nói "DB-02 §9.7" cho cặp permission nhưng DASH thực nằm ở DB-02 §9.6 (§9.7=NOTI) — sai chiếu nhẹ; không ảnh hưởng vì SEED permission được PARK (xem dưới).

HEAD/MIGRATION (neo thật, chống drift): _journal.json entry cuối idx 161 / 0481_s4_notiseed1_event_template_perms / when 1717500800000. File mới = 0482_s4_dashdb1_dashboard_core, idx 162, when 1717500805000 (+5000). PHẢI đọc lại _journal.json ngay trước khi db:generate — nếu có WO khác land trước, dịch số. Chèn giữa 2 migration đã có ⇒ journal mất đơn điệu ⇒ lane tươi xanh-giả nhưng dev-online/prod (đã áp head) BỎ QUA 0482 theo when ⇒ bảng dashboard không tồn tại ⇒ 500 runtime.

INVARIANTS: #1 RLS+FORCE + policy TẠO TRƯỚC mọi INSERT — copy nguyên 2 mẫu policy từ 0479: nullable-tenant (dashboard_widgets, catalog global company_id NULL đọc dùng-chung, ghi global chỉ qua owner-bypass tại migrate-time, app role chỉ ghi row tenant qua WITH CHECK) + literal-GUC (configs/cache company_id NOT NULL DEFAULT GUC). #2 KHÔNG hard-delete: cả 3 bảng có deleted_at/by (configs/cache soft-delete; cache invalidation = UPDATE status='Stale'/soft-delete, KHÔNG DELETE). cache/configs KHÔNG append-only (được UPDATE). #3 cache/config KHÔNG chứa secret/dữ liệu nhạy cảm chưa mask — ép ở DASH-BE service (DB-07 §4.6/§4.10/§9.7 step6), DDL chỉ ghi COMMENT invariant. UUID PK + timestamptz UTC-at-rest (ADR-0008).

GRANT (least-privilege, mirror precedent NOTI): dashboard_widgets app SELECT-only (custom-widget write=DASH-BE) · dashboard_widget_configs app SELECT-only (config-update endpoint=DASH-BE, tránh cấp write sớm) · dashboard_widget_cache app SELECT,INSERT,UPDATE (đường load-dashboard §9.5 step7 upsert cache khi miss + invalidation UPDATE) + worker SELECT,INSERT,UPDATE (refresh job §9.7). database-reviewer chốt lại nếu muốn siết cache về worker-only.

VERIFY/GATE: FULL gate (zone=red, crown — RLS/policy) = database-reviewer + rls-tenant-isolation-tester + silent-failure-hunter. Test RED-trước deny-path cross-tenant configs/cache. Gate CỨNG hasDb && !!process.env.LANE_DB (chỉ .env ⇒ hasDb=true = đỏ-giả — memory integration-test-lane-db-gate). rls-coverage-assert bắt buộc 3 bảng có case trong rls-registry (nếu thiếu = lưới thủng im lặng). migration-smoke 0000→head phải xanh (drizzle áp theo when đơn điệu).

OUT-OF-SCOPE (chống scope-creep, PARK sang WO sau — theo precedent TASK: DB 0478→SEED 0480; NOTI: DB 0479→SEED 0481): (1) SEED widget catalog (12 widget MVP DB-07 §8.1 dòng 1110-1123) + template + grant DASH.WIDGET.*/DASH.CONFIG.* → PARK sang S4-DASH-SEED-1. Lý do: role_permissions grant cần cặp (action,resource_type) tường minh; DB-02 §9.6 chỉ cho mã DOTTED (DASH.WIDGET.VIEW_MY_TASKS) KHÔNG phải tuple engine — bịa tuple = vi phạm done_when. Khi seed thật: PHẢI dùng RAISE EXCEPTION fail-LOUD nếu role/permission thiếu (mirror 0480/0481 DO-block), super-admin KHÔNG enumerate (bootstrap runtime). (2) audit object_type 'dashboard-widget-config' (DASH_WIDGET_CONFIG_UPDATED §9.6) → PARK sang DASH-BE (append hot-file audit object_types CHECK khi endpoint write land). (3) dashboard_user_widget_states + dashboard_cache_invalidations (DB-07 §8.4/§8.5 phase sau) → KHÔNG làm MVP. (4) required_permission_code chỉ là varchar (chuỗi mã dotted) — KHÔNG FK sang permissions; hợp lệ để seed catalog sau mà không cần tuple.

HOT-FILE & MERGE: schema/index.ts (APPEND export ./dashboard) · rls-registry.ts RLS_TABLES (APPEND cuối, sau block NOTI) · _journal.json (drizzle tự sinh, ĐỪNG sửa tay). Lane serial DUY NHẤT (db-migration) — migration đánh số đơn điệu, không song song.
</content>
