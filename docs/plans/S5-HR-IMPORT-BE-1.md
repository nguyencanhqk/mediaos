```yaml
wo: S5-HR-IMPORT-BE-1
zone: red
generated_by: auto-loop
reconciled_at: "df39364d"
lanes: [{"id":"mig-import-perm-audittype","task":"[db-migration · CROWN/RED · NỐI TIẾP TRƯỚC] ĐỌC LẠI apps/api/src/db/meta/_journal.json head THẬT ngay trước khi mint (head hiện = 0495 idx 175, when 1717500870000 → WO = 0496 idx 176, when 1717500875000; tránh double-mint dải 0494+). Viết 0496_s5_hr_import_be1_import_employee_sensitive.sql: (a) FLIP is_sensitive false→true cho cặp EXACT ('import','employee') — cặp ĐÃ tồn tại từ 0019 dòng 23 ('import','employee',false): INSERT ... ON CONFLICT DO NOTHING (no-op giữ DB dựng-mới) + UPDATE permissions SET is_sensitive=true WHERE (action,resource_type)=('import','employee') AND is_sensitive IS DISTINCT FROM true (mẫu 0492 export:employee / 0476 delete:user). (b) Grant per-(permission,role) data_scope='Company', effect ALLOW cho role hr + company-admin (system role: company_id IS NULL, is_system=true) qua DO-block resolve-by-attribute, ON CONFLICT(role_id,permission_id,effect) DO NOTHING (bài học §13 per-(permission,role), mẫu 0476 §c). (c) Union-add audit object_type 'employee_import' vào CHECK audit_logs qua DO-block ADD-only idempotent (mẫu 0456) + APPEND 'employee_import' vào AUDIT_OBJECT_TYPES (apps/api/src/db/schema/audit.ts:99) CÙNG commit. (d) Cập nhật docs/permission-matrix-spec.md: thêm hàng import:employee (hr + company-admin scope Company, sensitive=true) CÙNG PR. (e) Fail-LOUD verify cuối file (mẫu 0466/0476): RAISE EXCEPTION nếu pair KHÔNG is_sensitive=true, thiếu grant hr/company-admin × ALLOW × Company, hoặc object_type chưa vào CHECK. KHÔNG db:generate (SQL tay 04xx). KHÔNG đụng RLS/FORCE/policy/schema table.","builder":"db-migration","paths":["apps/api/migrations/**","apps/api/src/db/schema/audit.ts","docs/permission-matrix-spec.md"]},{"id":"contracts-import-dto","task":"[backend-builder] Contracts import (schema MỚI trong packages/contracts/src/hr/, KHÔNG tái dùng legacy importEmployeeRowSchema media-era ở packages/contracts/src/employees.ts): hrEmployeeImportRowSchema (cột chuẩn cho create UNLINKED employee — structural fields tái dùng từ createHrEmployeeSchema: employeeCode? orgUnit/position/jobLevel/contractType theo id HOẶC theo tên [chốt trong plan], workType/employmentType/salaryType, startDate/endDate; email chỉ dùng DUP-CHECK, KHÔNG provision user) + IMPORT_COLUMN_ORDER (một nguồn sự thật cho parser + template) + hrImportReportSchema (per-row {row, errors[]}, counts {ok, fail}, dryRun) + hrImportResultSchema (apply: created[], skipped[], sessionAuditId). Export qua barrel. Dual-build (ESM/CJS).","builder":"backend-builder","paths":["packages/contracts/src/**"]},{"id":"be-hr-import","task":"[backend-builder · CROWN/RED] (1) HrEmployeeImportService MỚI (file <800 dòng): parse XLSX bằng exceljs (MIT, thuần JS — CHỐT 2026-07-13, CẤM SheetJS/xlsx: advisory prototype-pollution/ReDoS + rời npm registry chính) / CSV bằng csv-parse@6.2.1 (đã có); MIME + extension (xlsx/csv) validate TƯỜNG MINH trong service (FileInterceptor/multer limits chỉ ép SIZE ~5MB, mirror employees.controller) → sai MIME trả 400 KHÔNG 500; validate TỪNG DÒNG bằng hrEmployeeImportRowSchema + duplicate email/mã TRONG file lẫn với DB. dryRun=true (mặc định preview) → report per-row, KHÔNG ghi, KHÔNG gọi SequenceService (không nhảy sequence_counters.current_value), 0 audit. apply → PARTIAL-SUCCESS: MỖI DÒNG = 1 giao dịch RIÊNG qua HrWriteService.createFromImportTx (tự bọc withTenant mỗi lần) — KHÔNG gộp cả lô trong 1 withTenant; dòng lỗi skip + report per-row; audit 'create' per-employee (sẵn có) + đúng 1 audit object_type='employee_import' {fileName, ok, fail} ghi SAU vòng lặp (KHÔNG PII/secret vào before/after — BẤT BIẾN #3). (2) HrWriteService.createFromImportTx (thin): BYPASS resolveUserId + nhánh provision, user_id=NULL, KHÔNG tạo/link user, KHÔNG phát outbox activation event (createHrEmployeeSchema:57 + resolveUserId hr-write.service.ts:535 hiện ép userId|email) — plan-reviewer/security-reviewer soi. (3) Controller MỚI hr-import.controller.ts gate PermissionGuard('import','employee',{isSensitive:true}): POST /api/v1/hr/employees/import + GET /api/v1/hr/employees/import/template (dựng từ IMPORT_COLUMN_ORDER, Content-Disposition attachment). (4) DỌN LEGACY CÙNG LANE: gỡ EmployeesController.importEmployees/confirmImport (employees.controller.ts:81-100) + EmployeesService.parseImportPreview/confirmImport (employees.service.ts:484-632) + import DTO/Valkey-staging chết theo — GREP consumer FE (apps/console · apps/app · packages/web-core registry) route /employees/import TRƯỚC khi gỡ (bài học review-gate-blind-to-deletions). (5) Wire provider/controller vào employees.module.ts (additive). (6) Thêm dep exceljs vào apps/api/package.json + pnpm install cập nhật pnpm-lock.yaml (paths lane ĐÃ nới — builder pnpm install trong worktree).","builder":"backend-builder","paths":["apps/api/src/employees/**","apps/api/package.json","pnpm-lock.yaml"]},{"id":"int-hr-import","task":"[backend-builder · RED-TRƯỚC · lane DB cô lập] (A) CHỐNG canonical-seed-pin regression (bài học canonical-seed-pin): APPEND {action:'import', resourceType:'employee', sensitive:true} vào REQUIRED_CATALOG + 2 dòng SEED_GRANTS {role:'hr'|'company-admin', action:'import', resourceType:'employee', scope:'Company'} trong apps/api/test/integration/auth-seed-canonical-roles.int-spec.ts (mirror NGUYÊN mẫu export:employee — SEED_GRANTS dòng 72-73, REQUIRED_CATALOG ~dòng 113-114) — nếu KHÔNG append, pin đỏ ngay sau khi 0496 flip is_sensitive. (B) hr-employee-import.int-spec.ts MỚI (real AppModule + supertest + REAL permission engine, gate hasDb && LANE_DB): deny-path (thiếu import:employee → 403 + 0 employee_profiles + 0 audit) · cross-tenant deny (import company A không ghi sang B) · dryRun mặc định không ghi + sequence_counters.current_value KHÔNG đổi + 0 insert + 0 audit · sai MIME → 400 (không 500) · duplicate TRONG file (email/mã) bị bắt · duplicate với DB (mã trùng employee CHƯA-xoá-mềm) bị bắt + unique partial index backstop không insert dup · apply happy-path N dòng → N employee UNLINKED (user_id null), COUNT(users) KHÔNG đổi, 0 outbox activation event, mã tăng đơn điệu qua SequenceService, N audit 'create' + đúng 1 audit object_type='employee_import' {ok,fail,fileName} · partial-success dòng lỗi skip + report đúng · assert legacy /employees/import + /import/confirm bị vô hiệu (403/gỡ).","builder":"backend-builder","paths":["apps/api/test/integration/**"]}]
acceptanceChecks: ["meta/_journal.json thêm idx 176, tag 0496_*, when 1717500875000; drizzle migrate áp SẠCH trên cả DB mới lẫn DB đã có 0019 (không vỡ, idempotent chạy lại 0 đổi).","SELECT is_sensitive FROM permissions WHERE (action,resource_type)=('import','employee') = true (đã flip từ 0019 false); role_permissions trả ĐÚNG 2 grant hr + company-admin × ALLOW × data_scope='Company'; DO-block fail-LOUD hiện diện.","AUDIT_OBJECT_TYPES (apps/api/src/db/schema/audit.ts) chứa 'employee_import' VÀ CHECK audit_logs chấp nhận nó (INSERT audit phiên KHÔNG lỗi 23514).","auth-seed-canonical-roles.int-spec.ts: REQUIRED_CATALOG có {import,employee,sensitive:true} + SEED_GRANTS có 2 dòng hr/company-admin import:employee scope Company → pin test GREEN sau khi 0496 flip (không canonical-seed-pin regression).","docs/permission-matrix-spec.md có hàng import:employee (hr + company-admin Company, sensitive) — cập nhật CÙNG PR.","POST /api/v1/hr/employees/import gate PermissionGuard('import','employee',{isSensitive:true}); caller thiếu cặp → 403 + 0 employee_profiles + 0 audit (deny = 0 side effect).","dryRun=true (mặc định) → 200 report {row,errors,ok,fail}, KHÔNG insert, sequence_counters.current_value KHÔNG đổi, 0 audit; sai MIME/extension → 400 (không 500).","apply → mỗi employee tạo qua HrWriteService.createFromImportTx (per-row withTenant tx) có employeeCode auto từ SequenceService + 1 audit 'create' object_type='employee'; employee UNLINKED (user_id null), COUNT(users) KHÔNG đổi, 0 outbox activation event; đúng 1 audit object_type='employee_import' {fileName, ok, fail}; dòng lỗi skip + report (partial-success).","Validate per-row bằng hrEmployeeImportRowSchema; duplicate email/mã TRONG file + với DB được bắt và báo cáo; unique index employee_profiles_company_code_active_uq (WHERE deleted_at IS NULL AND employee_code IS NOT NULL — CHƯA-xoá-mềm, gồm resigned/terminated) là backstop cuối (không insert dup, không 500).","GET /hr/employees/import/template trả template tải được (Content-Disposition attachment) với cột khớp IMPORT_COLUMN_ORDER (một nguồn sự thật parser+template).","Legacy /employees/import + /employees/import/confirm bị vô hiệu (gỡ) — grep xác nhận không còn consumer sống FE; grant cặp không mở lại đường bypass media-era (không SequenceService/không audit).","exceljs (MIT) thêm vào apps/api/package.json + pnpm-lock.yaml cập nhật; KHÔNG có SheetJS/xlsx.","DoD §8: input validation (Zod), permission guard, có int-spec, audit hành động quan trọng, FULL gate PASS, file <800 dòng, cập nhật harness/backlog.mjs."]
testTasks: ["RED deny-path: user KHÔNG có import:employee → POST /hr/employees/import trả 403; assert 0 row employee_profiles + 0 audit_logs (deny trước mọi side effect).","RED cross-tenant deny: actor company A, seed org_unit/employee company B; import file A không ghi sang B; report/preview scoped theo company_id + RLS.","RED dryRun no-write: dryRun mặc định → 200 report; assert sequence_counters.current_value KHÔNG đổi + 0 insert + 0 audit.","RED MIME/extension sai: upload file MIME/đuôi không phải xlsx/csv → 400 (KHÔNG 500), 0 ghi.","RED duplicate in-file: 2 dòng cùng employeeCode/email → cả 2 flag trong report; apply skip theo partial-success.","RED duplicate DB: dòng employeeCode trùng employee CHƯA-xoá-mềm → flag; unique partial index chặn insert dup (không 500).","RED apply happy-path: N dòng hợp lệ → N employee UNLINKED (user_id null), COUNT(users) KHÔNG đổi, 0 outbox activation event, mã tăng đơn điệu qua SequenceService, N audit 'create' + đúng 1 audit 'employee_import' {ok,fail,fileName}.","RED partial-success: file trộn dòng hợp lệ + lỗi → dòng hợp lệ tạo (tx riêng không rollback lẫn nhau), dòng lỗi report, session audit ok/fail đúng.","Canonical-pin: chạy auth-seed-canonical-roles.int-spec.ts sau append REQUIRED_CATALOG+SEED_GRANTS import:employee → GREEN (chống pin regression).","QA-03 alignment + assert legacy import route vô hiệu (403/gỡ); gate hasDb && LANE_DB; chạy như CI bằng bash harness/check.sh --lane-db trước PR đỏ."]
steps: ["ĐỌC LẠI apps/api/src/db/meta/_journal.json ngay trước khi mint (head phải = 0495 idx 175, when 1717500870000). Lane mig-import-perm-audittype (NỐI TIẾP TRƯỚC): viết 0496 flip is_sensitive('import','employee') false→true + grant hr/company-admin per-pair Company + union-add object_type 'employee_import' (SQL CHECK + AUDIT_OBJECT_TYPES) + cập nhật docs/permission-matrix-spec.md + fail-LOUD verify. Áp trên DB cô lập, kiểm idempotent (chạy 2 lần = 0 đổi).","Lane contracts-import-dto: hrEmployeeImportRowSchema + IMPORT_COLUMN_ORDER + report/result schema (schema MỚI trong packages/contracts/src/hr/, KHÔNG tái dùng legacy employees.ts). Build contracts (dual).","Lane be-hr-import: thêm dep exceljs (MIT) vào apps/api/package.json + pnpm install; HrEmployeeImportService (parse xlsx exceljs/csv csv-parse, MIME+extension+size check tường minh, validate per-row, dup in-file+DB, dryRun preview no-write, apply per-row withTenant partial-success reuse createFromImportTx unlinked never-provision, session audit) + HrWriteService.createFromImportTx (bypass resolveUserId, user_id null, 0 outbox) + hr-import.controller.ts (POST import + GET template) + GỠ legacy import route/service (grep FE consumer trước) + wire module additive.","Lane int-hr-import (RED-first): (A) APPEND import:employee vào auth-seed-canonical-roles.int-spec.ts REQUIRED_CATALOG + SEED_GRANTS (chống pin đỏ); (B) viết hr-employee-import.int-spec.ts deny/cross-tenant/dryRun/MIME/dup-in-file/dup-DB/apply/partial/session-audit; chạy RED trước impl, rồi GREEN với LANE_DB.","Vòng: RED → GREEN → FULL gate (security-reviewer + database-reviewer + silent-failure-hunter + santa-method crown) → checkpoint. Trước PR đỏ: bash harness/check.sh --lane-db (deny-path/IDOR/cross-tenant chạy thật). Migrate+deploy cùng đợt (N=1) tránh cửa sổ 403 cho role chỉ-wildcard.","plan-reviewer đối kháng chốt các điểm còn mở trong plan: cột orgUnit/position theo id vs tên · nội dung template endpoint · biên độ lỗi report per-row. Các CHỐT đã khóa (KHÔNG mở lại): exceljs (CẤM SheetJS) · per-row tx partial-success · createFromImportTx unlinked/never-provision · DEFER SENSITIVE_CAPABILITY_ALLOWLIST sang FE WO."]
```

RECONCILE-REFRESH 2026-07-13 (reused=false — code KHÔNG đổi nhưng WO đã REFINE, nhiều CHỐT khóa + paths thiếu ở plan cũ). ĐÃ VERIFY code hiện tại khớp gap-analysis cũ; cập nhật 4 điểm lệch:

DELTA vs plan cũ:
- +docs/permission-matrix-spec.md vào lane migration (WO paths + done_when 'cập nhật CÙNG PR'; plan cũ thiếu ở mọi lane).
- +apps/api/package.json + pnpm-lock.yaml vào lane be-hr-import (WO nới paths cho dep exceljs; plan cũ be lane chỉ có employees/**).
- Lane int-hr-import: THÊM task tường minh APPEND import:employee vào auth-seed-canonical-roles.int-spec.ts REQUIRED_CATALOG + SEED_GRANTS (mirror export:employee dòng 72-73 / ~113-114) — CHỐNG canonical-seed-pin regression (flip is_sensitive 0496 làm pin đỏ nếu không append). Plan cũ chỉ ngầm qua path test/integration/**.
- CHỐT khóa (plan cũ để plan-reviewer mở): exceljs MIT bắt buộc, CẤM SheetJS/xlsx; per-row withTenant tx partial-success qua createFromImportTx; SENSITIVE_CAPABILITY_ALLOWLIST DEFER sang FE WO (KHÔNG nới apps/api/src/permission/**, enforcement fail-closed không phụ thuộc allowlist).

VERIFIED (code khớp, không đổi): journal head 0495 idx 175 → WO=0496 idx 176 when 1717500875000. 0019:23 ('import','employee',false) tồn tại → FLIP không tạo mới. Legacy EmployeesController @Post('import')/import/confirm LIVE (controller 81-100, service parseImportPreview:484 + confirmImport:547) → media-era CSV/Valkey/employeeCode-từ-CSV/không-SequenceService/không-audit = bypass bất biến khi grant import:employee ⇒ PHẢI gỡ cùng lane, grep FE consumer trước. createHrEmployeeSchema.refine employee-write.ts:57 ép userId|email; resolveUserId hr-write.service.ts:535 (WO cite :552 = drift dòng, structural đúng) → cần createFromImportTx bypass. AUDIT_OBJECT_TYPES audit.ts:99, chưa có 'employee_import'. csv-parse@6.2.1 + multer@2 đã có, exceljs CHƯA. unique index employee_profiles_company_code_active_uq (schema/employees.ts:101-103) WHERE deleted_at IS NULL AND employee_code IS NOT NULL = phạm vi dup-check (CHƯA-xoá-mềm gồm resigned/terminated, KHÔNG theo status=active).

INVARIANTS (CLAUDE.md §2): #1 mọi query qua withTenant(companyId) + RLS+FORCE (createEmployee/SequenceService tuân); import KHÔNG cross-tenant. #2 không hard-delete; audit_logs append-only — chỉ union-add object_type, migration UPDATE flip in-place giữ permission id ⇒ FK role_permissions.permission_id nguyên; KHÔNG rewrite catalog/mảng AUDIT_OBJECT_TYPES (APPEND). #3 không secret/PII vào audit before/after; audit 'employee_import' chỉ {fileName,ok,fail}; report import không lộ row nhạy cảm ra role không quyền.

DEPS/THỨ TỰ: mig-import-perm-audittype (NỐI TIẾP TRƯỚC, đơn điệu theo head 0495) → contracts-import-dto → be-hr-import (depends mig + contracts) → int-hr-import (RED-first, depends mig cho seed/audit-type + be cho GREEN). Migrate+deploy cùng đợt (N=1) tránh cửa sổ 403 role chỉ-wildcard sau flip.

HOT-FILE & MERGE: migrations/** = file mới 0496 (append). schema/audit.ts:99 AUDIT_OBJECT_TYPES = APPEND 'employee_import' (KHÔNG rewrite). docs/permission-matrix-spec.md = APPEND hàng. employees.module.ts = wire additive. hr-write.service.ts = thêm createFromImportTx (crown, KHÔNG đổi hành vi createEmployee mặc định). packages/contracts barrel = APPEND export. auth-seed-canonical-roles.int-spec.ts = APPEND 1 catalog + 2 grant. apps/api/package.json = APPEND exceljs dep.

VERIFY/GATE: FULL gate (security-reviewer + database-reviewer + silent-failure-hunter + santa-method crown). Model routing: crown ⇒ Opus code+review, planner Sonnet5 xhigh micro-plan → plan-reviewer đối kháng TRƯỚC code. Int-spec chạy hasDb && LANE_DB; bash harness/check.sh --lane-db trước PR đỏ.

OUT-OF-SCOPE: liên kết tài khoản hàng loạt (HR-FUNC-011 làm tay sau, createFromImportTx never-provision) · FE import screen (S5-HR-IMPORT-FE-1) · SENSITIVE_CAPABILITY_ALLOWLIST append (DEFER FE WO) · PAYROLL/module khác. KHÔNG bịa lane ngoài spec.
</content>
