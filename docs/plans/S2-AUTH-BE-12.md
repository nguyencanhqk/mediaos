```yaml
wo: S2-AUTH-BE-12
zone: red
generated_by: auto-loop
reconciled_at: "68616f9"
lanes: [{"id":"L1-2fa-admin-controls-seq","task":"CROWN RED (auth/permission/secret/audit) — 1 LANE TUẦN TỰ DUY NHẤT (KHÔNG tách song song; vòng 1 block vì 2 lane cùng sửa auth-me-capabilities.int.spec.ts). Thứ tự trong lane BẮT BUỘC: (0) VERIFY read-only trong worktree ../mediaos-s2-auth-be-12 @42fcfd6 (mig 0466 + requireTwoFactor OR-per-user); (1) CONTRACTS packages/contracts additive — authUserDetailSchema = authUserSchema.extend({twoFactor:{enabled:boolean, requiredByRole:boolean, requiredByUser:boolean}}) + AuthUserDetailDto (DTO RIÊNG cho GET /:id, KHÔNG chạm authUserSchema dùng cho list → tránh N+1 lộ 2FA mỗi row); updateAuthUserRequestSchema → fullName z.string().min(1).max(200).optional() + thêm requireTwoFactor z.boolean().optional(), GIỮ .strict() (non-breaking); auth.ts APPEND 'TOTP_RESET' vào SECURITY_EVENT_TYPES (append-only §2 #2) + gán SECURITY_EVENT_SEVERITY['TOTP_RESET']='high' (exhaustiveness Record → quên = typecheck ĐỎ); build contracts dual ESM/CJS xanh. (2) ALLOWLIST permission.service.ts — APPEND-only 'reset-2fa:user' vào SENSITIVE_CAPABILITY_ALLOWLIST (giữ 8 cặp cũ nguyên vẹn); KHÔNG nới enforcement (cổng thật vẫn PermissionGuard per-resource). (3) RED-TRƯỚC deny-path int-spec (chi tiết testTasks). (4) USERS API apps/api/src/users/** — PATCH /auth/users/:id (gate update:user hiện có) nhận requireTwoFactor optional → updateProfileTx set users.require_two_factor CÙNG tx + audit 'user.updated' diff cờ (authUserSnapshot APPEND require_two_factor); PLAN-FIX no-op: PATCH không đổi field nào (body rỗng / giá trị bằng cũ) → no-op KHÔNG ghi audit (so-sánh before/after; mẫu lock đã-locked); GET /auth/users/:id đổi getOne trả AuthUserDetailDto.twoFactor{enabled=user_totp.enabled_at!=null (join), requiredByRole=join roles.requires_two_factor ROLES-ONLY qua active user_roles — KHÔNG tái dùng requiresTwoFactorTx (đã gộp OR-user), requiredByUser=cột 0466 đọc thẳng}; KHÔNG lộ secret_ciphertext/encrypted_dek/iv_nonce/auth_tag. (5) RESET ENDPOINT — POST /auth/users/:id/2fa/reset @RequirePermission('reset-2fa','user',{isSensitive:true}) → AuthUsersService.resetTwoFactor: trong CÙNG withTenant tx xoá user_totp + user_recovery_codes của target (mig 0120 GRANT DELETE) + TÁI DÙNG AuthService.revokeAllForUserTx(tx,id,'2fa_reset') (import, KHÔNG viết lại) + audit 'user.2fa_reset' objectType 'user' kèm revoked_session_count (KHÔNG secret) + securityEvents.record TOTP_RESET; self-reset CHO PHÉP (KHÔNG assertNotSelf, owner chốt 2026-07-03); cross-tenant/không tồn tại → 404 no-op 0 audit. (6) SPECS — mở rộng auth-me-capabilities.int.spec.ts (1 lane nên không còn xung đột sở hữu). auth/** = REUSE-ONLY: CẤM sửa production auth.service.ts/two-factor.service.ts (chỉ import revokeAllForUserTx + sửa/append int-spec trong src/auth hoặc test/integration) — reviewer Đội 3 assert diff auth production = 0 dòng.","builder":"backend-builder","paths":["apps/api/src/users/**","apps/api/src/auth/**","apps/api/src/permission/**","packages/contracts/src/**","apps/api/test/integration/**"]}]
acceptanceChecks: ["TIỀN-ĐỀ (bước 0, read-only trong worktree @42fcfd6): mig 0466 (users.require_two_factor + pair reset-2fa:user is_sensitive=true + grant company-admin scope Company) + requiresTwoFactor OR-per-user đều có; thiếu → STOP needs_human (KHÔNG tự tạo migration / switch branch). [PRE-VERIFY PASS]","CẤU TRÚC 1-LANE: toàn bộ deliverable ship trong 1 lane tuần tự (contracts → allowlist → users API → reset → specs); KHÔNG có 2 lane cùng sửa auth-me-capabilities.int.spec.ts (nguyên nhân block vòng 1 đã loại).","PATCH /auth/users/:id {requireTwoFactor:true} (gate update:user) → users.require_two_factor=true set CÙNG tx + audit_logs 'user.updated' objectType 'user' before/after chứa diff cờ; GET detail sau đó twoFactor.requiredByUser=true.","PATCH no-op (body rỗng HOẶC requireTwoFactor==giá trị cũ) → KHÔNG ghi audit_logs (0 row mới); response vẫn 200 (hoặc idempotent) — 0 audit rác.","GET /auth/users/:id trả twoFactor {enabled (user_totp.enabled_at != null), requiredByRole (join roles.requires_two_factor ROLES-ONLY), requiredByUser (cột 0466)} — 3 nguồn TÁCH BIỆT (requiredByRole KHÔNG lẫn requiredByUser); response KHÔNG chứa secret_ciphertext/encrypted_dek/iv_nonce/auth_tag.","POST /auth/users/:id/2fa/reset gate @RequirePermission('reset-2fa','user',{isSensitive:true}): user thiếu grant → 403 + 0 thay đổi (user_totp/user_recovery_codes/user_sessions nguyên vẹn) + 0 audit rác.","Reset thành công (company-admin có grant Company): user_totp + user_recovery_codes của target rỗng + refresh token cũ trình lại → 401 + user_sessions của target revoked (revokeAllForUserTx gọi đúng 1 lần) + audit_logs 'user.2fa_reset' objectType 'user' kèm revoked_session_count (KHÔNG secret) + 1 row user_security_events TOTP_RESET.","Self-reset (actor==target) CHO PHÉP (KHÔNG assertNotSelf): thành công, user vẫn login; nếu requiredByRole HOẶC requiredByUser → me().mustSetupTwoFactor=true trở lại.","Cross-tenant / target không tồn tại → 404 (RLS che, no-op) với 0 audit + 0 security-event ghi ra.","SENSITIVE_CAPABILITY_ALLOWLIST APPEND 'reset-2fa:user' (8 cặp cũ giữ nguyên); /auth/me capabilities['reset-2fa:user']===true cho company-admin, VẮNG cho employee(0008)/manager(0010).","contracts: SECURITY_EVENT_TYPES APPEND 'TOTP_RESET' (append-only, không xoá/đổi mã cũ) + SECURITY_EVENT_SEVERITY exhaustive (typecheck xanh); updateAuthUserRequestSchema thêm requireTwoFactor optional + fullName optional, GIỮ .strict().","REUSE-ONLY auth/**: git diff apps/api/src/auth/auth.service.ts + two-factor.service.ts = 0 dòng production (chỉ import revokeAllForUserTx từ users module + sửa/append int-spec); reviewer Đội 3 assert.","BẤT BIẾN §2: mọi data-access qua withTenant(companyId) + WHERE company_id; audit + security-event ghi TRONG cùng tx với mutation (rollback → 0 orphan); tuyệt đối KHÔNG plaintext/ciphertext TOTP secret vào DTO/audit/log/security-event payload.","DoD §8: test deny-path RED trước; coverage ≥80% code nhạy cảm chạm; pnpm --filter @mediaos/api typecheck + build xanh; contracts build xanh; FULL gate (security-reviewer) PASS + người chốt red-zone."]
testTasks: ["deny-path RED viết-TRƯỚC — int-spec HTTP mới apps/api/test/integration/auth-user-2fa-reset.int-spec.ts (mẫu role-admin-two-factor.int-spec.ts, Nest app thật + supertest + seed helpers, gate cứng hasDb && LANE_DB, DB cô lập): (a) user thiếu grant reset-2fa:user → POST reset 403 + user_totp/user_recovery_codes/user_sessions bất biến + 0 audit; (b) cross-tenant (admin tenant B → target tenant A) → 404 no-op 0 audit; (c) reset thành công → user_totp+user_recovery_codes rỗng + refresh cũ 401 + user_sessions revoked + audit user.2fa_reset (revoked_session_count) + user_security_events TOTP_RESET + nếu requiredByRole thì victim me().mustSetupTwoFactor=true; (d) PATCH requireTwoFactor=true → GET detail twoFactor.requiredByUser=true + audit user.updated diff cờ; (e) PATCH no-op (body rỗng / giá trị bằng cũ) → 0 audit mới; (f) 2-tenant deny lặp lại xác nhận cô lập.","Mở rộng apps/api/src/auth/auth-me-capabilities.int.spec.ts (1 lane → không xung đột sở hữu): company-admin(0001) → /auth/me capabilities['reset-2fa:user']===true; employee(0008)/manager(0010) → VẮNG (least-privilege, per-pair); wildcard '*:*' KHÔNG kế thừa (sensitive gate).","Unit spec colocated (src/**/*.spec.ts để vitest gom — spec để test/unit KHÔNG chạy = xanh-giả): AuthUsersService.resetTwoFactor — xoá cả 2 bảng + gọi auth.revokeAllForUserTx đúng 1 lần + audit user.2fa_reset (payload KHÔNG secret) + emit TOTP_RESET; updateUser với requireTwoFactor → audit diff cờ; updateUser no-op → KHÔNG gọi audit.record; getUserDetail 3 cờ đúng nguồn (mock tx).","Unit/type: SecurityEventWriter chấp nhận TOTP_RESET (SECURITY_EVENT_SEVERITY exhaustive) — thêm mã mà quên severity → typecheck ĐỎ (fail-closed).","Regression: chạy toàn bộ auth + users suite (auth.service.spec, two-factor.service.spec, auth-users.service.spec, admin-users.service.spec, role-admin.service.spec + int-spec liên quan) trên LANE_DB → xanh (không vỡ login/lock/enroll)."]
steps: ["Bước 0 (VERIFY read-only TRONG worktree ../mediaos-s2-auth-be-12 @42fcfd6 — KHÔNG đọc cây gốc, KHÔNG tự tạo migration, KHÔNG chuyển nhánh): xác nhận (i) apps/api/migrations/0466_s2_authdb4_user_require_2fa_reset_perm.sql tồn tại (ALTER users ADD require_two_factor + INSERT pair reset-2fa:user is_sensitive=true + grant company-admin scope Company); (ii) apps/api/src/db/schema/users.ts có requireTwoFactor; (iii) two-factor.service.ts requiresTwoFactor đã OR users.require_two_factor. THIẾU bất kỳ → DỪNG needs_human. [ĐÃ PRE-VERIFY = PASS]","L1 CONTRACTS (chạy trước — foundation): packages/contracts/src/auth/user-admin.ts thêm authUserDetailSchema + AuthUserDetailDto; updateAuthUserRequestSchema fullName→optional + requireTwoFactor optional, GIỮ .strict(); packages/contracts/src/auth.ts APPEND 'TOTP_RESET' vào SECURITY_EVENT_TYPES + SECURITY_EVENT_SEVERITY['TOTP_RESET']='high'; pnpm --filter @mediaos/contracts build (dual ESM/CJS) + typecheck xanh.","L2 ALLOWLIST: apps/api/src/permission/permission.service.ts APPEND 'reset-2fa:user' vào SENSITIVE_CAPABILITY_ALLOWLIST (giữ 8 cặp cũ); mở rộng apps/api/src/auth/auth-me-capabilities.int.spec.ts assert company-admin(0001) có cặp qua /auth/me, employee(0008)/manager(0010) VẮNG.","L3 RED-TRƯỚC: viết apps/api/test/integration/auth-user-2fa-reset.int-spec.ts (mẫu role-admin-two-factor.int-spec.ts, Nest app thật + supertest, gate cứng hasDb && LANE_DB) — deny (a) thiếu pair → 403 + 0 thay đổi + 0 audit; (b) cross-tenant → 404 no-op; happy (c); PATCH (d); no-op (e); 2-tenant (f). Chạy FAIL trên LANE_DB (RED).","L4 impl PATCH requireTwoFactor: dto/repo updateProfileTx set require_two_factor có điều kiện + service audit 'user.updated' diff cờ + authUserSnapshot append require_two_factor; PLAN-FIX no-op (body rỗng / giá trị == cũ) → so before/after, KHÔNG ghi audit.","L5 impl getUserDetail twoFactor{} (3 cờ TÁCH nguồn: enabled từ user_totp.enabled_at join, requiredByRole join roles-only, requiredByUser cột 0466) + controller getOne trả AuthUserDetailDto; KHÔNG lộ secret.","L6 impl POST /:id/2fa/reset: controller route mới + AuthUsersService.resetTwoFactor (xoá user_totp + user_recovery_codes + import revokeAllForUserTx + audit user.2fa_reset kèm revoked_session_count + securityEvents TOTP_RESET; self-reset cho phép; NotFound no-op cross-tenant). KHÔNG sửa production auth.service.ts/two-factor.service.ts.","GREEN: bash scripts/lane-db-setup.sh <lane> → export LANE_DB=mediaos_<lane> → pnpm --filter @mediaos/api test — int-spec + unit colocated xanh; regression auth+users suite xanh.","typecheck + build @mediaos/api + contracts xanh; assert diff production auth/** = 0 dòng; FULL gate (security-reviewer + silent-failure-hunter + santa-method crown) → người chốt red-zone trước merge."]
```

RE-RUN v2 — RECONCILE (reused=false): plan cũ tách 3 lane song song (L1-contracts / L2-permission-allowlist / L3-users). plan-reviewer VÒNG 1 BLOCK vì L2 và L3 CÙNG sửa auth-me-capabilities.int.spec.ts (xung đột sở hữu file). PLAN-FIX cấu trúc: gộp về 1 LANE TUẦN TỰ DUY NHẤT, thứ tự cứng contracts → allowlist → users API → reset endpoint → specs. Thêm 2 luật plan-fix mới: (i) PATCH no-op (body rỗng / giá trị bằng cũ) → KHÔNG ghi audit (so before/after, mẫu lock đã-locked); (ii) auth/** = REUSE-ONLY, cấm sửa production auth.service.ts/two-factor.service.ts (chỉ import revokeAllForUserTx + sửa int-spec), reviewer assert diff production auth = 0 dòng.

TIỀN-ĐỀ verify (bước 0, read-only) PHẢI làm TRONG worktree ../mediaos-s2-auth-be-12 @42fcfd6 — KHÔNG đọc cây gốc (cây gốc đang wip/s2-fe-hr-5-hr5-wc @68616f9 KHÔNG có tiền-đề; plan-reviewer vòng 1 đọc nhầm chỗ này). ĐÃ PRE-VERIFY PASS: mig 0466 (0198f38, PR #104) + BE-11 requiresTwoFactor OR-per-user (42fcfd6, PR #105) có trên base 42fcfd6; users.ts:41 requireTwoFactor; revokeAllForUserTx tại auth.service.ts:1460 (public, trả revoked count); ALLOWLIST 8 cặp (chưa reset-2fa); SECURITY_EVENT_TYPES có TOTP_ENABLED/DISABLED (chưa TOTP_RESET); updateAuthUserRequestSchema fullName required + .strict(); template role-admin-two-factor.int-spec.ts ở test/integration; auth-me-capabilities.int.spec.ts ở src/auth.

KHÔNG CẦN LANE DB/MIGRATION: 0466 đã cấp cột + catalog pair + grant. audit 'user.2fa_reset' dùng objectType='user' — hợp lệ trong object_types CHECK hiện có (user.created/updated/locked/unlocked) → KHÔNG cần migration mở rộng CHECK. user_totp + user_recovery_codes đã GRANT DELETE cho mediaos_app (mig 0120) — bảng credential MUTABLE, KHÁC audit_logs/user_security_events append-only.

GAP đóng bởi WO: (1) chưa có endpoint admin bật/tắt ép-2FA per-user (cột 0466 chưa có consumer ghi) → PATCH requireTwoFactor; (2) chưa có endpoint admin reset/gỡ 2FA người khác (pair reset-2fa:user đã seed nhưng chưa gắn route/guard) → POST /:id/2fa/reset; (3) FE chưa thấy cờ reset-2fa:user (getCapabilities lọc sensitive) → append allowlist; (4) detail user chưa phơi trạng thái 2FA → twoFactor{}.

QUYẾT ĐỊNH THIẾT KẾ (pin cho builder): (a) authUserDetailSchema RIÊNG (extend authUserSchema) CHỈ cho GET /:id — KHÔNG nhét twoFactor vào authUserSchema (dùng chung list) để tránh N+1 tính 2FA mỗi row + over-expose list. (b) updateAuthUserRequestSchema fullName→optional (hiện required) để PATCH chỉ set cờ được; giữ .strict() chống field lạ leo thang; updateProfileTx set có điều kiện (chỉ field gửi); no-op = 0 field đổi → KHÔNG audit. (c) requiredByRole PHẢI join roles-only (roles.requires_two_factor, active user_roles) — KHÔNG tái dùng requiresTwoFactor của BE-11 (đã gộp OR users.require_two_factor) vì detail cần 2 nguồn tách bạch; requiredByUser = cột 0466 đọc thẳng. (d) resetTwoFactor đặt TRONG AuthUsersService (domain users) — KHÔNG tái dùng TwoFactorService.disable() (chỉ audit auth.2fa_disabled + emit TOTP_DISABLED + chỉ khi enabled); admin reset xoá VÔ ĐIỀU KIỆN + audit 'user.2fa_reset' (actor=admin, subject=target) + revoke session + emit TOTP_RESET. TÁI DÙNG AuthService.revokeAllForUserTx (đã public, đã inject cho lock) — KHÔNG viết lại. (e) severity TOTP_RESET='high' (mirror USER_LOCKED — can thiệp privileged trên credential). (f) self-reset CHO PHÉP (KHÔNG assertNotSelf) — owner chốt 2026-07-03: giữa reset↔re-enroll tài khoản privileged tạm 1-factor, chấp nhận vì có audit high + forensic.

INVARIANTS §2: mọi query qua withTenant(companyId) + WHERE company_id (phòng thủ kép RLS); audit.record + securityEvents.record TRONG cùng tx mutation (rollback → 0 orphan); tuyệt đối KHÔNG secret TOTP (plaintext/ciphertext) vào DTO/audit/log/payload — masker che theo tên khóa nhưng KHÔNG đưa cột secret vào snapshot; cross-tenant → NotFound (không lộ tồn tại, no-op 0 audit).

HOT-FILE & QUY ƯỚC MERGE (append, KHÔNG rewrite — nhưng nay TRONG 1 LANE nên không có xung đột cross-lane): SENSITIVE_CAPABILITY_ALLOWLIST append 1 dòng 'reset-2fa:user' giữ 8 cặp cũ; SECURITY_EVENT_TYPES/SECURITY_EVENT_SEVERITY append TOTP_RESET ở cuối (append-only §2 #2); authUserSnapshot append field require_two_factor; auth-me-capabilities.int.spec.ts CHỈ lane này sửa (không còn 2 chủ).

VERIFY/GATE: RED deny-path trước (403 thiếu pair / 404 cross-tenant / no-op PATCH 0 audit) → GREEN; chạy int-spec trên DB cô lập (bash scripts/lane-db-setup.sh + LANE_DB — .env làm hasDb=true → gate cứng hasDb && LANE_DB nếu không đỏ-giả trên mediaos dev chung); unit spec colocated src/**/*.spec.ts (spec để test/unit KHÔNG chạy = xanh-giả). Reviewer Đội 3 assert diff production auth/** = 0 dòng. FULL gate security-reviewer + silent-failure-hunter + santa-method (crown) + người chốt red-zone trước merge. WO là crown RED → IMPLEMENT+REVIEW Opus, PLAN Sonnet5+xhigh.

OUT-OF-SCOPE (chống scope-creep): FE nút Reset 2FA + màn hình admin user detail (WO FE riêng — backend chỉ mở cờ reset-2fa:user qua /auth/me + trả twoFactor{}); access-token STATELESS đã cấp còn hiệu lực tới ~15' sau reset/lock (denylist theo jti là DEFER — follow-up ngoài scope, ghi rõ trong spec/PR); enroll/confirm/disable 2FA self-service (đã có G16-1); KMS/payslip N/A.
</content>
