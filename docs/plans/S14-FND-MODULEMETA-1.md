```yaml
wo: S14-FND-MODULEMETA-1
zone: yellow
generated_by: auto-loop
reconciled_at: "11a34d64"
lanes: [{"id":"L1-RATCHET-RED","task":"RED TRƯỚC — dựng CỔNG chống drift (quan trọng hơn dữ liệu). (a) `apps/api/test/foundation/module-app-metadata-ratchet.unit-spec.ts`: tách hàm THUẦN `auditModuleMetadataCoverage({seededCodes, metadataKeys, exempt})` NGAY TRONG file (KHÔNG tạo file census sibling — ngoài paths WO); `seededCodes` = PARSE THẬT mọi câu `INSERT INTO modules (...) VALUES` trong `apps/api/migrations/*.sql` (ghim ĐỊNH NGHĨA, không hard-code tên — memory index-ratchet-must-pin-definition-not-name); assert 3 chiều: [1] mọi mã seed có metadata HOẶC nằm EXEMPT_MODULES kèm `reason`, [2] NGƯỢC LẠI mọi key metadata phải có hàng `modules` (chặn key CHẾT kiểu LMS), [3] mã EXEMPT không được đồng thời có metadata. 🔒 BLOCKING 1 — deny-path PHẢI THỰC THI: thêm CẶP ALLOW+DENY cho MỖI module mới, gọi THẲNG hàm thuần `hasAnyCapability` (module-app-metadata.ts:121, KHÔNG cần DB): RECRUIT deny {'access:recruit':true}⇒false · allow {'view:job-opening':true}⇒true; PAYROLL deny {'access:payroll':true}⇒false · allow {'view:payroll-period':true}⇒true; ASSET deny {'access:asset':true}⇒false · allow {'view:asset':true}⇒true; ROOM deny {'access:room':true}⇒false · allow {'view:room':true}⇒true; GOAL caps {}⇒false · {'access:goal':true}⇒true. Caps trong MỌI ca deny PHẢI KHÔNG-wildcard; THÊM 1 ca GHI NHẬN hành vi wildcard (`{'*:*':true}` ⇒ true cho cả 5) kèm comment «PROD còn 2 role giữ grant wildcard ⇒ deny KHÔNG tuyệt đối» (memory permission-grant-census-must-cover-four-wildcard-shapes · deny-cases-vacuous-without-allow-case). 🔒 BLOCKING 3 — check đối chiếu BẰNG CHỮ: assert route+icon của 5 entry mới khớp ĐÚNG hằng APP_REGISTRY: goals '/goals'+'target' · assets '/assets'+'package' · rooms '/rooms'+'calendar-clock' · recruit '/recruit/job-openings'+'user-plus' · payroll '/payroll/periods'+'wallet' (giá trị viết literal trong spec, KHÔNG import cross-package). ⚠️(b) parser PHẢI có CA ÂM: tập parse KHÔNG chứa key của `system_modules` (mig 0330:48) và KHÔNG chứa 'LMS'. ⚠️(c) assert «đúng 17 mã» GIỮ, kèm comment «bump CÓ CHỦ Ý khi thêm hàng modules». (b2) `module-app-metadata-coverage.int.spec.ts` colocated trong `src/foundation/module-catalog/` (đuôi `.int.spec.ts` khớp glob `src/**/*.spec.ts`; `describe.skipIf(!hasDb || !process.env.LANE_DB)` theo tiền lệ `src/auth/auth-me-capabilities.int.spec.ts`): CHỈ SELECT, CẤM INSERT/UPDATE (memory test-fixture-stamps-global-permission-catalog); ghi tiền đề «DB-ở-head, PROD chưa áp 0564–0568» ở đầu file (⚠️f); [1] mọi `modules` `is_active=true AND deleted_at IS NULL` PHẢI có metadata, MIỄN TRỪ KHÔNG chấp nhận cho module active; [2] mỗi cặp `requiredAny` PHẢI tồn tại trong `permissions`; [3] cặp `is_sensitive=true` PHẢI nằm trong `__SENSITIVE_CAPABILITY_ALLOWLIST_FOR_TEST` (permission.service.ts). Kết thúc lane: cả hai spec ĐỎ với ĐÚNG 5 mã GOAL/ASSET/ROOM/RECRUIT/PAYROLL — lưu log đỏ.","builder":"backend-builder","paths":["apps/api/test/foundation/module-app-metadata-ratchet.unit-spec.ts","apps/api/src/foundation/module-catalog/module-app-metadata-coverage.int.spec.ts"]},{"id":"L2-META-FILL","task":"GREEN — APPEND 5 entry vào `MODULE_APP_METADATA` (KHÔNG rewrite khối 8 entry cũ AUTH·HR·ATT·LEAVE·TASK·DASH·NOTI·ME, CLAUDE.md §9.3), mỗi entry kèm comment truy vết migration+dòng+is_sensitive+role-được-grant+lý-do-chọn-cặp. 🔒 BLOCKING 2 — `route` = ĐÍCH ĐIỀU HƯỚNG (mirror APP_REGISTRY.defaultRoute), KHÔNG phải rootPath: GOAL {route:'/goals', icon:'target', requiredAny:[{access,goal}], feCodes:['GOAL.ACCESS']} (0506:46) · ASSET {'/assets','package',[{view,asset}],['ASSET.ASSET.VIEW']} (0550:61-101) · ROOM {'/rooms','calendar-clock',[{view,room}],['ROOM.ROOM.VIEW']} (0554:57-91) · RECRUIT {'/recruit/job-openings','user-plus',[{view,'job-opening'}],['RECRUIT.JOB.VIEW']} (0560:81,110-135 · registry.ts:820 · router.tsx:1910) · PAYROLL {'/payroll/periods','wallet',[{view,'payroll-period'}],['PAYROLL.PERIOD.VIEW']} (0565:189,232-249 · registry.ts:843 · router.tsx:2098). Dùng '/recruit'|'/payroll' = dựng lại ĐÚNG cái lỗ WO tuyên bố vá (chỉ đổi trục 403→404) ⇒ CẤM. LÝ DO CHỌN CẶP TẢI-TRANG thay vì `access:X`: `requiredAny` chỉ có ngữ nghĩa OR, FE gate 4 thẻ này bằng `requiredPermissions` (AND access+view); gate BE bằng mình `access:X` ⇒ manager có `access:recruit` (0560:105) nhưng KHÔNG `view:job-opening` sẽ thấy thẻ rồi ăn 403. KHÔNG OR cả hai cặp (nới lỏng = đúng cái lỗ đó). ⚠️(a) 3/5 feCode — GOAL.ACCESS · RECRUIT.JOB.VIEW · PAYROLL.PERIOD.VIEW — KHÔNG có trong PERMISSION_CODE_TO_PAIR (chỉ ASSET.ASSET.VIEW:225 · ROOM.ROOM.VIEW:249 có): GIỮ NGUYÊN giá trị, thêm comment «mã truy vết, chưa map trong PERMISSION_CODE_TO_PAIR» + mở nợ ở doc. ⚠️(d) vá đinh: `module-admin.service.spec.ts:107` fixture 'module thiếu metadata (vd PAYROLL)' → đổi sang 'MOBILE' VÀ thêm `expect(MODULE_APP_METADATA.MOBILE).toBeUndefined()` làm TIỀN ĐỀ TƯỜNG MINH; GIỮ NGUYÊN số ca `it()` (memory tests-can-pin-a-hole-open · same-builder-twice-makes-unit-spec-vacuous). KHÔNG đổi `hasAnyCapability`, `getMyApps`, `toAdminItem`, `SENSITIVE_CAPABILITY_ALLOWLIST`.","builder":"backend-builder","paths":["apps/api/src/foundation/module-catalog/module-app-metadata.ts","apps/api/src/foundation/module-catalog/module-admin.service.spec.ts"]},{"id":"L3-DOCS","task":"Cập nhật `docs/BACKEND/BACKEND-04_Foundation_Backend.md` §8.2 (dòng ~405-441) — hiện STALE: liệt kê 'Module MVP active' thiếu ME/GOAL, vẫn xếp PAYROLL/RECRUIT/ASSET/ROOM vào 'phase sau seed inactive' dù 0556/0557/0562/0567 đã bật. Ghi: (1) bảng hiện trạng 17 mã × is_active × có/không metadata; (2) 🔒 BLOCKING 3 — BẢNG 13 DÒNG `module_code ↔ MODULE_APP_METADATA.route ↔ APP_REGISTRY.defaultRoute ↔ icon`, kèm CẢNH BÁO in đậm «hằng CHÉO-PACKAGE, KHÔNG có cổng tự động ở runtime — chỉ ratchet unit-spec so literal; route rác qua sạch typecheck/build/int-spec vì BE không đọc trường này» (bẫy fe-widget-slug-map-is-unchecked-runtime-gate); (3) luật CỔNG mới: module `is_active=true` BẮT BUỘC có metadata, miễn trừ CHỈ hợp lệ khi inactive; (4) quy ước chọn cặp TẢI-TRANG; ⚠️(e) 1 dòng NỢ: «BE `requiredAny` (OR) LỎNG hơn FE `requiredPermissions` (AND) ⇒ role có view:X mà không access:X sẽ BE-hiện/FE-ẩn; parity tuyệt đối cần `requiredAll` — ngoài phạm vi WO 🟡»; ⚠️(a) nợ: 3 feCode chưa có trong PERMISSION_CODE_TO_PAIR; (5) LMS/fbpost là app vệ tinh SSO KHÔNG có hàng `modules` ⇒ KHÔNG có metadata (ratchet chiều-ngược chặn). ⚠️(g) `docs/plans/S14-FND-MODULEMETA-1.md` ĐÃ CÓ khối yaml+GAP-ANALYSIS ⇒ **APPEND** mục mới (log RED, kết quả 3 BLOCKING, bàn giao), KHÔNG rewrite (CLAUDE.md §9.3). Nêu bàn giao: S16-SOCIAL-FE-1 bật SOCIAL ⇒ phải thêm metadata cùng commit, nếu không int-spec ĐỎ; tương tự WO bật CHAT.","builder":"backend-builder","paths":["docs/BACKEND/BACKEND-04_Foundation_Backend.md","docs/plans/S14-FND-MODULEMETA-1.md"]}]
acceptanceChecks: ["`MODULE_APP_METADATA` có ĐÚNG 13 key = 8 cũ (AUTH·HR·ATT·LEAVE·TASK·DASH·NOTI·ME — KHÔNG sửa 1 ký tự) + 5 mới (GOAL·ASSET·ROOM·RECRUIT·PAYROLL). KHÔNG có key LMS (không có hàng `modules` ⇒ key chết).","🔒 BLOCKING 2 — route 5 entry mới ĐÚNG ĐÍCH ĐIỀU HƯỚNG, khớp `APP_REGISTRY.defaultRoute` từng ký tự: GOAL '/goals' · ASSET '/assets' · ROOM '/rooms' · RECRUIT '/recruit/job-openings' (registry.ts:820) · PAYROLL '/payroll/periods' (registry.ts:843). Diff KHÔNG chứa chuỗi `route: \"/recruit\"` hay `route: \"/payroll\"` (rootPath = SAI).","🔒 BLOCKING 3 — ratchet spec có check đối chiếu BẰNG CHỮ cả route VÀ icon của 5 entry mới (target·package·calendar-clock·user-plus·wallet); BACKEND-04 §8.2 có BẢNG 13 DÒNG route↔defaultRoute + câu cảnh báo tường minh «hằng chéo-package, không có cổng runtime tự động».","🔒 BLOCKING 1 — ratchet spec có ĐỦ 10 ca gọi thẳng `hasAnyCapability`: 5 ca DENY (caps KHÔNG-wildcard: {'access:recruit'} · {'access:payroll'} · {'access:asset'} · {'access:room'} · {} cho GOAL ⇒ false) + 5 ca ALLOW ({'view:job-opening'} · {'view:payroll-period'} · {'view:asset'} · {'view:room'} · {'access:goal'} ⇒ true) + 1 ca ghi nhận wildcard ({'*:*':true} ⇒ true cho cả 5, kèm comment PROD còn 2 role wildcard).","Mỗi `requiredAny` mới là 1 cặp DUY NHẤT, grep-verify khớp seed migration: access:goal (0506:46) · view:asset (0550:61-101) · view:room (0554:57-91) · view:job-opening (0560:81) · view:payroll-period (0565:189). KHÔNG mã dotted FE trong `requiredAny`; KHÔNG cặp legacy read:asset/read:room/read:recruit/read:payroll.","`feCodes` khớp mã quy ước của chính dòng seed: GOAL.ACCESS · ASSET.ASSET.VIEW · ROOM.ROOM.VIEW · RECRUIT.JOB.VIEW · PAYROLL.PERIOD.VIEW; `feCodes.length === requiredAny.length` cho 5 entry mới; 3 mã chưa map (GOAL.ACCESS · RECRUIT.JOB.VIEW · PAYROLL.PERIOD.VIEW) có comment «mã truy vết, chưa map trong PERMISSION_CODE_TO_PAIR» + nợ ghi ở BACKEND-04.","`SENSITIVE_CAPABILITY_ALLOWLIST` (`apps/api/src/permission/permission.service.ts:47`) diff = 0 dòng: cả 5 cặp `is_sensitive=false` ⇒ getCapabilities() đã surface, KHÔNG cần Option B. Int-spec CHỨNG MINH bằng truy vấn `permissions.is_sensitive`, không bằng lời.","Int-spec DB (LANE_DB) XANH: mọi `modules` `is_active=true AND deleted_at IS NULL` (13 mã) đều có metadata — miễn trừ KHÔNG được chấp nhận cho module active; int-spec CHỈ SELECT `permissions`/`modules`, diff không chứa INSERT/UPDATE vào 2 bảng này; đầu file ghi TIỀN ĐỀ «DB-ở-head (0564–0568 đã áp), PROD chưa áp».","Ratchet lấy `seededCodes` bằng PARSE `INSERT INTO modules ... VALUES` từ `apps/api/migrations/*.sql` (ra ĐÚNG 17 mã), KHÔNG hard-code mảng tên; assert «17» có comment «bump CÓ CHỦ Ý khi thêm hàng modules»; có CA ÂM chứng minh parser không trượt: tập parse KHÔNG chứa key `system_modules` (0330:48) và KHÔNG chứa 'LMS'.","Ratchet có chiều NGƯỢC: key metadata không có hàng `modules` ⇒ ĐỎ (chặn key chết LMS/typo). EXEMPT_MODULES chứa đúng 4 mã inactive kèm `reason` máy-đọc: CHAT · SOCIAL · MOBILE · AI; không mã nào vừa EXEMPT vừa có metadata.","Cổng KHÔNG rỗng — thử tay 1 lần: xoá 1 trong 5 entry mới ⇒ ratchet ĐỎ; đổi route RECRUIT thành '/recruit' ⇒ check literal ĐỎ. Ghi kết quả 2 phép thử vào phần APPEND của plan doc.","`module-admin.service.spec.ts` giữ NGUYÊN số ca `it()`; ca fail-soft đổi fixture PAYROLL→MOBILE VÀ có tiền đề tường minh `expect(MODULE_APP_METADATA.MOBILE).toBeUndefined()`; ca vẫn xanh (nhánh fail-soft còn được phủ).","`my-apps-canonical-role.int-spec.ts` (4 ca) + `migration-smoke.int-spec.ts` giữ nguyên số ca và xanh — hành vi 7 module MVP + ME không đổi (route/icon/feCodes bit-identical).","`pnpm typecheck` + `pnpm build` xanh; `bash harness/check.sh --lane-db=s14fndmodulemeta1` kết luận XANH (KHÔNG phải 'XANH KHÔNG ĐỦ BẰNG CHỨNG').","Diff KHÔNG chứa: file `apps/api/migrations/*` mới · `UPDATE modules ... is_active` · `harness/backlog.mjs` · `packages/web-core/src/lib/registry.ts` · thay đổi `hasAnyCapability`/`getMyApps`/`toAdminItem`.","`docs/plans/S14-FND-MODULEMETA-1.md` được APPEND (khối yaml cũ + GAP-ANALYSIS 1-11 còn nguyên trong diff, chỉ thêm mục mới) — KHÔNG rewrite (CLAUDE.md §9.3).","DoD §8: không đổi DB ⇒ không migration; input validation không đổi; không phát sinh audit event mới; có test; luồng chính (/foundation/modules, /foundation/modules/my-apps) không đổi contract (`myAppItemSchema` giữ nguyên ⇒ `packages/contracts/src/` dự kiến 0 dòng đổi, `apps/app/src/` 0 dòng đổi — nếu đổi phải nêu lý do trong PR)."]
testTasks: ["RED bắt buộc TRƯỚC code (L1): chạy ratchet unit-spec khi chưa thêm entry → PHẢI ĐỎ và in ĐÚNG 5 mã thiếu (GOAL·ASSET·ROOM·RECRUIT·PAYROLL); lưu log đỏ.","Unit thuần — `auditModuleMetadataCoverage` ca ALLOW: fixture {seeded:[A,B], metadata:[A], exempt:{B:'lý do'}} → 0 vi phạm (không có ca ALLOW thì mọi ca DENY xanh-RỖNG).","Unit ca DENY-1: mã seed không có metadata và không miễn trừ → đúng 1 vi phạm MISSING_METADATA.","Unit ca DENY-2: key metadata không có trong tập mã seed → đúng 1 vi phạm ORPHAN_METADATA (chặn key chết LMS).","Unit ca DENY-3: mã vừa EXEMPT vừa có metadata → đúng 1 vi phạm DOUBLE_LISTED.","🔒 Deny-path THỰC THI (BLOCKING 1) — 5 ca DENY gọi thẳng `hasAnyCapability` với caps KHÔNG-wildcard: RECRUIT {'access:recruit':true}⇒false · PAYROLL {'access:payroll':true}⇒false · ASSET {'access:asset':true}⇒false · ROOM {'access:room':true}⇒false · GOAL {}⇒false.","🔒 Ca ALLOW đối ứng (BLOCKING 1) — 5 ca: RECRUIT {'view:job-opening':true}⇒true · PAYROLL {'view:payroll-period':true}⇒true · ASSET {'view:asset':true}⇒true · ROOM {'view:room':true}⇒true · GOAL {'access:goal':true}⇒true.","🔒 Ca GHI NHẬN wildcard (BLOCKING 1) — `hasAnyCapability({'*:*':true}, meta.requiredAny)` ⇒ true cho cả 5 module, comment «deny KHÔNG tuyệt đối: PROD còn 2 role giữ grant wildcard» (memory permission-grant-census-must-cover-four-wildcard-shapes).","🔒 Check literal route+icon (BLOCKING 3): assert 5 cặp (route,icon) khớp APP_REGISTRY — '/goals'+target · '/assets'+package · '/rooms'+calendar-clock · '/recruit/job-openings'+user-plus · '/payroll/periods'+wallet.","Unit parser: parse `apps/api/migrations/*.sql` trả ĐÚNG 17 mã (15 từ 0435 + ME 0495 + GOAL 0506); assert có mặt 'MOBILE'/'AI'; CA ÂM — tập parse KHÔNG chứa key của `system_modules` (mig 0330:48) và KHÔNG chứa 'LMS'.","Integration (LANE_DB=mediaos_s14fndmodulemeta1) — coverage: `SELECT module_code FROM modules WHERE is_active AND deleted_at IS NULL` ⇒ mọi mã PHẢI có metadata; ĐỎ nếu wave sau bật `is_active` mà quên metadata (bàn giao S16-SOCIAL-FE-1).","Integration — pair-validity: mỗi cặp trong mọi `requiredAny` PHẢI tồn tại 1 hàng `permissions(action,resource_type)`; cặp bịa ⇒ ĐỎ. CHỈ SELECT, CẤM INSERT/UPDATE (memory test-fixture-stamps-global-permission-catalog).","Integration — sensitive/allowlist: cặp nào `permissions.is_sensitive=true` mà KHÔNG có trong `__SENSITIVE_CAPABILITY_ALLOWLIST_FOR_TEST` ⇒ ĐỎ (tổng quát hoá lỗi ASSET ẩn-ngầm; hôm nay 5 cặp đều false ⇒ ca ĐỀ PHÒNG).","Regression không thêm ca: `my-apps-canonical-role.int-spec.ts` 4/4 xanh + `migration-smoke.int-spec.ts` xanh — chứng minh 7 module MVP + ME không đổi hành vi.","Regression fixture-đinh: `module-admin.service.spec.ts` giữ nguyên số ca `it()`, ca fail-soft dùng MOBILE + có `expect(MODULE_APP_METADATA.MOBILE).toBeUndefined()` và vẫn xanh.","Verify tay ghi vào plan doc: manager có `access:recruit` (0560:105) nhưng KHÔNG `view:job-opening` ⇒ với requiredAny=[view:job-opening], my-apps KHÔNG trả RECRUIT cho manager (khớp FE requiredPermissions AND); tương tự employee (chỉ `access:payroll` @Own) KHÔNG thấy PAYROLL.","Thử-phá cổng (2 phép): xoá 1 entry mới ⇒ ratchet ĐỎ; đặt route RECRUIT='/recruit' ⇒ check literal ĐỎ. Ghi log cả hai.","Full regression: `pnpm --filter @mediaos/api test` với LANE_DB — số test-file SKIP không vượt ngưỡng; `harness/check.sh --lane-db=s14fndmodulemeta1` không in banner 'XANH KHÔNG ĐỦ BẰNG CHỨNG'."]
steps: ["0. Vào worktree ĐÃ DỰNG SẴN `../mediaos-s14-fnd-modulemeta-1` (branch auto/S14-FND-MODULEMETA-1, đã ff lên master 11a34d64) — KHÔNG tạo lại. `export LANE_DB=mediaos_s14fndmodulemeta1` TRƯỚC mọi lệnh test (CLAUDE.md §9.5).","1. Đọc lại 5 nguồn trước khi gõ code: `apps/api/src/foundation/module-catalog/module-app-metadata.ts` (khối DRIFT-GUARD đầu file + `hasAnyCapability` ở cuối, ~dòng 121), `module-catalog.service.ts:45-97` (getMyApps chỉ duyệt module ACTIVE, thiếu meta → warn + skip), `apps/api/migrations/0435_...sql:287-302` (15 mã seed), `packages/web-core/src/lib/registry.ts:660-847` (APP_REGISTRY defaultRoute/icon — CHỈ ĐỌC), `docs/BACKEND/BACKEND-04...md` §8.2.","2. L1 (RED): viết ratchet unit-spec (parser migration + 3-chiều audit + ALLOW/DENY hasAnyCapability + ca wildcard + đối chiếu literal route/icon) và int-spec DB-backed. Chạy `pnpm --filter @mediaos/api test -- module-app-metadata` → PHẢI ĐỎ, liệt kê ĐÚNG 5 mã GOAL/ASSET/ROOM/RECRUIT/PAYROLL. LƯU LOG ĐỎ (bằng chứng cổng không rỗng).","3. L1 (self-check cổng): ca ALLOW tổng hợp {seeded:[A,B], metadata:[A], exempt:{B:'lý do'}} → 0 vi phạm; 3 ca DENY (MISSING_METADATA · ORPHAN_METADATA · DOUBLE_LISTED) → đúng 1 vi phạm mỗi ca; ca ÂM parser (không chứa key `system_modules` mig 0330:48, không chứa 'LMS').","4. L2 (GREEN): APPEND 5 entry với route ĐÍCH ĐIỀU HƯỚNG ('/recruit/job-openings', '/payroll/periods', '/goals', '/assets', '/rooms') + icon (target·package·calendar-clock·user-plus·wallet) + 1 cặp engine grep-verified mỗi entry; comment truy vết migration+dòng+role.","5. L2 (vá đinh): `module-admin.service.spec.ts:107` fixture PAYROLL→MOBILE + thêm `expect(MODULE_APP_METADATA.MOBILE).toBeUndefined()`; giữ NGUYÊN số ca `it()` và ý nghĩa nhánh fail-soft.","6. Chạy lại `pnpm --filter @mediaos/api test` với LANE_DB → ratchet + int-spec XANH; `my-apps-canonical-role.int-spec.ts` (4 ca, assert bằng `codes.has(...)` nên thêm thẻ mới KHÔNG phá) và `migration-smoke.int-spec.ts` giữ nguyên số ca và vẫn xanh.","7. L3: cập nhật BACKEND-04 §8.2 (bảng 17 mã + BẢNG 13 DÒNG route↔defaultRoute↔icon + cảnh báo hằng chéo-package + 2 dòng nợ) rồi APPEND vào docs/plans/S14-FND-MODULEMETA-1.md.","8. Verify cuối: `pnpm typecheck` + `pnpm build` + `bash harness/check.sh --lane-db=s14fndmodulemeta1` (KHÔNG được rơi vào 'XANH KHÔNG ĐỦ BẰNG CHỨNG').","9. CẤM: viết migration · bật/tắt `modules.is_active` · sửa `harness/backlog.mjs` · sửa `SENSITIVE_CAPABILITY_ALLOWLIST` (5 cặp đều non-sensitive — nếu thấy cần sửa thì DỪNG, giả định đã sai) · sửa `packages/web-core/src/lib/registry.ts` (chỉ ĐỌC đối chiếu)."]
```

RECONCILE-REFRESH 02/09 (vòng 2 — sau khi done_when được ghim 3 BLOCKING + 7 cảnh báo của plan-reviewer). Plan cũ (khối yaml @0487dbef) KHÔNG còn dùng nguyên được; GAP-ANALYSIS 1-11 vẫn ĐÚNG, giữ nguyên. Ba điểm phải sửa so với plan cũ:

A) ROUTE SAI Ở PLAN CŨ (BLOCKING 2). Plan cũ ghi RECRUIT route '/recruit', PAYROLL '/payroll' = rootPath. Đã grep-verify `packages/web-core/src/lib/registry.ts`: dòng 820 `defaultRoute: "/recruit/job-openings"` (rootPath "/recruit"), dòng 843 `defaultRoute: "/payroll/periods"` (rootPath "/payroll"). Trường `route` trong ModuleAppMeta là ĐÍCH ĐIỀU HƯỚNG khi bấm thẻ app ⇒ dùng rootPath chỉ đổi trục lỗi 403→404, dựng lại đúng cái lỗ WO tuyên bố vá. GOAL/ASSET/ROOM giữ '/goals'·'/assets'·'/rooms' vì rootPath===defaultRoute và CÓ route thật (router.tsx 2217·1745·2208). Icon đã verify tại registry: target·package·calendar-clock·user-plus·wallet.

B) DENY-PATH PHẢI THỰC THI (BLOCKING 1). Plan cũ chỉ có ca fixture tổng hợp cho hàm audit + 1 dòng "verify tay" cho parity manager/employee — đó là VĂN XUÔI, không phải cổng. `hasAnyCapability` là hàm THUẦN ở cuối `module-app-metadata.ts` (~dòng 121), không cần DB ⇒ phải có cặp ALLOW+DENY cho MỖI module mới. Bẫy đã ghim: (i) thiếu ca ALLOW ⇒ ca DENY xanh-RỖNG (memory deny-cases-vacuous-without-allow-case); (ii) `hasAnyCapability` coi `*:*`, `*:resource`, `action:*` là TRUE — caps trong ca deny PHẢI không-wildcard, và cần 1 ca GHI NHẬN hành vi wildcard vì PROD còn 2 role giữ grant wildcard (memory permission-grant-census-must-cover-four-wildcard-shapes) để không ai tưởng deny là tuyệt đối.

C) NGHIỆM THU ROUTE/ICON KHÔNG ĐO ĐƯỢC (BLOCKING 3). BE không đọc trường `route`/`icon` ⇒ route rác qua sạch typecheck/build/int-spec. Phải có check đối chiếu BẰNG CHỮ trong ratchet spec (viết literal, KHÔNG import cross-package để tránh coupling api→web-core) + BẢNG 13 DÒNG route↔defaultRoute↔icon ở BACKEND-04 §8.2 kèm cảnh báo «hằng chéo-package, không có cổng runtime» (bẫy fe-widget-slug-map-is-unchecked-runtime-gate).

7 CẢNH BÁO ĐÃ PHÂN BỔ VÀO LANE: (a) 3/5 feCode chưa có trong PERMISSION_CODE_TO_PAIR — grep xác nhận chỉ `ASSET.ASSET.VIEW` (registry.ts:225) và `ROOM.ROOM.VIEW` (:249) tồn tại ⇒ GIỮ giá trị + comment «mã truy vết, chưa map» + nợ ở doc (feCodes chỉ để hiển thị, không enforce ⇒ không chặn WO). (b) parser cần ca ÂM (không nuốt `system_modules` mig 0330:48, không có 'LMS'). (c) assert 17 mã giữ + comment bump-có-chủ-ý. (d) fixture PAYROLL→MOBILE chỉ DỜI đinh ⇒ thêm `expect(MODULE_APP_METADATA.MOBILE).toBeUndefined()` làm tiền đề, giữ số ca. (e) BE requiredAny (OR) LỎNG hơn FE requiredPermissions (AND) ⇒ 1 dòng nợ ở BACKEND-04, KHÔNG mở rộng kiểu `requiredAll` trong WO 🟡 (đổi `hasAnyCapability` = đổi hành vi cổng, vượt phạm vi). (f) int-spec ghim DB Ở HEAD; PROD chưa áp 0564–0568 ⇒ ghi tiền đề, và CHỈ SELECT (memory test-fixture-stamps-global-permission-catalog). (g) plan doc APPEND, không rewrite.

GAP-ANALYSIS GIỮ NGUYÊN (đo lại vẫn đúng): `module-app-metadata.ts` hiện 8 key (7 MVP + ME từ mig 0495) chứ không phải 7 như tiêu đề WO. Bảng `modules` có 17 mã: 15 từ 0435:287-302 + ME (0495) + GOAL (0506); CHỈ 3 migration từng INSERT INTO modules, 4 migration khác chỉ UPDATE is_active (0556 ASSET · 0557 ROOM · 0562 RECRUIT · 0567 PAYROLL). Active = 13; inactive = CHAT·SOCIAL·MOBILE·AI. LMS KHÔNG thêm được (không có hàng `modules`; getMyApps duyệt HÀNG DB rồi mới merge metadata ⇒ key LMS là dữ liệu CHẾT; seed hàng = migration mới, done_when CẤM) ⇒ điểm LỆCH giữa tiêu đề WO và code: WO thêm 5 module, LMS ghi OUT-OF-SCOPE có lý do và ratchet chiều-ngược CHẶN key LMS. 5 cặp đều `is_sensitive=false` và đều grant cho role canonical ⇒ KHÔNG đụng SENSITIVE_CAPABILITY_ALLOWLIST (permission.service.ts:47, export test :297) — builder thấy mình đang sửa allowlist thì DỪNG, giả định đã sai.

GỐC RỄ DRIFT: cổng duy nhất chạm `modules` là `test/integration/migration-smoke.int-spec.ts:86-114` dùng BA MẢNG TÊN HARD-CODE (7+4+2=13/17), không nhắc MOBILE·AI·ME·GOAL, không liên hệ metadata — census-theo-TÊN (memory index-ratchet-must-pin-definition-not-name). Cổng mới: (a) unit ratchet PARSE migration lấy tập mã theo ĐỊNH NGHĨA, (b) int-spec đọc DB thật KHÔNG chấp nhận miễn trừ cho module active — vế (b) mới là thứ khiến wave sau bật cờ mà quên metadata sẽ ĐỎ.

BẤT BIẾN & GATE: không đụng bất biến #1/#2/#3 — `modules` là catalog GLOBAL no-RLS (0435:38 tự loại khỏi rls-registry), không company_id, không audit append-only, không secret. Không migration ⇒ KHÔNG có lane db-migration, 3 lane đều backend-builder, paths rời nhau hoàn toàn. Zone 🟡 LIGHT gate (typescript-reviewer + quality-gate); done_when tuyên bố lượt này chạy skipPlan (trần 1 vòng plan-review đã dùng hết — memory plan-review-rounds-inject-new-holes · red-zone-wo-cost-profile), Đội 3 (completion-evaluator + qa-test-engineer + security-reviewer) vẫn gác đủ. Nghiệm thu = các mục 🔒, KHÔNG phải «plan-reviewer PASS».

THỨ TỰ THI CÔNG (nối tiếp trong 1 phiên, không fan-out): L1 (RED, lưu log đỏ) → L2 (GREEN) → chạy lại test → L3 (docs). L1/L2 KHÔNG chồng paths (L1 chỉ 2 file spec; L2 chỉ metadata + spec fixture) nhưng L2 phụ thuộc L1 vì cần log ĐỎ làm bằng chứng cổng không rỗng.

HOT-FILE & QUY ƯỚC MERGE: `module-app-metadata.ts` = APPEND 5 entry vào cuối object, KHÔNG rewrite 8 khối cũ; `docs/plans/S14-FND-MODULEMETA-1.md` = APPEND mục mới dưới GAP-ANALYSIS, giữ nguyên khối yaml cũ; BACKEND-04 §8.2 = thay bảng stale + thêm bảng 13 dòng.

VERIFY: `export LANE_DB=mediaos_s14fndmodulemeta1` (DB đã chain-migrate sẵn) → `pnpm --filter @mediaos/api test` → `pnpm typecheck` → `pnpm build` → `bash harness/check.sh --lane-db=s14fndmodulemeta1`. Int-spec mới đặt ở `src/foundation/module-catalog/module-app-metadata-coverage.int.spec.ts` với đuôi `.int.spec.ts` (KHÔNG `.int-spec.ts`): `apps/api/vitest.config.ts` include `src/**/*.spec.ts` + `test/**/*.int-spec.ts` ⇒ file `.int-spec.ts` nằm dưới `src/` sẽ KHÔNG BAO GIỜ CHẠY (xanh-giả); tiền lệ đúng là `src/auth/auth-me-capabilities.int.spec.ts`.

OUT-OF-SCOPE (chống scope creep): KHÔNG bật/tắt `modules.is_active` (memory module-is-active-is-not-a-gate · module-enable-guard-blocks-next-wo · phase-modules-preseeded-inactive-in-0435) · KHÔNG migration · KHÔNG sửa harness/backlog.mjs · KHÔNG metadata cho CHAT/SOCIAL/MOBILE/AI (đưa vào EXEMPT kèm reason) · KHÔNG sửa packages/web-core/src/lib/registry.ts (chỉ ĐỌC) · KHÔNG đổi hasAnyCapability/getMyApps/toAdminItem · KHÔNG thêm `requiredAll`. Paths WO `packages/contracts/src/` và `apps/app/src/` dự kiến 0 dòng: `myAppItemSchema` đã có route/icon/required_permissions (hình dạng phản hồi không đổi; lưu ý `packages/contracts/src/module-registry.ts` là DTO `system_modules` của operator-plane media cũ, KHÔNG liên quan), còn FE dựng lưới «Ứng dụng của tôi» 100% từ APP_REGISTRY tĩnh (HomePortalLayout.tsx:55, AppSwitcher.tsx:70) — KHÔNG gọi /foundation/modules/my-apps ⇒ WO này không có phần FE nhìn thấy được.

BÀN GIAO: S16-SOCIAL-FE-1 bật `modules.SOCIAL` PHẢI thêm entry SOCIAL cùng commit (int-spec sẽ ĐỎ nếu quên — đúng như docs/plans/S16-SOCIAL-WAVE.md:100 dự liệu); WO bật CHAT tương tự. Câu hỏi mở cho owner: (a) có seed hàng `modules.LMS` để LMS lên my-apps không (cần migration ⇒ WO riêng); (b) có nâng ModuleAppMeta hỗ trợ `requiredAll` để parity tuyệt đối với FE requiredPermissions không; (c) map 3 feCode còn thiếu vào PERMISSION_CODE_TO_PAIR.

---

## APPEND 02/09 — KẾT QUẢ THI CÔNG (L1 RED → L2 GREEN → L3 DOCS)

_Khối yaml + RECONCILE-REFRESH + GAP-ANALYSIS ở trên GIỮ NGUYÊN (CLAUDE.md §9.3 — hot-file = APPEND). Mục này chỉ ghi thêm bằng chứng thi công._

Worktree `../mediaos-s14-fnd-modulemeta-1`, branch `auto/S14-FND-MODULEMETA-1` (off master `11a34d64`). Verify chạy với `LANE_DB=mediaos_s14fndmodulemeta1`.

### 1. Log RED — bằng chứng cổng KHÔNG rỗng (L1, commit `f8bbecbc`)

Hai spec được viết TRƯỚC khi có dữ liệu, chạy trên code sản phẩm nguyên vẹn:

| Spec | Kết quả RED | Nội dung vi phạm in ra |
| --- | --- | --- |
| `apps/api/test/foundation/module-app-metadata-ratchet.unit-spec.ts` | **18 failed / 13 passed** (31 ca) | `MISSING_METADATA` = ĐÚNG 5 mã: ASSET · GOAL · PAYROLL · RECRUIT · ROOM |
| `apps/api/src/foundation/module-catalog/module-app-metadata-coverage.int.spec.ts` | **1 failed / 4 passed** (5 ca) | Truy vấn DB-ở-head trả cùng 5 mã ⇒ xác nhận cả 5 đã `is_active=true` thật, nên miễn trừ KHÔNG hợp lệ |

13 ca xanh ngay từ vòng RED là ca **self-check của chính cổng** (4 ca parser + 5 ca ALLOW/DENY của hàm thuần `auditModuleMetadataCoverage`) — nghĩa là parser và bộ audit đã đúng, thứ duy nhất còn thiếu là **dữ liệu**. Đây là điều phân biệt "cổng thật" với "cổng đỏ vì chính nó hỏng".

Log đỏ lưu ngoài repo (scratchpad phiên): `ratchet-unit-RED.log` · `coverage-int-RED.log`.

### 2. Kết quả 3 BLOCKING

**🔒 BLOCKING 1 — deny-path PHẢI THỰC THI (không phải văn xuôi).** Ratchet có **11 ca** gọi THẲNG hàm thuần `hasAnyCapability` (`module-app-metadata.ts`), không DB, không Nest DI:

| Module | Ca DENY (caps KHÔNG-wildcard) | Ca ALLOW |
| --- | --- | --- |
| RECRUIT | `{'access:recruit':true}` ⇒ **false** | `{'view:job-opening':true}` ⇒ true |
| PAYROLL | `{'access:payroll':true}` ⇒ **false** | `{'view:payroll-period':true}` ⇒ true |
| ASSET | `{'access:asset':true}` ⇒ **false** | `{'view:asset':true}` ⇒ true |
| ROOM | `{'access:room':true}` ⇒ **false** | `{'view:room':true}` ⇒ true |
| GOAL | `{}` ⇒ **false** | `{'access:goal':true}` ⇒ true |

Ca ALLOW đối ứng là bắt buộc: thiếu nó thì 5 ca DENY xanh-RỖNG (một `requiredAny` rỗng kiểu ME cũng làm mọi ca DENY… vẫn đỏ, nhưng một `requiredAny` bịa sẽ làm mọi ca DENY xanh mà vô nghĩa). Spec neo thêm `expect(requiredAny.length).toBeGreaterThan(0)` và `not.toContain('*')` cho caps của ca DENY.

Ca thứ 11 = **GHI NHẬN wildcard**: `hasAnyCapability({'*:*':true}, meta.requiredAny)` ⇒ **true cho cả 5 module**, kèm comment «deny KHÔNG tuyệt đối — PROD còn 2 role giữ grant wildcard». Cố ý ghi lại để không ai đọc bảng DENY ở trên rồi tưởng đây là cổng enforcement; cổng THẬT vẫn là `can()`/`PermissionGuard` per-resource ở controller. `hasAnyCapability` coi cả 4 hình dạng `action:resource`, `*:resource`, `action:*`, `*:*` là khớp.

**🔒 BLOCKING 2 — route = ĐÍCH ĐIỀU HƯỚNG, không phải rootPath.** 5 entry đã APPEND (commit `b3ded511`, diff file hằng = **72 insert / 0 delete** ⇒ APPEND thuần, 8 key cũ không đổi 1 ký tự):

| Module | `route` | `icon` | Cặp engine DUY NHẤT | Seed (grep-verified) |
| --- | --- | --- | --- | --- |
| GOAL | `/goals` | `target` | `access:goal` | 0506:46 — grant Own ×4 role canonical |
| ASSET | `/assets` | `package` | `view:asset` | 0550:62 — grant 0550:81-102 |
| ROOM | `/rooms` | `calendar-clock` | `view:room` | 0554:58 — grant 0554:71-92 @Company |
| RECRUIT | `/recruit/job-openings` | `user-plus` | `view:job-opening` | 0560:81 — grant hr·company-admin·recruiter @Company |
| PAYROLL | `/payroll/periods` | `wallet` | `view:payroll-period` | 0565:189 — grant payroll-officer·company-admin @Company |

Diff KHÔNG chứa `route: "/recruit"` hay `route: "/payroll"`. Cả 5 cặp `is_sensitive=false` ⇒ `getCapabilities()` đã surface ⇒ `SENSITIVE_CAPABILITY_ALLOWLIST` (`permission.service.ts`) **diff 0 dòng**; int-spec C3 chứng minh bằng truy vấn `permissions.is_sensitive` chứ không bằng lời.

**🔒 BLOCKING 3 — nghiệm thu route/icon vốn KHÔNG đo được, nay có cổng chữ.** BE không đọc `route`/`icon` ⇒ route rác đi qua sạch typecheck/build/int-spec. Ratchet thêm khối `APP_REGISTRY_LITERALS` viết **literal** (cố ý không `import` cross-package để tránh coupling `api → web-core`), assert cả route VÀ icon của 5 entry. BACKEND-04 §8.2 nay có **bảng 13 dòng** `module_code ↔ route ↔ appKey ↔ rootPath ↔ defaultRoute ↔ icon` + cảnh báo in đậm «hằng chéo-package, không có cổng runtime tự động».

Phát hiện phụ khi dựng bảng 13 dòng (**không sửa ở WO này**): 3 module di sản **ATT · LEAVE · TASK** đang để `route` = `rootPath` trong khi FE `defaultRoute` sâu hơn (`/attendance/today` · `/leave/me/requests` · `/tasks/my-tasks`). Ratchet ghim nguyên giá trị hiện tại (no-regress) và doc mở nợ 🟡 — hoà giải là đổi hành vi điều hướng của module đang chạy, phải có WO riêng + QA.

### 3. Thử-phá cổng (2 phép, đã revert)

| Phép thử | Kết quả |
| --- | --- |
| Đặt `RECRUIT.route = '/recruit'` (rootPath) | **ĐỎ** 1 failed / 30 passed — `expected '/recruit' to be '/recruit/job-openings'` |
| Xoá entry `GOAL` | **ĐỎ** 6 failed / 25 passed — `MISSING_METADATA` + literal route/icon + DENY/ALLOW/wildcard GOAL + ca "đúng 1 cặp engine" |

Cổng bắt được cả ba chiều (thiếu · sai giá trị · lệch literal), không phải chỉ đếm số key.

### 4. Verify TAY — parity BE ↔ FE (yêu cầu của done_when)

Đọc thẳng migration, không suy từ comment:

- **manager** có đúng 3 grant RECRUIT (`0560:105-107`): `access:recruit@Own` · `view:interview@Own` · `feedback:interview@Own` — **KHÔNG có `view:job-opening`**. Với `requiredAny=[view:job-opening]`, `my-apps` **KHÔNG** trả thẻ RECRUIT cho manager. FE gate `requiredPermissions: ["access:recruit","view:job-opening"]` (AND, `registry.ts`) cũng ẩn ⇒ **khớp**.
- **employee** có đúng 3 grant PAYROLL (`0565:227-229`): `access:payroll@Own` · `view-own-payslip:payslip@Own` · `acknowledge-own-payslip:payslip@Own` — **KHÔNG có `view:payroll-period`** ⇒ BE ẩn, FE (`["access:payroll","view:payroll-period"]`) cũng ẩn ⇒ **khớp**.

Chiều ngược lại vẫn LỆCH và được ghi thành nợ: `requiredAny` là **OR** nên role có `view:X` mà **thiếu** `access:X` sẽ **BE-hiện / FE-ẩn**. Parity tuyệt đối cần `requiredAll` trong `ModuleAppMeta` — **ngoài phạm vi** WO 🟡 (đổi `hasAnyCapability` = đổi hành vi cổng).

### 5. Verify đã chạy

| Lệnh | Kết quả |
| --- | --- |
| ratchet unit-spec | **31/31 XANH** (RED trước: 18 failed) |
| `module-app-metadata-coverage.int.spec.ts` | **5/5 XANH** (RED trước: 1 failed) |
| `my-apps-canonical-role.int-spec.ts` | **4/4 XANH**, KHÔNG thêm/bớt ca |
| `migration-smoke.int-spec.ts` | **63/63 XANH**, giữ nguyên số ca |
| `module-admin.service.spec.ts` | **9/9 XANH**, giữ nguyên số ca `it()` |
| `src/foundation/**` | 44 file / 697 test XANH |
| `pnpm typecheck` · `pnpm build` | XANH (build 7/7 task) |
| `bash harness/check.sh --lane-db=s14fndmodulemeta1` | **XANH ✅** — KHÔNG rơi vào "XANH KHÔNG ĐỦ BẰNG CHỨNG" |

Vá **fixture-đinh**: `module-admin.service.spec.ts` từng dùng `PAYROLL` làm mẫu "module thiếu metadata"; PAYROLL nay CÓ metadata nên ca sẽ đo nhầm nhánh mà vẫn xanh. Đổi sang `MOBILE` (Phase 5, inactive, cố ý chưa có metadata) **và** thêm tiền đề tường minh `expect(MODULE_APP_METADATA.MOBILE).toBeUndefined()` — wave sau thêm metadata MOBILE thì ca ĐỎ NGAY thay vì âm thầm hết phủ nhánh fail-soft.

### 6. Bất biến · hot-file · phạm vi

Không đụng 3 bất biến: `modules` là catalog GLOBAL no-RLS (không `company_id`), không bảng audit append-only, không secret. **0 migration mới**, không `UPDATE modules ... is_active`, không sửa `harness/backlog.mjs`, không sửa `packages/web-core/src/lib/registry.ts` (chỉ ĐỌC đối chiếu), không đổi `hasAnyCapability`/`getMyApps`/`toAdminItem`, không đổi `SENSITIVE_CAPABILITY_ALLOWLIST`. `packages/contracts/src/` và `apps/app/src/` = **0 dòng** (`myAppItemSchema` giữ nguyên ⇒ contract API không đổi). Hot-file `module-app-metadata.ts` = APPEND cuối object; plan doc này = APPEND mục mới; BACKEND-04 §8.2 = thay 2 danh sách STALE + thêm bảng mới.

### 7. NỢ MỞ & BÀN GIAO

**Bàn giao bắt buộc — WO nào bật cờ module phải thêm metadata CÙNG COMMIT:**

- **S16-SOCIAL-FE-1** (`UPDATE modules SET is_active=true` cho `SOCIAL`): phải APPEND entry `SOCIAL` vào `MODULE_APP_METADATA` + gỡ `SOCIAL` khỏi `EXEMPT_MODULES` của ratchet. Quên ⇒ int-spec coverage **ĐỎ** (đúng thiết kế) thay vì thẻ app biến mất trong im lặng.
- **WO bật `CHAT`**: y hệt.
- Thêm hàng `modules` mới ⇒ phải **bump có chủ ý** hằng "17 mã" trong ratchet, kèm quyết định "có metadata hay vào EXEMPT".

**Nợ 🟡 (đã ghi ở BACKEND-04 §8.2):** (a) `requiredAll` để parity tuyệt đối với FE `requiredPermissions` (AND); (b) 3 `feCode` chưa map trong `PERMISSION_CODE_TO_PAIR` — `GOAL.ACCESS` · `RECRUIT.JOB.VIEW` · `PAYROLL.PERIOD.VIEW` (chỉ dùng để hiển thị, không enforce ⇒ không lỗ quyền); (c) hoà giải 3 route di sản ATT/LEAVE/TASK về `defaultRoute`.

**Câu hỏi mở cho owner:** có seed hàng `modules.LMS` để app vệ tinh LMS lên my-apps không? Hiện LMS/fbpost là app SSO **không có hàng `modules`** ⇒ key metadata `LMS` sẽ là dữ liệu chết và bị cổng chiều-ngược `ORPHAN_METADATA` **chặn**; muốn có thì cần migration ⇒ WO riêng.
