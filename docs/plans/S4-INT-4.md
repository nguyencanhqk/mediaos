```yaml
wo: S4-INT-4
zone: red
generated_by: auto-loop
reconciled_at: "db8f081"
lanes: [{"id":"attProducerActor","task":"Producer gap-fix (crown FSM ATT, additive-only): thêm field actorUserId vào 7 payload outbox ATT ĐÃ TỒN TẠI để engine actor-exclusion (notification-recipient-resolver.service.ts:50 đọc dto.actorUserId ← bridge lấy payload.actorUserId, outbox-notification-bridge.service.ts:69) chạy đúng. Điểm chèn — adjustment: attendance-adjustment.apply.ts emitAdjustmentRequested outbox (~L153, +actorUserId:p.actorId) + emitAdjustmentApproved outbox (~L186, +actorUserId:p.actorId); attendance-adjustment.service.ts reject outbox (~L376, +actorUserId:actor.id). Remote: remote-work-request.service.ts submit (~L214) + approve (~L361) + reject (~L453) + cancelOwn (~L495) — mỗi chỗ +actorUserId:actor.id. CHỈ thêm 1 field vào payload trong outbox.enqueue của tx nghiệp vụ hiện có — KHÔNG đổi chữ ký/FSM, KHÔNG đụng attendance_logs/attendance_adjustment_items/remote_work_request_approvals append-only, KHÔNG đụng audit. LOẠI attendance.record_adjusted (adjust-direct, ngoài phạm vi WO).","builder":"backend-builder","paths":["apps/api/src/attendance/attendance-adjustment.apply.ts","apps/api/src/attendance/attendance-adjustment.service.ts","apps/api/src/attendance/remote-work-request.service.ts"]},{"id":"attNotiBridgeReg","task":"Đăng ký 7 mapping ATT lên OutboxNotificationBridge (INT-1) — TÁI DÙNG bridge generic, KHÔNG class consumer/bridge MỚI. New att-noti-bridge.registrar.ts (OnModuleInit, mirror TaskNotiBridgeRegistrar; import CHỈ notifications/**+db/** — KHÔNG import AttendanceModule, giữ acyclic) + new att-approval-audience.reader.ts (raw-SQL withTenant+company_id bind: resolveAdjustment(requestId)→direct_manager_id của subject employee [employee_profiles.direct_manager_id references users.id] cho recipient submitted; resolveRemote(requestId)→{requestedBy, currentApproverUserId, watcherUserIds} cho approve/reject/cancel). Map dot-eventType→eventCode VERBATIM từ notification-event-catalog.const.ts:72-79. Recipient §9.4: adjustment_submitted→approver(direct manager); adjustment_approved/rejected→requester=payload.userId; remote_submitted→currentApproverUserId∪watcherUserIds(payload); remote_approved/rejected→requester=requestedBy(reader); remote_cancelled→approver∪watchers(reader). APPEND 7 khoá strategy:'DedupeKey' (dedupeKey=ctx.eventId) vào notification-dedupe.const.ts. APPEND AttNotiBridgeRegistrar+AttApprovalAudienceReader vào notifications.module.ts providers[] (khối comment S4-INT-4 additive — KHÔNG rewrite khối INT-1/exports[]/controllers[]).","builder":"backend-builder","paths":["apps/api/src/notifications/att-noti-bridge.registrar.ts","apps/api/src/notifications/att-approval-audience.reader.ts","apps/api/src/notifications/notification-dedupe.const.ts","apps/api/src/notifications/notifications.module.ts"]},{"id":"attNotiIntSpec","task":"Int-spec RED-trước (Postgres cô lập LANE_DB): att-noti-e2e.int-spec.ts — đường thật producer(ATT service)→OutboxWorker.processBatch→noti-bridge:attendance.*→OutboxNotificationBridge→NotificationEngineService.intake→notifications+notification_delivery_logs. Phủ: 7 hành động ATT→đúng số notification+đúng recipient_user_id (adjustment submit=direct manager; adjustment approve/reject=subject userId; remote submit=approver+watchers; remote approve/reject=requester(requestedBy); remote cancel=approver+watchers); actor KHÔNG tự nhận (manager approve→chỉ requester; employee tự cancel remote Pending→approver nhận, actor KHÔNG); idempotent 2 tầng (re-consume cùng event_id→vẫn 1 notification: processed_events + partial-unique uq_notifications_dedupe_active dedupeKey=eventId); cross-tenant deny (event company A→0 notification cho recipient company B); boot-guard standalone 7 eventCode ATT enabled không throw. Tắt tạm registrar→RED, bật→GREEN.","builder":"backend-builder","paths":["apps/api/test/integration/att-noti-e2e.int-spec.ts"]}]
acceptanceChecks: ["7 mapping ATT đăng ký lên OutboxNotificationBridge qua registerSource — KHÔNG có class consumer/bridge MỚI ngoài registrar+reader (grep: không thêm EventBus.register thủ công, không gọi NotificationEngine trực tiếp ngoài bridge).","eventType→eventCode VERBATIM khớp notification-event-catalog.const.ts:72-79: attendance.adjustment_requested→ATT_ADJUSTMENT_SUBMITTED, attendance.adjustment_approved→ATT_ADJUSTMENT_APPROVED, attendance.adjustment_rejected→ATT_ADJUSTMENT_REJECTED, attendance.remote_request_submitted→ATT_REMOTE_REQUEST_SUBMITTED, attendance.remote_request_approved→ATT_REMOTE_REQUEST_APPROVED, attendance.remote_request_rejected→ATT_REMOTE_REQUEST_REJECTED, attendance.remote_request_cancelled→ATT_REMOTE_REQUEST_CANCELLED (không hard-code chuỗi lệch).","Recipient §9.4/WO đo được bằng test: adjustment_submitted→approver=subject.direct_manager_id (user_id); adjustment_approved/rejected→requester=payload.userId; remote_submitted→currentApproverUserId∪watcherUserIds; remote_approved/rejected→requester=requestedBy; remote_cancelled→approver∪watchers; actorUserId LOẠI khỏi recipient (engine actor-exclusion).","payload 7 outbox ATT MANG actorUserId — chứng minh bằng test đọc outbox_events.payload->>'actorUserId' NOT NULL cho cả 7 eventType; countOutbox cũ (attendance-adjustment.int.spec, remote-work-request.int.spec) KHÔNG đổi.","consumerName DUY NHẤT: noti-bridge:attendance.adjustment_* / noti-bridge:attendance.remote_request_* (7 mã) — EventBus.register ném nếu trùng; attendance-leave-sync (leave.request.approved) là eventType KHÁC → chạy song song không đá nhau (attendance-leave-sync.int.spec xanh).","Idempotent retry: re-consume CÙNG outbox event → vẫn đúng 1 notification/recipient; chặn 2 tầng (OutboxWorker.processed_events theo consumer_name+event_id; partial-unique uq_notifications_dedupe_active theo company+recipient+event_code+dedupe_key=eventId).","Cross-tenant: event company A KHÔNG tạo notification cho recipient company B (RLS + resolver filterActiveUsers eq company_id); reader withTenant+company_id bind; droppedCount/skip đúng.","Zero-migration: head mig giữ 0493; chỉ thêm const DedupeKey + 2 provider + 2 file mới trong notifications/** + producer field — KHÔNG file migrations/** mới, KHÔNG đụng seed catalog (7 eventCode đã isEnabled=true).","DoD §8: input đọc payload an toàn (strField/strArrayField), audit đã có sẵn ở producer (KHÔNG đụng), FE N/A, có int-spec, không phá luồng ATT adjustment/remote; FULL gate security-reviewer + silent-failure-hunter PASS (handler bridge RE-THROW — không nuốt lỗi, OutboxWorker retry/dead-letter).","Coverage int ≥80% module nhạy cảm; check.sh --all không LOUD 'XANH KHÔNG ĐỦ BẰNG CHỨNG' (int-spec chạy thật dưới LANE_DB)."]
testTasks: ["Int-spec att-noti-e2e.int-spec.ts (test/integration, Postgres cô lập LANE_DB): mỗi hành động trong 7 → đúng số notification + đúng recipient_user_id (adjustment submit=direct manager; adjustment approve/reject=subject userId; remote submit=approver+watchers; remote approve/reject=requester=requestedBy; remote cancel=approver+watchers).","Deny-path RED workflow actor-exclusion: manager approve đơn adjustment → CHỈ requester(subject) nhận, approver KHÔNG; employee tự cancel đơn remote Pending của mình → approver+watchers nhận, employee(actor) KHÔNG (actorUserId=requestedBy bị loại).","Idempotent RED: mô phỏng OutboxWorker re-consume cùng event_id (retry/reaper) → assert vẫn ĐÚNG 1 notification (kiểm 2 tầng độc lập: xoá dấu processed_events vẫn bị partial-unique uq_notifications_dedupe_active chặn); dùng eventCode strategy 'DedupeKey' (dedupeKey=eventId).","Cross-tenant deny RED: outbox event company A với recipient thuộc company B → 0 notification cho B (RLS + company_id bind reader + resolver filterActiveUsers); đếm skipped/dropped đúng.","Recipient-rỗng RED: adjustment_submitted khi subject không có direct_manager_id (NULL) → recipient rỗng → engine log Skipped, KHÔNG tạo delivery_log ma (SPEC-08 §16.4); remote Draft-cancel (chưa submit, currentApproverUserId NULL) → 0 notification, không throw.","Boot-guard: standalone test registerSource 7 eventCode ATT (isEnabled=true) không throw; wire nhầm mã disabled → throw tại boot (fail-loud, mirror INT-1 boot-guard PROJECT_MEMBER_REMOVED).","Regression: attendance-leave-sync.int.spec (consumer attendance-leave-sync trên leave.request.approved vẫn xanh — không xung đột) + noti-event-intake.int.spec + attendance-adjustment.int.spec + remote-work-request.int.spec (countOutbox cũ KHÔNG đổi khi thêm field actorUserId).","QA đối chiếu: SPEC-04 §15 (adjustment/remote approval → đúng người nhận theo data scope) + SPEC-08 §16 (không resolve được recipient → log Skipped, không delivery_log ma)."]
steps: ["Lane attProducerActor: thêm actorUserId vào 7 payload outbox ATT (adjustment_requested/approved/rejected + remote submit/approve/reject/cancel). Chỉ additive field trong outbox.enqueue của tx hiện có — verify KHÔNG đổi countOutbox hiện có (attendance-adjustment.int.spec + remote-work-request.int.spec vẫn xanh). KHÔNG đụng emitRecordAdjustedDirect (attendance.record_adjusted ngoài phạm vi).","Lane attNotiBridgeReg (song song, merge cùng PR sau producer): viết att-approval-audience.reader.ts (resolveAdjustment→direct_manager_id subject; resolveRemote→requestedBy+currentApproverUserId+watcherUserIds; SELECT ... WHERE company_id=$1 bind tường minh, trả null/[] khi thiếu → engine log Skipped SPEC-08 §16.4); viết att-noti-bridge.registrar.ts đăng ký 7 mapping (eventType→eventCode VERBATIM từ catalog:72-79; resolveRecipients theo §9.4).","Lane attNotiBridgeReg: APPEND 7 khoá ATT_ADJUSTMENT_*/ATT_REMOTE_REQUEST_* vào DEFAULT_DEDUPE (notification-dedupe.const.ts) strategy:'DedupeKey' windowSeconds:null (dedupeKey mặc định=ctx.eventId — once-ever/outbox event, chống trùng khi OutboxWorker retry).","Lane attNotiBridgeReg: APPEND AttNotiBridgeRegistrar + AttApprovalAudienceReader vào notifications.module.ts providers[] (khối comment S4-INT-4 additive — KHÔNG sửa khối INT-1, OutboxNotificationBridge/exports[]/controllers[] giữ nguyên). Zero-migration (head giữ 0493).","Lane attNotiIntSpec: viết att-noti-e2e.int-spec.ts RED-trước (tắt tạm registrar khỏi providers[]→assert đỏ), bật lại→xanh; chạy regression attendance-leave-sync.int.spec (consumer attendance-leave-sync trên leave.request.approved KHÔNG bị đá) + noti-event-intake.int.spec + attendance-adjustment.int.spec + remote-work-request.int.spec (countOutbox không đổi).","Verify DB cô lập: bash scripts/lane-db-setup.sh int4 → export LANE_DB=mediaos_int4 → pnpm --filter @mediaos/api test; trước PR vùng đỏ: bash harness/check.sh --all (int-spec skip ≤ ngưỡng, exit 1 nếu vượt).","FULL gate crown: security-reviewer + silent-failure-hunter (+ santa-method) PASS; plan qua plan-reviewer trước code; ghi docs/plans/S4-INT-4.md (khối yaml máy-đọc); cập nhật harness/backlog.mjs; serialize MERGE 1 PR SAU INT-1 (đã ở master); nếu INT-3 LEAVE merge trước → rebase khối additive notifications.module.ts + notification-dedupe.const.ts."]
```

GAP-ANALYSIS (code hiện tại — 2026-07-12, master head mig 0493): (1) ⚠️ PREMISE WO SAI — 'ATT hiện CHƯA phát event nào' KHÔNG đúng với code. ATT ĐÃ phát 7+ outbox event: adjustment (attendance-adjustment.service.ts:376 reject; attendance-adjustment.apply.ts:153 emitAdjustmentRequested, :186 emitAdjustmentApproved) + remote-work (remote-work-request.service.ts:214 submit, :361 approve, :453 reject, :495 cancelOwn). SPEC-thắng-code: sửa premise — đây là bài toán GIỐNG HỆT INT-3 (LEAVE): producer đã có, chỉ THIẾU actorUserId trong payload + chưa wire bridge. (2) GAP THẬT #1 (producer): bridge đọc payload.actorUserId (outbox-notification-bridge.service.ts:69) để engine actor-exclusion (notification-recipient-resolver.service.ts:50); 7 payload ATT hiện KHÔNG mang actorUserId (chỉ có approvedBy/rejectedBy/cancelledBy/userId) ⇒ 'actor không tự nhận' FAIL. Vá additive: +actorUserId vào 7 payload (attendance/**). (3) GAP THẬT #2 (wiring): chưa có att-noti registrar/reader trên bridge INT-1 (đã merged 85ee393 #168) — thêm 1 registrar (mirror TaskNotiBridgeRegistrar) + 1 reader raw-SQL. KHÔNG bridge/consumer mới (registerSource là extension point). (4) 7 eventCode ATT_ADJUSTMENT_SUBMITTED/APPROVED/REJECTED + ATT_REMOTE_REQUEST_SUBMITTED/APPROVED/REJECTED/CANCELLED đã isEnabled=true trong notification-event-catalog.const.ts:72-79 → boot-guard PASS, ZERO-MIGRATION (head giữ 0493, KHÔNG seed/DB mới). (5) RECIPIENT §9.4 — điểm cần chú ý: adjustment KHÔNG lưu approver tường minh (HrTasks approval task assigneeUserId=null pool) ⇒ approver cho adjustment_submitted = direct_manager của subject employee (employee_profiles.direct_manager_id references users.id, employees.ts:53) — reader.resolveAdjustment đọc y hệt cơ chế LeaveApproverReader INT-3. Remote_submitted có currentApproverUserId + watcherUserIds SẴN trong payload → đọc thẳng. Remote_approved/rejected payload chỉ có employeeId+approvedBy/rejectedBy (KHÔNG có requester) ⇒ reader.resolveRemote đọc remote_work_requests.requested_by (attendance.ts:255). Remote_cancelled recipient = approver∪watchers (reader) vì actor=requester bị loại (self-cancel→0 nếu recipient=requester). INVARIANTS: #1 company_id — reader withTenant+bind company_id, engine resolver RLS ẩn cross-tenant; #2 append-only — producer CHỈ thêm field payload trong outbox.enqueue của tx nghiệp vụ hiện có, KHÔNG đụng attendance_logs/attendance_adjustment_items/remote_work_request_approvals/audit; handler bridge RE-THROW (không nuốt lỗi, OutboxWorker retry/dead-letter). CONSUMER UNIQUENESS: bridge tự đặt consumerName noti-bridge:attendance.* (7 mã) — EventBus cho nhiều consumer/eventType (idempotency riêng consumer_name+event_id); attendance-leave-sync dùng eventType KHÁC (leave.request.approved) → không đá. VERIFY: int-spec dưới LANE_DB cô lập (scripts/lane-db-setup.sh int4; check.sh --all trước PR vùng đỏ, exit 1 nếu int-spec skip vượt ngưỡng — deny-path/idempotent/cross-tenant chạy THẬT). GATE: crown/FULL — security-reviewer + silent-failure-hunter (+ santa-method), plan qua plan-reviewer trước code. MERGE: 1 PR SAU INT-1 (đã master). INT-3 (LEAVE) CHƯA merge master (không có leave-noti registrar) — INT-4 chỉ depends_on INT-1; nếu INT-3 land trước → rebase khối additive notifications.module.ts providers[] + notification-dedupe.const.ts (2 hot-file chung, append KHÔNG rewrite). OUT-OF-SCOPE: (a) events/** KHÔNG đổi (tái dùng EventBus/OutboxService/OutboxWorker). (b) attendance.record_adjusted (adjust-direct emitRecordAdjustedDirect apply.ts:210) — WO chỉ adjustment submit/approve/reject → KHÔNG wire, KHÔNG thêm actorUserId. (c) ATT_MISSING_CHECKOUT/ATT_LATE_DETECTED/ATT_ABSENT_DETECTED/ATT_AUTO_ATTENDANCE_CREATED (enabled trong catalog nhưng thuộc luồng chấm công tự động, chưa có producer/ngoài WO) — KHÔNG wire. (d) Template render ATT_* — nếu thiếu template thì renderer fallback tiêu đề mặc định; thêm template là seed/migration → out-of-scope owner (chỉ register+recipient, zero-migration), flag verify chứ không chặn. HOT-FILE & MERGE: notifications.module.ts providers[] APPEND khối S4-INT-4 (KHÔNG rewrite INT-1); notification-dedupe.const.ts APPEND 7 khoá cuối object (KHÔNG sửa khoá TASK/INT-1); thứ tự merge sau INT-1, đồng bộ với INT-3 nếu chạy song song.
</content>
