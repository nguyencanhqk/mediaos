```yaml
wo: HR-PERF-1
zone: red
generated_by: auto-loop
reconciled_at: "f6a0054"
lanes: [{"id":"feLazyRouter","task":"(a) Code-split apps/app router: chuyển static import trang mỗi module (HR/TASK/LEAVE/ATT/NOTI/DASH/System) sang React.lazy(() => import(...)) + bọc React.Suspense (fallback loading) TRONG buildModuleRouteContent (file duy nhất được phép). Mục tiêu: mở /hr KHÔNG tải chunk TASK/LEAVE/ATT (Vite auto-split theo dynamic import). GIỮ NGUYÊN route path + permission gate: getMeta/RouteMeta/authGuard/ProtectedRoute không đổi — chỉ đổi cách nạp component. Build trước/sau, ghi số initial JS (KB) + danh sách chunk vào PR.","builder":"frontend-builder","paths":["apps/app/src/router.tsx"]},{"id":"beBatchPermHr","task":"(b) CROWN: (1) Trích logic 4-tier ra pure fn dùng CHUNG cho can() và canBatch (chống drift — 1 nguồn sự thật cho object-DENY priority-1, sensitive-gate wildcard-fail, company-DENY override, fail-closed). (2) Thêm IPermissionRepository.getObjectGrantsBatch(userId,companyId,resourceType,resourceIds[]) → Map<resourceId,ObjectGrant[]> (1 query userRoles + 1 inArray object query) ở permission.repository.ts + passthrough/mget ở permission.cache.ts + type ở permission.types.ts. (3) PermissionService.canBatch cho hr-read list: fetch company grants 1 lần + object-batch 1 lần, trả decision per (resourceId×action) BẰNG CHÍNH XÁC per-row can(). (4) Wire HrReadService.listHrEmployees dùng canBatch cho view-salary + view-sensitive; GIỮ reveal⟹audit atomic per-row trên tenant tx (audit vẫn ghi per revealed row); detail/summary/me giữ can() đơn. ≤4 query permission/trang. (c) DEFER pg_trgm: KHÔNG tạo migration — ghi quyết định + điều kiện bật lại (headcount >1–2k) vào docs/plans/HR-PERF-1.md.","builder":"backend-builder","paths":["apps/api/src/permission/**","apps/api/src/employees/**","docs/plans/HR-PERF-1.md"]}]
acceptanceChecks: ["(a) Build sinh chunk RIÊNG mỗi module qua dynamic import; mở /hr KHÔNG tải chunk chứa Task/Leave/Attendance pages (kiểm bằng output build hoặc Network). Initial JS bundle GIẢM đo được so baseline (ghi số KB trước/sau vào PR).","(a) Route KHÔNG đổi: mảng addChildren, path mỗi route, RouteMeta/getMeta gate, authGuard/ProtectedRoute giữ nguyên; typecheck xanh; app render đúng trang sau lazy + Suspense fallback (loading/error/empty vẫn xử lý).","(b) PermissionService.canBatch tồn tại; trên fixture TRỘN object-ALLOW + object-DENY (+ wildcard *:* + company-DENY override + sensitive), kết quả per (resourceId×action) BẰNG CHÍNH XÁC PermissionService.can() từng-row (so khớp allow+reason+auditRequired).","(b) object-DENY priority-1 GIỮ NGUYÊN: 1 row có object DENY view-salary → canBatch trả deny bất kể company ALLOW; sensitive gate: wildcard *:* KHÔNG mở view-salary/view-sensitive (deny-sensitive) — y hệt can().","(b) Số query permission/trang ≤ 4: spy repository → getCompanyRoleGrants gọi ĐÚNG 1 lần + getObjectGrantsBatch ĐÚNG 1 lần cho 1 trang N rows (KHÔNG 2N getObjectGrants). Cold-cache/Valkey-down vẫn ≤4.","(b) deny-path GIỮ NGUYÊN: không read:employee → resolveAndAssert 403 TRƯỚC mọi batch/repo read. Masking per-row không đổi: row có object-ALLOW view-salary → baseSalary lộ + audit ghi; row object-DENY → null; reveal⟹audit atomic per-row trên tenant tx (audit fail → rollback read).","(b) BẤT BIẾN #1/#3: mọi read qua withTenant(company_id); object-grant batch KHÔNG rò id tenant khác (RLS, integration LANE_DB cross-tenant xanh); KHÔNG log/return secret; FULL gate security-reviewer PASS cho (b).","(c) KHÔNG có file migration mới trong git diff WO này (git status/diff apps/api/**/migrations sạch). docs/plans/HR-PERF-1.md CHỨA quyết định DEFER pg_trgm + điều kiện bật lại (headcount >1–2k).","DoD §8: có test (unit equivalence + query-count + deny-path RED + integration cô lập); permission.service.spec & hr-read.service.spec CŨ giữ xanh; harness/backlog.mjs cập nhật; không phá luồng chính."]
testTasks: ["RED-first (crown) permission.service.spec.ts: canBatch === can() per-row — bảng fixture gồm (i) object-ALLOW view-salary 1 row, (ii) object-DENY view-salary 1 row (priority-1 phải thắng company-ALLOW), (iii) chỉ company-ALLOW, (iv) wildcard *:* + sensitive → deny-sensitive, (v) company-DENY override, (vi) no-grant → deny-default. Assert khớp allow+reason+auditRequired với can() cho MỌI ô.","permission.service.spec.ts query-count: spy IPermissionRepository → chạy canBatch cho N ids (N≥3) → assert getCompanyRoleGrants gọi 1 lần + getObjectGrantsBatch 1 lần + getObjectGrants (single) KHÔNG gọi; assert tổng ≤4.","permission.service.spec.ts fail-closed: repo.getCompanyRoleGrants|getObjectGrantsBatch throw → mọi decision trong Map = allow:false (deny-default), không false-ALLOW, có log.","hr-read.service.spec.ts (deny-path RED, giữ nguyên bộ cũ + mở rộng): không read:employee → 403 trước batch; page trộn 1 row object-ALLOW view-salary (baseSalary lộ + audit.record gọi trên tx) + 1 row object-DENY (baseSalary null, không audit); PII view-sensitive per-row đúng; xác nhận listHrEmployees gọi canBatch (không vòng 2N can()).","Integration cô lập LANE_DB (cross-tenant/RLS): seed object_permissions cho id thuộc tenant B → canBatch của actor tenant A trên ids A KHÔNG kéo grant tenant B; batch chạy trong withTenant(company_id). Chạy bash harness/check.sh --lane-db.","FE router smoke (feLazyRouter): build thành công; route tree giữ path/meta (assert số route + path không đổi); (tùy) script đo initial JS trước/sau + xác nhận chunk HR không kèm Task/Leave/Att.","QA regression: pnpm test toàn suite — permission.g3-4.spec / permission.scopes.spec / hr-read.controller.spec / hr-read.service.spec CŨ giữ xanh (không regress semantics can())."]
steps: ["THỨ TỰ: 2 lane path-disjoint → chạy SONG SONG (feLazyRouter độc lập beBatchPermHr; không hot-file chung, không migration).","Lane beBatchPermHr (CROWN, plan-first Sonnet5 xhigh → plan-reviewer trước khi code): (1) Trích pure fn decideCan(companyGrants, objectGrants, input, now) TỪ can() hiện tại — refactor can() gọi lại nó (KHÔNG đổi hành vi can(), giữ nguyên spec test cũ xanh). RED-first: viết test object-DENY priority-1 + equivalence TRƯỚC.","Lane beBatchPermHr: (2) Thêm getObjectGrantsBatch vào permission.types.ts (interface + ObjectGrantBatch type), permission.repository.ts (1 truy vấn userRoles roleIds + 1 truy vấn object dùng inArray(objectId, ids), gộp about subjectConditions user+role), permission.cache.ts (passthrough inner batch HOẶC mget theo objKey rồi fill-miss — KHÔNG được fan-out N query).","Lane beBatchPermHr: (3) canBatch(userId,companyId,resourceType,resourceIds[],actions[],opts) → fetch getCompanyRoleGrants 1 lần + getObjectGrantsBatch 1 lần → for mỗi (resourceId×action) gọi decideCan CHUNG. Trả Map/Record. Fail-closed toàn cục nếu repo lỗi (mỗi decision = deny-default), mirror can().","Lane beBatchPermHr: (4) HrReadService.listHrEmployees — thay vòng lặp 2N can() (revealSalary+canViewSensitive per-row) bằng 1 canBatch(view-salary,view-sensitive) trên page ids; GIỮ ghi audit view-salary per revealed row TRÊN tenant tx (reveal⟹audit atomic không đổi). getHrEmployee/getMyProfile/getEmployeesSummary GIỮ NGUYÊN can() đơn.","Lane beBatchPermHr: (c) Ghi vào docs/plans/HR-PERF-1.md mục 'pg_trgm DEFER' — quyết định owner 2026-07-12 (headcount nhỏ, KHÔNG migration WO này) + điều kiện bật lại (headcount >1–2k → GIN pg_trgm cho search cột họ tên/mã NV) + rủi ro append-only/expand-contract khi bật.","Lane feLazyRouter (SONG SONG, LIGHT gate): đổi top-import trang module sang const LazyX = React.lazy(() => import('@/routes/...')); bọc <React.Suspense fallback=...> trong buildModuleRouteContent (bao mọi module route). Route tĩnh/nghiệp vụ (home/account/403/404) giữ hoặc bọc tương tự. KHÔNG đổi addChildren order, path, meta, gate.","Lane feLazyRouter: pnpm --filter @mediaos/web build (hoặc app tương ứng) TRƯỚC/SAU; ghi initial JS (KB) + xác nhận chunk HR tách khỏi TASK/LEAVE/ATT vào PR.","GATE: beBatchPermHr = FULL (security-reviewer + silent-failure-hunter + santa-method crown) + database-reviewer (repo/RLS) + typescript-reviewer + quality-gate; chạy integration cô lập LANE_DB (bash harness/check.sh --lane-db) cho cross-tenant object-grant batch. feLazyRouter = LIGHT (react-reviewer + quality-gate) + react-build-resolver nếu build đỏ.","Verify cuối: pnpm typecheck + pnpm test; permission.service.spec cũ + hr-read.service.spec cũ GIỮ XANH (không regress masking/deny-path); xác nhận KHÔNG có file migration mới trong git diff (c deferred)."]
```

GAP-ANALYSIS (code hiện tại 2026-07-12):
- (a) apps/app/src/router.tsx import TĨNH ~toàn bộ page component ở đầu file (HR/ATT/LEAVE/TASK/NOTI/DASH/System) ⇒ tất cả vào 1 initial bundle (~1.55MB). buildModuleRouteContent + makeModuleRoute là điểm bọc chung; CHƯA có React.lazy/Suspense/manualChunks trong apps/app (grep = 0). vite.config.ts KHÔNG nằm trong WO paths ⇒ dựa vào Vite auto code-split theo dynamic import (đủ để 'mở HR không tải TASK/LEAVE/ATT'), KHÔNG cần sửa vite.config. Suspense boundary phải sống TRONG router.tsx (file duy nhất được phép) — đặt trong buildModuleRouteContent bao mọi module route.
- (b) HrReadService.listHrEmployees hiện gọi PER-ROW: revealSalary(can view-salary, resourceId=row.id) + canViewSensitive(can view-sensitive, resourceId=row.id) ⇒ 2N can()/trang. Mỗi can() = getCompanyRoleGrants(1) [cache Valkey 5m] + getObjectGrants(resourceId) mà getObjectGrants NỘI BỘ = 2 truy vấn (userRoles roleIds + object). Cold-cache ⇒ ~4N truy vấn/trang. canBatch gộp: getCompanyRoleGrants(1) + getObjectGrantsBatch(1: userRoles 1 + inArray object 1) ⇒ ≤3 ≤4.

THIẾT KẾ CHỐNG-DRIFT (crown): TRÍCH pure fn decideCan(companyGrants, objectGrants, input, now) từ can() (giữ nguyên thứ tự 4-tier: object-DENY p1 → object-ALLOW p2 → company-DENY p3 → objectGrantRequired F2 → effectivelySensitive exact-only → company-ALLOW p4 → deny-default; reauth/expiresAt re-check). can() và canBatch DÙNG CHUNG fn ⇒ KHÔNG THỂ lệch ngữ nghĩa. canBatch chỉ khác ở tầng FETCH (batch) không ở tầng DECIDE.

INVARIANTS (CLAUDE.md §2): #1 mọi read qua withTenant(company_id) — getObjectGrantsBatch chạy trong withTenant, RLS chặn cross-tenant (integration LANE_DB phải chứng minh). #2 audit append-only: reveal view-salary ⟹ audit.record TRÊN tenant tx của caller (per revealed row) — canBatch chỉ thay tầng ĐỌC quyết định, HrReadService GIỮ ghi audit per-row (audit fail → rollback read). #3 không secret: canBatch không log/return grant nhạy cảm ra ngoài; masking server-side per-row không đổi (baseSalary/salaryType/PII null khi không reveal; wildcard *:* KHÔNG mở sensitive).

VERIFY: pnpm typecheck + pnpm test; giữ XANH permission.service.spec/permission.g3-4.spec/permission.scopes.spec/hr-read.service.spec/hr-read.controller.spec (không regress). Deny-path RED trước cho object-DENY priority-1 + query-count spy. Integration cross-tenant cô lập DB: bash harness/check.sh --lane-db (skipIf(!hasDb) không đủ — cần LANE_DB thật, per memory 'Integration test LANE_DB gate').

GATE: (b) crown FULL — security-reviewer + silent-failure-hunter + santa-method + database-reviewer (repo/inArray/RLS) + typescript-reviewer + quality-gate; model routing tự pickModel → Opus code/review, planner Sonnet5 xhigh + plan-reviewer trước code. (a) LIGHT — react-reviewer + quality-gate (+ react-build-resolver nếu build đỏ).

OUT-OF-SCOPE: (c) pg_trgm GIN — DEFER theo owner 2026-07-12 (headcount nhỏ). KHÔNG tạo migration ở WO này (head migration KHÔNG đổi, không có lane db-migration). Điều kiện BẬT LẠI ghi vào docs/plans/HR-PERF-1.md: khi headcount >1–2k → thêm extension pg_trgm + GIN index trên cột tìm kiếm họ tên/mã NV (employee_profiles) để tăng tốc ILIKE search; lưu ý expand-contract (CREATE INDEX CONCURRENTLY, RLS+FORCE giữ, đánh số migration nối tiếp theo head lúc đó). KHÔNG đụng vite.config, KHÔNG đổi route path/permission gate, KHÔNG đổi ngữ nghĩa can()/masking. HOT-FILE: không có object_types/permission-seed/schema.index/app.module bị chạm ⇒ không rủi ro merge-collide; 2 lane path-disjoint hoàn toàn.
</content>

---

## pg_trgm DEFER — quyết định OWNER 2026-07-12 (task (c))

**Quyết định:** HOÃN (không làm ở WO HR-PERF-1). KHÔNG tạo migration nào trong WO này — head migration
GIỮ NGUYÊN, `apps/api/migrations/**` sạch trong git diff của WO này. Lane `beBatchPermHr` chỉ chạm
`apps/api/src/permission/**`, `apps/api/src/employees/**`, `docs/plans/HR-PERF-1.md`.

**Lý do:** headcount hiện tại nhỏ (một công ty, single-tenant N=1). Tìm kiếm HR list dùng `ILIKE '%q%'`
trên cột họ tên / mã NV — với vài trăm hàng, seq-scan/filter là chấp nhận được; chi phí duy trì một GIN
index + extension `pg_trgm` (bảo trì index, WAL, autovacuum) LỚN HƠN lợi ích ở quy mô này (YAGNI). Nút
thắt hiệu năng THỰC của trang list là số truy vấn permission per-row (2N `can()`), đã xử lý ở (b) bằng
`canBatch` (≤2 repo read/trang) — KHÔNG phải tốc độ search.

**Điều kiện BẬT LẠI (trigger):** khi headcount vượt ~1–2k nhân viên (hoặc p95 latency của
`GET /hr/employees?search=...` vượt ngưỡng SLO, hoặc `EXPLAIN ANALYZE` cho thấy seq-scan trên
`employee_profiles` chi phối). Khi đó, mở một WO/lane `db-migration` RIÊNG:
  1. `CREATE EXTENSION IF NOT EXISTS pg_trgm;`
  2. GIN index trigram trên (các) cột tìm kiếm — họ tên (`full_name` từ `users`, hoặc cột tên hiển thị
     dùng trong search) + `employee_code` của `employee_profiles`: `USING gin (col gin_trgm_ops)`.
  3. Cân nhắc index riêng cho mỗi cột được `ILIKE`; đo lại bằng `EXPLAIN ANALYZE` trước/sau.

**Rủi ro append-only / expand-contract khi bật (lưu ý cho lane migration tương lai):**
  - **KHÔNG khóa bảng:** dùng `CREATE INDEX CONCURRENTLY` (build không giữ ACCESS EXCLUSIVE) — nhưng
    `CONCURRENTLY` KHÔNG chạy trong transaction ⇒ tách khỏi migration transaction-wrapped mặc định
    (drizzle bọc mỗi file trong 1 tx); cần file migration riêng chạy statement ngoài tx, hoặc thao tác
    thủ công có kiểm soát. Migrate KHÔNG dừng service → theo memory `migration-expand-contract-required`,
    chia release: (i) thêm extension + index (additive, không đổi query), (ii) đổi query dùng index —
    tránh cửa sổ code-vs-schema lệch.
  - **RLS+FORCE GIỮ NGUYÊN:** index không đổi chính sách RLS; `company_id` vẫn ở mọi query (bất biến #1).
  - **Đánh số migration NỐI TIẾP** theo head migration tại thời điểm bật (không phải head hôm nay).
  - **Append-only cho bảng audit/ledger KHÔNG liên quan** — index này chỉ trên `employee_profiles`/`users`
    (mutable), không đụng bảng append-only.

