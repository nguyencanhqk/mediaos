```yaml
wo: S5-NOTI-FIX-2
zone: red
generated_by: auto-loop
reconciled_at: "33bcdf68"
lanes: [{"id":"noti-fix2-comment","task":"Producer TASK_COMMENT_CREATED + TASK_MENTIONED additive: commentPayload() (task-comments.service.ts) thêm 2 key khớp CHÍNH XÁC placeholder template 0481 — `task_code` (= tasks.task_code) + `actor_name` (= full_name của actor comment). Nguồn field: mở rộng TASK_CORE_SELECT + interface TaskCoreRow thêm cột `taskCode`/task_code (additive, projection dùng chung list/detail/my — chỉ SELECT thêm); resolve tên actor bằng cách mở rộng findActiveEmployeeByUserTx trả kèm full_name HOẶC helper mới findActorDisplayNameTx (join users, withTenant + AND company_id). task.mentioned kế thừa commentPayload qua spread ⇒ tự có task_code+actor_name (giữ nguyên mentionedUserIds/mentionedEmployeeIds). TUYỆT ĐỐI KHÔNG đổi tên các key camelCase cũ (taskId/taskTitle/projectId/actorUserId/actorEmployeeId/assigneeEmployeeId/creatorUserId) — additive only. Xác nhận assertPayloadSafe không reject (task_code/actor_name không thuộc SENSITIVE_PAYLOAD_KEYS).","builder":"backend-builder","paths":["apps/api/src/tasks/task-comments.service.ts","apps/api/src/tasks/task-core.repository.ts"]},{"id":"noti-fix2-project","task":"Producer PROJECT_MEMBER_ADDED additive: payload project.member_added (projects.service.ts addMember, ~dòng 474-483) thêm 2 key khớp placeholder template 0481 — `project_name` (= raw.name, đã có từ findRawByIdTx) + `project_code` (= raw.projectCode). Giữ nguyên key cũ (eventCode/projectId/memberEmployeeId/memberUserId/actorUserId). LƯU Ý raw.projectCode NULLABLE: coalesce an toàn để body KHÔNG rớt lại '{project_code}' khi code null (quyết định: gửi '' hoặc bỏ qua — ghi rõ trong PR; short_body chỉ dùng {project_name} nên luôn sạch). KHÔNG đụng task-core.repository (không chồng lane comment).","builder":"backend-builder","paths":["apps/api/src/tasks/projects.service.ts"]},{"id":"noti-fix2-tests","task":"Integration + đảo characterization (DB cô lập, LANE_DB): (1) int-spec render 3 event — comment (actor≠assignee) + mention (nhân viên trong scope) + addMember (project CÓ code) → drain outbox → GET /notifications → title/short/body KHÔNG còn ký tự '{' placeholder chưa điền, và giá trị điền THẬT (task_code==tasks.task_code, actor_name==tên actor, project_name/project_code==project). (2) Regression: 5 event đã vá 0490 (TASK_ASSIGNED/TASK_STATUS_CHANGED/TASK_PRIORITY_CHANGED/TASK_DUE_DATE_CHANGED/TASK_ASSIGNEE_CHANGED) vẫn render sạch. (3) ĐẢO characterization qa2-e2e-task-noti-dash.int-spec.ts E1b (dòng 296-309): assertion cũ 'còn {task_code}/{actor_name}' → 'đã điền, KHÔNG còn placeholder' (đúng ghi chú in-test 'nếu ai vá … test này PHẢI đỏ ⇒ cập nhật'). TUYỆT ĐỐI KHÔNG đụng E4 (target_url = QA2-CRIT-001, out-of-scope). (4) edge: project không code + actor tự-comment (0 recipient) giữ hành vi.","builder":"backend-builder","paths":["apps/api/test/integration/qa2-e2e-task-noti-dash.int-spec.ts","apps/api/test/integration/task-noti-e2e.int-spec.ts","apps/api/test/integration/noti-event-intake.int-spec.ts"]}]
acceptanceChecks: ["commentPayload() (task-comments.service.ts) emit thêm ĐÚNG 2 key `task_code` (= tasks.task_code) và `actor_name` (= full_name actor) — additive; 8 key camelCase cũ (taskId/taskTitle/projectId/commentId/actorUserId/actorEmployeeId/assigneeEmployeeId/creatorUserId) GIỮ NGUYÊN tên (consumer/dash-cache-invalidate cũ không vỡ).","task.mentioned payload chứa task_code + actor_name (kế thừa commentPayload) VÀ mentionedUserIds/mentionedEmployeeIds như cũ.","project.member_added payload (projects.service.ts) emit thêm `project_name` (raw.name) + `project_code` (raw.projectCode) — additive; key cũ (eventCode/projectId/memberEmployeeId/memberUserId/actorUserId) giữ nguyên.","Đối chiếu 1-1: {task_code}{actor_name} ⊆ payload COMMENT/MENTIONED; {project_name}{project_code} ⊆ payload MEMBER_ADDED (mọi placeholder template 0481 có key tương ứng).","Int-spec LANE_DB: title/short/body 3 event KHÔNG chứa ký tự '{' placeholder chưa điền; giá trị điền THẬT khớp nguồn (task_code, actor_name, project_name, project_code).","Regression: 5 event vá 0490 (TASK_ASSIGNED/TASK_STATUS_CHANGED/TASK_PRIORITY_CHANGED/TASK_DUE_DATE_CHANGED/TASK_ASSIGNEE_CHANGED) vẫn render không placeholder sót.","qa2-e2e-task-noti-dash.int-spec.ts E1b đảo assertion sang hành vi đúng (không còn {task_code}/{actor_name}); E4 (target_url QA2-CRIT-001) KHÔNG bị chạm (vẫn null — out of scope).","BẤT BIẾN #3: task_code/actor_name/project_name/project_code KHÔNG thuộc SENSITIVE_PAYLOAD_KEYS ⇒ assertPayloadSafe không ném; payload vẫn non-sensitive (KHÔNG nội dung comment, ≤ MAX_PAYLOAD_STRING_LEN).","BẤT BIẾN #1: query mới (projection task_code, lookup tên actor, đọc project raw) đi qua db.withTenant + AND company_id tường minh.","check.sh xanh với LANE_DB (deny-path/render int-spec chạy thật); gate = LIGHT + database-reviewer (additive producer + chạm repository query, KHÔNG migration ⇒ KHÔNG FULL).","DoD §8: có test render + regression; không phá luồng chính (assign/comment/mention/addMember); cập nhật harness/backlog.mjs."]
testTasks: ["Int (DB cô lập, LANE_DB gate hasDb && LANE_DB): manager comment trên task của employee (employee=assignee, actor≠recipient) → drain outbox → GET /notifications của employee → TASK_COMMENT_CREATED short_content + body KHÔNG chứa '{', task_code == tasks.task_code, actor_name == tên manager.","Int: manager mention employee-trong-scope trong comment → TASK_MENTIONED render đủ ({actor_name}+{task_code} điền, KHÔNG placeholder).","Int: addMember employee vào project CÓ code → drain → PROJECT_MEMBER_ADDED body 'Bạn đã được thêm vào dự án <name> (<code>).' KHÔNG placeholder; short_content chứa <name>.","Regression int: 5 event 0490 vẫn render sạch (mở rộng/giữ assertion task-noti-e2e.int-spec.ts hoặc qa2 E1).","Đảo characterization qa2-e2e-task-noti-dash.int-spec.ts E1b: KHÔNG xoá test — đổi kỳ vọng từ 'còn placeholder' sang 'đã điền'; KHÔNG đụng E4/E2/E3.","Edge/deny: project KHÔNG có code (project_code null) → xác nhận producer coalesce ⇒ body không rớt lại '{project_code}' (hoặc ghi quyết định); actor tự comment task của mình (0 assignee/watcher khác) → 0 notification (giữ actor-exclusion); mention ngoài scope → 403 BLOCK, 0 outbox (giữ hành vi task-comments.service).","assertPayloadSafe: payload 3 event mới (kèm 4 key) không bị 400 reject."]
steps: ["RED trước: viết/điều chỉnh int-spec kỳ vọng render 3 event KHÔNG còn '{' (dùng project có code + task có task_code) → chạy LANE_DB thấy RED (producer chưa emit field).","Lane noti-fix2-comment: mở rộng TASK_CORE_SELECT + TaskCoreRow thêm task_code (additive); resolve actor full_name (mở rộng findActiveEmployeeByUserTx trả kèm name hoặc helper mới, withTenant + company_id); commentPayload() thêm additive `task_code` + `actor_name` (mention kế thừa qua spread). KHÔNG rename key cũ.","Lane noti-fix2-project: addMember payload thêm additive `project_name` (raw.name) + `project_code` (raw.projectCode, coalesce null an toàn). KHÔNG rename key cũ.","Đối chiếu từng placeholder ⊆ key payload cho 3 template global (0481); xác nhận assertPayloadSafe KHÔNG reject (4 key mới không thuộc SENSITIVE_PAYLOAD_KEYS) + không nội dung nhạy cảm (BẤT BIẾN #3).","Lane noti-fix2-tests: đảo E1b (characterization) sang hành vi đúng; giữ E4 nguyên; thêm regression 5 event 0490; edge null-code + actor-exclusion.","GREEN + verify như CI: bash harness/check.sh --lane-db (Postgres) xanh; gate LIGHT + database-reviewer (chạm repository query, KHÔNG migration); cập nhật harness/backlog.mjs; persist plan doc từ reconcileNotes."]
```

GAP-ANALYSIS (đối chiếu placeholder ↔ payload, bằng chứng đọc code):
- Renderer notification-renderer.service.ts:64-69 = exact-key {key}→payload[key]; thiếu key ⇒ GIỮ nguyên '{key}'. Engine notification-engine.service.ts:64 assertPayloadSafe(payload) + :96 renderer.render(ev,template,payload); bridge outbox-notification-bridge.service.ts:87 forward ctx.payload NGUYÊN (3 mapping TASK/PROJECT không khai payloadOf) ⇒ payload producer chạm renderer trực tiếp.
- Template 0481 (3 event) dùng snake_case: TASK_COMMENT_CREATED body '{actor_name} đã bình luận trong task {task_code}.' / short 'Có bình luận mới trong task {task_code}.'; TASK_MENTIONED body '{actor_name} đã nhắc đến bạn … task {task_code}.'; PROJECT_MEMBER_ADDED body 'Bạn đã được thêm vào dự án {project_name} ({project_code}).' / short chỉ {project_name}.
- Producer THIẾU HẲN các key (không chỉ sai case): commentPayload() (task-comments.service.ts:308-326) có taskId/taskTitle/projectId/commentId/actorUserId/actorEmployeeId/assigneeEmployeeId/creatorUserId — KHÔNG task_code (bất kỳ case) + KHÔNG tên actor (chỉ id). PROJECT payload (projects.service.ts:474-483) chỉ projectId/memberEmployeeId/memberUserId/actorUserId.
- Nguồn field ĐỀU sẵn (additive): tasks.task_code có trên bảng (mkTask insert task_code) nhưng TASK_CORE_SELECT/ TaskCoreRow chưa SELECT ⇒ thêm cột (dùng chung list/detail/my — chỉ additive); actor full_name = users.full_name (mẫu au/cu.full_name trong TASK_CORE_SELECT) ⇒ mở rộng findActiveEmployeeByUserTx (task-core.repository.ts:307) hoặc helper mới; project name/code = raw.name + raw.projectCode (đã trả bởi findRawByIdTx, xác nhận interface Project.projectCode:75 nullable).

QUYẾT ĐỊNH APPROACH (mirror 0490 nhưng khác BẢN CHẤT):
- 0490 §(5) vá 5 event bằng cách ĐỔI TEMPLATE sang camelCase vì producer ĐÃ emit camelCase (taskCode/taskTitle/toStatus). Ở đây producer KHÔNG có field ⇒ dù đổi template case cũng vô ích; PHẢI bổ sung producer. WO title ('task_code/actor_name/project_name' snake_case) + gate note ('chỉ producer additive ⇒ LIGHT') + done_when ('sửa payload; template chỉ khi sai chiều') ⇒ CHỌN Approach B = producer emit snake_case KHỚP template hiện có, KHÔNG migration. Đây là NGOẠI LỆ có chủ đích so với chuẩn camelCase 0490 (payload sẽ lẫn camelCase cũ + 4 key snake mới) — chấp nhận vì additive, không rename, giữ template ổn định, gate nhẹ.
- ALTERNATIVE (crown có thể chọn nếu muốn đồng bộ camelCase toàn TASK như 0490): migration 0497 db-migration lane (nối tiếp head 0496, idempotent UPDATE 3 template global sang {taskCode}/{actorName}/{projectName}/{projectCode} mirror 0490 §(5)) + producer emit camelCase. Lúc đó gate FLIP sang FULL (security-reviewer + database-reviewer + silent-failure-hunter). KHÔNG khuyến nghị cho lần này (thừa blast-radius so với yêu cầu).

EDGE quan trọng — PROJECT_CODE nullable: projects.project_code NULL ⇒ nếu gửi project_code=null renderer giữ '{project_code}' trong BODY (short an toàn vì chỉ {project_name}). done_when[1] yêu cầu BODY không còn placeholder ⇒ producer PHẢI coalesce (gửi '' hoặc rỗng an toàn) HOẶC (nếu crown muốn body sạch tuyệt đối) đây đúng ca 'template sai chiều' → simplify body PROJECT_MEMBER_ADDED bỏ '({project_code})' qua migration idempotent. Primary: coalesce trong producer + test dùng project CÓ code cho happy-path, ghi nhận ca null là minor.

INVARIANTS: #1 mọi query mới withTenant + company_id (projection task_code, actor-name join users theo tenant, project raw). #2 append-only KHÔNG liên quan (chỉ ghi payload outbox mới, không sửa audit). #3 payload non-sensitive — 4 key mới KHÔNG thuộc SENSITIVE_PAYLOAD_KEYS (password/token/salary/identity_number/… — notification-engine.errors.ts:83-99); actor_name = tên hiển thị (đã là pattern của HR/LEAVE template {employee_name}), KHÔNG nội dung comment. §3 luật phụ thuộc: audit/outbox/permission đã có sẵn (WO không tạo module mới).

VERIFY: bash harness/check.sh --lane-db (Postgres, deny-path/render int-spec chạy THẬT); TURBO_FORCE chống cache false-green; đối chiếu từng placeholder ⊆ key payload; qa2 E1b sau vá PHẢI đỏ trước khi cập nhật assertion (đúng ghi chú in-test) — đảo sang hành vi đúng, KHÔNG xoá.

GATE: LIGHT + database-reviewer (additive producer + chạm repository query, KHÔNG migration). NẾU builder buộc phải thêm migration (chọn ALTERNATIVE hoặc simplify template null-code) ⇒ diff chạm migrations/** ⇒ tự động FULL (security-reviewer + database-reviewer + silent-failure-hunter) — khai lại paths cho scheduler.

OUT-OF-SCOPE (KHÔNG làm, tránh scope-creep): E4/QA2-CRIT-001 target_url luôn null (WO riêng — seed 0/39 template có target_url_template); E3/QA2-HIGH-001 NOTIFICATIONS widget cache stale; đổi template 5 event 0490; thêm bảng task_comment_mentions (debt schema ghi ở task-comments.service class-doc); đổi tên/xoá key payload cũ.

HOT-FILE & MERGE: task-core.repository.ts (TASK_CORE_SELECT + TaskCoreRow) = APPEND cột task_code, KHÔNG rewrite projection. Không lane nào khác đụng file này (project lane dùng projects.repository/service riêng) ⇒ không xung đột. Thứ tự: producer lanes (comment, project) độc lập file, chạy song song được; test lane viết RED trước → GREEN sau khi 2 producer xong (depends_on logic, không depends_on file).
</content>

---

## ADDENDUM — thực tế triển khai + FULL gate (2026-07-17, phiên finish trực tiếp)

**Approach thực tế ≠ primary của plan (có chủ đích, đúng nhánh ALTERNATIVE đã dự liệu):** builder phát hiện
`tasks.task_code` = NULL cho MỌI task tạo qua API (cột 0478 có nhưng code-gen chưa cut-over) ⇒ chỉ thêm key
payload là vô ích với task thật. Diff cuối gồm: (1) producer additive 4 key snake_case khớp 0481; (2) cut-over
code-gen `task_code` ở `TaskCoreService.createTask` qua `SequenceService.nextCode` (tx riêng trước insert,
mirror allocateEmployeeCode); (3) migration **0498** seed counter 'task' per-company + backfill task NULL
còn-sống + sync counter + verify fail-loud; (4) test honest-hoá (`mkTask` qua `POST /tasks` THẬT). Diff chạm
`migrations/**` ⇒ **gate FLIP LIGHT→FULL** đúng như plan §GATE đã khai.

**FULL gate đã chạy (3 reviewer độc lập):** security-reviewer **PASS** (bất biến #1/#2/#3 giữ) ·
database-reviewer **PASS-có-điều-kiện** · silent-failure-hunter **BLOCK→đã gỡ** bằng các fix bên dưới.

**Fix sau gate (đã vào diff, có test khoá):**
- **HIGH-1** (cả 3 reviewer): `hr-tasks.service.createApprovalTaskTx` (caller sống: leave.service:194,
  attendance-adjustment.service:126) KHÔNG ghi task_code ⇒ task HR sau 0498 vẫn NULL, comment/mention lên đó
  tái sinh `{task_code}` câm. Vá triệu chứng tại emit-site: `commentPayload` coalesce `task_code → title`
  (test 8c). Vá GỐC = follow-up **S5-TASK-HRCODE-1** (đã seed backlog — cần thread mã từ caller TRƯỚC tx đơn,
  không nextCode trong tx dài).
- **HIGH-2** (hunter): `actor_name` = `users.full_name` nullable (hr-write provision cho phép trống) ⇒ user
  không tên làm MỌI comment/mention câm `{actor_name}`. Vá: coalesce sang `users.email` (NOT NULL, không
  thuộc SENSITIVE_PAYLOAD_KEYS) — thêm cột `userEmail` additive vào COMMENT_SELECT (test 8d).
- **MEDIUM** (db-review): `lpad` Postgres CẮT chuỗi khi số vượt padding (khác `padStart` service) ⇒ backfill/
  sync 0498 đổi pad-width = `GREATEST(v_padding, length(n))` — hết nguy cơ mã trùng nổ unique khi n ≥ 10000.

**Residual chấp nhận (ghi nhận, không chặn):** `project_code ?? ""` ⇒ body "dự án X ()" khi project không mã
(khoá bằng test 8b, short_body luôn sạch) · SequenceInactiveError → 500 raw (fail-loud, map 4xx ở follow-up) ·
fullName chưa `.max()` trong contracts (follow-up) · cửa sổ deploy migrate→restart tạo task NULL-code one-time
(đã có fallback title che) · trùng mã với task soft-deleted là pattern active-unique nhất quán runtime.

**Verify:** lint ✅ · typecheck ✅ · int-spec chủ đích 29/29 trên lane DB TẠO MỚI (0498 bản sửa chạy thật) ·
full-suite: vitest worker crash `ERR_IPC_CHANNEL_CLOSED`/segfault NGẪU NHIÊN trên máy local (tiền sử BSOD —
memory machine-bsod-under-load-loop-resume), 0 test fail thật ở mọi lần chạy; bằng chứng cuối = suite tuần tự
`--no-file-parallelism` + CI. Bẫy môi trường: worktree mới thiếu `.secrets/local-kek.bin` ⇒ 2 test AUTH đỏ oan
(memory worktree-missing-kek-false-red).
