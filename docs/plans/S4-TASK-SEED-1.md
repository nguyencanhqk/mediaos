```yaml
wo: S4-TASK-SEED-1
zone: red
generated_by: auto-loop
reconciled_at: "5833cce"
lanes: [{"id":"migtaskperms","task":"[NỐI TIẾP — crown/red, chạy TRƯỚC] Tạo migration 0480_s4_taskseed1_task_perms.sql (idx 160, when 1717500795000 > 0479 idx 159) mirror mig 0454/0455. (a) INSERT catalog cặp (action,resource_type) TASK theo permission-matrix §6 + DB-06 §12.1, engine-pair chuẩn: read:project/read:task (KHỚP FE PERMISSION_CODE_TO_PAIR registry.ts:137-138 — non-sensitive để làm cổng route/sidebar), create/update:project · close/delete/archive/manage-member/view-report:project · create/update/delete:task · assign · update-status · update-priority · update-deadline · comment · watch · checklist · view-kanban · file-upload · file-delete · export:task · view:task-audit-log (resource_type distinct, KHÔNG generic audit-log — tránh over-grant). ON CONFLICT(action,resource_type) DO NOTHING. (b) DO-block per-(role,pair)=data_scope cho 4 role canonical employee/manager/hr/company-admin (mig 0444): DELETE đúng bộ (role_id,permission_id,'ALLOW') có data_scope<>target rồi INSERT ON CONFLICT(role_id,permission_id,effect) DO NOTHING; verify fail-LOUD RAISE EXCEPTION nếu role/permission thiếu (mirror 0454:216, mig 0466/0476). KHÔNG DDL/RLS/FORCE. KHÔNG seed super-admin (runtime bootstrap — bất biến #1). KHÔNG re-active module task (đã active S0-FND-SEED-1). Append meta/_journal.json.","builder":"db-migration","paths":["apps/api/migrations/**","docs/plans/S4-TASK-SEED-1.md"]},{"id":"seedintspec","task":"[depends_on migtaskperms] Int-spec kiểm chứng seed (gate hasDb && LANE_DB — bài học integration-test-LANE_DB) đặt tại apps/api/src/foundation/seed/task-permission-seed.int.spec.ts: (1) catalog TASK đủ cặp + is_sensitive đúng (read:project/read:task=false; delete/close/archive/manage-member/view-report:project + delete/export:task + view:task-audit-log=true); (2) role_permissions per-(role,pair) data_scope đúng ma trận: employee=Own, manager=Team, hr/company-admin=Company; company-admin ĐỦ BỘ; (3) deny/least-privilege: employee KHÔNG có cặp manager-scope (assign/delete:task/manage-member/view-report/view:task-audit-log; không Team/Company scope trên bất kỳ cặp nào), manager KHÔNG có view:task-audit-log & max Team, hr KHÔNG có close/delete:project/manage-member/delete:task; (4) idempotent: chạy DO-block/migration lần 2 = 0 insert mới, 0 duplicate (UNIQUE role_id,permission_id,effect giữ); (5) getCapabilities(admin) surface read:task/read:project (non-sensitive) — cặp sensitive KHÔNG surface (allowlist chưa nới — đúng thiết kế).","builder":"backend-builder","paths":["apps/api/src/foundation/seed/**"]}]
acceptanceChecks: ["Migration 0480_s4_taskseed1_task_perms.sql đánh số NỐI TIẾP sau 0479 (journal idx 160, when 1717500795000 > 0479/159); `pnpm db:migrate` áp từ DB trống 0000→0480 KHÔNG lỗi.","Catalog chứa ĐỦ cặp TASK của permission-matrix §6 / DB-06 §12.1 dưới dạng engine-pair; cặp đọc = read:project & read:task (KHỚP PERMISSION_CODE_TO_PAIR registry.ts:137-138) và is_sensitive=false; INSERT ON CONFLICT(action,resource_type) DO NOTHING (append-only hot-file, không nhân đôi cặp legacy).","role_permissions grant PER-(permission,role) đúng data_scope: employee=Own · manager=Team · hr/company-admin=Company (DB-06 §12.2 + matrix §6); company-admin có ĐỦ BỘ cặp TASK ở Company.","Đổi data_scope trên grant đã tồn tại = DELETE đúng bộ (role_id,permission_id,'ALLOW') scope-sai + INSERT (UNIQUE role_id,permission_id,effect KHÔNG gồm data_scope); KHÔNG blanket DELETE theo role_id; app role KHÔNG UPDATE role_permissions (bất biến #2).","Verify fail-LOUD: RAISE EXCEPTION nếu role canonical (mig 0444) hoặc cặp catalog thiếu — mirror mig 0454:216 / 0466 / 0476.","Super-admin KHÔNG seed trong migration (runtime SuperAdminBootstrap) — bất biến #1; KHÔNG DDL/RLS/FORCE/policy — thuần INSERT additive.","Idempotent: chạy lại migration/DO-block = 0 INSERT mới + 0 duplicate; int-spec re-run xanh dưới LANE_DB.","Least-privilege đo được: employee 0 cặp manager-scope (assign/delete:task/manage-member/view-report/view:task-audit-log; toàn Own); manager KHÔNG có view:task-audit-log & max Team; hr KHÔNG có close/delete:project/manage-member/delete:task.","getCapabilities() của company-admin surface read:task/read:project; cặp sensitive KHÔNG surface (allowlist chưa nới — đúng CAP-2, xem out-of-scope).","FULL gate security-reviewer + database-reviewer PASS; int-spec gate hasDb && LANE_DB xanh; DoD §8: audit không cần (seed), backlog done_when tick, plan doc persist."]
testTasks: ["RED deny-path (permission) — employee token: can('assign','task')=DENY · can('delete','task')=DENY · can('manage-member','project')=DENY · can('view','task-audit-log')=DENY · can('view-report','project')=DENY (viết TRƯỚC seed → đỏ, sau seed vẫn đỏ vì employee không được grant).","RED scope — manager: resolveStrongestScope('read','task') = Team (KHÔNG Company); manager KHÔNG có view:task-audit-log; close/delete/manage-member:project chỉ tới Team (owner-check ở BE, không phải seed).","RED — hr: can Company trên read/create/update/assign/update-status/view-report/export:task nhưng DENY close:project/delete:project/manage-member:project/delete:task (matrix §6 '—').","GREEN — company-admin: mọi cặp TASK = ALLOW @Company; getCapabilities surface read:task/read:project.","Idempotency int-spec: đếm role_permissions TASK trước/sau lần chạy thứ 2 = bằng nhau; không có (role_id,permission_id,effect) trùng.","Integration DB-cô-lập: `bash scripts/lane-db-setup.sh taskseed` → export LANE_DB=mediaos_taskseed → chạy int-spec; gate skipIf(!(hasDb && LANE_DB)) — tránh false-red do .env (bài học integration-test-LANE_DB).","QA (S4-QA-1 downstream, ghi nhận): deny-path TASK CRUD/assign/status theo role × pair từ seed THẬT — backend gate theo (action,resourceType) engine, KHÔNG theo mã FE."]
steps: ["migtaskperms: đọc mig 0454 (ATT) + 0455 (LEAVE) làm khuôn; xác nhận head=0479 (idx 159) → migration mới = 0480_s4_taskseed1_task_perms.sql, idx 160, when 1717500795000.","migtaskperms (a): viết INSERT catalog TASK — chốt DANH SÁCH CẶP + is_sensitive theo permission-matrix §6 / DB-06 §12.1; read:project & read:task PHẢI non-sensitive (khớp FE registry.ts:137-138, dùng làm cổng getCapabilities); ON CONFLICT(action,resource_type) DO NOTHING.","migtaskperms (b): viết DO-block grants ARRAY[role,action,resource,data_scope] cho employee/manager/hr/company-admin theo ma trận trong reconcileNotes; per-pair DELETE-wrong-scope + INSERT ON CONFLICT(role_id,permission_id,effect) DO NOTHING; RAISE EXCEPTION nếu role/permission NULL (fail-LOUD).","migtaskperms: KHÔNG seed super-admin (runtime), KHÔNG DDL/RLS/FORCE, KHÔNG re-active module task; append meta/_journal.json; verify `pnpm db:migrate` từ DB trống (0000→0480) sạch.","seedintspec: viết int-spec DB-cô-lập (gate hasDb && LANE_DB) verify catalog + grants + least-privilege + idempotency (chạy DO-block 2 lần) theo acceptanceChecks.","FULL gate: security-reviewer (least-privilege/deny) + database-reviewer (migration numbering, append-only, DELETE+INSERT re-scope) PASS trước merge; người duyệt red-zone chốt ma trận scope.","Cập nhật docs/plans/S4-TASK-SEED-1.md (persist reconcileNotes) + tick harness/backlog.mjs done_when."]
```

GAP-ANALYSIS (2026-07-09, feat/ready-wave3): Plan doc chưa tồn tại → phân rã MỚI. Head migration = 0479_s4_notidb1_notification_core (idx 159, committed WIP 639fa62 trên branch) → TASK-SEED dùng 0480 (idx 160, when 1717500795000) — KHỚP done_when. LƯU Ý điều phối: 0479 (NOTI core) đã có file vật lý dù S4-NOTI-DB-1 còn READY; nếu merge-order đảo, GIỮ 0480 (file 0479 đã chiếm chỗ). Roles canonical employee/manager/hr/company-admin đã có (mig 0444); super-admin = runtime bootstrap KHÔNG seed (bất biến #1). Module 'task' đã active (S0-FND-SEED-1) → KHÔNG re-activate.

NGUỒN SỰ THẬT: permission-matrix-spec.md §6 (matrix role×chức năng) + DB-06 §12.1 (catalog 23 mã) + §12.2 (scope theo role) + §12.3 (điều kiện xem task). FE canonical: packages/web-core/src/lib/registry.ts:137-138 PERMISSION_CODE_TO_PAIR = TASK.TASK.VIEW→read:task, TASK.PROJECT.VIEW→read:project (chống pair-drift — bài học s3-fe-wave-pair-drift). Khuôn migration: mig 0454 (ATT) + 0455 (LEAVE) — DO-block ARRAY grants, per-pair DELETE-wrong-scope+INSERT, verify fail-LOUD.

CẶP CANONICAL ĐỀ NGHỊ (builder + security-reviewer chốt): resource 'project' = read(F)/create(F)/update(F)/close(S)/delete(S)/archive(S)/manage-member(S)/view-report(S). resource 'task' = read(F)/create(F)/update(F)/update-status(F)/update-priority(F)/update-deadline(F)/comment(F)/watch(F)/checklist(F)/view-kanban(F)/file-upload(F)/file-delete(F)/assign(S?)/delete(S)/export(S). resource 'task-audit-log' = view(S). [F]=is_sensitive false, [S]=true. RÀNG BUỘC CỨNG: read:project & read:task PHẢI is_sensitive=false (getCapabilities permission.service.ts:355 lọc bỏ MỌI sensitive → nếu để true thì cổng route/sidebar FE registry.ts:569/1000 vỡ).

QUYẾT ĐỊNH MEMBER (done_when ghi 'member.add/update/remove'): matrix §6 + DB-06 §12.1 + FE code đều gộp 1 capability TASK.PROJECT.MANAGE_MEMBER. KHUYẾN NGHỊ: seed 1 cặp manage-member:project (S) gác cả 3 endpoint add/update-role/remove của S4-TASK-BE-1 → tránh FE-BE pair-drift, trung thành matrix. Builder+security-reviewer xác nhận; nếu owner muốn granular thì thêm add/update/remove:project-member (append, cần đồng bộ FE map sau).

MA TRẬN SCOPE ĐỀ NGHỊ (role→data_scope, KHUYẾT '—'=không grant): read/create/update:project — employee Own(member)/manager Team/hr Company/admin Company. close·delete·archive·manage-member:project — admin Company, manager Team(owner-check ở BE qua project_members, seed chỉ cấp capability), hr —, employee —. view-report:project — admin Company, hr Company, manager Team, employee —. read·create·update·update-status·comment·watch·checklist·view-kanban·file-upload·file-delete:task — employee Own, manager Team, hr Company, admin Company. assign·update-priority·update-deadline·export:task — admin Company, hr Company, manager Team, employee —. delete:task — admin Company, manager Team(owner), hr —, employee —. view:task-audit-log — admin Company, hr Company, manager —, employee —. company-admin = ĐỦ BỘ @Company.

BẤT BIẾN/VERIFY: thuần INSERT (permissions/role_permissions), KHÔNG DDL/RLS/FORCE (bất biến #1 giữ nguyên); ON CONFLICT idempotent bộ-ba (role,perm,scope); đổi scope = DELETE+INSERT (UNIQUE loại data_scope, app role không UPDATE — bất biến #2); RAISE EXCEPTION khi role/perm thiếu (fail-LOUD). resource_type audit = 'task-audit-log' DISTINCT (KHÔNG tái dùng generic 'audit-log' của mig 0005 → tránh over-grant audit toàn công ty; mirror attendance-audit-log/leave-audit-log). task_activity_logs (mig 0478) là append-only — permission chỉ 'view'.

GATE: FULL (security-reviewer least-privilege/deny + database-reviewer numbering/append-only/re-scope). Int-spec integration gate hasDb && LANE_DB (skipIf(!hasDb) KHÔNG đủ vì .env làm hasDb=true → false-red).

OUT-OF-SCOPE / DECISION cho owner: (1) SENSITIVE_CAPABILITY_ALLOWLIST (apps/api/src/permission/permission.service.ts:43) NẰM NGOÀI paths WO (chỉ foundation/seed + migrations + docs/plans). Cặp sensitive TASK (export/delete/view-report/audit) HIỆN CHƯA có nút FE nào tiêu thụ (S4-FE-TASK-* chưa build; registry chỉ read:task/read:project). Theo precedent S3-ATT-EXPORT-1 (allowlist thêm KHI nút FE ra đời, KHÔNG ở seed-time) → HOÃN cập nhật allowlist sang các lane S4-FE-TASK-1/2/3; int-spec 'admin thấy đủ cặp' giới hạn ở cặp NON-sensitive qua getCapabilities + kiểm enforcement cặp sensitive qua can(). Nếu owner muốn allowlist ngay → phải MỞ RỘNG paths WO gồm apps/api/src/permission/**. (2) 'assign:task' is_sensitive: đề xuất false (để nút giao-việc manager surface không cần allowlist về sau) NHƯNG security-reviewer chốt — nếu true phải bổ allowlist khi FE assign-control ra. (3) update-priority/update-deadline scope cho assignee (Own) vs chỉ manager (Team) — matrix gộp vào UPDATE; đề xuất manager Team/hr+admin Company, employee — (flag). (4) Enforcement 'owner' của close/delete/manage-member ở manager = project role (project_members) tại BE S4-TASK-BE-1, KHÔNG phải seed — seed chỉ cấp capability @Team.
</content>
