```yaml
wo: S2-AUTH-DOC-1
zone: green
generated_by: auto-loop
reconciled_at: "48382cd"
lanes: [{"id":"docpin-api-be-auth","task":"PIN 'code thắng' vào API-02 + BACKEND-03 (cùng bộ quyết định auth để wording khớp nhau): (a) API-02 §7 AUTH-API-001 login = uniform 401, gạch/đánh dấu nhánh 403 USER-LOCKED/INACTIVE (anti-enumeration UNIFORM_LOGIN_ERROR); (b) §3.4 permission dùng CẶP ENGINE (action, resource_type) thay MODULE.RESOURCE.ACTION; (c) §6 Login DTO companySlug BẮT BUỘC (min1) + login response UNION tokens | {twoFactorRequired, challengeToken}; (d) §7 AUTH-API-007 change-password thu hồi MỌI phiên (SPEC-02 §14.5); (e) §5.1 endpoint list bổ sung path THẬT /auth/refresh · /auth/sessions(+/:id/revoke,+/revoke-others) · /auth/2fa/{enroll,enable,disable,status,verify} · /auth/redirect-allowed; (f) BACKEND-03 §10.4 refresh_tokens family model SONG SONG user_sessions (cả hai live) · §11.3/§13.4 uniform 401 · §13.3 login union · §6.2 cặp engine. Mỗi điểm 1 dòng 'CHỐT 2026-07-02: code thắng, lý do', giữ nguyên cấu trúc doc.","builder":"backend-builder","paths":["docs/API Design/API-02 AUTH API Design.md","docs/BACKEND/BACKEND-03_Auth_Session_RBAC_Permission_Guard.md"]},{"id":"docpin-fe-auth","task":"PIN 'code thắng' vào FRONTEND-06: (a) §7.1/§16.1 SSO 3-app redirect qua ?redirect + server allowlist /auth/redirect-allowed (THAY returnUrl client-side); (b) §16.1 nhánh 403 'tài khoản khóa' ở login = DEAD CODE với uniform 401 → bỏ HOẶC ghi rõ chỉ dành cho 403 ACCESS_RESTRICTED (security policy), KHÔNG cho USER-LOCKED/INACTIVE; (c) §16.5 change-password → logout CƯỠNG BỨC; (d) §16.4/§5.1 self-profile-edit = change-request workflow (apps/app/routes/hr/profile-change-requests) KHÔNG direct PATCH; (e) §15.1 login schema companySlug bắt buộc; (f) §10.1 auth session model xử lý twoFactorChallenge union. Mỗi điểm 1 dòng 'CHỐT 2026-07-02: code thắng, lý do'.","builder":"frontend-builder","paths":["docs/FRONTEND/FRONTEND-06_AUTH_Account_Frontend.md"]},{"id":"docpin-db-auth","task":"PIN hiện trạng DB đã chốt vào DB-02: (a) §7.3 permissions = cặp (action, resource_type) + uniqueIndex(action,resource_type); (b) §7.2 roles name = code (KHÔNG có cột role_code); (c) §7.5 role_permissions effect ALLOW/DENY + data_scope PER-grant; (d) §7.7 password_reset_tokens KHÔNG có purpose → user_invites là bảng RIÊNG (per-tenant FORCE-RLS); (e) §7.11 user_mfa_methods → THAY bằng user_totp + user_recovery_codes (ĐÃ live, không phải phase sau); (f) §4.7/§4.6 data_scope = Own/Team/Department/Company/System (contracts DATA_SCOPES bỏ Project CÓ CHỦ Ý). §4.7 GHI QUYẾT ĐỊNH OWNER về 'Project' (xem risk — cần người chốt). Mỗi điểm 1 dòng 'CHỐT 2026-07-02: code thắng, lý do', sửa đúng chỗ, KHÔNG viết lại doc.","builder":"db-migration","paths":["docs/DB/DB-02 AUTH RBAC Database Design.md"]}]
acceptanceChecks: ["API-02 §7 AUTH-API-001: mục lỗi login chỉ còn uniform 401; nhánh 403 USER-LOCKED/INACTIVE bị gạch/đánh dấu kèm dòng 'CHỐT 2026-07-02: code thắng — uniform 401 anti-enumeration (auth.service UNIFORM_LOGIN_ERROR)'.","API-02 §3.4 + BACKEND-03 §6.2: permission mô tả bằng CẶP ENGINE (action, resource_type); ghi rõ thay MODULE.RESOURCE.ACTION + dòng CHỐT (schema permissions uniqueIndex(action,resource_type)).","API-02 §6/§7: Login DTO ghi companySlug BẮT BUỘC (min 1, contracts/auth.ts:10); login success = UNION tokens | {twoFactorRequired, challengeToken}.","API-02 §7 AUTH-API-007: change-password ghi rõ thu hồi MỌI phiên (khớp SPEC-02 §14.5 + revokeAllSessionsForUserTx 'password_changed') kèm dòng CHỐT.","API-02 §5.1: danh sách endpoint gồm /auth/refresh · /auth/sessions · /auth/sessions/:id/revoke · /auth/sessions/revoke-others · /auth/2fa/{enroll,enable,disable,status,verify} · /auth/redirect-allowed — khớp auth.controller.ts.","BACKEND-03 §10.4: refresh_tokens family model ghi rõ SONG SONG/coexist với user_sessions (cả hai live); §13.4 uniform 401; §13.3 login union — mỗi mục có dòng CHỐT.","FRONTEND-06 §7.1/§16.1: SSO redirect qua ?redirect + server allowlist /auth/redirect-allowed (thay returnUrl client-side); nhánh 403 'tài khoản khóa' được bỏ HOẶC narrow rõ về 403 ACCESS_RESTRICTED kèm dòng CHỐT.","FRONTEND-06 §16.5: change-password → logout cưỡng bức; §16.4/§5.1: self-profile-edit = change-request workflow (không direct PATCH); §15.1 login schema companySlug bắt buộc.","DB-02: §7.3 permissions cặp (action,resource_type); §7.2 roles name=code (không role_code); §7.5 role_permissions effect ALLOW/DENY + data_scope per-grant; §7.7 password_reset_tokens không purpose + user_invites bảng riêng; §7.11 user_mfa_methods → user_totp+user_recovery_codes (live) — mỗi mục 1 dòng CHỐT.","DB-02 §4.7: data_scope = Own/Team/Department/Company/System; khối OWNER-DECISION 'Project' được ghi kết luận (1 trong 2 phương án) kèm dòng CHỐT + lý do; nếu owner CHƯA chốt → mục này đánh dấu BLOCKED-on-owner, phần còn lại vẫn ship.","Mỗi điểm pin có ĐÚNG 1 dòng 'CHỐT 2026-07-02: code thắng, <lý do>' ngay tại section liên quan; KHÔNG viết lại cả doc (giữ heading/breadcrumb/cross-ref).","DoD §8 doc-only: git diff CHỈ chạm 4 file trong paths, KHÔNG đụng apps/** (không đổi hành vi code); không có anchor/link gãy; harness/backlog.mjs cập nhật."]
testTasks: ["Doc-consistency verify (grep-assert từng pin khớp symbol code THẬT, chống pin-giả): auth.service.ts::UNIFORM_LOGIN_ERROR · contracts/auth.ts companySlug z.string().min(1) · auth.service.ts revokeAllSessionsForUserTx('password_changed') · auth.controller.ts @Post('refresh')/@Get('sessions')/@Post('sessions/:id/revoke')/@Post('sessions/revoke-others')/@Post('2fa/enroll')/@Get('redirect-allowed') · auth.service.ts login return {twoFactorRequired,challengeToken} · schema/permissions.ts uniqueIndex('permissions_action_resource_uq',[action,resourceType]) + effect ALLOW/DENY · contracts/auth.ts DATA_SCOPES KHÔNG chứa 'Project' · schema/index.ts user_totp+user_recovery_codes+user_invites.","KHÔNG cần deny-path RED test: WO doc-only, KHÔNG đổi permission/workflow behavior — ghi rõ trong PR để reviewer không đòi test hành vi.","Link/anchor integrity: kiểm mọi cross-ref (README §8 map, breadcrumb nội bộ, liên kết SPEC-02/§14.5, BACKEND-03↔API-02↔DB-02) còn hợp lệ sau sửa; markdown không hỏng bảng.","QA regression guard (doc-only): xác nhận không có QA-02/QA-05 test case nào phụ thuộc doc bị đổi ngữ nghĩa (chỉ pin hiện trạng, không đổi contract) → 0 test đỏ mới.","Scope check: git diff --name-only ⊆ paths (4 file docs/**), 0 file apps/** — chứng minh không đổi code."]
steps: ["Xác nhận từng 'code thắng' vs code THẬT trước khi pin (đã verify Đội 1): UNIFORM_LOGIN_ERROR (auth.service.ts) · companySlug min(1) (contracts/auth.ts:10) · revokeAllSessionsForUserTx 'password_changed' (auth.service.ts:538) · @Post refresh/sessions/2fa + @Get redirect-allowed (auth.controller.ts) · login {twoFactorRequired,challengeToken} (auth.service.ts:344) · refresh_tokens family + user_sessions (schema/auth.ts) · permissions uniqueIndex(action,resourceType) + effect ALLOW/DENY + DATA_SCOPES no Project (schema/permissions.ts, contracts/auth.ts:95) · user_totp/user_recovery_codes/user_invites (schema/index.ts:46,62).","Lane docpin-api-be-auth: sửa đúng section API-02 (§3.4 · §3.5 · §5.1 · §6 · §7 AUTH-API-001/002/003/004/007) + BACKEND-03 (§10.4 · §11.3 · §12.1 · §13.3/§13.4 · §6.2), mỗi điểm chèn 1 dòng 'CHỐT 2026-07-02: code thắng, <lý do>'. Bổ sung mục 2FA endpoints (enroll/enable/disable/status/verify) nếu API-02 chưa có — additive, không viết lại doc.","Lane docpin-fe-auth: sửa FRONTEND-06 §7.1 · §16.1 · §16.5 · §16.4 · §15.1 · §10.1 theo done_when FE; quyết định nhánh 403 login: ưu tiên GIỮ nhưng narrow về 403 ACCESS_RESTRICTED (không dead-drop) — ghi rõ.","Lane docpin-db-auth: sửa DB-02 §7.2 · §7.3 · §7.5 · §7.7 · §7.11 · §4.6 · §4.7; tại §4.7 chèn khối OWNER-DECISION data_scope 'Project' — ghi kết luận CHỐT (mặc định đề xuất: TASK dùng project-membership, KHÔNG dùng data_scope Project) SAU khi owner xác nhận.","Verify: git diff CHỈ chạm 4 file trong paths (không đụng apps/**), doc structure/breadcrumb/cross-ref còn nguyên, không có link gãy. Cập nhật harness/backlog.mjs done_when nếu cần.","Cập nhật/ghi micro-plan vào docs/plans/S2-AUTH-DOC-1.md (khối yaml máy-đọc) để tái dùng."]
```

GAP-ANALYSIS (nguồn: audit 2026-07-02 [[auth-account-audit-2026-07]] + verify code trực tiếp Đội 1): WO này KHÔNG sửa gap thật, chỉ PIN 8 'lệch-có-chủ-đích' vào doc để audit sau không báo 'lệch' giả. 8 điểm code-thắng đã verify: (1) uniform 401 login — auth.service.ts UNIFORM_LOGIN_ERROR (mọi nhánh sai user/pass/lock/inactive/slug → 401 đồng nhất, burn-time chống timing); FE branch 403 'tài khoản khóa' ≈ dead code. (2) cặp engine (action,resource_type) thay MODULE.RESOURCE.ACTION — schema/permissions.ts:44-47 uniqueIndex(action,resourceType). (3) companySlug BẮT BUỘC — contracts/auth.ts:10 z.string().min(1); email chỉ unique theo tenant → resolve slug→companyId qua SECURITY DEFINER. (4) change-password thu hồi MỌI phiên — auth.service.ts:538 revokeAllSessionsForUserTx('password_changed') khớp SPEC-02 §14.5. (5) path THẬT /auth/refresh + /auth/sessions(+/:id/revoke,+/revoke-others) + bộ /auth/2fa/* + /auth/redirect-allowed — auth.controller.ts đã ship đủ. (6) login response UNION: nhánh tokens HOẶC {twoFactorRequired:true, challengeToken} (auth.service.ts:344) — 2FA TOTP ĐÃ ship (doc cũ ghi 'phase sau' §7.11 → sai). (7) refresh_tokens family model (family_active_idx, rotation+reuse-detection) SONG SONG user_sessions — cả hai live (không còn deferred như [[s2-auth-be1-shipped]]). (8) DB đã chốt: user_totp+user_recovery_codes thay user_mfa_methods (index.ts:46), user_invites bảng riêng thay 'purpose' ở password_reset_tokens (index.ts:62), roles name=code (không role_code), role_permissions effect ALLOW/DENY + data_scope per-grant (0005/0441). SSO: /auth/redirect-allowed = server allowlist thay returnUrl client-side.

INVARIANTS: doc-only nên KHÔNG chạm 3 bất biến CLAUDE.md §2 (không sửa code/schema/migration). Chỉ MÔ TẢ lại hiện trạng đã ép (RLS+FORCE, audit append-only, hash password) — không nới lỏng. Guard: git diff ⊆ 4 file paths; 0 file apps/**.

DECOMPOSITION: 3 lane doc độc lập, paths KHÔNG chồng, chạy SONG SONG (không phụ thuộc), zone green → LIGHT gate. Không có lane migration (không DDL). Lane docpin-db-auth builder=db-migration theo domain DB (chỉ sửa markdown, không SQL); docpin-api-be-auth gộp API-02+BACKEND-03 (backend-builder) để wording quyết định auth KHỚP giữa 2 doc; docpin-fe-auth (frontend-builder).

ĐIỂM CẦN NGƯỜI CHỐT (crown-adjacent, blocker cục bộ): OWNER phải chốt data_scope 'Project' — (A) widen CHECK 0441 + DATA_SCOPES trước Sprint TASK, HAY (B) pin 'TASK dùng project-membership, KHÔNG dùng data_scope Project'. Đội 1 ĐỀ XUẤT MẶC ĐỊNH (B): engine + contracts (auth.ts:95 DATA_SCOPES = Own/Team/Department/Company/System) hiện BỎ Project CÓ CHỦ Ý; chọn (A) là code/migration FUTURE, NGOÀI WO doc-only này. Trước khi owner xác nhận, mục §4.7 DB-02 đánh dấu BLOCKED-on-owner, phần còn lại vẫn ship. LƯU Ý PATH: done_when nhắc 'ghi kết luận vào DB-02 + DECISIONS nếu cần' NHƯNG paths KHÔNG gồm docs/DECISIONS/** → nếu owner muốn 1 ADR trong docs/DECISIONS, PHẢI mở rộng paths hoặc tách follow-up WO; hiện chỉ ghi được vào DB-02 (trong paths).

VERIFY/GATE: LIGHT gate (typescript-reviewer không áp doc → dùng quality-gate + doc-consistency). RED deny-path KHÔNG cần (không đổi hành vi permission/workflow). Kiểm: (i) grep-assert mỗi pin khớp symbol code; (ii) link/anchor integrity; (iii) scope diff ⊆ paths.

OUT-OF-SCOPE (chống scope-creep): KHÔNG sửa gap THẬT trong cùng WO (user_security_events writer, lock-revoke-session ngay, refresh check company active, user_roles hard-delete, mã lỗi granular §24, cache invalidation role_permissions, enroll 2FA ở apps/app) — các gap này là WO reconcile riêng. KHÔNG viết lại doc (chỉ sửa đúng section, chèn dòng CHỐT). KHÔNG chạm docs/DECISIONS (ngoài paths). KHÔNG đổi hành vi code.
</content>
