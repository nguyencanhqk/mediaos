```yaml
wo: S2-AUTH-CAP-1
zone: red
generated_by: auto-loop
reconciled_at: "48382cd"
lanes: [{"id":"capAllowlist","task":"Permission crown — RED-first rồi GREEN: APPEND đúng 3 cặp LITERAL sensitive (export:leave · view:leave-audit-log · view:attendance-audit-log) vào SENSITIVE_CAPABILITY_ALLOWLIST (Set trong permission.service.ts), append-only sau 'view:leave', gắn comment tag S2-AUTH-CAP-1. Cập nhật int-spec colocated auth-me-capabilities.int.spec.ts thêm case expose/deny cho 3 cặp. KHÔNG đổi can()/PermissionGuard/getCapabilities()/data-scope/shape /auth/me; KHÔNG migration/seed/catalog.","builder":"backend-builder","paths":["apps/api/src/permission/permission.service.ts","apps/api/src/auth/auth-me-capabilities.int.spec.ts"]}]
acceptanceChecks: ["SENSITIVE_CAPABILITY_ALLOWLIST = 5 key hiện có (view:audit-log · view-own:attendance · view-team:attendance · view-company:attendance · view:leave) + đúng 3 key APPEND (export:leave · view:leave-audit-log · view:attendance-audit-log); KHÔNG bỏ/đổi tên/đổi thứ tự key cũ","3 key mới khớp CẶP SEED THẬT is_sensitive=true trong catalog: export:leave (leave-permissions.const dòng 60 / mig 0455) · view:leave-audit-log (dòng 85 / mig 0455) · view:attendance-audit-log (attendance-permissions.const dòng 84 / mig 0454)","/auth/me của company-admin (role 0001) VÀ hr (role 0011) — cả hai giữ grant Company theo mig 0454/0455 — trả capabilities['export:leave']===true, ['view:leave-audit-log']===true, ['view:attendance-audit-log']===true","/auth/me của employee (role 0008) và manager (role 0010) → KHÔNG chứa bất kỳ key nào trong 3 (least-privilege đúng seed: 3 cặp chỉ hr/company-admin)","user chỉ có wildcard *:* → KHÔNG kế thừa 3 cặp nhạy cảm (sensitive gate); key view:audit-log vẫn vắng (no-regress S2-AUTH-BE-5); cross-tenant: user tenant B không thấy grant tenant A","KHÔNG có file migration mới; KHÔNG sửa *-permissions.const / seed / permission catalog; shape /auth/me (tập key của data object) KHÔNG đổi — chỉ thêm entry boolean vào map capabilities","can() / PermissionGuard / data-scope KHÔNG đổi (git diff chỉ chạm literal Set trong permission.service.ts + auth-me-capabilities.int.spec.ts) — enforcement vẫn per-resource ở mọi API","int-spec colocated src/auth/auth-me-capabilities.int.spec.ts, gate cứng hasDb && LANE_DB, XUẤT HIỆN trong vitest run summary; regression permission + auth suite XANH trên LANE_DB cô lập","FULL gate (security-reviewer) PASS; PR mở KHÔNG auto-merge; owner chốt trước merge (DoD §8: audit không cần vì đây chỉ đọc-hiển-thị cờ, không hành động quan trọng)"]
testTasks: ["RED expose-path (permission crown): company-admin (0001) + hr (0011) qua /auth/me nhận ĐỦ 3 cặp export:leave / view:leave-audit-log / view:attendance-audit-log — chỉ GREEN sau khi APPEND allowlist (chứng minh gate allowlist là điểm mở khóa duy nhất)","Deny-path (bắt buộc RED cho permission): employee (0008) + manager (0010) KHÔNG thấy bất kỳ cặp nào trong 3 — least-privilege per-seed, không over-expose","Wildcard non-inheritance: user chỉ *:* (non-sensitive ALLOW) → KHÔNG kế thừa 3 cặp nhạy cảm; view:audit-log vẫn vắng (no-regress)","Cross-tenant / 2-tenant deny giữ nguyên: seed tenant B, user B trơn → capabilities KHÔNG chứa 3 cặp của tenant A (company isolation, BẤT BIẾN #1)","(nice-to-have) DENY-override: role có DENY trên export:leave hoặc *:* → key bị suppress (mirror deny-override wildcard-aware của getAllowlistedSensitiveCapabilities)","Chạy toàn bộ dưới LANE_DB cô lập (hasDb && LANE_DB), spec colocated src/**/*.spec.ts, xác nhận có trong run summary — chống xanh-giả (memory vitest-unit-specs-must-be-colocated + integration-test-lane-db-gate)"]
steps: ["RED — mở rộng auth-me-capabilities.int.spec.ts (append section S2-AUTH-CAP-1, KHÔNG rewrite case cũ): (a) company-admin role 0001 + hr role 00000000-0000-0000-0000-000000000011 → /auth/me.capabilities có đủ 3 key export:leave / view:leave-audit-log / view:attendance-audit-log ===true; (b) employee 0008 + manager 0010 → KHÔNG có bất kỳ key nào trong 3; (c) user chỉ có wildcard *:* → KHÔNG kế thừa 3 cặp; (d) cross-tenant: user tenant B trơn → KHÔNG thấy 3 key. Chạy trên LANE_DB cô lập → RED (3 key vắng vì chưa allowlist).","GREEN — trong apps/api/src/permission/permission.service.ts APPEND đúng 3 dòng vào SENSITIVE_CAPABILITY_ALLOWLIST sau 'view:leave': 'export:leave', 'view:leave-audit-log', 'view:attendance-audit-log' + comment tag S2-AUTH-CAP-1 (append-only, giữ nguyên 5 key hiện có). Không sửa getAllowlistedSensitiveCapabilities() (thuật toán đã dùng Set này).","Verify — DB cô lập: bash scripts/lane-db-setup.sh capAllowlist → export LANE_DB=mediaos_capAllowlist → pnpm --filter @mediaos/api test (chạy permission + auth int-spec). Xác nhận spec mới XUẤT HIỆN trong run summary (colocated src/**/*.spec.ts) và toàn bộ RED chuyển GREEN.","Đối chiếu không-đổi: git diff chỉ chạm literal Set + spec; xác nhận /auth/me trả capabilities vẫn Record<string,boolean> (auth.service.ts merge {...allowlistedSensitiveCaps, ...nonSensitiveCaps} không đổi); KHÔNG file migration mới; KHÔNG đổi *-permissions.const/seed/catalog.","FULL gate (security-reviewer — permission crown) + typescript-reviewer + quality-gate; mở PR KHÔNG auto-merge → owner chốt red-zone trước merge."]
```

GAP-ANALYSIS: getAllowlistedSensitiveCapabilities() (permission.service.ts:293) đã tồn tại và gate MỌI cặp qua SENSITIVE_CAPABILITY_ALLOWLIST.has(key); auth.service.ts (~982-999) merge allowlistedSensitiveCaps + nonSensitiveCaps vào /auth/me.capabilities ADDITIVE. getCapabilities() CỐ Ý lọc bỏ mọi grant sensitive ⇒ export:leave / view:leave-audit-log / view:attendance-audit-log (đều sensitive=true trong catalog) hiện KHÔNG BAO GIỜ vào capabilities ⇒ FE useCan() luôn false ⇒ (a) FE LEAVE-6 bị khóa, (b) trang ATT audit-logs render forbidden ngầm cho cả hr/company-admin đã grant. FIX = APPEND đúng 3 key literal vào Set — KHÔNG cần code khác. Đã xác minh 3 cặp là CẶP SEED THẬT sensitive=true (leave-permissions.const:60,85 · attendance-permissions.const:84) và grant Company cho hr(0011)+company-admin(0001), KHÔNG manager(0010)/employee(0008) (mig 0455 dòng 132-134/200-201; mig 0454 dòng 124-125/211-212). PIN theo CẶP ENGINE, KHÔNG mã FE (chống drift S1-FND-MODULE). INVARIANTS: BẤT BIẾN #1 company_id/RLS — capabilities đọc getCompanyRoleGrants(userId,companyId) đã scoped, cross-tenant không rò; đây chỉ CỜ hiển thị UI, KHÔNG phải cổng enforcement — can()/PermissionGuard per-resource vẫn là cổng THẬT ở mọi API (owner duyệt 2026-07-03, KHÔNG xin lại). Wildcard *:* KHÔNG có trong allowlist ⇒ sensitive KHÔNG kế thừa (mirror sensitive gate của can()). Fail-safe: lỗi hạ tầng → {} (UI-hint, KHÔNG fail-closed). VERIFY: DB cô lập LANE_DB (drizzle migrator đơn điệu theo when — DB chung skip band thấp ⇒ xanh/đỏ-giả); spec PHẢI colocated src/**/*.spec.ts (apps/api vitest KHÔNG chạy test/). GATE: FULL (security-reviewer — permission crown) + typescript-reviewer + quality-gate; PR KHÔNG auto-merge → owner chốt. OUT-OF-SCOPE (KHÔNG làm ở WO này): (1) mọi thay đổi FE — LEAVE-6 và trang ATT audit-logs là WO FE riêng, path apps/app + packages/web-core, KHÔNG thuộc paths WO này; downstream FE PHẢI map mã FE→cặp engine ĐÚNG (PERMISSION_CODE_TO_PAIR: export→export:leave, audit-log ATT→view:attendance-audit-log, audit-log LEAVE→view:leave-audit-log) — nếu map sai app vẫn ẩn (bài học s3-fe-wave-pair-drift-blocker); (2) KHÔNG migration/seed/catalog; (3) KHÔNG đổi shape /auth/me hay semantics getCapabilities(). MODEL: crown-jewel permission ⇒ planner Sonnet5+xhigh lập micro-plan → plan-reviewer → IMPLEMENT/REVIEW Opus.
