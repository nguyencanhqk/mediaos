```yaml
wo: S5-TASK-HRCODE-1
zone: red
generated_by: auto-loop
reconciled_at: "d5fc9e14"
lanes: [{"id":"hrcode-tasks","task":"GỐC crown — cấp + ghi task_code cho task HR. (1) Tạo tiện ích DÙNG CHUNG tasks/task-code.util.ts: allocateTaskCode(db, sequence, companyId) — mirror task-core.service.allocateTaskCode (nextCode ở tx RIÊNG do SequenceService.nextCode tự mở withTenant; ensure-on-miss counter 'task' canonical rồi retry ĐÚNG 1 lần, KHÔNG loop); export TASK_CODE_SEQUENCE_KEY + TASK_CODE_COUNTER_DEFAULTS. (2) BẮT SequenceInactiveError trong util → ném HttpException 4xx (khuyến nghị ConflictException) mã TASK-ERR-CODE-COUNTER-INACTIVE (fail-loud, KHÔNG 500 raw) — 1 điểm map duy nhất cho CẢ 3 caller. (3) task-core.service.ts: xoá allocate/nextTaskCode/ensureTaskCounter riêng, DELEGATE sang util (POST /tasks nay map inactive→4xx thay vì 500) — giữ hành vi allocate-before-tx; cập nhật task-core-codegen.spec cho xanh. (4) hr-tasks.service.ts: inject SequenceService + DatabaseService; thêm allocateTaskCodeBeforeTx(companyId) gọi util; đổi createApprovalTaskTx(tx, companyId, {title, assigneeUserId, dueDate, taskCode}) để GHI task_code vào row tasks. (5) Ghi task_code: map cột drizzle taskCode:text('task_code') (nullable) vào bảng tasks ở db/schema/workflow.ts rồi .values({...taskCode}) [KHUYẾN NGHỊ — type-safe, KHÔNG đụng drift due_date/due_at], HOẶC raw SQL INSERT mirror task-core.repository nếu owner từ chối mở paths schema. (6) GIỮ NGUYÊN commentPayload() coalesce task_code→title (task-comments.service.ts) làm defense-in-depth — KHÔNG xoá. (7) Thêm hr-tasks.service.spec.ts.","builder":"backend-builder","paths":["apps/api/src/tasks/**","apps/api/src/db/schema/workflow.ts"]},{"id":"hrcode-contracts","task":"Cap độ dài fullName write-side (chống actor_name không giới hạn → dead-letter payload quá dài). packages/contracts/src/employees.ts: thêm .max(200) cho createEmployeeProfileSchema.fullName (L62) + importEmployeeRowSchema.fullName (L106). Xác nhận users.ts write-DTO (updateProfileRequestSchema/adminUser update/user-admin) ĐÃ .max(200) — no-op nếu đã có. Thêm/cập nhật unit spec biên (accept ≤max, reject >max).","builder":"backend-builder","paths":["packages/contracts/src/employees.ts","packages/contracts/src/employees.spec.ts"]},{"id":"hrcode-leave","task":"Đường đơn nghỉ dùng mã thật. leave.module.ts: thêm SequenceModule vào imports (để HrTasksService inject được SequenceService). leave.service.createRequest: gọi hrTasks.allocateTaskCodeBeforeTx(actor.companyId) TRƯỚC khi mở db.withTenant (tx đơn), rồi truyền taskCode vào createApprovalTaskTx — KHÔNG nextCode bên trong tx đơn (tránh giữ lock counter suốt tx dài); mã bị 'đốt' khi validate fail/rollback là CHẤP NHẬN (gap-OK). Cập nhật leave.service.spec: mock HrTasksService.allocateTaskCodeBeforeTx + chữ ký mới createApprovalTaskTx.","builder":"backend-builder","paths":["apps/api/src/leave/**"]},{"id":"hrcode-att","task":"Đường điều chỉnh công dùng mã thật. attendance.module.ts: thêm SequenceModule vào imports. attendance-adjustment.service.createRequest: gọi hrTasks.allocateTaskCodeBeforeTx TRƯỚC db.withTenant, truyền taskCode vào createApprovalTaskTx (gap-OK khi rollback). Bảo đảm SequenceInactiveError (ném TRƯỚC tx) là HttpException nên .catch(mapConflict) giữ nguyên 4xx (mapError re-throw HttpException). Cập nhật attendance-adjustment.service.spec.","builder":"backend-builder","paths":["apps/api/src/attendance/**"]},{"id":"hrcode-int","task":"Bằng chứng LANE_DB (Postgres cô lập). int-spec: (a) HrTasksService thật + SequenceService thật + DB thật — allocateTaskCodeBeforeTx→createApprovalTaskTx ⇒ row tasks task_type='hr' có task_code KHỚP /^TASK-\\d+$/, 2 lần cấp liên tiếp mã KHÁC + tăng dần; (b) PATCH counter 'task'→Inactive ⇒ allocateTaskCodeBeforeTx + POST /tasks NÉM 4xx TASK-ERR (KHÔNG 500); (c) task-comments payload cho task HR đó render mã THẬT (không '{task_code}', không rơi về title). Gate skipIf(!(hasDb && LANE_DB)).","builder":"backend-builder","paths":["apps/api/test/integration/**"]}]
acceptanceChecks: ["Task HR tạo từ đơn nghỉ VÀ đơn điều chỉnh công có task_code THẬT (non-null, khớp /^TASK-\\d+$/ theo counter 'task' 0498) — chứng minh bằng int-spec LANE_DB; 2 lần cấp liên tiếp mã distinct + tăng dần.","task_code cấp qua tx RIÊNG TRƯỚC tx đơn (từ caller, mirror createTask) — KHÔNG gọi nextCode BÊN TRONG tx đơn (không giữ lock counter suốt tx dài); mã bị đốt/gap khi tx rollback là chấp nhận (đo bằng unit invocation-order: allocateTaskCodeBeforeTx chạy TRƯỚC createApprovalTaskTx).","SequenceInactiveError (counter 'task' bị PATCH Inactive) map 4xx (khuyến nghị 409) mã TASK-ERR-CODE-COUNTER-INACTIVE — KHÔNG 500 raw — ở CẢ 3 điểm: POST /tasks, leave.createRequest, attendance-adjustment.createRequest; verify bằng int-spec/unit RED-first.","commentPayload() coalesce task_code→title được GIỮ (defense-in-depth); comment/mention lên task HR nay render mã THẬT (payload task_code != '{task_code}' và != title fallback).","packages/contracts/src/employees.ts: createEmployeeProfileSchema.fullName + importEmployeeRowSchema.fullName có .max() (≤200/255) — reject over-length; users.ts write-DTO xác nhận đã .max(200).","KHÔNG migration mới (cột tasks.task_code có từ 0478 + counter 'task' seed 0498 đã phủ mọi company; ensure-on-miss phủ company tạo sau); KHÔNG hard-delete; audit/append-only + outbox không đổi (BẤT BIẾN #2); mọi ghi qua withTenant/RLS+FORCE (BẤT BIẾN #1).","FULL gate (crown — chạm đường tạo đơn nghỉ/điều chỉnh công + sequence): security-reviewer + database-reviewer + silent-failure-hunter (+ santa-method). bash harness/check.sh --lane-db XANH (int deny/error-path chạy thật, không skip vượt ngưỡng).","Coverage ≥80% file nhạy cảm đã sửa (hr-tasks.service, task-code.util, leave.service, attendance-adjustment.service); task-core-codegen.spec GIỮ XANH sau refactor (POST /tasks vẫn cấp mã qua util)."]
testTasks: ["RED error-path (int/unit): counter 'task' Inactive ⇒ POST /tasks trả 4xx TASK-ERR (trước fix là 500) — prove RED-first rồi GREEN.","RED error-path (int/unit): submit đơn nghỉ khi counter 'task' Inactive ⇒ 4xx TASK-ERR (không 500); tương tự submit đơn điều chỉnh công.","RED unit (hr-tasks.service.spec): createApprovalTaskTx GHI task_code (non-null) vào row tasks — đỏ trước khi wire.","Unit (leave.service.spec + attendance-adjustment.service.spec): createRequest gọi hrTasks.allocateTaskCodeBeforeTx TRƯỚC db.withTenant và truyền taskCode vào createApprovalTaskTx (invocation-order / mock assert).","Int LANE_DB (apps/api/test/integration/**): đơn nghỉ → row tasks task_type='hr' có task_code /^TASK-\\d+$/; task-comments payload cho task đó render mã thật (không '{task_code}', không title fallback).","Int LANE_DB: đơn điều chỉnh công → cùng khẳng định task_code thật + comment render mã thật; 2 lần allocate distinct/tăng dần (không trùng dưới FOR UPDATE).","Contracts spec (employees.spec): fullName reject khi > max, accept khi ≤ max (create + import row).","Regression: task-core-codegen.spec giữ xanh (createTask vẫn allocate qua util, ensure-on-miss retry ĐÚNG 1 lần, không loop)."]
steps: ["Lane hrcode-tasks TRƯỚC (không phụ thuộc): tạo tasks/task-code.util.ts (allocate + ensure-on-miss retry-once + map SequenceInactiveError→4xx TASK-ERR); refactor task-core.service delegate util; sửa hr-tasks.service (inject SequenceService+DatabaseService, allocateTaskCodeBeforeTx, createApprovalTaskTx ghi task_code); map cột taskCode vào drizzle tasks (workflow.ts) hoặc raw SQL; giữ commentPayload coalesce; thêm hr-tasks.service.spec.","Lane hrcode-contracts SONG SONG (độc lập): thêm .max() cho employees.ts fullName (create + import) + spec biên.","Sau hrcode-tasks XANH → Lane hrcode-leave ‖ hrcode-att SONG SONG: import SequenceModule; allocate taskCode TRƯỚC withTenant; truyền vào createApprovalTaskTx; cập nhật service.spec (mock allocateTaskCodeBeforeTx + chữ ký mới).","Sau tasks+leave+att → Lane hrcode-int: viết int-spec LANE_DB (task_code thật cho task HR từ đơn nghỉ + điều chỉnh công; Inactive→4xx; comment render mã thật).","RED trước cho error-path/workflow (Inactive→4xx + createApprovalTaskTx ghi task_code) → GREEN → FULL gate (crown) → check.sh --lane-db xanh → checkpoint; đỏ/CRITICAL/nhạy cảm ⇒ người chốt red-zone.","Cập nhật harness/backlog.mjs (status) + docs/plans/S5-TASK-HRCODE-1.md (persist reconcileNotes) theo DoD §8."]
```

GAP-ANALYSIS (đối chiếu code 2026-07-17): (1) hr-tasks.service.createApprovalTaskTx (apps/api/src/tasks/hr-tasks.service.ts:17-36) drizzle-insert KHÔNG set task_code ⇒ task HR từ leave.service.createRequest (leave.service.ts:194) + attendance-adjustment.service.createRequest (attendance-adjustment.service.ts:126) NULL sau cut-over 0498. (2) SequenceInactiveError: task-core.service.allocateTaskCode (task-core.service.ts:355) chỉ bắt SequenceNotFoundError; else `throw err` ⇒ Inactive propagate 500 raw ở POST /tasks; leave/attendance cũng vậy. (3) contracts employees.ts fullName (L62 create, L106 import) THIẾU .max(); users.ts write-DTO đã .max(200).

DRIFT LANDMINE (quan trọng cho builder): drizzle `tasks` (apps/api/src/db/schema/workflow.ts:387) STALE — KHÔNG có cột task_code, dùng due_date; trong khi task-core.repository (task-core.repository.ts:361-378) raw-SQL ghi task_code + due_at/start_at (cột thật thêm bởi migration sau, 0478). Cột DB tasks.task_code TỒN TẠI (mig 0478) nhưng chưa map vào drizzle object. ⇒ 2 lối ghi task_code cho task HR: [A KHUYẾN NGHỊ] thêm 1 dòng taskCode:text('task_code') (nullable) vào drizzle tasks (workflow.ts) rồi .values({...taskCode}) — type-safe, KHÔNG đụng drift due_date/due_at, giữ nguyên hành vi cột hiện có; [B thay thế] raw SQL INSERT mirror task-core.repository nếu owner KHÔNG muốn mở paths schema.

PATHS GAP: WO.paths THIẾU apps/api/src/db/schema/workflow.ts (cần cho lối A) ⇒ guard-scope sẽ WARN cho lane hrcode-tasks; owner nên bổ sung paths HOẶC chọn lối B (ở nguyên tasks/**). Đã đưa workflow.ts vào paths lane hrcode-tasks.

THIẾT KẾ CHỐT: allocate mã ở tx RIÊNG TRƯỚC tx đơn — SequenceService.nextCode TỰ mở withTenant nên chỉ cần gọi allocateTaskCodeBeforeTx TRƯỚC khi caller mở db.withTenant; KHÔNG nextCode trong tx đơn (chống giữ lock counter suốt tx dài). Gap khi rollback = CHẤP NHẬN (counter đơn điệu, cần duy nhất chứ không liên tục). DRY 1 điểm map lỗi: tasks/task-code.util.ts dùng CHUNG cho POST /tasks + leave + attendance ⇒ SequenceInactiveError→4xx TASK-ERR-CODE-COUNTER-INACTIVE (khuyến nghị 409 ConflictException, HttpException nên surface 4xx cả khi ném TRƯỚC .catch). Vì leave/attendance KHÔNG nên import ERR của tasks ⇒ util tự ném HttpException. GIỮ commentPayload() coalesce task_code→title (task-comments.service.ts:353) làm defense-in-depth (KHÔNG xoá — WO ghi 'CÂN NHẮC giữ').

MODULE WIRING: leave.module.ts + attendance.module.ts (đều provide HrTasksService cục bộ, đều CHƯA import SequenceModule) phải thêm SequenceModule vào imports để HrTasksService inject SequenceService (SequenceModule đã export SequenceService; DatabaseModule đã có ở cả hai). KHÔNG có module thứ 3 provide HrTasksService (chỉ leave + attendance).

BẤT BIẾN/INVARIANTS: KHÔNG migration mới (0478 + 0498 phủ đủ; ensure-on-miss phủ company mới). Mọi ghi qua withTenant RLS+FORCE (#1). KHÔNG hard-delete; audit/append-only + outbox không đổi (#2). KHÔNG secret plaintext (#3) — task_code là mã công khai non-sensitive. Thứ tự phụ thuộc §3 thỏa (permission/audit đã có sẵn; đây là bổ sung mã).

VERIFY/GATE: crown (chạm FSM phê duyệt đơn nghỉ/điều chỉnh công) ⇒ FULL gate + planner Sonnet5 xhigh (crown PLAN) nhưng IMPLEMENT/REVIEW Opus; RED deny/error-path TRƯỚC (Inactive→4xx + task_code ghi) rồi GREEN; bash harness/check.sh --lane-db (Postgres) để int deny-path chạy thật, tránh xanh-giả (INT_SKIP_THRESHOLD).

THỨ TỰ THI CÔNG: hrcode-tasks (nền, không phụ thuộc) + hrcode-contracts (song song, độc lập) → sau tasks XANH: hrcode-leave ‖ hrcode-att (song song, paths không chồng, cùng phụ thuộc tasks) → hrcode-int (phụ thuộc tasks+leave+att). Không lane db-migration.

OUT-OF-SCOPE / OPTIONAL: (a) Residual LOW db-review — ensureCounterTx recovery 23505 re-select trên tx aborted 25P02 (sequence.repository.ts:~119, foundation/sequences/** có trong WO.paths): sửa CÙNG lượt NẾU tiện, KHÔNG phải acceptance; nếu làm, mở rộng lane hrcode-tasks thêm path foundation/sequences/**. (b) KHÔNG động vào drift due_date vs due_at của bảng tasks (task HR hiện ghi due_date qua drizzle — giữ nguyên để tránh regression; ngoài phạm vi WO). (c) KHÔNG gỡ commentPayload coalesce. HOT-FILE: hr-tasks.service.ts (append method + đổi sig, cùng lane) · task-core.service.ts (refactor delegate util, cùng lane) · workflow.ts drizzle tasks (append 1 cột mapping) · leave.module.ts/attendance.module.ts (append SequenceModule vào imports — additive) · packages/contracts/src/employees.ts (append .max()). Không file nào bị 2 lane cùng sửa.
</content>

---

## RECONCILE 2026-07-18 — phạm vi THU HẸP (owner chốt)

**Phát hiện khi viết lane `hrcode-int`:** `LeaveService.createRequest/approveRequest/rejectRequest/cancelRequest` là **CODE CHẾT** — không route HTTP nào tới được:

- `POST /leave/requests` (`leave.controller.ts:254`) gọi `LeaveRequestService.createDraft`, **không phải** `LeaveService.createRequest`.
- approve/reject/cancel thật đi qua `LeaveApprovalService`/`LeaveRevokeService` — cả hai **không import `HrTasksService`**.
- `CreateLeaveRequestDto`/`createLeaveRequestSchema` **không được import ở bất kỳ controller nào** (grep toàn `apps/api/src`).
- `leave_requests.task_id` chỉ được SELECT (`leave.repository.ts:286`), **không bao giờ được ghi** bởi đường sống ⇒ luôn NULL dưới traffic thật.

**Đây KHÔNG phải bug bỏ sót.** `SPEC-05 LEAVE` không hề nhắc Task Hub (grep 0 kết quả); `SPEC-06` chỉ nhắc nghỉ phép ở §11.1 *"cảnh báo khi giao task cho nhân viên đang nghỉ phép"*. Bản rebuild Sprint 3 (#56, `LeaveRequestService` theo SPEC-05) bỏ liên kết Task Hub thời G11 là **đúng spec**; khối cũ nằm lại thành code chết sau đợt de-media-fy. Dòng comment `BẤT BIẾN: đơn nghỉ duyệt QUA Task Hub` (`leave.service.ts:38`) là bất biến G11 **đã hết hiệu lực** — kế hoạch WO gốc tin vào comment đó thay vì spec.

**Quyết định (owner, 2026-07-18):** ship phần THẬT, gỡ wiring leave.

| Hạng mục | Trạng thái |
| --- | --- |
| `tasks/task-code.util.ts` (util dùng chung, Inactive→409, ensure-on-miss retry-once) | ✅ giữ |
| `task-core.service` delegate util (POST /tasks: 500 raw → 409) | ✅ giữ |
| `attendance-adjustment` cấp mã trước tx (ĐƯỜNG SỐNG) | ✅ giữ, int-spec HTTP xanh |
| `contracts/employees.ts` `fullName.max(200)` | ✅ giữ |
| `leave.service.ts` / `leave.module.ts` / `leave.service.spec.ts` | ❌ **revert về master** |
| Lane `hrcode-leave` trong kế hoạch trên | ❌ **vô hiệu** — đọc mục này thay thế |

**Bằng chứng LANE_DB:** `apps/api/test/integration/hr-task-code.int-spec.ts` — 3 test qua HTTP thật trên `mediaos_hrcode`, xanh.

**Việc phái sinh:** `S5-LEAVE-DEADCODE-1` (đã seed vào `harness/backlog.mjs`) — dọn khối chết. Là thao tác XÓA ở vùng đỏ ⇒ review riêng (memory `review-gate-blind-to-deletions`).
