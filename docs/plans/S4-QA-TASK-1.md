```yaml
wo: S4-QA-TASK-1
zone: yellow
generated_by: auto-loop
reconciled_at: "912e6fe"
lanes: [{"id":"qapermmatrix","task":"QA canonical: ma trận permission per-(role × pair) TASK + data-scope Own/Team/Project + IDOR cross-tenant. Viết task-qa1-permission-matrix.int-spec.ts (DB thật, DB cô lập mediaos_qatask1, gate hasDb && LANE_DB). Iterate 4 canonical role (employee/manager/hr/company-admin) trên các cặp TASK tiêu thụ bởi route sống (read/create/update/delete/assign/update-status/update-priority/update-deadline/comment/watch/view:task-audit-log); cặp KHÔNG grant (deny-hole theo TASK_GRANT_MATRIX ∪ TASK_DEFERRED_GRANTS thực-tế trong DB) → 403; cặp có grant → 2xx. Data-scope: employee @Own thao tác task được-assign/tự-tạo → 200, task ngoài Own → 404; manager @Team task trong team → 200, ngoài team → 404; membership Project (project_members) mở scope đúng; cross-tenant taskId (tenant B) → 404 (IDOR, KHÔNG 403). RED-first: mọi assert deny chứng minh RED trước khi có enforcement. Harden-in-place src/tasks/** CHỈ khi lộ bug thật (fail-open permission/scope).","builder":"backend-builder","paths":["apps/api/test/integration/task-qa1-permission-matrix.int-spec.ts","apps/api/src/tasks/**"]},{"id":"qafsmcollab","task":"QA canonical: FSM workflow + collaboration deny. Viết task-qa1-fsm-collab.int-spec.ts (DB thật, DB cô lập mediaos_qatask1, gate hasDb && LANE_DB). Phủ: (1) FSM chuyển sai bị chặn — transition ngoài bảng SPEC-06 §14.11 → 409 TASK-ERR-WORKFLOW-INVALID + state KHÔNG đổi + 0 outbox/activity; Cancelled terminal → 422 TASK-ERR-TASK-CLOSED; Done khi checklist is_required_for_done chưa tick → 400 CHECKLIST-REQUIRED; from===to → 200 no-op 0 event. (2) kanban move (POST /tasks/:id/move) mirror update-status: thiếu update-status:task → 403; move ĐI QUA CÙNG FSM (transition sai → 409). (3) watcher self-only: add self → Active; add/remove watcher người khác → deny; duplicate → 409 DUPLICATE-WATCHER; actor không có employee_profiles → 400 WATCHER-NO-EMPLOYEE. (4) comment: thiếu comment:task → 403; comment task ngoài scope đọc → 404; PATCH/DELETE comment người khác → 403 (self-only); mention ngoài scope → 403 BLOCK. (5) checklist/item: mutate thiếu update:task → 403; task-audit-log view thiếu quyền (employee/manager) → 403. (6) assign cảnh báo assignee-on-leave (Approved leave trùm ngày) → 200 + warning GIỮ NGUYÊN (KHÔNG chặn); assign ngoài Team → 404. (7) actor-exclusion nơi áp dụng: self-assign / self-comment KHÔNG tự sinh recipient cho actor (assert trên warnings/outbox payload, KHÔNG kiểm NOTI e2e — thuộc S4-INT-1).","builder":"backend-builder","paths":["apps/api/test/integration/task-qa1-fsm-collab.int-spec.ts"]}]
acceptanceChecks: ["Deny-matrix: với mỗi role canonical (employee/manager/hr/company-admin), MỌI cặp TASK không-grant (theo grant thực-tế DB) trên route sống trả 403; cặp có-grant trả 2xx — không lỗ over/under-grant. Assert đi qua đường thật JwtAuthGuard→CompanyGuard→PermissionGuard (KHÔNG mock permission).","Data-scope: employee thao tác (update-status/comment/watch) task được-assign/tự-tạo → 200; cùng action trên task ngoài Own → 404; manager @Team task trong team → 200, ngoài team → 404; project membership (project_members Active) mở đúng scope. Ngoài-scope WRITE trả 404 (fail-closed), KHÔNG 403.","IDOR: taskId thuộc tenant B, actor tenant A (mọi verb read/update/delete/assign/change-status/priority/deadline/comment/checklist) → 404 nhất quán (không lộ tồn tại chéo tenant, RLS+withTenant là hàng rào).","FSM: transition ngoài bảng SPEC-06 §14.11 → 409 TASK-ERR-WORKFLOW-INVALID, task_status KHÔNG đổi, 0 outbox/0 activity; from=Cancelled mọi đích → 422 TASK-ERR-TASK-CLOSED; Done khi checklist is_required_for_done chưa xong → 400 TASK-ERR-CHECKLIST-REQUIRED; from===to → 200 no-op.","kanban move (POST /tasks/:id/move) thiếu update-status:task → 403; move dùng CÙNG FSM (transition sai → 409, không lách).","watcher self-only: add self → Active; duplicate → 409 TASK-ERR-DUPLICATE-WATCHER; actor không employee_profiles → 400 TASK-ERR-WATCHER-NO-EMPLOYEE; add/remove watcher người khác không được phép.","comment: thiếu comment:task → 403; comment task ngoài scope đọc → 404; PATCH/DELETE comment người khác → 403 self-only; mention ngoài scope → 403 BLOCK. checklist/item mutate thiếu update:task → 403; view:task-audit-log thiếu quyền (employee/manager) → 403 (TASK-ERR-042).","assign: assignee có Approved leave trùm ngày → 200 + warning (KHÔNG chặn, GIỮ NGUYÊN hành vi); assign ngoài Team (manager) → 404; actor-exclusion: self-assign/self-comment KHÔNG sinh recipient cho actor (assert warnings/outbox payload).","Gate LANE_DB: cả hai spec chỉ chạy khi hasDb && process.env.LANE_DB; chạy THẬT trên mediaos_qatask1 (không skip im lặng); khớp glob vitest include test/**/*.int-spec.ts.","Coverage lớp nhạy cảm TASK (src/tasks/**) ≥80%; check.sh xanh; LIGHT gate PASS (+ security-reviewer trên nhánh permission); DoD §8: có test, audit-on-action còn nguyên, không phá luồng chính, backlog.mjs cập nhật."]
testTasks: ["RED-first: task-qa1-permission-matrix.int-spec.ts — deny-hole 403 per-(role × pair không-grant) trên đường thật; chứng minh RED trước enforcement (không mock PermissionGuard).","task-qa1-permission-matrix.int-spec.ts — data-scope Own/Team/Project: in-scope 2xx vs out-of-scope WRITE 404; và IDOR cross-tenant taskId (tenant B) → 404 cho mọi verb.","task-qa1-fsm-collab.int-spec.ts — FSM deny: transition sai → 409 (state bất biến, 0 event), Cancelled terminal → 422, Done checklist-required → 400, from===to no-op.","task-qa1-fsm-collab.int-spec.ts — kanban move thiếu update-status → 403 + move dùng FSM; watcher self-only (duplicate 409, no-employee 400, người khác deny).","task-qa1-fsm-collab.int-spec.ts — comment/checklist deny (thiếu cặp → 403, ngoài scope → 404, self-only PATCH/DELETE, mention out-of-scope BLOCK, view:task-audit-log employee/manager → 403).","task-qa1-fsm-collab.int-spec.ts — assign on-leave warning giữ nguyên (200+warning, không chặn) + actor-exclusion (self-assign/self-comment không tự-recipient) qua warnings/outbox payload.","Toàn bộ int-spec gate hasDb && LANE_DB, chạy trên DB cô lập mediaos_qatask1; đo + đạt coverage ≥80% src/tasks/** nhạy cảm."]
steps: ["Lane-DB cô lập: bash scripts/lane-db-setup.sh qatask1 (chain 0000→head 0490) → export LANE_DB=mediaos_qatask1. Cả hai spec skipIf(!(hasDb && !!process.env.LANE_DB)) — mirror hasLaneDb của task-actions.int-spec (chống false-green/false-red theo memory integration-test-LANE_DB-gate + ci-skips-most-integration-specs).","Đối chiếu grant THỰC-TẾ trong DB sau 0485/0486 (TASK_GRANT_MATRIX ∪ TASK_DEFERRED_GRANTS: emp create/update @Own, mgr create/update/delete @Team đã áp qua BE-2/RECON-2) — QA đọc role_permissions từ DB hoặc dùng const task-permissions.const.ts làm nguồn, KHÔNG hardcode kỳ vọng lệch seed.","qapermmatrix: seed 2 tenant (A actor, B cross-tenant) + 4 role canonical + org-unit/team + project membership; author matrix deny (missing-pair→403) + data-scope Own/Team/Project (out-of-scope write→404) + IDOR (cross-tenant taskId→404). Chạy RED trước enforcement, xác nhận từng assert deny.","qafsmcollab: seed task fixtures theo trạng thái (Todo/In Progress/In Review/Done/Cancelled) + checklist required + assignee có Approved leave; author FSM invalid/terminal/checklist-required + move-mirror + watcher self-only + comment/checklist deny + assign on-leave warning + actor-exclusion.","Chạy pnpm --filter @mediaos/api test với LANE_DB=mediaos_qatask1; đo coverage lớp nhạy cảm TASK (src/tasks/**) ≥80%; bổ sung case tới khi đạt ngưỡng.","Nếu lộ bug fail-open (permission/scope/FSM) → harden-in-place src/tasks/** trong lane qapermmatrix, ghi rõ diff; nhánh permission chạm → security-reviewer soi deny-path.","check.sh xanh (typecheck + test có LANE_DB) → LIGHT gate (typescript-reviewer + quality-gate); cập nhật harness/backlog.mjs done_when. LƯU khối yaml máy-đọc vào docs/plans/S4-QA-TASK-1.md để tái dùng."]
```

GAP-ANALYSIS: WO là QA CONSOLIDATION, không build feature. Coverage TASK đã DÀY từ các WO builder: task-actions.int-spec.ts (789 dòng, BE-3 crown-FSM: FSM invalid/terminal/checklist-required, assign deny+on-leave warning, watcher self-only, priority/deadline deny, cross-tenant 404), task-core.int-spec.ts (547, CRUD data-scope), task-core-tenant-deny.int-spec.ts (313, RLS 5 bảng mới), task-comments-checklists.int-spec.ts (406), task-kanban-move-activity.int-spec.ts (357), tasks-board.int-spec.ts (154). QA-TASK-1 KHÔNG lặp lại — nó thêm (a) ma trận HỆ THỐNG per-(role × pair) đối chiếu grant thực-tế DB (đóng lỗ over/under-grant mà per-WO spec chỉ chạm lẻ), (b) IDOR cross-tenant nhất quán cho MỌI verb, (c) làm CỨNG gate hasDb && LANE_DB trên mediaos_qatask1 để không false-green. NGUỒN CHUẨN: TASK_GRANT_MATRIX + TASK_DEFERRED_GRANTS ở apps/api/src/foundation/seed/task-permissions.const.ts (23 cặp canonical, 8 sensitive: delete/close/archive/manage-member/view-report:project + delete/export:task + view:task-audit-log). LƯU Ý grant thực-tế: 5 grant HOÃN (emp create/update @Own, mgr create/update/delete @Team) ĐÃ áp qua S4-TASK-BE-2/RECON-2 → QA phải đọc grant THẬT trong DB, KHÔNG hardcode kỳ vọng lệch. INVARIANTS (CLAUDE.md §2): mọi query withTenant(companyId) + RLS+FORCE (BB#1) → cross-tenant = 404; task_activity_logs append-only (BB#2) → assert 0-mutate; outbox payload chỉ ID/enum/title/taskCode+actorUserId, KHÔNG description/reason (BB#3). Data-scope: DataScopeService.resolveAndAssert(user, action, 'task') → Own/Team/Department/Company/System; 'Project' của done_when = MEMBERSHIP project_members (buildReadScopeExists OR scopeCond), KHÔNG phải enum DataScope. Ngoài-scope WRITE → NotFoundException (404 fail-closed), KHÔNG 403 (đã đọc task-core.service.ts:333-351). FSM nguồn: task-fsm.ts (bảng SPEC-06 §14.11): Todo→{In Progress,Cancelled}; In Progress→{In Review,Done,Cancelled}; In Review→{In Progress,Done,Cancelled}; Done=∅ (reopen hard-off); Cancelled terminal. Mã lỗi: 409 TASK-ERR-WORKFLOW-INVALID, 422 TASK-ERR-TASK-CLOSED, 400 TASK-ERR-CHECKLIST-REQUIRED, 409 TASK-ERR-DUPLICATE-WATCHER, 400 TASK-ERR-WATCHER-NO-EMPLOYEE, 400 TASK-ERR-INVALID-DATE-RANGE. Assign on-leave = buildAssignWarnings → 200 + warning (KHÔNG chặn — giữ nguyên). VERIFY/GATE: scripts/lane-db-setup.sh qatask1 → export LANE_DB=mediaos_qatask1 → pnpm --filter @mediaos/api test; check.sh dòng 52 chạy pnpm test với LANE_DB (vitest include test/**/*.int-spec.ts, dòng 47). Spec PHẢI skipIf(!(hasDb && !!process.env.LANE_DB)) mirror hasLaneDb — memory integration-test-LANE_DB-gate + ci-skips-most-integration-specs: skipIf(!hasDb) đơn thuần là false-green vì .env làm hasDb=true nhưng không có LANE_DB. Migration: KHÔNG cần (QA/test WO, head idx 170/0490 — không đổi schema). Builder: cả hai lane backend-builder (test dưới apps/api, không .tsx). Hai lane KHÔNG chồng path (matrix.int-spec.ts + src/tasks harden vs fsm-collab.int-spec.ts riêng) → chạy song song, không đụng guard-scope; mỗi lane seed tenant riêng, cleanup afterAll. OUT-OF-SCOPE (không kéo vào QA này): project-resource CRUD deny (read/create/update/delete/close/archive/manage-member/view-report:project — thuộc projects/project-states controller, QA project riêng — title WO chỉ liệt TASK CRUD/assign/status/kanban/comment/checklist); file-upload/file-delete:task (route chưa ship — S4-TASK-BE-5); NOTI recipient e2e/actor-exclusion đầu-cuối (S4-INT-1 — ở đây chỉ assert warnings/outbox payload, không kiểm notification thật); legacy PATCH /tasks/:id/status (giữ song song, không phải luồng core). GATE gợi ý: nếu QA lộ bug fail-open permission/scope/FSM → harden-in-place src/tasks/** (lane qapermmatrix), route security-reviewer + silent-failure-hunter (FULL cho nhánh permission) trước merge.
</content>
