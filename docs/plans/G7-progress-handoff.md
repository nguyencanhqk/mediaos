# G7 — Progress Handoff (LUỒNG A spine)

> Trạng thái thực thi G7 Workflow Builder. Cập nhật mỗi viên. Nguồn kế hoạch: [`G7-workflow-builder.md`](./G7-workflow-builder.md) (§4 micro-steps, §5 RED suite, §8 luồng, **§10 residual = chi tiết quyết định từng viên**).

**Branch:** `feat/g7-workflow` (main dir `c:/dev 2/MediaOS`) · 1a…3c + ⑥ gate + 4a + 4b + **4c (`119a94a` migration 0036 + `e539938` eval hook)** **đã push** · **LUỒNG A spine (1a→4c) HOÀN TẤT.**

> **✅ GATE TỔNG + PR XONG (2026-06-12, HEAD `cff1994`):** đã **merge `master` (G5-fix) + reconcile** (`32ac739`) — drop `0030_g5fix_org_team_permissions_seed` redundant (master 0030 superset), **rename g3fix→`0037`**, journal dựng lại đơn điệu; **DB dev nay ở migration 0037** (chain `0000→0037` apply sạch trên DB reset). Gate HOLISTIC: **BE B1 FOCUSED** (security+database+silent-failure) → fix **S2** (null-reviewer fail-open→tự duyệt, FSM Guard 5 FAIL-CLOSED, RED-first) + **D4** (`requestRevision` thêm per-instance FOR UPDATE, parity approve()) + **SF3/SF5** → **santa dual-review cả 2 PASS** (re-attack S2 5 vector — không bypass). **FE LIGHT** → checklist submit-gate fail-closed khi load (TS-HIGH1) + canvas a11y. Verify XANH: BE typecheck · workflow unit 123 · full unit 427 · int 284+2skip · e2e 17 · FE typecheck/test 133/build. Dấu vết đầy đủ + residual: [`docs/reviews/g7-gates.md`](../reviews/g7-gates.md) (§4: S1 RBAC-layer, D1/D2, **TS-HIGH2 editor permission-gate**, FE a11y hardening). **PR `feat/g7-workflow` → `master`** (đã push). Commits gate: `2fbe7d0` · `0c0e88d` · `e9e93c7` · `cff1994`.

**🔀 HỘI TỤ G7-C (FE) — `feat/g7-fe` merged vào A (fast-forward, HEAD `da352f8`, CHƯA push):** Track C (FE workflow builder: templates + DAG canvas + instance view, nối API thật) + **checklist UI 4b FE** (tick required + chặn submit) đã gộp vào spine. Verify tích hợp: `web build` + `api typecheck` XANH (cần `pnpm install` + rebuild contracts ở main dir do FE thêm `@xyflow/react` & export contract mới). **Residual:** GET checklist endpoint mới (`0379e2a`) CHƯA có api test riêng (int cần Docker/DB) → +FULL gate cho phần BE mới — xem cuối file.

---

## ✅ ĐÃ XONG (đừng làm lại)

| Viên | Commit | Tóm tắt |
| --- | --- | --- |
| 1a–1c | `5c0f7a9`…`91fb4f5` | 0032 (template/DAG/checklist) + contracts freeze (1b) + 0033 audit object_type + WorkflowTemplates CRUD (template/step/dep/checklist, draft-only, RLS+audit) |
| 2a (LUỒNG B) | `05ea98a` | `DagValidatorService` (pure, DV1–DV6) — merge vào A |
| merge | `4a012ac` | merge `feat/g7-dag` → A |
| 2a-fix | `7fbf2cd` | NUL-byte trong dag-validator → escape `\u0000` (de-binary) |
| **2b** | `12f7b19` | **publish/clone lifecycle** — DAG gate (422 + dagValidation), clone version+1 copy steps/deps/checklists + node_key giữ; adapter `dag-result.adapter.ts` (service code → contract code) |
| 1a-fix | `8ccec9e` | seed `node_key` (G4-7 e2e xanh) |
| **3a** | `a4f7b5b` | **migration 0034** — instance đa-target (content XOR project) + `definition_version` + `workflow_steps.node_key` + `workflow_step_checklist_states` (RLS+FORCE) |
| **3b** | `2032894` | **applyTemplate** — instance từ template published, snapshot steps, mở **bước root** (auto-task idempotent); endpoint `POST /workflow-templates/:id/apply` gate `apply:workflow-instance` |
| **3c-i** | `0d31be6` | **DAG dep-guard** — `workflow-dag.ts` PURE (`allDependenciesApproved`, key=node_key??step_code, root→true, fail-closed); FSM start/submit guard = `dependenciesApproved===false→DependenciesNotMetError` (bỏ guard stepOrder===currentStepOrder); repo `findStepsByInstanceIdInTx`; service `resolveDependenciesApproved` (await tuần tự) |
| **3c-ii** | `c26eb94` | **approve() fan-out + complete theo DAG** — `computeNewlyUnblockedStepIds` (mở 0..n downstream khi dep đủ → auto-task idempotent) + `isWorkflowComplete` (mọi step required approved, KHÔNG theo order); task read vào tx (`findActiveTaskByStepIdInTx`); giữ alias `isLastStep:isWorkflowComplete` (e2e linear xanh). `findMaxStepOrder`/`advanceInstanceStepOrder` **ngừng dùng** (W2→3c-iii) |
| **3c-iii** | `af8f583` | **FOR UPDATE race-safety + W2 cleanup + FS5/FS10** — `approve()` chèn `lockInstanceForUpdateInTx` (SELECT…FOR UPDATE per-instance) NGAY sau `findInstanceByIdInTx`, TRƯỚC mọi read dep → 2 approver đóng 2 dep cuối của join serialize (BLOCKING #2; **approve-only**, không deadlock). W2: xoá `findMaxStepOrder`+`advanceInstanceStepOrder`+`updateInstanceStepOrder` (+ bỏ `max` import; `current_step_order` chỉ advisory). FS10 = **probe lock xác định** (giữ khoá → approve() block; bỏ Promise.all timing vì phụ-thuộc-thứ-tự-test). FS5 = `validateConsumerTransition` writer DUY NHẤT của approved/revision (cặp D6). ⑤ FS11 skip (e2e linear che). **⑥ gate FULL+santa XONG → fix `62b5374`.** |
| **⑥ gate** | `62b5374` | **FULL+santa gate** trên crown-jewel 3c — security+database+silent-failure (song song) → santa dual-review **NICE**. Vá **F1** (23505→409 ở `approve()`+`requestRevision()`), **F2** (`requestRevision` đọc task in-tx → đóng TOCTOU), **F3** (fan-out no-op `logger.warn`). Chỉ `approval.service.ts`+spec. Verify XANH (fsm 27 · approval 19 · int 5 FS10 3× · e2e 17 · typecheck). |
| **4b** | `187bb3c` | **Checklist enforcement — submit (T2) gated bởi required-checklist completion.** KHÔNG migration (bảng `workflow_step_checklist_states`/`checklists`/`checklist_items` đã có từ 0032/0034, RLS+registry sẵn). **Linkage = QUYẾT ĐỊNH #1 ĐƯỜNG A:** instance `workflow_steps.node_key` (+ `instance.workflow_definition_id`) → `workflow_definition_steps` (uq def+node_key) → `checklists.workflow_definition_step_id` → `checklist_items WHERE is_required` − ticked rows. **`default_checklist_id` LUÔN NULL** (3b/clone không set; xác nhận `workflow-templates.service.ts:324`) → KHÔNG dùng đường B. **Gate:** FSM **guard 7** (submit-only; `checklistComplete===false`→`ChecklistIncompleteError`→409; `undefined` bỏ qua = chống over-gate, đặt SAU guard 6 nên from-state/actor/deps/lock báo lỗi trước). `resolveChecklistComplete` = 1 query `count(*)` NOT EXISTS; **fail CLOSED** nếu count trả 0 row (KHÔNG `?? 0` fail-open); `node_key` null = vacuously complete + debug log. **Tick/untick API** `POST`/`DELETE` `/workflow/steps/:stepId/checklist-items/:itemId`: **assignee-gated** (QĐ #2 phương án A — actor===assignee→`ForbiddenException`), cross-item scope (item PHẢI thuộc checklist của def-step bước → `NotFoundException`, chống tick item lạ), idempotent (uq step+item, onConflictDoNothing target tường minh / DELETE no-op), **audit chỉ khi đổi thật** (`ChecklistItemChecked`/`Unchecked`, no-op replay KHÔNG audit). **Race:** `FOR UPDATE` trên row `workflow_steps` ở CẢ submit + tick/untick → đóng kẽ untick-during-submit (1 tài nguyên, cùng thứ tự → không deadlock; submit KHÔNG khoá instance, approve KHÔNG khoá step). **Contract additive** (`workflowStepChecklistStateSchema` + `toggleChecklistItemResultSchema` có `changed`); schema 1b FROZEN KHÔNG đụng. **RED→GREEN:** FSM unit 4 case + int LK4 8 test (gate · no-checklist/optional over-gate · required block/tick · mixed · tick idempotent · cross-item · non-assignee). Verify XANH (typecheck · unit 116 · int 282 · e2e 17). **Gate FULL:** security CLEAN (company_id mọi query, soft-state KHÔNG audit-data, authz kín) · database (đóng race untick-vs-submit H1) · silent-failure (fail-closed count, debug-log node_key null, audit-on-change) · **santa dual-review PASS/PASS** (bypass-hunter xác nhận node_key NOT NULL, version/clone checklist isolation, lock serialize, cross-tenant/cross-item kín). |
| **4c-ii** | `119a94a` | **Migration 0036 — permissions seed (sub-gate riêng, LIGHT).** Catalog 5 quyền **HYPHEN** (`create/update/publish/read:workflow-template` + `apply:workflow-instance`, `is_sensitive=false`) + **grant company-admin (001)** cả 5 → mở khoá endpoint workflow-templates đang fail-closed 403. **QUYẾT ĐỊNH #A = A2:** catalog & grant TÁCH (`can()` đọc `role_permissions`, KHÔNG tra catalog; 0005 grant admin "all non-sensitive" chạy 1 lần ở idx 5 → KHÔNG hồi tố → grant TAY ở đây). KHÔNG bảng/policy mới (catalog toàn cục). Hand-author SQL + journal idx 37 / `when 1717500044000` tay (drizzle-kit generate KHÔNG dùng được). `ON CONFLICT DO NOTHING` bare (arbiter `role_permissions_uq (role_id,permission_id,effect)` — named target sai sẽ ERROR; idempotent verified). KHÔNG grant project-manager (hoãn grant-catalog runtime), KHÔNG seed `manage:workflow-instance` (YAGNI). **Regression 0:** không test nào assert HTTP-403 (templates int gọi thẳng service; PD2/PD3 metadata-static; e2e worker=PM không chạm endpoint). **Gate LIGHT:** `database-reviewer` APPROVE-WITH-NITS + tenant-isolation(144)/rls-guards(3) XANH; migrate EXIT=0, 5 catalog + grant verified qua psql. |
| **4c-i** | `e539938` | **Eval hook — `approve()` emit `step.evaluation_required` khi bước `requires_evaluation=true` (transactional outbox).** Sau enqueue `step.approved`: match def-step theo `node_key (step.nodeKey ?? step.stepCode)` từ `defStepRows` đã đọc (no N+1); nếu `requiresEvaluation` → enqueue outbox `{stepId,instanceId,evaluationTemplateId,approvedBy}` + audit `StepEvaluationRequired`, **CÙNG tx approve** (rollback ⇒ không event ma). **QUYẾT ĐỊNH #B = emit-only, KHÔNG consumer:** worker xử lý 0-consumer = `done` (outbox-worker.ts:114-118, KHÔNG dead-letter); G8 nối consumer sau. No-match def-step → `logger.warn` (KHÔNG drop lặng). Eval cols inert từ 0035 → đây là nơi ĐẦU TIÊN dùng. **RED→GREEN:** EV1 (true → đúng 1 event payload đủ + audit assert + rowCount guard) / EV2 (false → 0 event, control `step.approved`). **Gate FULL-lite:** `security-reviewer` + `silent-failure-hunter` (APPROVE-WITH-NITS → sửa warn-log no-match + `instanceId` vào audit) → `santa-method` dual-review **NICE/NICE**. Verify XANH (typecheck · unit 116 · int 284 · e2e 17). |
| **4a** | `0d8535b`+`c7fab96` | **LockPropagationService (BR-006/WF-003 `downstream_blocked_by_revision`).** Migration **0035** (sub-gate riêng): eval-hook cols (`requires_evaluation`, `evaluation_template_id` soft-ref — inert, dùng ở 4c) + partial-uq `wf_step_locks_active_uq (company,locked,caused_by) WHERE released_at IS NULL` (bảng locks đã có từ 0008, RLS sẵn). ⚠️ **drizzle-kit generate KHÔNG dùng được** (repo chỉ giữ `0000_snapshot.json` → generate full-recreate) — **hand-author SQL** + journal `when` thủ công (1717500043000); idx 36 / tag 0035 (idx≠tag-number theo lệ repo). **Lock:** revision N → `computeTransitiveDescendants` (pure BFS, dedup diamond, fail-closed) → INSERT lock hậu duệ (caused_by=N); nhánh độc lập KHÔNG khóa (LK2). Re-approve N → release caused_by=N (soft `released_at`) → fan-out mở chỉ khi NOT EXISTS lock active khác (multi-source LK5, batch `findLockedStepIds` 1-query). FSM `stepLocked` guard (start/submit) → `StepLockedError`→409, trước deps-guard. Wiring: `requestRevision` (insert in-tx), `approve` (release+open-filter), `workflow.service` start/submit, module. **RED→GREEN:** LK1/LK2/LK3/LK5 int + pure-fn 6 + FSM-guard 3 + service unit 7. Verify XANH (typecheck · unit 70 · int lock 4/approve 5/apply 6 · e2e 17). **FULL+santa gate:** security CLEAN · database (N+1→batch) · silent-failure (observability +log) · santa dual-review **NICE/NICE**. |
| **4b-FE read** | `0379e2a` | **GET `/workflow/steps/:stepId/checklist`** (BE read-path còn thiếu cho FE 4b) → `{stepId, items:[{id,label,isRequired,checked}]}`. Company-scoped read (no permission gate, workflow-internal), CÙNG linkage ĐƯỜNG A như gate (node_key→def-step→checklists→items, LEFT JOIN tick-states). KHÔNG migration (bảng/RLS từ 0034). Contract additive (`stepChecklistSchema`); frozen 1b không đụng. **Residual: chưa có api test riêng** (int cần Docker/DB → +FULL gate sau). |
| **C/FE** | `0e9b9a6`→`da352f8` | **Track C hội tụ (ff).** FE workflow builder (templates/DAG canvas/instance view, nối API thật) + **checklist UI 4b FE** (`da352f8`): render required, tick/untick API thật, **chặn submit tới khi đủ required** (mirror gate BE), loading/error/empty + a11y. web test **98** (+14). |

**Test:** toàn bộ xanh — workflow unit **116** (fsm 27+FS5 · approval 19 · dag/adapter/lock/assign) · full int-suite **284** (+2 skip; workflow-approve **7** = FS2/FS3/FS7/FS6/FS10 + **EV1/EV2**) · e2e linear **17** · typecheck sạch.

---

## ✅ 3c-iii + ⑥ gate ĐÃ XONG — crown-jewel 3c KHÉP (đã push)

> 3c-i + 3c-ii + 3c-iii + ⑥ FULL+santa gate **XONG**. Crown-jewel 3c hoàn tất — code + gate + push (`62b5374`).

- **① RED FS10 → ② GREEN lock → ③ W2 cleanup → ④ FS5** — xong, commit `af8f583` (5 file, +149/−51), **local, CHƯA push**.
- **Lock:** `approve()` chèn `lockInstanceForUpdateInTx` (SELECT…FOR UPDATE per-instance) NGAY sau `findInstanceByIdInTx`, TRƯỚC mọi read dep. **approve-only** (start/submit không cần — chỉ ghi step của chính nó; giữ 1 row instance/tx → không deadlock).
- **FS10 (quyết định quan trọng):** `Promise.all` timing tự nhiên **KHÔNG đỏ tin cậy** (phụ thuộc thứ-tự-test/độ-ấm-pool — từng thấy xanh-trong-trạng-thái-lỗi). Đã thay bằng **probe lock xác định**: blocker (`directPool`) giữ `SELECT…FOR UPDATE` trên instance → real `approve()` PHẢI block (chưa-lock=không-block=ĐỎ; có-lock=block→release=XANH). Đúng-tuần-tự open/complete đã có FS3/FS7.
- **W2:** xoá `findMaxStepOrder`+`advanceInstanceStepOrder`+`updateInstanceStepOrder` (cả 3 dead) + bỏ `max` import; `current_step_order` chỉ advisory. Spec dọn mock + 2 assertion legacy.
- **FS5:** đặt ở **FSM unit** — `validateConsumerTransition` là writer DUY NHẤT của approved/revision (cặp với **D6** đã có; D6 chứng minh service path KHÔNG ghi được approved/revision).
- **⑤ FS11: SKIP** — e2e linear 17 đã che vòng start/submit/approve (seed `video_standard_v0` không seed deps → mọi bước root, guard `allDependenciesApproved` mới vẫn xanh).
- **KHÔNG migration** trong 3c. ~~Migration kế = 0035 (4a eval hook)~~ → **0035 XONG** (`0d8535b`, idx 36, `when 1717500043000`, tag `0035_g7_eval_hook_lock_uq`). Migration kế = **0036** (4c permissions seed — PHẢI seed hyphen: `create/update/publish/read:workflow-template` + `apply:workflow-instance`).
- **4a + 4b + 4c XONG → LUỒNG A spine HOÀN TẤT.** Viên kế = gate tổng + PR (§8) HOẶC nợ residual (xem cuối file).
  - **4b chốt:** linkage ĐƯỜNG A (node_key→def-step→checklists→required items; `default_checklist_id` chết NULL). Gate submit-only ở FSM guard 7 (fail-closed). Tick/untick assignee-gated + cross-item scope + `FOR UPDATE` step-row đóng race untick-vs-submit. KHÔNG migration. Gate FULL + santa PASS/PASS (`187bb3c`).
  - **4c chốt:** 0036 catalog HYPHEN + grant admin (QĐ #A=A2; catalog/grant tách, regression 0). Eval hook emit-only `step.evaluation_required` cùng tx approve (QĐ #B=no-consumer; worker 0-consumer=done). Eval cols 0035 nay đã DÙNG. Migration `119a94a` + eval hook `e539938`.

### ✅ ⑥ FULL+santa gate XONG — fix `62b5374`

- **Scope:** review CẢ 3c-i (`0d31be6`) + 3c-ii (`c26eb94`) + 3c-iii (`af8f583`).
- **Agent:** `ecc:security-reviewer` + `ecc:database-reviewer` + `ecc:silent-failure-hunter` (song song) → `ecc:santa-method` dual-review **NICE (cả 2 reviewer PASS)**. KHÔNG CRITICAL/HIGH net-new; lock/DAG/FSM/idempotency đúng.
- **Đã vá (`62b5374` — chỉ `approval.service.ts` + spec, theo PATH, attribution OFF):**
  - **F1 (HIGH):** `approve()` + `requestRevision()` map `approval_steps_request_level_uq` **23505 → ConflictException(409)** thay raw 500 khi 2 approver đua cùng request. Toàn vẹn vốn AN TOÀN (uq + atomic rollback — santa chứng minh `createApprovalStep` là write ĐẦU TIÊN nên rollback trọn tx); **mapping-only ĐỦ, KHÔNG cần re-read-under-lock** (YAGNI).
  - **F2 (MED, pre-existing):** `requestRevision()` đọc task qua `findActiveTaskByStepIdInTx(...tx)` thay non-tx `findTaskByStepId` → đóng TOCTOU `nextRevisionRound`.
  - **F3 (LOW):** fan-out `createTask` no-op nay `logger.warn` (parity `applyTemplate`/`startWorkflow`).
- **Verify XANH:** typecheck · fsm 27 · approval 19 · int 5 (FS10 deterministic 3×: 862/855/868ms) · e2e 17.
- **Gate points ĐẠT:** (a) lock TRƯỚC mọi DAG-read ✓; (b) `.for("update")` qua `tx`+`company_id`, RLS còn ép ✓; (c) approve-only KHÔNG tạo race khác (`revision` không bao giờ = `approved`, gating monotonic, X4 chặn approved→revision) ✓; (d) FS10 xanh-xác-định 3× ✓.

### Nợ ghi nhận sau ⑥ gate (KHÔNG chặn — santa nêu)

- **Multi-level approvals (forward-looking):** chốt 23505 dựa trên `level=1`/`maxLevel=1`. Duyệt đa-cấp tương lai → 2 approver KHÁC cấp sẽ KHÔNG bị `approval_steps_request_level_uq` chặn; revisit khi làm. MVP-0 single-level → hiện an toàn.
- **Contract `c26eb94`:** outbox `step.approved` bỏ `nextStepOrder`, thêm `newlyOpenedStepIds` — xác nhận consumer notify đã migrate sang field mới.

## Bất biến vận hành (giữ cả G7)
- HAND-DRIVEN: đọc + trình plan từng viên, duyệt rồi mới code. Commit theo PATH tường minh (KHÔNG `git add -A`). Attribution tắt.
- CLAUDE §2: `withTenant(companyId)` mọi query; bảng mới RLS+FORCE+policy + thêm `rls-registry.ts`; không hard-delete quan trọng; không secret plaintext.
- Per-migration gate (khi có migration): set env TAY → migrate → tenant-isolation + rls-guards xanh → commit. Chạy test: vitest.config đã inject `DATABASE_URL`/`DATABASE_DIRECT_URL`.
- FULL gate (security+database+silent-failure+santa) cho logic crown-jewel (FSM/lock/DAG/permission/audit).

## Nợ kỹ thuật nổi bật (chi tiết §10)
- ✅ **0036 ĐÃ seed** (`119a94a`): catalog HYPHEN `create/update/publish/read:workflow-template` + `apply:workflow-instance` + grant company-admin → endpoint hết 403 cho admin.
- **Residual từ 4c (chuyển G8/G9):**
  - **`evaluationTemplateId` nullable khi `requires_evaluation=true`** — chưa có CHECK; payload `step.evaluation_required` có thể mang `null`. G8 consumer phải xử lý hoặc thêm CHECK (migration mới).
  - **AUTHORING `requires_evaluation`/`evaluation_template_id`** chưa có — hiện chỉ set qua SQL trực tiếp (test fixture); DTO `addStep/updateStep` + FE = slice sau.
  - **PD1 runtime HTTP-deny** chưa thêm (chỉ PD2/PD3 metadata-static tồn tại; cần dựng e2e auth fixture).
  - **Grant project-manager + role khác** cho workflow-template hoãn sang **grant-catalog runtime (G9)**; nợ `permission.changed` cache-invalidation vẫn còn cho endpoint grant runtime (G3-4).
  - `manage:workflow-instance` KHÔNG seed (YAGNI — không route dùng; thêm khi có route manage).
- **tasks thiếu `project_id`**: project-target workflow tạo task không anchor project → cần migration + query (3c/G8). G7 thực tế đi content_item.
- `id` param chưa validate UUID (toàn controller workflow-templates) — defer fix đồng bộ.
- `defaultChecklistId` clone hoãn (luôn NULL trước 3b; remap khi 3b/3c set).
