```yaml
wo: S2-FND-DOC-1
zone: green
generated_by: auto-loop
reconciled_at: "48382cd"
lanes: [{"id":"dbdocs","task":"DB-domain: pin lệch-có-chủ-đích + SỬA doc-drift NỘI BỘ trong bộ DB Foundation. DB-08: (a) is_paid → is_paid_holiday (dòng ~1333, đã ép mig 0434); (b) audit_logs Option-A — company_id NOT NULL (mạnh hơn doc) + giữ cột legacy object_type/object_id/before/after/ip + CHECK union cho writer v1 (đã pin comment 0438); (c) data_retention_policies: audit_logs + file_access_logs thuộc PROTECTED_TABLES → không bao giờ purge (an toàn hơn doc), archive = future; (d) OWNER-DECISION #1 companies §8.1: PIN code-thắng ở N=1 single-tenant (company_code=slug nullable, status enum active/suspended, thiếu legal_name/country_code/... + còn DELETE grant + cột legacy) — reconcile khi mở multi-company (đề xuất PIN, cần owner chốt); (e) OWNER-DECISION #2 modules sort_order (dòng ~557): code dùng 1-15 liền vs doc khoảng 10-70 — cosmetic, PIN code; (f) xác nhận seed_batches status enum đã là 'Success' khớp code (không sửa). DB-09 SỬA doc-drift khớp DB-08 (đây là doc-fix, KHÔNG phải code-thắng): checksum→checksum_sha256/content_hash (dòng 554-555), file_access_logs.accessed_at→created_at (dòng 599/602/605/611/2219), uq_public_holidays theo name→holiday_code (dòng 662-663), ghi chú index audit thiếu company_id-first (kế thừa 0438). DB-10: seed_batches status 'Applied'→'Success' khớp DB-08+code (dòng ~581); pin seed company-scoped chuyển RUNTIME seeder (MasterDataSeedRunner) thay migration (0445:14-18). MỖI điểm = 1 dòng 'CHỐT 2026-07-02: code thắng/doc sửa, lý do' tại chỗ, KHÔNG viết lại cả doc.","builder":"db-migration","paths":["docs/DB/DB-08 Audit Files Settings Seeds Database Design.md","docs/DB/DB-09 Database Index Query Pattern Performance Design.md","docs/DB/DB-10_Migration_Plan_Initial_Seed_Data_Database_Design.md"]},{"id":"apidocs","task":"API-domain: pin lệch-có-chủ-đích hợp đồng Foundation. API-09: (a) mô hình quyền = tuple (action, resource_type) namespace foundation-* khớp seed 0435, THAY notation MODULE.RESOURCE.ACTION (banner đầu doc + chú tại bảng endpoint); (b) single-tenant v2 CẮT nhóm multi-company: GET /companies (dòng 544), POST /companies (593), suspend (725), activate (726) — out-of-scope tới khi mở multi-company; (c) 7 internal REST /internal/v1/foundation/* (dòng 147/153-157, 1241/1424/1832/2085/2494/2622/2667/2715) → thay bằng in-process service call (modular monolith); NGOẠI LỆ ghi rõ retention/cleanup-jobs/run + seeds/run là THIẾU THẬT (chưa có trigger) — gap không phải pin; (d) download = 302 presigned TTL-ngắn thay stream (không lộ storage_path); (e) my-apps = Authenticated-only tự lọc capability (không gate bằng quyền foundation); (f) path/method lệch nhỏ đã chấp nhận: preview GET theo :id (không POST), check→check-working-day, seed-batches→seeds, file-links nested. API-10 (PERMISSION MATRIX + PERMISSION AUDIT REPORT): pin ma trận tuple + cờ sensitive khớp seed 0435 là chuẩn; GHI CHÚ 7 cặp seed-orphan + pair-drift audit-log H4 (view:audit-log vs view:foundation-audit-log) là gap ĐÃ BIẾT xử lý ở WO FND-AUDIT-PAIR-PIN, KHÔNG sửa ở WO này; drift read/update:company (0005) chỉ còn catalog-noise. Mỗi điểm = 1 dòng CHỐT 2026-07-02 tại chỗ.","builder":"backend-builder","paths":["docs/API Design/API-09_FOUNDATION_API_Design.md","docs/API Design/API-10 PERMISSION MATRIX.md","docs/API Design/API-10 PERMISSION AUDIT REPORT.md"]},{"id":"bedocs","task":"BE-domain: pin lệch-có-chủ-đích triển khai Foundation. BACKEND-11: (a) §11.9 File download flow (dòng 646-649) + dòng 389-390/518/1401/1686 — download = 302 presigned TTL-ngắn (đạt mục tiêu không lộ storage_path), KHÔNG backend stream; (b) notation quyền FOUNDATION.FILE.DOWNLOAD (dòng 389-390) → tuple (download, foundation-file) khớp seed 0435; (c) retention: audit_logs + file_access_logs thuộc PROTECTED_TABLES → không purge (an toàn hơn doc), archive path = future. BACKEND-04: (d) pin seed company-scoped chuyển RUNTIME seeder (MasterDataSeedRunner idempotent theo batch/item + SHA-256) thay migration; (e) GHI CHÚ GET /settings/public gate chặt hơn doc (đòi view:foundation-setting vs doc Authenticated — H6) là lệch ĐÃ BIẾT, quyết định hoãn sang WO FND-SETTINGS-PUBLIC-GATE, KHÔNG đổi gate ở WO này. Mỗi điểm = 1 dòng CHỐT 2026-07-02 tại chỗ, KHÔNG viết lại cả doc.","builder":"backend-builder","paths":["docs/BACKEND/BACKEND-11_File_Audit_Settings_System_Jobs.md","docs/BACKEND/BACKEND-04_Foundation_Backend.md"]},{"id":"reviewsync","task":"HỢP NHẤT (chạy SAU dbdocs/apidocs/bedocs): cập nhật docs/_review/FOUNDATION-SYSTEM-AUDIT-2026-07-02.md — đánh dấu §8 hành động #20 'Pin vào spec' là DONE (CHỐT 2026-07-02) và ghi CHÉO doc nào nhận CHỐT nào; mục 2 (companies §8.1 + module sort_order) ghi rõ 2 OWNER-DECISION đã chốt (companies=PIN code-thắng N=1; sort_order=PIN code) kèm lý do; §3.3 doc-drift DB-08↔DB-09 đánh dấu ĐÃ SỬA; §5.3 + §6.1/6.3 các lệch-có-chủ-đích (tuple, single-tenant cắt multi-company + internal REST, download 302, PROTECTED_TABLES, path/method lệch nhỏ, my-apps Authenticated-only) đánh dấu PINNED. GHI RÕ các mục CÒN MỞ KHÔNG đóng bởi WO này: H1/H2/H3/H4/H5/H7/H8 + DELETE-grant companies/users + settings/public gate — là WO hành vi riêng, chỉ #20 hoàn tất.","builder":"backend-builder","paths":["docs/_review/FOUNDATION-SYSTEM-AUDIT-2026-07-02.md"]}]
acceptanceChecks: ["git diff --name-only origin/master...HEAD CHỈ chứa file dưới docs/DB, docs/API Design, docs/BACKEND, docs/_review; 0 file dưới apps/**, packages/**, apps/api/migrations/** (done_when: KHÔNG đổi hành vi code, không migration).","DB-09 sau sửa: grep 0 tham chiếu file_access_logs.accessed_at (đã →created_at, dòng 599/602/605/611/2219); 0 index files (company_id, checksum) (đã →checksum_sha256/content_hash, 554-555); uq_public_holidays dùng holiday_code không phải name (662-663) — mỗi sửa kèm 1 dòng CHỐT 2026-07-02 nêu doc-fix.","DB-08: is_paid_holiday pinned (~1333); audit_logs Option-A có dòng CHỐT (company_id NOT NULL + cột legacy + CHECK union); retention PROTECTED_TABLES có CHỐT; companies §8.1 có CHỐT ghi quyết-định PIN code-thắng N=1; modules sort_order có CHỐT PIN code.","DB-10: seed_batches 'Applied'→'Success' đã sửa (~581) khớp DB-08+code; có CHỐT pin seed company-scoped qua RUNTIME seeder (0445:14-18).","API-09: có CHỐT cho — tuple (action,resource_type) foundation-*; cắt multi-company tại GET/POST /companies + suspend + activate (544/593/725/726); internal REST→in-process (kèm ghi chú cleanup-jobs/run + seeds/run là gap thật); download 302; my-apps Authenticated-only; path/method lệch nhỏ (preview GET/check-working-day/seeds/file-links nested).","API-10 (matrix + audit report): ma trận tuple + cờ sensitive pinned authoritative; 7 seed-orphan + pair-drift audit-log H4 ghi là gap ĐÃ BIẾT (không sửa ở WO này).","BACKEND-11: §11.9 có CHỐT download=302 presigned TTL-ngắn (không stream); notation FOUNDATION.FILE.DOWNLOAD ghi map→tuple (download,foundation-file); PROTECTED_TABLES audit/file_access_logs có CHỐT.","BACKEND-04: có CHỐT pin runtime-seeder; settings/public gate ghi là lệch-đã-biết hoãn (không đổi gate).","Mỗi điểm pin = ĐÚNG 1 dòng CHỐT 2026-07-02 (append/inline), KHÔNG rewrite cả doc — kiểm bằng git diff: các hunk là dòng nhỏ additive, không xóa/ghi lại section lớn.","docs/_review/FOUNDATION-SYSTEM-AUDIT-2026-07-02.md: §8 #20 đánh dấu DONE; 2 OWNER-DECISION (companies + sort_order) ghi kèm lý do; §3.3/§5.3/§6 các lệch tương ứng đánh dấu PINNED; các HIGH còn mở (H1/H2/H3/H4/H5/H7/H8 + DELETE-grant + settings/public) ghi rõ KHÔNG đóng bởi WO này.","DoD §8: docs cập nhật, không migration, không đổi luồng chính; harness/backlog.mjs WO status do loop xử lý (done-on-commit)."]
testTasks: ["No-code-change deny guard (RED-equivalent cho docs WO): assert `git diff --name-only origin/master...HEAD` KHÔNG khớp apps/**, packages/**, apps/api/src/**, apps/api/migrations/** — nếu khớp thì FAIL (WO cấm đổi hành vi code).","Doc-consistency grep suite: sau sửa DB-09 → 0 hit `accessed_at` trong ngữ cảnh index file_access_logs; 0 hit `files (company_id, checksum)`; uq public_holidays không còn dùng cột `name`. DB-10 → seed_batches status dùng 'Success', 0 hit 'Applied'.","Cross-doc consistency: seed_batches enum ('Success') nhất quán DB-08 ↔ DB-10 ↔ code seed; audit_logs Option-A (company_id NOT NULL) nhất quán DB-08 ↔ BACKEND references.","Pin-completeness checklist: đếm dòng 'CHỐT 2026-07-02' theo từng file ≥ số điểm pin liệt kê trong done_when cho file đó (DB-08 ≥5, DB-09 ≥3, DB-10 ≥2, API-09 ≥6, BACKEND-11 ≥3, BACKEND-04 ≥1, _review ≥1).","Owner-decision surfacing: PR body/notes nêu rõ 2 quyết định (companies PIN N=1; sort_order PIN code) để owner chốt lần cuối trước merge.","KHÔNG có integration/deny-path runtime test (WO docs-only, không thay đổi permission/workflow code) — Đội 3 verify bằng grep-consistency + git-scope guard ở trên, không cần LANE_DB/Postgres."]
steps: ["OWNER-GATE trước: xác nhận 2 quyết định treo — (1) companies PIN code-thắng ở N=1 single-tenant (reconcile khi mở multi-company); (2) module sort_order PIN code (cosmetic). Đề xuất mặc định = PIN cả hai; builder dbdocs viết CHỐT theo đề xuất và flag trong PR để owner chốt lần cuối.","SONG SONG (3 lane, file rời rạc, không migration nối tiếp): dbdocs pin DB-08/09/10 + sửa doc-drift DB-09; apidocs pin API-09/API-10; bedocs pin BACKEND-11/BACKEND-04.","Mỗi lane chỉ APPEND/inline dòng 'CHỐT 2026-07-02: ...' tại đúng vị trí — KHÔNG rewrite section, KHÔNG đổi cấu trúc doc, KHÔNG chạm code/schema/migration.","SAU cùng: reviewsync đồng bộ báo cáo audit _review theo đúng những gì 3 lane đã pin (depends_on dbdocs+apidocs+bedocs).","Verify no-code-change: git diff --name-only chỉ nằm dưới docs/DB, docs/API Design, docs/BACKEND, docs/_review; ĐỎ nếu có apps/**, packages/**, apps/api/migrations/**.","Gate LIGHT (doc/quality-gate) + 1 lượt người đọc kỹ tính CHÍNH XÁC của pin audit_logs Option-A + companies + tuple permission (pin phải khớp code thật, không phát biểu sai hành vi)."]
```

GAP-ANALYSIS: WO docs-only (green) hiện thực hoá §8 hành động #20 của báo cáo audit FOUNDATION-SYSTEM-AUDIT-2026-07-02 — 'pin lệch-có-chủ-đích để chặn audit sau báo lệch giả'. Đầu vào đã có sẵn phân tích đầy đủ trong báo cáo; việc còn lại là GHI pin đúng chỗ + sửa doc-drift NỘI BỘ. Phân biệt 2 loại thao tác: (1) CODE-THẮNG (giữ code, thêm dòng CHỐT giải thích): is_paid_holiday, audit_logs Option-A, PROTECTED_TABLES, tuple permission, single-tenant cắt multi-company, internal REST→in-process, download 302, my-apps Authenticated-only, path/method lệch nhỏ, seed runtime, companies N=1, sort_order 1-15; (2) DOC-SỬA (doc-drift nội bộ DB-08↔DB-09, code đã đúng theo DB-08): accessed_at→created_at, checksum→checksum_sha256/content_hash, uq holiday name→holiday_code, seed_batches Applied→Success (DB-10). Anchor đã xác minh: DB-08 is_paid dòng 1333, seed_batches enum đã 'Success' 1489/1503; DB-09 checksum 554-555, accessed_at 599/602/605/611/2219, uq holiday name 662-663; DB-10 'Applied' dòng 581; API-09 multi-company 544/593/725/726, internal REST 147/153-157; BACKEND-11 download-stream 389-390/649/1401/1686, notation FOUNDATION.FILE.DOWNLOAD 389-390.

CẮT LANE (ranh giới FILE, không chồng paths): dbdocs=3 file docs/DB; apidocs=3 file docs/API Design; bedocs=2 file docs/BACKEND; reviewsync=1 file docs/_review. Không có lane docs/FRONTEND: done_when KHÔNG yêu cầu pin FE (defaultRoute H8 là WO code FE-SYSTEM-FIXES, sort_order là DB/seed domain) — docs/FRONTEND/** chỉ là scope cho phép, không phải việc phải làm → tránh scope-creep. Builder: DB docs = db-migration (chuyên môn schema/index/RLS để pin chính xác); API/BE docs + review = backend-builder; không frontend-builder.

MIGRATION LANE: KHÔNG có — WO docs-only, không đổi DDL/DML, head mig giữ 0465. (Ràng buộc 'migration nối tiếp' không áp dụng.)

CROWN/ZONE/GATE: Green, KHÔNG crown-jewel (không chạm code permission/RLS/secret/audit/auth; chỉ mô tả tài liệu). Gate = LIGHT (doc/quality-gate). NHƯNG nội dung pin đụng chủ đề permission/audit → yêu cầu 1 lượt người đọc kỹ tính chính xác (pin phải khớp hành vi code thật; sai pin còn nguy hiểm hơn không pin vì đóng băng hiểu nhầm). 2 OWNER-DECISION (companies N=1; sort_order) cần owner chốt: đề xuất PIN cả hai; builder viết CHỐT theo đề xuất + flag PR cho owner xác nhận (không tự quyết red-zone kiến trúc companies-reconcile).

HOT-FILE & MERGE: docs/_review/FOUNDATION-SYSTEM-AUDIT-2026-07-02.md là điểm cắt nhau tiềm năng nếu mọi lane cùng ghi → GIẢI QUYẾT bằng cách CHỈ lane reviewsync sở hữu _review, chạy SAU 3 lane pin (depends_on dbdocs+apidocs+bedocs) để phản ánh đúng pin thực tế → 0 xung đột 3-chiều trên audit report. Trùng nội dung 'runtime seeder' xuất hiện ở cả DB-10 (dbdocs) và BACKEND-04 (bedocs) nhưng KHÁC file (mô tả kế hoạch seed vs mô tả triển khai) → không phải hot-file, chỉ cần đồng bộ câu chữ. Trùng 'PROTECTED_TABLES' ở DB-08 (dbdocs) và BACKEND-11 (bedocs) — khác file, hợp lệ. Quy ước: chỉ APPEND/inline 1 dòng 'CHỐT <ngày>: ...', KHÔNG rewrite section (giảm nhiễu review + tránh trôi nội dung gốc).

INVARIANTS (CLAUDE.md §2, luôn áp dụng dù docs-only): WO này KHÔNG được đổi hành vi ép company_id/RLS, append-only, secret — chỉ MÔ TẢ. Kiểm bằng deny guard git-scope (không có file code trong diff). Pin audit_logs Option-A phải giữ đúng tinh thần bất biến #2 (append-only): ghi rõ company_id NOT NULL + CHECK union là ràng buộc MẠNH HƠN doc, không nới lỏng.

VERIFY (Đội 3, không cần Postgres/LANE_DB vì không có runtime): git-scope deny guard + grep-consistency (DB-09 hết cột ma, DB-10 dùng Success) + cross-doc consistency (seed_batches enum, audit Option-A) + pin-completeness count 'CHỐT 2026-07-02' theo file + owner-decision surfacing trong PR.

OUT-OF-SCOPE (KHÔNG làm ở WO này — là WO hành vi riêng, chỉ GHI CHÚ trong _review là còn mở): H1 file-policy resolver rỗng (FND-FILE-POLICY), H2 download bỏ scan_status (FND-FILE-DOWNLOAD-GUARD), H3 upload E2E (FND-UPLOAD-E2E), H4 audit pair-drift (FND-AUDIT-PAIR-PIN), H5 holiday audit (FND-HOLIDAY-AUDIT), H7 seed sequence counters (FND-SEED-SEQUENCES), H8 FE defaultRoute (FE-SYSTEM-FIXES), DELETE-grant companies/users (DB-COMPANIES-GRANT), settings/public gate (FND-SETTINGS-PUBLIC-GATE), Swagger/OpenAPI + FOUNDATION-ERR-* + pagination convention (P2). WO này CHỈ hoàn tất mục #20 'Pin vào spec'.
