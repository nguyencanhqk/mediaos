```yaml
wo: S2-FND-CONTRACT-1
zone: yellow
generated_by: owner-manual (sau 4 vòng auto-loop plan-block — nội dung đã hội tụ, chỉ thiếu vật chất hoá trường)
reconciled_at: "0a0b4ed"
lanes: [{"id":"contract-1","task":"API contract hygiene: (a) reconcile FOUNDATION_FILE_ERROR_CODES (xoá bản cục bộ apps/api/src/common/errors/error-codes.ts, re-export GIỮ NGUYÊN TÊN SYMBOL từ @mediaos/contracts packages/contracts/src/files.ts để files.service.ts — ngoài scope lane — resolve KHÔNG cần sửa); (b) wire payload.code=FOUNDATION-ERR-* ADDITIVE vào packages/contracts cho ĐÚNG 8 file trong scope (settings 7 throw, company.service+company-status 4 throw, audit.service 2 throw, module-catalog+module-toggle 3 throw, holidays.service 5 throw, retention 3 throw ≈ 24 throw — files.service.ts/sequence.service.ts NGOÀI scope, giữ code cũ, ghi rõ deferred), MỖI throw chuyển từ `new XException('msg')` sang `new XException({code, message: 'msg'})` GIỮ NGUYÊN message gốc (không mất context cho client) — TRỪ case validation_schema 422 (setting assertSchema) GIỮ NGUYÊN mã VALIDATION-ERR-* (không đổi, vì packages/web-core/src/lib/api-error-kind.ts mapStatusToErrorKind có prefix-match code.startsWith('VALIDATION-ERR') để style riêng lỗi validation); (c) ranh giới 403: PermissionGuard giữ nguyên AUTH-ERR-FORBIDDEN (guard-level, KHÔNG sửa), chỉ ForbiddenException do service tự ném (business rule vd module-core-lock) mới mang FOUNDATION-ERR-FORBIDDEN; (d) AllExceptionsFilter GIỮ NGUYÊN không sửa; (e) SwaggerModule mount /docs — thêm dep @nestjs/swagger + patchNestjsSwagger(), env-gate tắt prod (404/401), openapi.json sinh bằng script + gitignore, test positive-control (DTO đã biết xuất hiện) RỒI mới assert denylist field nhạy cảm (nếu response schema rỗng thì đổi allowlist); (f) migrate DTO settings/holidays/company-patch vào packages/contracts (dual-build, giữ shape, gỡ ghi chú nợ contracts/src/foundation/index.ts); (g) pagination + validation-status: KHÔNG đổi code, chỉ sửa doc BACKEND-12 §15.1 phản ánh đúng quy ước hiện có (page+pageSize entity-list VÀ limit+offset audit/log song song; 422 cho validation_schema). Toàn bộ 1 lane MỨC NHẠY CẢM CAO NHẤT (Opus + FULL security-reviewer) vì chạm settings/audit/retention (secret-adjacent + append-only) — không tách lane cơ học.","builder":"backend-builder","paths":["apps/api/src/main.ts","apps/api/src/common/errors/**","apps/api/src/foundation/settings/**","apps/api/src/foundation/audit/**","apps/api/src/foundation/retention/**","apps/api/src/foundation/holidays/**","apps/api/src/foundation/company/**","apps/api/src/foundation/module-catalog/**","apps/api/package.json","packages/contracts/src/**","docs/BACKEND/**","apps/api/test/**"]}]
acceptanceChecks: ["FOUNDATION_FILE_ERROR_CODES: bản cục bộ error-codes.ts xoá, re-export TỪ @mediaos/contracts GIỮ Y NGUYÊN tên symbol export — files.service.ts (ngoài scope) import không cần sửa, typecheck xanh, giá trị chuỗi DUP_LINK/DUP_PRIMARY bất biến.","FOUNDATION-ERR-* ADDITIVE trong packages/contracts, KHÔNG bản riêng ở apps/api. Đúng 8 file / ~24 throw trong scope pin ở reconcileNotes (settings/company/audit/module-catalog/holidays/retention) — files.service.ts + sequence.service.ts NGOÀI scope, giữ nguyên code cũ, ghi rõ DEFERRED (không phải bug, cố ý ngoài phạm vi WO).","Mỗi throw chuyển đổi GIỮ NGUYÊN message gốc bên cạnh code mới (payload {code,message}) — client không mất ngữ cảnh lỗi.","validation_schema 422 GIỮ mã VALIDATION-ERR-* (không đổi FOUNDATION-ERR-*) để KHÔNG phá prefix-match code.startsWith('VALIDATION-ERR') ở web-core mapStatusToErrorKind.","Ranh giới 403: PermissionGuard giữ AUTH-ERR-FORBIDDEN nguyên vẹn (không sửa file guard); chỉ ForbiddenException service-level mới đổi mã.","AllExceptionsFilter 0 diff.","SwaggerModule mount /docs qua @nestjs/swagger + patchNestjsSwagger (dep mới thêm vào package.json); env-gate: 404/401 khi NODE_ENV=production, mount OK dev/staging; openapi.json sinh bằng script + .gitignore (không commit).","DTO settings/holidays/company-patch chuyển vào packages/contracts (dual-build ESM/CJS), apps/api import lại, KHÔNG đổi shape; gỡ ghi chú nợ contracts/src/foundation/index.ts.","BACKEND-12 §15.1 sửa để phản ánh ĐÚNG 2 quy ước pagination song song hiện có (page+pageSize / limit+offset) — zero code change cho pagination.","typecheck + full suite foundation xanh; FULL gate (Opus + security-reviewer) PASS; regression: 0 test xanh cũ (theo baseline reconcileNotes) chuyển đỏ ngoài ý muốn."]
testTasks: ["RED: body lỗi 4xx từ setting-update sai type/schema KHÔNG chứa raw settingValue/secret_ref — chỉ code + message vô hại.","RED positive-control-rồi-denylist: openapi.json chứa ít nhất 1 DTO đã @ApiResponse annotate với field mong đợi xuất hiện, RỒI assert VẮNG MẶT field nhạy cảm (salary/salaryType/PII/password/hash/secretRef/secret_ref/raw settingValue/validationSchema) — nếu response schema rỗng thì đổi sang allowlist schema thay vì denylist-trên-rỗng.","RED Swagger env-gate: GET /docs trả 404/401 khi NODE_ENV=production; mount thành công (200 + JSON hợp lệ) ở dev/staging.","RED ranh giới 403: gọi API thiếu permission → AUTH-ERR-FORBIDDEN (PermissionGuard, không đổi); gọi hành động bị business-rule chặn (vd toggle module MVP core) → FOUNDATION-ERR-FORBIDDEN (service-level).","RED regression: chạy full-suite unit+int-spec foundation SAU khi đổi code, đối chiếu baseline (liệt kê trong reconcileNotes) — 0 test xanh cũ chuyển đỏ ngoài ý muốn.","RED schema FE (packages/contracts): DTO settings/holidays/company-patch xuất sang bundle FE KHÔNG chứa secretRef/secret_ref/raw settingValue/validationSchema server-only.","RED FOUNDATION_FILE_ERROR_CODES: sau khi xoá bản cục bộ + re-export từ contracts, (a) typecheck xanh cho files.service.ts (ngoài scope, không sửa) (b) giá trị chuỗi DUP_LINK/DUP_PRIMARY bất biến.","RED message-preservation: ít nhất 1 throw 404 và 1 throw 409 đã chuyển đổi vẫn trả message gốc (không bị thay bằng default class-name message) bên cạnh code mới."]
steps: ["Bước 1 — BASELINE (bắt buộc trước code): grep apps/api/test/**, apps/console/src/**, apps/app/src/**, packages/web-core/src/** tìm chỗ assert error.code cũ (VALIDATION-ERR-001/RESOURCE-ERR-NOT-FOUND/RESOURCE-ERR-CONFLICT/AUTH-ERR-FORBIDDEN) của 8 file trong scope; ghi danh sách + liệt kê mapping throw→FOUNDATION-ERR-code cho từng throw vào reconcileNotes. Đã xác nhận trước (2026-07-05): FE KHÔNG match cứng chuỗi mã cho màn hình foundation (chỉ dùng HTTP status) — an toàn; NGOẠI LỆ case 422 (giữ VALIDATION-ERR-*, xem acceptance).","Bước 2 — Reconcile FOUNDATION_FILE_ERROR_CODES: xoá const cục bộ trong apps/api/src/common/errors/error-codes.ts, thay bằng `export { FOUNDATION_FILE_ERROR_CODES } from '@mediaos/contracts'` (giữ tên export y hệt); verify files.service.ts (ngoài scope) vẫn resolve, typecheck xanh.","Bước 3 — Wire payload.code: sửa ~24 throw ở 8 file trong scope (settings.service.ts, company.service.ts + company-status, audit.service.ts, module-catalog/**, holidays.service.ts, retention/**) — mỗi throw đổi từ message-only sang {code: FOUNDATION-ERR-*, message: <message gốc giữ nguyên>}; TRỪ case validation_schema giữ VALIDATION-ERR-*; files.service.ts + sequence.service.ts NGOÀI scope, KHÔNG đụng.","Bước 4 — Swagger: thêm @nestjs/swagger vào apps/api/package.json; SwaggerModule.setup + patchNestjsSwagger() trong main.ts, env-gate (chỉ mount khi NODE_ENV != production); script sinh openapi.json + thêm vào .gitignore.","Bước 5 — DTO migrate: chuyển Zod schema settings/holidays/company-patch từ apps/api local vào packages/contracts/src/foundation/** (dual-build), apps/api import lại qua @mediaos/contracts, xoá định nghĩa cục bộ trùng lặp, gỡ ghi chú nợ ở contracts/src/foundation/index.ts.","Bước 6 — Verify: chạy FULL gate (Opus + security-reviewer trên toàn lane do nhạy cảm); chạy lại toàn bộ test/int-spec foundation, đối chiếu baseline bước 1 (0 regression ngoài ý muốn); sửa doc BACKEND-12 §15.1 phản ánh đúng 2 quy ước pagination song song + pin 422."]
```

Kế hoạch tổng hợp sau 4 vòng plan-review (2026-07-04→05) — plan-reviewer đã xác nhận toàn bộ nội dung "sound" ở round 4, chỉ chặn vì `testTasks[]`/`steps[]`/`reconcileNotes` bị bỏ trống khi Team 1 tự phân rã. File này viết sẵn đầy đủ để lần chạy kế tiếp chỉ cần RECONCILE-REFRESH (đối chiếu code hiện tại còn khớp không) thay vì tự soạn lại từ đầu.

GAP-ANALYSIS: FOUNDATION-ERR-* codes chưa tồn tại (error-codes.ts chỉ có generic ERROR_CODES). FOUNDATION_FILE_ERROR_CODES trùng tên 2 nơi (error-codes.ts 2 thành viên vs packages/contracts/src/files.ts 15 thành viên — bug drift thật, phải reconcile). @nestjs/swagger chưa là dependency. Swagger/OpenAPI hoàn toàn chưa tồn tại trong main.ts.

INVARIANTS: #1 (n/a — không company_id query mới); #2 audit/retention append-only KHÔNG đổi (chỉ thêm payload.code lên throw sẵn có, không đổi write-path/grant); #3 secret-strip — error.message KHÔNG được nội suy raw settingValue/secret_ref; OpenAPI response schema KHÔNG được lộ field nhạy cảm.

RANH GIỚI 403 (chốt): PermissionGuard = AUTH-ERR-FORBIDDEN luôn (không sửa). ForbiddenException từ foundation SERVICE (business rule) = FOUNDATION-ERR-FORBIDDEN.

OWNER-CHỐT ĐÃ RESOLVE (nghiên cứu 2026-07-05, không cần hỏi lại):
- Pagination: KHÔNG đổi code, 2 quy ước song song (page+pageSize entity-list / limit+offset audit-log) đều là chủ ý — chỉ sửa doc §15.1.
- Validation status: GIỮ 422 (tiền lệ chủ ý setting.service.ts assertSchema, khác 400 cho sai type) — pin doc, không đổi code, và GIỮ mã VALIDATION-ERR-* cho case này (không đổi FOUNDATION-ERR-*) vì FE web-core prefix-match phụ thuộc vào đó.

OUT-OF-SCOPE (chống scope-creep): files.service.ts (23 throw, dùng FOUNDATION_FILE_ERROR_CODES riêng) và sequence.service.ts (2 throw) NGOÀI paths của WO này — giữ nguyên code lỗi hiện tại, KHÔNG đổi, ghi nhận catalog sẽ còn "chưa đồng nhất 100%" tạm thời (deferred, không phải lỗi).

VERIFY/GATE: FULL gate toàn lane (Opus + security-reviewer) vì chạm settings/audit/retention (secret-adjacent). Không có migration → không rủi ro thứ tự RLS. Regression suite foundation chạy trước/sau đối chiếu baseline bước 1.
