```yaml
wo: S3-INT-1
zone: red
generated_by: auto-loop
reconciled_at: "8427004"
lanes: [{"id":"S3-INT-1-L1","task":"LEAVE post-approval revoke/cancel FSM + idempotent balance restore + revert-event emission (crown/red). Extend leave-request/leave-approval: cho phép Approved → Cancelled (self, gated policy.allowCancelAfterApproved + cancelBeforeDays) / Revoked (manager|HR, action REVOKE) với FOR UPDATE + status-guard (double-revoke → 409). Trong CÙNG tx withTenant: hoàn phép ĐÚNG SỐ (Used→REFUND ledger leave_balance_transactions, used_days -= totalDays race-safe, KHÔNG hoàn 2 lần nhờ status-guard), mark leave_request_days.attendance_sync_status='Pending Revert' (reuse mẫu markDaysSyncPendingTx), append leave_request_approvals (REVOKE/CANCEL), audit + outbox emit 'leave.request.revoked' (mới) / reuse 'leave.request.cancelled' BÊN TRONG tx. KHÔNG chạm attendance_records ở đây.","builder":"backend-builder","paths":["apps/api/src/leave/**"]},{"id":"S3-INT-1-L2","task":"ATT AttendanceLeaveSyncService + event consumers + retry endpoint (crown/red). AttendanceLeaveSyncService trong attendance/**: đăng ký EventBus consumer onLeaveApproved (forward sync) + onLeaveCancelled/onLeaveRevoked (revert) — mỗi consumerName DUY NHẤT, handler chạy db.withTenant(companyId) trên APP pool (mediaos_app có UPDATE; KHÔNG dùng workerDb SELECT-only). Forward: map leave_request_days→attendance_records — Full Day → upsert record status='Leave' + attendance_status 'Leave' + work_mode 'Leave' + required_working_minutes=0 + leaveRequestId; Half Day → giảm ~1/2 required; Hourly → giảm theo leaveMinutes; record đã có check-in/out → recalculate late/early/missing GIỮ NGUYÊN dữ liệu chấm công; chống trùng qua uq_attendance_records_employee_date_shift. Set leave_request_days.attendance_sync_status='Synced'+syncedAt+attendanceRecordId khi OK; 'Failed'+attendance_sync_error + log khi lỗi (KHÔNG nuốt). Revert handler: gỡ Leave, khôi phục required từ shift/rule hiệu lực, tính lại late/early/missing nếu có check-in, set day='Reverted'. IDEMPOTENT: guard theo attendance_sync_status + processed_events (replay event KHÔNG tạo record trùng / không revert 2 lần). POST /internal/v1/attendance/recalculate (retry/manual) reprocess Pending/Failed/Pending Revert, bảo vệ bằng internal-guard/admin permission (manage:attendance), DTO zod cục bộ trong attendance/** (KHÔNG public contracts). Wire consumer + service vào attendance.module.ts (khối additive). Giữ + regression cho block check-in/today đã có (findApprovedFullDayLeaveTx).","builder":"backend-builder","paths":["apps/api/src/attendance/**"]}]
acceptanceChecks: ["Forward full-day: đơn nghỉ Approved cả ngày → attendance_records có (hoặc cập nhật) 1 record status/attendance_status='Leave', work_mode='Leave', required_working_minutes=0, leaveRequestId set; leave_request_days.attendance_sync_status='Synced' + attendance_synced_at + attendance_record_id.","Forward half-day: required_working_minutes giảm ~1/2 theo shift; Hourly: required giảm đúng theo leaveMinutes; day sync_status='Synced'.","No data-loss: record đã có check-in/out trước khi sync → sau sync check_in_at/check_out_at GIỮ NGUYÊN, late/early/missing tính lại đúng theo required mới.","No-dup: chạy sync (hoặc replay event) 2 lần cho cùng employee/date/shift → CHỈ 1 attendance_record (uq_attendance_records_employee_date_shift), sync_status vẫn 'Synced' (idempotent).","Sync fail: lỗi khi ghi → leave_request_days.attendance_sync_status='Failed' + attendance_sync_error lưu + log ERROR (không nuốt); POST /internal/v1/attendance/recalculate xử lý lại → 'Synced'.","Check-in/today block: ngày có full-day leave Approved → getToday.onLeave=true, checkIn/checkOut → 409 (đã có S3-ATT-BE-1, còn xanh); half-day/hourly KHÔNG bị chặn.","Revert (cancel/revoke đơn ĐÃ Approved+synced): attendance_records gỡ 'Leave', required_working_minutes khôi phục về shift/rule hiệu lực, late/early/missing tính lại nếu có check-in; day sync_status='Reverted'.","Balance restore ĐÚNG SỐ + idempotent: Used→REFUND đúng totalDays; gọi revoke/replay revert-event 2 lần → CHỈ hoàn 1 lần (status-guard + processed_events); ledger leave_balance_transactions append-only.","Cross-tenant: đơn nghỉ company B KHÔNG tạo/sửa attendance_records company A; handler withTenant(companyId từ event) — proof bằng dữ liệu 2 công ty.","Consumer idempotency: replay outbox 'leave.request.approved' (processed_events) → không tạo record trùng, không đổi số công.","Audit + outbox trong CÙNG tx với mọi mutation (rollback ⇒ không audit/event ma); object_type ∈ CHECK hiện có (attendance_record/leave_request/leave_balance).","Endpoint /internal/v1/attendance/recalculate có guard (internal/admin manage:attendance) — không lộ public; DoD §8: validation input, permission guard, audit hành động quan trọng, coverage ≥80%, cập nhật harness/backlog.mjs."]
testTasks: ["RED deny-path (viết TRƯỚC): full-day leave Approved → checkIn/checkOut trả 409 + getToday.onLeave=true; half-day/hourly KHÔNG bị chặn (int-spec DB cô lập, LANE_DB).","Integration forward sync (src/attendance/*.int-spec.ts): full-day→Leave+required 0; half-day→required ~1/2; hourly→required giảm theo minutes; record có check-in→recalc no-data-loss; assert no-dup (1 record) + sync_status transitions Pending→Synced.","Integration sync-fail + retry: ép lỗi ghi → sync_status='Failed' + attendance_sync_error + log; gọi recalculate → 'Synced' (không nhân đôi record).","Integration revert (src/leave + src/attendance *.int-spec.ts): revoke/cancel đơn Approved+synced → attendance gỡ Leave + khôi phục required + tính lại late/early/missing; balance REFUND đúng số; day='Reverted'.","Idempotency RED: replay 'leave.request.approved' + double-revoke / double revert-event → không tạo record trùng, không hoàn phép 2 lần (processed_events + status-guard).","Cross-tenant deny: seed 2 công ty, đơn nghỉ company B → KHÔNG chạm attendance_records company A (RLS/withTenant).","Unit: mapper leave_request_days→attendance record values (full/half/hourly required minutes) + recalc late/early/missing logic (colocated *.spec.ts để thực chạy).","Bàn giao QA S3-QA-2 (apps/api/test/**): kịch bản end-to-end LEAVE→ATT (Approved full-day→Leave record + check-in block + cancel/revoke recalc+balance restore) + regression Auth/HR/ATT — Đội 3 verify."]
steps: ["VERIFY-DB (không migration): xác nhận 0452/0453 đã cấp đủ — leave_request_days.attendance_sync_status enum {Pending,Synced,Failed,Reverted,Pending Revert} + attendance_sync_error/at; attendance_records.attendance_status có 'Leave', work_mode có 'Leave', leaveRequestId, required_working_minutes; leave_balance_transactions type có REFUND + SYSTEM_RECALCULATE; audit object_types có attendance_record/leave_request/leave_balance; GRANT app = SELECT,INSERT,UPDATE trên leave_request_days + attendance_records. NẾU crown micro-plan quyết thêm worker-grant hoặc object_type mới → CHÈN lane db-migration (nối tiếp, RLS/grant additive) TRƯỚC L2.","L1: mở FSM Approved→Cancelled(self)/Revoked(manager|HR) với FOR UPDATE + status-guard; hoàn phép Used→REFUND (race-safe, idempotent theo status) trong tx; mark day rows 'Pending Revert'; append approval history; audit+outbox emit revert event BÊN TRONG tx.","L2: dựng AttendanceLeaveSyncService + repository đọc leave_request_days / ghi attendance_records; đăng ký 3 consumer (approved forward + cancelled/revoked revert) qua EventBus.register, handler dùng db.withTenant APP pool.","L2 forward: full-day=Leave/required 0; half-day giảm ~1/2; hourly giảm theo leaveMinutes; record có check-in → recalc KHÔNG mất dữ liệu; chống dup theo (employee,date,shift); cập nhật sync_status Synced/Failed(+error+log).","L2 revert: gỡ Leave + khôi phục required từ shift/rule + tính lại late/early/missing nếu có check-in; set day 'Reverted'; idempotent (không revert 2 lần).","L2: POST /internal/v1/attendance/recalculate (retry Pending/Failed/Pending Revert) có bảo vệ quyền/internal-guard; wire module additive.","VERIFY: check-in/check-out/today vẫn chặn full-day approved leave (đã có S3-ATT-BE-1) + thêm regression; half-day/hourly KHÔNG bị chặn.","RED deny-path viết TRƯỚC (Đội 2), GREEN, FULL gate (security+database+silent-failure+santa) + người chốt; coverage ≥80% (module nhạy cảm)."]
```

GAP-ANALYSIS (đối chiếu CODE 2026-07-02):
- ĐÃ CÓ (không làm lại): (1) check-in/check-out/today ĐÃ chặn full-day approved leave qua AttendanceRepository.findApprovedFullDayLeaveTx + rule.blockWhenLeaveApproved (S3-ATT-BE-1) — block key theo leaveRequests.durationType FullDay/MultipleDays, độc lập với attendance_record ⇒ đúng, giữ + regression; (2) approve() ĐÃ gọi approvalRepo.markDaysSyncPendingTx đặt leave_request_days.attendance_sync_status='Pending' (handoff sang WO này) + emit outbox 'leave.request.approved'; (3) cancel() Draft|Pending→Cancelled + releaseReserve đã có (NHƯNG chưa cho Approved).
- THIẾU (làm ở WO này): AttendanceLeaveSyncService + consumer onLeaveApproved (forward) + onLeaveCancelled/onLeaveRevoked (revert) + POST /internal/v1/attendance/recalculate; FSM revoke/cancel-after-approved trong leave (Approved→Cancelled self / Revoked manager|HR) + balance restore Used→REFUND idempotent + emit 'leave.request.revoked'.

MIGRATION: KHÔNG cần lane db-migration ở baseline — 0452/0453 đã cấp đủ cột/enum/grant: leave_request_days.attendance_sync_status {Not Required,Pending,Synced,Failed,Reverted,Pending Revert} + attendance_sync_error/synced_at + attendanceRecordId; attendance_records.attendance_status có 'Leave', work_mode 'Leave', leaveRequestId, required_working_minutes; leave_balance_transactions.type có REFUND + SYSTEM_RECALCULATE; audit object_types CHECK có attendance_record/leave_request/leave_balance; GRANT mediaos_app = SELECT,INSERT,UPDATE trên cả leave_request_days lẫn attendance_records. ĐIỀU KIỆN: nếu crown micro-plan quyết thêm GRANT cho mediaos_worker hoặc object_type mới → CHÈN lane builder=db-migration (nối tiếp trước L2, số migration tiếp head idx 136/0456 → 0457, RLS/grant additive).

QUYẾT ĐỊNH KIẾN TRÚC crown (để plan-reviewer chốt): (a) Sync ASYNC qua EventBus consumer (khớp markDaysSyncPendingTx + retry endpoint) HAY đồng bộ in-tx trong approve()? Đề xuất ASYNC-consumer vì handoff 'Pending' đã có. (b) BẮT BUỘC: handler consumer chạy trên APP pool qua db.withTenant(companyId lấy từ EventContext) — KHÔNG dùng workerDb (mediaos_worker chỉ SELECT trên attendance_records/leave_request_days ⇒ ghi sẽ 42501). OutboxWorker chỉ trigger + ghi processed_events bằng workerDb; DB-work của handler tách sang app pool. (c) Đây là CONSUMER production ĐẦU TIÊN (hiện chỉ có test) ⇒ mẫu register phải sạch, consumerName DUY NHẤT toàn hệ (EventBus ném nếu trùng). (d) Balance restore đặt Ở LEAVE (L1) vì leave sở hữu leave_balances+ledger+grants+helper releaseReserve/convertReserveToUsed; attendance chỉ recalc record ⇒ tránh ghi chéo module.

BẤT BIẾN áp dụng: #1 mọi query có company_id qua withTenant (RLS+FORCE) — handler lấy companyId từ event, KHÔNG từ payload tin cậy mù; audit+outbox trong CÙNG tx với mutation. #2 append-only: leave_balance_transactions + leave_request_approvals + attendance_logs chỉ INSERT (không UPDATE/DELETE); leave_request_days + attendance_records là soft-delete/UPDATE-được. #3 không secret plaintext (không liên quan trực tiếp).

IDEMPOTENCY (crown): forward = guard theo attendance_sync_status (chỉ xử lý row còn 'Pending') + UNIQUE (employee,date,shift) chống dup + processed_events(consumer,event). revert = status-guard FSM (Approved→ chỉ 1 lần) + attendance_sync_status 'Pending Revert'→'Reverted'. Replay event / retry endpoint KHÔNG được: tạo record trùng, hoàn phép 2 lần, hay revert 2 lần. Rủi ro mixed-transaction: handler.withTenant COMMIT xong nhưng markProcessed lỗi ⇒ event retry ⇒ handler chạy lại ⇒ PHẢI idempotent (đã cover bằng guard).

VERIFY: chạy DB cô lập bash scripts/lane-db-setup.sh <lane> → export LANE_DB=mediaos_<lane> → pnpm --filter @mediaos/api test (memory: skipIf cần hasDb && LANE_DB, nếu không sẽ đỏ-giả/xanh-giả). int-spec + unit spec PHẢI colocated trong src/**/*.spec.ts|*.int-spec.ts để thực chạy (memory: spec để ở test/unit KHÔNG chạy).

GATE: FULL (crown) — security-reviewer + database-reviewer + silent-failure-hunter + santa-method; RED deny-path viết TRƯỚC; người chốt red-zone trước merge; auto-merge OFF cho tới khi chốt.

OUT-OF-SCOPE: packages/contracts (endpoint /internal/* dùng zod cục bộ, không phải public API); FE (S3-FE-* riêng); S3-QA-1/S3-QA-2 (QA kẹt phụ thuộc WO này — bàn giao kịch bản, Đội 3 verify); adjustment/remote-work workflow (S3-ATT-BE-4/5). Không mở rộng accrual/policy engine ngoài allowCancelAfterApproved+cancelBeforeDays.

MERGE HOT-FILE: attendance.module.ts (khối additive: provider AttendanceLeaveSyncService + repo + đăng ký consumer + internal controller) — append, KHÔNG rewrite; L2 sau L1 (event contract 'leave.request.revoked' do L1 định nghĩa). Không đụng schema/index.ts (không bảng mới).
