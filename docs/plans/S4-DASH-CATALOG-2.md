```yaml
wo: S4-DASH-CATALOG-2
zone: red
generated_by: auto-loop
reconciled_at: "9cda20d"
lanes: [{"id":"CATALOG2-DOC-DB","task":"SERIAL FIRST (đi trước, nối tiếp, crown/db-migration). (a) RECONCILE DB-07: bổ sung required_permission_code cho các Admin widget (USER_SUMMARY/EMPLOYEE_SUMMARY/MODULE_STATUS/SYSTEM_LOGS) verbatim từ API-10 PERMISSION MATRIX; VERIFY heading THẬT — bảng widget→permission ở §8.1 (~1109-1123), §8.5 heading thực = dashboard_cache_invalidations; const doc-block + DRIFT §14.3 lặp mislabel '§8.5' → SỬA mislabel. Cập nhật DRIFT §14.3: BÂY GIỜ defer CẢ TEAM_TASKS_TODAY (lý do: KHÔNG có resolver viewer→teamId — resolveContext trả managedUserIds+org-units, KHÔNG teamId; TasksService.listByTeam cần teamId bảng teams — đúng Trim-MVP owner) LẪN CONFIG_WARNINGS (chưa read-service warnings). §14.3 khớp tập seed 9. (b) LOCK bảng metadata 7 cột (widgetCode|moduleCode|widgetType@sortOrder|gatePair-ĐÃ-VERIFY-migration:dòng|defaultDataScope|dataSourceKey/method|PII?) cho ĐÚNG 9 widget trong docs/plans/S4-DASH-CATALOG-2.md TRƯỚC — const + migration transcribe TỪ ĐÂY (chống mâu thuẫn thứ tự lane). (c) Verify HEAD thật RUNTIME: ls apps/api/migrations|tail = 0492 ⇒ mint 0493; wave song song có thể land trước ⇒ +1 head mới, TUYỆT ĐỐI KHÔNG hard-code 0493. Migration MIRROR khối (1) của 0484: INSERT dashboard_widgets 9 row GLOBAL company_id NULL (KHÔNG per-company — dashboard_widgets là catalog toàn cục, owner rolbypassrls ghi trực tiếp; per-company chỉ ở default-config do seeder), ON CONFLICT (widget_code) WHERE company_id IS NULL AND deleted_at IS NULL DO NOTHING, giá trị row MIRROR DASH_WIDGET_CATALOG (module_code/widget_type/required_permission_code/default_data_scope/data_source_key/component_key/is_system_widget/status). TUYỆT ĐỐI KHÔNG khối (2)/(3) của 0484 — KHÔNG INSERT permissions, KHÔNG INSERT role_permissions (mọi gate-pair đã tồn tại + đã grant — cite migration:dòng trong comment); KHÔNG DDL/CREATE/ALTER; KHÔNG db:generate; KHÔNG seed refresh:dashboard-cache & KHÔNG cặp *:dashboard-widget.","builder":"db-migration","paths":["docs/DB/**","apps/api/migrations/**","docs/plans/S4-DASH-CATALOG-2.md"]},{"id":"CATALOG2-BE","task":"WIRING → CONST → HANDLERS → SEEDER(auto) → INT-SPEC (transcribe từ bảng LOCK). BƯỚC 0 WIRING TRƯỚC (tránh boot provider-not-found — đã verify HEAD): APPEND exports[] LeaveReadService+LeaveCalendarService vào leave.module.ts (đang providers dòng 66/23; exports hiện=[LeaveService,LeaveApprovalService] dòng 89 — CHỈ thêm vào exports[], KHÔNG method mới); THÊM exports:[AuthUsersService] vào users.module.ts (provider dòng 34, module CHƯA có exports[] — giữ nguyên forwardRef(AuthModule)+onModuleInit SecurityEventWriter fail-fast); THÊM exports:[AuditQueryService] vào foundation/audit/audit.module.ts (provider dòng 18, CHƯA có exports[]); import ModuleCatalogModule+UsersModule+AuditModule vào DashboardModule.imports (TasksModule/AttendanceModule/LeaveModule/EmployeesModule đã import ⇒ HrReadService/ContractService/AttendanceReadService sẵn; ModuleCatalogModule đã export ModuleCatalogService) — verify KHÔNG circular-dep (DASH là leaf; module nguồn KHÔNG import DASH; UsersModule forwardRef nội bộ). (1) APPEND DASH_WIDGET_CATALOG 9 widget (mirror bảng LOCK); THU HẸP DASH_WIDGETS_NOT_SEEDED về đúng ['TEAM_TASKS_TODAY','CONFIG_WARNINGS'] + comment lý do defer từng cái; DASH_WIDGET_COUNT 7→16 (là .length, tự cập nhật). (2) APPEND DASH_WIDGET_GATE_PAIR 9 cặp engine ĐÃ-VERIFY, mỗi entry comment migration:dòng: USER_SUMMARY→view:user(0444:88) · EMPLOYEE_SUMMARY→read:employee(0019:19) · MODULE_STATUS→view:foundation-module(0435) · SYSTEM_LOGS→view:audit-log SENSITIVE(0340:31,grant CA 0340:38) · LEAVE_BALANCE→view-own:leave-balance(0455:59) · NEW_EMPLOYEES→read:employee(0019:19) · CONTRACT_EXPIRING→view:contract(0462:157) · LEAVE_CALENDAR→view-team:leave-calendar(0455:65) · ATTENDANCE_ALERTS→view-team:attendance(0454). KHÔNG thêm cặp cho TEAM_TASKS_TODAY (defer). (3) APPEND DASH_DEFAULT_CONFIG 9 entry theo §14.3: Employee+LEAVE_BALANCE@40; Manager+LEAVE_CALENDAR@40/ATTENDANCE_ALERTS@50 (KHÔNG TEAM_TASKS_TODAY); HR+NEW_EMPLOYEES@20/CONTRACT_EXPIRING@30/ATTENDANCE_ALERTS@50; Admin+USER_SUMMARY@10/EMPLOYEE_SUMMARY@20/MODULE_STATUS@30/SYSTEM_LOGS@50 — CONFIG_WARNINGS+TEAM_TASKS_TODAY KHÔNG thêm. (4) APPEND 9 handler vào buildRegistry() + inject 6 read-service mới vào constructor (AuthUsersService/ModuleCatalogService/AuditQueryService/LeaveReadService/LeaveCalendarService/ContractService) REUSE method đã-scope/đã-mask (AuthUsersService.listUsers CHỈ .total · HrReadService.getEmployeesSummary/listHrEmployees sort=startDate · ModuleCatalogService.getAllModules · AuditQueryService.listCompany COUNT-ONLY · LeaveReadService.listMyBalances(own) · ContractService.list+filter endDate≤N (toDto không baseSalary) · LeaveCalendarService.listCalendar fail-closed scope · AttendanceReadService.listTeamRecords+filter Late/Absent/Missing), KHÔNG thêm method vào module nguồn, KHÔNG raw-query bảng module khác. GATE-Ở-HANDLER BẤT BIẾN: MỖI handler tự gateOrThrow/resolveAndAssert(gate-pair) 403 fail-closed TRƯỚC aggregate (KHÔNG tin AuditQueryService.listCompany/AuthUsersService.listUsers/ModuleCatalogService.getAllModules tự check — gate ở controller gốc); method đã-scope vẫn gate lại ở handler cho nhất quán. source fail → Degraded/Error KHÔNG sập dashboard. SYSTEM_LOGS: handler tự resolveAndAssert('view','audit-log') fail-closed TRƯỚC; COUNT-ONLY qua meta.total (filter action-group/thời gian, BỎ rows); response+cache TUYỆT ĐỐI KHÔNG ip/ipAddress/userAgent/actorUserId/actorEmployeeId/actor-email/changes/payload/metadata/errorMessage. ATTENDANCE_ALERTS filter 'hôm nay' dùng nguồn TZ công ty SẴN CÓ (tái dùng day-boundary attendance/tz.util, KHÔNG date ad-hoc). 5 PII widget (SYSTEM_LOGS/NEW_EMPLOYEES/LEAVE_CALENDAR/ATTENDANCE_ALERTS/CONTRACT_EXPIRING) KHÔNG lộ baseSalary/salaryType/identity_*. (5) Mở rộng int-spec theo tập mới (RED-first) + deny-path per-handler.","builder":"backend-builder","paths":["apps/api/src/dashboard/**","apps/api/src/leave/leave.module.ts","apps/api/src/users/users.module.ts","apps/api/src/foundation/audit/audit.module.ts","apps/api/test/integration/**"]}]
acceptanceChecks: ["SEED ĐÚNG 9 widget: USER_SUMMARY/EMPLOYEE_SUMMARY/MODULE_STATUS/SYSTEM_LOGS/LEAVE_BALANCE/NEW_EMPLOYEES/CONTRACT_EXPIRING/LEAVE_CALENDAR/ATTENDANCE_ALERTS. TEAM_TASKS_TODAY + CONFIG_WARNINGS DEFER → DASH_WIDGETS_NOT_SEEDED == ['TEAM_TASKS_TODAY','CONFIG_WARNINGS']; DASH_WIDGET_COUNT == 16 (=7 cũ + 9).","MODULE-WIRING: leave.module.ts exports thêm LeaveReadService+LeaveCalendarService; users.module.ts có exports:[AuthUsersService]; foundation/audit/audit.module.ts có exports:[AuditQueryService]; DashboardModule.imports thêm ModuleCatalogModule+UsersModule+AuditModule. App boot OK (pnpm --filter @mediaos/api build) — KHÔNG provider-not-found, KHÔNG circular-dep; DashboardWidgetHandlersService constructor inject 6 read-service mới resolve được.","MIGRATION: file = head THẬT +1 (0492⇒0493, verify journal RUNTIME, KHÔNG hard-code); INSERT dashboard_widgets 9 row GLOBAL company_id NULL, ON CONFLICT (widget_code) WHERE company_id IS NULL AND deleted_at IS NULL DO NOTHING; row MIRROR từng cột DASH_WIDGET_CATALOG; KHÔNG ALTER/CREATE TABLE, KHÔNG db:generate; TUYỆT ĐỐI KHÔNG INSERT permissions/role_permissions.","CHỐNG-DRIFT 1 nguồn: dashboard-widget-catalog.const.ts = NGUỒN; migration + dashboard-config.seeder + int-spec đọc từ đây. Mỗi widget mới có (a) DASH_WIDGET_CATALOG entry khớp từng cột row migration; (b) DASH_WIDGET_GATE_PAIR = cặp engine THẬT (comment migration:dòng); (c) handler đăng ký trong buildRegistry() reuse read-service module nguồn, gate 403 fail-closed TRƯỚC aggregate.","GATE-Ở-HANDLER (bất biến): MỖI trong 9 handler tự gateOrThrow/resolveAndAssert(gate-pair) 403 fail-closed TRƯỚC aggregate — kể cả widget dùng method đã-scope; ĐẶC BIỆT SYSTEM_LOGS(listCompany)/USER_SUMMARY(listUsers)/MODULE_STATUS(getAllModules) mà method nguồn KHÔNG tự check quyền ⇒ handler PHẢI tự gate.","SYSTEM_LOGS (crown, Admin@50, view:audit-log SENSITIVE CA-only): COUNT-ONLY qua meta.total — response + dashboard_widget_cache KHÔNG chứa ip/ipAddress/userAgent/actorUserId/actorEmployeeId/actor-email/changes/payload/metadata/errorMessage; chỉ count theo action-group/thời gian; non-CA → 403.","5 PII widget (SYSTEM_LOGS/NEW_EMPLOYEES/LEAVE_CALENDAR/ATTENDANCE_ALERTS/CONTRACT_EXPIRING): response + row dashboard_widget_cache KHÔNG chứa baseSalary/salaryType/identity_*; source fail → widget Degraded/Error KHÔNG sập dashboard (silent-failure-hunter clear). ATTENDANCE_ALERTS 'hôm nay' dùng day-boundary TZ công ty sẵn có (KHÔNG date ad-hoc).","DOC reconcile: DB-07 widget→permission có required_permission_code (NOT NULL) cho Admin widget verbatim API-10; mislabel §8.5 (heading thật=dashboard_cache_invalidations, bảng thật §8.1 ~1109-1123) đính chính; DRIFT §14.3 = defer CẢ TEAM_TASKS_TODAY (không resolver viewer→teamId) + CONFIG_WARNINGS (chưa read-service) kèm lý do; bảng metadata 7 cột LOCK trong docs/plans TRƯỚC const+migration.","DASH_DEFAULT_CONFIG mở rộng 9 entry đúng §14.3 sortOrder; Dashboard Admin sau seed = 5 widget (USER_SUMMARY@10/EMPLOYEE_SUMMARY@20/MODULE_STATUS@30/SYSTEM_LOGS@50 + NOTIFICATIONS@50); config_scope=Company, role_id/user_id NULL; Manager KHÔNG có TEAM_TASKS_TODAY.","PHANTOM vắng mặt: permissions KHÔNG có refresh:dashboard-cache (SA-only, no-endpoint) và KHÔNG cặp *:dashboard-widget (Option B); refresh:dashboard-cache KHÔNG seed. TEAM_TASKS_TODAY KHÔNG có row dashboard_widgets, KHÔNG gate-pair, KHÔNG default-config.","BẤT BIẾN + gate: #1 widget GLOBAL company_id NULL owner-bypass, KHÔNG đụng RLS/FORCE/policy 0482; configs/cache vẫn RLS+FORCE cô lập tenant. #2 append-only (ON CONFLICT DO NOTHING; KHÔNG role_permissions INSERT; không hard-delete). #3 chỉ count/aggregate — không lương/PII/secret. FULL gate security-reviewer + database-reviewer PASS; check.sh xanh; coverage nhạy cảm ≥80%; backlog.mjs cập nhật (DoD §8)."]
testTasks: ["A — catalog: dashboard_widgets GLOBAL (company_id NULL) == DASH_WIDGET_CATALOG mới (count=16 + khớp TỪNG cột module_code/widget_type/required_permission_code/default_data_scope/data_source_key/component_key/is_system_widget/status cho 9 widget mới).","A2 — defer assert: TEAM_TASKS_TODAY + CONFIG_WARNINGS VẮNG MẶT trong dashboard_widgets (không tạo widget degraded rỗng handler); DASH_WIDGETS_NOT_SEEDED == ['TEAM_TASKS_TODAY','CONFIG_WARNINGS'].","M — grant-matrix VÉT CẠN 4 role canonical (employee/manager/hr/company-admin) THEO THỰC TẾ SEED (KHÔNG blanket-absence tautological): HARD-assert view:audit-log + view:dashboard-audit-log CHỈ company-admin (sensitive, chống leo thang SYSTEM_LOGS); view:user PIN tập role thật (hr+CA, 0444:88); view:contract PIN tập thật (employee/manager/hr/CA, 0462/0465); view:foundation-module VERIFY empirically (is_sensitive=false → khả năng blanket qua wildcard) rồi PIN. TUYỆT ĐỐI KHÔNG assert 'hr absent view:user/foundation-module/contract' (RED-giả).","M2 — grant-count trên 8 resource_type nguồn THẬT của 9 widget (employee/user/foundation-module/audit-log/leave-balance/leave-calendar/contract/attendance — LƯU Ý: KHÔNG còn 'task' vì TEAM_TASKS_TODAY defer; done_when nói '9' là số cũ của bản 10-widget, dùng tập THỰC TẾ tính từ DASH_WIDGET_GATE_PAIR) KHÔNG tăng sau khi áp migration 0493 (chứng migration KHÔNG INSERT role_permissions).","E3 chống pair-drift — MỌI widget seeded trong catalog (16) có DASH_WIDGET_GATE_PAIR trỏ cặp permission THẬT tồn tại; không widget seeded nào thiếu map (fail-closed); reviewer đối chiếu bằng mắt cặp-đúng-ngữ-nghĩa (E3 chỉ chứng 'cặp TỒN TẠI').","E2 phantom — permissions KHÔNG có refresh:dashboard-cache và KHÔNG cặp *:dashboard-widget (Option B + SA-only no-endpoint).","Deny-path per-handler (RED, DB cô lập): user thiếu cặp gate nguồn của TỪNG widget mới → handler 403 fail-closed (runner KHÔNG nuốt thành Degraded — degrade CHỈ khi source lỗi kỹ thuật, KHÔNG khi thiếu quyền). ĐẶC BIỆT USER_SUMMARY/MODULE_STATUS/SYSTEM_LOGS (method nguồn không tự gate) → chứng handler tự gate chặn.","SYSTEM_LOGS deny (RED, crown): non-CA → 403; assert response CHỈ count, KHÔNG ip/ipAddress/userAgent/actorUserId/actorEmployeeId/actor-email/changes/payload/metadata/errorMessage (cả response lẫn dashboard_widget_cache row).","PII (RED): 5 widget PII (SYSTEM_LOGS/NEW_EMPLOYEES/LEAVE_CALENDAR/ATTENDANCE_ALERTS/CONTRACT_EXPIRING) — response + row dashboard_widget_cache assert KHÔNG chứa baseSalary/salaryType/identity_* (handler chỉ count/map/filter).","Cross-tenant (RLS+FORCE): company A KHÔNG lộ dữ liệu company B (dashboard_widget_configs/cache); GUC company mismatch → 0 row.","G/F/I default-config — DASH_DEFAULT_CONFIG §14.3 đầy đủ cho 9 widget mới qua reconcileCompany (config_scope=Company/role_id/user_id NULL); idempotent (reconcile lần 2 + re-apply không nhân đôi/không drift); Manager KHÔNG có TEAM_TASKS_TODAY.","Boot/wiring smoke — pnpm --filter @mediaos/api build (hoặc app.init test): DI resolve 9 handler dependency mới (AuthUsersService/ModuleCatalogService/AuditQueryService/LeaveReadService/LeaveCalendarService/ContractService/HrReadService/AttendanceReadService) KHÔNG provider-not-found, KHÔNG circular-dep.","DB cô lập bắt buộc (bài học LANE_DB gate): gate hasDb && LANE_DB; bash scripts/lane-db-setup.sh dashcat2 --reset; LANE_DB=mediaos_dashcat2; chạy qua ĐƯỜNG THẬT MasterDataSeedRunner.reconcileCompany (app role + RLS FORCE), KHÔNG gọi seed() owner."]
steps: ["MICRO-PLAN (crown, Sonnet5 xhigh → plan-reviewer đối kháng TRƯỚC code): chốt bảng LOCK ĐÚNG 9 widget (7 cột) + verify từng gate-pair verbatim migration:dòng (view:user 0444:88 · read:employee 0019:19 · view:foundation-module 0435 · view:audit-log 0340:31 grant CA 0340:38 · view-own:leave-balance 0455:59 · view:contract 0462:157 · view-team:leave-calendar 0455:65 · view-team:attendance 0454). XÁC NHẬN TEAM_TASKS_TODAY DEFER (không resolver viewer→teamId sạch) + CONFIG_WARNINGS DEFER. Chốt kỳ vọng grant-matrix M THEO THỰC TẾ SEED (KHÔNG máy móc 'admin-only' — chỉ view:audit-log + view:dashboard-audit-log genuinely CA-only). Chốt SYSTEM_LOGS count-only. Xác nhận module-wiring KHÔNG circular-dep.","Lane CATALOG2-DOC-DB (serial first): reconcile DB-07 (required_permission_code cho Admin widget verbatim API-10 + sửa mislabel §8.5↔§8.1 + DRIFT §14.3 defer CẢ TEAM_TASKS_TODAY+CONFIG_WARNINGS kèm lý do) → LOCK bảng metadata 7 cột 9 widget trong docs/plans TRƯỚC → verify head thật (ls migrations|tail=0492⇒0493, KHÔNG hard-code) → mint migration INSERT dashboard_widgets 9 row GLOBAL company_id NULL ON CONFLICT DO NOTHING mirror const (khối 1 của 0484); KHÔNG permissions/role_permissions/DDL/db:generate/refresh:dashboard-cache.","Lane CATALOG2-BE BƯỚC 0 WIRING (TRƯỚC handler): APPEND exports LeaveReadService+LeaveCalendarService (leave.module.ts), exports:[AuthUsersService] (users.module.ts), exports:[AuditQueryService] (audit.module.ts); import ModuleCatalogModule+UsersModule+AuditModule vào DashboardModule.imports; verify boot (pnpm --filter @mediaos/api build) KHÔNG provider-not-found / KHÔNG circular-dep.","Lane CATALOG2-BE CONST: APPEND DASH_WIDGET_CATALOG/GATE_PAIR/DEFAULT_CONFIG 9 widget (transcribe bảng LOCK) + thu hẹp DASH_WIDGETS_NOT_SEEDED=['TEAM_TASKS_TODAY','CONFIG_WARNINGS'] + DASH_WIDGET_COUNT tự = 16.","Lane CATALOG2-BE HANDLERS: inject 6 read-service mới vào constructor + APPEND 9 handler buildRegistry() reuse read-service module nguồn (gate→scope→mask, degrade-not-crash); MỖI handler tự gate 403 fail-closed TRƯỚC aggregate; SYSTEM_LOGS count-only self-gate view:audit-log; 5 PII widget không lộ baseSalary/salaryType/identity_*; ATTENDANCE_ALERTS 'hôm nay' theo TZ công ty sẵn có.","Lane CATALOG2-BE INT-SPEC RED-first: mở rộng dash-seed-catalog-permissions.int-spec.ts (catalog count=16/grant-matrix M/phantom/pair-drift E3) + dashboard-widget-security/data int-spec (per-handler 403 deny, SYSTEM_LOGS non-CA 403+count-only, no-PII 5 widget, cross-tenant). KHÔNG viết deny-path TEAM_TASKS_TODAY (defer).","Verify DB cô lập (bắt buộc): TURBO_FORCE=1 pnpm --filter @mediaos/contracts build → bash scripts/lane-db-setup.sh dashcat2 --reset → LANE_DB=mediaos_dashcat2 (gate hasDb && LANE_DB) → chạy ĐƯỜNG THẬT MasterDataSeedRunner.reconcileCompany (app role + RLS FORCE) → npx vitest run test/integration/*dash*.int-spec.ts liên quan.","FULL gate (security-reviewer + database-reviewer + silent-failure-hunter; santa-method cho gate-pair/seed logic) → đối chiếu BẰNG MẮT từng gate-pair (E3 chỉ chứng 'cặp TỒN TẠI') → check.sh xanh → cập nhật harness/backlog.mjs (DoD §8) → người duyệt red-zone chốt tập seed 9 + không-rò-phantom + PII + wiring + SYSTEM_LOGS count-only trước merge."]
```

RECONCILE-REFRESH vs plan đã lưu (reconciled_at=a95badb, plan cũ seed 10 widget). reused=FALSE — WO ĐÃ ĐỔI SCOPE: owner DEFER TEAM_TASKS_TODAY (ngoài CONFIG_WARNINGS đã defer từ trước). Delta so plan 10-widget: (1) seed 9 KHÔNG phải 10; (2) DASH_WIDGET_COUNT 16 (=7+9) KHÔNG phải 17; (3) DASH_WIDGETS_NOT_SEEDED=['TEAM_TASKS_TODAY','CONFIG_WARNINGS'] KHÔNG phải ['CONFIG_WARNINGS']; (4) GỠ HOÀN TOÀN TEAM_TASKS_TODAY khỏi catalog/gate-pair/default-config/handler — đây là widget rủi ro thiết kế cao nhất (cần resolve 'team của viewer' sạch), owner đã loại đúng → đơn giản hoá lớn, KHÔNG còn cần TasksService.listByTeam ở handler; (5) M2 grant-count còn 8 resource_type nguồn (BỎ 'task'), done_when còn ghi '9' là số của bản cũ — dùng tập THỰC TẾ tính từ DASH_WIDGET_GATE_PAIR.

STATE HIỆN TẠI (verified HEAD): DASH_WIDGET_CATALOG=7 widget (ATTENDANCE_TODAY/MY_TASKS/TASK_ALERTS/NOTIFICATIONS/PENDING_LEAVE/PROJECT_PROGRESS/HR_OVERVIEW); DASH_WIDGET_COUNT=CATALOG.length=7; DASH_WIDGETS_NOT_SEEDED hiện chứa CẢ 11 (bao gồm 9 sắp seed + TEAM_TASKS_TODAY + CONFIG_WARNINGS). Sau WO: catalog 16, NOT_SEEDED=['TEAM_TASKS_TODAY','CONFIG_WARNINGS']. buildRegistry() hiện 7 handler (dashboard-widget-handlers.service.ts:106-141) → APPEND 9. Migration head = 0492 (ls|tail; các file 0489-0492 tồn tại) ⇒ mint 0493 nếu không wave đua.

WIRING GAP ĐÃ RE-VERIFY (bắt buộc TRƯỚC handler): (a) leave.module.ts exports=[LeaveService,LeaveApprovalService] (dòng 89) — THIẾU LeaveReadService (provider dòng 53) + LeaveCalendarService (provider dòng 66); CHỈ APPEND exports[]. (b) users.module.ts KHÔNG có exports[] — AuthUsersService provider (dòng 34) ⇒ THÊM exports:[AuthUsersService] (giữ forwardRef(AuthModule)+onModuleInit SecurityEventWriter fail-fast dòng 50-60). (c) foundation/audit/audit.module.ts KHÔNG có exports[] — AuditQueryService provider (dòng 18) ⇒ THÊM exports:[AuditQueryService]. (d) DashboardModule.imports (dòng 40-49)=[PermissionModule,SeedModule,TasksModule,NotificationsModule,AttendanceModule,LeaveModule,EmployeesModule] — THIẾU ModuleCatalogModule+UsersModule+AuditModule ⇒ THÊM. ĐÃ SẴN (không thêm): EmployeesModule exports HrReadService+ContractService; AttendanceModule exports AttendanceReadService; ModuleCatalogModule exports ModuleCatalogService. Circular-dep: DASH leaf, module nguồn KHÔNG import DASH; UsersModule forwardRef(AuthModule) nội bộ ⇒ import UsersModule an toàn. Handlers constructor (hiện 10 dep dòng 47-58) APPEND 6 dep mới: AuthUsersService/ModuleCatalogService/AuditQueryService/LeaveReadService/LeaveCalendarService/ContractService.

CROWN — SYSTEM_LOGS: reuse AuditQueryService.listCompany (AuditController gate view:audit-log 0340:31 sensitive, grant CA 0340:38); method KHÔNG tự check quyền (gate ở controller gốc) ⇒ handler PHẢI tự resolveAndAssert('view','audit-log') fail-closed TRƯỚC. Gate-pair Option B = view:audit-log (khớp enforcement source). WO nhắc 'view:audit-log/dashboard-audit-log'; view:dashboard-audit-log (seeded 0484 CA, chưa endpoint) là cặp DASH riêng — test M HARD-assert CẢ HAI CA-only để chống leo thang. COUNT-ONLY: đếm theo action-group/thời gian; response+cache TUYỆT ĐỐI KHÔNG ip/userAgent/actor*/changes/payload/metadata/errorMessage.

DEFER TEAM_TASKS_TODAY (design gap owner chốt): TasksService.listByTeam(companyId, teamId, page) (tasks.service.ts:96) NHẬN teamId tường minh + CHỈ tenant-guard (KHÔNG actor-scope); dataScope.resolveContext trả managedUserIds+org-units, KHÔNG teamId; teams cần lookup riêng. KHÔNG có đường resolve 'team của viewer' sạch ⇒ Trim-MVP defer (giữ trong DASH_WIDGETS_NOT_SEEDED + lý do §14.3). NẾU sau này mở: mirror gateProjectProgress (handlers:297-316) — resolve từ resolveContext, authorize scope TRƯỚC aggregate, KHÔNG nhận teamId client, viewer không quản team → empty; deny-path RED manager A≠B. Widget này KHÔNG thuộc WO hiện tại.

GAP-ANALYSIS grant-matrix M (spec/code-reality thắng, SỬA done_when nếu lệch): done_when hàm ý một số cặp 'admin-only'. CHỈ ĐÚNG cho view:audit-log + view:dashboard-audit-log (genuinely CA-only). view:user granted hr+CA (0444:88); view:contract granted employee Own/manager Team/hr Company/CA (0462/0465); view:foundation-module is_sensitive=false → gần chắc blanket qua wildcard CROSS JOIN (bài học blanket-grant role drift) — builder VERIFY empirically rồi PIN. int-spec M assert THEO THỰC TẾ (KHÔNG RED-giả). Không phá bảo mật: widget non-sensitive (user COUNT-only/module status/contract expiry) HR/others thấy hợp lệ theo Option B (module nguồn ép scope+mask); default-config vị trí (Admin) tách biệt với gate. Widget PII genuinely-admin duy nhất = SYSTEM_LOGS (view:audit-log sensitive) → gate chặt + count-only.

MIGRATION per-company vs GLOBAL (đính chính done_when): done_when ghi 'idempotent per-company INSERT' — SAI ngữ nghĩa: dashboard_widgets là CATALOG TOÀN CỤC (company_id NULL, mig 0482 RLS+FORCE app GRANT SELECT-only; row global chỉ ghi được qua owner rolbypassrls). Migration INSERT 9 row GLOBAL company_id NULL, ON CONFLICT (widget_code) WHERE company_id IS NULL AND deleted_at IS NULL DO NOTHING (mirror khối 1 của 0484:49-61). 'Per-company' chỉ áp cho DEFAULT-CONFIG (dashboard_widget_configs) do dashboard-config.seeder.ts sinh qua reconcileCompany — KHÔNG ở migration.

MISLABEL DOC: bảng widget→permission thực ở DB-07 §8.1 (~1109-1123); §8.5 heading=dashboard_cache_invalidations; const doc-block (catalog:10-11) + DRIFT §14.3 lặp mislabel '§8.5'. Builder verify heading thật, sửa mislabel, LOCK bảng 7 cột trong docs/plans TRƯỚC (const+migration transcribe → int-spec ép hội tụ).

INVARIANTS (CLAUDE.md §2 / DECISIONS-02): #1 dashboard_widgets GLOBAL company_id NULL owner-bypass; KHÔNG đụng RLS/FORCE/policy WITH CHECK 0482; configs/cache vẫn RLS+FORCE. #2 append-only: widget ON CONFLICT DO NOTHING; KHÔNG role_permissions INSERT; không hard-delete. #3 no secret/PII: widget chỉ count/status/aggregate; 5 PII widget assert cache+response KHÔNG baseSalary/salaryType/identity_*. Phụ thuộc §3: audit/permission/RLS đã nền sẵn, widget CHỈ ĐỌC → migration không backfill company_id, không đụng RLS policy → không cần RLS-trước-backfill.

VERIFY (bài học LANE_DB): .env DATABASE_URL DB chung ⇒ hasDb=true kể cả không cô lập ⇒ chỉ skipIf(!hasDb) là ĐỎ/XANH-giả; gate CỨNG hasDb && LANE_DB. TURBO_FORCE=1 build contracts (turbo-cache false-green); lane-db-setup.sh dashcat2 --reset; LANE_DB=mediaos_dashcat2; chạy ĐƯỜNG THẬT reconcileCompany (app role + RLS FORCE), KHÔNG gọi seed() owner. Head mig verify RUNTIME (ls|tail) — wave song song (S4-INT/QA/DEVOPS) có thể tiến quá 0492 ⇒ mint head+1, KHÔNG hard-code 0493 (bài học double-mint).

GATE: FULL (chạm permission/seed/migration/RLS-adjacent + module-wiring): security-reviewer + database-reviewer + silent-failure-hunter, santa-method cho gate-pair/seed logic. E3 chỉ chứng 'cặp TỒN TẠI' — module nhiều cặp, chọn nhầm cặp-có-thật-nhưng-sai-ngữ-nghĩa vẫn xanh ⇒ reviewer đối chiếu BẰNG MẮT từng entry (comment migration:dòng + lý do). Crown planner Sonnet5 xhigh → plan-reviewer đối kháng TRƯỚC code; IMPLEMENT/REVIEW Opus. Người duyệt red-zone chốt tập seed 9 + không-rò-phantom + PII + wiring + SYSTEM_LOGS count-only trước merge.

HOT-FILE & MERGE: dashboard-widget-catalog.const.ts = NGUỒN DUY NHẤT — APPEND DASH_WIDGET_CATALOG/GATE_PAIR/DEFAULT_CONFIG, THU HẸP DASH_WIDGETS_NOT_SEEDED=['TEAM_TASKS_TODAY','CONFIG_WARNINGS'], KHÔNG rewrite khối 0484/BE-1/BE-2/BE-3 đã land. buildRegistry() APPEND 9 add(...). Migration mint file mới, KHÔNG sửa 0484. 3 module nguồn (leave/users/audit): CHỈ append exports[]/imports[], KHÔNG rewrite providers/controllers. DashboardModule: APPEND imports[] additive. Thứ tự lane serial: DOC-DB reconcile+LOCK bảng → BE wiring → BE const transcribe → DOC-DB migration transcribe từ cùng bảng LOCK → BE handler → BE int-spec hội tụ. Lane paths KHÔNG chồng: DOC-DB={docs/DB, migrations, docs/plans}; BE={src/dashboard, 3 module file, test/integration}.

OUT-OF-SCOPE: TEAM_TASKS_TODAY (không resolver viewer→teamId sạch — defer, giữ NOT_SEEDED) · CONFIG_WARNINGS (chưa read-service warnings — defer). FE widget mở rộng = S4-FE-DASH-2/3. DASH cache invalidation từ event = S4-INT-2. Không thêm endpoint refresh riêng; không seed refresh:dashboard-cache / *:dashboard-widget; không CROSS JOIN blanket-grant; KHÔNG thêm method vào module nguồn.

---

## LOCK — Bảng metadata 9 widget (NGUỒN DUY NHẤT — const + migration + int-spec transcribe TỪ ĐÂY)

> Chốt bởi lane **CATALOG2-DOC-DB** (serial-first) TRƯỚC khi CATALOG2-BE viết const hay DOC-DB mint migration.
> Mọi con số neo `migration:dòng` đã VERIFY runtime (grep migration land). ĐÚNG 9 widget mới — KHÔNG gồm 7
> widget đã seed ở 0484 (ATTENDANCE_TODAY/MY_TASKS/TASK_ALERTS/NOTIFICATIONS/PENDING_LEAVE/PROJECT_PROGRESS/HR_OVERVIEW),
> KHÔNG gồm TEAM_TASKS_TODAY + CONFIG_WARNINGS (DEFER — xem cuối mục).
>
> `required_permission_code` = chuỗi SPEC verbatim theo API-10 PERMISSION MATRIX (cột "Permission"), lưu là
> DỮ LIỆU catalog (`dashboard_widgets.required_permission_code`), KHÔNG phải engine key. **Gate thật** khi
> handler chạy = cột **gatePair** (cặp `(action,resource_type)` của MODULE NGUỒN — Option B, owner chốt 0484).

| # | widgetCode | moduleCode | widgetType@sortOrder | gatePair — VERIFY `migration:dòng` | defaultDataScope | dataSourceKey / read-method | PII? |
| - | --- | --- | --- | --- | --- | --- | --- |
| 1 | `USER_SUMMARY` | AUTH | Summary@10 | `view:user` — 0444:39 (grant hr 0444:88 · CA 0444:89) | Company | `user-summary` / `AuthUsersService.listUsers` (COUNT-only `.total`) | Không |
| 2 | `EMPLOYEE_SUMMARY` | HR | Summary@20 | `read:employee` — 0019:19 | Company | `employee-summary` / `HrReadService.getEmployeesSummary` | Không |
| 3 | `MODULE_STATUS` | SYSTEM | List@30 | `view:foundation-module` — 0435:338 (is_sensitive=false) | Company | `module-status` / `ModuleCatalogService.getAllModules` | Không |
| 4 | `SYSTEM_LOGS` | SYSTEM | Summary@50 | `view:audit-log` — 0340:31 (SENSITIVE · grant CA 0340:38) | Company | `system-logs` / `AuditQueryService.listCompany` (COUNT-only `meta.total`) | **Có** |
| 5 | `LEAVE_BALANCE` | LEAVE | Summary@40 | `view-own:leave-balance` — 0455:59 (grant 4 role 0455:136-139) | Own | `leave-balance` / `LeaveReadService.listMyBalances` (own) | Không |
| 6 | `NEW_EMPLOYEES` | HR | List@20 | `read:employee` — 0019:19 | Company | `new-employees` / `HrReadService.listHrEmployees` sort=startDate | **Có** |
| 7 | `CONTRACT_EXPIRING` | HR | Alert@30 | `view:contract` — 0462:157 (grant hr/CA 0462:169-170) | Company | `contract-expiring` / `ContractService.list` filter endDate≤N | **Có** |
| 8 | `LEAVE_CALENDAR` | LEAVE | Calendar@40 | `view-team:leave-calendar` — 0455:65 | Team | `leave-calendar` / `LeaveCalendarService.listCalendar` (fail-closed scope) | **Có** |
| 9 | `ATTENDANCE_ALERTS` | ATT | Alert@50 | `view-team:attendance` — 0454:36 | Team | `attendance-alerts` / `AttendanceReadService.listTeamRecords` filter Late/Absent/Missing | **Có** |

**Giá trị ROW migration (transcribe verbatim — mirror khối (1) của 0484, cột `company_id,widget_code,module_code,name,widget_type,required_permission_code,default_data_scope,data_source_key,component_key,is_system_widget,status,sort_order`):**

| widgetCode | name (vi) | component_key | is_system_widget | status |
| --- | --- | --- | --- | --- |
| `USER_SUMMARY` | Tổng số user | `UserSummaryWidget` | true | Active |
| `EMPLOYEE_SUMMARY` | Tổng số nhân viên | `EmployeeSummaryWidget` | true | Active |
| `MODULE_STATUS` | Module đang dùng | `ModuleStatusWidget` | true | Active |
| `SYSTEM_LOGS` | Log quan trọng gần đây | `SystemLogsWidget` | true | Active |
| `LEAVE_BALANCE` | Số ngày phép còn lại | `LeaveBalanceWidget` | true | Active |
| `NEW_EMPLOYEES` | Nhân sự mới | `NewEmployeesWidget` | true | Active |
| `CONTRACT_EXPIRING` | Hợp đồng sắp hết hạn | `ContractExpiringWidget` | true | Active |
| `LEAVE_CALENDAR` | Lịch nghỉ team | `LeaveCalendarWidget` | true | Active |
| `ATTENDANCE_ALERTS` | Bất thường chấm công | `AttendanceAlertsWidget` | true | Active |

**GATE-PAIR đã VERIFY (grep migration runtime):**
- `view:user` → permission `0444:39`; grant `hr` `0444:88` + `company-admin` `0444:89` (Company). PIN M: view:user granted = {hr, CA} (KHÔNG blanket).
- `read:employee` → permission `0019:19` (is_sensitive=false; blanket qua wildcard CROSS JOIN — VERIFY empirically rồi PIN).
- `view:foundation-module` → permission `0435:338` (is_sensitive=false → khả năng blanket, VERIFY empirically).
- `view:audit-log` → permission `0340:31` (SENSITIVE); grant `company-admin` TƯỜNG MINH `0340:38-40`. **CA-only** (genuinely admin — chống leo thang SYSTEM_LOGS). `view:dashboard-audit-log` (0484 CA-only) cũng HARD-assert CA-only ở M.
- `view-own:leave-balance` → permission `0455:59`; grant 4 role `0455:136-139` (Own).
- `view:contract` → permission `0462:157`; grant hr/CA `0462:169-170` (Company). Scope Own/Team cho employee/manager có ở 0462/0465 — PIN theo thực tế seed.
- `view-team:leave-calendar` → permission `0455:65`.
- `view-team:attendance` → permission `0454:36`.

**DEFER (KHÔNG seed row · KHÔNG gate-pair · KHÔNG default-config) — `DASH_WIDGETS_NOT_SEEDED == ['TEAM_TASKS_TODAY','CONFIG_WARNINGS']`:**
- `TEAM_TASKS_TODAY`: KHÔNG có resolver `viewer→teamId` sạch. `dataScope.resolveContext` trả `managedUserIds`+org-units, KHÔNG `teamId`; `TasksService.listByTeam` nhận `teamId` tường minh (bảng `teams`) + chỉ tenant-guard (KHÔNG actor-scope) ⇒ Trim-MVP owner defer.
- `CONFIG_WARNINGS`: chưa có read-service warnings cấu hình hệ thống ⇒ seed sẽ luôn degraded, defer.

**Hệ quả**: `DASH_WIDGET_CATALOG` = 7 (0484) + 9 (đây) = **16** (`DASH_WIDGET_COUNT == 16`, tự = `.length`).
Dashboard Admin sau reconcile = 5 widget: USER_SUMMARY@10 · EMPLOYEE_SUMMARY@20 · MODULE_STATUS@30 · SYSTEM_LOGS@50 + NOTIFICATIONS@50 (CONFIG_WARNINGS@40 KHÔNG có).
</content>
