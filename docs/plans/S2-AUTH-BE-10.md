```yaml
wo: S2-AUTH-BE-10
zone: red
generated_by: auto-loop
reconciled_at: "48382cd"
lanes: [{"id":"s2authbe10redtests","task":"RED deny-path viết TRƯỚC (crown, auth) — 2 spec. (A) Mở rộng apps/api/src/auth/auth-status-guard.spec.ts: cập nhật makeTx phục vụ thêm hàng companies (default status='active' để MỌI test regression user-suspended/active cũ GIỮ xanh khi code bắt đầu đọc companies). Thêm case UNIT: (1) refresh() company='suspended' → 401 UNIFORM + refreshInserts=0 + refreshUpdates>=1 (thu hồi family) + audit 'auth.refresh_blocked' after.reason='company_inactive'; (2) refresh() company='active'+user active → xoay bình thường (regression, refreshInserts=1); (3) PLAN-FIX fail-closed: refresh() companies SELECT trả 0 hàng (row undefined — mô phỏng RLS-lọc/soft-delete/race) → 401 UNIFORM + refreshInserts=0 + family revoked, KHÔNG ném TypeError/500; (4) completeTwoFactorLogin() company='suspended' → 401 UNIFORM + refreshInserts=0 + audit 'auth.login_blocked' reason='company_inactive'; (5) 2FA company='active' → cấp token (regression). (B) Spec tích hợp MỚI apps/api/src/auth/auth-refresh-company-active.int.spec.ts (skipIf(!(hasDb && LANE_DB)), Postgres THẬT + RLS THẬT): (b1) refresh: seed company+user active, mint refresh, UPDATE companies SET status='suspended' → refresh 401 + assert TOÀN family refresh_tokens.revoked_at NOT NULL trong DB + audit_logs action='auth.refresh_blocked' reason='company_inactive' TỒN TẠI + user_sessions.revoked_at set & revoked_reason='company_inactive'; (b2) PLAN-FIX #3 (proof commit-không-rollback): 2FA company-inactive — seed user bật 2FA, lấy challengeToken, UPDATE companies status='suspended' → completeTwoFactorLogin 401 + assert audit_logs action='auth.login_blocked' reason='company_inactive' TỒN TẠI trong Postgres (chứng minh audit COMMIT chứ không bị rollback) + refresh_tokens của user = 0 hàng mới; (b3) control company='active' → refresh xoay (token mới hợp lệ, token cũ revoked+replaced_by). Xác nhận CẢ 2 spec THỰC SỰ xuất hiện trong vitest run summary + FAIL trên code hiện tại (RED thật).","builder":"backend-builder","paths":["apps/api/src/auth/auth-status-guard.spec.ts","apps/api/src/auth/auth-refresh-company-active.int.spec.ts"]},{"id":"s2authbe10svccheck","task":"GREEN (crown, auth) — thêm cổng company-active trong apps/api/src/auth/auth.service.ts, tái dùng allow-list isAuthorizedStatus('active') làm nguồn DUY NHẤT. (1) refresh(): NGAY SAU nhánh user-active (:635-651) và TRƯỚC securityPolicy/issueTokens, đọc companies.status trong CÙNG withTenant tx bằng PREDICATE TƯỜNG MINH eq(companies.id, companyId) (mirror me() :916-920, defense-in-depth không dựa thuần RLS). FAIL-CLOSED: company row undefined HOẶC status ≠ 'active' ⇒ thu hồi CẢ family refresh (UPDATE refreshTokens family_id=row.familyId AND revoked_at IS NULL, mirror :636-639) + revokeAllSessionsForUserTx(tx, user.id, 'company_inactive') + audit 'auth.refresh_blocked' objectType 'auth' after={reason:'company_inactive',familyRevoked:true} trong tx (append-only) → return {kind:'invalid'} → caller ném 401 UNIFORM NGOÀI tx (KHÔNG TypeError 500). (2) completeTwoFactorLogin(): PLAN-FIX #3 — RESTRUCTURE khối withTenant (:416-450) trả DISCRIMINATED UNION sentinel (vd {kind:'ok',tokens,userId,email} | {kind:'blocked_company'}) THAY VÌ ném trong tx. Sau check user active (:429), đọc companies.status bằng eq(companies.id, claims.companyId); FAIL-CLOSED company undefined HOẶC ≠'active' ⇒ audit 'auth.login_blocked' reason='company_inactive' GHI TRONG tx + return {kind:'blocked_company'}; NGOÀI block: kind==='blocked_company' → ném UnauthorizedException(UNIFORM) để tx COMMIT kèm audit (KHÔNG cấp token; KHÔNG family để thu hồi vì là login mới). BẤT KỲ nhánh nào ghi audit PHẢI return sentinel + ném ngoài block (không ghi-rồi-ném trong tx). KHÔNG đặt check trong issueTokens (throw sẽ abort tx cuốn ngược thu hồi family + query thừa). KHÔNG đổi login() (resolveCompanyId :177-185 đã là cổng company — xác nhận, không sửa).","builder":"backend-builder","paths":["apps/api/src/auth/auth.service.ts"]}]
acceptanceChecks: ["refresh(): NGAY SAU check user-active, đọc companies bằng eq(companies.id, companyId) trong CÙNG withTenant tx (mirror me() :916-920); status ≠ 'active' → 401 UNIFORM (đúng message UNIFORM_LOGIN_ERROR, KHÔNG lộ trạng thái/tenant trong HTTP body).","FAIL-CLOSED: companies row undefined/vắng (RLS lọc, soft-delete, race) ⇒ coi như inactive ⇒ 401 UNIFORM + audit + thu hồi family; KHÔNG ném TypeError/500. Có deny-path test riêng: company row vắng → 401 + refreshInserts=0.","refresh() nhánh company-inactive thu hồi TOÀN BỘ family refresh (UPDATE refreshTokens WHERE family_id=row.familyId AND revoked_at IS NULL) + revokeAllSessionsForUserTx(tx,user.id,'company_inactive') ⇒ user_sessions.revoked_reason='company_inactive'; KHÔNG xoay token (0 insert refreshTokens).","Audit 'auth.refresh_blocked' objectType='auth' after.reason='company_inactive' after.familyRevoked=true ghi TRONG cùng tx (append-only); reason CHỈ ở audit_logs, KHÔNG vào HTTP body.","completeTwoFactorLogin(): khối withTenant trả DISCRIMINATED UNION sentinel — company ≠ 'active' (hoặc row vắng) ⇒ audit 'auth.login_blocked' reason='company_inactive' GHI TRONG tx + return {kind:'blocked_company'}; 401 UNIFORM ném NGOÀI block để tx COMMIT kèm audit (KHÔNG ghi-rồi-ném trong tx → không bị db.service rollback nuốt audit). KHÔNG cấp token (0 insert refreshTokens).","int-spec Postgres THẬT assert: sau 2FA company-inactive TỒN TẠI hàng audit_logs action='auth.login_blocked' reason='company_inactive' (bằng chứng commit — unit mock withTenant KHÔNG mô phỏng rollback nên KHÔNG được coi là bằng chứng).","login() KHÔNG đổi hành vi: resolveCompanyId (:177-185) vẫn là cổng company (status ≠ active → null → 401); không thêm code path mới trong login().","3/3 call-site issueTokens (login=303 qua resolveCompanyId, 2FA=432, refresh=675) đều KHÔNG cấp token khi company inactive; check đặt tại call-site refresh+2FA, KHÔNG trong issueTokens.","Regression: company='active' + user active → refresh xoay bình thường (1 insert refreshTokens, token cũ revoked+replaced_by); 2FA active → cấp token; reuse-detection/suspended-user/access-restricted KHÔNG đổi.","KHÔNG thêm migration; tái dùng companies.status (CHECK active|suspended, mig 0002); head mig giữ 0465.","FULL gate auth-crown PASS (security-reviewer+database-reviewer+silent-failure-hunter+santa-method) + người chốt red-zone TRƯỚC merge (không auto-merge).","Coverage auth.service.ts ≥80% mọi trục dưới test:cov:sensitive (DoD §8)."]
testTasks: ["RED unit (auth-status-guard.spec.ts, viết TRƯỚC): (1) refresh company='suspended' → 401 + refreshInserts=0 + refreshUpdates>=1 + audit refresh_blocked reason='company_inactive'; (2) refresh company='active'+user active → refreshInserts=1 (regression); (3) FAIL-CLOSED refresh companies SELECT 0 hàng → 401 + refreshInserts=0 (không TypeError); (4) 2FA company='suspended' → 401 + refreshInserts=0 + audit login_blocked reason='company_inactive'; (5) 2FA company='active' → cấp token (regression). Cập nhật makeTx phục vụ hàng companies default active — GIỮ xanh case user-suspended/active cũ.","RED integration (auth-refresh-company-active.int.spec.ts, LANE_DB cô lập, skipIf(!(hasDb && LANE_DB)), Postgres+RLS thật): (b1) refresh company suspended → 401 + toàn family refresh_tokens.revoked_at NOT NULL + audit_logs refresh_blocked reason='company_inactive' + user_sessions.revoked_at set & revoked_reason='company_inactive'; (b2) 2FA company suspended → completeTwoFactorLogin 401 + audit_logs action='auth.login_blocked' reason='company_inactive' TỒN TẠI (proof audit COMMIT không rollback) + 0 refresh_tokens mới; (b3) control company active → refresh xoay (token mới hợp lệ, token cũ revoked+replaced_by). Deny-path viết TRƯỚC, xác nhận FAIL trên code hiện tại.","Verify 2 spec mới THỰC SỰ chạy (xuất hiện trong vitest run summary) — không skip câm (memory: colocated .spec.ts phải khớp include src/**/*.spec.ts; int-spec gate hasDb && LANE_DB, .env một mình → đỏ/xanh-giả).","QA-AUTH regression trên LANE_DB: chạy lại toàn bộ auth*.spec/int-spec (login/2FA/refresh active + reuse-detection + suspended-user + access-restricted) đảm bảo cổng company-active KHÔNG phá luồng chính."]
steps: ["Bước 1 (RED — lane s2authbe10redtests): mở rộng auth-status-guard.spec.ts (5 case unit gồm fail-closed company-absent) + viết auth-refresh-company-active.int.spec.ts (refresh + 2FA audit-commit proof + control); chạy `pnpm --filter @mediaos/api test` (unit) và LANE_DB cô lập cho int-spec; xác nhận RED THẬT (fail vì code chưa đọc companies) + cả 2 spec xuất hiện trong run summary (không skip câm).","Bước 2 (GREEN — lane s2authbe10svccheck, sau lane 1): thêm cổng company-active vào refresh() (eq(companies.id,companyId), fail-closed→revoke family + audit refresh_blocked reason=company_inactive→return invalid→401 ngoài tx) + RESTRUCTURE completeTwoFactorLogin() trả sentinel {kind} + audit login_blocked reason=company_inactive TRONG tx + ném 401 UNIFORM NGOÀI block. Không đổi login().","Bước 3: chạy lại TOÀN BỘ auth spec + int-spec trên LANE_DB cô lập (bash scripts/lane-db-setup.sh s2authbe10 → export LANE_DB=mediaos_s2authbe10 → pnpm --filter @mediaos/api test) → xanh; xác minh KHÔNG phá regression login/2FA/refresh active + reuse-detection + suspended-user + access-restricted.","Bước 4: coverage nhạy cảm `test:cov:sensitive` giữ auth.service.ts ≥80% mọi trục (DoD §8).","Bước 5: FULL gate auth-crown (security-reviewer + database-reviewer + silent-failure-hunter + santa-method); người chốt red-zone TRƯỚC merge — KHÔNG auto-merge."]
```

RECONCILE-REFRESH (RE-RUN plan-fix vòng 1) vs code @48382cd: reused=FALSE — plan cũ (docs/plans/S2-AUTH-BE-10.md) STALE ở đúng điểm plan-reviewer vòng 1 flag. Lane s2authbe10svccheck cũ mô tả 2FA 'audit login_blocked + ném UnauthorizedException(UNIFORM)' — ám chỉ ném TRONG withTenant block; ĐÂY LÀ ANTI-PATTERN vì db.service.withTenant (db.service.ts:74-92, comment :69 'Lỗi trong fn → rollback toàn bộ transaction') gói toàn bộ trong db.transaction() ⇒ throw trong callback ROLLBACK cả audit vừa ghi ⇒ audit_logs KHÔNG tồn tại. Plan mới BUỘC mirror pattern login()/refresh(): ghi audit TRONG tx → return sentinel kind → ném 401 UNIFORM NGOÀI block để tx COMMIT kèm audit.

GAP #3 CÒN MỞ (xác minh Grep/Read code hiện tại): refresh() (:587-700) đọc user (:628) + check isAuthorizedStatus(user.status) (:635-651) NHƯNG KHÔNG đọc companies.status ⇒ company suspend trong khi refresh token còn sống (refreshTtlSec≈14 ngày) → user vẫn xoay token + có access token mới. completeTwoFactorLogin() (:368-466) đọc user (:417-426) + check status (:429) rồi THROW TRONG block, thiếu companies. login() ĐÃ an toàn: resolveCompanyId (:177-185, resolve_company_by_slug SECURITY DEFINER) trả null khi status≠'active' → 401 (done_when #2 'KHÔNG đổi login').

BỔ SUNG so với plan cũ (PLAN-FIX vòng 1, tất cả từ done_when RE-RUN): (a) FAIL-CLOSED company row undefined/vắng (RLS lọc, soft-delete, race) ⇒ coi inactive ⇒ 401 UNIFORM + audit + thu hồi family, KHÔNG TypeError 500 → THÊM deny-path unit riêng (company SELECT 0 hàng → 401, refreshInserts=0). (b) Đọc companies bằng PREDICATE TƯỜNG MINH eq(companies.id, companyId) mirror me() :916-920 (defense-in-depth, không thuần RLS). (c) int-spec THÊM assert user_sessions.revoked_at set + revoked_reason='company_inactive' (revokeAllSessionsForUserTx(tx,userId,'company_inactive') → helper :1298-1307 set revoked_reason=reason). (d) int-spec THÊM case 2FA company-inactive assert audit_logs action='auth.login_blocked' reason='company_inactive' TỒN TẠI trong Postgres thật (proof commit-không-rollback — unit mock withTenant không mô phỏng rollback nên không đủ). (e) completeTwoFactorLogin RESTRUCTURE khối withTenant trả discriminated union thay vì throw-in-tx.

THIẾT KẾ: đọc companies TRONG withTenant tx sẵn có (KHÔNG mở tx mới) — RLS lọc company_id + eq(companies.id,companyId) tường minh. refresh(): cổng NGAY SAU user-active branch, TRƯỚC securityPolicy/issueTokens; !company || !isAuthorizedStatus(company.status) → mirror suspended (revoke family UPDATE refreshTokens WHERE family_id AND revoked_at IS NULL + revokeAllSessionsForUserTx reason='company_inactive' + audit 'auth.refresh_blocked' reason='company_inactive' familyRevoked=true trong tx → return {kind:'invalid'} → 401 ngoài tx). Reason company_inactive TÁCH khỏi nhánh user 'suspended' (:640,:648 giữ nguyên 'suspended'). Tái dùng isAuthorizedStatus (:68-71) — allow-list fail-closed, companies_status_chk=(active|suspended) nên trạng thái tương lai default KHÔNG lọt.

QUYẾT ĐỊNH KHÔNG đặt check trong issueTokens: throw trong issueTokens ABORT tx cuốn ngược thu hồi family + audit deny mà refresh cần BỀN VỮNG (mirror suspended commit-rồi-ném-ngoài); + SELECT companies thừa cho happy login đã resolve company. ⇒ check tại 2 call-site (refresh + 2FA), login giữ nguyên. 3 call-site issueTokens: login=303 (che qua resolveCompanyId), 2FA=432 (CẦN), refresh=675 (CẦN) — đủ, không path cấp-token ẩn khác.

INVARIANTS (CLAUDE.md §2): company_id ép qua RLS trong withTenant + eq(companies.id,companyId) tường minh (defense-in-depth); audit_logs append-only (record trong tx, không update/delete); reason chỉ vào audit — HTTP body 401 ĐỒNG NHẤT (anti status/tenant probing, không lộ company tồn tại/trạng thái). Thu hồi = UPDATE revoked_at (KHÔNG hard-delete, §2).

VERIFY/GATE: RED viết TRƯỚC (CLAUDE.md §6 deny-path RED cho auth/workflow) → GREEN → FULL gate auth-crown (security-reviewer+database-reviewer+silent-failure-hunter+santa-method) → người chốt red-zone, KHÔNG auto-merge. Chạy LANE_DB cô lập (bash scripts/lane-db-setup.sh s2authbe10 + export LANE_DB=mediaos_s2authbe10) vì int-spec gate hasDb && LANE_DB (memory integration-test-lane-db-gate: .env một mình → hasDb=true gây đỏ/xanh-giả). Xác nhận spec mới xuất hiện trong run summary (memory vitest-unit-specs-must-be-colocated). Coverage auth.service.ts ≥80% qua test:cov:sensitive.

NO-MIGRATION: companies.status có sẵn (schema companies.ts:16 default 'active'; mig 0002 companies_status_chk CHECK IN active|suspended). Head mig 0465 KHÔNG đổi. KHÔNG lane db-migration.

OUT-OF-SCOPE (chống scope-creep): KHÔNG đổi login() (đã gated); KHÔNG per-request company-active middleware (§2 = check tại điểm CẤP token, không per-request); KHÔNG đổi UI/FE (401 → controller xóa cookie → login; FE không phân biệt company_inactive vs suspended — cùng UNIFORM); KHÔNG đụng audit gap AUTH khác (user_security_events writer, lock-no-revoke, user_roles hard-delete, data_scope thiếu Project — WO riêng).
