# S15-PAYROLL-FE-2 — micro-plan (vùng 🟡 · LIGHT gate)

> **WO:** FE track B — PAY-SCREEN-009 «Thành phần lương» (catalog + editor công thức kiểm tại chỗ) ·
> 010 «Mẫu bảng lương» (danh sách + chi tiết + xem trước) · 011 «Tỉ lệ luật định» · chi tiết kỳ v2
> (cột theo mẫu · breakdown phiếu theo thành phần · chọn/đổi mẫu).
> **Nguồn sự thật:** `docs/SPEC/SPEC-11 PAYROLL.md` §9.1 · §12.1 · §13.6 · §14 · §15.1 (044–058) ·
> contracts `payroll-catalog.ts` (444 dòng, ĐỦ) · BE `payroll-catalog.controllers.ts`.
> **Tiên quyết đã merge:** BE-2 #506/#507 (044–058) · BE-3 #510 (templateId kỳ · `components[]` dòng ·
> `meta.componentCode` phiếu) · FE-1 #513 · FE-3 #514.

---

## 0. Đo TRƯỚC khi viết plan (16/09/2026)

| Hạng mục                               | Trạng thái đo được                                                                                                                                                                                                      | Hệ quả                                           |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ |
| contracts track B                      | ✅ `payroll-catalog.ts` — 15 route, DTO + preview + write envelope `{id}`                                                                                                                                               | **KHÔNG thêm DTO**                               |
| contracts kỳ/dòng/phiếu v2             | ✅ `templateId` (kỳ + create/update) · `components[]`/`templateFingerprint`/`statutoryRateId` (dòng) · `meta` (item phiếu)                                                                                              | KHÔNG thêm DTO                                   |
| 15 cặp quyền track B ở FE              | ✅ `constants.ts:84-101` (`component*` · `template*` · `statutoryRate*`)                                                                                                                                                | KHÔNG khai cặp mới                               |
| Sidebar 3 mục track B                  | ✅ `PAYROLL_SIDEBAR_V2` đã khai `payroll.salaryComponents` · `payroll.templates` · `payroll.statutoryRates`; `pruneUnbuiltScreens()` đang cắt                                                                           | **thêm ROUTE_REGISTRY là mục tự hiện**           |
| `kind` lỗi track B (catalog/mẫu/tỉ lệ) | ✅ 20 kind (`component-*` · `template-*` · `rate-*` · `system-component-*`) + chữ Việt                                                                                                                                  | KHÔNG thêm                                       |
| 🔴 `kind` của **máy công thức**        | ❌ **15 kind** ở `formula/formula.errors.ts` (`formula-syntax` · `formula-cycle` · `statutory-rate-incomplete`…) phát qua `payrollDetails(err.kind, …)` với **BIẾN** ⇒ census 3 hình dạng MÙ, FE rơi «Đã có lỗi xảy ra» | **PHẢI thêm** bảng + chữ + census hình 4 (§2 D2) |
| web-core client                        | ❌ chỉ có 044 (`payroll-employees-api.ts`); thiếu 045–058                                                                                                                                                               | phải dựng                                        |
| `payrollKeys.catalog`                  | 🟡 chỉ `components(params)`                                                                                                                                                                                             | mở rộng                                          |
| i18n màn + `routeTitle`                | ❌                                                                                                                                                                                                                      | phải dựng                                        |
| `PayslipBreakdown`                     | 🟡 v1 (theo `itemType`) — BE-3 đã ghi `meta {componentCode, kind, isVisible}`                                                                                                                                           | nâng cấp, KHÔNG rẽ `isOwn`                       |
| Chi tiết kỳ                            | 🟡 cột v1 cố định; không hiện/đổi mẫu                                                                                                                                                                                   | nâng cấp                                         |

---

## 1. Phạm vi ĐÓNG + phản-phạm-vi

**LÀM:**

| #   | Hạng mục                                                                                                                                                               | Vị trí                                                                                                                                                                                                            |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | web-core: client 045–058 (+046 chi tiết) + `payrollKeys.catalog` mở rộng                                                                                               | `packages/web-core/src/lib/payroll-catalog-api.ts` (MỚI, spread vào `payrollApi`) · `query-keys.ts`                                                                                                               |
| 2   | 3 mục `ROUTE_REGISTRY` + 4 route `router.tsx` (chi tiết mẫu = RouteMeta cục bộ) + 4 `routeTitle`                                                                       | `registry.ts` · `router.tsx` · `nav.ts`                                                                                                                                                                           |
| 3   | PAY-SCREEN-009: list (lọc loại/hệ thống/đang dùng) + form tạo/sửa + **FormulaEditor** + xoá mềm                                                                        | `SalaryComponentListPage.tsx` · `components/SalaryComponentFormDialog.tsx` · `components/FormulaEditor.tsx` · `formula-tokens.ts`                                                                                 |
| 4   | PAY-SCREEN-010: list + tạo/sửa/ngưng/xoá; chi tiết: bảng thành phần (nhãn · công thức ghi đè · ẩn/hiện · kéo-thả + ↑↓) · lưu 053 · **xem trước 054**                   | `PayrollTemplateListPage.tsx` · `PayrollTemplateDetailPage.tsx` · `components/TemplateFormDialog.tsx` · `components/TemplateComponentsEditor.tsx` · `components/TemplatePreviewDialog.tsx` · `template-editor.ts` |
| 5   | PAY-SCREEN-011: bảng phiên bản theo `effectiveFrom` + băng cảnh báo §3.11 + form tạo/sửa (khoá khi `inUse`) + «sao chép thành bản mới»                                 | `StatutoryRateListPage.tsx` · `components/StatutoryRateFormDialog.tsx`                                                                                                                                            |
| 6   | Chi tiết kỳ v2: cột động theo `components` (visible + label) · hàng tổng · ghim cột NV · khối «Mẫu bảng lương» (hiện/đổi, ẩn khi > CollectingData) · băng «mẫu đã đổi» | `PayrollPeriodDetailPage.tsx` (tách `PeriodLinesSection.tsx` — trả nợ N1) · `components/PeriodTemplateBlock.tsx` · `period-line-columns.ts` · `use-template-drift.ts`                                             |
| 7   | Chọn mẫu khi tạo kỳ                                                                                                                                                    | `components/PeriodFormDialog.tsx`                                                                                                                                                                                 |
| 8   | `PayslipBreakdown` theo thành phần (`meta.componentCode`) — dùng chung quản trị + ME                                                                                   | `components/PayslipBreakdown.tsx`                                                                                                                                                                                 |
| 9   | Bảng lỗi công thức + census hình 4 · helper FSM `canChangePeriodTemplate` · i18n · wiring spec                                                                         | `payroll-errors.ts` · `payroll-actions.ts` · `i18n/locales/vi/payroll.ts` · spec                                                                                                                                  |

**⛔ KHÔNG LÀM:** không BE/migration/DTO · không sửa `PAYROLL_SIDEBAR_V2` · không đụng `packages/ui`
(ngoài paths) · không «Tổng quan»/«Báo cáo»/PDF (FE-4/BE-5) · không màn sửa phiên bản hồ sơ lương (nợ G3 FE-1) ·
không bảng tổng TOÀN KỲ theo từng cột (API chỉ có tổng gross/net — §2 D9).

---

## 2. Quyết định thiết kế

| #   | Quyết định                                                                                                                                                                                                                                                                                                                                                                                                | Vì sao                                                                                                                                                                                                                                                                                                                             |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | Client track B ở file riêng `payroll-catalog-api.ts`, spread vào `payrollApi`; khoá idempotency 045/050/056 = `idempotencyKeyFor` BĂM payload                                                                                                                                                                                                                                                             | Khuôn FE-1/FE-3; không import ngược `payroll-api.ts` (vòng). 056 chở tiền (trần, giảm trừ) ⇒ băm, không nối chuỗi.                                                                                                                                                                                                                 |
| D2  | **Bảng lỗi công thức RIÊNG** `PAYROLL_FORMULA_ERROR_KINDS` (15) — `payrollErrorI18nKey` tra cả hai bảng; census đọc khoá `FORMULA_ERROR_KINDS` ở `formula/formula.errors.ts` (hình 4) và so ĐÚNG BẰNG                                                                                                                                                                                                     | BE phát qua biến ⇒ 3 hình grep literal không thấy. Tách bảng để census cũ (tập = tập BE ba hình) vẫn đúng nghĩa. Chữ dùng `details` (`pos`+1, `ref`, `func`, `cycle`, `missing`, `reason`) để nói **ở đâu** sai.                                                                                                                   |
| D3  | **FormulaEditor = textarea + dải token tô màu bên dưới + gợi ý mã theo từ đang gõ + kiểm 048 debounce 400 ms**                                                                                                                                                                                                                                                                                            | Overlay tô màu trong textarea mong manh (IME, cuộn); dải tô màu tách riêng đủ để «tô màu tham chiếu» (mã catalog · `SYS_` · `TL_/GT_` · hàm · mã lạ = đỏ). 048 gác `manage:salary-component` ⇒ không có cặp đó thì editor KHÔNG gọi (lỗi vẫn về lúc lưu, 422 có chữ). Tokenizer FE CHỈ để tô màu — cú pháp đúng/sai do server nói. |
| D4  | **Thành phần hệ thống:** huy hiệu «Hệ thống», nút Xoá/Ngưng **ẩn**, form chỉ mở `name`/`sortOrder`                                                                                                                                                                                                                                                                                                        | Trigger 0570 + BE-2 M1: trường khác ⇒ 409 `system-component-immutable`. Không tạo được `aggregate`/`engine` (contracts `exclude`).                                                                                                                                                                                                 |
| D5  | 🔁 done_when «sửa công thức **hệ thống** có hộp xác nhận» **đọc lại**: công thức hàng hệ thống BẤT BIẾN (D4) ⇒ hộp xác nhận áp cho **mọi thành phần đang được mẫu dùng** (`usedByTemplates` của 046) khi đổi công thức/loại/giá trị/`pitDeductible`: nêu số mẫu + «kỳ Nháp/Đang gom/Đã tính dùng công thức mới ở lần tính (lại) kế tiếp; kỳ từ Chờ duyệt trở đi không đổi»                                | done_when viết trước BE-2 M1; ý định (người sửa biết ảnh hưởng tới kỳ chưa chốt) giữ nguyên. Snapshot dòng đóng băng từ `Reviewing` (BE-3).                                                                                                                                                                                        |
| D6  | Chi tiết mẫu: sửa **cục bộ** rồi «Lưu» một lượt PUT 053 (`sortOrder = index × 10`); hàng `valueType ∈ {engine, profile_item}` khoá ô công thức ghi đè; hàng `engine` không gỡ được                                                                                                                                                                                                                        | 053 đặt lại toàn bộ (sửa từng dòng = đồ thị tạm có vòng). `formula-override-not-allowed` là lỗi chắc chắn ⇒ khoá sẵn. Thiếu 4 mã đầu vào ⇒ để server nói (`template-input-missing` + `missing`), KHÔNG chép danh sách mã hệ thống sang FE.                                                                                         |
| D7  | **Kéo-thả HTML5 + nút ↑↓** (bàn phím)                                                                                                                                                                                                                                                                                                                                                                     | SPEC ghi kéo-thả; nút ↑↓ cho a11y. Không thêm thư viện dnd.                                                                                                                                                                                                                                                                        |
| D8  | Xem trước 054 chạy trên mẫu **ĐÃ LƯU**; còn thay đổi chưa lưu ⇒ nút xem trước khoá + gợi ý «Lưu trước». Đầu vào giả: 15 `SYS_*` có mặc định (`SYS_WORK_DAYS=22` bắt buộc — vắng ⇒ 020 `division-by-zero`) · `profileItems` cho thành phần `profile_item` của mẫu · `pitPayer` · `statutory` **điền sẵn từ bản tỉ lệ mới nhất** (055, cần `view:statutory-rate`; thiếu ⇒ báo cần quyền, không bịa số luật) | Server đọc `components` đã lưu; FE không hard-code số luật định (§3.11).                                                                                                                                                                                                                                                           |
| D9  | Cột động = thành phần `isVisible` của dòng ĐẦU TIÊN có `components` (sắp theo `sortOrder`), đặt giữa «Ngày công» và «Điều chỉnh/Thực lĩnh»; dòng v1/bị che ⇒ cột cũ. **Hàng tổng** cộng theo trang bằng số nguyên xu (×100); nhãn «Tổng» khi cả kỳ nằm trong một trang, «Tổng trang này» khi không                                                                                                        | API không có tổng theo cột toàn kỳ; cộng float trên `numeric(18,2)` lệch xu. Nhãn nói thật phạm vi tổng. Hàng tổng là hàng giả `__total__` (DataTable không có `tfoot`, `packages/ui` ngoài paths) — `onRowClick` bỏ qua nó.                                                                                                       |
| D10 | Đổi mẫu: nút **ẩn** khi `status ∉ {Draft, CollectingData}` ∨ thiếu `manage:payroll-period` ∨ thiếu `view:payroll-template` (không có danh sách để chọn). Helper thuần `canChangePeriodTemplate` + spec                                                                                                                                                                                                    | done_when «nút không hiện thay vì 409»; mirror `payroll-periods.service.ts:156`.                                                                                                                                                                                                                                                   |
| D11 | Băng «mẫu đã đổi sau lần tính»: so `line.templateFingerprint` với `sha256(`${formulaSetFingerprint}:${statutoryRateId ?? "none"}`)` (WebCrypto) trên các dòng đang hiện, chỉ khi kỳ `Calculated` + có `view:payroll-template`                                                                                                                                                                             | Công thức mirror `formula.fingerprint.ts:lineFingerprint`. Gợi ý «tính lại» chỉ có nghĩa ở `Calculated` (từ `Reviewing` snapshot đã đóng băng).                                                                                                                                                                                    |
| D12 | `PayslipBreakdown` nhóm theo `meta.kind` khi `meta` parse được (Thu nhập: earning/tax_exempt · Khấu trừ: deduction/statutory_employee/tax · Điều chỉnh); hiện mã thành phần; item `isVisible=false` vẫn hiện (mờ) vì nó thuộc thực lĩnh; `meta` vắng (v1 hoặc bị che) ⇒ bảng v1. **KHÔNG có cờ `isOwn`**, KHÔNG tự cộng ra net                                                                            | Server che `meta` cùng `amount`; nhánh duy nhất là theo DỮ LIỆU.                                                                                                                                                                                                                                                                   |
| D13 | Chọn mẫu khi tạo kỳ: picker 049 (`isActive=true`, `scope=company`) gác `useCanExact(view:payroll-template)`; không có cặp ⇒ ẩn picker (gắn sau ở chi tiết). `scope=org_unit` không liệt kê                                                                                                                                                                                                                | 023 `template-scope-unsupported` là lỗi chắc chắn ⇒ không mời.                                                                                                                                                                                                                                                                     |
| D14 | Tỉ lệ luật định: `inUse` ⇒ nút Sửa ẩn, thay bằng «Tạo bản mới từ bản này» (form điền sẵn, ngày trống); 7 bậc: 6 bậc `upTo` số + bậc cuối «trở lên» (`null`), `rate` là **phần trăm 0–100**                                                                                                                                                                                                                | 058 ⇒ 409 `rate-in-use`; `assertBracketsContinuous` ép `rate ∈ [0,100]`, đúng 7 bậc, chỉ bậc cuối `null`.                                                                                                                                                                                                                          |

---

## 3. Kiểm thử (RED trước ở chỗ có luật)

| Spec                                      | Neo điều gì                                                                                                                              |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `payroll-error-kind-census.spec.ts` (+ca) | hình 4: khoá `FORMULA_ERROR_KINDS` BE === `PAYROLL_FORMULA_ERROR_KINDS` FE; mỗi kind có chữ vi; chữ nội suy `pos`/`ref`/`cycle`          |
| `payroll-actions.spec.ts` (+ca)           | `canChangePeriodTemplate` đủ 8 trạng thái × quyền                                                                                        |
| `formula-tokens.spec.ts`                  | tô màu: mã catalog / `SYS_` / `TL_`·`GT_` / hàm / mã lạ; từ đang gõ tại con trỏ                                                          |
| `template-editor.spec.ts`                 | di chuyển/gỡ/thêm; payload 053 `sortOrder = i×10`; khoá override theo `valueType`; dirty                                                 |
| `period-line-columns.spec.ts`             | cột động từ `components` (ẩn cột `isVisible=false`); tổng theo xu; dòng v1                                                               |
| `payroll-track-b-screens.spec.tsx`        | gate: thiếu cặp ⇒ 0 gọi API; hệ thống ⇒ không nút xoá; `inUse` ⇒ không nút Sửa; editor không gọi 048 khi thiếu `manage:salary-component` |
| `payslip-breakdown.spec.tsx`              | nhóm theo `meta`; `meta` vắng ⇒ bảng v1; không cộng ra net                                                                               |
| `payroll-wiring.spec.ts` (+ca)            | 3 route mới + gate = cặp BE; chi tiết mẫu là RouteMeta cục bộ                                                                            |

Cổng: `pnpm --filter @mediaos/web-core build` TRƯỚC spec app · typecheck app/web-core · build · `bash harness/check.sh --quick` ·
LIGHT gate `typescript-reviewer` (+`react-reviewer` vì FE).

---

## 7. Bằng chứng (17/09/2026)

| Cổng                                                             | Kết quả                                                                                                                                                                                                   |
| ---------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm --filter @mediaos/web-core build` (trước spec app)         | ✅ exit 0                                                                                                                                                                                                 |
| `pnpm --filter @mediaos/web-core test`                           | ✅ 45 file · **742/742**                                                                                                                                                                                  |
| `pnpm --filter @mediaos/app test` (toàn bộ)                      | ✅ **284/284 file · 2837 test**, exit 0 (lượt trước khi vá HIGH)                                                                                                                                          |
| `vitest run src/routes/payroll/` (sau khi vá HIGH)               | ✅ 15 file · **198** test (+2 ca ghim HIGH)                                                                                                                                                               |
| `pnpm --filter @mediaos/app typecheck` · web-core `tsc --noEmit` | ✅ 0 lỗi                                                                                                                                                                                                  |
| `pnpm --filter @mediaos/app build` (vite)                        | ✅ (cảnh báo chunk > 500 kB CÓ SẴN, không do WO)                                                                                                                                                          |
| `bash harness/check.sh --quick`                                  | ✅ XANH (secret-literals · lint · typecheck · migration-no-drop · tooling 177/177)                                                                                                                        |
| Census kind lỗi                                                  | ✅ hình 4 — 15 kind `FORMULA_ERROR_KINDS` BE === FE, mỗi kind có chữ vi; chữ nói được vị trí/mã/chu trình                                                                                                 |
| Đột biến ca HIGH                                                 | ✅ ép `mayResync = true` ⇒ ca `[deny] đang sửa dở + 051 về bản mới` ĐỎ; khôi phục ⇒ xanh                                                                                                                  |
| LIGHT gate — `typescript-reviewer` (Sonnet)                      | 🟡→✅ BLOCK 1 HIGH + 1 LOW ⇒ vá cả hai (bên dưới); 7 nghi vấn còn lại reviewer xác nhận ĐÚNG (khoá cache · PATCH diff · `mode` ổn định · hàng tổng · deps drift · khoá query editor · gate `useCanExact`) |

**Vá sau review:**

- **HIGH** `PayrollTemplateDetailPage` đồng bộ mù theo `detail` ⇒ «Ngưng dùng»/«Sửa thông tin» (invalidate cả nhánh
  catalog) xoá sạch bảng đang sửa. Vá: `baseline` + đồng bộ CHỈ khi lần đầu · đổi mẫu · chưa sửa · ngay sau «Lưu»; ba mục
  ⋯ khoá khi `dirty`. Ghim 2 ca (deny + allow đối chứng) ở `payroll-track-b-screens.spec.tsx`.
- **LOW** `PayslipBreakdown` `KIND_GROUP.aggregate` — chú thích: BE không sinh item cho `aggregate`/`statutory_employer`.

**Nợ để lại:** `apps/app/src/i18n/locales/vi/payroll.ts` 879 dòng (825 TRƯỚC WO — vượt trần sẵn; khối track B đã
tách `payroll-catalog.ts`) · xem trước 054 chưa có ca render (logic `statutoryForPreview` đã ghim) · hàng tổng là tổng
TRANG khi kỳ > 20 dòng (API không có tổng theo cột toàn kỳ — D9) · `StatusBadges.tsx` prettier drift CÓ SẴN (không đụng).
