```yaml
wo: S2-AUTH-BE-9
zone: red
generated_by: auto-loop
reconciled_at: "48382cd"
lanes: [{"id":"be9revoke","task":"crown-jewel auth/token/session: thêm helper PUBLIC AuthService.revokeAllForUserTx(tx,userId,reason):Promise<number> TÁI DÙNG revokeAllSessionsForUserTx + pattern bulk-revoke refresh_tokens theo userId sẵn có (KHÔNG viết lại logic revoke), trả revoked_session_count; gọi trong CÙNG tx của AuthUsersService.lockUser (POST /auth/users/:id/lock) và AdminUsersService.suspendUser (POST /users/admin/:id/suspend) NGAY sau set status; enrich audit user.locked/user.suspended kèm revoked_session_count; inject AuthService (UsersModule đã forwardRef AuthModule, đã export AuthService); viết deny-path RED-TRƯỚC + integration DB cô lập","builder":"backend-builder","paths":["apps/api/src/auth/auth.service.ts","apps/api/src/users/auth-users.service.ts","apps/api/src/users/admin-users.service.ts","apps/api/src/users/users.module.ts","apps/api/src/users/auth-users.service.spec.ts","apps/api/src/users/admin-users.service.spec.ts","apps/api/test/integration/auth-users-admin.int-spec.ts","apps/api/test/integration/admin-users-deny.int-spec.ts"]}]
acceptanceChecks: ["POST /auth/users/:id/lock: trong CÙNG withTenant(companyId) tx, sau set status='locked' → MỌI refresh_tokens active của target có revoked_at set VÀ MỌI user_sessions active của target revoked_at set, qua AuthService.revokeAllForUserTx (tái dùng revokeAllSessionsForUserTx + pattern bulk-revoke refresh_tokens — grep xác nhận KHÔNG có khối UPDATE user_sessions/refresh_tokens mới viết trong users services).","POST /users/admin/:id/suspend: đối xứng lock — set status='suspended' + revoke toàn bộ refresh/session của target trong cùng tx qua cùng helper.","audit 'user.locked' và 'user.suspended': after payload chứa revoked_session_count (integer) = đúng số phiên active bị thu hồi (seed N phiên → count == N).","Sau lock/suspend: refresh token CŨ gọi POST /auth/refresh trả 401 NGAY (không phụ thuộc reuse-detection); GET danh sách phiên của target rỗng/đều revoked.","unlock (POST /auth/users/:id/unlock) và reactivate (POST /users/admin/:id/reactivate): KHÔNG re-issue/khôi phục phiên cũ — user_sessions cũ vẫn revoked_at IS NOT NULL, user phải đăng nhập lại.","Guard giữ nguyên: self-lock/self-suspend → 400; cross-tenant target → 404 + phiên tenant khác KHÔNG bị revoke (không rò); thiếu lock:user / suspend:user → 403.","BẤT BIẾN: mọi revoke đi qua withTenant(companyId) (RLS, #1); chỉ UPDATE revoked_at, KHÔNG hard-delete (#2 mirror); không lộ secret trong audit (#3).","Out-of-scope ghi rõ (comment + note): access token còn hiệu lực tối đa 900s (ACCESS_TOKEN_TTL_SEC, ≤15'); denylist jti là follow-up.","DoD §8: typecheck xanh, có test (RED viết trước, coverage ≥80% cho nhánh đổi), FULL gate PASS, người chốt red-zone, cập nhật harness/backlog.mjs."]
testTasks: ["DENY-RED (auth-users-admin.int-spec.ts): role rỗng → POST /auth/users/:id/lock 403; sau khi lock hợp lệ, refresh token cũ → /auth/refresh 401 NGAY; user_sessions target đều revoked; cross-tenant target B → 404 + phiên B nguyên vẹn; self-lock → 400.","DENY-RED (admin-users-deny.int-spec.ts): thiếu suspend:user → 403; sau suspend, refresh cũ → 401 NGAY + phiên revoked; cross-tenant 404; self-suspend 400.","AUDIT: assert audit 'user.locked'/'user.suspended' after.revoked_session_count == số phiên active đã seed (test đếm chính xác, không chỉ >0).","NO-RESTORE: sau unlock/reactivate, user_sessions cũ vẫn revoked_at IS NOT NULL (không tự hồi phiên) — user login lại mới có phiên mới.","INTEGRATION DB cô lập (LANE_DB): seed ≥2 phiên active (refresh_tokens + user_sessions dual-write) cho target rồi lock/suspend → cả hai bảng revoked; chạy dưới LANE_DB=mediaos_be9revoke (gate hasDb && LANE_DB).","UNIT spec colocated (auth-users.service.spec.ts / admin-users.service.spec.ts): mock AuthService.revokeAllForUserTx được gọi đúng 1 lần trong tx với (tx,id,reason) và count được ghi vào audit after — spec đặt trong src/** để thực sự chạy (memory: apps/api chỉ chạy src/**/*.spec.ts)."]
steps: ["RED-TRƯỚC (deny-path): mở rộng apps/api/test/integration/auth-users-admin.int-spec.ts — seed target với ≥2 phiên active (login thật hoặc insert refresh_tokens+user_sessions), gọi POST /auth/users/:id/lock → khẳng định refresh token CŨ gọi POST /auth/refresh trả 401 NGAY (không đi qua reuse-detection), user_sessions của target đều revoked_at IS NOT NULL, audit 'user.locked'.after chứa revoked_session_count = số phiên active; self-lock 400; cross-tenant (target B) 404 + phiên B KHÔNG bị revoke; role rỗng → 403.","RED-TRƯỚC (suspend): mở rộng apps/api/test/integration/admin-users-deny.int-spec.ts (hoặc spec suspend riêng cùng paths) cho POST /users/admin/:id/suspend đối xứng: refresh 401 NGAY, phiên revoked, audit 'user.suspended'.after có revoked_session_count; self-suspend 400; cross-tenant 404; thiếu suspend:user → 403. Chạy RED (đỏ) trước khi impl.","GREEN 1 — helper tái dùng: trong apps/api/src/auth/auth.service.ts thêm method PUBLIC revokeAllForUserTx(tx: TenantTx, userId: string, reason: string): Promise<number> — gộp (a) UPDATE refresh_tokens SET revoked_at WHERE user_id AND revoked_at IS NULL (đúng pattern dòng 1147-1150) và (b) gọi revokeAllSessionsForUserTx(tx,userId,reason) sẵn có; đếm số user_sessions active TRƯỚC/hoặc qua .returning() để trả revoked_session_count. KHÔNG nhân bản logic match session; KHÔNG hard-delete (chỉ UPDATE revoked_at — BẤT BIẾN #2 mirror).","GREEN 2 — call-site lock: apps/api/src/users/auth-users.service.ts lockUser — sau repo.setLockTx thành công, trong CÙNG tx gọi this.auth.revokeAllForUserTx(tx, id, 'locked'), lấy count, đưa vào audit after: { ...authUserSnapshot(updated), revokedSessionCount: count }. Giữ nguyên assertNotSelf, NotFound cross-tenant TRƯỚC audit, ALREADY_LOCKED 400.","GREEN 3 — call-site suspend: apps/api/src/users/admin-users.service.ts suspendUser — sau repo.setStatusTx('suspended'), trong CÙNG tx gọi revokeAllForUserTx(tx, id, 'suspended'), enrich audit after: { ...adminUserSnapshot(updated), reason, revokedSessionCount: count }. Giữ assertNotSelf + NotFound.","GREEN 4 — DI wiring: inject AuthService vào AuthUsersService và AdminUsersService (UsersModule đã imports forwardRef(() => AuthModule), AuthModule đã exports AuthService). Nếu resolver báo vòng → dùng @Inject(forwardRef(() => AuthService)) ở constructor param, KHÔNG đổi cấu trúc module.","Xác nhận KHÔNG hồi phiên: đọc lại unlockUser/reactivateUser — KHÔNG gọi bất kỳ re-issue/restore session nào (chỉ set status active). Thêm assert trong test: sau unlock/reactivate, user_sessions cũ vẫn revoked_at IS NOT NULL (user phải login lại).","Ghi note out-of-scope trong reconcileNotes + comment code: access token stateless còn sống tối đa ACCESS_TOKEN_TTL_SEC=900s (≤15'); chặn tức thì hoàn toàn cần denylist theo jti (Valkey) — DEFER, không làm ở WO này.","Verify DB cô lập: bash scripts/lane-db-setup.sh be9revoke → export LANE_DB=mediaos_be9revoke → pnpm --filter @mediaos/api test (int-spec chỉ chạy khi hasDb && LANE_DB — memory integration-test-lane-db-gate). pnpm --filter @mediaos/api typecheck xanh. FULL gate (security-reviewer + database-reviewer + silent-failure-hunter nếu có + santa-method) rồi người chốt red-zone."]
```

GAP (audit gap #2): lockUser (auth-users.service.ts:173) và suspendUser (admin-users.service.ts:90) hiện CHỈ set status + audit, KHÔNG thu hồi session/refresh → sau khi khóa, refresh token + phiên cũ vẫn sống, kẻ tấn công refresh liên tục hoặc dùng access token tới hết TTL. Cần đóng cửa sổ NGAY khi khóa.

TÁI DÙNG (KHÔNG viết lại revoke): AuthService đã có primitive revokeAllSessionsForUserTx(tx,userId,reason) (private, auth.service.ts:1298) + pattern bulk-revoke refresh_tokens theo userId (inline tại dòng 535-537 password_changed và 1147-1150 password_reset). Giải pháp = thêm 1 method PUBLIC AuthService.revokeAllForUserTx(tx,userId,reason):Promise<number> gộp 2 việc đó và trả revoked_session_count; gọi từ cả 2 call-site trong CÙNG tx của chúng. Đây là consolidation, KHÔNG nhân bản logic match session.

WIRING: UsersModule (users.module.ts:23) đã imports forwardRef(() => AuthModule); AuthModule (auth.module.ts:62-71) đã exports AuthService → chỉ cần inject vào 2 service. TenantTx là type chung (db.service.ts:13); withTenant trao đúng TenantTx nên truyền tx qua ranh giới module an toàn. PasswordService đã được inject từ AuthModule vào AuthUsersService không cần forwardRef ở param → AuthService dự kiến tương tự; nếu Nest báo vòng thì dùng @Inject(forwardRef(() => AuthService)).

KHÔNG CÓ MIGRATION: audit_logs.after là jsonb → thêm khóa revoked_session_count không đổi schema/CHECK; action user.locked/user.suspended và object_type 'user' đã tồn tại (đang dùng). ⇒ không cần lane db-migration.

INVARIANTS: #1 mọi revoke trong withTenant(companyId) (RLS lọc company_id, phòng thủ kép); #2 chỉ UPDATE revoked_at (session/refresh mutable theo DB-02, KHÔNG hard-delete) + audit append-only cùng tx (cùng commit/rollback với set status); #3 không đưa token/hash vào audit. Self-guard assertNotSelf và NotFound-first (cross-tenant che tồn tại, 0 audit rác) GIỮ NGUYÊN.

VERIFY: lane-db-setup.sh be9revoke → LANE_DB=mediaos_be9revoke → pnpm --filter @mediaos/api test + typecheck. RED-first cho deny-path/workflow (mở rộng auth-users-admin.int-spec.ts + admin-users-deny.int-spec.ts). Colocated unit spec đặt trong src/** (memory: apps/api chỉ chạy src/**/*.spec.ts — spec ở test/unit = xanh giả). int-spec chỉ chạy khi hasDb && LANE_DB (memory integration-test-lane-db-gate).

GATE: FULL (diff chạm auth/token/session revoke = crown-jewel) → security-reviewer + database-reviewer + silent-failure-hunter (nếu tồn tại trong repo; memory ghi agent này có thể chưa có → không block) + santa-method; model Opus code+review (crown); người chốt red-zone trước merge (không auto-merge).

OUT-OF-SCOPE (ghi note bắt buộc trong code + WO): access token là JWT stateless → còn hiệu lực tối đa ACCESS_TOKEN_TTL_SEC=900s (≤15', env.schema.ts:45). Chặn tức thì TUYỆT ĐỐI cần denylist theo jti (Valkey, jti = user_sessions.id đã nhúng vào claim từ S2-AUTH-BE-7) — DEFER thành follow-up WO. WO này đóng cửa sổ refresh (vô hạn → 0) và giới hạn access xuống ≤15'.

SINGLE-LANE (không fan-out): helper AuthService + 2 call-site + RED test phụ thuộc tuần tự (test cần symbol impl, call-site cần helper) → tách lane không cho song song, chỉ thêm coordination cost. Giữ 1 backend lane crown, RED→GREEN nội bộ, khớp mô hình §9 tuần tự 1 WO/phiên.
</content>
