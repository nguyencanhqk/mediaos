```yaml
wo: S4-INT-3
zone: red
generated_by: auto-loop
reconciled_at: "fc33165"
lanes: [{"id":"leaveProducerActor","task":"Producer gap-fix (crown FSM, ADDITIVE-ONLY): thêm field actorUserId: actor.id vào 5 outbox payload LEAVE để engine actor-exclusion (notification-recipient-resolver.service.ts:50 đọc dto.actorUserId ← bridge lấy payload.actorUserId) loại người khởi tạo. BẮT BUỘC cho ca owner-hủy-Approved (cancelApproved owner-only: actor=employee VỪA là recipient trong danh sách [employee,manager] → phải bị loại → chỉ manager nhận). Điểm chèn: leave-request.service.ts submitted(L500) + cancelled-self Pending/Draft(L344); leave-approval.service.ts approved(L179) + rejected(L257); leave-revoke.service.ts undoApprovedTx cancelled/revoked(L221). CHỈ thêm 1 field vào payload trong tx enqueue hiện có — KHÔNG đổi chữ ký/FSM/audit/balance; fromStatus ĐÃ có sẵn ở 2 producer cancelled (KHÔNG thêm).","builder":"backend-builder","paths":["apps/api/src/leave/leave-request.service.ts","apps/api/src/leave/leave-approval.service.ts","apps/api/src/leave/leave-revoke.service.ts"]},{"id":"leaveNotiBridgeReg","task":"Đăng ký 5 mapping LEAVE lên OutboxNotificationBridge (INT-1 generic — TÁI DÙNG, KHÔNG consumer/bridge mới). New leave-noti-bridge.registrar.ts (OnModuleInit, mirror AttNotiBridgeRegistrar; import CHỈ notifications/**+db/** — KHÔNG import LeaveModule, giữ acyclic) + new leave-approver.reader.ts (raw-SQL đọc employee_profiles.direct_manager_id = user_id theo payload.employeeId fallback userId; chạy TRONG this.db.withTenant(ctx.companyId, tx) + BIND company_id mọi câu). Map dot-eventType→eventCode VERBATIM từ notification-event-catalog.const.ts:80-84. resolveRecipients theo SPEC-05 §19.1: submitted→[manager] (reader); approved→[payload.userId]; rejected→[payload.userId]; cancelled→RẼ NHÁNH theo payload.fromStatus ('Approved'→[payload.userId, manager] · Pending/Draft→[manager]); revoked→[payload.userId, manager]. APPEND 5 khoá LEAVE_REQUEST_* strategy:'DedupeKey' windowSeconds:null vào notification-dedupe.const.ts. APPEND LeaveNotiBridgeRegistrar + LeaveApproverReader vào notifications.module.ts providers[] (khối S4-INT-3 additive — KHÔNG rewrite khối INT-1/INT-4/INT-5/exports[]/controllers[]).","builder":"backend-builder","paths":["apps/api/src/notifications/leave-noti-bridge.registrar.ts","apps/api/src/notifications/leave-approver.reader.ts","apps/api/src/notifications/notification-dedupe.const.ts","apps/api/src/notifications/notifications.module.ts"]},{"id":"leaveNotiIntSpec","task":"Int-spec RED-trước (Postgres cô lập LANE_DB, mirror att-noti-e2e.int-spec.ts): leave-noti-e2e.int-spec.ts — mỗi event → đúng số notification & recipient. submitted→direct manager (KHÔNG requester); approved/rejected→requester(payload.userId). cancelled 2 KỊCH BẢN TÁCH BIỆT: (a) requester tự hủy Pending → recipient = direct manager; (b) owner tự hủy Approved → resolved [employee,manager], actor(employee) BỊ LOẠI → chỉ manager (chứng minh actor-exclusion, KHÔNG trùng hợp). revoked (actor=HR≠employee) → employee + manager (2 recipient). Idempotent 2 tầng (processed_events + partial-unique uq_notifications_dedupe_active dedupeKey=eventId). Cross-tenant: trồng direct_manager thuộc company B → 0 notification cho B (RLS + filterActiveUsers eq company_id). Boot-guard: 5 eventCode enabled registerSource không throw; wire mã disabled → throw. Regression: attendance-leave-sync.int.spec (consumer thứ 2 trên leave.request.approved vẫn xanh).","builder":"backend-builder","paths":["apps/api/test/integration/leave-noti-e2e.int-spec.ts"]}]
acceptanceChecks: ["5 mapping LEAVE đăng ký lên OutboxNotificationBridge qua registerSource — KHÔNG có class consumer/bridge MỚI ngoài registrar+reader (grep: không EventBus.register thủ công, không gọi NotificationEngine trực tiếp ngoài bridge).","eventType(chấm)→eventCode VERBATIM notification-event-catalog.const.ts:80-84: submitted→LEAVE_REQUEST_SUBMITTED, approved→LEAVE_REQUEST_APPROVED, rejected→LEAVE_REQUEST_REJECTED, cancelled→LEAVE_REQUEST_CANCELLED, revoked→LEAVE_REQUEST_REVOKED (không hard-code chuỗi lệch).","Recipient theo SPEC-05 §19.1 (dòng 2380-2384)/§14.19 (dòng 1529-1532): submitted→direct_manager (reader); approved/rejected→requester(payload.userId); cancelled RẼ NHÁNH theo payload.fromStatus ('Approved'→[requester, manager] · Pending/Draft→[manager]); revoked→[requester, manager] — KHÔNG mapping phẳng.","resolveRecipients cho cancelled PHÂN BIỆT 2 producer qua payload.fromStatus: leave-request.service.ts:350 (fromStatus=Pending/Draft, hủy-Pending) → manager; leave-revoke.service.ts:227 (fromStatus='Approved', hủy-Approved) → [requester, manager].","5 payload outbox LEAVE MANG actorUserId=actor.id; engine actor-exclusion: owner hủy Approved (actor=employee ∈ danh sách) → employee bị loại → chỉ manager; HR revoke (actor=HR ∉ danh sách) → employee + manager. Test đọc outbox_events.payload->>'actorUserId' NOT NULL; countOutbox cũ không đổi.","leave-approver.reader chạy TRONG db.withTenant(ctx.companyId, tx) + bind company_id mọi câu (defense-in-depth trên RLS+FORCE); thiếu/NULL → [] fail-soft → engine log Skipped (SPEC-08 §16.4), KHÔNG delivery_log ma.","consumerName DUY NHẤT toàn hệ: noti-bridge:leave.request.* — không trùng attendance-leave-sync (EventBus ném nếu trùng); ATT-sync trên leave.request.approved vẫn chạy song song (attendance-leave-sync.int.spec xanh).","Idempotent retry: re-consume CÙNG outbox event → vẫn đúng 1 notification/recipient (2 tầng: OutboxWorker.processed_events theo consumer_name+event_id + partial-unique uq_notifications_dedupe_active theo company+recipient+event_code+dedupe_key=eventId).","Cross-tenant: event company A với direct_manager thuộc company B → 0 notification cho B (RLS + filterActiveUsers eq company_id); droppedCount/skip đúng.","Zero-migration: head mig giữ 0492; chỉ thêm const DedupeKey + 2 provider — không file migrations/** mới.","DoD §8: payload đọc an toàn (strField); audit producer không đụng; FE N/A; có test; luồng LEAVE/ATT chính không phá; handler bridge RE-THROW (không nuốt lỗi → OutboxWorker retry); FULL gate security-reviewer + silent-failure-hunter PASS; coverage int ≥80%; check.sh --all không LOUD 'XANH KHÔNG ĐỦ BẰNG CHỨNG'."]
testTasks: ["Int-spec leave-noti-e2e.int-spec.ts (test/integration, Postgres cô lập LANE_DB): mỗi event trong 5 → đúng số notification + recipient_user_id (submitted=direct manager; approved/rejected=requester; cancelled/revoked theo nhánh).","Cancelled 2 KỊCH BẢN TÁCH BIỆT RED: (a) requester tự hủy đơn Pending của mình → recipient = direct manager (KHÔNG requester); (b) owner (employee) tự hủy đơn Approved của mình → resolved [employee, manager], employee=actor BỊ LOẠI → chỉ manager (1 notification), chứng minh actor-exclusion không phải trùng hợp.","Revoked RED: HR/admin (revoke:leave, ≠ requester) thu hồi đơn Approved → recipient = employee + manager (2 notification), cả hai nhận vì actor(HR) không trong danh sách.","Deny-path RED workflow (actor không tự nhận): approve → chỉ requester nhận, approver KHÔNG; owner hủy Approved → employee(actor) KHÔNG nhận.","Idempotent RED: mô phỏng OutboxWorker re-consume cùng event_id (retry/reaper) → vẫn đúng 1 notification (2 tầng độc lập: xoá dấu processed_events vẫn bị partial-unique dedupeKey=eventId chặn).","Cross-tenant deny RED: outbox event company A với direct_manager thuộc company B → 0 notification cho B (RLS + company_id bind); đếm dropped/skipped đúng.","Boot-guard: standalone registerSource 5 eventCode LEAVE (enabled) không throw; wire mã disabled → throw tại boot (fail-loud).","Regression: attendance-leave-sync.int.spec (ATT-sync leave.request.approved vẫn xanh — consumer thứ 2 không xung đột) + noti-event-intake.int-spec + leave-request.int.spec/leave-approval.int.spec (countOutbox cũ không đổi).","QA-05/QA-08 đối chiếu: SPEC-05 §14.19 (đúng sự kiện→đúng người nhận theo data scope) + SPEC-08 §16 (không resolve được recipient → log Skipped, không tạo delivery_log ma)."]
steps: ["Thứ tự thi công: leaveProducerActor ‖ leaveNotiBridgeReg (paths disjoint, build song song) → leaveNotiIntSpec (depends cả 2: cần actorUserId từ producer + registrar để actor-exclusion & recipient đúng). MERGE serialize = 1 PR duy nhất SAU INT-1/INT-4/INT-5 đã ở master.","Lane leaveProducerActor: thêm actorUserId: actor.id vào 5 payload outbox LEAVE (submitted L500, cancelled-self L344, approved L179, rejected L257, cancelled/revoked-undo L221). Chỉ additive field trong tx enqueue hiện có; fromStatus GIỮ NGUYÊN. Verify countOutbox cũ KHÔNG đổi (leave-request.int.spec:421/520, leave-approval.int.spec:485/512 vẫn xanh).","Lane leaveNotiBridgeReg: viết leave-approver.reader.ts — SELECT direct_manager_id FROM employee_profiles WHERE company_id=$1 AND deleted_at is null AND (id=$employeeId OR (employeeId null AND user_id=$userId)) LIMIT 1; trả user_id manager hoặc null (fail-soft). Reader nhận tx từ caller (registrar mở withTenant), bind company_id mọi câu.","Lane leaveNotiBridgeReg: viết leave-noti-bridge.registrar.ts (OnModuleInit, mirror AttNotiBridgeRegistrar) đăng ký 5 mapping; eventCode VERBATIM catalog:80-84; resolveRecipients: submitted→reader(manager); approved/rejected→[payload.userId]; cancelled→branch payload.fromStatus ('Approved'→[payload.userId, manager] else [manager]); revoked→[payload.userId, manager]. Reader gọi qua this.db.withTenant(ctx.companyId, (tx)=>reader.resolve(tx, ctx.companyId, {...})).","Lane leaveNotiBridgeReg: APPEND 5 khoá LEAVE_REQUEST_* {strategy:'DedupeKey', windowSeconds:null} vào DEFAULT_DEDUPE (notification-dedupe.const.ts) kèm comment S4-INT-3; APPEND LeaveNotiBridgeRegistrar + LeaveApproverReader vào notifications.module.ts providers[] (khối S4-INT-3 additive — KHÔNG đụng INT-1/INT-4/INT-5/exports/controllers).","Lane leaveNotiIntSpec: viết leave-noti-e2e.int-spec.ts RED-trước (tắt tạm registrar → assert đỏ, bật lại → xanh); phủ 2 kịch bản cancelled + revoked + actor-exclusion + idempotent 2 tầng + cross-tenant + boot-guard; chạy regression attendance-leave-sync.int.spec + noti-event-intake.int-spec.","Verify DB cô lập: bash scripts/lane-db-setup.sh int3 → export LANE_DB=mediaos_int3 → pnpm --filter @mediaos/api test; trước PR vùng đỏ: bash harness/check.sh --all (int-spec skip ≤ ngưỡng, exit 1 nếu chưa chạy thật).","FULL gate crown: security-reviewer + silent-failure-hunter (+ santa-method) PASS; plan qua plan-reviewer trước code; ghi lại docs/plans/S4-INT-3.md + cập nhật harness/backlog.mjs; merge 1 PR SAU INT-1/INT-4/INT-5 ở master."]
```

RECONCILE-REFRESH → reused=FALSE (thay đổi bản chất so với plan cũ db8f081). KEY CHANGE: plan cũ dùng SPEC-08 §9.4 map PHẲNG cancelled/revoked → requester(payload.userId) — SAI, gây plan-block wf_b860acb0. WO đã bake lại theo SPEC-05 §19.1 (dòng 2380-2384)/§14.19 (dòng 1529-1532) — spec THẮNG. Mapping ĐÚNG (đã xác minh code+spec): submitted→Manager/Approver(direct_manager); approved→Employee(requester); rejected→Employee(requester); cancelled RẼ NHÁNH theo payload.fromStatus [ 'Approved'(leave-revoke.service.ts:227 owner-hủy-Approved)→[requester,manager] · Pending/Draft(leave-request.service.ts:350 self-hủy-Pending)→[manager] ]; revoked(fromStatus='Approved')→[requester,manager]. Hệ quả: leave-approver.reader giờ dùng cho submitted+cancelled+revoked (KHÔNG chỉ submitted như plan cũ), và int-spec thêm 2 kịch bản cancelled tách biệt + revoked.

GAP-ANALYSIS (code hiện tại): (1) OutboxNotificationBridge generic + 3 registrar (TASK/ATT/AUTH-HR) ĐÃ trên master — registerSource là extension point; LEAVE chỉ append 1 registrar + 1 reader. (2) 5 mã LEAVE_REQUEST_* isEnabled=true trong notification-event-catalog.const.ts:80-84 → boot-guard PASS, ZERO-migration (head giữ 0492). (3) PRODUCER GAP THẬT: 5 payload outbox LEAVE ĐÃ mang eventCode verbatim + (cancelled/revoked) fromStatus, NHƯNG THIẾU actorUserId → engine actor-exclusion (recipient-resolver:50 đọc dto.actorUserId←payload.actorUserId) không chạy ⇒ ca owner-hủy-Approved (cancelApproved owner-only, actor=employee ∈ [employee,manager]) sẽ tự-notify employee (SAI). Vá additive: thêm actorUserId:actor.id vào 5 payload (leave/**). fromStatus ĐÃ có sẵn (leave-request L350 prevStatus Draft/Pending; leave-revoke L227 'Approved') — KHÔNG thêm. (4) approver = employee_profiles.direct_manager_id references users.id (employees.ts:53) → LÀ user_id, dùng trực tiếp UserIds mode; NULL → recipient rỗng → engine Skipped (SPEC-08 §16.4).

INVARIANTS: BẤT BIẾN #1 company_id — reader trong db.withTenant + bind company_id mọi câu; engine filterActiveUsers eq company_id ẩn cross-tenant (defense-in-depth trên RLS+FORCE). #2 append-only — producer chỉ thêm 1 field payload; KHÔNG đụng audit/leave_balance_transactions/leave_request_approvals. #3 no-secret — payload LEAVE không mang secret (không cần payloadOf whitelist như INT-5 AUTH). Handler bridge RE-THROW (silent-failure-hunter): KHÔNG nuốt lỗi intake → OutboxWorker retry/dead-letter.

CONSUMER UNIQUENESS: bridge tự đặt consumerName 'noti-bridge:leave.request.*'; ATT-sync dùng 'attendance-leave-sync' trên CÙNG leave.request.approved (EventBus multi-consumer, idempotency riêng theo consumer_name+event_id) → 2 consumer song song KHÔNG đá nhau (attendance-leave-sync.service.ts:47).

VERIFY: int-spec DƯỚI LANE_DB cô lập (memory integration-test-LANE_DB-gate: gate hasDb && LANE_DB, KHÔNG .env-only false-green); check.sh --all trước PR (exit 1 nếu int-spec skip vượt ngưỡng — deny-path/cross-tenant/idempotent phải chạy THẬT). GATE: crown/FULL — security-reviewer + silent-failure-hunter (+ santa-method); plan qua plan-reviewer trước code; merge 1 PR serialize SAU INT-1/INT-4/INT-5 (nếu đụng notification-dedupe.const.ts hoặc notifications.module.ts thì UNION-append như pattern INT-4+INT-5, KHÔNG rewrite).

OUT-OF-SCOPE: (a) events/** KHÔNG đổi (tái dùng EventBus/OutboxService/OutboxWorker). (b) Template render LEAVE_REQUEST_* — thiếu template thì renderer fallback tiêu đề mặc định; thêm template = seed/migration → out-of-scope (chỉ register+recipient, zero-migration) — flag verify chứ không chặn. (c) leave.requested/leave.approved/leave.rejected (legacy path leave.service.ts:228/324/373) KHÔNG thuộc WO — chỉ workflow mới leave.request.*. (d) LEAVE_BALANCE_ADJUSTED / LEAVE_SYNC_TO_ATT_FAILED (SPEC-05 §19.1 dòng 2385-2386) KHÔNG thuộc WO này (WO chỉ 5 mã request FSM).
</content>
