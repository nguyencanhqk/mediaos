```yaml
wo: S2-FND-BE-4
zone: red
generated_by: auto-loop
reconciled_at: "48382cd"
lanes: [{"id":"filehardening","task":"H1 fail-closed link-aware + H2 download state-guard (crown-jewel: file access). PIN thiết kế PLAN-FIX: dispatch link-aware ở POLICY LAYER — thêm FilePolicyService.hasResolver(module,entity) + decideForLinkedFile(baseInput, links[], action): links RỖNG → giữ fallback FOUNDATION.FILE.* (reason allow/deny-foundation, KHÔNG đổi); links KHÔNG rỗng → BẤT KỲ link (module,entity) thiếu resolver đăng ký → DENY {allow:false, reason:'deny-no-resolver'}; MỌI link có resolver → dispatch từng resolver, AND most-restrictive (ALLOW chỉ khi TẤT CẢ allow, 1 deny → DENY). Policy KHÔNG chạm DB (links do service nạp và truyền vào). Thêm 'deny-no-resolver' vào FilePolicyReason. FileService GIỮ MỎNG: getMetadata/getDownloadUrl/deleteFile nạp file_links qua linkRepo.listByFileTx rồi gọi policy.decideForLinkedFile — KHÔNG double-fallback trong service; deny → logDeny(reason)+403. link()/unlink() GIỮ NGUYÊN. H2: getDownloadUrl SAU authz ALLOW, nếu upload_status!=='Uploaded' HOẶC scan_status==='Infected' → BẮT BUỘC ghi file_access_logs access_granted=false + denied_reason ∈ {'not-uploaded','infected'} TRƯỚC storage.get, rồi ném 404/409 (chốt 1 mã theo convention); storage.get KHÔNG được gọi; view metadata KHÔNG siết. download 302 kế thừa vì gọi chung getDownloadUrl. Cập nhật fixture unit 'download ALLOW' → makeFileRow({uploadStatus:'Uploaded', scanStatus:'Clean'}); thêm 1 unit-case multi-link DENY. RED deny-path integration sống trong int-spec MỚI colocated file-access-hardening.int.spec.ts (khớp glob src/**/*.spec.ts — KHÔNG đặt tên .int-spec.ts dưới src kẻo green-false). File ≤800 dòng, KHÔNG @ts-ignore/eslint-disable.","builder":"backend-builder","paths":["apps/api/src/foundation/files/file-policy.types.ts","apps/api/src/foundation/files/file-policy.service.ts","apps/api/src/foundation/files/files.service.ts","apps/api/src/foundation/files/file-policy.service.spec.ts","apps/api/src/foundation/files/files.service.spec.ts","apps/api/src/foundation/files/file-access-hardening.int.spec.ts"]}]
acceptanceChecks: ["H1 fail-closed: FilePolicyReason có 'deny-no-resolver'; policy.decideForLinkedFile với ≥1 link mà (module,entity) KHÔNG có resolver đăng ký → {allow:false, reason:'deny-no-resolver'} (KHÔNG rơi về FOUNDATION.FILE.*) — chứng minh bởi int-spec (a) user chỉ download:foundation-file → 403.","H1 multi-link AND: mọi link có resolver → ALLOW chỉ khi TẤT CẢ resolver allow; 1 resolver deny → DENY (most-restrictive). Có ≥1 unit-case multi-link trong files.service.spec.ts (hoặc file-policy.service.spec.ts) + int-spec (b2).","H1 regression: file 0-link (foundation-owned) → view/download/delete vẫn qua FOUNDATION.FILE.* (reason allow/deny-foundation) như cũ; toàn bộ unit case cũ (fallback/resolver-dispatch/tenant-guard/unlink/soft-delete-404) xanh.","FileService mỏng, KHÔNG double-fallback: getMetadata/getDownloadUrl/deleteFile nạp file_links qua linkRepo.listByFileTx và dispatch qua policy.decideForLinkedFile; quyết định link-aware nằm ở POLICY layer; service KHÔNG có nhánh fallback độc lập cho file-có-link. link()/unlink() không đổi. Files ≤800 dòng, không @ts-ignore/eslint-disable.","H2 state-guard BẮT BUỘC: getDownloadUrl (và GET /:id/download 302) — SAU authz ALLOW, upload_status!=='Uploaded' HOẶC scan_status==='Infected' → ném 404/409 (1 mã chốt), storage.get KHÔNG được gọi, VÀ ghi 1 dòng file_access_logs access_granted=false denied_reason ∈ {'not-uploaded','infected'} (không phải 'khuyến nghị') — int-spec (b) assert cả deny-log lẫn storage-not-called.","H2 không siết view + scan Pending/Failed vẫn tải (chủ ý): GET /:id metadata của file Pending/Infected (đã authz) → 200; file Uploaded + scan_status ∈ {Pending, Failed, NotRequired, Clean} vẫn presign (chỉ Infected chặn) — note pin: AV chưa nối, mặc định NotRequired.","Fixture regression cập nhật: unit 'download ALLOW → DownloadUrlDto' dùng makeFileRow({uploadStatus:'Uploaded', scanStatus:'Clean'|'NotRequired'}) (không giữ Pending thụ động).","Vị trí int-spec đúng glob: deny-path integration sống trong apps/api/src/foundation/files/file-access-hardening.int.spec.ts (khớp include src/**/*.spec.ts — KHÔNG .int-spec.ts dưới src ⇒ tránh green-false), khai báo trong lane paths; describe.skipIf(!(hasDb && LANE_DB)); tái dùng test/helpers/integration-db.ts.","Deny-path RED đủ 5 nhánh (a HR-link no-resolver; b Infected/Pending-upload; c foundation-thuần Uploaded+Clean/NotRequired tải OK; d view metadata 200; e 2-tenant/RLS 404) + access_log ghi THẬT + 403 qua guard-chain THẬT (không chỉ unit-mock).","Blast-radius H1 xác nhận: KHÔNG luồng production phục vụ file-đã-link qua FileService trước H1 (registry rỗng ở prod); delete file-đã-link cũng fail-closed; note file-mồ-côi tạm thời được ghi lại.","BẤT BIẾN §2: mọi query qua withTenant (RLS+FORCE); file_access_logs APPEND-ONLY (chỉ INSERT allow+deny); KHÔNG lộ storage_path/signed-url ra DTO/log — không đổi.","DoD §8 + FULL gate: input validate giữ, permission-gate giữ, audit/access-log cho hành động quan trọng, có test, không phá luồng chính; suite xanh trên LANE_DB cô lập; FULL gate PASS (security-reviewer, file access = crown) + người chốt; không auto-merge."]
testTasks: ["RED int-spec (a) DENY fail-closed no-resolver: seed file + file_links (module='HR', entity='EmployeeContract', KHÔNG resolver) + user role chỉ có download:foundation-file → GET /foundation/files/:id/download → 403; assert file_access_logs có dòng Download access_granted=false denied_reason='deny-no-resolver' và FOUNDATION.FILE.* fallback KHÔNG được đạt tới.","RED int-spec (b) no-presign state-guard BẮT BUỘC: seed file upload_status='Uploaded'+scan_status='Infected' VÀ biến thể upload_status='Pending' + user authz → GET /:id/download-url và /:id/download → 404/409, body KHÔNG chứa url/signed-url; assert file_access_logs deny denied_reason ∈ {'infected','not-uploaded'} và storage.get KHÔNG được gọi.","RED int-spec (b2) multi-link DENY: file có 2 file_links, 1 link (module,entity) thiếu resolver → decideForLinkedFile → DENY (deny-no-resolver) bất kể link kia; assert 403 + deny-log.","Regression int-spec (c): file foundation-thuần 0-link + upload_status='Uploaded' + scan_status ∈ {Clean, NotRequired} + user download:foundation-file → GET /:id/download → 302 (hoặc /:id/download-url → 200 có url).","Regression int-spec (d) view-not-restricted: file Pending/Infected (đã authz) → GET /:id metadata → 200 (chỉ nội dung bị chặn).","Regression int-spec (e) 2-tenant/RLS: A KHÔNG truy cập file của B (cross-tenant → 404); deny 2-tenant giữ nguyên.","Unit spec file-policy.service.spec.ts: hasResolver(module,entity) true/false; decideForLinkedFile — links rỗng → fallback (allow/deny-foundation); ≥1 link thiếu resolver → deny-no-resolver; mọi link có resolver + AND most-restrictive (all-allow→allow, 1-deny→deny); giữ xanh 8 case pipeline cũ.","Unit spec files.service.spec.ts: nhánh multi-link DENY khi thiếu resolver (getMetadata/getDownloadUrl/deleteFile nạp links → policy.decideForLinkedFile); H2 guard (upload!=='Uploaded' hoặc Infected → 404/409, storage.get NOT called, deny-log ghi); CẬP NHẬT fixture 'download ALLOW' → makeFileRow({uploadStatus:'Uploaded', scanStatus:'Clean'}); giữ xanh case fallback/resolver/tenant/unlink/soft-delete-404.","Vị trí + gate: int-spec colocate apps/api/src/foundation/files/file-access-hardening.int.spec.ts (khớp src/**/*.spec.ts — KHÔNG .int-spec.ts dưới src), khai báo trong lane paths, tái dùng test/helpers/integration-db.ts; describe.skipIf(!(hasDb && LANE_DB)) tránh đỏ-giả trên DB dev chung (memory integration-test-lane-db-gate)."]
steps: ["RED trước (viết test FAIL trên code hiện tại): (1) TẠO int-spec colocated apps/api/src/foundation/files/file-access-hardening.int.spec.ts — describe.skipIf(!(hasDb && LANE_DB)), tái dùng test/helpers/integration-db.ts + seedFile-pattern (RAW INSERT files upload_status='Uploaded'): (a) file có file_links (module='HR', entity='EmployeeContract', KHÔNG resolver) + user role chỉ download:foundation-file → GET /:id/download → 403 + file_access_logs Download access_granted=false denied_reason='deny-no-resolver'; (b) file Uploaded+scan_status='Infected' VÀ file upload_status='Pending' + user authz → GET /:id/download-url và /:id/download → 404/409, body KHÔNG chứa url, file_access_logs deny denied_reason ∈ {'infected','not-uploaded'}, storage.get KHÔNG gọi; (b2) multi-link: file có 2 link, 1 link thiếu resolver → DENY; (c) regression file foundation-thuần 0-link upload_status='Uploaded' scan_status ∈ {Clean,NotRequired} + download:foundation-file → 302 (hoặc download-url 200); (d) view GET /:id metadata của file Pending/Infected (đã authz) → 200; (e) 2-tenant/RLS: A không thấy file B → 404. (2) Unit RED: file-policy.service.spec.ts — hasResolver + decideForLinkedFile (empty→fallback, no-resolver→deny-no-resolver, multi-link AND); files.service.spec.ts — nhánh multi-link DENY + H2 state-guard. Xác nhận đỏ trên code hiện tại.","H1 types: file-policy.types.ts — thêm 'deny-no-resolver' vào union FilePolicyReason (kèm comment ý nghĩa fail-closed).","H1 policy: file-policy.service.ts — thêm hasResolver(moduleCode,entityType):boolean (tái dùng lookupResolver + normalize) + decideForLinkedFile(input, links, action): links rỗng → decideViaFallback (giữ FOUNDATION.FILE.*); links≠rỗng → nếu bất kỳ link thiếu resolver → {allow:false,reason:'deny-no-resolver'}; else dispatch resolver từng link (module,entity THẬT của link) + AND most-restrictive (mọi allow → allow-resolver; 1 deny → deny-resolver). Fail-closed try/catch → deny-error. Policy KHÔNG chạm DB (links truyền vào từ service).","H1 service: files.service.ts — getMetadata/getDownloadUrl/deleteFile: nạp file_links qua linkRepo.listByFileTx TRƯỚC quyết định, gọi policy.decideForLinkedFile(baseInput, links, action) — GỠ phụ thuộc policyInputForFile-fallback cho file-có-link (KHÔNG double-fallback trong service); deny → logDeny(reason)+ForbiddenException. link()/unlink() GIỮ NGUYÊN (không áp fail-closed lên gắn/gỡ). Không vượt 800 dòng.","H2 service: files.service.ts getDownloadUrl — SAU khi authz ALLOW (tránh oracle lộ state cho user không quyền), guard upload_status!=='Uploaded' HOẶC scan_status==='Infected' → BẮT BUỘC logDeny(action=Download, denied_reason='not-uploaded'|'infected', access_granted=false) TRƯỚC storage.get, rồi ném 404/409 (chốt 1 mã + message FOUNDATION-FILE-ERR-*). storage.get KHÔNG chạy trên nhánh deny. download 302 kế thừa (gọi chung).","Fixture regression: files.service.spec.ts — cập nhật test 'download ALLOW → DownloadUrlDto' sang makeFileRow({uploadStatus:'Uploaded', scanStatus:'Clean'}) (KHÔNG giữ nguyên thụ động); giữ xanh mọi case cũ (fallback/resolver-dispatch/tenant/unlink/soft-delete-404).","Blast-radius verify: grep-usage xác nhận KHÔNG luồng production nào phục vụ file-đã-link qua FileService trước H1 (registry resolver rỗng ở prod — registerResolver chỉ trong *.spec.ts) và CHƯA file nào lên upload_status='Uploaded' qua app (confirm→Uploaded là S2-FND-BE-5). Ghi note chấp nhận file-mồ-côi tạm thời (delete file-đã-link cũng fail-closed cho tới khi module đăng ký resolver).","GREEN + gate: pnpm --filter @mediaos/api test (unit case cũ+mới xanh) + int-spec trên DB cô lập (bash scripts/lane-db-setup.sh <lane> → export LANE_DB=mediaos_<lane> → test); gate cứng hasDb && LANE_DB. File ≤800 dòng, KHÔNG @ts-ignore/eslint-disable. FULL gate (security-reviewer + silent-failure-hunter nếu có + typescript-reviewer + quality-gate — file access = crown) + người chốt red-zone (KHÔNG auto-merge; PR cắt sạch từ origin/master, verify scope gh pr diff). Cập nhật harness/backlog.mjs done_when."]
```

RECONCILE-REFRESH tại HEAD 48382cd (plan reconciled_at cũng 48382cd): gap-analysis cũ VẪN ĐÚNG với code hiện tại, nhưng RE-RUN done_when mang PLAN-FIX buộc CẬP NHẬT lane (reused=false).

GAP xác minh (còn mở):
- H1: files.service.ts policyInputForFile() (L572-584) hard-code moduleCode=FOUNDATION/entityType=File; getMetadata(L180)/getDownloadUrl(L238)/deleteFile(L450) đều đi qua nó ⇒ LUÔN fallback FOUNDATION.FILE.* company-wide, KHÔNG BAO GIỜ đọc file_links thật. Registry resolver RỖNG ở prod (registerResolver chỉ trong *.spec.ts) ⇒ file gắn entity HR nhạy cảm chỉ cần download:foundation-file company-wide là tải được (trái BACKEND-11 §11.10 deny-by-default). hasResolver/decideForLinkedFile CHƯA tồn tại.
- H2: getDownloadUrl (L231-264) presign vô điều kiện; chỉ link() (L310) chặn Infected. Không guard upload_status/scan_status trước storage.get (L251).
- Fixture: files.service.spec.ts L38 makeFileRow default uploadStatus='Pending'/scanStatus='NotRequired'; L432-434 'download ALLOW' dùng makeFileRow() default ⇒ sau H2 sẽ bị DENY ⇒ PHẢI cập nhật sang Uploaded+Clean/NotRequired.

DELTA khiến reused=false:
1) Lane paths CŨ thiếu file int-spec mới — PLAN-FIX buộc int-spec deny-path phải KHAI BÁO trong lane paths ⇒ thêm apps/api/src/foundation/files/file-access-hardening.int.spec.ts.
2) Thiết kế PIN theo PLAN-FIX: 'dispatch link-aware ở policy layer (decideForLinkedFile), KHÔNG double-fallback' ⇒ chọn Phương án B (policy-layer) chứ KHÔNG orchestration-trong-service như plan cũ dẫn đầu. Policy KHÔNG chạm DB → service nạp file_links (linkRepo.listByFileTx) rồi TRUYỀN VÀO decideForLinkedFile; service không có nhánh fallback độc lập.
3) H2 deny-log chuyển từ 'khuyến nghị' → BẮT BUỘC: ghi file_access_logs access_granted=false denied_reason ∈ {'not-uploaded','infected'} + test assert dòng deny-log + storage.get NOT called.
4) Multi-link fail-closed AND: ≥1 link thiếu resolver → DENY; mọi link có resolver → ALLOW chỉ khi TẤT CẢ allow (most-restrictive) + 1 unit-case multi-link.

TRAP naming (mới phát hiện, PIN): vitest.config.ts:47 include = ["src/**/*.spec.ts","test/**/*.e2e-spec.ts","test/**/*.int-spec.ts"]. Ví dụ literal của WO 'src/foundation/files/file-access-hardening.int-spec.ts' KHỚP KHÔNG glob nào (dưới src nhưng đuôi -spec.ts ≠ .spec.ts; không dưới test/) ⇒ GREEN-FALSE (không bao giờ chạy). Đặt colocated .int.spec.ts (giống sibling file-access-log-api.int.spec.ts đã chạy được) HOẶC test/**/*.int-spec.ts. Đã pin .int.spec.ts colocated.

INVARIANTS (CLAUDE §2/§3): #2.1 mọi query qua withTenant (RLS+FORCE mig 0433) — không đổi. #2.2 file_access_logs APPEND-ONLY (chỉ INSERT allow+deny); files/file_links soft-delete. #2.3 KHÔNG lộ storage_path/signed-url ra DTO/log; log chỉ reason/module/entity/requestId. Permission engine (§3) đã có; đây là siết tầng policy (deny-by-default/fail-closed là bất biến FilePolicyService).

THỨ TỰ getDownloadUrl: authz (H1 link-aware) TRƯỚC → state-guard (H2) SAU — tránh oracle lộ Infected/Pending cho user không quyền. Chốt 1 mã lỗi H2 (409 ConflictException 'file tồn tại+authz nhưng state không tải được' hoặc 404) + message FOUNDATION-FILE-ERR-*. link()/unlink() GIỮ NGUYÊN (không áp fail-closed lên gắn/gỡ link kẻo chặn linking mọi module khi chưa có resolver).

EDGE cần builder chốt: file tự-link vào module=FOUNDATION/File (hiếm) — rule đơn giản → DENY; muốn coi 'foundation-linked=foundation-owned' phải normalize('foundation') ngoại lệ. Mặc định giữ rule đơn giản (an toàn).

VERIFY: unit — pnpm --filter @mediaos/api test (case cũ+mới xanh). Integration — DB cô lập BẮT BUỘC (bash scripts/lane-db-setup.sh <lane> → export LANE_DB=mediaos_<lane> → test); gate cứng hasDb && LANE_DB (.env làm hasDb=true → thiếu LANE_DB ⇒ đỏ-giả). seed RAW file upload_status='Uploaded' (theo seedFile trong file-access-log-api.int.spec.ts, tái dùng test/helpers/integration-db.ts).

GATE: red-zone/crown (file access) → FULL gate security-reviewer + silent-failure-hunter (nếu có) + typescript-reviewer + quality-gate; RED deny-path viết TRƯỚC. KHÔNG auto-merge — người chốt trước merge (PR cắt sạch từ origin/master để tránh phồng diff; verify scope gh pr diff, đừng tin self-report).

OUT-OF-SCOPE (chống scope-creep): đăng ký resolver thật HR/ATT/LEAVE/TASK (WO module-owner sau); upload E2E presigned-PUT + confirm→Uploaded + checksum (S2-FND-BE-5, depends_on WO này, cùng path files/**); validate entity-existence của file_links; download_count/last_accessed_at; TEMP_FILE_CLEANUP job (S2-FND-JOBS-1); bộ mã FOUNDATION-ERR-* đầy đủ; AV scanner nối thật (scan Pending/Failed vẫn tải = chủ ý, mặc định NotRequired). KHÔNG migration (không đổi DB — cột upload_status/scan_status đã có).
</content>
