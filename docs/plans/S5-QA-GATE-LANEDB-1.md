```yaml
wo: S5-QA-GATE-LANEDB-1
zone: yellow
generated_by: auto-loop
reconciled_at: "993914c"
lanes: [{"id":"qagatelanedb","task":"Làm harness/check.sh LOUD với int-spec bị skip im lặng vì thiếu LANE_DB. Chốt CÁCH: (a) PHÁT HIỆN+CẢNH BÁO là mặc định + (b) tự-provision opt-in. Tách logic thuần vào harness/lane-db-guard.mjs (parseVitestSkips(text)->{files,tests} + shouldWarn(n,threshold)); check.sh tee output test → parse → khi LANE_DB CHƯA set và N int-spec bị skip > ngưỡng: in banner LOUD 'N int-spec SKIPPED (thiếu LANE_DB) — deny-path/IDOR/cross-tenant KHÔNG chạy' + đổi summary từ 'XANH ✅' sang trạng-thái-thứ-ba 'XANH KHÔNG ĐỦ BẰNG CHỨNG'. Chạy step test với TURBO_FORCE=1 để vitest luôn EXECUTE + in summary (chống turbo-cache false-green). Thêm cờ opt-in --lane-db[=name] gọi scripts/lane-db-setup.sh + export LANE_DB chạy như CI khi có Postgres. Escalate exit=1 (đỏ) khi N>ngưỡng ở tier --all HOẶC REQUIRE_LANE_DB=1 (deny-path BẮT BUỘC chạy trước khi mở PR). GIỮ nguyên --quick/--no-test/--smoke/--all + 4 step lint/typecheck/test/smoke. Cập nhật CLAUDE.md §9 item5 + header check.sh nếu đổi hành vi.","builder":"backend-builder","paths":["harness/check.sh","harness/lane-db-guard.mjs","harness/lane-db-guard.test.mjs","apps/api/test/**","CLAUDE.md","docs/plans/S5-QA-GATE-LANEDB-1.md"]}]
acceptanceChecks: ["`bash harness/check.sh` (Postgres up, LANE_DB CHƯA set) in dòng đo được 'N int-spec SKIPPED (thiếu LANE_DB)' với N khớp con số vitest thật (~68+), và summary cuối KHÔNG còn là 'XANH ✅' trần mà là trạng-thái-thứ-ba rõ ràng ('XANH KHÔNG ĐỦ BẰNG CHỨNG'/WARN).","Sau `bash scripts/lane-db-setup.sh qagate && export LANE_DB=mediaos_qagate`, chạy lại → int-spec EXECUTE, N tụt về baseline ≤ ngưỡng (~≤10), KHÔNG in banner skip-LOUD, summary = 'XANH ✅' khi mọi test pass.","Ngưỡng: N > INT_SKIP_THRESHOLD (mặc định ~20, env-tunable) + LANE_DB chưa set → WARN mặc định; ở tier `--all` HOẶC REQUIRE_LANE_DB=1 → exit code = 1 (đỏ). Kiểm bằng `echo $?`.","Step test chạy với TURBO_FORCE=1 → chạy check.sh 2 lần liên tiếp, lần 2 vitest VẪN execute (không bị turbo cache short-circuit) và VẪN in summary parser đọc được (đối xứng bài học turbo-cache-false-green + done_when TURBO_FORCE).","`bash harness/check.sh --quick` KHÔNG chạy test/không đếm-skip (giữ nguyên); 4 step lint/typecheck/test/smoke đều còn + hoạt động — done_when 'KHÔNG phá luồng check.sh hiện có'.","`node harness/lane-db-guard.test.mjs` (hoặc node --test) PASS: parseVitestSkips xử lý đúng summary có/không 'skipped', dòng prefix turbo, đa-package, và cache/null; decideGate đúng ok/warn/red theo laneDbSet+threshold+strict.","Nếu đổi default pass/fail contract → CLAUDE.md §9 item5 được cập nhật ghi rõ cơ chế phát hiện skip + cách chạy như CI + tier --all yêu cầu deny-path đã chạy; header check.sh cập nhật (DoD §8 doc-in-sync).","Đối xứng CI 2026-07-10: với fix + LANE_DB, permission-rule-apply.int-spec.ts + suite deny-path/IDOR hiển thị EXECUTED (không skipped) — đúng lớp spec mà false-green từng che (regression permission/IDOR KHÔNG còn lọt cổng local)."]
testTasks: ["QA golden-fixture unit (harness/lane-db-guard.test.mjs): feed mẫu output vitest thật (summary có N skipped / không skipped=0 / dòng prefix '@mediaos/api:test:' / nhiều package / không-có-summary=cache→null) → assert parseVitestSkips().files/.tests + decideGate() trả đúng level. Chứng minh RED-trước: xoá hàm → test đỏ, rồi GREEN.","QA nghiệm thu 2-run tái lập (script/checklist trong docs/plans): (i) run KHÔNG LANE_DB → assert banner LOUD 'N int-spec SKIPPED' + summary trạng-thái-thứ-ba + exit theo tier; (ii) run LANE_DB=mediaos_qagate (sau lane-db-setup) → assert int-spec EXECUTED + XANH ✅. Ghi lại N thực đo cả 2 lần để cố định ngưỡng.","QA escalation deny-path-must-run: `bash harness/check.sh --all` (hoặc REQUIRE_LANE_DB=1) khi LANE_DB chưa set và N>ngưỡng → assert exit code = 1 (đỏ), chứng minh cổng pre-merge/red-zone BẮT BUỘC deny-path/IDOR đã chạy trước khi mở PR.","QA preservation: `--quick`/`--no-test` vẫn bỏ test; default 4 step (lint/typecheck/test/smoke) vẫn chạy + báo cáo như cũ; `--smoke` không bị ảnh hưởng bởi logic đếm-skip.","QA regression-proof (đối xứng CI): với LANE_DB, xác nhận permission-rule-apply.int-spec.ts + ≥1 spec deny-path/cross-tenant (vd employees-rbac-scope / att-core-tenant-deny) chạy THẬT (không skip) qua cổng local — không cần viết deny-path RED mới (không đụng code permission), mục tiêu là làm suite deny-path SẴN CÓ thực sự chạy."]
steps: ["Đo baseline (bằng chứng cho ngưỡng): trên Postgres đang chạy, `TURBO_FORCE=1 bash harness/check.sh` KHÔNG set LANE_DB → ghi lại N int-spec skipped từ dòng vitest 'Test Files ... skipped'; rồi `bash scripts/lane-db-setup.sh qagate && export LANE_DB=mediaos_qagate` chạy lại → ghi N_residual (dự kiến ~≤10: pgbouncer/e2e luôn skip). Chốt INT_SKIP_THRESHOLD nằm GIỮA hai số (đề xuất ~20, tunable qua env).","Tách logic thuần vào harness/lane-db-guard.mjs: export parseVitestSkips(text) trả {files,tests} (regex bắt 'Test Files ... N skipped' và 'Tests ... M skipped', bỏ prefix turbo '@mediaos/api:test:', trả 0 khi không có token 'skipped', trả null khi KHÔNG tìm thấy dòng summary=cache/không chạy); export decideGate({skippedFiles,laneDbSet,threshold,strict}) trả {level:'ok'|'warn'|'red', message}.","Sửa check.sh step test: chạy với TURBO_FORCE=1 và `tee` output vào file tạm (giữ streaming ra console); sau step gọi `node harness/lane-db-guard.mjs <logfile>` để lấy N + level. Khi level='warn' → in banner LOUD + đổi summary sang 'XANH KHÔNG ĐỦ BẰNG CHỨNG' (giữ exit 0 mặc định). Khi level='red' (tier --all hoặc REQUIRE_LANE_DB=1) → FAIL=1.","Thêm cờ --lane-db[=name] (opt-in option b): nếu set → chạy scripts/lane-db-setup.sh <name|check> rồi export LANE_DB trước step test, để 1 lệnh chạy như CI khi máy có Postgres; nếu lane-db-setup lỗi (không có Docker) → báo rõ, KHÔNG làm Docker thành bắt buộc.","Cập nhật header comment check.sh (mô tả cờ mới + trạng-thái-thứ-ba) và CLAUDE.md §9 item5 (ghi rõ check.sh giờ phát hiện int-spec skip im lặng + cách chạy như CI + --all yêu cầu deny-path đã chạy). Giữ default pass/fail contract của `bash harness/check.sh` trần KHÔNG đổi (warn-only) để không phá tier thường.","Viết harness/lane-db-guard.test.mjs (node --test / assert thuần): fixture summary có/không 'skipped', dòng prefix turbo, đa-package, và trường-hợp không-có-summary (cache→null); assert parseVitestSkips + decideGate (laneDbSet=true→ok kể cả N cao; laneDbSet=false&N>threshold→warn; strict→red). Chứng minh RED trước (hàm chưa có → test đỏ) rồi GREEN.","Nghiệm thu 2 lần chạy: (i) không LANE_DB → banner LOUD + summary thứ-ba + exit theo tier; (ii) LANE_DB=mediaos_qagate → int-spec EXECUTE (permission-rule-apply + deny-path suite KHÔNG skip), summary XANH ✅. Xác nhận `--quick` vẫn bỏ test, lint/typecheck/test/smoke còn nguyên."]
```

PHÂN RÃ MỚI (plan chưa tồn tại). GAP: harness/check.sh l.52 `step test pnpm test` chạy turbo KHÔNG set LANE_DB và KHÔNG có bất kỳ logic đếm-skip nào trong harness/ (đã grep). ~68-70 file int-spec gate `hasDb && Boolean(process.env.LANE_DB)` dưới 3 alias (runIsolatedDb/hasLaneDb/runDb) rồi describe.skipIf → skip IM LẶNG khi thiếu LANE_DB. hasDb=Boolean(DIRECT&&APP url) — LƯU Ý memory integration-test-LANE_DB-gate: .env local có thể làm hasDb=true nên gate ĐÚNG phải là hasDb&&LANE_DB (đã đúng trong code). CI api.yml ĐÃ vá 2026-07-10 (set LANE_DB=mediaos trên DB ephemeral + canary SEED-1) → WO này là fix ĐỐI XỨNG cho cổng LOCAL, KHÔNG đụng CI.

CHỐT CÁCH (done_when cho 2 lựa chọn a/b): dùng (a) PHÁT-HIỆN+LOUD làm MẶC ĐỊNH (an toàn: không phải máy nào cũng có Docker/Postgres → không ép chạy DB) + (b) tự-provision là OPT-IN qua cờ --lane-db (gọi scripts/lane-db-setup.sh + export LANE_DB chạy như CI khi có Postgres). Cơ chế: tee output step test → parse dòng summary vitest 'Test Files … N skipped' (bỏ prefix turbo) → nếu LANE_DB chưa set và N>INT_SKIP_THRESHOLD (đề xuất ~20, đo baseline để chốt: có LANE_DB ~≤10 skip vs không ~68+; ledger đã ghi 'full api suite 2276 pass/10 skip' dưới LANE_DB) → banner LOUD + summary TRẠNG-THÁI-THỨ-BA thay 'XANH ✅' trần.

TURBO-CACHE (done_when nêu đích danh + memory turbo-cache-false-green): step test PHẢI chạy TURBO_FORCE=1 để vitest thực sự EXECUTE và in summary — nếu cache short-circuit thì không có summary để đếm (parseVitestSkips trả null) ⇒ coi như bằng-chứng-thiếu → cũng cảnh báo. Đây là bằng-chứng-xanh, phải force.

TIER/ESCALATION: mặc định `bash harness/check.sh` trần = WARN-only (giữ default pass/fail contract, không block iteration không-Docker). Đỏ (exit 1) CHỈ khi tier `--all` (đúng ngữ nghĩa header 'xanh ở đây mới mở PR' = pre-merge/red-zone) HOẶC REQUIRE_LANE_DB=1 — vì trước khi mở PR deny-path/IDOR BẮT BUỘC đã chạy. Vì default contract KHÔNG đổi (chỉ thêm banner + escalation opt-in), việc cập nhật CLAUDE.md §9 item5 là ghi-chú-doc bổ sung (append) chứ không phải 'đổi hành vi mặc định' theo nghĩa pass/fail — vẫn cập nhật để doc-in-sync (DoD §8). CLAUDE.md KHÔNG nằm trong WO.paths gốc → đã THÊM vào paths để guard-scope không cảnh báo; nếu builder chọn escalate default→đỏ thì BẮT BUỘC cập nhật §9 (đã có path).

BẤT BIẾN §2: WO này KHÔNG đụng RLS/migration/company_id/secret/audit — thuần harness bash + 1 helper node thuần + self-test. KHÔNG có lane db-migration (head migration không đổi). Không có permission code mới nên KHÔNG cần viết deny-path RED mới; mục tiêu ngược lại là làm suite deny-path SẴN CÓ thực sự chạy.

VERIFY: đo N thật 2 chiều (có/không LANE_DB) để cố định ngưỡng — đây là số load-bearing, phải đo không đoán. Chứng minh permission-rule-apply.int-spec.ts (memory: từng đỏ 2 test mà CI+check.sh cùng skip che) hiển thị EXECUTED dưới LANE_DB.

GATE: LIGHT (zone yellow, không chạm permission/RLS/secret/audit/auth/migration) → typescript-reviewer(helper .mjs)+quality-gate; không cần FULL/security-reviewer. Reviewer soi thêm shell-injection ở tee/tmpfile và regex parser robust với prefix turbo.

OUT-OF-SCOPE: (1) KHÔNG sửa .github/workflows/api.yml — CI đã đúng từ 2026-07-10; (2) KHÔNG đổi logic gate hasDb&&LANE_DB trong các int-spec (đúng rồi, sửa sẽ phá bài học integration-test-LANE_DB-gate); (3) KHÔNG làm Docker/Postgres thành phụ thuộc bắt buộc của cổng thường; (4) KHÔNG đụng scripts/lane-db-setup.sh (chỉ GỌI lại). Builder = backend-builder (bash+node, không FE, không migration). Một lane duy nhất — WO nhỏ, tuần tự (test tự-kiểm phụ thuộc helper), không có song song thật để khai thác.
</content>
