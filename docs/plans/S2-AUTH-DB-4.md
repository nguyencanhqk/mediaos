```yaml
wo: S2-AUTH-DB-4
zone: red
generated_by: auto-loop
reconciled_at: "68616f9"
lanes: [{"id":"mig-0466-user-2fa-reset-perm","task":"Migration 0466 (SERIAL, nối tiếp head 0465) ADDITIVE: (a) ALTER TABLE users ADD COLUMN require_two_factor boolean NOT NULL DEFAULT false — không backfill, KHÔNG đụng RLS/FORCE/policy/grant của users; (b) seed catalog UNION permissions ('reset-2fa','user',is_sensitive=true) ON CONFLICT (action,resource_type) DO NOTHING; (c) DO-block resolve role company-admin (name='company-admin' AND company_id IS NULL AND is_system AND deleted_at IS NULL) + permission (reset-2fa,user) rồi INSERT role_permissions (role_id,permission_id,'ALLOW','Company') ON CONFLICT (role_id,permission_id,effect) DO NOTHING; (d) fail-loud DO-block RAISE EXCEPTION nếu pair HOẶC grant company-admin không tồn tại sau seed (mẫu mig 0120); (e) append journal entry idx 146 when 1717500725000. Đồng thời sửa drizzle schema apps/api/src/db/schema/users.ts thêm requireTwoFactor: boolean('require_two_factor').notNull().default(false) ADDITIVE — KHÔNG sửa file schema khác, schema/index.ts đã export users (không cần đổi).","builder":"db-migration","paths":["apps/api/migrations/0466_s2_authdb4_user_require_2fa_reset_perm.sql","apps/api/migrations/meta/_journal.json","apps/api/src/db/schema/users.ts"]}]
acceptanceChecks: ["Migration đánh số kế tiếp head THẬT trong worktree (base 0465); journal thêm đúng 1 entry đơn điệu (idx = prev+1, when > max-applied); không trùng/không sửa entry cũ.","information_schema.columns: users.require_two_factor tồn tại, data_type=boolean, is_nullable='NO', column_default='false'; migration KHÔNG chứa câu UPDATE/backfill nào trên users.","Migration KHÔNG chứa ALTER ... ENABLE/FORCE ROW LEVEL SECURITY, CREATE/DROP POLICY, GRANT/REVOKE trên bảng users (bất biến #1 giữ nguyên — users đã có RLS+FORCE từ mig 0002).","permissions có đúng 1 hàng (action='reset-2fa', resource_type='user') với is_sensitive=true sau migrate; ON CONFLICT DO NOTHING nên chạy lại không nhân đôi (COUNT không tăng).","role_permissions có đúng 1 hàng company-admin(system, company_id NULL) × reset-2fa:user × effect='ALLOW' × data_scope='Company'; không tạo grant cho role khác; idempotent (re-run COUNT không đổi).","Có fail-loud DO-block RAISE EXCEPTION khi pair HOẶC grant company-admin không tồn tại sau seed (đối chiếu mẫu mig 0120 dòng 90-98).","apps/api/src/db/schema/users.ts có requireTwoFactor additive khớp cột SQL; KHÔNG file schema khác bị sửa; pnpm typecheck + pnpm --filter @mediaos/api build xanh.","roles.requires_two_factor, security_policies.two_factor_enforced, TwoFactorEnforcementGuard KHÔNG bị đụng (out-of-scope — xác nhận git diff sạch ngoài 3 path lane).","DoD §8: có test (migration-smoke mở rộng, xanh trên LANE_DB); FULL gate PASS; PR không auto-merge; owner chốt trước merge."]
testTasks: ["Mở rộng apps/api/test/integration/migration-smoke.int-spec.ts (LƯU Ý: path ngoài WO paths — xem reconcileNotes), gate runIsolatedDb = hasDb && !!process.env.LANE_DB (KHÔNG bare skipIf): assert cột users.require_two_factor tồn tại với is_nullable='NO' và column_default chứa 'false'.","Integration (LANE_DB cô lập): assert permissions có (action='reset-2fa', resource_type='user') và is_sensitive=true sau chain-migrate tới head mới.","Integration (LANE_DB cô lập): assert role_permissions JOIN roles+permissions có company-admin(company_id IS NULL, is_system) × reset-2fa:user × effect='ALLOW' × data_scope='Company' — đúng 1 hàng.","Idempotency: chạy lại 2 câu seed (INSERT permission + INSERT grant với ON CONFLICT) trên LANE_DB → COUNT permissions/role_permissions KHÔNG tăng.","Migrate-clean: DB LANE_DB trống migrate đơn điệu tới head mới 0 lỗi (drizzle migrator chain).","Deny-path (ghi chú follow-up, KHÔNG bắt buộc ở WO này): pair reset-2fa:user hiện CHỈ ở catalog+grant, chưa có endpoint tiêu thụ → verify is_sensitive=true (ép object-grant tương lai); deny-path 403 cho endpoint reset-2fa được kiểm ở WO backend gắn guard sau này."]
steps: ["Trong worktree, kiểm HEAD THẬT: liệt kê apps/api/migrations + đọc meta/_journal.json (hiện idx 145 / 0465 / when 1717500720000). Nếu một nhánh WIP đã chèn 0466, đặt số TỰ DO kế tiếp và idx = maxIdx+1, when > max-applied (giữ đơn điệu). Không trùng số.","Tạo apps/api/migrations/0466_s2_authdb4_user_require_2fa_reset_perm.sql với header nêu rõ zone=red, ADDITIVE, bất biến giữ nguyên (không đụng RLS/FORCE users).","(a) ALTER TABLE users ADD COLUMN require_two_factor boolean NOT NULL DEFAULT false; --> statement-breakpoint. KHÔNG có câu UPDATE/backfill; KHÔNG ALTER policy/grant/RLS trên users.","(b) INSERT INTO permissions (action, resource_type, is_sensitive) VALUES ('reset-2fa','user',true) ON CONFLICT (action, resource_type) DO NOTHING; --> statement-breakpoint.","(c) DO-block: SELECT id role company-admin (WHERE name='company-admin' AND company_id IS NULL AND is_system=true AND deleted_at IS NULL) — RAISE nếu NULL; SELECT id permission (reset-2fa,user) — RAISE nếu NULL; INSERT INTO role_permissions (role_id,permission_id,effect,data_scope) VALUES (v_role,v_perm,'ALLOW','Company') ON CONFLICT (role_id,permission_id,effect) DO NOTHING (UNIQUE không gồm data_scope — memory §13).","(d) fail-loud DO-block riêng (mẫu 0120): RAISE EXCEPTION nếu NOT EXISTS pair (reset-2fa,user) HOẶC NOT EXISTS grant (company-admin × reset-2fa:user × ALLOW × Company) — tránh âm thầm trượt seed.","(e) Append vào meta/_journal.json 1 entry: idx 146, version '7', when 1717500725000, tag '0466_s2_authdb4_user_require_2fa_reset_perm', breakpoints true (append thuần, không rewrite entry cũ).","Sửa apps/api/src/db/schema/users.ts: thêm dòng requireTwoFactor: boolean('require_two_factor').notNull().default(false) (import boolean từ drizzle-orm/pg-core) — ADDITIVE cuối object cột, kèm comment trỏ S2-AUTH-DB-4 + phân biệt với roles.requires_two_factor. KHÔNG drizzle-kit generate (viết SQL tay theo convention 04xx).","Verify trên LANE_DB cô lập: bash scripts/lane-db-setup.sh authdb4 → export LANE_DB=mediaos_authdb4 → chain-migrate tới head mới KHÔNG lỗi; kiểm cột + pair + grant tồn tại; pnpm --filter @mediaos/api typecheck xanh.","FULL gate (database-reviewer + security-reviewer). Mở PR KHÔNG auto-merge — chờ người chốt (red-zone)."]
```

GAP-ANALYSIS (2026-07-03, fresh decomp — no prior docs/plans/S2-AUTH-DB-4.md): 'reset-2fa' chỉ xuất hiện ở harness/backlog.mjs + docs/STATUS.md (chính WO này) — KHÔNG có trong bất kỳ migration/code nào → pair GENUINELY MỚI. Hạ tầng 2FA hiện có 2 TẦNG: (1) role-level roles.requires_two_factor (mig 0120, seed company-admin=true); (2) company-policy two_factor_enforced (security_policies, CS-9). WO này thêm TẦNG THỨ 3 = per-USER users.require_two_factor + quyền admin reset-2fa:user.

INVARIANTS: #1 users đã có RLS+FORCE (migration-smoke RLS_REQUIRED_TABLES) → ADD COLUMN thuần additive, TUYỆT ĐỐI không đụng policy/grant/FORCE users. #2 role_permissions app-role không có UPDATE (mig 0005) → grant chỉ INSERT ON CONFLICT; pair mới nên không cần DELETE-wrong-scope như 0444. #3 không secret — không liên quan (chỉ boolean flag). UNIQUE role_permissions=(role_id,permission_id,effect) KHÔNG gồm data_scope (memory §13) → ON CONFLICT (role_id,permission_id,effect).

HOT-FILE & MERGE: permissions/role_permissions = seed UNION ON CONFLICT DO NOTHING (append). meta/_journal.json = append 1 entry idx 146/when 1717500725000 (đơn điệu > head 0465 idx 145/1717500720000) — builder PHẢI re-verify head trong worktree (nhánh WIP song song có thể đã lấn 0466). schema/index.ts KHÔNG cần sửa (đã `export * from './users'`). Chỉ users.ts đổi (thêm requireTwoFactor). SQL tay + journal tay theo pattern 04xx — KHÔNG drizzle-kit generate.

VERIFY: chạy trên LANE_DB cô lập (bash scripts/lane-db-setup.sh authdb4 → export LANE_DB=mediaos_authdb4) vì drizzle migrator áp đơn điệu theo when → DB dev chung sẽ skip band thấp = xanh-giả. Test gate hasDb && LANE_DB (KHÔNG bare skipIf — .env local làm hasDb=true chạm DB chung = đỏ-giả).

GATE: FULL (database-reviewer + security-reviewer) vì chạm migration + permission/RLS-adjacent + auth. PR KHÔNG auto-merge (red-zone) — owner chốt (owner đã chốt phạm vi 2026-07-03, vẫn cần chốt-merge PR).

OUT-OF-SCOPE (KHÔNG làm ở WO này — paths chỉ migrations/** + schema/**): (a) users.require_two_factor là cột INERT cho tới khi WO backend tương lai nối nó vào TwoFactorEnforcementGuard.mustHaveTwoFactor (hiện = companyEnforced || roleRequired; sẽ thêm || userRequired) — KHÔNG sửa guard/service/auth ở đây. (b) endpoint admin reset-2fa:user (vd POST /auth/users/:id/reset-2fa xoá user_totp/user_recovery_codes + set require_two_factor) là WO backend riêng — pair chỉ được SEED ở đây để sẵn sàng. (c) cập nhật docs/permission-matrix-spec.md + docs/spec pin cặp reset-2fa:user = follow-up doc (paths không gồm docs/).

FLAG PATHS: migration-smoke.int-spec.ts nằm ở apps/api/test/** — NGOÀI 2 path khai báo của WO (migrations/** + schema/**). guard-scope sẽ cảnh báo khi builder sửa test. Khuyến nghị: coi việc mở rộng migration-smoke là testTask do Đội 2 viết (Đội 3 verify); nếu executor cần đưa test vào lane thì nới WO.paths thêm apps/api/test/integration/migration-smoke.int-spec.ts (cảnh báo guard-scope chấp nhận được cho file test verify chính migration này).

LANE STRUCTURE: toàn WO = 1 lane db-migration SERIAL duy nhất (migration SQL + journal + drizzle users.ts) — schema edit gộp cùng lane vì cùng builder db-migration, non-conflicting, phải nhất quán với SQL; không tách để tránh split-brain.
