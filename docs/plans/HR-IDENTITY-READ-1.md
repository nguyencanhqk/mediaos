```yaml
wo: HR-IDENTITY-READ-1
zone: red
generated_by: auto-loop
reconciled_at: "7d77e953"
lanes: [{"id":"lane-mig-identity-catalog","task":"Migration 0494 (crown/red — permission catalog seed) NỐI TIẾP head thật. INSERT INTO permissions (action,resource_type,is_sensitive) VALUES ('view-identity','employee',true) ON CONFLICT (action,resource_type) DO NOTHING. TUYỆT ĐỐI KHÔNG INSERT role_permissions (mirror tiền lệ view-salary/0444: pair-only, tránh blanket-grant drift). THUẦN DATA — không DDL, không RLS/FORCE, không backfill company_id (permissions là catalog GLOBAL không company_id — mirror 0492/0444/0481), không db:generate. Idempotent. KIỂM head THẬT origin/master trước khi mint (0493→0494; bẫy double-mint).","builder":"db-migration","paths":["apps/api/migrations/**"]},{"id":"lane-be-identity-reveal","task":"BE reveal identity_* fail-closed + audit-on-reveal + nâng gate change-request. (1) contracts: thêm identityNumber/identityIssueDate/identityIssuePlace = z.string().nullable() vào hrEmployeeDetailSchema (hrMeProfileSchema kế thừa); KHÔNG thêm vào list-item. (2) hr-read.repository: DETAIL_COLUMNS + HrDetailRow thêm 3 cột identity_* (raw); LIST_COLUMNS/HrListRow GIỮ NGUYÊN. (3) hr-read.service: hàm revealIdentity() mirror revealSalary — can({action:'view-identity',resourceType:'employee',resourceId,isSensitive:true}); reveal = allow && auditRequired; reveal ⟹ audit.record(tx,{action:'view-identity',objectType:'employee',objectId:targetId,actorUserId}) TRONG CÙNG tenant tx (atomic, rollback nếu audit fail); toDetail chiếu identity_* theo revealIdentity, else null. (4) profile-change-request.service: đổi cổng duyệt field §14.18 từ view-sensitive → view-identity (permissionCode audit HR.EMPLOYEE.VIEW_IDENTITY, message + Denied audit cập nhật). (5) permission.service: APPEND 'view-identity:employee' vào SENSITIVE_CAPABILITY_ALLOWLIST. (6) deny-path int-spec colocated (src/employees/**, src/permission/**).","builder":"backend-builder","paths":["packages/contracts/src/hr/**","apps/api/src/employees/**","apps/api/src/permission/**","apps/api/test/integration/**"]},{"id":"lane-fe-identity-group","task":"FE render nhóm Giấy tờ (CMND/CCCD) chỉ khi có capability view-identity. HR constants: thêm HR_PERMS.EMPLOYEE.VIEW_IDENTITY='HR.EMPLOYEE.VIEW_IDENTITY' + HR_ENGINE_PAIRS.VIEW_IDENTITY={action:'view-identity',resourceType:'employee'}. profile-sections.tsx: thêm prop canViewIdentity + SectionCard 'Giấy tờ' render identityNumber/identityIssueDate/identityIssuePlace qua pii(value,canViewIdentity,masked) — TÁCH khỏi canViewSensitive. employee-profile-panel.tsx (+ EmployeeDetailPage nếu render section trực tiếp): useCan(view-identity,employee) → truyền canViewIdentity. Server đã mask (null) nên FE chỉ ẩn nhãn — không phải cổng thật. web-core registry: CHỈ sửa nếu cần key mapping (useCan đọc pair 'view-identity:employee' trực tiếp từ /auth/me caps — thường KHÔNG cần edit). i18n label CCCD.","builder":"frontend-builder","paths":["apps/app/src/routes/**","packages/web-core/src/**"]}]
acceptanceChecks: ["Migration 0494 seed ĐÚNG 1 cặp ('view-identity','employee',is_sensitive=true) vào permissions qua ON CONFLICT(action,resource_type) DO NOTHING; grep migration = 0 dòng INSERT INTO role_permissions (pair-only, mirror view-salary/0444); nối tiếp head thật (origin/master=0493→0494); idempotent (chạy lại 0 hàng). [done_when a,b]","hrEmployeeDetailSchema + hrMeProfileSchema có identityNumber/identityIssueDate/identityIssuePlace = z.string().nullable(); hrEmployeeListItemSchema KHÔNG có identity_*; pnpm --filter @mediaos/contracts build + typecheck xanh. [done_when c]","hr-read.repository DETAIL_COLUMNS + HrDetailRow gồm identity_number/identity_issue_date/identity_issue_place (raw); LIST_COLUMNS/HrListRow không đổi (list không mang identity). [done_when c]","hr-read.service reveal identity_* CHỈ khi can('view-identity','employee',resourceId,isSensitive:true) allow && auditRequired; else null (fail-closed); wildcard *:* KHÔNG mở (isSensitive→exact-only); reveal ⟹ đúng 1 audit_logs row action='view-identity' objectType='employee' objectId=per-row TRONG CÙNG tenant tx (audit fail ⇒ rollback read). [done_when c + 'Reveal ⟹ audit']","profile-change-request.service: duyệt request chạm PROFILE_CHANGE_SENSITIVE_FIELDS yêu cầu view-identity (không còn view-sensitive); thiếu ⇒ 403 + audit resultStatus=Denied, permissionCode=HR.EMPLOYEE.VIEW_IDENTITY, sensitivityLevel=Sensitive, cùng tx. [done_when #3]","permission.service SENSITIVE_CAPABILITY_ALLOWLIST append 'view-identity:employee'; /auth/me surface caps['view-identity:employee']=true CHỈ khi user có grant per-user THẬT; user không grant ⇒ vắng; user chỉ có wildcard *:* ⇒ vắng (sensitive không kế thừa). [done_when #5]","FE: HR_ENGINE_PAIRS.VIEW_IDENTITY tồn tại; profile-sections render nhóm Giấy tờ (identity_number/issue_date/issue_place) gated useCan('view-identity','employee'); không cap ⇒ nhãn masked; server null tôn trọng. [done_when #5]","FULL gate security-reviewer + database-reviewer PASS; deny-path + auth-me identity-cap int-spec CHẠY với hasDb && LANE_DB (không skip, không xanh-giả); DoD §8: audit-on-reveal + permission guard + test + không phá luồng chính + cập nhật backlog. [done_when #5 + DoD]"]
testTasks: ["RED deny-path (BE, colocated apps/api/src/employees/hr-identity-read.int.spec.ts, LANE_DB): caller có read:employee nhưng KHÔNG có view-identity → getHrEmployee + getMyProfile trả identityNumber/identityIssueDate/identityIssuePlace = null. Assert RED trước impl.","RED tách-quyền: caller có view-sensitive:employee (nhóm PII cũ) nhưng KHÔNG có view-identity → identity_* vẫn null (view-identity ≠ view-sensitive; PII thường lộ, giấy tờ KHÔNG).","RED wildcard: caller grant '*:*' → identity_* vẫn null (is_sensitive exact-only, mirror salary).","RED cross-tenant: caller company B xin employee id thuộc company A → 404, không rò identity + không ghi audit reveal.","GREEN reveal+audit: plant grant per-user view-identity:employee → identity lộ ĐÚNG giá trị + đúng 1 audit_logs (action view-identity, object employee, resourceId per-row) trong cùng tx; giả lập audit fail ⇒ read rollback (atomicity).","RED→GREEN change-request approve (colocated profile-change-request int/service spec): approver có approve:profile-change-request + view-sensitive nhưng KHÔNG view-identity duyệt request chạm identity field → 403 + audit Denied; sau khi grant view-identity → duyệt thành công, áp field, history append masked.","auth-me cap proof (colocated src/permission hoặc src/employees .int.spec.ts, gate hasDb && LANE_DB, mirror auth-me-capabilities.int.spec): user grant view-identity per-user ⇒ caps['view-identity:employee']===true; user không grant ⇒ vắng; user chỉ wildcard ⇒ vắng.","FE vitest (profile-sections/employee-profile-panel .spec.tsx): nhóm Giấy tờ ẩn/masked khi useCan(view-identity)=false; hiện giá trị khi =true và server trả non-null."]
steps: ["1. lane-mig-identity-catalog (NỐI TIẾP, chạy TRƯỚC): re-verify head origin/master (0493) → mint 0494 seed cặp view-identity:employee is_sensitive=true, ON CONFLICT DO NOTHING, KHÔNG role-grant. Áp migration lên LANE_DB cô lập.","2. lane-be-identity-reveal (depends_on lane-mig): RED deny-path trước (thiếu quyền→null, view-sensitive KHÔNG mở identity, wildcard KHÔNG mở, cross-tenant deny) → GREEN: contracts DTO + repo DETAIL_COLUMNS + revealIdentity+audit + toDetail + nâng gate change-request + append allowlist. FULL gate (security-reviewer + database-reviewer + silent-failure-hunter).","3. lane-fe-identity-group (depends_on lane-be — cần contracts + pair): rebuild contracts trước; thêm pair + section Giấy tờ gated canViewIdentity; spec FE ẩn/mask khi thiếu cap.","4. Verify như CI: bash harness/check.sh --lane-db (deny-path/IDOR/auth-me chạy THẬT, không skip); đối chiếu baseline ~31 int-spec env-red trước khi quy đỏ.","5. PR red-zone cắt từ origin/master; FULL gate PASS + owner duyệt red-zone trước merge (pair mới + audit + fail-closed)."]
```

GAP ANALYSIS (code hiện tại): identity_* (identity_number/identity_issue_date/identity_issue_place — schema employees.ts:74-76, mig 0451) CHỦ ĐÍCH bị loại khỏi read surface: hrEmployeeDetailSchema (employee-read.ts:141-142) và DETAIL_COLUMNS (hr-read.repository.ts:78) đều ghi chú 'identity_* never projected — needs owner decision'. Owner ĐÃ CHỐT 2026-07-12. WO này mở khoá qua cặp MỚI view-identity:employee. Tiền lệ tái dùng: revealSalary (hr-read.service.ts:255-275) = mẫu chuẩn reveal⟹audit-atomic + isSensitive→exact-only; 0492/0444/0481 = mẫu seed catalog pair-only (không role-grant); export:attendance trong SENSITIVE_CAPABILITY_ALLOWLIST + auth-me-capabilities.int.spec = mẫu chứng minh /auth/me surface (bài học HR-PROFILE-UI-1/CAP-2: thiếu allowlist ⇒ cap bị lọc, nút ẩn cả với người có grant thật).

INVARIANTS (CLAUDE.md §2): #1 mọi read qua db.withTenant(companyId) + scope predicate — reveal identity KHÔNG nới cổng tenant/scope, chỉ mở field. #2 audit append-only: reveal ghi audit_logs cùng tenant tx (objectType 'employee' đã hợp lệ mig 0014 — KHÔNG cần audit object_type migration; action 'view-identity' là free-text như 'view-salary'). #3 fail-closed: is_sensitive=true ⇒ PermissionService chỉ thoả EXACT grant, wildcard *:* KHÔNG kế thừa; thiếu quyền ⇒ null (mask SERVER-side, client không nhận). Change-request history vẫn mask identity_number qua AuditMasker (BẤT BIẾN #3) — KHÔNG đổi.

MIGRATION: head thật origin/master = 0493 (đã đối chiếu git ls-tree) → 0494. permissions là catalog GLOBAL không company_id ⇒ KHÔNG RLS/FORCE, KHÔNG backfill company_id, KHÔNG DDL, KHÔNG db:generate — câu hỏi 'RLS+FORCE trước backfill' = N/A cho WO này (không đụng bảng tenant). Migrator chạy DATABASE_DIRECT_URL (role owner) nên INSERT catalog trực tiếp; app role chỉ SELECT. Bẫy double-mint (WO cảnh báo 'migration DUY NHẤT đang bay') ⇒ re-verify head origin/master ngay trước mint.

VERIFY: chạy như CI bash harness/check.sh --lane-db (deny-path/IDOR/cross-tenant/auth-me THỰC THI — gate hasDb && LANE_DB, không đủ chỉ skipIf(!hasDb)); .env làm hasDb=true nên thiếu LANE_DB ⇒ đỏ-giả. Đối chiếu baseline ~31 int-spec env-red (Vault/KMS/2FA/mail) trước khi quy đỏ. Trước PR red-zone: check.sh --all (REQUIRE_LANE_DB) ⇒ exit 1 nếu int-spec skip vượt ngưỡng.

GATE: FULL (security-reviewer + database-reviewer + silent-failure-hunter, + santa-method cho reveal/audit crown) cho lane-mig + lane-be (permission/audit/migration). lane-fe = LIGHT (react-reviewer + typescript-reviewer + quality-gate) — nhấn: FE chỉ ẩn nhãn, cổng THẬT là server-mask; reviewer xác nhận không có identity_* rò qua client-only gate.

OUT-OF-SCOPE (chống scope-creep): KHÔNG role-grant view-identity cho bất kỳ role nào (owner chốt — grant per-user về sau). KHÔNG endpoint reveal riêng (inline detail). KHÔNG thêm identity vào list-item/summary/export. KHÔNG đổi masking PII thường (view-sensitive giữ nguyên). KHÔNG mã hoá at-rest identity (Phase sau nếu cần). LƯU Ý VẬN HÀNH (không phải bug): sau khi nâng gate duyệt change-request lên view-identity, HR/company-admin sẽ KHÔNG duyệt được request chạm giấy tờ cho tới khi được grant view-identity per-user — đây là siết fail-closed CÓ CHỦ ĐÍCH (owner đã chốt nhất quán read gate).

WEB-CORE: useCan đọc caps['view-identity:employee'] trực tiếp từ /auth/me — thường KHÔNG cần sửa PERMISSION_CODE_TO_PAIR; chỉ chạm packages/web-core nếu có nav/screen-entry mới cần key mapping.
</content>
