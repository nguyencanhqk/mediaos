```yaml
wo: S2-HR-EMPFILE-1
zone: red
generated_by: auto-loop
reconciled_at: "f613f08"
lanes: [{"id":"s2hrempfile1-mig","task":"Migration 0477 (NỐI TIẾP head 0476): seed permission catalog 3 cặp ('file-view','employee')+('file-upload','employee')+('file-delete','employee') is_sensitive=false (ON CONFLICT(action,resource_type) DO NOTHING) + role_permissions grants hr/company-admin → data_scope='Company' cho cả 3 cặp, theo mẫu per-pair DELETE-wrong-scope+INSERT của mig 0462 (RAISE nếu role canonical/permission chưa có). KHÔNG tạo bảng mới, KHÔNG đụng RLS employee_profiles, KHÔNG đụng CHECK audit_logs.object_type (tái dùng 'file'/'file_link' đã seed mig 0440). Manager/Employee KHÔNG grant (deny→403).","builder":"db-migration","paths":["apps/api/migrations/**"]},{"id":"s2hrempfile1-be","task":"BE Employee File trong employees module + contracts DTO + int-spec RED. (1) contracts: linkEmployeeFileSchema {fileId,(category?)}, listEmployeeFilesQuerySchema, EmployeeFileDto (compose từ FileMetadataDto: linkId,fileId,originalName,mimeType,sizeBytes,scanStatus,uploadStatus,uploadedAt,category?); tái dùng DownloadUrlDto. (2) employee-file.controller.ts routes /hr/employees/:id/files: POST (link, @RequirePermission('file-upload','employee')), GET list + GET :fileId (metadata) + GET :fileId/download (@RequirePermission('file-view','employee')), DELETE :fileId soft (@RequirePermission('file-delete','employee')); class-level PermissionGuard fail-closed. (3) employee-file.service.ts: assertReadScope/assertWriteScope IDOR trên employee (dataScope.resolveAndAssert cặp file-* + resolveContext + load employee_profile scope-target + isEmployeeInScope → cross-tenant/out-scope 404, mirror ContractService/HrReadService); POST → FileService.link(user,{fileId,moduleCode:'HR',entityType:'employee_profile',entityId,linkType:'Document',accessScope:'Company'}); download → scan-guard CHẶT (chỉ scanStatus∈{Clean,NotRequired} mới cấp URL, còn lại 409) TRƯỚC khi gọi FileService.getDownloadUrl; DELETE → FileService.deleteFile (soft). (4) employee-file.resolver.ts EmployeeFileResolver (moduleCode='HR', entityTypes=['employee_profile']) canView/DownloadFile→scope('file-view','employee'), canLinkFile→('file-upload','employee'), canDelete/UnlinkFile→('file-delete','employee') + isEmployeeInScope theo entityId(=employeeId); đăng ký vào FilePolicyService qua EmployeesModule.onModuleInit (APPEND, không rewrite registry). (5) employee-file.repository.ts: list file_links⋈files WHERE module_code='HR' AND entity_type='employee_profile' AND entity_id=:employeeId AND file_links.deleted_at IS NULL AND files.deleted_at IS NULL (soft-delete loại khỏi list). (6) wire employees.module.ts additive.","builder":"backend-builder","paths":["apps/api/src/employees/**","packages/contracts/src/**","apps/api/test/integration/**"]}]
acceptanceChecks: ["Route đúng API-03 HR-API-801..805 dưới /hr/employees/:id/files: POST (link file), GET (list), GET :fileId (metadata), GET :fileId/download (302/URL), DELETE :fileId (soft); mỗi route có @RequirePermission đúng cặp: POST=('file-upload','employee'), GET*=('file-view','employee'), DELETE=('file-delete','employee').","Migration 0477 seed ĐÚNG 3 cặp permission (file-view/file-upload/file-delete, employee) + grant hr & company-admin data_scope='Company'; ON CONFLICT DO NOTHING idempotent; KHÔNG có ALTER ... employee_profiles RLS/POLICY, KHÔNG CREATE TABLE, KHÔNG sửa CHECK audit_logs.object_type.","Mọi read/write đi qua db.withTenant(companyId); file_links tạo với module_code='HR', entity_type='employee_profile', entity_id=employeeId; company_id do RLS+FORCE ép (BẤT BIẾN #1).","IDOR đóng: assertReadScope/assertWriteScope resolve data_scope cặp file-* + isEmployeeInScope trên employee_profile owner; employee cross-tenant hoặc ngoài scope → 404 (KHÔNG 403-after-200, KHÔNG lộ tồn tại); fileId không thuộc employee :id → 404.","EmployeeFileResolver đăng ký vào FilePolicyService (onModuleInit) cho (HR, employee_profile) — không còn deny-no-resolver; canView/Download↔file-view, canLink↔file-upload, canDelete/Unlink↔file-delete; resolver chỉ nhận FilePermissionInput (không storage_path/checksum/binary — BẤT BIẾN #2.3).","Download tôn trọng scan_status: file scanStatus∈{Pending,Infected,Failed} → 409 (không cấp URL); chỉ {Clean,NotRequired} mới trả signed-url TTL-ngắn; đồng thời FileService state-guard chặn Infected/not-Uploaded.","file_access_logs append-only ghi Link/Download/Delete (grant + deny) qua FileService/FileAccessLogService trong tx nghiệp vụ; audit FileLinked/FileDeleted (object_type 'file_link'/'file') ghi CÙNG tx, KHÔNG log nội dung/byte/storage_path file.","Soft-delete loại khỏi list: sau DELETE, GET list KHÔNG còn file (query lọc file_links.deleted_at IS NULL AND files.deleted_at IS NULL); không hard-delete (BẤT BIẾN #2).","DoD §8: input validate (zod DTO contracts), permission guard, FE-agnostic error envelope chuẩn, có int-spec ≥ ngưỡng nhạy cảm, FULL gate (security-reviewer + database-reviewer) PASS, cập nhật harness/backlog.mjs.","typecheck + build + pnpm --filter @mediaos/api test XANH trên LANE_DB=mediaos_s2hrempfile1 (0477 đã áp)."]
testTasks: ["RED deny-path (QA-05): user KHÔNG có file-view/file-upload/file-delete:employee → GET/POST/DELETE trả 403 (PermissionGuard fail-closed); manager/employee mặc định 403.","RED IDOR cross-tenant: file/employee của công ty B → user công ty A gọi GET list/metadata/download/DELETE → 404 (RLS 0-row + isEmployeeInScope false); KHÔNG lộ tồn tại.","RED IDOR cross-employee: fileId đã link vào employee X, gọi qua /hr/employees/{Y}/files/{fileId} → 404 (file không thuộc employee Y).","RED scan_status: file scanStatus='Pending' (và 'Infected') → GET :fileId/download → 409 NOT_DOWNLOADABLE, KHÔNG cấp URL; file 'Clean'/'NotRequired' → 200/302 có URL.","RED soft-delete: sau DELETE :fileId → GET list KHÔNG còn file đó; file row/link vẫn còn (deleted_at set) — chứng minh không hard-delete.","access-log append-only (QA-06): DELETE/Download ghi 1 dòng file_access_logs (grant + nhánh deny có denied_reason); thử UPDATE/DELETE trên file_access_logs bị chặn (REVOKE).","happy-path hr/company-admin (Company scope): POST link fileId đã upload+confirm → 201/200; GET list thấy file; GET download (scan Clean) → URL; DELETE → 204 + rời list.","QA-04 contract: response envelope chuẩn + mã lỗi (403 forbidden, 404 not-found, 409 not-downloadable) đúng shape; int-spec chạy dưới LANE_DB cô lập (skipIf !(hasDb && LANE_DB))."]
steps: ["MIG (lane s2hrempfile1-mig) NỐI TIẾP TRƯỚC: xác nhận head hiện tại (docs/STATUS.md; hiện 0476) → tạo 0477_s2_hrempfile1_employee_file_perms.sql. INSERT permissions 3 cặp ('file-view'|'file-upload'|'file-delete','employee') ON CONFLICT DO NOTHING; DO-block seed role_permissions hr+company-admin → 'Company' per-pair (DELETE scope-sai + INSERT, clone khối D mig 0462). KHÔNG RLS/bảng/audit-CHECK.","contracts (lane BE): thêm employee-file schemas vào packages/contracts/src (linkEmployeeFileSchema, listEmployeeFilesQuerySchema, employeeFileDtoSchema) + export; build contracts trước (turbo).","BE resolver: viết EmployeeFileResolver mirror hr-contract-file.resolver.ts nhưng pair file-view/file-upload/file-delete + entity 'employee_profile'; dùng employee scope-target (hr-read.repository.findByIdTx hoặc thêm findScopeTargetTx theo employeeId).","BE repository: employee-file.repository.ts — insert KHÔNG cần (link qua FileService); chỉ cần listByEmployeeTx (file_links⋈files) + findLinkedFileTx (1 file theo employeeId+fileId, để metadata/download/scan-guard, đảm bảo file THUỘC employee này — chống nhầm fileId của employee khác).","BE service: employee-file.service.ts — assertWriteScope (POST/DELETE) + assertReadScope (GET) trên employee; POST=FileService.link; GET list=repo.listByEmployeeTx; GET download=findLinkedFile → scan-guard chặt → FileService.getDownloadUrl; DELETE=FileService.deleteFile. Không tự ghi audit object_type mới — FileService đã audit FileLinked/FileDeleted (append-only) + access log Link/Download/Delete.","BE controller + module: employee-file.controller.ts (5 route HR-API-801..805) + wire employees.module.ts (controller+providers+onModuleInit.registerResolver, APPEND vào khối S2-FND-BE-4 hiện có).","RED-first int-spec trong apps/api/test/integration/ (skipIf !(hasDb && LANE_DB)): deny thiếu quyền 403 · cross-tenant/cross-employee 404 (IDOR) · download scan Pending/Infected 409 · soft-delete loại khỏi list · happy-path hr Company. Viết test TRƯỚC (đỏ) → implement → xanh.","Verify DB cô lập: bash scripts/lane-db-setup.sh s2hrempfile1 → export LANE_DB=mediaos_s2hrempfile1 → pnpm --filter @mediaos/api test (đảm bảo 0477 áp trên lane DB, tránh xanh/đỏ-giả). FULL gate security-reviewer + database-reviewer."]
```

GAP-ANALYSIS (fresh plan, chưa có docs/plans/S2-HR-EMPFILE-1.md): Toàn bộ hạ tầng đã sẵn — chỉ thiếu bề mặt HR-specific + seed quyền. Tái dùng NGUYÊN pattern đã ship: ContractService.linkFile + HrContractFileResolver (S2-FND-BE-4) là bản sao trực tiếp cho (module='HR', entity='employee_profile'). FileService (apps/api/src/foundation/files/files.service.ts) đã lo upload/link/getDownloadUrl/deleteFile + audit + access-log + withTenant. FilePolicyService dispatch theo resolver. => WO này = (a) migration seed quyền, (b) controller+service+resolver+repo trong employees module + contracts DTO. KHÔNG bảng employee_files (WO chọn generic file_links; DB-03 §7.8 cho 2 lựa chọn, chọn file_links).

PIN CẶP QUYỀN (chống pair-drift, memory s1-fnd-module): engine gate trên (action,resource_type), KHÔNG phải code hiển thị. SPEC-03 §6.8 / API-03 HR-API-801..805 dùng code HR.EMPLOYEE.FILE_VIEW/UPLOAD/DELETE → map engine pair: ('file-view','employee') · ('file-upload','employee') · ('file-delete','employee'). Grant seed: hr + company-admin → data_scope 'Company' (khớp SPEC access-matrix: Admin/HR=Có, Manager='Không mặc định', Employee='Không'). SA=runtime System (không seed). data_scope là PER-(permission,role) → per-pair DELETE-wrong-scope+INSERT (memory §13). resolveAndAssert/scope check dùng CHÍNH cặp file-* (không mượn read:employee) để scope đúng ngữ nghĩa quyền file.

ENTITY-TYPE reconcile (SPEC vs code): DB-03 §7.8 gợi entity_type='Employee' / FK employees.id, NHƯNG bảng 'employees' KHÔNG tồn tại (đã reconcile sang employee_profiles ở mig 0462, memory s2-hr-be2). WO PIN entity_type='employee_profile' (lowercase) + entity_id=employee_profiles.id. Giữ nhất quán call-site↔resolver (bài học pair-drift 'EmployeeContract' hụt).

INVARIANTS: #1 mọi query withTenant + RLS+FORCE trên files/file_links (mig 0433) — KHÔNG đụng RLS employee_profiles. #2 soft-delete (FileService.deleteFile) + file_access_logs/audit_logs append-only (FileService đã ghi FileLinked/FileDeleted object_type 'file_link'/'file' — đã trong CHECK mig 0440, KHÔNG cần sửa CHECK). #2.3 KHÔNG lộ storage_path/checksum/byte ra DTO/log; resolver chỉ nhận FilePermissionInput.

DIVERGENCE có chủ đích cần builder biết: FileService.getDownloadUrl hiện CHỈ chặn scanStatus='Infected' (comment: AV chưa wire, default NotRequired). WO yêu cầu 'không trả file chưa quét sạch' NGHIÊM hơn → employee-file.service THÊM scan-guard (chỉ Clean/NotRequired mới cấp URL; Pending/Infected/Failed→409) TRƯỚC khi gọi getDownloadUrl. FileService (foundation, NGOÀI paths lane) KHÔNG sửa — guard đặt trong employees/**.

DESIGN DECISION (byte-upload): POST /hr/employees/:id/files nhận {fileId} của file ĐÃ register+PUT+confirm qua Foundation (/foundation/files/upload → PUT → confirm), rồi LINK entity_type='employee_profile' — y hệt ContractService.linkFile (proven, không thêm route confirm). HR/Admin cần grant upload:foundation-file (seed mig 0435, đã dùng cho contract). Builder CÓ THỂ mở rộng register-passthrough sau, nhưng acceptance = link+list+download+soft-delete.

READ-side: FileLinkRepository chỉ có listByFileTx (theo fileId) — CHƯA có listByEntity. FileService/foundation NGOÀI paths lane → thêm employee-file.repository.ts trong employees/** (join file_links⋈files theo entity). findLinkedFileTx(employeeId,fileId) để chống cross-employee IDOR + lấy scanStatus cho guard.

VERIFY/GATE: DB cô lập bắt buộc (memory integration-test LANE_DB) — 0477 phải áp trên mediaos_s2hrempfile1, chạy DB dev chung sẽ đỏ-giả. Int-spec skipIf !(hasDb && LANE_DB) + để ở apps/api/test/integration/*.int-spec.ts (vitest include đã bắt). FULL gate (red zone): security-reviewer + database-reviewer + typescript-reviewer + quality-gate.

OUT-OF-SCOPE (chống scope-creep): KHÔNG bảng employee_files; KHÔNG FE (FRONTEND-08 màn 'Employee Files' là WO khác); KHÔNG AV/scan pipeline thật (chỉ tôn trọng scan_status hiện có); KHÔNG đổi FileService/foundation; KHÔNG thêm is_sensitive gate view-sensitive per-file (DB-03 §7.8 rule 2 — enhancement Phase sau, WO core dùng file-view scope). Migration CHỈ seed quyền, KHÔNG media/finance.
</content>
