```yaml
wo: S4-TASK-DB-1
zone: red
generated_by: auto-loop
reconciled_at: "4b4b29b"
lanes: [{"id":"s4taskdb1-mig","task":"Migration 0478 (nối tiếp 0477, idx 158): CREATE 5 bảng TASK mới (task_assignees·task_watchers·task_checklists·task_checklist_items·task_activity_logs) với company_id NOT NULL + ENABLE/FORCE RLS + POLICY tenant_isolation company_id TRƯỚC mọi backfill; task_activity_logs GRANT chỉ SELECT,INSERT (append-only). ALTER additive 4 bảng legacy (projects·tasks·project_members·task_comments) thêm CỘT MỚI nullable (task_status/task_priority/task_code/member_status/project_role/employee_id/project_status/project_priority/project_code + task_comments.deleted_at/deleted_by/created_by) — TUYỆT ĐỐI KHÔNG CHECK TitleCase lên cột legacy status/priority. Đăng ký 5 entry MỚI (mỗi entry có seedRow FK-valid) trong apps/api/test/integration/rls-registry.ts (KHÔNG phải src/db/schema).","builder":"db-migration","paths":["apps/api/src/db/schema/**","apps/api/migrations/**","apps/api/test/integration/**","docs/plans/S4-TASK-DB-1.md"]}]
acceptanceChecks: ["Migration 0478 (idx 158, when > 1717500780000, nối tiếp 0477) apply sạch 0000→head trên LANE_DB TRỐNG (migration-smoke xanh) VÀ trên DB có seed hàng legacy — KHÔNG FAIL ADD CONSTRAINT (Option-A evolve-additive)","5 bảng MỚI (task_assignees·task_watchers·task_checklists·task_checklist_items·task_activity_logs): id UUID gen_random_uuid() + company_id NOT NULL DEFAULT NULLIF(current_setting('app.current_company_id',true),'')::uuid + FK companies + audit cols (created/updated_at/by) + soft-delete (deleted_at/by cho bảng chính); ENABLE+FORCE RLS + POLICY tenant_isolation (USING+WITH CHECK GUC) TRƯỚC mọi backfill","task_activity_logs append-only (BẤT BIẾN #2): GRANT chỉ SELECT,INSERT cho mediaos_app — KHÔNG UPDATE/DELETE (khớp retention config retention.service.ts:58)","ALTER additive 4 bảng legacy: task_status/task_priority/project_role/member_status/project_status/project_priority/employee_id là CỘT MỚI nullable; CHECK MỚI cho phép NULL và CHỈ trên cột mới; GIỮ NGUYÊN tasks.priority(NOT NULL DEFAULT 'none' lowercase)/tasks.status('not_started')/projects.status('active')/projects.priority(nullable lowercase)/project_members.status('active'/'inactive') + mọi CHECK legacy — KHÔNG đụng","task_comments: ADD deleted_at/deleted_by/created_by (nullable, additive) TRƯỚC khi tạo index/partial-unique tham chiếu deleted_at (legacy chỉ có user_id + created_at, KHÔNG có deleted_at)","Partial-unique chống trùng member/assignee/watcher ACTIVE tham chiếu CỘT MỚI (vd member_status='Active') + guard employee_id IS NOT NULL (mirror ATT 0452 dòng 330-335); GIỮ legacy project_members_active_uq(company_id,project_id,user_id) WHERE deleted_at IS NULL LIVE làm guard writer cũ","project_code/task_code là cột MỚI + partial-unique (company_id,*_code) WHERE deleted_at IS NULL AND *_code IS NOT NULL; GIỮ legacy projects.code/identifier/last_task_sequence + tasks.sequence làm code-gen guard LIVE tới khi TASK-BE cut over","CHECK task_status(Todo/In Progress/In Review/Done/Cancelled) · task_priority(Low/Medium/High/Urgent) · project_role(Owner/Manager/Member/Viewer) CHỈ trên CỘT MỚI, cho phép NULL","FK dùng TÊN BẢNG THẬT: employee_profiles(id) (KHÔNG employees) · org_units(id) (KHÔNG departments) · users(id) · companies(id) · tasks(id) · projects(id); index my-tasks (assignee+status+due), Kanban theo project, activity-log theo task/project","RLS registry = apps/api/test/integration/rls-registry.ts (KHÔNG src/db/schema): thêm ĐÚNG 5 entry MỚI {name,table,seedRow async chèn 1 hàng FK-valid}; KHÔNG trùng projects(:677)/project_members(:717)/tasks(:912)/task_comments(:925) — 4 bảng cũ ALTER additive giữ seedRow legacy","rls-guards.int-spec (registry-dependent: mọi bảng company_id PHẢI có trong RLS_TABLES + ENABLE/FORCE) + rls-coverage-assert.int-spec (registry-independent: quét mọi bảng company_id ⇒ ENABLE+FORCE + policy đọc-USING & ghi-WITH CHECK theo GUC) đều PASS với 5 bảng mới","Khối '-- Down (manual)' liệt kê DROP 5 bảng mới + ALTER DROP COLUMN cột mới trên 4 bảng legacy","FULL gate database-reviewer + rls-tenant-isolation-tester PASS; DoD §8: migration đánh số tiếp, RLS+FORCE trước backfill, có test, cập nhật harness/backlog.mjs"]
testTasks: ["cross-tenant deny (RED): tenant A KHÔNG đọc/ghi được 5 bảng TASK mới của tenant B qua RLS (set_config app.current_company_id) — chạy trên LANE_DB cô lập","append-only task_activity_logs (RED): INSERT OK bằng app role; UPDATE/DELETE bằng app role bị DENY (thiếu GRANT)","unique-active guard (RED): INSERT 2 project_members ACTIVE cùng (company_id,project_id,employee_id) với member_status='Active' → vi phạm partial-unique mới (đo bằng employee_id + CỘT-STATUS-MỚI, KHÔNG cột legacy status)","legacy-row guard (RED — bắt lỗi mà smoke-DB-trống KHÔNG bắt): seed 1 hàng tasks legacy (priority='none', status='not_started') + projects (status='active', priority NULL) TRƯỚC khi apply constraint mới → migration KHÔNG FAIL; khẳng định enum legacy vẫn chạy, CHECK mới không đụng hàng legacy","migration-smoke 0000→head XANH trên LANE_DB cô lập (mediaos_s4taskdb1)","rls-guards.int-spec + rls-coverage-assert.int-spec XANH sau khi thêm 5 bảng mới + 5 entry registry (chống thủng lưới RLS im lặng)"]
steps: ["Đọc apps/api/test/integration/rls-registry.ts (:677 projects/:717 project_members/:912 tasks/:925 task_comments đã đăng ký + SHAPE entry {name,table,seedRow async}) + migration ATT 0452 (mẫu CREATE→ENABLE/FORCE→POLICY→index→GRANT + cột status MỚI nullable + backfill + §Down) + schema legacy media.ts(projects/project_members) & workflow.ts(tasks/task_comments) + DB-06 §4.3/§4.10/§5","Viết int-spec RED-trước (5 test bên dưới) trên LANE_DB cô lập: bash scripts/lane-db-setup.sh s4taskdb1 → export LANE_DB=mediaos_s4taskdb1 → pnpm --filter @mediaos/api test","Migration 0478 (idx 158, when > 1717500780000): CREATE 5 bảng mới (company_id NOT NULL DEFAULT NULLIF(current_setting('app.current_company_id',true),'')::uuid REFERENCES companies) → ENABLE+FORCE RLS → POLICY tenant_isolation (USING+WITH CHECK GUC) TRƯỚC mọi backfill; task_activity_logs GRANT SELECT,INSERT (append-only, KHÔNG UPDATE/DELETE)","ALTER additive 4 bảng legacy: thêm cột MỚI nullable; ADD task_comments.deleted_at/deleted_by/created_by TRƯỚC khi tạo index/partial-unique WHERE deleted_at IS NULL; CHECK MỚI cho phép NULL và chỉ trên CỘT MỚI; partial-unique cột mới + guard employee_id IS NOT NULL; GIỮ mọi CHECK/unique/giá trị legacy","Đăng ký 5 entry MỚI trong apps/api/test/integration/rls-registry.ts — MỖI entry có seedRow async chèn 1 hàng FK-valid (mirror shape projects/project_members); GIỮ nguyên seedRow legacy 4 bảng cũ; KHÔNG db:generate drop — migration viết tay additive","Thêm khối '-- Down (manual)' (DROP 5 bảng mới + ALTER DROP COLUMN cột mới trên 4 bảng legacy)","pnpm db:migrate + migration-smoke; chạy rls-guards.int-spec + rls-coverage-assert.int-spec (phải XANH với 5 bảng mới); FULL gate database-reviewer + rls-tenant-isolation-tester"]
```

RECONCILE-REFRESH của docs/plans/S4-TASK-DB-1.md (khối yaml) vs code hiện tại. Plan CÒN ĐÚNG về chiến lược (Option-A evolve-additive, đúc từ plan-block wf_4a728732 trước) nhưng có 1 lệch đường-dẫn nghiêm trọng + 1 yêu cầu bị bỏ sót → reused=false (cập nhật).

GAP #1 (SỬA — quan trọng nhất): Plan trước trỏ RLS registry ở `apps/api/src/db/schema/rls-registry.ts` — FILE NÀY KHÔNG TỒN TẠI. Registry THẬT ở `apps/api/test/integration/rls-registry.ts` (import bởi rls-guards.int-spec.ts qua `./rls-registry`). Line :677/:717/:912/:925 (projects/project_members/tasks/task_comments) ĐÚNG cho file này. Builder phải sửa ĐÚNG file test/integration, KHÔNG tạo file mới trong src/db/schema. Đường dẫn này đã nằm trong WO paths (apps/api/test/integration/**) nên không nới scope.

GAP #2 (BỔ SUNG): mỗi entry registry có SHAPE `{name, table, seedRow: async (direct,t)=>{ INSERT ... RETURNING id }}` — seedRow phải chèn 1 hàng FK-valid (mirror projects/project_members). 5 entry mới BẮT BUỘC có seedRow chèn được vào 5 bảng mới, nếu không rls-guards test đỏ. Hai guard ép: (a) rls-guards.int-spec = registry-DEPENDENT: bất kỳ bảng public có company_id mà THIẾU trong RLS_TABLES ⇒ test 'lưới không thủng im lặng' ĐỎ ⇒ builder BẮT BUỘC đăng ký đủ 5; (b) rls-coverage-assert.int-spec = registry-INDEPENDENT: quét pg_class/pg_policies mọi bảng company_id đòi ENABLE+FORCE + policy đọc(USING GUC) & ghi(WITH CHECK GUC hoặc ALL/UPDATE dùng USING ngầm) ⇒ 5 bảng mới phải có RLS thật.

XÁC NHẬN vs code (không đổi so plan trước): head migration = 0477 (journal idx 157, when 1717500780000) → 0478 idx 158, when > 1717500780000. projects/project_members ở media.ts (legacy: projects.status active/paused/archived, projects.priority nullable lowercase, projects.code/identifier/last_task_sequence; project_members.user_id NOT NULL, status active/inactive, project_members_active_uq(company_id,project_id,user_id) WHERE deleted_at IS NULL). tasks/task_comments ở workflow.ts (tasks.priority NOT NULL DEFAULT 'none' lowercase, tasks.status not_started..., tasks.sequence; task_comments CHỈ có user_id+created_at, KHÔNG có deleted_at/deleted_by/created_by → phải ADD trước index). 5 bảng mới (task_assignees/task_watchers/task_checklists/task_checklist_items/task_activity_logs) CHƯA tồn tại (task_activity_logs chỉ xuất hiện dạng chuỗi trong retention.service.ts:58 = ý định append-only nhất quán).

INVARIANTS (CLAUDE.md §2/§3/§9): #1 company_id NOT NULL + RLS+FORCE + POLICY TRƯỚC backfill (5 bảng mới) · #2 task_activity_logs append-only (GRANT SELECT,INSERT — KHÔNG UPDATE/DELETE) · KHÔNG hard-delete (soft-delete deleted_at) · migration đánh số ĐƠN ĐIỆU tiếp head · timestamptz UTC-at-rest · UUID PK · DDL viết TAY (RLS/GRANT/CHECK Drizzle không sinh được) — KHÔNG db:generate. Option-A evolve-additive (mirror ATT 0452): mọi enum TitleCase là CỘT MỚI nullable tên riêng, CHECK mới cho phép NULL và chỉ trên cột mới, GIỮ cột+CHECK+unique legacy LIVE ⇒ migration KHÔNG FAIL trên dev-online/prod có dữ liệu Plane lowercase (none/not_started/active). Lý do migration-smoke đơn thuần KHÔNG đủ: chạy LANE_DB TRỐNG (0 hàng) ⇒ ADD CONSTRAINT luôn thành công ⇒ xanh-giả; PHẢI có legacy-row guard test (seed hàng lowercase trước constraint).

VERIFY: LANE_DB cô lập bắt buộc (bash scripts/lane-db-setup.sh s4taskdb1 → export LANE_DB=mediaos_s4taskdb1). RED-trước 5 int-spec + 2 guard cũ (rls-guards, rls-coverage-assert) phải xanh. FK theo TÊN BẢNG THẬT: employee_profiles(id) KHÔNG employees, org_units(id) KHÔNG departments (DB-06 ghi employees/departments nhưng bảng đó KHÔNG tồn tại — SPEC/DB thắng tên nhưng ánh xạ sang quan hệ thật).

GATE: FULL (red-zone, crown: RLS/migration) → database-reviewer + rls-tenant-isolation-tester; migration red-zone chờ người chốt trước merge (không auto-commit). Model routing: crown-jewel → planner Sonnet5 xhigh (plan này) + IMPLEMENT/REVIEW Opus.

OUT-OF-SCOPE / DEFER (ghi rõ cho WO sau): KHÔNG dựng ở WO này — project_files, task_files, task_comment_mentions (DB-06 §3.1 'Nên có'), task_tags/* (phase sau). task_comment_mentions là điều kiện cho @mention → NOTI TASK_MENTIONED: S4-NOTI-BE + S4-TASK-BE (comment) phải biết bảng này CHƯA có. Cột legacy code-gen (projects.code/identifier/last_task_sequence, tasks.sequence) GIỮ LIVE tới khi TASK-BE cut over sang project_code/task_code — tránh 2 nguồn sinh mã. task_comments hiện GRANT (append-only ở comment schema) — WO này CHỈ ADD cột deleted_at additive; thay đổi grant/soft-delete-write để lại TASK-BE.
</content>
