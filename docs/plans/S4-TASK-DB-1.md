# Micro-plan — S4-TASK-DB-1 (TASK core schema + migration)

> Reconcile-refresh cho auto-loop. Bake các fix BLOCKING của plan-reviewer (run wf_4a728732).
> Nguyên tắc chốt: **Option-A evolve-additive** (mirror ATT `0452`) — KHÔNG add CHECK TitleCase lên cột legacy; mọi enum TitleCase là **cột MỚI nullable**.

```yaml
wo: S4-TASK-DB-1
zone: red
generated_by: hand-authored (post plan-block wf_4a728732)
reconciled_at: "9f4ca6d"
lanes: [{"id":"s4taskdb1-mig","task":"Migration 0478: CREATE 5 bảng TASK mới (task_assignees·task_watchers·task_checklists·task_checklist_items·task_activity_logs) với RLS+FORCE+policy company_id; ALTER additive 4 bảng legacy (projects·tasks·project_members·task_comments) thêm CỘT MỚI nullable — TUYỆT ĐỐI KHÔNG CHECK TitleCase lên cột legacy status/priority","paths":["apps/api/src/db/schema/**","apps/api/migrations/**","apps/api/test/integration/**"],"builder":"db-migration"}]
acceptanceChecks: ["Migration 0478 (đánh số tiếp 0477) apply sạch 0000→head trên LANE_DB TRỐNG (migration-smoke xanh) VÀ trên DB có seed hàng legacy (test riêng) — không FAIL ADD CONSTRAINT","5 bảng MỚI: id UUID gen_random_uuid() + company_id NOT NULL + audit cols (created/updated_at/by) + soft-delete (deleted_at/by) cho bảng chính; ENABLE + FORCE RLS + policy company_id TRƯỚC mọi backfill; đăng ký ĐÚNG 5 entry MỚI trong rls-registry.ts","task_activity_logs append-only: app role KHÔNG UPDATE/DELETE (grant chỉ INSERT/SELECT)","ALTER additive 4 bảng legacy: status/priority/project_role/employee_id là CỘT MỚI nullable; CHECK MỚI cho phép NULL và chỉ trên CỘT MỚI; giữ nguyên tasks.priority(NOT NULL DEFAULT 'none'), projects.priority, tasks.status='not_started', projects.status='active', project_members.status(lowercase 'active'/'inactive') — KHÔNG đụng CHECK/giá trị legacy","task_comments: ADD deleted_at/deleted_by/created_by (nullable, additive) TRƯỚC khi tạo index/partial-unique tham chiếu deleted_at","Partial-unique chống trùng member/assignee/watcher ACTIVE tham chiếu CỘT MỚI (vd member_status='Active') + guard employee_id IS NOT NULL (mirror ATT 0452 dòng 330-335); GIỮ legacy project_members_active_uq(company_id,project_id,user_id) LIVE làm guard writer cũ","project_code/task_code là cột MỚI + partial-unique (company_id,*_code) WHERE deleted_at IS NULL AND *_code IS NOT NULL; legacy projects.code/identifier/last_task_sequence giữ làm code-gen guard LIVE tới khi TASK-BE cut over","CHECK status (Todo/In Progress/In Review/Done/Cancelled) · priority (Low/Medium/High/Urgent) · project_role (Owner/Manager/Member/Viewer) chỉ trên CỘT MỚI, cho phép NULL","FK companies/users/employee_profiles/org_units (tên bảng THẬT — KHÔNG employees/departments); index my-tasks (assignee+status+due), project Kanban, activity log","rls-registry.ts: chỉ thêm 5 entry MỚI; KHÔNG thêm trùng projects(:677)/project_members(:717)/tasks(:912)/task_comments(:925) đã đăng ký — 4 bảng cũ ALTER additive giữ seedRow legacy","Khối '-- Down (manual)' liệt kê DROP 5 bảng mới + ALTER DROP COLUMN cột mới trên 4 bảng legacy","FULL gate database-reviewer + rls-tenant-isolation-tester PASS"]
testTasks: ["cross-tenant deny (RED): tenant A KHÔNG đọc/ghi 5 bảng TASK mới của tenant B qua RLS","append-only task_activity_logs (RED): INSERT OK; UPDATE/DELETE bằng app role bị DENY","unique-active guard (RED): INSERT 2 project_members ACTIVE cùng (company_id,project_id,employee_id) với member_status='Active' → vi phạm partial-unique (đo bằng employee_id + CỘT-STATUS-MỚI, KHÔNG cột legacy status)","legacy-row guard (RED — bắt lỗi smoke-DB-trống KHÔNG bắt): seed 1 hàng tasks legacy (priority='none', status='not_started') + projects (status='active', priority NULL) TRƯỚC khi apply constraint mới → migration KHÔNG FAIL; khẳng định enum legacy vẫn chạy, CHECK mới không đụng hàng legacy","migration-smoke 0000→head xanh trên LANE_DB cô lập"]
steps: ["Đọc apps/api/src/db/schema/rls-registry.ts (:677/:717/:912/:925 xác nhận 4 bảng đã đăng ký + seedRow legacy) + migration ATT 0452 (mẫu cột status MỚI nullable + khối §Down) + schema legacy tasks/projects","Viết int-spec RED-trước (5 test ở trên) trên LANE_DB cô lập (bash scripts/lane-db-setup.sh s4taskdb1 → LANE_DB=mediaos_s4taskdb1)","Migration 0478: CREATE 5 bảng mới + ENABLE/FORCE RLS + policy company_id TRƯỚC backfill; ALTER 4 bảng legacy thêm cột MỚI nullable; ADD deleted_at task_comments trước index; index/partial-unique/CHECK chỉ trên CỘT MỚI + guard employee_id IS NOT NULL","Đăng ký 5 entry rls-registry MỚI (giữ nguyên seedRow 4 bảng cũ); KHÔNG db:generate drop — migration viết tay additive","Thêm khối '-- Down (manual)'","pnpm db:migrate + migration-smoke; FULL gate database-reviewer + rls-tenant-isolation-tester"]
```

## Reconcile notes (prose)

**Bối cảnh block:** run trước, kế hoạch định add CHECK enum TitleCase (`IN ('Low','Medium',...)`) lên **chính cột legacy** `tasks.priority`/`tasks.status`/`projects.*`/`project_members.status`. Vì `migration-smoke` chạy trên LANE_DB **trống (0 hàng)** nên `ADD CONSTRAINT` thành công → gate xanh-giả, nhưng apply trên dev-online/prod (có dữ liệu Plane lowercase `none/not_started/active`) sẽ **FAIL toàn bộ migration**.

**Quyết định (Option-A evolve-additive, mirror ATT 0452):**
1. Mọi enum TitleCase (status/priority/project_role) là **CỘT MỚI nullable** với tên riêng (vd `task_status`, `task_priority`, `member_status`, `project_status`, `project_priority`, `project_role`). CHECK mới cho phép NULL, **chỉ trên cột mới**. Cột legacy status/priority **giữ nguyên** giá trị + CHECK cũ.
2. `project_members` reconcile: legacy định danh `user_id + role_in_project + status(lowercase)`; DB-06 dùng `employee_id + project_role + status(TitleCase)`. Thêm **cột mới** `employee_id → employee_profiles(id)`, `project_role`, `member_status`. **Giữ** unique legacy `project_members_active_uq (company_id,project_id,user_id)` LIVE làm guard chống-trùng cho writer cũ tới khi TASK-BE cut over. Unique mới theo `employee_id` chỉ forward-looking + guard `employee_id IS NOT NULL` (hàng legacy `employee_id` NULL → NULL-distinct không đụng).
3. `rls-registry.ts`: `projects`(:677)/`project_members`(:717)/`tasks`(:912)/`task_comments`(:925) **đã đăng ký** với seedRow cột legacy → **chỉ thêm 5 bảng mới**; ALTER 4 bảng cũ additive, seedRow legacy vẫn chạy.
4. `task_comments` legacy **thiếu** `deleted_at`/`created_by` → ADD (nullable) **trước** khi dựng index `WHERE deleted_at IS NULL`.
5. `project_code`/`task_code` là cột mới + partial-unique WHERE deleted_at IS NULL AND *_code IS NOT NULL (additive-an-toàn: legacy NULL không fire). Ghi rõ: `code`/`identifier`/`last_task_sequence` legacy vẫn là **code-gen guard LIVE** tới khi TASK-BE dùng `*_code` — tránh 2 nguồn sinh mã.

**Defer (ghi rõ để WO sau biết còn thiếu):** `project_files`, `task_files`, `task_comment_mentions` (DB-06 'Nên có') và `task_tags/*` (phase sau) **KHÔNG** dựng ở WO này. `task_comment_mentions` là điều kiện cho @mention → NOTI `TASK_MENTIONED`; S4-NOTI-BE/S4-TASK-BE-4 cần biết chưa có.

**Bất biến:** #1 RLS+FORCE trước backfill (5 bảng mới) · #2 audit `task_activity_logs` append-only (grant chỉ INSERT/SELECT) · migration additive, KHÔNG hard-delete.
