```yaml
wo: S3-ATT-BE-4
zone: red
generated_by: auto-loop
reconciled_at: "8427004"
lanes: [{"id":"S3-ATT-BE-4-MIG","task":"Migration 0457 NỐI TIẾP head reconcile attendance_adjustment_requests về canonical DB-04 §7.6: (a) reconcile status LOWERCASE→TitleCase (migrate rows cũ pending→Pending... rồi swap CHECK att_adj_status_check → chk_att_adj_requests_status Draft/Pending/Approved/Rejected/Cancelled) — CROWN, cần owner chốt stance reconcile; (b) thêm uq_att_adj_pending_employee_date_type UNIQUE(company_id,employee_id,work_date,request_type) WHERE deleted_at IS NULL AND status='Pending' (chống trùng request pending §7.6.1); (c) verify đủ cột §7.6 (0452 đã có — chỉ bổ nếu thiếu); (d) append audit object_types CHECK union nếu cần object_type mới cho ledger/recalc (append, KHÔNG rewrite); (e) drizzle schema/attendance.ts + audit.ts phản ánh canonical + adjustment_items. RLS+FORCE đã có — GIỮ; grant append-only adjustment_items (SELECT,INSERT) KHÔNG đổi. Idempotent.","builder":"db-migration","paths":["apps/api/migrations/**","apps/api/src/db/schema/**"]},{"id":"S3-ATT-BE-4-CONTRACTS","task":"Zod DTO canonical trong packages/contracts/src/attendance.ts: createAdjustmentRequestSchema (workDate, requestType ENUM 9 giá trị MISSING_CHECK_IN..OTHER, reason required, requestedCheckInAt/Out optional, items[] field-change optional, attachmentFileId optional, targetEmployeeId optional-cho-create-thay); adjustmentListQuerySchema (scope me|team|company, status filter TitleCase, pagination); approveAdjustmentSchema (note optional) + rejectAdjustmentSchema (reason/review_note REQUIRED §7.6.7); directAdjustSchema (recordId|workDate + field changes + reason); response DTO detail kèm items[]. Bám mã ATT-FUNC-018..022, cặp engine adjustment/attendance.","builder":"backend-builder","paths":["packages/contracts/src/**"]},{"id":"S3-ATT-BE-4-SVC","task":"Rebuild adjustment surface canonical trong apps/api/src/attendance/** (controller+service+repository+logic+mappers): routes POST /attendance/adjustment-requests (create-own:adjustment, resolve employee_id từ actor, guard create-thay→403 nếu thiếu capability), GET /adjustment-requests/my (view-own:adjustment), GET /adjustment-requests (view-team/view-company:adjustment qua DataScopeService, out-of-scope→404 no-leak, 403 nếu không grant), GET /adjustment-requests/:id (view-own|detail scope), POST :id/approve (approve:adjustment), POST :id/reject (reject:adjustment, reason bắt buộc), POST /attendance/records/:id/adjust-direct (adjust-direct:attendance). State-machine Pending→Approved/Rejected/Cancelled (Approved/Rejected terminal); row-lock FOR UPDATE chống double-approve→409; approve/adjust-direct → recalc attendance_records GIỮ attendance_logs (APPEND log_type='Adjustment', KHÔNG xoá/mất dữ liệu) + ghi attendance_adjustment_items(is_applied=true) ledger; audit + outbox mỗi action; MỌI mutation (record + items + logs + request) trong 1 tx withTenant; period-lock check; unique-pending-guard→409 ConflictException.","builder":"backend-builder","paths":["apps/api/src/attendance/**"]}]
acceptanceChecks: ["POST /attendance/adjustment-requests: tạo row status='Pending', employee_id resolve từ actor (KHÔNG nhận từ body trừ khi có quyền create-thay), request_type ∈ 9 enum §7.6, submitted_at set; audit AttendanceAdjustmentRequested + outbox attendance.adjustment_requested trong cùng tx.","Gate cặp engine ĐÚNG (không generic): create-own:adjustment · view-own/view-team/view-company:adjustment · approve:adjustment · reject:adjustment · adjust-direct:attendance — khớp attendance-permissions.const.ts + seed 0454.","GET /adjustment-requests/my chỉ trả đơn của actor; GET /adjustment-requests scope Team (DataScope Team) chỉ đơn nhân viên thuộc team, scope Company toàn công ty; không grant→403; đơn tồn-tại-ngoài-scope→404 (no existence leak, parity records read).","POST :id/approve: Pending→Approved dưới FOR UPDATE row-lock; approve thứ 2 đồng thời→409 ConflictException; recalc attendance_records (working_minutes/attendance_status/late/early recompute) KHÔNG DELETE attendance_logs (append log_type='Adjustment'); ghi attendance_adjustment_items(is_applied=true, old/new/applied_value); audit AttendanceAdjustmentApproved + AttendanceRecordAdjusted + outbox — tất cả 1 tx.","POST :id/reject: reason/review_note BẮT BUỘC (thiếu→400) §7.6.7; Pending→Rejected; audit + outbox; task/notification đóng.","POST /records/:id/adjust-direct (adjust-direct:attendance): non-holder→403; recalc + append attendance_logs + adjustment_items(is_applied) + audit + event trong 1 tx.","Deny-path: tạo hộ employee khác khi thiếu capability→403; manager approve đơn ngoài team→403; đơn công ty khác→404 (RLS FORCE); kỳ công đã khoá→409 khi create/approve/adjust-direct.","Unique-pending-guard: (company,employee,work_date,request_type) trùng khi status='Pending'→409.","Migration 0457 idempotent, nối tiếp head; RLS+FORCE giữ; grant adjustment_items append-only (SELECT,INSERT — KHÔNG UPDATE/DELETE); status CHECK canonical TitleCase; audit object_types CHECK là UNION (append).","DoD §8: input validation (Zod), permission guard fail-closed, FE loading/error N/A (BE), audit log mọi action quan trọng, test có, typecheck+build xanh, coverage ≥80% (module nhạy cảm), harness/backlog.mjs cập nhật; KHÔNG @ts-ignore/eslint-disable."]
testTasks: ["deny-path RED (viết TRƯỚC, int-spec colocated apps/api/src/attendance/*.int-spec.ts): create-thay không quyền→403; approve out-of-scope (manager ≠ team)→403; approve/detail cross-tenant→404; reject thiếu reason→400; double-approve đồng thời→second 409.","integration DB cô lập (LANE_DB, skipIf(!hasDb && LANE_DB)): happy path create→approve → attendance_records recalc + attendance_logs GIỮ (append Adjustment, count tăng không giảm) + adjustment_items ghi is_applied=true; reject path; adjust-direct path; unique-pending-guard 409; period-locked 409.","unit state-machine: bảng chuyển Pending→Approved/Rejected/Cancelled hợp lệ; Approved/Rejected/Cancelled terminal (approve lại→409).","unit recalc logic: working_minutes/attendance_status/late/early recompute từ requested times; khẳng định KHÔNG xoá attendance_logs.","QA-04 regression: check-in/out + records read (my/team/company) không vỡ; writer cũ /attendance/adjustments đã converge/gate (không 2 writer lệch status).","migration-smoke: 0457 apply trên LANE_DB, RLS+FORCE intact, grant append-only adjustment_items intact, status CHECK canonical, không phá 0452."]
steps: ["1. LANE MIG (nối tiếp TRƯỚC): micro-plan (Sonnet xhigh, crown) chốt stance reconcile status lowercase→TitleCase + dual-writer cũ /attendance/adjustments → viết migration 0457 (band idx nối tiếp head 0456, when monotonic), RLS+FORCE giữ nguyên, unique-pending-guard, drizzle schema phản ánh. plan-reviewer đối kháng trước code.","2. LANE CONTRACTS (song song được, trước SVC): Zod schemas canonical + build dual ESM/CJS; export cho api import.","3. LANE SVC (sau MIG+CONTRACTS): viết deny-path RED TRƯỚC (create-thay→403, approve out-of-scope→403, cross-tenant→404, reject thiếu reason→400, double-approve→409) → GREEN service+controller+repo dùng employee_id + cặp adjustment + DataScope + recalc-giữ-log + items ledger + audit/outbox trong tx.","4. Đối chiếu writer cũ: converge HOẶC gate /attendance/adjustments cũ để không có 2 writer lệch status/guard (owner chốt — crown).","5. FULL gate (security-reviewer + database-reviewer + silent-failure-hunter + santa-method) do chạm workflow phê duyệt + attendance mutation + permission; coverage ≥80%; người chốt red-zone trước merge.","6. Verify DB cô lập: bash scripts/lane-db-setup.sh s3attbe4 → export LANE_DB=mediaos_s3attbe4 → migrate → pnpm --filter @mediaos/api test.","7. Cập nhật harness/backlog.mjs (DoD §8)."]
```

GAP vs code hiện tại: (1) Surface CŨ /attendance/adjustments (attendance.controller.ts L177-216, service L465-707) build trên shape CŨ — user_id (không employee_id), status LOWERCASE (att_adj_status_check cũ ≠ DB-04 TitleCase §7.6 L972), gate generic read/adjust/approve:attendance (không phải cặp adjustment đã seed 0454), list scope me/all thô (chưa DataScope Team/Company), CHƯA ghi attendance_adjustment_items ledger, CHƯA append attendance_logs khi recalc (buildAdjustmentRecordValues chỉ update record), CHƯA có adjust-direct, unique-guard cũ trên user_id không phải (employee,work_date,request_type). (2) 0452 đã ALTER-ADD đủ cột §7.6 + tạo attendance_adjustment_items (append-only) + RLS+FORCE + idx_att_adj_employee_status/status_submitted/current_approver — CÒN THIẾU: uq_att_adj_pending_employee_date_type (unique pending guard) và status TitleCase CHECK (0452 giữ CHECK lowercase cũ).

INVARIANTS (CLAUDE.md §2/§3): company_id mọi query qua withTenant; RLS+FORCE đã có (migration chỉ reconcile, KHÔNG tháo); attendance_logs + attendance_adjustment_items APPEND-ONLY (app role SELECT,INSERT — recalc KHÔNG xoá log, đây là lõi 'giữ log, không mất dữ liệu'); audit mọi action; không hard-delete (soft deleted_at). §3 phụ thuộc: Permission engine + audit/outbox đã sẵn (BE-1 dùng) → OK code module nhạy cảm. Migration RLS+FORCE đã có TRƯỚC — chỉ backfill/reconcile status; nếu migrate rows lowercase→TitleCase phải trong migration (migrator=owner bypass RLS).

VERIFY: DB cô lập LANE_DB bắt buộc (drizzle áp migration đơn điệu).

GATE: FULL (workflow phê duyệt FSM + attendance mutation + permission) = security-reviewer + database-reviewer + silent-failure-hunter + santa-method; crown → planner Sonnet xhigh micro-plan + plan-reviewer đối kháng TRƯỚC code, IMPLEMENT/REVIEW Opus; người chốt red-zone trước merge (no auto-merge).

RỦI RO / ĐIỂM CHỐT NGƯỜI: (a) Reconcile status lowercase→TitleCase phá writer cũ nếu không converge — owner chốt: rebuild canonical thay writer cũ HAY chạy song song rồi gate? Khuyến nghị converge 1 writer canonical, giữ route cũ deprecated-not-delete để FE không vỡ. (b) Row-lock FOR UPDATE + unique-pending-guard chống double-approve/double-request — silent-failure-hunter soi TOCTOU. (c) create-thay (Manager/HR tạo hộ) cần capability riêng — mặc định fail-closed 403 nếu thiếu, KHÔNG mặc định cho phép.

OUT-OF-SCOPE (chống scope creep): remote_work_requests workflow (WO riêng), shift/rule config, FE screens ATT-SCREEN-006/007/008 (frontend WO), payroll recalc downstream, notification template nội dung (chỉ enqueue event).

SPEC refs: SPEC-04 §ATT-FUNC-018..022, permission ATT.ADJUSTMENT.* + ATT.ATTENDANCE.ADJUST_DIRECT (SPEC-04 L687-695), DB-04 §7.6/§7.7 (L913-1000), quy tắc §7.6.6 (Approved→cập nhật records + items + audit + noti), §7.6.7 (Rejected bắt buộc review_note), §7.6.8 (kỳ khoá chặn).
</content>
