```yaml
wo: S4-QA-1
zone: yellow
generated_by: auto-loop
reconciled_at: "d10b80d"
lanes: [{"id":"qadashaggdeny","task":"DASH aggregation-route deny-path (crown, permission) + cross-module 2-tenant regression. (1) dashboard-agg-routes-deny.int-spec.ts: POST /dashboard/refresh yêu cầu manage:dashboard (DASH.CACHE.REFRESH, SPEC-07 §8.2 DN-7) — user chỉ read:dashboard → 403, user có manage:dashboard → 200 (RED nếu route rớt manage-gate = leo thang); GET /dashboard/report sub-gate per-type (read:finance_report/employee_report/attendance_report seed 0101) — employee bị chặn/thiếu section, HR/manager có employee+attendance report, finance chỉ finance/admin, KHÔNG rò chéo type; GET /dashboard/{summary,mv-stats,alerts}: no-role/thiếu read:dashboard → 403 fail-closed, có quyền → 200, summary tôn trọng can() nội tầng (thiếu read:task ⇒ KHÔNG có task-count). (2) dash-xmodule-2tenant.int-spec.ts: 2 company A/B, seed marker A (task/notification/employee-salary/audit/project); user B (đủ quyền tenant B) gọi summary/mv-stats/alerts/report/widgets → KHÔNG chứa BẤT KỲ marker A (response + row cache); project-progress với project_id của A → 404, KHÔNG rò tiến độ; project-member scope regression (trích dẫn task-qa1 + widget S7c, mở rộng sang /summary). Tất cả describe.skipIf(!(hasDb&&LANE_DB)). KHÔNG viết trùng task-qa1-*/noti-qa-permission/my-notifications — chỉ ghi checklist đối chiếu vào plan.","builder":"backend-builder","paths":["apps/api/test/integration/dashboard-agg-routes-deny.int-spec.ts","apps/api/test/integration/dash-xmodule-2tenant.int-spec.ts","docs/plans/S4-QA-1.md"]},{"id":"qadashfevis","task":"FE role-based widget-visibility smoke gap-fill (defense-in-depth cho 'widget Manager/HR với employee', SPEC-07 §9 ma trận). Employee (caps chỉ read:task/attendance/leave own) → widget Manager/HR-only (PENDING_LEAVE/HR_OVERVIEW/LEAVE_CALENDAR/ATTENDANCE_ALERTS) KHÔNG render qua PermissionGate/useCan; HR/Manager (đủ cap) → render. Mirror pattern MyTasksWidget.spec.tsx (PermissionGate THẬT, mock dashboardApi). CHỈ bổ sung nếu gap thật — DashboardWidgetGrid/WidgetCard đã có PermissionGate + isolation.spec.tsx; KHÔNG dựng lại. Colocated apps/app/src/** (chạy qua apps/app vitest).","builder":"frontend-builder","paths":["apps/app/src/components/dashboard/DashboardWidgetGrid.rolevis.spec.tsx"]}]
acceptanceChecks: ["dashboard-agg-routes-deny.int-spec.ts: POST /dashboard/refresh — role chỉ read:dashboard → 403; role có manage:dashboard → 200 (chứng manage-boundary = DASH.CACHE.REFRESH SPEC-07 §8.2 DN-7; RED nếu handler rớt @RequirePermission('manage','dashboard')).","GET /dashboard/report sub-gate: employee (KHÔNG read:finance_report/employee_report/attendance_report — seed 0101) → các section report bị chặn/vắng; HR & manager → có employee_report + attendance_report; finance_report chỉ finance/admin. KHÔNG rò type chéo.","GET /dashboard/{summary,mv-stats,alerts}: no-role/thiếu read:dashboard → 403 fail-closed (KHÔNG 200 rỗng); có read:dashboard → 200; /summary tôn trọng can() nội tầng (user thiếu read:task ⇒ response KHÔNG có task-count; thiếu read:leave ⇒ KHÔNG leave-count).","dash-xmodule-2tenant.int-spec.ts: user B KHÔNG thấy marker A (task/notification/employee-salary/audit/project) ở summary/mv-stats/alerts/report/widgets — cả response LẪN row dashboard_widget_cache; project-progress với project_id của A → 404 (không lộ tồn tại); project-member scope không nới cross-tenant (BẤT BIẾN §2 company_id + RLS).","TASK/NOTI đối chiếu: KHÔNG thêm spec trùng task-qa1-*/noti-qa-permission/my-notifications; plan liệt kê assertion đã phủ + xác nhận 0 lỗ hổng còn lại (hoặc bù đúng chỗ thiếu).","Coverage vùng DASH (src/dashboard/**) ≥80% đo dưới LANE_DB (ngưỡng riêng module nhạy cảm CLAUDE.md §6); số liệu ghi trong docs/plans/S4-QA-1.md.","Mọi int-spec mới đặt apps/api/test/integration/**/*.int-spec.ts (chạy theo vitest.config include glob test/**/*.int-spec.ts, gated describe.skipIf(!(hasDb&&LANE_DB))); FE smoke colocated apps/app/src/**/*.spec.tsx (chạy qua apps/app vitest) — KHÔNG để spec ở nơi không chạy (bài học vitest-unit-specs-must-be-colocated).","bash harness/check.sh --lane-db (hoặc --all/REQUIRE_LANE_DB=1 trước PR vùng đỏ) XANH, không rơi trạng thái 'XANH KHÔNG ĐỦ BẰNG CHỨNG'; FULL gate PASS cho phần permission/workflow (DoD §8)."]
testTasks: ["RED deny (isolated LANE_DB): POST /dashboard/refresh với role chỉ read:dashboard → 403; sau đó role có manage:dashboard → 200 — chứng manage-boundary tách khỏi read (chống leo thang).","RED deny: employee GET /dashboard/report → section finance/employee/attendance report bị chặn/vắng theo per-type gate; positive: HR/manager thấy employee+attendance report (seed 0101).","RED deny: no-role GET /dashboard/{summary,mv-stats,alerts} → 403 fail-closed; summary inner-gating — user thiếu read:task KHÔNG nhận task-count, thiếu read:leave KHÔNG nhận leave-count.","2-tenant cross-module regression: seed marker A (task/notification/employee-salary/audit/project); user B đủ quyền tenant B gọi summary/mv-stats/alerts/report/widgets → 0 marker A trong response + row cache; project-progress project_id-của-A → 404.","FE role-visibility smoke: employee caps (read:task/attendance/leave) → Manager/HR-only widget (PENDING_LEAVE/HR_OVERVIEW/LEAVE_CALENDAR/ATTENDANCE_ALERTS) KHÔNG render; HR/manager caps → render (PermissionGate THẬT).","Đối chiếu (KHÔNG viết trùng): xác nhận task-qa1-permission-matrix (deny-matrix + data-scope Own/Team/project-member + IDOR cross-tenant), task-qa1-fsm-collab (FSM transition sai 409/422, watcher self-only, comment self-only), noti-qa-permission + my-notifications (own-scope read/mark-read cross-user 404, cross-tenant 404, mark-read idempotent, actor-exclusion) đã phủ done_when TASK/NOTI."]
steps: ["Đọc lại spec đã ship của S4-QA-TASK-1 (apps/api/test/integration/task-qa1-permission-matrix.int-spec.ts + task-qa1-fsm-collab.int-spec.ts) và S4-QA-NOTI-1 (noti-qa-permission.int-spec.ts + my-notifications.int-spec.ts) → liệt kê assertion đã phủ; xác nhận gap = aggregation routes chưa có deny-path (chỉ dash-seed2 chạm /summary tầng role-gate).","Viết docs/plans/S4-QA-1.md: gap-analysis (TASK/NOTI đã đủ; DASH gap = refresh/report/summary/mv-stats/alerts + cross-module 2-tenant) + khối yaml máy-đọc (lanes/acceptanceChecks/testTasks/steps) để tái dùng.","Lane qadashaggdeny — RED trước: dựng dashboard-agg-routes-deny.int-spec.ts trên DB cô lập (bash scripts/lane-db-setup.sh qadashagg → export LANE_DB=mediaos_qadashagg). Assert manage:dashboard boundary (refresh), report per-type sub-gate, summary/mv-stats/alerts fail-closed + inner can(). Chứng RED trước GREEN (bài học reviewers-pass-real-bugs).","Lane qadashaggdeny — dash-xmodule-2tenant.int-spec.ts: seed 2-tenant, marker A ở task/notification/employee/audit/project; sweep summary/mv-stats/alerts/report/widgets của B KHÔNG chứa marker A (response + dashboard_widget_cache row); project-progress A-id → 404; project-member scope.","Lane qadashfevis — FE role-visibility smoke (nếu gap): employee caps → Manager/HR widget ẩn; HR/manager caps → hiện. Chạy pnpm --filter @mediaos/app test.","Đo coverage vùng DASH dưới LANE_DB: vitest run src/dashboard + test/integration/dash*/dashboard* --coverage --coverage.include='src/dashboard/**' → xác nhận ≥80%, ghi số vào plan.","bash harness/check.sh --lane-db (hoặc --all trước PR vùng đỏ) xanh; FULL gate (security-reviewer + database-reviewer + silent-failure-hunter) cho phần permission; commit theo lane, cập nhật backlog."]
```

## GAP-ANALYSIS (đối chiếu code hiện tại)

- **TASK**: S4-QA-TASK-1 (PR #165) đã phủ ĐỦ — `task-qa1-permission-matrix.int-spec.ts` (deny-matrix theo role, data-scope employee@Own / manager@Team / project-member mở scope, LIST lọc theo scope, checklist mutate fail-closed, IDOR cross-tenant 404) + `task-qa1-fsm-collab.int-spec.ts` (FSM change-status transition ngoài bảng→409 WORKFLOW-INVALID, terminal→422 TASK-CLOSED, checklist-required→400, kanban move dùng cùng FSM, watcher self-only, comment self-only 403 + mention-out-of-scope, view:task-audit-log gate, actor-exclusion). ⇒ KHÔNG viết trùng.
- **NOTI**: S4-QA-NOTI-1 (PR #167) đã phủ ĐỦ — `noti-qa-permission.int-spec.ts` (C01 no-role 403, C02 cross-user read/mark/delete→404 + owner đọc được, C03 cross-tenant 404, C04 admin config deny, C05/C06 per-pair config/template/delivery-log, C07 mark-read idempotent, C08 mark-all idempotent, C09 dedupe, C10 actor-exclusion non-system vs system, C11 delivery-log cross-tenant, C12 filters own-scope) + `my-notifications.int-spec.ts` (soft-delete append-only, unread-count). ⇒ KHÔNG viết trùng.
- **DASH widget-data** (`/dashboard/widgets/:slug`): ĐÃ phủ sâu — `dashboard-widget-security.int-spec.ts` (S1 scope Own, S2 cross-tenant, S3 cache no-leak, S4 masking-tier viewer-independent, S5 authorize-before-aggregate spy, S6 append-only cache, S7 cross-tenant sweep 7 widget) + `dashboard-widget-catalog2-security.int-spec.ts` (9 widget đợt-2: grant-matrix, SYSTEM_LOGS count-only, PII 5 widget, cross-tenant) + `dashboard-resolver.int-spec.ts` (M1-M10 role-visibility employee/manager/hr/admin, DASHBOARD_NOT_RESOLVED, gate tầng-2 hai chiều) + `dash-seed2-manager-hr-grant.int-spec.ts` (read:dashboard backfill manager/hr). ⇒ KHÔNG viết trùng.
- **GAP THẬT** = 5 route AGGREGATION của `apps/api/src/dashboard/dashboard.controller.ts` CHƯA có deny-path/cross-tenant: (a) `POST /dashboard/refresh` `@RequirePermission('manage','dashboard')` — manage-boundary tách khỏi read, KHÔNG có test ⇒ mặt leo thang; (b) `GET /dashboard/report` gate `read:dashboard` ở controller RỒI sub-gate `read:finance_report/employee_report/attendance_report` (seed 0101) bên trong — KHÔNG có deny-path per-type; (c) `GET /dashboard/summary` gate `read:dashboard` + `can()` nội tầng cho task/attendance/leave/attendance_all — chỉ dash-seed2 chạm ở tầng role-gate 200/403, KHÔNG kiểm inner data-leak/cross-tenant; (d) `GET /dashboard/mv-stats` + `/dashboard/alerts` (`read:dashboard`) — 0 deny/cross-tenant test. Không có 1 spec cross-module 2-tenant HỢP NHẤT phủ các route aggregation này.

## INVARIANTS

BẤT BIẾN CLAUDE.md §2 áp dụng: company_id ở mọi query + RLS+FORCE (cross-tenant B KHÔNG đọc data A qua aggregation route lẫn cache row — chứng bằng GUC tenant B); append-only `dashboard_widget_cache` (đã có ở S6, không cần lặp); masking là việc SERVER (report/summary KHÔNG trả PII/lương cho role thiếu quyền). `manage:dashboard` ≠ `read:dashboard` (least-privilege).

## VERIFY

Mọi int-spec mới `describe.skipIf(!(hasDb && LANE_DB))` (bài học integration-test-lane-db-gate — `.env` làm `hasDb=true` nên phải gate thêm LANE_DB tránh đỏ/xanh-giả). Chạy DB cô lập: `bash scripts/lane-db-setup.sh qadashagg` → `export LANE_DB=mediaos_qadashagg` → `TURBO_FORCE=1 pnpm --filter @mediaos/contracts build` → `npx vitest run <spec>`. RED trước GREEN (reviewers-pass-real-bugs: query DB, chứng test đi RED khi bỏ gate). Coverage đo dưới LANE_DB với `--coverage.include='src/dashboard/**'` (int-spec skip khi no-DB nên coverage phải chạy có LANE_DB, giống `test:cov:sensitive`).

## GATE

Lane `qadashaggdeny` = crown (permission/authz) ⇒ FULL gate: security-reviewer + database-reviewer + silent-failure-hunter (+ santa-method nếu chạm logic authz). Lane `qadashfevis` = LIGHT (react-reviewer + quality-gate). WO zone=yellow nhưng phần permission-deny nâng lên FULL theo domain-routing CLAUDE.md §6.

## COLOCATED vs INTEGRATION (giải mâu thuẫn done_when)

`done_when#3` "colocated `src/**/*.spec.ts`" là bài học cho UNIT spec (đặt ở `test/` sẽ KHÔNG chạy). QA WO này dùng INTEGRATION deny-path — `*.int-spec.ts` ở `test/integration/**` CÓ chạy (`vitest.config.ts` include `test/**/*.int-spec.ts`, dòng 47), gated `hasDb&&LANE_DB`. FE unit smoke thì colocated `apps/app/src/**/*.spec.tsx` (chạy qua `apps/app/vitest.config.ts`). ⇒ không đặt sai chỗ, không mâu thuẫn WO paths.

## OUT-OF-SCOPE

`finance_report` thuộc hướng finance-theo-kênh đã park (de-media-fy) — CHỈ test cổng deny (employee bị 403/thiếu section), KHÔNG dựng nội dung finance_report. Không migration (WO thuần test, head mig giữ nguyên). Không đụng approval-FSM LEAVE/ATT (in-scope nhưng ngoài WO này). platform-admin/SaaS-tier ngoài scope.

## MIGRATION LANE

KHÔNG cần — WO không đổi schema; đã có seed 0101 (report perms), 0484/0488 (dashboard perms), 0493 (catalog2). Nếu phát hiện `manage:dashboard` CHƯA seed cho role nào để test positive-path → dùng `seedRole`+`seedRolePermission` trực tiếp trong spec (direct pool), KHÔNG thêm migration.
</content>
