```yaml
wo: S2-FND-JOBS-1
zone: red
generated_by: auto-loop
reconciled_at: "cdb266b"
lanes: [{"id":"JOBS-DB","task":"[NỐI TIẾP TRƯỚC · crown migration/RLS] Migration 0475 tiếp head idx154/0474: (1) system_job_runs đúng DB-08 §8.14 — id uuid PK, company_id uuid NULLABLE FK companies (NULL=job cấp system), job_code varchar(100), status varchar(50) CHECK IN(Running,Success,Failed,Partial,Skipped), triggered_by varchar(50) CHECK IN(Scheduler,User,System), triggered_by_user_id uuid FK users nullable, started_at timestamptz, finished_at timestamptz, duration_ms bigint, total_items/success_items/failed_items int, error_message text, metadata jsonb, created_at timestamptz default now; index DB-09 §8.11: idx_system_job_runs_job_time(job_code,started_at DESC) + idx_system_job_runs_company_job_time(company_id,job_code,started_at DESC) + idx_system_job_runs_status_time(status,started_at DESC) WHERE status IN(Failed,Partial). RLS ENABLE+FORCE: policy tenant_iso TO mediaos_app USING(company_id=NULLIF(current_setting('app.current_company_id',true),'')::uuid OR company_id IS NULL) + policy system_job_runs_worker_all TO mediaos_worker USING(true) WITH CHECK(true) (mẫu 0003:66-69). GRANT: mediaos_app SELECT-only; mediaos_worker SELECT,INSERT,UPDATE; KHÔNG DELETE role nào. (2) system_job_locks đúng DB-08 §8.15 — job_code varchar(100) PK, locked_by varchar(255), locked_until timestamptz, acquired_at timestamptz, metadata jsonb; index idx_system_job_locks_locked_until(locked_until). KHÔNG company_id ⇒ KHÔNG RLS (worker-infra, mẫu processed_events). GRANT: mediaos_worker SELECT,INSERT,UPDATE (release lock = UPDATE locked_until về quá khứ, KHÔNG DELETE); KHÔNG grant app. (3) Drizzle schema apps/api/src/db/schema/system-jobs.ts parity với migration + append export vào schema/index.ts (hot-file: chỉ thêm dòng export, KHÔNG rewrite). db:check chain xanh.","builder":"db-migration","paths":["apps/api/migrations/**","apps/api/src/db/schema/system-jobs.ts","apps/api/src/db/schema/index.ts"]},{"id":"JOBS-RUNNER","task":"[crown worker-role/lock/scrubber · depends JOBS-DB] (1) File TOKEN STANDALONE apps/api/src/scheduler/job-handler.ts: interface JobHandler{jobCode; run(ctx:{companyId,tx?}):Promise<{total,success,failed,metadata?}>} + token SYSTEM_JOB_HANDLER (multi-provider) — KHÔNG định nghĩa trong scheduler.module.ts (để Retention/Files import file token này, KHÔNG import SchedulerModule ⇒ tránh circular). (2) JobLockService: acquire lock system_job_locks(job_code) qua workerDb bằng INSERT ... ON CONFLICT(job_code) DO UPDATE ... WHERE system_job_locks.locked_until < now() RETURNING (phân biệt acquired vs skip); release = UPDATE locked_until về quá khứ. Gọi assertWorkerRoleSafe(mode:'prod-only') TRƯỚC mọi INSERT/UPDATE (mẫu outbox-worker.ts:52-60); workerDb undefined (DATABASE_WORKER_URL vắng) ⇒ throw fail-closed. (3) secret string-scrubber RIÊNG (regex redact password=/token=/secret=/api_key= + credential trong URL user:pass@host) chạy TRÊN error_message text tự-do TRƯỚC khi ghi — KHÔNG chỉ AuditMaskerService.mask (mask key-based, passthrough scalar); metadata jsonb vẫn dùng mask(). (4) JobRunLogger: ghi system_job_runs (INSERT Running → UPDATE status/counts/duration_ms/error_message-đã-scrub) qua workerDb + assertWorkerRoleSafe. (5) JobRunner trên WorkerSchedulerService (GIỮ setInterval + swallow-tick): mỗi job_code acquire lock ĐÚNG 1 LẦN quanh TOÀN BỘ vòng lặp per-tenant; skip nếu lock giữ; enumerate tenant qua DatabaseService.withPlatformContext (mẫu seed-runner:208); mỗi tenant ghi 1 run-row BÊN TRONG + gọi JobHandler khớp trong withTenant(companyId); job fail bắt lỗi → run 'Failed' (error scrub) + app SỐNG; release lock finally. (6) scheduler.module.ts (append) inject SYSTEM_JOB_HANDLER multi (optional, mảng rỗng OK) + import RetentionModule+FilesModule để gom handler; đăng ký lịch RetentionCleanup + TEMP_FILE_CLEANUP. RED int-spec colocated apps/api/src/scheduler/*.int.spec.ts.","builder":"backend-builder","paths":["apps/api/src/scheduler/**"]},{"id":"JOBS-RETENTION","task":"[red · depends JOBS-RUNNER] RetentionJobHandler implements JobHandler (jobCode='AUDIT_LOG_RETENTION' hoặc 'RETENTION_CLEANUP') gọi RetentionCleanupJob.run per-tenant: dryRun default TRUE GIỮ NGUYÊN + kill-switch env (vd RETENTION_CLEANUP_ENABLED); PROTECTED_TABLES GIỮ NGUYÊN (audit_logs/file_access_logs KHÔNG BAO GIỜ purge — BẤT BIẾN #2); trả {total=policiesProcessed, success, failed, deleted} cho JobRunLogger. Provide {provide:SYSTEM_JOB_HANDLER, useClass:RetentionJobHandler, multi:true} export từ RetentionModule; RetentionModule import FILE token standalone (apps/api/src/scheduler/job-handler.ts) — KHÔNG import SchedulerModule. Colocated spec: dry-run default true; protected table never purged; chạy thật per-tenant có ghi run.","builder":"backend-builder","paths":["apps/api/src/foundation/retention/**"]},{"id":"JOBS-TEMPFILE","task":"[red · depends JOBS-RUNNER] (1) Thêm setting default file.pending_ttl_hours vào SETTING_DEFAULTS (Number, category File, moduleCode SYSTEM, isPublic true). (2) FileRepository: method mới findTempCleanupEligible(companyId,pendingTtlHours,tx) + softDeleteSystemTx(companyId,fileIds,tx) đặt deleted_by=NULL, upload_status='Deleted'. Eligibility = (is_temporary=true AND expires_at<now()) OR (upload_status='Pending' AND uploaded_at < now()-pending_ttl_hours) — VÀ deleted_at IS NULL — VÀ NOT EXISTS(SELECT 1 FROM file_links WHERE file_links.file_id=files.id AND file_links.deleted_at IS NULL) (đã link entity nghiệp vụ = đang dùng, KHÔNG xóa dù expired/temporary). (3) TempFileCleanupJobHandler implements JobHandler (jobCode='TEMP_FILE_CLEANUP'): chạy trong withTenant(companyId); resolve pending_ttl_hours qua SettingService (precedence company>system>default); soft-delete batch; ghi file_access_logs action='Delete' + audit actorType='System' actorUserId=null; BỎ QUA FilePolicy (job hệ thống, không user). Provide SYSTEM_JOB_HANDLER multi export từ FilesModule; FilesModule import FILE token standalone — KHÔNG import SchedulerModule. RED int-spec colocated: temp-expired NHƯNG đã link active → KHÔNG xóa; temp-expired KHÔNG link → xóa; Pending quá TTL → xóa; ghi file_access_logs actorType='System' actorUserId=null.","builder":"backend-builder","paths":["apps/api/src/foundation/files/**","apps/api/src/foundation/settings/setting-defaults.ts"]}]
acceptanceChecks: ["Migration 0475 NỐI TIẾP head 0474 (không lấp band cũ); system_job_runs cột đúng DB-08 §8.14 (13 cột + company_id nullable) + 2 CHECK (status, triggered_by); 3 index đúng DB-09 §8.11 (job_time, company_job_time, status_time partial WHERE Failed/Partial).","system_job_runs có RLS ENABLE+FORCE; policy tenant_iso TO mediaos_app USING(company_id=GUC OR company_id IS NULL); policy system_job_runs_worker_all TO mediaos_worker USING(true) WITH CHECK(true); GRANT app=SELECT-only, worker=SELECT,INSERT,UPDATE; KHÔNG có GRANT DELETE cho bất kỳ role (kiểm rls-coverage-assert / db-roles).","system_job_locks: job_code PK, KHÔNG company_id, KHÔNG RLS (worker-infra); index locked_until; GRANT worker SELECT,INSERT,UPDATE (release qua UPDATE), KHÔNG DELETE, KHÔNG grant app; drizzle schema parity + export index.ts append-only.","Token SYSTEM_JOB_HANDLER + interface JobHandler ở FILE STANDALONE apps/api/src/scheduler/job-handler.ts; Retention/FilesModule import file token này, KHÔNG import SchedulerModule (không circular — `pnpm --filter @mediaos/api build` xanh).","JobLockService/JobRunLogger ghi qua workerDb + gọi assertWorkerRoleSafe(mode:'prod-only') TRƯỚC mọi INSERT/UPDATE; workerDb undefined ⇒ throw (fail-closed); lock acquire ĐÚNG 1 LẦN/job_code quanh toàn vòng per-tenant, ghi 1 run-row/tenant bên trong.","error_message chạy qua string-scrubber RIÊNG: token EMBEDDED trong chuỗi (vd 'password=abc123') bị redact TRONG error_message đã ghi (không chỉ mask key JSON); metadata jsonb vẫn mask().","RetentionCleanupJob wire qua JobRunner: dryRun default TRUE giữ nguyên + kill-switch env; PROTECTED_TABLES (audit_logs/file_access_logs) KHÔNG BAO GIỜ purge (BẤT BIẾN #2); chạy thật per-tenant có ghi run-row.","TEMP_FILE_CLEANUP eligibility đúng: (is_temporary AND expires_at<now) OR (upload_status='Pending' quá file.pending_ttl_hours) AND NOT EXISTS(file_links active); soft-delete đặt deleted_by=NULL upload_status='Deleted'; ghi file_access_logs+audit actorType='System' actorUserId=null; chạy trong withTenant(companyId); BỎ QUA FilePolicy.","OUT-OF-SCOPE tôn trọng: KHÔNG có route /system-jobs (permission FOUNDATION.JOB.RUN) ở WO này; KHÔNG migration cho files.deleted_by (đã nullable); KHÔNG thêm object_type audit_logs CHECK (SYSTEM_JOB_RUN_COMPLETED DEFER).","DoD §8: có test (RED deny-path + integration DB cô lập LANE_DB); typecheck+lint xanh; FULL gate PASS (security-reviewer + database-reviewer) + người chốt red-zone; cập nhật harness/backlog.mjs."]
testTasks: ["RED (lock) integration LANE_DB: 2 JobRunner chạy song song cùng job_code → 1 acquire chạy, 1 skip Ở CẤP job_code (system_job_locks); assert đúng 1 vòng cleanup thực thi.","RED (job error) integration: JobHandler.run ném lỗi → run-row ghi status 'Failed' + app KHÔNG sập (mirror outbox swallow); assert run-row tồn tại + process sống.","RED (secret scrubbing) integration: gieo `new Error('connect failed password=abc123 token=xyz')` → assert error_message ĐÃ GHI đã redact chuỗi nhúng (KHÔNG lộ abc123/xyz), KHÔNG chỉ test key JSON.","RED (assertWorkerRoleSafe) integration: dựng ĐIỀU KIỆN THẬT — NODE_ENV=production + directPool role super/bypassrls THẬT (mẫu apps/api/test/integration/db-roles.int-spec.ts) → assert JobLockService/JobRunLogger throw có ý nghĩa; KHÔNG viết test giả-định (tránh xanh-giả).","RED (temp-file link exclusion) integration: file temp+expired NHƯNG có file_links active → KHÔNG bị xóa; file temp+expired KHÔNG link → bị soft-delete; file Pending quá pending_ttl_hours → bị xóa.","RED (System actor) integration: TEMP_FILE_CLEANUP ghi file_access_logs action='Delete' + audit actorType='System' actorUserId=null, chạy trong withTenant(companyId), bỏ qua FilePolicy.","Retention job spec: dryRun default true (không xóa); PROTECTED_TABLES entity (audit_logs) → deletedRecords=0 (BẤT BIẾN #2); kill-switch env tắt job.","QA sign-off: chạy DB cô lập `bash scripts/lane-db-setup.sh jobs` + LANE_DB=mediaos_jobs; verify int-spec THỰC SỰ xuất hiện trong run summary (bài học colocated *.int.spec.ts phải khớp src/**/*.spec.ts — không để spec ở test/ sai đuôi = green giả); coverage ≥80% module nhạy cảm."]
steps: ["Lane JOBS-DB (db-migration, NỐI TIẾP TRƯỚC): viết migration 0475 tạo system_job_runs (§8.14) + system_job_locks (§8.15) + index (§8.11/8.12) + RLS/FORCE (runs) + grant (app SELECT / worker SELECT,INSERT,UPDATE; locks worker-only, no DELETE) theo mẫu 0003; drizzle schema system-jobs.ts + append export index.ts; `pnpm --filter @mediaos/api db:generate` khớp + db:check xanh.","Lane JOBS-RUNNER (backend, sau JOBS-DB): tạo file token standalone job-handler.ts; JobLockService + JobRunLogger qua workerDb + assertWorkerRoleSafe(prod-only) fail-closed; secret string-scrubber cho error_message; JobRunner trên WorkerSchedulerService (lock 1 lần/vòng, run-row/tenant, job fail không sập app); wire scheduler.module.ts import Retention+Files + inject SYSTEM_JOB_HANDLER multi; RED int-spec lock-race/worker-role/secret-scrub/job-error.","Lane JOBS-RETENTION (backend, sau JOBS-RUNNER): RetentionJobHandler bọc RetentionCleanupJob.run (dry-run default true + kill-switch, PROTECTED_TABLES giữ nguyên); provide SYSTEM_JOB_HANDLER multi export từ RetentionModule (import file token, không import SchedulerModule); spec dry-run/protected.","Lane JOBS-TEMPFILE (backend, sau JOBS-RUNNER): thêm default file.pending_ttl_hours; repo findTempCleanupEligible + softDeleteSystemTx(deleted_by=null) loại trừ file_links active; TempFileCleanupJobHandler chạy withTenant + audit/file_access_logs actorType='System' + bỏ qua FilePolicy; provide SYSTEM_JOB_HANDLER multi export từ FilesModule; RED spec link-exclusion + Pending-TTL + System-actor.","Verify: DB cô lập `bash scripts/lane-db-setup.sh jobs` → export LANE_DB=mediaos_jobs → `pnpm --filter @mediaos/api test`; typecheck + lint; FULL gate (security-reviewer + database-reviewer) + người chốt (crown red-zone: migration/RLS/worker-role/append-only kề audit)."]
```

GAP-ANALYSIS (code hiện tại vs WO): system_job_runs/system_job_locks CHƯA tồn tại (grep 0 hit trong migrations/schema) → cần mig 0475 mới. JobRunner/JobHandler/SYSTEM_JOB_HANDLER CHƯA có; nền tái dùng: WorkerSchedulerService (setInterval + swallow-tick, scheduler/worker-scheduler.service.ts) — JobRunner MỞ RỘNG service này, KHÔNG viết lại; OutboxWorker.assertWorkerRoleSafe (events/outbox-worker.ts:52-60) + apps/api/src/db/worker-role.ts (mode prod-only/strict) → TÁI DÙNG nguyên. RetentionCleanupJob.run ĐÃ dry-run-default-true + per-tenant (retention-cleanup.job.ts) → chỉ cần bọc thành JobHandler + kill-switch. RetentionService.PROTECTED_TABLES (retention.service.ts:42-72) ĐÃ chặn audit_logs/file_access_logs → GIỮ, không đụng. files schema (files.ts) có is_temporary/expires_at/upload_status/deleted_by-NULLABLE(line72) → KHÔNG cần migration files; NHƯNG FileRepository.softDeleteTx (file.repository.ts:80) yêu cầu deletedBy:string non-null → TEMP_FILE cần method MỚI softDeleteSystemTx đặt deleted_by=NULL. Tenant enumeration: TÁI DÙNG DatabaseService.withPlatformContext (mẫu master-data-seed-runner.service.ts:208 listActiveCompanyIds). AuditService.record (events/audit.service.ts:98) hỗ trợ actorType='System'; FileAccessLogService.record (files/file-access-log.service.ts) append-only in-tx.

INVARIANTS: #1 mọi cleanup work qua withTenant(companyId) RLS+FORCE (run-log/lock là worker-infra qua workerDb role mediaos_worker — worker_all policy USING true, KHÔNG cần GUC); #2 append-only — system_job_runs KHÔNG DELETE role nào, không purge audit/file_access_logs; #3 KHÔNG secret plaintext — error_message qua string-scrubber RIÊNG (khác key-based mask, vì mask passthrough scalar không che token nhúng trong 1 chuỗi lỗi tự-do), metadata jsonb dùng mask().

CIRCULAR-IMPORT: token SYSTEM_JOB_HANDLER + JobHandler PHẢI ở file standalone apps/api/src/scheduler/job-handler.ts (KHÔNG trong scheduler.module.ts) — SchedulerModule import RetentionModule+FilesModule để gom multi-provider; ngược lại 2 module đó chỉ import FILE token, KHÔNG import SchedulerModule.

VERIFY: DB cô lập bash scripts/lane-db-setup.sh jobs → LANE_DB=mediaos_jobs → pnpm --filter @mediaos/api test (drizzle migrator đơn điệu theo when — DB chung skip band thấp = xanh/đỏ giả). Colocated int spec dùng đuôi *.int.spec.ts (khớp include src/**/*.spec.ts) — KHÔNG để ở test/ sai đuôi = không chạy = green giả (bài học vitest-unit-specs-must-be-colocated). Worker-role RED phải dựng role bypassrls THẬT (mẫu test/integration/db-roles.int-spec.ts) + NODE_ENV=production, không giả-định.

GATE: FULL (migration + append-only kề audit + worker-role/RLS) → security-reviewer + database-reviewer + người chốt red-zone TRƯỚC merge; migration RLS+FORCE tạo TRƯỚC (không backfill company_id ở WO này). Migration NỐI TIẾP head idx154/0474 → 0475 (không lấp band).

OUT-OF-SCOPE (KHÔNG hỏi lại): (a) API /system-jobs trigger tay + permission FOUNDATION.JOB.RUN DEFER — job này CHỈ Scheduler-triggered; (b) files.deleted_by đã nullable — KHÔNG migration; (c) RLS visibility system_job_runs cho row company_id NULL là cross-tenant CHỦ Ý ở N=1 (policy app USING company_id=GUC OR company_id IS NULL) — ghi rõ, KHÔNG sửa; (d) audit summary SYSTEM_JOB_RUN_COMPLETED DEFER — system_job_runs tự đủ chi tiết, KHÔNG thêm object_type audit_logs CHECK.

THỨ TỰ THI CÔNG (tuần tự, mỗi lane 1 agent, KHÔNG song song thật vòng này): JOBS-DB → JOBS-RUNNER → JOBS-RETENTION → JOBS-TEMPFILE (JOBS-RUNNER phụ thuộc bảng của JOBS-DB; JOBS-RETENTION & JOBS-TEMPFILE phụ thuộc token+JobRunner của JOBS-RUNNER; 2 lane cuối không chồng paths — retention/** vs files/**+setting-defaults.ts). HOT-FILE: schema/index.ts (chỉ JOBS-DB append 1 dòng export system-jobs); scheduler.module.ts (chỉ JOBS-RUNNER, append import+provider); setting-defaults.ts (chỉ JOBS-TEMPFILE, thêm 1 key file.pending_ttl_hours).

RỦI RO CẦN NGƯỜI CHỐT: toàn bộ 4 lane crown red-zone (migration/RLS/worker-role/append-only/scheduler) → duyệt red-zone trước merge; đặc biệt grant matrix system_job_runs/locks (no DELETE) + policy company_id-NULL cross-tenant visibility + string-scrubber coverage (đảm bảo không rò secret embedded).
</content>
