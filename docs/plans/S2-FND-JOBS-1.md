```yaml
wo: S2-FND-JOBS-1
zone: red
generated_by: auto-loop
reconciled_at: "4d3befb"
lanes: [{"id":"jobsdb","task":"MIGRATION NỐI TIẾP (SERIAL, chạy TRƯỚC): 0474 tạo 2 bảng system_job_runs + system_job_locks đúng shape DB-08 §8.14/8.15 + index DB-09 §8.11/8.12, RLS+FORCE nullable-tenant, grant worker/app (KHÔNG DELETE). Hand-write SQL mẫu outbox mig 0003 (drizzle-kit KHÔNG emit RLS/grant/policy). Thêm schema drizzle system-jobs.ts + append schema/index.ts. db:check chain xanh. KHÔNG chạm audit_logs CHECK.","builder":"db-migration","paths":["apps/api/migrations/0474_s2_fndjobs1_system_jobs.sql","apps/api/migrations/meta/**","apps/api/src/db/schema/system-jobs.ts","apps/api/src/db/schema/index.ts"]},{"id":"jobsrunner","task":"JobRunner + JobLockService + JobRunLogger trên scheduler/** (GIỮ WorkerSchedulerService.setInterval — chỉ append register). Định nghĩa JobHandler interface + token SYSTEM_JOB_HANDLER (multi). JobLockService acquire/release system_job_locks(job_code) qua workerDb + assertWorkerRoleSafe('prod-only') TRƯỚC INSERT/UPDATE. JobRunLogger ghi start(Running)→finish(Success/Failed) qua workerDb; lọc secret cả error_message LẪN metadata (dùng AuditMaskerService.mask). JobRunner.runJob: acquire lock ĐÚNG 1 LẦN quanh TOÀN BỘ vòng lặp tenant → enumerate companies (withPlatformContext) → mỗi tenant ghi 1 run-row + gọi handler.runForTenant → finish; job fail KHÔNG sập app (try/catch, mẫu outbox). Wire register vào WorkerScheduler + SchedulerModule (import Retention/Files modules gom multi-provider). RED int-spec colocated.","builder":"backend-builder","paths":["apps/api/src/scheduler/**"]},{"id":"jobsretention","task":"RetentionCleanupJob (foundation/retention/**) implement JobHandler.runForTenant + đăng ký multi-provider SYSTEM_JOB_HANDLER cho job_code AUDIT_LOG_RETENTION/RETENTION_CLEANUP. GIỮ dryRun default TRUE + thêm kill-switch env (mẫu WORKERS_SCHEDULER_ENABLED); PROTECTED_TABLES GIỮ NGUYÊN (audit_logs/file_access_logs KHÔNG BAO GIỜ purge — BẤT BIẾN #2). Xóa comment TODO 'nợ lane DB / KHÔNG insert audit' vì run-row do JobRunner ghi. KHÔNG tự chế scheduler riêng.","builder":"backend-builder","paths":["apps/api/src/foundation/retention/**"]},{"id":"jobstempfile","task":"TempFileCleanupJob (foundation/files/**) implement JobHandler.runForTenant cho job_code TEMP_FILE_CLEANUP + đăng ký multi-provider. Thêm file.repository query eligible: is_temporary=true VÀ expires_at < now, HOẶC upload_status='Pending' quá TTL 'file.pending_ttl_hours' (SettingService.resolve + hằng fallback DEFAULT_PENDING_TTL_HOURS). Per tenant qua withTenant(companyId): storage.delete + soft-delete file (repo variant deletedBy nullable) + file_access_logs Delete + audit FileDeleted actorType='System'/actorUserId=null; BỎ QUA FilePolicy (job hệ thống, không user) — KHÔNG dùng FileService.deleteFile. Tái dùng audit object_type 'file' (đã có). Thêm default 'file.pending_ttl_hours' vào SETTING_DEFAULTS (append map frozen — 1 key). RED int-spec colocated.","builder":"backend-builder","paths":["apps/api/src/foundation/files/**","apps/api/src/foundation/settings/setting-defaults.ts"]}]
acceptanceChecks: ["Migration 0474 NỐI TIẾP head 0473 (journal idx đơn điệu, db:check xanh); system_job_runs + system_job_locks có ĐỦ cột DB-08 §8.14/8.15 + CHECK chk_system_job_runs_status + chk_system_job_runs_triggered_by.","Index đúng DB-09 §8.11/8.12: idx_system_job_runs_job_time(job_code,started_at DESC), idx_system_job_runs_company_job_time(company_id,job_code,started_at DESC), idx_system_job_runs_status_time(status,started_at DESC) WHERE status IN('Failed','Partial'), idx_system_job_locks_locked_until.","system_job_runs: RLS ENABLE+FORCE; policy system_job_runs_worker_all TO mediaos_worker USING true WITH CHECK true (mẫu outbox_worker_all 0003:66-69) BÊN CẠNH tenant-iso TO mediaos_app; GRANT SELECT app + SELECT,INSERT,UPDATE worker; KHÔNG có DELETE grant cho bất kỳ role.","Đo được RLS: worker INSERT thành công CẢ row per-tenant (company_id=X) LẪN row global (company_id NULL); app SELECT cross-tenant (company_id=OTHER) trả 0 row; app KHÔNG INSERT/UPDATE/DELETE được (no grant).","system_job_locks KHÔNG có company_id → KHÔNG RLS (infra worker-only, mẫu processed_events); GRANT SELECT,INSERT,UPDATE worker; release qua UPDATE locked_until (KHÔNG DELETE).","JobRunner acquire system_job_locks(job_code) ĐÚNG 1 LẦN quanh TOÀN BỘ vòng lặp per-tenant trong runJob; ghi 1 run-row/tenant BÊN TRONG lock; đo concurrency Ở CẤP job_code (không per-tenant).","Ghi run-row/lock qua workerDb (mediaos_worker), KHÔNG withTenant/mediaos_app; assertWorkerRoleSafe(mode:'prod-only') gọi TRƯỚC mọi INSERT/UPDATE; fail-closed (ném) khi DATABASE_WORKER_URL vắng ở NODE_ENV=production.","Bộ lọc secret che CẢ error_message LẪN metadata (JSONB) — token/khóa hiện '***' trong row đã ghi; job error → run status='Failed' + error_message đã lọc + tiến trình app KHÔNG sập (WorkerScheduler tick nuốt lỗi giữ nguyên).","RetentionCleanupJob wire qua JobRunner: dryRun default TRUE giữ nguyên + kill-switch env; PROTECTED_TABLES nguyên (audit_logs/file_access_logs deletedRecords=0 kể cả enabled+Delete+dryRun=false — BẤT BIẾN #2).","TempFileCleanupJob: soft-delete + storage.delete cho is_temporary quá expires_at HOẶC upload_status='Pending' quá 'file.pending_ttl_hours'; ghi file_access_logs Delete + audit FileDeleted actorType='System'/actorUserId=null; BỎ QUA FilePolicy; chạy trong withTenant(companyId); TÁI DÙNG audit object_type 'file' (KHÔNG thêm type mới).","SETTING_DEFAULTS có key 'file.pending_ttl_hours' (append map frozen, KISS 1 key) + fallback constant trong files.","KHÔNG thêm object_type SYSTEM_JOB_RUN_COMPLETED vào audit_logs CHECK (DEFER tôn trọng — system_job_runs tự đủ chi tiết kỹ thuật); DoD §8: test có, audit ghi hành động quan trọng, coverage ≥80% code job mới, FULL gate PASS + người chốt."]
testTasks: ["RED concurrency (int, gate hasDb && LANE_DB): chạy JobRunner.runJob(jobCode) đầy đủ ở 2 runner SONG SONG → ĐÚNG 1 chạy body / 1 skip Ở CẤP job_code (lock); assert đúng 1 run-row/tenant (không double).","RED job-error: handler ném → run-row status='Failed' + error_message set (đã mask) + tiến trình app SỐNG (không unhandled rejection; scheduler tick nuốt).","RED secret-in-metadata: gieo token vào metadata VÀ error_message → assert persisted row = '***' cho khóa nhạy cảm ở CẢ HAI field (chống lọt qua metadata JSONB).","RED worker-role fail-open: DATABASE_WORKER_URL vắng + NODE_ENV=production → assertWorkerRoleSafe ném TRƯỚC INSERT run-row/lock (mẫu outbox-worker test).","RLS acceptance (int): worker INSERT OK cho company_id=X và company_id NULL; app SELECT cross-tenant → 0 row; app không ghi được (no grant).","RED PROTECTED_TABLES (BẤT BIẾN #2): retention chạy trên policy entity audit_logs/file_access_logs (enabled+Delete+dryRun=false) → deletedRecords=0.","RED temp-file eligibility (int): file is_temporary quá expires_at + Pending quá pending_ttl_hours → soft-deleted + storage.delete gọi + file_access_logs Delete + audit FileDeleted actorType='System'/actorUserId=null; file temp CHƯA hết hạn + Pending mới → KHÔNG đụng; FilePolicy KHÔNG chặn (không user).","RED dry-run/kill-switch: retention job default dryRun=TRUE → không xóa thật; kill-switch env → job KHÔNG chạy.","Integration migration-smoke (gate hasDb && LANE_DB, lane db SERIAL): 0474 áp được; 2 bảng + index + policy + grant tồn tại đúng shape."]
steps: ["Lane jobsdb (SERIAL first): viết schema/system-jobs.ts (drizzle systemJobRuns + systemJobLocks) + hand-write migration 0474 mẫu outbox 0003: system_job_runs (company_id NULLABLE, CHECK status∈Running/Success/Failed/Partial/Skipped + triggered_by∈Scheduler/User/System, 3 index DB-09), RLS ENABLE+FORCE, policy system_job_runs_tenant_iso TO mediaos_app (USING own-tenant OR company_id IS NULL) + system_job_runs_worker_all TO mediaos_worker USING true WITH CHECK true, GRANT SELECT app / SELECT,INSERT,UPDATE worker (NO DELETE); system_job_locks (job_code PK, KHÔNG company_id → KHÔNG RLS, mẫu processed_events, index locked_until, GRANT SELECT,INSERT,UPDATE worker). Append schema/index.ts. db:check xanh.","Lane jobsrunner: định nghĩa JobHandler interface (runForTenant(companyId)→{total,success,failed}) + token SYSTEM_JOB_HANDLER; JobLockService.acquire (INSERT ... ON CONFLICT(job_code) DO UPDATE ... WHERE locked_until<now() RETURNING) + release (UPDATE locked_until=past) qua workerDb, assertWorkerRoleSafe('prod-only') 1 lần/instance TRƯỚC ghi; JobRunLogger.start/finish (workerDb) mask secret error_message+metadata; JobRunner.runJob(jobCode): lock 1 lần → enumerate companies (withPlatformContext) → per-tenant run-row+handler bên trong lock → release; try/catch bọc, fail không rethrow ra timer. Append register() vào WorkerSchedulerService (giữ setInterval) + SchedulerModule import RetentionModule+FilesModule.","Lane jobsretention: RetentionCleanupJob implements JobHandler, provide multi SYSTEM_JOB_HANDLER; giữ dryRun default TRUE + env kill-switch; PROTECTED_TABLES nguyên; dọn TODO comment cũ.","Lane jobstempfile: file.repository.findEligibleTempTx + softDeleteSystemTx (deletedBy nullable); TempFileCleanupJob (System actor, bypass FilePolicy, storage.delete+soft-delete+logs+audit trong withTenant); SETTING_DEFAULTS thêm file.pending_ttl_hours (+ fallback constant); provide multi handler.","Verify: FULL gate (security-reviewer + database-reviewer + silent-failure-hunter) + người chốt red-zone; chạy RED trên lane DB SERIAL cô lập (bash scripts/lane-db-setup.sh <lane> → export LANE_DB); coverage ≥80% code job mới; cập nhật harness/backlog.mjs."]
```

FRESH decomposition (docs/plans/S2-FND-JOBS-1.md chưa tồn tại; head migration = 0473 → kế 0474). Nguồn chuẩn: DB-08 §8.14/8.15 (shape 2 bảng + rule), DB-09 §8.11/8.12 (index), BACKEND-11 §18.1-18.7 + §22 (flow/lock/run-log/idempotency).

GAP ANALYSIS (code hiện tại):
- WorkerSchedulerService (scheduler/worker-scheduler.service.ts) đã có setInterval + tick-swallow an toàn, hiện CHỈ register 'outbox'. GIỮ cơ chế, chỉ append register cho system-jobs (KHÔNG đổi @nestjs/schedule).
- OutboxWorker (events/outbox-worker.ts) + assertWorkerRoleSafe (db/worker-role.ts) là MẪU CHUẨN để mirror: workerDb (mediaos_worker) + assertWorkerRoleSafe('prod-only') 1 lần/instance + fail-closed. RLS worker policy mẫu outbox_worker_all (mig 0003:66-69).
- RetentionCleanupJob (foundation/retention/retention-cleanup.job.ts) đã có skeleton run(companyId) + PROTECTED_TABLES trong RetentionService (28 bảng append-only, isProtectedTable public) — CHỈ cần implement JobHandler + wire multi-provider; comment TODO 'nợ lane DB / KHÔNG insert audit' đã LỖI THỜI (run-row do JobRunner ghi) → dọn.
- FileService.deleteFile CẦN user (FilePolicy) → job hệ thống KHÔNG dùng được; phải viết TempFileCleanupJob riêng + file.repository query eligible (is_temporary/expires_at + upload_status='Pending' index đã sẵn schema/files.ts:85,89) + softDelete variant deletedBy nullable. files.service.ts:59-62 đã để con trỏ TODO(S2-FND-JOBS-1) — WO này sở hữu.
- audit object_type 'file' ĐÃ có trong AUDIT_OBJECT_TYPES + DB CHECK (mig 0440, schema/audit.ts:233) → TempFileCleanup ghi FileDeleted KHÔNG cần type mới. Masker (audit-masker.service.ts) đệ quy che token/secret cả subtree → tái dùng cho lọc metadata run-row.
- Enumerate companies cross-tenant: mẫu master-data-seed-runner.listActiveCompanyIds qua db.withPlatformContext (mở app.platform_admin, chỉ 'companies' thấy chéo tenant). JobRunner dùng cho vòng per-tenant; run-row/lock vẫn qua workerDb.

INVARIANTS (CLAUDE §2):
- #1: mọi cleanup body qua withTenant(companyId); run-row/lock qua workerDb+RLS worker policy (KHÔNG bypassrls — assertWorkerRoleSafe chặn superuser).
- #2: system_job_runs kề audit → grant KHÔNG DELETE (append-ish; chỉ INSERT Running + UPDATE final). PROTECTED_TABLES tuyệt đối KHÔNG purge. TempFile = soft-delete file (KHÔNG hard-delete row) — storage object hard-delete SAU soft-delete (đúng port StorageAdapter.delete).
- #3: KHÔNG secret plaintext → lọc error_message + metadata trước ghi (masker).

QUYẾT ĐỊNH THIẾT KẾ:
- Lock granularity: 1 lock/job_code quanh TOÀN BỘ runJob (FIX round1), run-row per-tenant bên trong; concurrency đo ở job_code.
- system_job_locks KHÔNG company_id → KHÔNG RLS (mẫu processed_events infra worker-only); release qua UPDATE locked_until=past (tránh cần DELETE grant).
- system_job_runs app read policy: USING(own-tenant OR company_id IS NULL) = nullable-tenant (mẫu sequences/holidays) → tenant thấy row global + của mình, deny cross-tenant.
- JobHandler decouple qua token multi-provider SYSTEM_JOB_HANDLER: jobsrunner định nghĩa interface/token; jobsretention+jobstempfile provide handler của mình → SchedulerModule import 2 module gom lại (tránh phụ thuộc vòng; runner KHÔNG import trực tiếp job class).

VERIFY / GATE:
- FULL gate (migration + append-only-kề-audit + worker-role/RLS): security-reviewer + database-reviewer + silent-failure-hunter + người chốt red-zone TRƯỚC merge.
- Lane DB SERIAL (jobsdb chạy trước), test cô lập LANE_DB (int-spec gate hasDb && LANE_DB — bài học integration-test-lane-db-gate: .env làm hasDb=true → đỏ-giả). int-spec COLOCATE trong src/**/*.spec.ts (bài học vitest colocation; test/**/*.spec.ts KHÔNG chạy — chỉ *.int-spec.ts/*.e2e-spec.ts dưới test/).
- Thứ tự thi công: jobsdb → jobsrunner → {jobsretention ∥ jobstempfile}.

PATH FLAG: 'file.pending_ttl_hours' default sống ở foundation/settings/setting-defaults.ts — NGOÀI paths literal của WO (chỉ liệt kê foundation/files/**). Đã thêm file đơn này vào paths lane jobstempfile (append 1 key vào SETTING_DEFAULTS frozen map, additive, KISS); nếu coordinator muốn giữ chặt WO-scope thì dùng hằng fallback DEFAULT_PENDING_TTL_HOURS trong files/** thay vì sửa settings. Khuyến nghị: thêm ở setting-defaults (canonical) + fallback constant (defense-in-depth).

OUT-OF-SCOPE (chống scope-creep): DASHBOARD_CACHE_CLEANUP/NOTIFICATION_RETRY/TASK_OVERDUE_DETECTOR/LEAVE_ACCRUAL (BACKEND-11 §18.2 registry) KHÔNG làm ở khung tối thiểu — chỉ 2 job MVP (RETENTION + TEMP_FILE). FOUNDATION.JOB.RUN permission + endpoint chạy tay (triggered_by=User) DEFER. Partition-by-month system_job_runs DEFER.

DEFER (chốt sẵn, KHÔNG hỏi lại): audit summary SYSTEM_JOB_RUN_COMPLETED (DB-08 §8.14 mục 4) KHÔNG ghi ở khung tối thiểu — system_job_runs đã lưu đủ chi tiết kỹ thuật (started/finished/duration/counts/error); tránh thêm object_type mới vào audit_logs CHECK cho việc trùng lặp. Ghi rõ để lane sau (nếu cần dashboard audit) mới bổ sung.
</content>
