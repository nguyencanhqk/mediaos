```yaml
wo: S2-FND-JOBS-1
zone: red
generated_by: auto-loop
reconciled_at: "bdb0dba"
lanes: [{"id":"jobs_db","task":"[JOBS-DB · db-migration · crown RLS/migration] Migration NỐI TIẾP 0475 (idx 155, when > 1717500765000, nối sau 0474): CREATE TABLE system_job_runs theo DB-08 §8.14 (ĐỦ cột: id, company_id nullable KHÔNG DEFAULT + FK companies, job_code, status, triggered_by, triggered_by_user_id FK users, started_at, finished_at, duration_ms bigint, total_items, success_items, failed_items, error_message text, metadata jsonb, created_at) + 2 CHECK (chk_system_job_runs_status IN Running/Success/Failed/Partial/Skipped; chk_system_job_runs_triggered_by IN Scheduler/User/System) + 3 index DB-09 §8.11 (idx_system_job_runs_job_time; idx_system_job_runs_company_job_time; idx_system_job_runs_status_time (status,started_at DESC) WHERE status IN ('Failed','Partial')). RLS ENABLE+FORCE: policy system_job_runs_tenant_iso TO mediaos_app USING (company_id = NULLIF(current_setting('app.current_company_id',true),'')::uuid OR company_id IS NULL) + policy system_job_runs_worker_all TO mediaos_worker USING(true) WITH CHECK(true) (mẫu outbox_worker_all mig 0003:66-69). CREATE TABLE system_job_locks (§8.15: job_code varchar(100) PK, locked_by varchar(255), locked_until, acquired_at, metadata jsonb) — KHÔNG company_id → KHÔNG RLS (worker-infra, mẫu processed_events) + index idx_system_job_locks_locked_until. GRANT: mediaos_app SELECT-only trên runs; mediaos_worker SELECT/INSERT/UPDATE trên runs+locks; TUYỆT ĐỐI KHÔNG GRANT DELETE cho role nào (release lock = UPDATE locked_until về quá khứ, KHÔNG DELETE). Drizzle schema apps/api/src/db/schema/system-jobs.ts parity (KHÔNG db:generate — RLS/grant thủ công) + append `export * from \"./system-jobs\"` vào index.ts. Viết int-spec cấu trúc: CHECK, RLS enabled+forced, grant no-DELETE, 2-tenant app-role isolation (thấy A + NULL, KHÔNG thấy B), worker USING(true) bypass. db:check chain xanh.","builder":"db-migration","paths":["apps/api/migrations/0475_s2_fndjobs1_system_jobs.sql","apps/api/migrations/meta/_journal.json","apps/api/src/db/schema/system-jobs.ts","apps/api/src/db/schema/index.ts","apps/api/test/integration/system-jobs-schema.int-spec.ts"]},{"id":"jobs_runner","task":"[JOBS-RUNNER · backend-builder · crown worker/audit/secret-scrub] File STANDALONE apps/api/src/scheduler/job-handler.ts export token SYSTEM_JOB_HANDLER (multi-provider) + interface JobHandler { jobCode: string; run(ctx:{companyId}): Promise<{total,success,failed,metadata?}> } — BỎ tham số tx (chốt round4). JobLockService + JobRunLogger ghi qua workerDb (mediaos_worker), gọi assertWorkerRoleSafe(mode:'prod-only') TRƯỚC MỌI INSERT/UPDATE, fail-closed khi workerDb vắng (throw). String-scrubber RIÊNG (regex redact password=/token=/secret=/api_key= + credential-in-URL user:pass@host) áp cho error_message TRƯỚC ghi; metadata jsonb qua AuditMaskerService.mask (EventsModule @Global). JobRunner.runJob(jobCode, handler): assertWorkerRoleSafe → acquire lock system_job_locks(job_code) ĐÚNG 1 LẦN quanh TOÀN BỘ vòng lặp per-tenant (INSERT ON CONFLICT DO UPDATE WHERE locked_until<now() RETURNING → rỗng=skip) → enumerate companyIds qua DatabaseService.withPlatformContext (materialize danh sách RỒI đóng tx, KHÔNG gọi handler.run bên trong → chống nested-context) → mỗi tenant: JobRunLogger.start(company_id TƯỜNG MINH) run-row Running (workerDb, system_job_runs KHÔNG DEFAULT company_id) → try handler.run({companyId}) → finish() ĐÚNG 1 LẦN (Running→Success/Failed/Partial, không expose gọi lại) → release lock (UPDATE locked_until past). Job fail 1 tenant KHÔNG sập app/tenant khác (mẫu outbox try/catch per-item). Wire SchedulerModule: providers JobLockService/JobRunLogger/JobRunner + import RetentionModule/FilesModule để gom SYSTEM_JOB_HANDLER[] (chiều Scheduler→feature; feature KHÔNG import SchedulerModule); WorkerSchedulerService đăng ký thêm 1 tick 'system-jobs' gọi JobRunner cho từng handler (interval qua worker-scheduler.config; tắt khi NODE_ENV=test / WORKERS_SCHEDULER_ENABLED=false). RED tests: assertWorkerRoleSafe điều-kiện-THẬT (NODE_ENV=production + role bypassrls thật, mẫu test/integration/db-roles.int-spec.ts), secret-scrub (Error('connect failed password=abc123') → error_message đã redact), lock single-active (2 runner song song → 1 chạy 1 skip ở cấp job_code), finish-once semantics, company_id ghi tường minh.","builder":"backend-builder","paths":["apps/api/src/scheduler/job-handler.ts","apps/api/src/scheduler/job-lock.service.ts","apps/api/src/scheduler/job-run-logger.ts","apps/api/src/scheduler/job-error-scrubber.ts","apps/api/src/scheduler/job-runner.ts","apps/api/src/scheduler/scheduler.module.ts","apps/api/src/scheduler/worker-scheduler.service.ts","apps/api/src/scheduler/worker-scheduler.config.ts","apps/api/src/scheduler/job-runner.spec.ts","apps/api/src/scheduler/job-error-scrubber.spec.ts","apps/api/test/integration/job-runner-worker-role.int-spec.ts","apps/api/test/integration/system-job-lock.int-spec.ts"]},{"id":"jobs_retention","task":"[JOBS-RETENTION · backend-builder · crown retention/append-only] Bọc RetentionCleanupJob hiện có thành RetentionCleanupJobHandler implements JobHandler (jobCode ví dụ 'AUDIT_LOG_RETENTION'/'RETENTION_CLEANUP'): run({companyId}) gọi RetentionCleanupJob.run(companyId, {dryRun}) — RetentionCleanupJob/RetentionService ĐÃ tự mở withTenant nội bộ (KHÔNG sửa RetentionCleanupJob, KHÔNG nhận tx). dryRun default TRUE giữ nguyên (§17.4) + đọc kill-switch env (ví dụ RETENTION_JOB_ENABLED). PROTECTED_TABLES giữ nguyên (audit_logs/file_access_logs KHÔNG BAO GIỜ purge — BẤT BIẾN #2, đã ép ở RetentionService.PROTECTED_TABLES + REVOKE-DELETE DB). Map JobRunResult → {total,success,failed,metadata:{policiesProcessed,totalDeleted,dryRun}}. Đăng ký `{ provide: SYSTEM_JOB_HANDLER, useClass: RetentionCleanupJobHandler, multi: true }` + export trong RetentionModule; import job-handler.ts (token) — KHÔNG import SchedulerModule. RED/unit: handler chạy per-tenant có ghi run-row; dryRun mặc định; PROTECTED_TABLES giữ deletedRecords=0.","builder":"backend-builder","paths":["apps/api/src/foundation/retention/retention-cleanup.job-handler.ts","apps/api/src/foundation/retention/retention.module.ts","apps/api/src/foundation/retention/retention-cleanup.job-handler.spec.ts"]},{"id":"jobs_tempfile","task":"[JOBS-TEMPFILE · backend-builder · crown audit/file-soft-delete] TempFileCleanupJobHandler implements JobHandler (jobCode 'TEMP_FILE_CLEANUP'): run({companyId}) TỰ mở withTenant(companyId) nội bộ (KHÔNG nhận tx từ JobRunner). Eligibility = (is_temporary=true AND expires_at<now) OR (upload_status='Pending' quá TTL setting 'file.pending_ttl_hours') — AND deleted_at IS NULL — AND NOT EXISTS(file_links active WHERE file_id=files.id AND deleted_at IS NULL). Thêm repository method softDeleteBySystemTx (deleted_by=NULL, upload_status='Deleted' — KHÔNG dùng softDeleteTx yêu cầu deletedBy non-null; KHÔNG hard-delete BẤT BIẾN #2) + query find eligible. Với mỗi file eligible (cùng tx): ghi file_access_logs action='Delete' accessGranted=true actorUserId=null + audit.record objectType='file' (đã có trong CHECK, KHÔNG migration) action='FileDeleted' actorType='System' actorUserId=null resultStatus='Success' dataScope='Company'. BỎ QUA FilePolicy (KHÔNG đi FileService.deleteFile). Thêm default 'file.pending_ttl_hours' vào setting-defaults.ts (Number, category File, isPublic true — KHÔNG secret). Đăng ký `{ provide: SYSTEM_JOB_HANDLER, useClass: TempFileCleanupJobHandler, multi: true }` + export trong FilesModule; import job-handler.ts — KHÔNG import SchedulerModule. RED test (LANE_DB): temp-expired+linked → KHÔNG xóa; temp-expired+KHÔNG link → xóa (deleted_by=NULL, upload_status='Deleted', file_access_logs Delete, audit System/null); Pending quá TTL → xóa.","builder":"backend-builder","paths":["apps/api/src/foundation/files/temp-file-cleanup.job-handler.ts","apps/api/src/foundation/files/temp-file-cleanup.repository.ts","apps/api/src/foundation/files/files.module.ts","apps/api/src/foundation/settings/setting-defaults.ts","apps/api/src/foundation/files/temp-file-cleanup.job-handler.spec.ts","apps/api/test/integration/temp-file-cleanup.int-spec.ts"]}]
acceptanceChecks: ["Migration 0475_s2_fndjobs1_system_jobs.sql tồn tại; _journal.json thêm idx 155 với when > 1717500765000 (nối tiếp đơn điệu sau 0474); pnpm db:migrate + db:check xanh trên LANE_DB.","system_job_runs có ĐỦ cột DB-08 §8.14 (15 cột incl id/created_at) + CHECK chk_system_job_runs_status (Running/Success/Failed/Partial/Skipped) + CHECK chk_system_job_runs_triggered_by (Scheduler/User/System); company_id NULLABLE + KHÔNG DEFAULT (khác audit_logs); 3 index idx_system_job_runs_job_time / _company_job_time / _status_time (partial WHERE status IN Failed,Partial).","system_job_runs RLS ENABLE + FORCE; policy tenant_iso TO mediaos_app USING(company_id match GUC OR company_id IS NULL); policy system_job_runs_worker_all TO mediaos_worker USING(true) WITH CHECK(true). system_job_locks KHÔNG có company_id + KHÔNG RLS; PK job_code; index idx_system_job_locks_locked_until.","GRANT (verify information_schema.role_table_grants): mediaos_app = SELECT-only trên system_job_runs; mediaos_worker = SELECT/INSERT/UPDATE trên runs+locks; KHÔNG có DELETE cho bất kỳ role nào trên cả 2 bảng.","drizzle apps/api/src/db/schema/system-jobs.ts parity với DDL; index.ts có `export * from \"./system-jobs\"` (append-only, KHÔNG rewrite).","apps/api/src/scheduler/job-handler.ts là file STANDALONE export token SYSTEM_JOB_HANDLER + interface JobHandler; RetentionModule/FilesModule import file token này, KHÔNG import SchedulerModule (verify: không có import cycle; SchedulerModule import Retention/FilesModule một chiều).","interface JobHandler.run signature = run(ctx:{companyId}): Promise<{total,success,failed,metadata?}> — KHÔNG có tham số tx.","JobLockService + JobRunLogger gọi assertWorkerRoleSafe(mode:'prod-only') TRƯỚC mọi INSERT/UPDATE run-row/lock; ghi qua workerDb; fail-closed (throw) khi DATABASE_WORKER_URL/workerDb vắng.","JobRunner acquire lock system_job_locks(job_code) ĐÚNG 1 LẦN quanh toàn bộ vòng per-tenant; ghi 1 run-row/tenant BÊN TRONG; enumerate tenant qua withPlatformContext materialize RỒI mới gọi handler.run ngoài tx (không nested-context).","JobRunLogger truyền company_id TƯỜNG MINH mỗi lần ghi run-row; finish() finalize Running→Success/Failed/Partial ĐÚNG 1 LẦN (không có API gọi lại cho cùng runId).","error_message qua string-scrubber (redact password=/token=/secret=/api_key=/user:pass@host) TRƯỚC ghi; metadata jsonb qua AuditMaskerService.mask.","RetentionCleanupJob wire qua JobRunner (bọc handler, TỰ mở withTenant); dryRun default TRUE + kill-switch env; PROTECTED_TABLES giữ nguyên (audit_logs/file_access_logs deletedRecords=0); chạy thật per-tenant ghi run-row.","TEMP_FILE_CLEANUP: eligibility đúng công thức (is_temporary+expires_at<now) OR (Pending quá file.pending_ttl_hours) AND deleted_at IS NULL AND NOT EXISTS(file_links active); soft-delete deleted_by=NULL + upload_status='Deleted'; ghi file_access_logs Delete + audit objectType='file' actorType='System' actorUserId=null; BỎ QUA FilePolicy.","default 'file.pending_ttl_hours' thêm vào setting-defaults.ts (Number, isPublic true, KHÔNG secret — BẤT BIẾN #3).","Không có object_type mới thêm vào audit_logs CHECK (SYSTEM_JOB_RUN_COMPLETED DEFER; objectType='file' đã có sẵn); KHÔNG migration cho files.deleted_by (đã nullable).","Mọi file ≤800 dòng; pnpm typecheck + lint xanh; test xanh trên LANE_DB=mediaos_jobs; FULL gate (security-reviewer + database-reviewer) PASS + owner chốt red-zone."]
testTasks: ["[RED · integration LANE_DB · jobs_db] 2-tenant isolation cho system_job_runs qua app-role: set app.current_company_id=A → SELECT thấy run-row company_id=A VÀ run-row company_id IS NULL (global), NHƯNG 0 row của tenant B (seed sẵn run-row B). Bài test RIÊNG, khác test worker-bypass.","[RED · integration LANE_DB · jobs_db] worker-bypass: mediaos_worker (workerDb) thấy/ghi run-row của MỌI tenant (policy USING(true)/WITH CHECK(true)); ghi company_id tường minh cho A và B đều thành công.","[RED · integration LANE_DB · jobs_db] cấu trúc DB: CHECK status + triggered_by chặn giá trị sai; RLS enabled+forced trên runs; system_job_locks không RLS; grant no-DELETE cho mọi role (query role_table_grants).","[RED · integration LANE_DB · jobs_runner] assertWorkerRoleSafe điều-kiện-THẬT: NODE_ENV=production + kết nối role bypassrls thật (mẫu db-roles.int-spec.ts) → JobLockService/JobRunLogger NÉM trước INSERT (không mock).","[RED · unit · jobs_runner] secret-scrub: JobRunLogger.finish với Error('connect failed password=abc123') → system_job_runs.error_message đã redact 'abc123'/'password='; token EMBEDDED trong chuỗi (không phải key-based) vẫn bị che; metadata có key nhạy cảm → mask() che.","[RED · integration LANE_DB · jobs_runner] lock single-active: 2 JobRunner song song cùng job_code → 1 chạy (ghi run-row) 1 skip Ở CẤP job_code (lock acquire rỗng RETURNING).","[unit · jobs_runner] finish-once semantics: 1 run-row chỉ chuyển Running→terminal 1 lần; company_id ghi tường minh mỗi run-row; job fail 1 tenant KHÔNG chặn tenant kế.","[RED · integration LANE_DB · jobs_tempfile] link-safety: temp-expired + có file_link active → KHÔNG xóa; temp-expired + KHÔNG link → xóa (deleted_by=NULL, upload_status='Deleted', file_access_logs Delete, audit actorType='System' actorUserId=null); upload_status='Pending' quá file.pending_ttl_hours → xóa.","[unit · jobs_retention] RetentionCleanupJobHandler: dryRun default true; PROTECTED_TABLES (audit_logs/file_access_logs) deletedRecords=0 kể cả enabled; chạy per-tenant map ra {total,success,failed,metadata}.","[verify · Đội 3] chạy qua bash scripts/lane-db-setup.sh <lane> → export LANE_DB=mediaos_jobs → pnpm --filter @mediaos/api test (vitest.config.ts đã redirect DATABASE_URL/DIRECT/WORKER theo LANE_DB — verify chạy đúng, KHÔNG cần config worker-url mới)."]
steps: ["1. jobs_db (NỐI TIẾP, TRƯỚC mọi lane BE): migration 0475 tạo system_job_runs (RLS+FORCE + policy app-tenant/worker-all + grant no-DELETE) + system_job_locks (no-RLS worker-infra) + index DB-09 §8.11/8.12; drizzle system-jobs.ts + append index.ts; db:migrate + db:check xanh. RLS+FORCE dựng TRƯỚC khi bất kỳ lane nào ghi run-row (CLAUDE §3).","2. jobs_runner (depends jobs_db): job-handler.ts token STANDALONE + JobLockService/JobRunLogger (workerDb + assertWorkerRoleSafe prod-only + fail-closed) + string-scrubber error_message + JobRunner (lock 1-lần/job_code, enumerate withPlatformContext materialize, per-tenant run-row company_id tường minh, finish 1-lần, không nested-context) + SchedulerModule wiring (import Retention/FilesModule gom SYSTEM_JOB_HANDLER[]) + WorkerScheduler tick 'system-jobs'.","3. jobs_retention (depends jobs_runner token): RetentionCleanupJobHandler bọc RetentionCleanupJob (tự withTenant, dryRun default true, kill-switch, PROTECTED_TABLES nguyên) + register multi trong RetentionModule.","4. jobs_tempfile (depends jobs_runner token; path-disjoint với jobs_retention nhưng WO chốt chạy TUẦN TỰ trong cùng vòng): TempFileCleanupJobHandler (tự withTenant, eligibility + softDeleteBySystem deleted_by=NULL + file_access_logs Delete + audit System/null, bỏ FilePolicy) + default file.pending_ttl_hours + register multi trong FilesModule.","5. RED-trước cho mọi test nhạy cảm (2-tenant isolation, worker-bypass, assertWorkerRoleSafe điều-kiện-thật, secret-scrub, lock single-active, tempfile link-safety) chạy trên LANE_DB=mediaos_jobs qua lane-db-setup.sh; sau đó GREEN.","6. FULL gate (security-reviewer + database-reviewer) + người chốt red-zone TRƯỚC merge."]
```

GAP-ANALYSIS (đã đối chiếu code hiện tại 2026-07-04, head idx 154 / 0474): (1) Chưa có bảng system_job_runs/system_job_locks, chưa có drizzle schema system-jobs.ts → lane jobs_db build mới. (2) Scheduler hiện chỉ có WorkerSchedulerService chạy MỖI outbox (worker-scheduler.service.ts:58) + SchedulerModule tối giản; chưa có JobRunner/JobHandler/lock/logger → lane jobs_runner build mới. (3) RetentionCleanupJob (retention-cleanup.job.ts) ĐÃ có, TỰ quản tenant-context (run(companyId)→listEnabledPolicies/runCleanup đều db.withTenant) — KHÔNG sửa; chỉ bọc handler. RetentionModule đã export RetentionCleanupJob. (4) FileService.deleteFile ĐÃ có mẫu soft-delete+file_access_logs Delete+audit objectType='file' (files.service.ts:705-762) NHƯNG đi qua FilePolicy + softDeleteTx yêu cầu deletedBy non-null → TEMP_FILE_CLEANUP KHÔNG tái dùng deleteFile: cần repository method softDeleteBySystemTx (deleted_by=NULL) + bỏ FilePolicy (WO chốt). files.deleted_by ĐÃ nullable (files.ts:72) → KHÔNG migration.

INVARIANTS: #1 company_id/RLS — system_job_runs RLS+FORCE + policy app (USING company_id match GUC OR IS NULL) + worker_all (mẫu outbox mig 0003:66-69); system_job_locks KHÔNG company_id → KHÔNG RLS (worker-infra, mẫu processed_events). RLS+FORCE dựng TRƯỚC ghi (CLAUDE §3). Handler TỰ mở withTenant nội bộ; JobRunner enumerate qua withPlatformContext (materialize list RỒI đóng tx trước khi gọi handler → chống nested/double-context, round4). #2 append-only-có-kiểm-soát: system_job_runs KHÔNG DELETE role nào (grant); UPDATE CHỈ để finalize 1-lần Running→terminal (JobRunLogger.finish, không phải append-only thuần — GHI RÕ cho database-reviewer). RetentionCleanupJob PROTECTED_TABLES (audit_logs/file_access_logs) không bao giờ purge. TEMP_FILE_CLEANUP soft-delete (deleted_at/deleted_by=NULL/upload_status='Deleted'), KHÔNG hard-delete. #3 secret: error_message qua string-scrubber RIÊNG (regex redact password=/token=/secret=/api_key=/credential-in-URL user:pass@host) vì AuditMaskerService.mask là key-based passthrough scalar (không che token nhúng trong chuỗi tự do); metadata jsonb dùng mask() đủ. assertWorkerRoleSafe(mode:'prod-only') TRƯỚC mọi ghi worker (mẫu OutboxWorker), fail-closed khi workerDb vắng.

PIN round4 (KHÔNG hỏi lại): JobHandler.run(ctx:{companyId}) — BỎ tham số tx; JobRunner không mở transaction/context cho business logic (chỉ workerDb ghi run-row/lock ngoài withTenant, đúng thiết kế worker-role). Lock system_job_locks(job_code) acquire ĐÚNG 1 LẦN quanh toàn bộ vòng per-tenant, ghi 1 run-row/tenant bên trong. finish() 1-lần/runId. company_id TƯỜNG MINH mỗi run-row (system_job_runs KHÔNG DEFAULT company_id — khác audit_logs vì worker ghi không qua GUC). RED test 2-tenant isolation là bài RIÊNG (integration app-role), khác worker-bypass. assertWorkerRoleSafe RED dựng ĐIỀU KIỆN THẬT (NODE_ENV=production + role bypassrls thật, mẫu test/integration/db-roles.int-spec.ts). "13 cột" trong done_when là con số ước lệ — CHUẨN là DB-08 §8.14 (15 cột incl id/created_at); spec thắng. release lock = UPDATE locked_until về quá khứ (KHÔNG DELETE vì không grant DELETE).

CROSS-TENANT READ (chủ ý N=1): row company_id IS NULL trong system_job_runs là đường đọc chéo-tenant DUY NHẤT app-role thấy (job cấp system). Ghi lại cho lần mở đa-tenant sau XEM LẠI policy tenant_iso (khi N>1, cân nhắc tách quyền đọc global khỏi tenant thường).

VERIFY: chạy LANE_DB=mediaos_jobs qua scripts/lane-db-setup.sh (vitest.config.ts đã redirect DATABASE_URL/DIRECT/WORKER theo LANE_DB — không cần config worker-url mới). Migration nối tiếp: idx 155, when 1717500770000 > 0474 (1717500765000). db:check chain xanh.

GATE: red-zone → FULL gate = security-reviewer + database-reviewer (silent-failure-hunter KHÔNG tồn tại trong repo — đủ theo harness/policy.md hiện hành) + santa-method cho logic crown (runner/lock/scrub) + người chốt TRƯỚC merge. Model: crown → Opus code+review; plan qua plan-reviewer.

HOT-FILE & THỨ TỰ MERGE: schema/index.ts (jobs_db, APPEND `export * from ./system-jobs`, KHÔNG rewrite); _journal.json (jobs_db, append idx 155); scheduler.module.ts (jobs_runner-owned, thêm providers + import Retention/FilesModule); retention.module.ts providers (jobs_retention-owned, thêm multi-provider handler); files.module.ts providers + setting-defaults.ts (jobs_tempfile-owned, append). KHÔNG có hot-file chia sẻ chéo lane (mỗi module file 1 chủ). Multi-provider SYSTEM_JOB_HANDLER: mỗi feature-module provide+export (multi:true), SchedulerModule import cả 2 để gom → VERIFY Nest aggregate đúng mảng handler (rủi ro nếu module không export token). Thứ tự thi công: jobs_db → jobs_runner → jobs_retention/jobs_tempfile (TUẦN TỰ trong cùng vòng theo WO; jobs_retention & jobs_tempfile path-disjoint, kỹ thuật có thể song song nhưng đều phụ thuộc job-handler.ts của jobs_runner).

OUT-OF-SCOPE (xác nhận): API /system-jobs trigger tay DEFER; audit summary action SYSTEM_JOB_RUN_COMPLETED DEFER (system_job_runs tự đủ chi tiết, KHÔNG thêm object_type audit_logs CHECK); files.deleted_by KHÔNG migration (đã nullable); DASHBOARD_CACHE_CLEANUP/NOTIFICATION_RETRY/TASK_OVERDUE/LEAVE_ACCRUAL (§18.2) KHÔNG build đợt này — chỉ RETENTION + TEMP_FILE_CLEANUP; partition-by-month system_job_runs = FUTURE.
</content>
