# S15-PAYROLL-BE-4B — Trả nợ FULL gate BE-4 (reader `users.status` · Own-guard 065 · rowCount XLSX · `mappedWrite` · `.strict()` · Logger 071 · EXISTS đợt)

> 🟡 LIGHT gate (`typescript-reviewer`) + `security-reviewer` HẸP **một câu** (vế `users.status` ở reader quyền).
> KHÔNG migration. Nhánh `feat/s15-payroll-be-4b` cắt từ `master` `47cf75bb` (BE-4 #511 đã merge). Lane `mediaos_be4b`
> (dựng sạch từ chain 0000→0575). Nguồn nợ: `docs/plans/S15-PAYROLL-BE-4.md` **§11b** — bảng phát hiện của ba reviewer
> ngày 15/09/2026 (security M3 · L4 · L5 · L6 · L7 · silent-failure #4 · database LOW-1). Không mở lại việc đã chốt ở BE-4.

## 1. Bảy hạng mục — mỗi cái vá ở đâu, RED bằng gì

| #      | Nợ (BE-4 §11b)                                                                                                                                                                                                              | Vá                                                                                                                                                                                                                                                                                     | RED trước (đã đo đỏ rồi mới vá)                                                                                                                                                                                                                                                                                                                                                          |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| M3     | Hai reader quyền (`PayrollPairHoldersReader` · `PayrollApproverReader`) không soi `users.status` ⇒ holder bị đình chỉ vẫn được đếm: C3/017 xanh giả (không ai hoàn tất/duyệt được), NOTI 020/024/027 gửi vào tài khoản chết | **MỘT câu SQL** `pairHoldersQuery(companyId, pair, excludeUserId)` export từ `payroll-pair-holders.reader.ts`, thêm vế `u.status = 'active'` (allow-list, khớp cổng đăng nhập `auth.service.ts`); `PayrollApproverReader.eligibleApproverIdsTx` gọi cùng hàm (xoá bản sao SQL thứ hai) | int-spec batches: **C3b** (holder thứ hai `suspended` ⇒ vẫn 422 017; kích hoạt lại ⇒ 201 — đối chứng ALLOW cùng người/cùng cặp, chỉ đổi status) · **NOTI-027** không tới viewer đình chỉ · int-spec advances: admin2 đình chỉ **không** trong `recipientUserIds` 024 · người duyệt DUY NHẤT đình chỉ ⇒ `warnings ["no-eligible-approver"]`, 0 outbox; kích hoạt lại ⇒ 024 tới đúng người |
| L4     | 065 Own đi qua `filterCond` với truthy-guard `if (f.userId)` — `userId` rỗng ⇒ vế lọc biến mất ⇒ danh sách TOÀN CÔNG TY (có `amount`)                                                                                       | Repo: `PayrollAdvanceOwnFilter { ownerUserId: string }` + `listOwnTx/countOwnTx` qua `ownCond` — rỗng/khoảng trắng ⇒ **ném** trước khi chạm `tx`; `filterCond` (059 quản trị) đổi `if (f.userId !== undefined)`; service `listMine` dùng hai hàm Own                                   | unit `payroll-advances.repository.spec.ts`: `""` · `undefined` · `"   "` ⇒ rejects `/ownerUserId/`, `tx.select` **không** được gọi                                                                                                                                                                                                                                                       |
| L5     | XLSX dựng cả ma trận chuỗi rồi `parseAdjustmentMatrix` mới đếm dòng (zip-bomb qua cap 5 MB)                                                                                                                                 | `parseXlsx`: `sheet.rowCount > MAX_ROWS + 1` ⇒ `PayrollImportParser.tooLarge()` TRƯỚC `eachRow`; factory `tooLarge()` là MỘT nguồn cho cả parser lẫn service (service không còn dựng lỗi riêng)                                                                                        | unit `payroll-import.parser.spec.ts`: 5.001 dòng ⇒ 422 `import-too-large` **và** spy `Worksheet.prototype.eachRow` không được gọi; 5.000 dòng ⇒ đủ header + N (không từ chối oan); XLSX hỏng ⇒ `import-invalid {reason: parse}`                                                                                                                                                          |
| L6     | 4 chỗ `throw mapPayrollPgError(err) ?? err` ở `payroll-advances.service.ts` ném lại `DrizzleQueryError` thô — message «params: [...]» chở `amount`, filter log stack 5xx                                                    | `payroll-pg-write.util.ts` (mới) `mappedWrite(table, fn)` = thân cũ của `mappedLineWrite` tổng quát hoá theo nhãn bảng; `mappedLineWrite` (batches) = delegate, nhãn `payroll_payment_lines` giữ nguyên; 4 catch của advances ⇒ `mappedWrite("payroll_advances", …)`                   | unit `payroll-pg-write.util.spec.ts`: 22003 không map ⇒ Error mới có `code/constraint`, KHÔNG số tiền, KHÔNG `cause`; 23514 tag lạ ⇒ `tag=<trigger>:<tag>`; 23505 đã map ⇒ 409 `batch-code-exists` đi thẳng; lỗi không phải PG ⇒ `code=unknown`, không lộ message gốc                                                                                                                    |
| L7     | 4 schema chưa `.strict()`                                                                                                                                                                                                   | `approvePayrollAdvanceSchema` (063) · `rejectPayrollAdvanceSchema` (064) · `completePaymentBatchSchema` (072) · `payrollAdvanceListQuerySchema` (059)                                                                                                                                  | contracts `payroll-disbursement.spec.ts` (4 schema: hợp lệ qua, `amount` lạ ⇒ `unrecognized_keys`; 059 `user_id` gõ nhầm ⇒ từ chối) + int-spec advances: 063 body `{note, amount}` ⇒ **400**, hàng vẫn `Pending`                                                                                                                                                                         |
| SFH#4  | 071: audit `read` commit trong tx TRƯỚC khi sinh tệp — `exceljs` ném ⇒ có audit «đã đọc» mà không tệp nào rời server, 0 dấu vết                                                                                             | Tách `buildUncBuffer(batch, rows, names)`; `export` bọc try/catch ⇒ `Logger.error("PAYROLL-API-071: audit 'read' đã ghi cho đợt <id> (<n> dòng) nhưng sinh tệp UNC thất bại…", ctx)` rồi **rethrow** (filter tự log stack). Message chỉ id đợt + số dòng — không TK/tiền               | unit export spec: spy `buildUncBuffer` reject ⇒ audit 1 hàng · `Logger.error` 1 lần (có batchId + «071», không số TK, không `1000                                                                                                                                                                                                                                                        | net`) · rejects; đường xanh 0 lời gọi |
| DB-LOW | `insertLinesTx` INSERT…SELECT không soi đợt — FK không soi `company_id`, trigger guard là lưới cuối                                                                                                                         | Thêm `EXISTS (select 1 from payroll_payment_batches b where b.id = :batch and b.company_id = :company and b.deleted_at is null)` ngay trong câu                                                                                                                                        | int-spec batches: batch id ma ⇒ `[]` (trước vá: 23514 `payroll_payment_line_guard` thô); đợt THẬT của tenant B gọi dưới `withTenant(A)` ⇒ `[]` và 0 dòng                                                                                                                                                                                                                                 |

### 1b. Quyết định nhỏ (ghi để lật rẻ)

| #   | Chốt                                                                                                        | Vì sao                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | ----------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Q-1 | Vế tài khoản = **allow-list** `status = 'active'` (không deny-list `'suspended'`)                           | Khớp `auth.service.ts` (cổng đăng nhập allow-list; trạng thái mới mặc định KHÔNG lọt). Không soi `locked_at`/`must_change_password`/`require_two_factor`: đó là cổng phiên đăng nhập, không phải «tài khoản không tồn tại để duyệt» — người bị khoá tạm vẫn là người duyệt hợp lệ khi mở khoá; security-reviewer HẸP xác nhận ở §3                                                                 |
| Q-2 | «list» của L7 = **059** (`payrollAdvanceListQuerySchema`) — schema query ĐẦU TIÊN trong repo có `.strict()` | 059 là list DUY NHẤT của track C mang bộ lọc THEO NGƯỜI (`userId`): khoá gõ sai (`user_id`/`userid`) bị Zod cũ bỏ im lặng ⇒ trả toàn công ty (có `amount`) trong khi người gọi tưởng đã lọc một người. Các list còn lại (066/070/074) không mang bộ lọc theo người — giữ nguyên, tránh đổi hành vi rộng. FE-3 phải KHÔNG gửi khoá lạ lên 059 (cache-buster đặt ở header/URL khác, không vào query) |
| Q-3 | `rowCount` (hàng vật lý, kể cả trống ở giữa) chứ không `actualRowCount`                                     | Mục tiêu là chặn CHI PHÍ dựng ma trận, nên đếm cái rẻ nhất và bảo thủ nhất; tệp có ≤ 5.000 dòng dữ liệu nhưng dải hàng vật lý > 5.001 (rải xa) bị từ chối cùng mã/kind `import-too-large` — chấp nhận, khuôn mẫu 077 không sinh tệp như vậy                                                                                                                                                        |
| Q-4 | `mappedWrite` bọc **mọi** lỗi (kể cả không phải PG) thành Error mới `code=unknown`                          | Giữ đúng hành vi `mappedLineWrite` đã qua FULL gate BE-4 (MEDIUM-2): đường ghi tiền không có nhánh «ném lại nguyên lỗi». Đổi thành mất stack gốc cho bug lập trình — chấp nhận, filter vẫn log stack của Error mới (điểm ném)                                                                                                                                                                      |
| Q-5 | `Logger.error` ở 071 **không** đính `err.stack`                                                             | Filter đã log stack của 5xx (không log hai lần); stack của lỗi exceljs không đảm bảo sạch PII                                                                                                                                                                                                                                                                                                      |

## 2. File

| File                                                         | Đổi                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `apps/api/src/payroll/payroll-pair-holders.reader.ts`        | export `pairHoldersQuery` · `holderRowsToIds`; vế `u.status = 'active'`                                                                                                                                                                                                                                                                          |
| `apps/api/src/payroll/payroll-approver.reader.ts`            | thân query ⇒ `pairHoldersQuery(companyId, PAIR, exclude)` (bỏ bản sao SQL)                                                                                                                                                                                                                                                                       |
| `apps/api/src/payroll/payroll-advances.repository.ts`        | `PayrollAdvanceOwnFilter` · `ownCond` · `listOwnTx` · `countOwnTx`; `filterCond` `!== undefined`                                                                                                                                                                                                                                                 |
| `apps/api/src/payroll/payroll-advances.service.ts`           | 4 × `mappedWrite("payroll_advances")`; `listMine` ⇒ Own hàm riêng; bỏ import `mapPayrollPgError`                                                                                                                                                                                                                                                 |
| `apps/api/src/payroll/payroll-pg-write.util.ts` (MỚI)        | `mappedWrite(table, fn)` + `triggerTagOf`                                                                                                                                                                                                                                                                                                        |
| `apps/api/src/payroll/payroll-payment-batches.service.ts`    | `mappedLineWrite` = delegate; bỏ import `pgErrorCode/pgErrorField`                                                                                                                                                                                                                                                                               |
| `apps/api/src/payroll/payroll-import.parser.ts`              | `tooLarge()` factory; tiền-kiểm `rowCount`                                                                                                                                                                                                                                                                                                       |
| `apps/api/src/payroll/payroll-adjustments-import.service.ts` | dùng `PayrollImportParser.tooLarge()`; bỏ import hằng MAX_ROWS                                                                                                                                                                                                                                                                                   |
| `apps/api/src/payroll/payroll-payment-export.service.ts`     | `buildUncBuffer` + `Logger.error` khi ném sau audit                                                                                                                                                                                                                                                                                              |
| `apps/api/src/payroll/payroll-payment-batches.repository.ts` | `insertLinesTx` + `EXISTS` đợt                                                                                                                                                                                                                                                                                                                   |
| `packages/contracts/src/payroll-disbursement.ts`             | `.strict()` × 4 (063 · 064 · 072 · 059)                                                                                                                                                                                                                                                                                                          |
| Test mới                                                     | `payroll-advances.repository.spec.ts` · `payroll-import.parser.spec.ts` · `payroll-pg-write.util.spec.ts` · +describe `payroll-payment-export.service.spec.ts` · `packages/contracts/src/payroll-disbursement.spec.ts` · +describe `test/integration/s15-payroll-be4-batches.int-spec.ts` (3 ca) · `s15-payroll-be4-advances.int-spec.ts` (3 ca) |

## 3. Bằng chứng (16/09/2026 — lane `mediaos_be4b`)

| Cổng                                                                                                                                               | Kết quả                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RED trước khi vá                                                                                                                                   | unit: 5 ca đỏ đúng chỗ (Own-guard ×3 `listOwnTx is not a function` · parser `expected null to be instance of UnprocessableEntityException` · export `buildUncBuffer does not exist`) + module `payroll-pg-write.util` chưa có · contracts 5/5 đỏ (`expected true to be false`) · int-spec **6/6 ca mới đỏ** đúng lý do (admin2 đình chỉ vẫn trong recipients `[…(3)]` · warnings `[]` thay vì `no-eligible-approver` · 063 khoá lạ ⇒ 201 · C3b ⇒ 201 · NOTI-027 recipients 2 · insertLinesTx batch ma ⇒ 23514 thô), 42 ca cũ vẫn xanh |
| GREEN sau vá — unit `src/payroll` (không DB)                                                                                                       | **19 file · 335 test XANH**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| GREEN — contracts                                                                                                                                  | `payroll-disbursement.spec.ts` 5/5                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| GREEN — int-spec lane (`be4-batches` · `be4-advances` · `be4-budgets-import` · `payroll-be2-lifecycle` — 017 ở `submit` đi qua `pairHoldersQuery`) | **4 file · 80 test XANH**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `bash harness/check.sh --quick`                                                                                                                    | secret-literals · lint · typecheck · migration-no-drop · tooling XANH (một vòng đỏ lint do `\b` trong regex bị shell nuốt thành `\x08` — vá spec, không đụng code)                                                                                                                                                                                                                                                                                                                                                                    |
| `test:cov:payroll` (lane)                                                                                                                          | **40 file · 927 test XANH** · coverage `src/payroll/**` **94,37 % stmts · 86,43 % branch · 97,04 % funcs** (BE-4: 94,25 / 86,01 / 97 — không giảm)                                                                                                                                                                                                                                                                                                                                                                                    |
| `typescript-reviewer` (LIGHT, tĩnh)                                                                                                                | **PASS** — 0 CRITICAL/HIGH · 1 MEDIUM (docstring 065 ở `payroll-advances.service.ts` còn ghi `listTx({userId})` ⇒ ĐÃ SỬA) · 1 LOW (`s15-payroll-be4-batches.int-spec.ts` 945 dòng > 800, tồn từ BE-4 — nợ §4). Xác minh riêng: không client FE nào gọi 059/063/064/072 (FE-3 chưa build) ⇒ `.strict()` không 400 oan; test mới không giả xanh                                                                                                                                                                                         |
| `security-reviewer` HẸP 1 câu (vế `users.status`)                                                                                                  | **PASS** — (a) CHECK thật có BỐN giá trị `active                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | invited | suspended | locked`(mig 0002 + 0450); cổng đăng nhập allow-list`isAuthorizedStatus`(login/refresh/2FA) ⇒ reader KHỚP,`invited`/`locked`tự rớt · (b) bỏ`locked_at`(đi cặp`status=locked`) · `must_change_password`·`require_two_factor`(cổng phiên) ·`companies.status`(login đã chặn) là ĐÚNG — lý do ĐÃ ghi vào comment reader · (c) tenant sạch (3 vế`company_id`, 3 caller trong `withTenant`, chỉ trả `user_id`). 1 MEDIUM ⇒ thêm ca **D1b** `payroll-be2-lifecycle`(approver duy nhất đình chỉ ⇒ officer submit 422 017, kỳ ở nguyên Calculated; kích hoạt lại ⇒ 201 Reviewing) · 2 LOW comment ⇒ ĐÃ sửa. Ngoài paths WO, để lại:`auth.service.ts:90-91` comment cũng ghi sai state-space |
| `bash harness/check.sh --lane-db=be4b` (đầy đủ, chunked)                                                                                           | **XANH** — secret-literals · lint · typecheck · migration-no-drop · tooling · test: api 709/709 file (7 lần chạy lại crash hạ tầng, 0 test đỏ) · app 275/275 · contracts 40/40 · web-core 45/45 · ui 24/24 · console 22/22 · auth 4/4. (`--all` = build/prod-tenant/db-readiness KHÔNG chạy ở phiên này — WO không đụng migration/build; CI build là cổng cuối)                                                                                                                                                                       |
| Đột biến (memory: đo cổng cần VI PHẠM thật)                                                                                                        | Bỏ vế `and u.status = 'active'` ⇒ **ĐÚNG 3 ca đỏ** (D1b · 060 «admin2 đình chỉ vẫn trong recipients» · 060 «người duyệt duy nhất đình chỉ»), 43 ca còn lại xanh; khôi phục `cmp` byte-giống · chạy lại `payroll-be2-lifecycle` 21/21 (D1b xanh)                                                                                                                                                                                                                                                                                       |

## 4. Nợ để lại có ý thức

- **QA-1** (kế thừa BE-4): đột biến (k) NOTI-027 dedupe theo `batchId` không đo được bằng int-spec (producer enqueue một lần/kỳ).
- `.strict()` trên query 059 là tiền lệ đầu tiên của repo cho schema query — nếu FE-3 cần tham số phụ (sort/cột), phải THÊM vào schema, không nới `.strict()`.
- `sheet.rowCount` bảo thủ (Q-3): không có ca «5.000 dòng rải tới hàng 9.000» — cố ý.
- `s15-payroll-be4-batches.int-spec.ts` 945 dòng (> 800; tồn từ BE-4) — tách theo describe khi QA-1 mở.
- `auth.service.ts:90-91` comment ghi CHECK `active|suspended` trong khi thật là 4 giá trị (mig 0450) — ngoài paths WO này, sửa ở WO AUTH kế tiếp.
