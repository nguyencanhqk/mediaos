# Kế hoạch (v2 — TOÀN DIỆN): App quản lý dự án parity-với-Plane `apps/projects` trên backend MediaOS dùng chung

## Context
Bạn muốn một công cụ PM **đầy đủ tính năng như Plane/Linear/Jira** — không phải lát cắt 4 tính năng như v1. Task/dự án trong `apps/studio` quá đơn giản, không còn phù hợp.
Quyết định đã chốt: **(1)** app frontend độc lập mới `apps/projects` (Vite/React SPA, port 5279), lấy cảm hứng Plane, KHÔNG dùng code Plane (né AGPL); **(2)** **dùng chung backend** `apps/api` — mở rộng domain `tasks`/`projects` sẵn có (`task`=work item, `project`=project, `company`=workspace), tái dùng RLS/RBAC/audit/comments/attachments/outbox/scheduler/`@mediaos/ui`/`@mediaos/web-core`; **(3)** mục tiêu nghiệm thu mỗi phase: **chạy local demo end-to-end**.

Kế hoạch này dựa trên **đọc trọn 116 trang docs.plane.so** (workflow 63 agents, sitemap đầy đủ, không có llms.txt) + gap-map từng tính năng vs MediaOS. 1 company = 1 workspace (single-tenant qua SSO `company_id`; KHÔNG có workspace switcher/billing).

## 0. Quyết định đã chốt (2026-06-18)
1. **Data: mở rộng `tasks` ADDITIVE, giữ compat.** Thêm cột `state_id`/`work_item_type_id`/`priority`/`parent_id`/`cycle_id`/`sequence`/`description`/`start_date`/`estimate_value_id`; GIỮ `status`/`task_type` CHECK cũ chạy; lớp map PM-state↔status legacy cho task do FSM studio sinh. PM & studio CHIA SẺ chung work item. KHÔNG big-bang, KHÔNG domain tách rời.
2. **Tham vọng: FULL parity**, build từng phase 1→5, mỗi phase ra demo chạy được + checkpoint để bạn review trước khi sang phase sau.
3. **Phase 1 = như plan** (States tùy biến + Priority + Labels + Description Tiptap + Board/List + chi tiết + members + seed guest).

---

## 1. Kiểm kê đầy đủ tính năng Plane (catalogue)
**A. Workspaces & settings** — lifecycle/slug/logo, members (invite/CSV/role/remove/leave/activity), pending invites, role matrix (Owner/Admin/Member/Guest) + **custom roles & permission schemes** (paid), billing/seats (Stripe), security/SSO/2FA, exports, webhooks, API tokens, **Teamspaces** (nhóm members+projects), integrations.
**B. Projects** — tạo (name/identifier 3–5 ký tự/access public-private/lead), display ID `{IDENT}-{seq}`, settings hub (General/Members/Features/States/Labels/Estimates/Automations), timezone, **features toggle** per-project (Cycles/Modules/Views/Pages/Intake/Time), **work-item states** (5 nhóm Backlog/Unstarted/Started/Completed/Cancelled, tùy biến/reorder/default), project lifecycle states (Pro), members+role, automations (auto-close/archive), archiving.
**C. Work Items** — tạo (modal/inline quick-add/`N>I`/drafts auto-save), thuộc tính: Title, **rich-text description + edit history**, State, **Priority** (Urgent/High/Medium/Low/None), **multi-assignee**, **Labels**, Start/Due date, **Parent**, **Estimate**, **Cycle**, **Module**, custom props; attachments, links, **sub-items**, **relations & dependencies** (Blocking/Blocked-by, FS/SS/FF, Relates/Duplicate/Implements), comments, **activity log**, subscribe, copy-link, **multi-select bulk ops**.
**D. Work Item Types & Custom Properties** — types per-project (Bug/Story/… seed Task lvl0+Epic lvl1, color/icon/default), **custom properties** (Text/Number/Dropdown/Boolean/Date/Member/URL, mandatory/read-only/default), workspace types + type-hierarchy (Enterprise).
**E. Sub-items & Relations** — cha/con cross-project, breadcrumb+siblings, hierarchy enforce, relations bidirectional, blocking, timeline deps (auto-reschedule + red-line).
**F. Estimates** — system per-project (Points: Linear/Fib/Squares/Custom · Categories: T-shirt · Time-Pro), switch archives cũ, estimate as display property, burndown theo points, dashboard sum-of-points.
**G. Cycles (sprint)** — tạo (start/due, `Q`), lifecycle active/upcoming/completed (start/end manual, 1 active/project), assign item (1 cycle/item), **transfer incomplete**, progress + burn-down/build-up, parallel (Enterprise), auto-schedule (Business), active-cycles cross-project.
**H. Modules** — nhóm time-boxed trong project (`M`), lead+members, start/target, state 6-giá-trị, progress/pipeline/attention, link items M:N.
**I. Views** — saved filters+sort+group+layout, private/public/favorite/default; **PQL** (query DSL, AND/OR, function ops, save as view).
**J. Pages/Wiki** — rich-text nested, editor blocks, inline comments, embed work items, versions, access, AI (Pro), collections/report pages.
**K. Intake/Inbox** — triage queue (in-app/email/forms), accept/decline/snooze/duplicate, convert→work item; **Customers & customer-requests** (CRM-lite).
**L. Time tracking / Worklogs** (Pro) — log time/item/day, estimate-vs-actual.
**M. Initiatives & Portfolios** (Business) — gom projects/epics roll-up.
**N. Layouts & cross-cutting** — List/Board/Calendar/Spreadsheet/Gantt-Timeline; group-by/sub-group/order-by/filter/display-options; peek slide-over + detail page.
**O. Khác** — notifications inbox + subscribers + project-updates; global search/`Cmd-K`; dashboards & widgets; **Workflows & Approvals** (transition governance + approval gates); **templates** (project/work-item/page); **recurring work items**; importers (Jira/Asana/ClickUp/Confluence/Notion/Linear/GitHub/CSV/Flatfile) + export; integrations (GitHub/GitLab/Slack/Sentry/Cursor/draw.io); **Plane AI (Pi)** + MCP; **mobile app**; stickies; keyboard shortcuts.

---

## 2. Gap vs MediaOS (status · effort S/M/L/XL · pri must/should/could/wont)
**Workspaces/Members:** members invite+CSV+role+leave (partial→**must**,L: +`invites` table) · pending invites (new,should) · role matrix→map company-admin=Owner/PM=PM/employee=Member + seed **guest** (partial,**must**) · transfer-ownership (new,M) · security/SSO/2FA (have,deep-link console) · webhooks/API-tokens/exports/teamspaces (have/partial, should/could) · **billing/seats/integrations/Stripe (wont)**.
**Projects:** creation +identifier/access/tz/lead/icon (partial→**must**,L) · **identifier+sequence displayId** (new,**must**,M — counter `FOR UPDATE`) · access public/private (new,should,L) · settings hub (partial,**must**) · features toggle (new,should) · **states & state-groups** (new,**must**,XL — thay `tasks.status` CHECK) · members+role enum (partial,**must**) · automations (partial,could) · archiving (have,**must**,S).
**Work Items:** creation widen office-guard (partial,**must**) · **priority** (add col,**must**,S) · **labels**+join (new,**must**,M) · **description+Tiptap** (new,**must**,M) · attachments (have FE-only,**must**) · comments (have FE-only,**must**) · links (new,should) · **sub-items parent_id** (new,**must**,M) · **relations/blocking** `task_relations` (new,**must**,L) · activity log projection (partial,should) · subscribe `task_subscriptions` (partial,should) · multi-select bulk (new,should) · multi-assignee (defer v1 → `task_assignees` sau).
**Types/Custom props:** `work_item_types` + seed 8 system types (partial,**must**,L) · **custom_properties EAV** (new,**must**,XL) · property types (new,**must**,M) · epics (new,should) · workspace-types/hierarchy (could/**wont**).
**Estimates:** `estimate_systems`+`estimate_values` (new,should,L) · points presets (should) · as-display-property (partial,should) · time-type (**wont**) · burndown-by-points (could, blocked-on-cycles).
**Cycles:** `cycles`+module (new,**must**,L) · lifecycle start/end (new,**must**) · `tasks.cycle_id` (partial,**must**) · transfer (new,should) · progress+`cycle_progress_snapshots` daily job (partial,should,L) · active cross-project (new,should) · auto-schedule (**wont**) · parallel (could).
**Modules:** full cluster (new,**must**,M–L) · `module_members`+`module_tasks` join.
**Views/Pages/Intake/Time:** saved Views (new,should,M) · Pages+Tiptap+versions (new,could,L) · Intake (new,could,M) · Worklogs (new,could,M) · PQL (new,could).
**Layouts:** List (reuse DataTable,**must**) · Board (partial,**must**) · Calendar (new,should) · Spreadsheet (partial,could) · **Gantt/Timeline** (new,should,XL) · peek/detail (new,**must**).
**Khác:** notifications inbox (partial,should) · Cmd-K (new,could) · dashboards/widgets (partial,could) · workflows&approvals (partial — đã có FSM/approval-request, could) · templates (partial — `workspace_templates`, could) · recurring (new,could) · importers Jira/Asana (**wont**, CSV could) · integrations Slack/GitHub (**wont**) · Plane AI/Pi (**wont**) · mobile (**wont** — `apps/mobile` riêng) · customers/initiatives (**wont**).

---

## 3. Data model mới (quy ước: mọi bảng có `company_id` FK + default GUC, RLS+FORCE+policy tenant, index company-leading, GRANT mediaos_app, soft-delete; audit trong `withTenant` tx)
**Cột thêm `tasks` (GOM 1 migration, 1 lane sở hữu):** `priority` CHECK(urgent|high|medium|low|none) · `start_date` · `description` (text/jsonb sanitized) · `parent_id` self-FK SET NULL (cycle-guard) · `estimate_value_id` FK · `state_id` FK `project_states` (nullable→backfill, **giữ `status` CHECK trong cutover**) · `work_item_type_id` FK (backfill 8 system types, nới `task_type` CHECK) · `cycle_id` FK SET NULL · `sequence` int (counter/project → displayId).
**Cột thêm `projects`:** `identifier` (+partial-unique upper) · `access` enum · `timezone` · `lead_user_id` · `icon_emoji`/`cover_image_url` · `default_assignee_user_id` · `features` jsonb · `modules_enabled`.
**Bảng mới:** `invites` · `project_states`(group enum 5) · `company_project_states` · `project_automations` · `labels` + `task_labels` · `task_links` · `task_relations`(type CHECK, unique, no-self-loop) · `task_subscriptions` · `work_item_types` · `custom_properties` + `property_options` + `task_property_values`(EAV jsonb) · `estimate_systems` + `estimate_values` · `cycles` + `cycle_progress_snapshots` · `modules` + `module_members` + `module_tasks` · `views` · `pages` + `page_versions` · `intake_items` · `worklogs`. (Defer/wont: `task_assignees`, `hierarchy_rules`, `cycle_auto_schedule`, `initiatives`+`initiative_projects`, `project_type_imports`.)
**Permission resource mới seed:** `cycle` `module` `label` `work_item_type` `custom_property` `view` `page` `intake` `estimate`/`project_settings` + role `guest`.

## 4. API mới (apps/api, reuse `withTenant`/`@RequirePermission`/audit-in-tx)
**Members:** `GET /companies/current` · `invites` CRUD + accept/decline + bulk-CSV · `POST /members/leave` · transfer-ownership.
**Projects:** archive/restore/join · states CRUD · `PATCH /features` · work-item-types CRUD · labels CRUD · estimates CRUD+switch · automations CRUD (members CRUD đã có).
**Work items (extend tasks):** `PATCH /tasks/:id` (general field — nới khỏi status/office) · bulk PATCH/DELETE · parent/children/siblings · type (+bulk) · links · relations · activity · subscribe · property-values · board+list filter thêm cycle/module/label/type/priority/state.
**Cycles:** CRUD + start/end/transfer/progress/active. **Modules:** CRUD + members + work-items. **Views/Pages/Intake/Worklogs:** CRUD (+versions, +convert).

## 5. Frontend `apps/projects` (clone `apps/studio` shell; **dep mới: Tiptap**)
AppShell + sidebar feature-gated theo `projects.features` + Cmd-K(could). **Project surfaces:** list (active+Archived+public Join) · create-modal (identifier auto-suggest) · settings tabs (General/Members/Features/States drag-reorder/Labels/Estimates/Types+CustomProps/Automations). **Layouts:** List(DataTable+inline quick-add) · Board(cột từ states, drag, blocked badge) · Calendar · Spreadsheet · Gantt(xyflow). Toolbar group-by/sub-group/order/filter/display-options/save-view + bulk toolbar. **Detail/peek:** slide-over + route `/projects/:id/work-items/:taskId` + copy-link; sidebar (State/Priority/Assignee/Labels/dates/Parent+breadcrumb+siblings/Estimate/Cycle/Module/custom-props); Tiptap desc + edit-history; attachments(reuse)/links/relations/sub-items; comments+activity timeline; subscribe. **Cycles:** list Active/Upcoming/Completed + detail (Recharts burn-down + list) + start/end/transfer. **Modules:** gallery progress + detail. **Views/Pages/Intake** (sau).

## 6. Lộ trình phân kỳ (mỗi phase chạy demo độc lập; gom thay-đổi shape `tasks` vào ít migration nhất, 1 lane sở hữu)
- **Phase 1 — "Feels like Plane" demo nhỏ nhất khả tín:** scaffold `apps/projects`+SSO+AppShell+nav · project tạo (**identifier+sequence displayId**)+settings(General/Members)+**archiving** · **configurable States** (`project_states` 5 nhóm — keystone, giữ `status` compat) · work item: tạo(modal+inline+`N>I`)+**Priority**+**Labels**+**Description Tiptap**+attachments(reuse)+comments(reuse) · **List+Board** (cột từ states) + peek/detail + copy-link · members invite/role/remove + seed **guest**. → demo: tạo project → workflow tùy biến → issue trên Kanban có priority/nhãn/mô tả/file/comment.
- **Phase 2 — Hierarchy/relations/types:** `parent_id`+sub-items+breadcrumb/siblings+cycle-guard · `task_relations`+blocking · `work_item_types`+seed 8+epics · **custom properties EAV**+property types · bulk ops · activity log · subscribe.
- **Phase 3 — Sprint & grouping:** **Cycles** (lifecycle/dates/`cycle_id`/transfer/progress+burn-down snapshots) · **Modules** (full) · active-cycles cross-project.
- **Phase 4 — Estimates/Views/Calendar:** estimate systems(points)+as-display+dashboard sum · saved Views+Calendar+display-options · features-toggle+timezone+public self-join · members CSV+pending-invites+transfer-ownership.
- **Phase 5 — Timeline/Pages/Intake parity hơn:** Gantt/Timeline(FS/SS/FF+`start_date`+red-line) · Pages(Tiptap+versions)+Intake+Spreadsheet+Cmd-K · automations+lifecycle-chart+exports.
- **Defer/WONT (lý do):** billing/seats/Stripe, plan-editions/tier-gating, integrations Slack/GitHub, workspace-types/hierarchy-Enterprise, auto-schedule/parallel cycles, time-tracking type, initiatives/portfolio, importers Jira/Asana (CSV=could), Plane-AI/Pi, self-serve workspace create/switcher, multi-membership/hard-delete — đều ngoài phạm vi PM-parity hoặc đụng single-tenant SSO/RLS/soft-delete invariant; engine `saas`+webhooks+outbox đã phủ phần generic.

## 7. Rủi ro chính & phần dùng lại
**Reuse as-is:** RLS+company_id+FORCE · RBAC (`@RequirePermission`+G3-4 grant/revoke, mạnh hơn matrix cố định Plane; chỉ thêm `guest`) · audit append-only (backs Activity log) · comments (BE xong, port FE) · attachments (presigned mig 0190, FE-only) · SSO web-core bootstrapSession · `@mediaos/ui` (DataTable/Dialog/Avatar/Badge/PageHeader/EmptyState/Skeleton) + Recharts + date-fns/tz + @xyflow/react · outbox+worker-scheduler (notif/webhook/snapshot/automation) · provisioning workspace_templates (giữ operator-plane).
**Rủi ro:** **(1)** `tasks` coupling FSM video-pipeline (MANUAL_TASK_TYPES=['office'], CHECK cứng) → thay `status→state_id`/`taskType→type_id` là **crown**: seed 8 system types/states back-compat, giữ CHECK cũ trong cutover, tách PM-state khỏi FSM-step, gom 1 migration 1 lane. **(2)** state-group model: map 5-nhóm↔6-giá-trị status cũ bắt buộc. **(3)** Guest vs RBAC company-wide: seed `guest` + cân nhắc `project_members.role_in_project` authoritative (effective = company-role AND project-role); per-project ACL có thể defer. **(4)** sequence allocator concurrency-safe (counter row `FOR UPDATE` trong tx, KHÔNG `max()+1`) + backfill. **(5)** EAV custom props: render form động + validate per-type + mandatory fail-fast. **(6)** Tiptap = editor đầu tiên → sanitize server-side (no SVG/script), tăng bundle+XSS surface. **(7)** broaden `PATCH /tasks/:id` cẩn thận (re-validate field vs RLS+permission+ownership; bulk validate company_id từng id 1 tx + audit). **(8)** migration-band contention: reserve band riêng, 1 lane sở hữu hot-file (`tasks` schema, `nav.ts`, `router.tsx`), rebuild `@mediaos/contracts` trước typecheck (né false-RED).

## 8. Quy ước & verify (mỗi phase)
Theo CLAUDE.md: BẤT BIẾN (withTenant/RLS, soft-delete, no-secret) · diff chạm RLS/permission/`tasks`-shape = **FULL gate** (security+database+silent-failure+santa crown) + **test deny-path RED trước** · migration band riêng append sau 0400 (journal idx 108+, when>1717500420000) · contracts=nguồn DTO. **Verify:** lane DB `bash scripts/lane-db-setup.sh projects` → `LANE_DB=mediaos_projects pnpm exec vitest run` (RLS+permission+service xanh) + typecheck api/projects → chạy api:3100+auth:5275+projects:5279(+web:5273) → Playwright (`--browser chrome`) login demo admin → thao tác từng tính năng → screenshot. Làm trên branch `feat/projects-pm-app`; chỉ commit/push khi bạn yêu cầu.
