{
  "name": "mediaos",
  "version": "0.0.0",
  "private": true,
  "description": "MediaOS — Hệ thống quản trị doanh nghiệp (modular monolith, multi-tenant, SaaS-ready)",
  "packageManager": "pnpm@11.5.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
